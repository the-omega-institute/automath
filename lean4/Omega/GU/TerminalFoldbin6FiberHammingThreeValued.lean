import Omega.Folding.BinFold

/-!
# Terminal foldbin6 fiber Hamming three-valued

The requested paper theorem for this file is intentionally left unformalized in the current
development.  Under the present `Omega.Folding.BinFold` definitions, direct evaluation of
`cBinFiberMinHamming 6` shows that the distance-`5` fibers occur at different window-`6` words
than the signature supplied for this round, so introducing the requested theorem would be
mathematically false in this repository state.

The corresponding TeX label is therefore marked `\leanpartial`.
-/

namespace Omega.GU

end Omega.GU
