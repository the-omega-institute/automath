import Mathlib.Tactic

namespace Omega.Conclusion

/-- Paper label:
`cor:conclusion-window6-coinvariant-poincare-package-generated-by-tail-measure`. -/
theorem paper_conclusion_window6_coinvariant_poincare_package_generated_by_tail_measure :
    (21 * 1 + 13 * 2 + 9 * 3 = 74) ∧
      (((2 ^ 21) * (3 ^ 13) * (4 ^ 9) : ℕ) = (2 ^ 39) * (3 ^ 13)) := by
  norm_num

end Omega.Conclusion
