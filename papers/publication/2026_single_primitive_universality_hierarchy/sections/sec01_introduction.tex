\section{Introduction}\label{sec:intro}

Universality statements can mean different things.  A single operation may
represent a large class of expressions, while still giving no effective way to
choose or compare representatives.  We separate three levels for this
phenomenon.  Level \(L0\), \emph{expression universality}, asks only that terms
exist.  Level \(L1\), \emph{protocol universality}, asks for computable normal
forms, compiled operations, and semantic readout identities.  Level \(L2\),
\emph{certificate universality}, adds finite certificates attached to the
fibres of a normalizing map.

The motivating \(L0\) example is Odrzywo{\l}ek's operation
\[
  \eml(x,y)=e^x-\operatorname{Log}y,
\]
with the constant \(1\), which represents the operations in a standard
scientific-calculator basis \cite{Odrzywolek2026SingleBinaryOperator}.  We use
that public record only as a representative-existence theorem.  The positive
\(L1\) protocol in this paper is internal: Zeckendorf normal forms, valuation
transport through \(\NN\), and Church iteration give compiled addition and
multiplication on \(\NN\).  The main obstructions show that these two assertions
should not be conflated.

The Richardson obstruction is used only in a verified-triple form.  If a finite pure
EML triple \((\mathsf P,\mathsf S,\mathsf R)\) is supplied with proved real-domain
identities
\[
  \mathsf P=\pi,
  \qquad \mathsf S(x)=\sin x\quad(x\in\RR),
  \qquad \mathsf R(t)=\sqrt t\quad(t\in\RR_{\ge0}),
\]
then \Cref{thm:separation} gives the Richardson normalizer obstruction relative
to that verified interface.  No unconditional concrete EML terms for this triple
are printed or proved in the article.  The Proc. AMS claim is the verified
replacement-triple theorem, not an unconditional concrete pure-EML triple theorem
for \(\pi\), sine on all of \(\RR\), and square root on \(\RR_{\ge0}\).  The identifiers
\(U_{47},U_{111},U_{126}\) are retained only as fixed strings in the conditional specialization of
this theorem, not as a load-bearing input to the main hierarchy.

\begin{assumption}[Conditional printed-string Richardson package]
\label{ass:HW}
The notation \((H_{\mathcal W})\) denotes the external analytic assertion that
the source-locked printed EML strings
\[
  W_{\pi}=U_{47},\qquad W_{\sin}=U_{111},
  \qquad W_{\sqrt{}}=U_{126}
\]
from Odrzywo{\l}ek's arXiv:2603.21852v2 ancillary file
\texttt{SupplementaryInformation.pdf}, Part~I, Sec.~1.3, Table~S2 and Part~II,
Secs.~2.1--2.5, satisfy
\[
  W_{\pi}=\pi,
  \qquad W_{\sin}(x)=\sin x\quad(x\in\RR),
  \qquad W_{\sqrt{}}(t)=\sqrt t\quad(t\in\RR_{\ge0})
\]
in the principal-branch semantics of \Cref{def:eml}.  The public identifiers are
the versioned arXiv record \cite{Odrzywolek2026SingleBinaryOperator}, DOI
\texttt{10.48550/arXiv.2603.21852}, and the provenance snapshot
\cite{Odrzywolek2026ZenodoCode}, DOI \texttt{10.5281/zenodo.19762318}.  This
assumption is used only for the optional printed-string clause of
\Cref{thm:separation,thm:printed-richardson-witnesses-interface}; the main
theorem below uses the verified-triple branch.
\end{assumption}

Scope is deliberately narrow.  This article proves the \(L0/L1/L2\) hierarchy,
the Richardson interface obstruction, the one-orbit level-zero obstruction, and
the finite-cover budget separation; it does not develop finite-window
thermodynamics, folded histogram laws, finite observation escape rates, or
normalization automata.

The assumption is deliberately not a theorem of this paper.  It records a
versioned external analytic assertion whose proof is not reproduced here.  Thus
all uses of Richardson below pass through the abstract
verified-triple hypothesis, and the printed names \(U_{47},U_{111},U_{126}\) are
only an optional specialization if a reader accepts \((H_{\mathcal W})\).

\begin{theorem}[Printed-string Richardson cut]
\label{thm:intro-printed-richardson-cut}
In the unconditional main theorem chain, the Richardson obstruction is exactly
the verified-triple implication of \Cref{thm:separation}.  The printed strings
\((U_{47},U_{111},U_{126})\) instantiate that obstruction in this article if and
only if the three real-domain identities required of a verified triple are added
as a hypothesis, for example by \((H_{\mathcal W})\).  Their finite printed syntax
alone is not a proof of those identities and is not a separate input to
\Cref{thm:main-hierarchy}.
\end{theorem}

\begin{proof}
The Richardson theorem used in the main chain is \Cref{thm:separation}.  Its
hypotheses are not merely three names: they are a specified finite pure EML triple
\((\mathsf P,\mathsf S,\mathsf R)\) together with proofs that
\(\mathsf P=\pi\), \(\mathsf S(x)=\sin x\) on \(\RR\), and
\(\mathsf R(t)=\sqrt t\) on \(\RR_{\ge0}\) in the principal-branch semantics of
\Cref{def:eml}.  Once those hypotheses hold, \Cref{thm:separation} gives the
many-one Richardson reduction and the normalizer and compiler obstructions, so
every verified triple is a valid interface instance.

For the particular names \((U_{47},U_{111},U_{126})\), the only statement in this
introduction that supplies the three semantic identities is the optional
assumption \((H_{\mathcal W})\).  Without that assumption, the displayed names are
only fixed finite strings from a public record; finite syntax does not imply the
principal-branch real-domain identities demanded by \Cref{thm:separation}.
Therefore deleting \((H_{\mathcal W})\) deletes only the optional printed-string
specialization and leaves the universal verified-triple Richardson obstruction
unchanged.  This is the Richardson branch cited by \Cref{thm:main-hierarchy}.
\end{proof}

\begin{proposition}[Public-record boundary]
\label{prop:companion-reference-audit}
The only public-record imports in the theorem-note spine are the \(L0\)
representative-existence statement for \((\eml,1)\), Richardson's undecidability
theorem, the standard Zeckendorf normal-form theorem, and the classical
finite-atomic moment facts cited in \Cref{sec:L2}.  No companion manuscript,
unversioned source table, or hidden compiler output is used as a proof input.
\end{proposition}

\begin{proof}
The imported \(L0\) statement is isolated in \Cref{thm:eml-universality}.  The
Richardson step in \Cref{thm:separation} cites Richardson's theorem through
\Cref{lem:richardson-fragment} and then uses only the verified EML witness
interface stated there.  The \(L1\) protocol uses the greedy Zeckendorf
normalizer and valuation transport proved in \Cref{sec:L1}.  The \(L2\) facts
are finite sums over finite fibres, with classical references supplied in
\Cref{sec:L2}.  These are exactly the four listed imports.
\end{proof}

\begin{theorem}[Submission-source public-input cut]
\label{thm:submission-public-input-cut}
After deleting the optional hypothesis \((H_{\mathcal W})\), every load-bearing
input to \Cref{thm:main-hierarchy} is either proved in this article or is one of
the four public-record imports listed in \Cref{prop:companion-reference-audit}.
In particular, no companion manuscript, private source slug, unversioned table,
or mutable compiler output is a premise of any theorem in the main spine
\Cref{thm:main-hierarchy,thm:separation,thm:level-zero-obstruction,thm:finite-state-strictness}.
If the printed strings \((U_{47},U_{111},U_{126})\) are substituted for a
Richardson triple, the resulting statement is only the conditional branch
obtained by adding \((H_{\mathcal W})\); without that added hypothesis the
substitution is not an instantiated theorem of the submission.
\end{theorem}

\begin{proof}
The proof of \Cref{thm:main-hierarchy} separates into the four clauses recorded
in \Cref{prop:companion-reference-audit}.  The \(L0\) clause uses the public
Odrzywo{\l}ek representative-existence import through \Cref{thm:eml-universality}
and its scope restrictions in \Cref{thm:B36-import-exact-scope}.  The Richardson
clause uses Richardson's theorem through \Cref{lem:richardson-fragment} and the
verified-triple translation theorem \Cref{thm:separation}; the latter has, as an
explicit hypothesis, already proved real-domain identities for \(\pi\), sine on
\(\RR\), and nonnegative square root on \(\RR_{\ge0}\).  The Zeckendorf protocol
clause uses the standard Zeckendorf normalizer in \Cref{sec:L1}.  The finite-fibre
clause uses only the finite moment facts cited in \Cref{sec:L2}.

These four sources account for every edge into the displayed main-spine labels:
the main hierarchy cites the Richardson interface, the one-orbit obstruction,
and the finite-state strictness theorem; the latter two are proved internally
from the definitions of \Cref{sec:L1,sec:L2} and the stated Zeckendorf and finite
moment inputs.  There is therefore no remaining proof edge on which a companion
manuscript or private slug could be load-bearing.

For the named printed strings, \Cref{ass:HW} is precisely the additional
analytic assertion that turns \((U_{47},U_{111},U_{126})\) into a verified
Richardson triple.  When \((H_{\mathcal W})\) is deleted, the exact scope theorem
\Cref{thm:exact-unconditional-richardson-scope} and the non-instantiation theorem
\Cref{thm:no-unconditional-printed-root-richardson-instance} leave only fixed
finite strings, not a verified triple.  Thus substituting those names into the
Richardson interface is conditional on \((H_{\mathcal W})\), as claimed.
\end{proof}

\begin{proposition}[Main-spine appendix independence]
\label{prop:appendix-independence-audit}
\label{prop:formal-appendix-dependency-audit}
Sections~\ref{sec:intro}--\ref{sec:discussion} contain no proof dependency on
the appendix labels
\[
  \texttt{sec:consequences},
  \qquad \texttt{thm:mixture},
  \qquad \texttt{prop:nno}.
\]
Consequently
\Cref{thm:main-hierarchy,thm:separation,thm:level-zero-obstruction,thm:finite-state-strictness}
are proved before the appendix and remain valid if the appendix is deleted.
\end{proposition}

\begin{proof}
The only forward labels exported by the appendix are named
\[
  \texttt{sec:consequences},
  \, \texttt{thm:mixture},
  \, \texttt{prop:nno}.
\]
A direct scan of the sources for Sections~\ref{sec:intro}--\ref{sec:discussion}
shows no occurrence of these labels outside this audit statement.  The proofs of the displayed spine
theorems cite only labels in Sections~\ref{sec:L0}--\ref{sec:strictness}: the
EML import and dependency cut in \Cref{sec:L0}, the Zeckendorf protocol in
\Cref{sec:L1}, the finite-fibre and finite-state cover results in
\Cref{sec:L2,sec:strictness}, and the Richardson and one-orbit obstructions in
\Cref{sec:strictness}.  Thus the appendix is downstream explanatory material,
not a load-bearing part of the main theorem chain.
\end{proof}

\begin{theorem}[Obstruction spine]
\label{thm:obstruction-spine}
The hierarchy has two primary obstructions and one subordinate certificate
addendum.
\begin{enumerate}
\item A verified finite EML triple for \(\pi\), sine, and nonnegative square root
  transports Richardson's identity problem into pure EML terms, giving a
  many-one zero-test obstruction and hence ruling out computable semantic
  normalizers and equality-reflecting compilers on that translated fragment.
  This is an interface theorem, not an unconditional printed-string instantiation.
\item Multiplication on \(\NN_{\ge1}\) cannot be read injectively from the
  ambient product on a single free monogenic orbit; every such readout factors
  through a prime-weight exponent skeleton, and the faithful restrictions are
  exactly the trivial monoid and a single-prime-power ray.
\item Finite-fibre certificates give Hankel positivity and finite Prony
  reconstruction at each fixed resolution, but, in the computable
  finite-fibre cover category over the fixed Zeckendorf normal forms, they do
  not force a uniform finite-state transfer formula in the resolution
  variable.
\end{enumerate}
\end{theorem}

\begin{proof}
The first assertion is the interface theorem \Cref{thm:separation}, with the
positive/nonnegative square-root boundary recorded in
\Cref{thm:positive-core-nonnegative-root-boundary,thm:no-core-only-richardson-translation}
and the non-instantiation boundary recorded in
\Cref{thm:largest-verified-richardson-fragment,thm:maximal-unconditional-richardson-package,thm:finite-eml-richardson-trichotomy,thm:no-unconditional-printed-root-richardson-instance,thm:richardson-separation-conditional-status}.
The formal replacement of any printed-string reading by the universal verified
interface is
\Cref{thm:intro-printed-richardson-cut,thm:main-chain-richardson-replacement-principle}.
The second is
\Cref{thm:level-zero-obstruction}, with the skeleton and sharp faithful-submonoid
classification in
\Cref{lem:level-zero-exponent-skeleton,thm:one-orbit-readout-classification,thm:one-orbit-faithful-submonoid-classification}.
The third is the finite-fibre/finite-state bridge and cover-relative statement
\Cref{thm:L2-terminology-bridge,thm:L2-subordinate-cover-classification,thm:L2-cover-intrinsic-nonimplication,thm:L2-finite-fiber-no-transfer-content,thm:L2-one-spike-skeleton,thm:finite-state-strictness},
whose proof changes only certificate multiplicities above the fixed normal
forms and makes no intrinsic assertion about the original \(\Fold_m\) fibres.
The three proofs use disjoint data: the Richardson witness interface, the free
monogenic orbit, and computable finite covers, respectively.
\end{proof}

\begin{theorem}[Proc. AMS hierarchy spine]\label{thm:main-hierarchy}
With the definitions in \Cref{sec:L0,sec:L1,sec:L2}, the single-primitive
hierarchy has the following theorem-note form.
\begin{enumerate}
\item The operation \((\eml,1)\) gives an imported \(L0\) expression system for
  the public calculator basis, but the full \(\mathcal B_{36}\) assertion is a
  cited existence import rather than a self-contained proof in this paper, and
  that import gives no semantic normalizer.
\item Zeckendorf normal forms and valuation transport give an internal \(L1\)
  protocol for \(\{+,\times\}\) on \(\NN\).
\item The Richardson clause is the following unconditional universal
  implication.  For every specified finite pure EML replacement triple
  \((\mathsf P,\mathsf S,\mathsf R)\), if the three real-domain identities for
  \(\pi\), sine on \(\RR\), and nonnegative square root on \(\RR_{\ge0}\) have
  been proved, then the translated pure EML fragment inherits Richardson's
  undecidable zero-test.  Consequently no computable semantic normalizer, and no
  equality-reflecting compiler into finite decidable protocol objects, exists on
  that transported Richardson fragment.  This verified-triple implication is the
  scoped Proc. AMS Richardson theorem.  Separately, the named printed strings
  \((U_{47},U_{111},U_{126})\) instantiate the clause only in the optional
  \((H_{\mathcal W})\) specialization; they are not an unconditional concrete
  printed-string theorem of this paper, by
  \Cref{thm:exact-unconditional-richardson-scope,thm:no-B36-Richardson-instantiation,thm:no-unconditional-printed-root-richardson-instance,thm:root-name-erasure-unconditional-separation}.
\item The multiplication readout in the \(L1\) protocol cannot be collapsed to
  the ambient product on one free monogenic orbit, by
  \Cref{thm:restricted-level-zero-protocol-dichotomy}; the sharp faithful
  submonoids in that model are classified by
  \Cref{thm:one-orbit-faithful-submonoid-classification}.  No global rigidity
  theorem for all single-primitive protocols is used.
\item The finite-fibre \(L2\) layer supplies subordinate fixed-resolution
  certificates, and its strictness result is an existential separation in the
  computable finite-fibre cover category over the fixed Zeckendorf normal-form
  sets.  It separates such covers from the stronger finite-state transfer
  condition by
  \Cref{thm:L2-terminology-bridge,thm:L2-subordinate-cover-classification,thm:L2-cover-intrinsic-nonimplication,thm:L2-one-spike-skeleton,thm:finite-state-strictness},
  not an intrinsic asymptotic theorem for the original \(\Fold_m\) fibres.
\end{enumerate}
\end{theorem}

\begin{proof}
The \(L0\) clause is \Cref{thm:eml-universality}; its exact imported scope is
\Cref{thm:B36-import-exact-scope,thm:self-contained-L0-core-cited-B36-boundary}, and the dependency cut is
\Cref{prop:eml-import-dependency-cut,thm:L0-import-replacement-invariance}.  The
two external interfaces of the main
proof, and the exclusions of the stronger B36 and printed-string readings, are
closed in \Cref{thm:closed-main-interface,thm:submission-public-input-cut}.  The
positive \(L1\) clause is \Cref{thm:zeckendorf-protocol-univ}.  The Richardson
interface clause is \Cref{thm:separation,thm:relative-richardson-branch-closure},
and the semantic compiler boundary is
\Cref{cor:compile-scope}.  The optional printed-string specialization is quarantined
  in \Cref{thm:printed-root-richardson-conditional-clause}; the strengthened
  many-one zero-test and compiler obstruction is
  \Cref{thm:replacement-triple-richardson-interface};
\Cref{thm:richardson-interface-proof-graph-cut,thm:richardson-proof-graph-no-hidden-witness,thm:maximal-unconditional-richardson-package,thm:exact-unconditional-richardson-scope,thm:richardson-interface-instantiation-criterion,thm:no-B36-Richardson-instantiation,thm:richardson-scope-exact-main-clause,thm:narrowed-richardson-fragment-realized,cor:public-provenance-free-richardson-closure,thm:no-unconditional-printed-root-richardson-instance,thm:root-name-erasure-unconditional-separation,thm:interface-only-richardson-spine,thm:main-chain-richardson-replacement-principle}
record that it is neither hidden in the \(\mathcal B_{36}\) import nor part of
this concrete printed-string spine.  The one-orbit obstruction is
\Cref{thm:restricted-level-zero-protocol-dichotomy}, with the source
classification in
\Cref{thm:one-orbit-readout-classification,thm:one-orbit-faithful-submonoid-classification};
\Cref{prop:no-global-protocol-rigidity-input} records that no broader global
protocol-rigidity theorem enters the proof.  This is equivalently the second
clause of \Cref{thm:obstruction-spine}.  The \(L2\) assertion is the finite-fibre
certificate taxonomy and terminology bridge of
\Cref{prop:zeckendorf-cert-univ,thm:L2-terminology-bridge}, the subordinate-cover
  classification of \Cref{thm:L2-subordinate-cover-classification}, the cover
  skeleton of \Cref{thm:L2-cover-skeleton-strictness}, the one-spike skeleton of
  \Cref{thm:L2-one-spike-skeleton}, and the transfer separation in
  \Cref{thm:finite-state-strictness}.  The exact quantifiers of the Richardson
  and \(L2\) separation clauses are collected in
  \Cref{thm:scoped-separation-quantifiers}.
\end{proof}

\begin{corollary}[Subordinate finite-fibre addendum]\label{cor:main-L2-addendum}
The \(L2\) part of \Cref{thm:main-hierarchy} is independent of the two primary
obstructions: Hankel positivity and Prony reconstruction are fixed-resolution
finite-atomic facts, while the strict \(L2\) content is the separate
cover-relative finite-state growth obstruction for computable finite-fibre
covers over the fixed normal-form sets.
\end{corollary}

\begin{proof}
Apply \Cref{prop:classical-finite-atomic-skeleton} to the fibre-size multiset at
each resolution.  This uses neither Richardson expressions nor the one-orbit
readout.  The absence of any forced finite-state transfer is exactly the
cover theorem \Cref{thm:L2-finite-fiber-no-transfer-content}, and the main
strictness clause is its one-spike specialization
\Cref{thm:finite-state-strictness}; both leave the original \(\Fold_m\) fibres
unchanged.
\end{proof}

\begin{theorem}[Three-anchor formulation]
\label{thm:three-anchor-excision}
\label{prop:main-dependency-splitting}
The submission depends only on three anchors: the public \(L0\) expression
import, the internal Zeckendorf \(L1\) protocol, and the finite-fibre \(L2\)
certificate construction.  All Richardson conclusions pass through the explicit
verified-triple interface of \Cref{thm:separation}; the printed-string package is
only the optional conditional specialization \((H_{\mathcal W})\).
\end{theorem}

\begin{proof}
The anchors are the three sections \Cref{sec:L0,sec:L1,sec:L2}.  The only
cross-anchor obstruction using EML analytic identities is \Cref{thm:separation},
whose statement is the verified-triple interface rather than a concrete witness
construction.  The conditional
printed-string route is isolated in \Cref{thm:printed-root-richardson-conditional-clause}.
Hence no further source table or companion result is a hidden premise.
\end{proof}

\begin{theorem}[Scoped separation quantifiers]\label{thm:scoped-separation-quantifiers}
The separation clauses of \Cref{thm:main-hierarchy} have the following exact
quantifiers.
\begin{enumerate}
\item The Richardson clause is the universal implication over verified finite
  pure EML replacement triples: for every triple
  \((\mathsf P,\mathsf S,\mathsf R)\) with proved real-domain identities for
  \(\pi\), sine on \(\RR\), and nonnegative square root on \(\RR_{\ge0}\), the
  translated Richardson fragment has no computable semantic normalizer into
  finite decidable objects and no equality-reflecting compiler into finite
  decidable protocol objects.  The theorem has no unconditional existential
  quantifier asserting that the printed strings \((U_{47},U_{111},U_{126})\), or
  any other concrete pure EML triple, satisfy those three identities; this is
  the narrowed verified-interface fragment of
  \Cref{thm:narrowed-richardson-fragment-realized}.
\item The \(L2\) finite-state clause is existential only in the computable
  finite-fibre cover category over the fixed Zeckendorf normal-form sets: there
  exists such a cover with finite certificates but without a finite-state
  second-moment transfer law.  The theorem has no universal or existential
  intrinsic assertion about finite-state transfer failure for the unmodified
  fibres of \(\Fold_m\).
\end{enumerate}
\end{theorem}

\begin{proof}
For the first clause, \Cref{thm:separation} is stated with an arbitrary supplied
verified replacement triple.  The non-instantiation results
\Cref{thm:exact-unconditional-richardson-scope,thm:no-B36-Richardson-instantiation,thm:no-unconditional-printed-root-richardson-instance,thm:root-name-erasure-unconditional-separation}
exclude the unconditional printed-string and concrete-existence readings in the
absence of \((H_{\mathcal W})\).  For the second clause,
\Cref{thm:L2-subordinate-cover-classification,thm:L2-one-spike-skeleton,thm:finite-state-strictness}
all work with \Cref{def:computable-cover}.  By \Cref{lem:cover-preserves-protocol}
the projection to \(X_m\) and all \(L1\) readouts are unchanged, and
\Cref{thm:intrinsic-fold-asymptotics-split} records that the original
\(\Fold_m\) fibres are not the subject of the transfer obstruction.
\end{proof}

\begin{corollary}[Compact hierarchy form]\label{cor:compact-hierarchy-form}
The main theorem chain is the chain
\[
  \Cref{thm:eml-universality}
  \Longrightarrow L0,
  \qquad
  \Cref{thm:zeckendorf-protocol-univ}
  \Longrightarrow L1,
\]
with the Richardson interface supplied by \Cref{thm:separation}, strict
one-orbit obstruction supplied by \Cref{thm:level-zero-obstruction},
and the cover-relative \(L2\) addendum supplied by
\Cref{cor:main-L2-addendum,thm:scoped-separation-quantifiers}.
\end{corollary}

\begin{proof}
This is a restatement of \Cref{thm:main-hierarchy} with the cited theorem labels
substituted for the five clauses.
\end{proof}

\begin{corollary}[Richardson submission boundary]\label{cor:richardson-submission-boundary}
The paper proves a Richardson obstruction interface: once a finite EML triple is
specified and its three displayed real-domain identities are proved, Richardson's
undecidability theorem gives the transported EML normalizer obstruction.  It
does not construct such a triple for \(\pi\) and \(\sin\), and it does not assert
an instantiated printed-string theorem for \(U_{47},U_{111},U_{126}\) unless
\((H_{\mathcal W})\) is assumed.
\end{corollary}

\begin{proof}
The interface statement is \Cref{thm:separation}, whose hypothesis includes the
finite triple and the three semantic identities.  The internal core library in
\Cref{lem:eml-real-witness-library} explicitly omits \(\pi\) and sine, so this
paper cannot instantiate the interface from the core witnesses alone.  The only
printed-string specialization is isolated in
\Cref{thm:printed-root-richardson-conditional-clause} and uses the definition of
\((H_{\mathcal W})\) in \Cref{ass:HW}.
\end{proof}

\begin{theorem}[Richardson obstruction interface]\label{thm:richardson-obstruction-interface}
The Richardson input to the main theorem is exactly the following interface: a
finite pure-EML triple with proved identities for \(\pi\), sine on \(\RR\), and
the nonnegative square root on \(\RR_{\ge0}\).  Under this interface, identity
to zero for Richardson expressions reduces effectively to equality with the
translated zero EML term, so a computable semantic normalizer would decide
Richardson's undecidable problem.
\end{theorem}

\begin{proof}
This is \Cref{thm:replacement-triple-richardson-interface} in interface
form.  The proof there constructs the translation by structural recursion and
then applies Richardson's undecidability theorem recorded in
\Cref{lem:richardson-fragment}.
\end{proof}

\begin{corollary}[Positive realizations]\label{cor:main-positive-realizations}
The positive parts of the hierarchy are constructive: \((\eml,1)\) gives the
imported expression representatives, Zeckendorf valuation transport gives the
compiled \(L1\) arithmetic operations, and finite fibre-size multisets give the
\(L2\) certificates.
\end{corollary}

\begin{proof}
The three constructions are respectively
\Cref{thm:eml-universality,thm:zeckendorf-protocol-univ,prop:zeckendorf-cert-univ}.
\end{proof}
