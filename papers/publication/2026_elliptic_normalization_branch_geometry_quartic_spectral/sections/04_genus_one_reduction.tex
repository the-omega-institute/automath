\section{Genus-one reduction of the quartic spectral curve}\label{sec:genus-one}

The present spectral polynomial is treated through a finite algebraic reduction tailored to this quartic.
The substitution \(\xi=y-\lambda^2\) embeds it in a balanced four-parameter family whose coefficients are tuned so that the unwanted \(\lambda^4\), \(\xi\lambda^2\), and \(\lambda^2\) terms cancel simultaneously.
We use that family only to obtain the Weierstrass model, finite real-projection comparisons, and double-root normalization needed for the specialization \(\Pi(\lambda,y)\); it is not developed here as a classification theory for balanced quartics.
For navigation, the section has four independent modules: the balanced Weierstrass reduction proving Theorem~A, the fixed-member real-projection criterion proving the real-image part of Theorem~B, the double-root normalization proving Theorem~C, and the specialization result that computes the branch values and discriminant for \(\Pi\).

The proof order is as follows.
Proposition~\ref{prop:balanced-family}, Corollary~\ref{cor:balanced-smooth}, Lemma~\ref{lem:balanced-projective-completion}, and Corollary~\ref{cor:balanced-ramification} give the affine Weierstrass change of variables, smoothness criterion, projective normalization, and finite ramification equation collected in Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization}.
Lemma~\ref{lem:real-component-extrema-enumeration} and Theorem~\ref{thm:balanced-real-projection} then use only that normalized model and ramification equation to reduce real images to finite endpoint and critical-value comparisons.
Proposition~\ref{prop:quartic-real-projection-framework} and Corollary~\ref{cor:real-fibers-specialization} specialize this finite comparison to \(\Pi=P_{-1,-1,1,0}\), giving the component-image input used in Theorem~B.
Independently, Lemma~\ref{lem:balanced-affine}, Lemma~\ref{lem:balanced-classification}, and Theorem~\ref{thm:canonical-specialization-up-to-normalization} prove the normalized double-root selection used as Theorem~C.
That theorem is purely algebraic: it does not assert dominance of the chosen collision, and the positive-side dominance statement is imported only later from Theorem~\ref{thm:canonical-branch-dominance} by the real weighted pullback lemma.
Finally, Theorem~\ref{thm:elliptic-reduction}, Lemma~\ref{lem:projective-normalization}, Corollary~\ref{cor:disc-factor}, and Corollary~\ref{cor:canonical-theorem-a-specialization} record the canonical specialization of Theorem~A and provide the branch-value and discriminant data used in Sections~\ref{sec:ep-classification} and~\ref{sec:positive-maximal-modulus-branch}.
Thus this section supplies exactly the algebraic inputs A--C to the later branch-dominance and endpoint-zero arguments; no dominance comparison, recurrence asymptotic, or endpoint-zero statement is used to prove the genus-one normalization result.

\subsection{A balanced quartic family}

The affine normalization, squarefreeness criterion, and finite ramification equations for the balanced family are algebraic over every base field \(K\) of characteristic different from \(2\) and \(3\), as recorded in Lemma~\ref{lem:balanced-base-field}.
The theorem and corollaries used later in the manuscript are stated over \(\CC\), obtained by taking \(K=\CC\) in that lemma.
The phrase ``smooth projective completion'' always means the smooth projective curve obtained from the affine Weierstrass model after the change of variables below; equivalently, for the plane quartic it is the normalization of the projective closure.
Statements about real ovals, real images, and interval comparisons are made only after the parameters \(a,c,d,g\) and the variable \(y\) are specialized to \(\RR\).
Complex branch and dominance statements use the complexification of that real curve.

\begin{lemma}[Base-field compatibility of the balanced normalization]\label{lem:balanced-base-field}
Let \(K\) be a field with \(\operatorname{char}K\ne2,3\), and let \(a,c,d,g\in K\).
The formulas
\[
\xi=y-\lambda^2,
\qquad
Y=2\xi-c,
\qquad
y=\lambda^2+\frac{Y+c}{2}
\]
define mutually inverse affine changes of variables over \(K\) between
\[
P_{a,c,d,g}(\lambda,y)=0
\]
and
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g.
\]
Moreover this identification, the squarefreeness condition for the cubic on the right, and the finite ramification equation used below are preserved by every field extension \(L/K\).
\end{lemma}

\begin{proof}
The displayed formulas involve only addition, multiplication, and division by \(2\), so they are defined over \(K\) because \(\operatorname{char}K\ne2\).
Substituting \(\xi=y-\lambda^2\) gives
\[
P_{a,c,d,g}(\lambda,y)=\xi^2-c\xi+a\lambda^3+d\lambda+g,
\]
and multiplying the equation \(\xi^2-c\xi+a\lambda^3+d\lambda+g=0\) by \(4\) after setting \(Y=2\xi-c\) gives exactly
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g.
\]
The inverse formula for \(y\) recovers \(\xi=(Y+c)/2\), hence recovers the original point, so the two affine curves are isomorphic over \(K\).
Squarefreeness of the cubic is equivalent to nonvanishing of its discriminant; because \(\operatorname{char}K\ne3\), the usual derivative and discriminant criterion for a cubic applies and remains unchanged after scalar extension.
On the smooth locus, differentiating the Weierstrass equation and the formula for \(y\) gives the same algebraic equation
\[
2\lambda Y=3a\lambda^2+d
\]
for finite ramification; since this equation has coefficients in \(K\), its base change to any extension \(L/K\) is precisely the corresponding ramification equation over \(L\).
\end{proof}

\begin{proposition}[Balanced quartic family]\label{prop:balanced-family}
For parameters \(a,c,d,g\in\CC\), define
\begin{equation}\label{eq:balanced-family}
P_{a,c,d,g}(\lambda,y)
=
\lambda^4+a\lambda^3+(c-2y)\lambda^2+d\lambda+y(y-c)+g.
\end{equation}
Set
\begin{equation}\label{eq:balanced-xi}
\xi=y-\lambda^2,
\qquad
Y=2\xi-c=2y-2\lambda^2-c.
\end{equation}
Then the affine curve \(P_{a,c,d,g}(\lambda,y)=0\) is isomorphic to the affine curve
\begin{equation}\label{eq:balanced-weierstrass}
Y^2=c^2-4a\lambda^3-4d\lambda-4g.
\end{equation}
The inverse map is
\begin{equation}\label{eq:balanced-inverse}
y=\lambda^2+\frac{Y+c}{2}.
\end{equation}
If \(a\neq 0\) and the cubic polynomial
\[
F_{a,c,d,g}(\lambda)=c^2-4a\lambda^3-4d\lambda-4g
\]
has no repeated root, then the smooth projective completion \(E_{a,c,d,g}\) is a genus-one curve, and the function \(y\) defines a degree-\(4\) map
\[
y:E_{a,c,d,g}\to\mathbf P^1_y.
\]
\end{proposition}

\begin{proof}
Using \(\xi=y-\lambda^2\), one rewrites \eqref{eq:balanced-family} as
\[
P_{a,c,d,g}(\lambda,y)
=
(y-\lambda^2)^2-c(y-\lambda^2)+a\lambda^3+d\lambda+g
=
\xi^2-c\xi+a\lambda^3+d\lambda+g.
\]
Thus
\[
P_{a,c,d,g}(\lambda,y)=0
\iff
\xi^2-c\xi+a\lambda^3+d\lambda+g=0.
\]
Multiplying by \(4\) and completing the square gives
\[
(2\xi-c)^2=c^2-4a\lambda^3-4d\lambda-4g.
\]
This is \eqref{eq:balanced-weierstrass} with \(Y=2\xi-c\); equivalently, it is the specialization of Lemma~\ref{lem:balanced-base-field} to \(K=\CC\).
Conversely, from \(Y=2\xi-c\) one gets \(\xi=(Y+c)/2\), hence \eqref{eq:balanced-inverse}.
Therefore the affine change of variables \((\lambda,y)\leftrightarrow(\lambda,Y)\) is polynomial and invertible, so the two affine curves are isomorphic.

Assume now that \(a\neq 0\) and \(F_{a,c,d,g}\) has no repeated root.
Then \eqref{eq:balanced-weierstrass} is a smooth cubic Weierstrass model.
The projection \((\lambda,Y)\mapsto \lambda\) is a double cover of \(\mathbf P^1_\lambda\) branched at the three finite roots of \(F_{a,c,d,g}\) and, because \(\deg F_{a,c,d,g}=3\), also at infinity.
Hence there are four simple branch points.
By Riemann--Hurwitz,
\[
2g-2=2(-2)+4,
\]
so \(g=1\).

Let \(O\) be the point at infinity on the smooth projective completion.
For a cubic Weierstrass model one has
\[
\operatorname{ord}_O(\lambda)=-2,
\qquad
\operatorname{ord}_O(Y)=-3.
\]
Using \eqref{eq:balanced-inverse},
\[
\operatorname{ord}_O(y)
=
\operatorname{ord}_O\!\left(\lambda^2+\frac{Y+c}{2}\right)
=
\min(-4,-3)
=
-4.
\]
Therefore \(y\) has a unique pole of order \(4\) at \(O\), so the map \(y:E_{a,c,d,g}\to\mathbf P^1_y\) has degree \(4\).
\end{proof}


\begin{corollary}[Smooth genus-one condition]\label{cor:balanced-smooth}
For \(a,c,d,g\in\CC\) with \(a\neq 0\), the cubic \(F_{a,c,d,g}\) has no repeated root if and only if
\begin{equation}\label{eq:balanced-genus-condition}
64d^3+27a(c^2-4g)^2\neq 0.
\end{equation}
\end{corollary}

\begin{proof}
Write
\[
F_{a,c,d,g}(\lambda)
=
-4a\left(\lambda^3+\frac{d}{a}\lambda+\frac{4g-c^2}{4a}\right).
\]
For a monic cubic \(u^3+pu+q\), the discriminant is \(-4p^3-27q^2\).
Substituting
\[
p=\frac{d}{a},
\qquad
q=\frac{4g-c^2}{4a}
\]
gives the condition
\[
-4\left(\frac{d}{a}\right)^3-27\left(\frac{4g-c^2}{4a}\right)^2\neq 0,
\]
which is equivalent to \eqref{eq:balanced-genus-condition} after clearing denominators.
\end{proof}

\begin{lemma}[Projective completion for the balanced quartic]\label{lem:balanced-projective-completion}
Assume \(a\neq0\) and \eqref{eq:balanced-genus-condition}.
Let \(\overline C_{a,c,d,g}\subset \mathbf P^2_{[\Lambda:W:Z]}\) be the projective closure of the affine plane quartic
\(P_{a,c,d,g}(\lambda,y)=0\), with \(\lambda=\Lambda/Z\) and \(y=W/Z\).
Let \(E_{a,c,d,g}\) be the smooth projective Weierstrass curve obtained from
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g.
\]
Then the affine isomorphism of Proposition~\ref{prop:balanced-family} extends uniquely to a finite birational morphism
\[
\nu_{a,c,d,g}:E_{a,c,d,g}\longrightarrow \overline C_{a,c,d,g},
\]
and \(E_{a,c,d,g}\) is the normalization of \(\overline C_{a,c,d,g}\).
In particular the ``smooth projective completion'' in Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization} is not an additional model: it is the nonsingular projective model of the function field of the original quartic, equivalently the normalization of its projective closure.
\end{lemma}

\begin{proof}
By Proposition~\ref{prop:balanced-family}, the affine quartic \(P_{a,c,d,g}=0\) and the affine Weierstrass curve are isomorphic on the common affine chart \(Z\neq0\).
Hence they have the same function field, namely
\[
\CC(\lambda,y)=\CC(\lambda,Y),
\qquad
Y=2y-2\lambda^2-c.
\]
The discriminant condition \eqref{eq:balanced-genus-condition} is equivalent to squarefreeness of the cubic by Corollary~\ref{cor:balanced-smooth}; therefore the projective Weierstrass model \(E_{a,c,d,g}\) is smooth and projective.

The affine isomorphism gives a rational map from the smooth projective curve \(E_{a,c,d,g}\) to the projective curve \(\overline C_{a,c,d,g}\).
Because the source is nonsingular, a rational map from it to a projective variety has no indeterminacy at a codimension-one point; for a curve this means the map extends across the single point at infinity of the Weierstrass model.
Thus the rational map extends uniquely to a morphism \(\nu_{a,c,d,g}:E_{a,c,d,g}\to\overline C_{a,c,d,g}\).
Its restriction to the dense affine chart is the isomorphism above, so \(\nu_{a,c,d,g}\) is birational onto \(\overline C_{a,c,d,g}\).
Since a nonconstant morphism from a projective curve to a curve is finite, \(\nu_{a,c,d,g}\) is finite.
The source is normal because it is a smooth curve, and a finite birational morphism from a normal curve to an integral curve is the normalization.
Therefore \(E_{a,c,d,g}\) is the normalization of \(\overline C_{a,c,d,g}\), as claimed.
\end{proof}

\begin{corollary}[Finite ramification in the balanced family]\label{cor:balanced-ramification}
Let \(a\neq 0\) and assume \eqref{eq:balanced-genus-condition}.
Then the finite ramification points of the degree-\(4\) map \(y:E_{a,c,d,g}\to\mathbf P^1_y\) satisfy
\begin{equation}\label{eq:balanced-ramification-eq}
2\lambda Y=3a\lambda^2+d.
\end{equation}
Eliminating \(Y\) with \eqref{eq:balanced-weierstrass} yields the quintic equation
\begin{equation}\label{eq:balanced-ramification}
4\lambda^2\bigl(c^2-4a\lambda^3-4d\lambda-4g\bigr)-(3a\lambda^2+d)^2=0,
\end{equation}
equivalently
\begin{equation}\label{eq:balanced-quintic}
16a\lambda^5+9a^2\lambda^4+16d\lambda^3+(16g+6ad-4c^2)\lambda^2+d^2=0.
\end{equation}
\end{corollary}

\begin{proof}
Differentiating \(\xi^2-c\xi+a\lambda^3+d\lambda+g=0\) gives
\[
(2\xi-c)\,\dd\xi+(3a\lambda^2+d)\,\dd\lambda=0.
\]
Since \(Y=2\xi-c\),
\[
\dd\xi=-\frac{3a\lambda^2+d}{Y}\,\dd\lambda.
\]
Using \(y=\lambda^2+\xi\), we obtain
\[
\dd y
=
\left(2\lambda-\frac{3a\lambda^2+d}{Y}\right)\dd\lambda
=
\frac{2\lambda Y-(3a\lambda^2+d)}{Y}\,\dd\lambda.
\]
Therefore the finite ramification condition is \eqref{eq:balanced-ramification-eq}.
Squaring and substituting \(Y^2=c^2-4a\lambda^3-4d\lambda-4g\) gives \eqref{eq:balanced-ramification}, and expanding gives \eqref{eq:balanced-quintic}.
\end{proof}

\begin{theorem}[Balanced elliptic normalization (Theorem A)]\label{thm:theorem-a-balanced-elliptic-normalization}
For the balanced quartic family
\[
P_{a,c,d,g}(\lambda,y)
=
\lambda^4+a\lambda^3+(c-2y)\lambda^2+d\lambda+y(y-c)+g
\]
with \(a,c,d,g\in\CC\), the following normalization result holds.
\begin{enumerate}
\item The affine change of variables
\[
\xi=y-\lambda^2,
\qquad
Y=2\xi-c
\]
identifies \(P_{a,c,d,g}(\lambda,y)=0\) with the Weierstrass equation
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g,
\qquad
y=\lambda^2+\frac{Y+c}{2}.
\]
\item If \(a\ne0\), the smooth projective completion is a genus-one curve and the map \(y:E_{a,c,d,g}\to\mathbf P^1_y\) has degree \(4\) exactly when
\[
64d^3+27a(c^2-4g)^2\ne0.
\]
Under this hypothesis, \(E_{a,c,d,g}\) is also the normalization of the projective plane closure of \(P_{a,c,d,g}=0\).
\item Under the same smoothness hypothesis, the finite ramification points of \(y\) are exactly the points satisfying
\[
2\lambda Y=3a\lambda^2+d,
\]
or, after eliminating \(Y\),
\[
16a\lambda^5+9a^2\lambda^4+16d\lambda^3+(16g+6ad-4c^2)\lambda^2+d^2=0.
\]
\end{enumerate}
The statement records the exact sources of its three clauses: clause~(1) is exactly Proposition~\ref{prop:balanced-family}; the genus-one, degree-four, and smoothness assertions in clause~(2) are Corollary~\ref{cor:balanced-smooth}, while the comparison with the original plane quartic in the last sentence of clause~(2) is Lemma~\ref{lem:balanced-projective-completion}; and clause~(3) is exactly Corollary~\ref{cor:balanced-ramification}.
\end{theorem}

\begin{proof}
Part~(1) is Proposition~\ref{prop:balanced-family}: the displayed substitution gives a polynomial inverse and hence an affine isomorphism, with inverse formula \(y=\lambda^2+(Y+c)/2\).
For \(a\ne0\), the cubic on the right has no repeated root precisely under the discriminant condition in Corollary~\ref{cor:balanced-smooth}; in that case Proposition~\ref{prop:balanced-family} identifies the smooth projective completion as genus one and computes the order-four pole of \(y\) at infinity, giving degree \(4\).
Lemma~\ref{lem:balanced-projective-completion} supplies the projective comparison with the original plane quartic: the same smooth curve is the normalization of the quartic's projective closure.
Finally, Corollary~\ref{cor:balanced-ramification} differentiates the Weierstrass equation and the inverse formula for \(y\), obtaining \(2\lambda Y=3a\lambda^2+d\); substituting \(Y^2=c^2-4a\lambda^3-4d\lambda-4g\) gives the displayed quintic.
The preceding paragraph gives a clause-by-clause implication from the cited results to the theorem.
Conversely, the displayed assertions of those cited results are precisely the three clauses just proved: Proposition~\ref{prop:balanced-family} supplies the displayed affine identification, Corollary~\ref{cor:balanced-smooth} supplies the smooth genus-one and degree-four criterion, Lemma~\ref{lem:balanced-projective-completion} supplies the normalization of the plane closure, and Corollary~\ref{cor:balanced-ramification} supplies both forms of the finite ramification equation.
No additional lemma is used, so these proved statements are exactly the asserted normalization result.
\end{proof}

\subsection{Real projection structure for fixed balanced members}

The next theorem is a fixed-member real-projection criterion for a smooth balanced quartic; Theorem~\ref{thm:theorem-b-real-projection-criterion} is its canonical spectral-quartic specialization.
Its only subsequent use is Proposition~\ref{prop:quartic-real-projection-framework}, where the theorem reduces the real \(y\)-image of the quartic specialization \((a,c,d,g)=(-1,-1,1,0)\) to a finite comparison of endpoint and critical values.
No family-level classification or dominance theory for balanced quartics is used in the sequel.
The balanced-parameter notation is kept here only in this specialization-serving form: for one already chosen smooth member it records the endpoint, critical-value, and transition comparisons that make the later quartic computation checkable from finitely many algebraic numbers.

\begin{lemma}[Endpoint and critical values on a real component]\label{lem:real-component-extrema-enumeration}
Let \(a,c,d,g\in\RR\), let
\[
F(\lambda)=c^2-4a\lambda^3-4d\lambda-4g
\]
be squarefree, and let \(J\) be a closed connected component of \(\{\lambda\in\RR:F(\lambda)\ge0\}\).
On the real curve \(Y^2=F(\lambda)\), consider
\[
y(\lambda,Y)=\lambda^2+\frac{Y+c}{2}.
\]
Every finite endpoint value or finite extremum of \(y\) on the real component above \(J\) is one of the following:
\begin{enumerate}
\item an endpoint value \(\beta^2+c/2\) with \(\beta\in J\) and \(F(\beta)=0\);
\item a non-endpoint critical value with \(Y\ne0\) and \(\lambda\ne0\), where
\[
2\lambda Y=3a\lambda^2+d,
\qquad
y=\lambda^2+\frac{3a\lambda^2+d}{4\lambda}+\frac c2;
\]
\item an exceptional value over \(\lambda=0\), which occurs only when \(0\in J\) and \(d=0\), and is then \((c\pm\sqrt{F(0)})/2\), with the two signs coinciding when \(F(0)=0\).
\end{enumerate}
Thus no endpoint or interior extremum is lost by separating the cases \(F(\beta)=0\), \(\lambda\ne0\), and \(\lambda=0\).
\end{lemma}

\begin{proof}
The classical input used here is only the following local real form of a squarefree Weierstrass cubic: over the interior of \(J\), where \(F>0\), the two real branches are the analytic graphs \(Y=\pm\sqrt{F(\lambda)}\), while at an endpoint \(\beta\) with \(F(\beta)=0\) and \(F'(\beta)\neq0\) the normalization supplies one real analytic arc with \(\lambda-\beta\) a square of a local parameter \cite{Silverman2009,Miranda1995}.
Thus the real component above \(J\) is a one-dimensional real analytic manifold, with the two sheets over the interior of \(J\) glued at the endpoints where \(F=0\); all endpoint and critical-value enumeration below is then the displayed differentiation of the present function \(y\).
At such an endpoint, \(Y=0\), and substituting in \(y=\lambda^2+(Y+c)/2\) gives \(\beta^2+c/2\).

On the interior of \(J\), one has \(Y\ne0\), so \(\lambda\) is a valid local coordinate on each sheet.
Differentiating
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g
\]
gives
\[
2Y\,\frac{\dd Y}{\dd\lambda}=-12a\lambda^2-4d,
\]
and hence
\[
\frac{\dd y}{\dd\lambda}=2\lambda-\frac{3a\lambda^2+d}{Y}.
\]
Therefore an interior extremum satisfies \(2\lambda Y=3a\lambda^2+d\).
If \(\lambda\ne0\), substituting \(Y=(3a\lambda^2+d)/(2\lambda)\) gives the displayed rational critical value.
Squaring the same relation and using \(Y^2=F(\lambda)\) is precisely the ramification equation \eqref{eq:balanced-ramification}.

It remains to handle \(\lambda=0\), where the rational expression is not available.
The derivative condition at \(\lambda=0\) becomes \(-d/Y=0\) when \(F(0)>0\), so it is equivalent to \(d=0\); the two real points over \(0\) then give \((c\pm\sqrt{F(0)})/2\).
If \(F(0)=0\), the point over \(0\) is an endpoint already included in the first case, and when also \(d=0\) its value is the single number \(c/2\).
These alternatives cover all points of the real component, proving the exhaustion statement.
\end{proof}

\begin{theorem}[Fixed-member real-projection criterion for one balanced quartic]\label{thm:balanced-real-projection}
Let $a,c,d,g\in\RR$ with $a\neq0$ and assume \eqref{eq:balanced-genus-condition}, so that $E_{a,c,d,g}$ is a smooth genus-one curve.
Let $F(\lambda)=c^2-4a\lambda^3-4d\lambda-4g$ be the cubic in \eqref{eq:balanced-weierstrass}, and let
\[
R_{a,c,d,g}(\lambda)
=
16a\lambda^5+9a^2\lambda^4+16d\lambda^3+(16g+6ad-4c^2)\lambda^2+d^2
\]
be the ramification polynomial from \eqref{eq:balanced-quintic}.
Fix this smooth real member once and for all.
The following statement is the finite real-projection criterion for the single real map just fixed; it is not a chamber decomposition of the parameter space of all balanced quartics.
For every closed connected component $J\subset\{\lambda\in\RR:F(\lambda)\ge0\}$ define the finite algebraic set
\begin{align}\label{eq:balanced-real-extrema-set}
\mathcal V(J)
&:=
\{\beta^2+\tfrac{c}{2}:\beta\in J,\ F(\beta)=0\}
\notag\\
&\quad{}
\cup
\left\{
\lambda^2+\frac{3a\lambda^2+d}{4\lambda}+\frac{c}{2}:
\lambda\in J\setminus\{0\},\ R_{a,c,d,g}(\lambda)=0
\right\}
\notag\\
&\quad{}
\cup\ \mathcal Z(J),
\end{align}
where
\[
\mathcal Z(J):=
\begin{cases}
\left\{\dfrac{c+\sqrt{F(0)}}{2},\dfrac{c-\sqrt{F(0)}}{2}\right\},
& 0\in J,\ d=0,\ F(0)>0,\\[7pt]
\left\{\dfrac{c}{2}\right\},
& 0\in J,\ d=0,\ F(0)=0,\\[7pt]
\emptyset, & \text{otherwise.}
\end{cases}
\]
The set $\mathcal Z(J)$ is present only for the exceptional critical fibre at $\lambda=0$; for $\lambda\ne0$ all finite critical values are given by the displayed rational formula.
All entries in \(\mathcal V(J)\) are interpreted set-theoretically, with the following degeneracy conventions.
An endpoint value which is also the value of a non-endpoint critical point is counted once.
Distinct roots of \(R_{a,c,d,g}\), or the two signs in \(\mathcal Z(J)\), are allowed to give the same real number; this produces a repeated extremal value, not an additional interval endpoint.
If \(0\in J\) and \(F(0)=0\), the value \(c/2\) is the endpoint value over \(\lambda=0\); when also \(d=0\), the second line of \(\mathcal Z(J)\) records the same limiting exceptional fibre and does not create a new point.
If an endpoint of \(J\) satisfies \(\lambda=0\) but \(d\ne0\), no exceptional \(\lambda=0\) critical value is present.
Finally, values attached to different real components may coincide; in that case the two component images touch or overlap exactly as described in~(4).
The output is the following finite componentwise description of the real image of this single map $y:E_{a,c,d,g}(\RR)\to\RR$.

\begin{enumerate}
\item The real locus $E_{a,c,d,g}(\RR)$ is connected (a single oval) if and only if $F$ has exactly one real root.
It has two connected components if and only if $F$ has three distinct real roots.

\item Assume first that $F$ has exactly one real root and let $J_{\mathrm{n}}\subset\RR$ be the unique unbounded component of \(\{\lambda\in\RR:F(\lambda)\ge 0\}\).
Then
\[
\qquad
y\bigl(E_{a,c,d,g}(\RR)\bigr)=[m_{\mathrm{n}},\infty),
\qquad
m_{\mathrm{n}}
=
\min \mathcal V(J_{\mathrm{n}}).
\]

\item Assume now that $F$ has three distinct real roots.
Let \(J_{\mathrm{c}}\) be the bounded component and \(J_{\mathrm{n}}\) the unbounded component of \(\{\lambda\in\RR:F(\lambda)\ge 0\}\).
Then \(E_{a,c,d,g}(\RR)=E_{\mathrm{c}}(\RR)\sqcup E_{\mathrm{n}}(\RR)\), where \(E_{\mathrm{c}}(\RR)\) lies above \(J_{\mathrm{c}}\) and \(E_{\mathrm{n}}(\RR)\) lies above \(J_{\mathrm{n}}\), and their images are
\[
\qquad
y\bigl(E_{\mathrm{c}}(\RR)\bigr)=[m_{\mathrm{c}},M_{\mathrm{c}}],
\qquad
y\bigl(E_{\mathrm{n}}(\RR)\bigr)=[m_{\mathrm{n}},\infty),
\]
where
\[
m_{\mathrm{c}}
=
\min \mathcal V(J_{\mathrm{c}}),
\]
\[
M_{\mathrm{c}}
=
\max \mathcal V(J_{\mathrm{c}}),
\]
\[
m_{\mathrm{n}}
=
\min \mathcal V(J_{\mathrm{n}}).
\]
Thus the total real image is obtained by ordering a finite list of algebraic numbers determined by the real roots of \(F\) and \(R_{a,c,d,g}\).
This finite ordering is the closed criterion: no limiting process, sampling argument, or unlisted real critical value is needed.
In particular, subject only to the stated real-parameter, smoothness, squarefreeness, and component hypotheses, the real image is exactly one closed interval if \(M_{\mathrm c}\ge m_{\mathrm n}\) and exactly two disjoint closed intervals if \(M_{\mathrm c}<m_{\mathrm n}\); equality is the only endpoint-degenerate case and is a one-interval image with the two component images touching at the single value \(M_{\mathrm c}=m_{\mathrm n}\).

\item In the two-component situation of~(3), the total real image is
\[
[m_{\mathrm{c}},M_{\mathrm{c}}]\cup[m_{\mathrm{n}},\infty).
\]
It is disjoint, adjacent, or overlapping according as
\[
M_{\mathrm{c}}<m_{\mathrm{n}},
\qquad
M_{\mathrm{c}}=m_{\mathrm{n}},
\qquad\text{or}\qquad
M_{\mathrm{c}}>m_{\mathrm{n}}.
\]
All three cases are therefore decided by explicit comparisons among the finitely many algebraic numbers in~(3).
Equivalently, after the real roots of the two displayed polynomials have been isolated, the one-interval versus two-disjoint-interval alternative is decided by the single algebraic sign comparison \(\operatorname{sign}(M_{\mathrm c}-m_{\mathrm n})\).

\item The same finite sets are complete transition frameworks for this fixed map.
For a component \(J\) let
\[
N_J(t):=\#\{p\in E_J(\RR):y(p)=t\}.
\]
Then \(N_J(t)\) is constant on every connected component of \(\RR\setminus\mathcal V(J)\).
In the two-component case the total real fibre count
\[
N(t):=\#\{p\in E_{a,c,d,g}(\RR):y(p)=t\}
=N_{J_{\mathrm c}}(t)+N_{J_{\mathrm n}}(t)
\]
is constant on every connected component of
\[
\RR\setminus\bigl(\mathcal V(J_{\mathrm c})\cup\mathcal V(J_{\mathrm n})\bigr).
\]
Thus every change in the componentwise or total number of real preimages is certified by one of the finitely many algebraic values already listed in \eqref{eq:balanced-real-extrema-set}.
\end{enumerate}
\end{theorem}

\begin{proof}
Write
\[
E(\RR)=\{(\lambda,Y)\in\RR^2:Y^2=F(\lambda)\}\cup\{O\}.
\]
Since \(F\) is a real nonsingular cubic, its real roots are either one simple root or three simple roots.
The affine real locus is the two-sheeted cover of the closed set \(\{F\ge0\}\), with the two sheets glued exactly at finite endpoints where \(F=0\).
If \(F\) has one real root, \(\{F\ge0\}\) has one closed unbounded component; after adding the point at infinity the two affine sheets glue to a single real oval.
If \(F\) has three distinct real roots, \(\{F\ge0\}\) has one bounded component and one unbounded component; the bounded interval gives a compact oval in the affine plane, while the unbounded interval gives the second oval after adjoining \(O\).
This proves the component statement in~(1), with the evident notation \(J_{\mathrm c}\) and \(J_{\mathrm n}\).

Now fix a closed component \(J\subset\{F\ge0\}\), and let \(E_J(\RR)\) be the real component lying over it.
The function
\[
y(\lambda,Y)=\lambda^2+\frac{Y+c}{2}
\]
is continuous on \(E_J(\RR)\).
On the compact component this already gives a compact interval image.
On the unbounded component, the projective point \(O\) is the only point at infinity and the Weierstrass local parameter gives \(\lambda\) a pole of order \(2\) and \(Y\) a pole of order \(3\).
Thus \(y=\lambda^2+(Y+c)/2\) has a pole of order \(4\) at \(O\), and along either real end approaching \(O\) its leading term is \(\lambda^2\), hence tends to \(+\infty\).
The restriction to the real unbounded component is therefore proper as a map to \(\RR\) and attains a finite minimum.

It remains only to identify all finite endpoint and critical values.
Lemma~\ref{lem:real-component-extrema-enumeration} applies to each closed component \(J\subset\{F\ge0\}\).
Its endpoint values give the first line of \(\mathcal V(J)\), its \(\lambda\ne0\) critical values give the rational values in the second line together with the ramification equation \(R_{a,c,d,g}(\lambda)=0\), and its exceptional \(\lambda=0\) alternatives give exactly \(\mathcal Z(J)\), including the boundary case \(F(0)=0\) where the two signs coincide with the endpoint value \(c/2\).
The smoothness assumption forbids repeated roots of \(F\), but it does not forbid coincidences among the resulting real values.
Such coincidences do not change the interval image because extrema of a continuous real-valued function on a connected one-dimensional real component are governed by the minimum and maximum of the set of endpoint and critical values, not by their multiplicities.
The rational critical-value formula is used only for \(\lambda\ne0\), so no endpoint over \(\lambda=0\) is lost or double-counted: if \(F(0)=0\), it contributes the endpoint value \(c/2\), and the exceptional critical alternative occurs only in the limiting case \(d=0\); if \(F(0)>0\), the derivative computation in Lemma~\ref{lem:real-component-extrema-enumeration} shows that the two interior points over \(0\) are critical exactly when \(d=0\), giving the two values in \(\mathcal Z(J)\), possibly equal only when \(F(0)=0\), which is the previous endpoint case.
Coincidence of values coming from different components is therefore handled only after the two component images have been computed, by the comparison in part~(4).
The polynomial \(R_{a,c,d,g}\) has degree five, \(F\) has only finitely many endpoints, and \(\mathcal Z(J)\) has at most two values, so this list exhausts all endpoint and critical values.
Consequently the compact component image is \([\min\mathcal V(J_{\mathrm c}),\max\mathcal V(J_{\mathrm c})]\), and the unbounded component image is \([\min\mathcal V(J_{\mathrm n}),\infty)\).
Part~(4) follows immediately by comparing these two interval endpoints.

For part~(5), fix one component \(E_J(\RR)\) and one connected open interval \(U\) in \(\RR\setminus\mathcal V(J)\).
If \(U\) is disjoint from \(y(E_J(\RR))\), then \(N_J\) is identically zero on \(U\).
Otherwise each fibre over \(U\) is finite, because the map \(y:E\to\mathbb P^1\) has finite degree and the only point above \(\infty\) is not in a finite fibre.
For every \(t_0\in U\) and every \(p\in y^{-1}(t_0)\cap E_J(\RR)\), the value \(t_0\notin\mathcal V(J)\) excludes both endpoint values and interior critical values, so \(p\) is a regular point of the real map on the component.
The real implicit-function theorem therefore gives a unique local real branch of the fibre through \(p\) over a neighbourhood of \(t_0\).
Since all endpoint and interior critical values of the component have already been listed in \(\mathcal V(J)\), these local branches cannot be created, annihilated, or collide while \(t\) remains inside \(U\).
Equivalently, the restriction of \(y\) over any compact subinterval of \(U\) is a finite covering of real one-manifolds, after deleting the empty case, and hence has constant fibre cardinality on that subinterval.
The interval \(U\) is connected, so these local constants agree on overlaps and \(N_J\) is constant on all of \(U\).
Adding the two componentwise constants proves the assertion for \(N(t)\) in the two-component case.
\end{proof}

\begin{proposition}[Finite comparison for a fixed real projection]\label{prop:balanced-finite-comparison-algorithm}
Fix one smooth balanced quartic in the two-oval case of Theorem~\ref{thm:balanced-real-projection}.
For this fixed member, the following finite algebraic procedure is a necessary and sufficient criterion for the real image of the balanced map \(y:E_{a,c,d,g}(\RR)\to\RR\) to be one interval rather than two disjoint intervals.
\begin{enumerate}
\item Isolate the three real roots of \(F\) and hence the bounded and unbounded components \(J_{\mathrm c}\) and \(J_{\mathrm n}\) of \(\{F\ge0\}\).
\item Form the two finite sets \(\mathcal V(J_{\mathrm c})\) and \(\mathcal V(J_{\mathrm n})\) from \eqref{eq:balanced-real-extrema-set}, using exactly the endpoint, ramification-root, and exceptional-\(\lambda=0\) conventions stated in Theorem~\ref{thm:balanced-real-projection}.
\item Compute
\[
m_{\mathrm c}=\min\mathcal V(J_{\mathrm c}),
\qquad
M_{\mathrm c}=\max\mathcal V(J_{\mathrm c}),
\qquad
m_{\mathrm n}=\min\mathcal V(J_{\mathrm n}).
\]
Then
\[
y(E_{a,c,d,g}(\RR))\text{ has one connected component}
\quad\Longleftrightarrow\quad
M_{\mathrm c}\ge m_{\mathrm n},
\]
while \(M_{\mathrm c}<m_{\mathrm n}\) is equivalent to a disjoint two-interval image with gap \((M_{\mathrm c},m_{\mathrm n})\).
The equality case is adjacent touching at one algebraic value, and the strict inequality \(M_{\mathrm c}>m_{\mathrm n}\) is genuine interval overlap.
\end{enumerate}
All comparisons in the procedure are comparisons of real algebraic numbers obtained from the two explicitly displayed polynomials \(F\) and \(R_{a,c,d,g}\) and the exceptional values in \(\mathcal Z(J)\).
\end{proposition}

\begin{proof}
Theorem~\ref{thm:balanced-real-projection}(1) identifies the two-oval case exactly with the case where \(F\) has three distinct real roots, so isolating these roots determines the closed intervals \(J_{\mathrm c}\) and \(J_{\mathrm n}\).
By Lemma~\ref{lem:real-component-extrema-enumeration}, every finite endpoint or critical value of \(y\) on the real component above a closed component \(J\subset\{F\ge0\}\) belongs to \(\mathcal V(J)\), and the proof of Theorem~\ref{thm:balanced-real-projection} shows conversely that the extrema of the image interval are obtained from this same finite set.
Thus the bounded oval has image \([m_{\mathrm c},M_{\mathrm c}]\) and the noncompact oval has image \([m_{\mathrm n},\infty)\).
Two closed intervals of these forms have connected union if and only if their overlap is nonempty, which is precisely \(M_{\mathrm c}\ge m_{\mathrm n}\); they are disjoint exactly when \(M_{\mathrm c}<m_{\mathrm n}\), in which case the complement between them is the open gap \((M_{\mathrm c},m_{\mathrm n})\).
If equality holds, the intersection consists of the single value \(M_{\mathrm c}=m_{\mathrm n}\); if the inequality is strict in the other direction, the intersection is the nondegenerate interval \([m_{\mathrm n},M_{\mathrm c}]\).
The displayed construction uses only real roots of the cubic \(F\), real roots of the quintic \(R_{a,c,d,g}\) lying in the relevant components, rational expressions in those roots, and the square-root values in \(\mathcal Z(J)\), hence only finite comparisons of real algebraic numbers.
\end{proof}

\begin{corollary}[Fixed-member interval trichotomy for one balanced real projection]\label{cor:balanced-real-interval-alternatives}
In the two-component case of Theorem~\ref{thm:balanced-real-projection}, put
\[
I_{\mathrm c}=[m_{\mathrm c},M_{\mathrm c}],
\qquad
I_{\mathrm n}=[m_{\mathrm n},\infty).
\]
Here $m_{\mathrm c}$, $M_{\mathrm c}$, and $m_{\mathrm n}$ are the extrema of the finite algebraic sets in Theorem~\ref{thm:balanced-real-projection}(3), with the endpoint and exceptional-$\lambda=0$ conventions of \eqref{eq:balanced-real-extrema-set}.
Then
\[
y(E_{a,c,d,g}(\RR))\text{ is one interval}
\quad\Longleftrightarrow\quad
M_{\mathrm c}\ge m_{\mathrm n},
\]
and it is a disjoint union of two intervals if and only if $M_{\mathrm c}<m_{\mathrm n}$.
More precisely, exactly one of the following mutually exclusive alternatives occurs.
\begin{enumerate}
\item \emph{Disjoint two-interval image:} if \(M_{\mathrm c}<m_{\mathrm n}\), then
\[
y(E_{a,c,d,g}(\RR))=I_{\mathrm c}\sqcup I_{\mathrm n}
\]
has two connected components, separated by the nonempty gap \((M_{\mathrm c},m_{\mathrm n})\).
\item \emph{Adjacent one-interval image with a touching value:} if \(M_{\mathrm c}=m_{\mathrm n}\), then
\[
y(E_{a,c,d,g}(\RR))=[m_{\mathrm c},\infty),
\]
and the two component images meet only at the common endpoint \(M_{\mathrm c}=m_{\mathrm n}\).
\item \emph{Overlapping one-interval image:} if \(M_{\mathrm c}>m_{\mathrm n}\), then
\[
y(E_{a,c,d,g}(\RR))=[\min(m_{\mathrm c},m_{\mathrm n}),\infty),
\]
and the overlap is the nondegenerate interval
\[
I_{\mathrm c}\cap I_{\mathrm n}=[m_{\mathrm n},M_{\mathrm c}].
\]
\end{enumerate}
The degenerate endpoint conventions are as follows.
If the same algebraic value is produced by several endpoints, critical points, or exceptional $\lambda=0$ entries, it is used once in the set-theoretic extrema defining $m_{\mathrm c},M_{\mathrm c},m_{\mathrm n}$.
If $M_{\mathrm c}=m_{\mathrm n}$ the union has one connected component but the two oval images are only adjacent, whereas if $M_{\mathrm c}>m_{\mathrm n}$ their overlap is a genuine interval; if $M_{\mathrm c}<m_{\mathrm n}$ the open gap is exactly $(M_{\mathrm c},m_{\mathrm n})$.
No further degeneration is hidden in this trichotomy: the smoothness hypothesis excludes repeated roots of \(F\), the two real components are distinct ovals, \(I_{\mathrm c}\) is a compact interval, \(I_{\mathrm n}\) is an unbounded interval, and every endpoint used above is one of the finitely many algebraic values in \(\mathcal V(J_{\mathrm c})\cup\mathcal V(J_{\mathrm n})\).
\end{corollary}

\begin{proof}
Theorem~\ref{thm:balanced-real-projection}(3) gives exactly the two component images \(I_{\mathrm c}\) and \(I_{\mathrm n}\), and the conventions preceding part~(1) of that theorem identify the repeated endpoint and exceptional-fibre values set-theoretically before the extrema are taken.
Since \(I_{\mathrm c}\) is closed and bounded and \(I_{\mathrm n}\) is closed and unbounded, their union is disconnected precisely when the right endpoint of \(I_{\mathrm c}\) is strictly smaller than the left endpoint of \(I_{\mathrm n}\), which is the inequality \(M_{\mathrm c}<m_{\mathrm n}\).
If equality holds, the intervals meet in the single endpoint \(M_{\mathrm c}=m_{\mathrm n}\), so their union is the single closed ray \([m_{\mathrm c},\infty)\).
If \(M_{\mathrm c}>m_{\mathrm n}\), the intersection is the interval \([m_{\mathrm n},M_{\mathrm c}]\), which has positive length, and the union is the closed ray beginning at the smaller of the two left endpoints.
The remaining assertions are the smooth two-component hypotheses and the finite-extrema conclusion of Theorem~\ref{thm:balanced-real-projection}.
\end{proof}

\begin{proposition}[Completeness of the fixed-member real-projection criterion]\label{prop:balanced-fixed-criterion-complete}
Fix a smooth real balanced member in the two-oval case of Theorem~\ref{thm:balanced-real-projection}.
Let
\[
\Sigma
=
\mathcal V(J_{\mathrm c})\sqcup\mathcal V(J_{\mathrm n})
\]
be the tagged finite framework of endpoint, ramification, and exceptional-$\lambda=0$ values from \eqref{eq:balanced-real-extrema-set}, where the two copies are kept tagged by the real oval on which they occur.
Then the tagged order type of \(\Sigma\), together with the three distinguished extrema
\[
m_{\mathrm c}=\min\mathcal V(J_{\mathrm c}),
\qquad
M_{\mathrm c}=\max\mathcal V(J_{\mathrm c}),
\qquad
m_{\mathrm n}=\min\mathcal V(J_{\mathrm n}),
\]
is a complete fixed-member criterion for the real projection in the following sense.
\begin{enumerate}
\item The connected-component alternative for \(y(E_{a,c,d,g}(\RR))\) is decided by the single tagged comparison \(M_{\mathrm c}\ge m_{\mathrm n}\).
The strict reverse inequality gives the unique gap \((M_{\mathrm c},m_{\mathrm n})\), equality gives adjacent touching, and strict overlap gives one ray with nondegenerate overlap.
\item For every connected component \(U\) of \(\RR\setminus\Sigma\), the componentwise fibre counts over \(U\) are constant; hence the tagged framework also contains every possible real-fibre transition value of this fixed projection.
\item The ordered tagged framework is the complete finite obstruction list for the fixed map: after the three distinguished extrema have been read from it, the one-interval, adjacent, or two-disjoint-interval alternative is determined by the single comparison \(M_{\mathrm c}\) versus \(m_{\mathrm n}\).
After one componentwise fibre count is sampled in each connected component of \(\RR\setminus\Sigma\), all componentwise fibre counts on the real line are determined.
\end{enumerate}
Thus the real-projection theorem used in this article is exactly the finite algebraic criterion for a fixed member.
It supplies no converse classification of which parameter regions in \((a,c,d,g)\)-space realize a given criterion.
\end{proposition}

\begin{proof}
Theorem~\ref{thm:balanced-real-projection}(3) identifies the two component images as
\[
y(E_{\mathrm c}(\RR))=[m_{\mathrm c},M_{\mathrm c}],
\qquad
y(E_{\mathrm n}(\RR))=[m_{\mathrm n},\infty),
\]
with the three extrema obtained from the finite sets \(\mathcal V(J_{\mathrm c})\) and \(\mathcal V(J_{\mathrm n})\).
The union of intervals of these two forms is connected exactly when the right endpoint of the bounded interval is not smaller than the left endpoint of the ray, which proves the first assertion and identifies the gap, touching point, and overlap in the three cases.

For the second assertion, Theorem~\ref{thm:balanced-real-projection}(5) states that the componentwise fibre count is constant on every connected component of the complement of the tagged endpoint-and-critical-value set.
The proof of that theorem shows that every endpoint of a component image and every finite critical value of the map \(y:E(\RR)\to\RR\) is included in the relevant \(\mathcal V(J)\), with the exceptional \(\lambda=0\) convention accounting for the only critical value not covered by the rational ramification formula.
Therefore no real-fibre transition can occur away from \(\Sigma\).

The third assertion combines these two facts without adding any parameter-space statement.
The ordered tagged framework supplies the three extrema, so the comparison \(M_{\mathrm c}<m_{\mathrm n}\), \(M_{\mathrm c}=m_{\mathrm n}\), or \(M_{\mathrm c}>m_{\mathrm n}\) gives respectively the gap, touching point, or overlap.
The connected components of \(\RR\setminus\Sigma\) are exactly the real-line intervals on which the second assertion makes the componentwise counts constant.
Therefore one sample componentwise count in each such interval determines the counts throughout that interval, and no further obstruction or transition value remains.
All data used in the proposition are extracted from the fixed member by isolating roots of the two explicit polynomials \(F\) and \(R_{a,c,d,g}\), evaluating the displayed algebraic expressions, and applying the exceptional-value convention in \(\mathcal Z(J)\).
\end{proof}

\begin{corollary}[Fixed-member criterion for canonical real projection]\label{cor:fixed-member-projection-comparison}
The real-projection input used later for the canonical quartic is exhausted by the following fixed-member data:
\[
F(\lambda)=4\lambda^3-4\lambda+1,
\qquad
R_{-1,-1,1,0}(\lambda),
\qquad
\mathcal V(J_{\mathrm c})\sqcup\mathcal V(J_{\mathrm n}).
\]
After these data have been evaluated for the single member \((a,c,d,g)=(-1,-1,1,0)\), the subsequent real-root count, interval-overlap conclusion, and branch-dominance arguments depend only on the two interval images
\[
y(E_{\mathrm c}(\RR))=[\ySub,1],
\qquad
y(E_{\mathrm n}(\RR))=[0,\infty).
\]
Consequently Theorem~\ref{thm:theorem-b-real-projection-criterion} is a specialization of the fixed-member criterion and not a statement about connected chambers, walls, or adjacency relations in the parameter space \(\RR^4_{a,c,d,g}\) of balanced quartics.
\end{corollary}

\begin{proof}
Theorem~\ref{thm:balanced-real-projection} first identifies the real image of one fixed smooth member from the cubic \(F\), the ramification polynomial \(R_{a,c,d,g}\), and the exceptional \(\lambda=0\) convention included in \(\mathcal V(J)\).
Proposition~\ref{prop:balanced-finite-comparison-algorithm} and Proposition~\ref{prop:balanced-fixed-criterion-complete} then show that, for that same fixed member, the connectedness alternative and all real-fibre transition values are determined by the tagged finite framework \(\mathcal V(J_{\mathrm c})\sqcup\mathcal V(J_{\mathrm n})\).

For the canonical specialization, Proposition~\ref{prop:quartic-real-projection-framework} performs this finite evaluation and gives exactly
\[
y(E_{\mathrm c}(\RR))=[\ySub,1],
\qquad
y(E_{\mathrm n}(\RR))=[0,\infty).
\]
Theorem~\ref{thm:theorem-b-real-projection-criterion} uses these two intervals to count real roots by the two-to-one covering of the interiors of the oval images, and Theorem~\ref{thm:canonical-branch-dominance} later uses the same branch values and real-root separation as its canonical real input.
No step in this implication varies \((a,c,d,g)\), follows a path in parameter space, or proves that a fixed finite framework is stable on an open parameter region.
Thus the result used here is precisely the fixed-member criterion stated above; a global chamber decomposition would be an additional problem not treated here.
\end{proof}

\begin{remark}[Use of the projection criterion]
Theorem~\ref{thm:balanced-real-projection}, Proposition~\ref{prop:balanced-finite-comparison-algorithm}, Corollary~\ref{cor:balanced-real-interval-alternatives}, Proposition~\ref{prop:balanced-fixed-criterion-complete}, and Corollary~\ref{cor:fixed-member-projection-comparison} are used below as finite fixed-member tests for a fixed smooth member.
For the specialization \((a,c,d,g)=(-1,-1,1,0)\), Proposition~\ref{prop:quartic-real-projection-framework} performs the resulting comparison explicitly and gives
\[
y(E_{\mathrm c}(\RR))=[y_{\mathrm{sub}},1],
\qquad
y(E_{\mathrm n}(\RR))=[0,\infty).
\]
These two intervals and their overlap \([0,1]\) are the only balanced-family real-projection inputs used later in Theorem~\ref{thm:theorem-b-real-projection-criterion} and Theorem~\ref{thm:canonical-branch-dominance}; all of them are obtained from the fixed canonical member, not from a global parameter-space chamber theorem.
\end{remark}

\begin{proposition}[Quartic real-projection classification]\label{prop:quartic-real-projection-framework}\label{cor:two-interval-specialization}
For the specialization $(a,c,d,g)=(-1,-1,1,0)$ the cubic $F(\lambda)=4\lambda^3-4\lambda+1$ has three distinct real roots $\beta_1<\beta_2<\beta_3$ (the discriminant $\Delta=37>0$ confirms this).
Thus $E_{-1,-1,1,0}(\RR)$ has two real ovals.
The finite obstruction framework for the real projection consists of
\[
\begin{aligned}
\mathcal V(J_{\mathrm c})
&=
\left\{\beta_1^2-\frac12,\ \beta_2^2-\frac12,\ 1,\ y_{\mathrm{sub}}\right\},\\
\mathcal V(J_{\mathrm n})
&=
\left\{\beta_3^2-\frac12,\ 0\right\},
\end{aligned}
\]
where \(J_{\mathrm c}=[\beta_1,\beta_2]\), \(J_{\mathrm n}=[\beta_3,\infty)\), and \(y_{\mathrm{sub}}\) is the unique real zero of
\[
256y^3+411y^2+165y+32.
\]
Moreover
\[
\min\mathcal V(J_{\mathrm c})=y_{\mathrm{sub}},
\qquad
\max\mathcal V(J_{\mathrm c})=1,
\qquad
\min\mathcal V(J_{\mathrm n})=0.
\]
Consequently the compact oval maps under $y$ to the interval $[y_{\mathrm{sub}},1]$ and the noncompact oval maps to $[0,+\infty)$, so the total real image is $[y_{\mathrm{sub}},\infty)$ with an overlap on $[0,1]$.
In particular $y<y_{\mathrm{sub}}$ yields no real preimages, $y\in(y_{\mathrm{sub}},0)$ has two real preimages, and $y\in(0,1)$ has four.
\end{proposition}

\begin{proof}
For this specialization one has
\[
\{\lambda\in\RR:4\lambda^3-4\lambda+1\ge 0\}=[\beta_1,\beta_2]\cup[\beta_3,\infty),
\]
so \(J_{\mathrm{c}}=[\beta_1,\beta_2]\) and \(J_{\mathrm{n}}=[\beta_3,\infty)\).
The interval membership used below is explicit.
Since
\begin{gather*}
F(-9/8)<0,
\quad F(-1)>0,
\quad F(0)>0,\\
F(1/2)<0,
\quad F(3/4)<0,
\quad F(1)>0,
\end{gather*}
and \(F'(\lambda)=12\lambda^2-4\), the roots satisfy
\[
-\frac98<\beta_1<-1,
\qquad
0<\beta_2<\frac12,
\qquad
\frac34<\beta_3<1.
\]
Equation~\eqref{eq:ramification-poly} shows that the three finite ramification abscissae are $\lambda\in\{-1,1,\alpha\}$, where $\alpha\in(-\tfrac12,0)$ is the real root of $16\lambda^3-9\lambda^2+1$.
The cubic factor has exactly one real root because its derivative is \(\lambda(48\lambda-18)\), its local values at \(0\) and \(3/8\) are positive, and its limits at \(\pm\infty\) have opposite signs.
Since \(16(-1/2)^3-9(-1/2)^2+1<0\) and the value at \(0\) is positive, this root \(\alpha\) lies in \((-1/2,0)\).
Thus \(-1,\alpha\in J_{\mathrm c}\) and \(1\in J_{\mathrm n}\), while no other real ramification abscissa lies on either oval.

At a finite endpoint \(F(\beta_i)=0\), one has \(Y=0\), hence
\[
y(\beta_i,0)=\beta_i^2-\frac12.
\]
The displayed root bounds give
\[
\frac12<\beta_1^2-\frac12<\frac{49}{64}<1,
\qquad
-\frac12<\beta_2^2-\frac12<-\frac14,
\qquad
\frac{1}{16}<\beta_3^2-\frac12<\frac12.
\]
At the ramification abscissae the critical-value formula gives
\[
y(-1)=1,
\qquad
y(1)=0,
\qquad
y(\alpha)=y_{\mathrm{sub}}
=\frac{4\alpha^3-3\alpha^2-2\alpha+1}{4\alpha}.
\]
By Lemma~\ref{lem:cubic-branch-real-root}, this last number is the unique real zero of \(256y^3+411y^2+165y+32\), hence \(y_{\mathrm{sub}}\in(-2,-1)\).
Theorem~\ref{thm:balanced-real-projection} now shows that the displayed endpoint values and these three critical values are the complete finite obstruction framework for the real projection, giving precisely the two sets \(\mathcal V(J_{\mathrm c})\) and \(\mathcal V(J_{\mathrm n})\) in the statement.
Therefore on \(J_{\mathrm c}\) the finite endpoint and critical candidates are ordered within
\[
y_{\mathrm{sub}}< -1 < \beta_2^2-\frac12<0<\beta_1^2-\frac12<1,
\]
together with the critical maximum \(1\) at \(\lambda=-1\).
Thus \(y(E_{\mathrm c}(\RR))=[y_{\mathrm{sub}},1]\).

On \(J_{\mathrm n}\), the finite candidates are only \(0\) and
\(\beta_3^2-1/2\in(1/16,1/2)\).
The endpoint value is therefore strictly positive, while the only interior critical value on \(J_{\mathrm n}\) is \(0\) at \(\lambda=1\).
Hence the minimum is attained at \(\lambda=1\), with value \(0\).
The pole at infinity forces the image to be unbounded above, so \(y(E_{\mathrm{n}}(\RR))=[0,\infty)\).
Counting the number of real preimages in the open intervals follows from continuity and the fact that the ramification points contribute multiplicity two.
\end{proof}

\begin{corollary}[Real images for the canonical specialization]\label{cor:real-fibers-specialization}
For the specialized quartic, the compact real oval maps to \([y_{\mathrm{sub}},1]\), the noncompact real oval maps to \([0,\infty)\), and the total real image is \([y_{\mathrm{sub}},\infty)\).
The definitive real-root count derived from these two images is Theorem~\ref{thm:theorem-b-real-projection-criterion}; the definitive branch-value and dominance classification is Theorem~\ref{thm:canonical-branch-dominance}.
\end{corollary}

\begin{proof}
This is exactly Proposition~\ref{prop:quartic-real-projection-framework}: the compact component has extrema \(y_{\mathrm{sub}}\) and \(1\), while the noncompact component has finite minimum \(0\) and a pole at infinity.
The final counting table is not repeated here; it follows in Theorem~\ref{thm:theorem-b-real-projection-criterion} by applying the two-to-one covering property over the interiors of these images.
\end{proof}

\begin{remark}\label{rem:balanced-family-generality}
Theorem~\ref{thm:balanced-real-projection} is stated with balanced parameters because the proof is no harder in that notation and because the specialization requires exactly this finite comparison for one fixed smooth member.
In the present paper its role is narrower: it reduces the real \(y\)-projection of \(P_{-1,-1,1,0}\) to branch values and critical values, after which Proposition~\ref{prop:quartic-real-projection-framework} performs the needed specialization.
No later argument uses, or asserts, a global chamber classification of balanced quartic projections as the parameters vary.
\end{remark}

\subsection{Double-root normalization inside the balanced family}

Two elementary operations preserve the balanced form: a shift \(y\mapsto y+\beta\) and a simultaneous scaling \((\lambda,y)\mapsto(L\lambda,L^2 y)\).
They allow us to place a chosen double root at \(y=0\) with eigenvalue \(1\) and then to fix one further real branch value.

\begin{lemma}[Shift and scaling]\label{lem:balanced-affine}
Let $P_{a,c,d,g}$ be given by \eqref{eq:balanced-family}.
\begin{enumerate}
\item For any $\beta\in\RR$, the polynomial
\[
\widehat P(\lambda,y):=P_{a,c,d,g}(\lambda,y+\beta)
\]
equals $P_{a,c',d,g'}(\lambda,y)$ with $c'=c-2\beta$ and $g'=g+\beta(\beta-c)$.
\item For any $L\neq 0$, the change of variables $(\lambda,y)=(L\widetilde\lambda,L^2\widetilde y)$ yields
\[
P_{a,c,d,g}(L\widetilde\lambda,L^2\widetilde y)=L^4 P_{\widetilde a,\widetilde c,\widetilde d,\widetilde g}(\widetilde\lambda,\widetilde y),
\]
where $\widetilde a=a/L$, $\widetilde c=c/L^2$, $\widetilde d=d/L^3$, and $\widetilde g=g/L^4$.
\end{enumerate}
\end{lemma}

\begin{proof}
Both statements follow from direct substitution.
For (1), expand $(y+\beta)((y+\beta)-c)+g=y(y-(c-2\beta))+\beta(\beta-c)+g$ and note that the remaining coefficients do not change.
For (2), divide by $L^4$ after substituting $(L\widetilde\lambda,L^2\widetilde y)$ into \eqref{eq:balanced-family}.
\end{proof}

Lemma~\ref{lem:balanced-affine} shows that the algebraic double-root data can be shifted so that the chosen collision height is $y=0$ and scaled so that the chosen nonzero colliding eigenvalue equals $1$.

\begin{definition}[Normalized collision data]\label{def:normalized-collision-data}
Let $P_{a,c,d,g}$ be a balanced polynomial and let $(\rho,u)\in\RR^\times\times\RR$.
We say that $P_{a,c,d,g}$ has normalized collision data $(\rho,u)$ if the only normalization used is
\[
\lambda=\rho\widetilde\lambda,
\qquad
y=u+\rho^2\widetilde y,
\]
and the two points
\[
(\lambda,y)=(\rho,u),
\qquad
(\lambda,y)=(-\rho,u+\rho^2)
\]
are double roots in their corresponding $\lambda$-fibres, equivalently
\[
P(\rho,u)=P_\lambda(\rho,u)=P(-\rho,u+\rho^2)=P_\lambda(-\rho,u+\rho^2)=0.
\]
The normalized coordinates of these two collisions are therefore $(1,0)$ and $(-1,1)$.
\end{definition}

The following fixed-coordinate lemma solves the algebraic constraints used inside the normalized specialization theorem when the neighbouring normalized double root is fixed at $\widetilde y=1$.

\begin{lemma}[Fixed-coordinate double-root normalization]\label{lem:balanced-classification}
Let $a,c,d,g\in\RR$.
Assume that the balanced polynomial $P_{a,c,d,g}$ has double roots at $(\lambda,y)=(1,0)$ and $(\lambda,y)=(-1,1)$.
Then $(a,c,d,g)=(-1,-1,1,0)$.
\end{lemma}

\begin{proof}
Set $Q(\lambda,y)=P_{a,c,d,g}(\lambda,y)$.
The two double-root conditions mean that $Q(1,0)=Q_\lambda(1,0)=Q(-1,1)=Q_\lambda(-1,1)=0$.
Direct evaluation gives four linear equations:
\[
1+a+c+d+g=0,\qquad
4+3a+2c+d=0,
\]
\[
-a-d+g=0,\qquad
3a-2c+d=0.
\]
Solving yields $c=-1$, $a=-1$, $d=1$, and $g=0$.
\end{proof}

Lemma~\ref{lem:balanced-classification} is the algebraic proof component used to prove the next result, which is the normalized double-root specialization stated as Theorem~C in the introduction.

\begin{theorem}[Algebraic normalized double-root specialization (Theorem C)]\label{thm:canonical-specialization-up-to-normalization}
Let $u\in\RR$ and $\rho\in\RR^\times$, and let $P_{a,c,d,g}$ be a balanced polynomial.
This theorem is an algebraic selection theorem only: it identifies the unique normalized balanced member forced by the two double-root constraints, but it does not assert that either collision is dominant.
Dominance of the positive normalized collision is the separate Theorem~\ref{thm:canonical-branch-dominance}; it is carried back only by the local bridge Corollary~\ref{cor:canonical-dominance-after-normalization} and by the post-Theorem-D linkage Corollary~\ref{cor:normalized-branch-classification} after Theorem~D is invoked.
The only allowed normalization is the shift by the chosen collision height followed by the weighted dilation
\begin{equation}\label{eq:canonical-normalization-change}
\lambda=\rho\widetilde\lambda,
\qquad
y=u+\rho^2\widetilde y,
\end{equation}
so that the chosen collision is sent to $(\widetilde\lambda,\widetilde y)=(1,0)$.
The following conditions are equivalent.
\begin{enumerate}
\item The polynomial $P_{a,c,d,g}$ has normalized collision data $(\rho,u)$ in the sense of Definition~\ref{def:normalized-collision-data}.
\item The algebraic double-root constraints hold at the two points
\[
(\lambda,y)=(\rho,u),
\qquad
(\lambda,y)=(-\rho,u+\rho^2),
\]
that is,
\[
P(\rho,u)=P_\lambda(\rho,u)=P(-\rho,u+\rho^2)=P_\lambda(-\rho,u+\rho^2)=0.
\]
\end{enumerate}
When these equivalent conditions hold, the weighted affine normalization forces
\begin{equation}\label{eq:canonical-normalization-equation}
P_{a,c,d,g}(\rho\widetilde\lambda,u+\rho^2\widetilde y)
=
\rho^4 P_{-1,-1,1,0}(\widetilde\lambda,
\widetilde y),
\end{equation}
and hence the original parameters are uniquely
\begin{equation}\label{eq:canonical-normalization-parameters}
(a,c,d,g)=(-\rho,\,2u-\rho^2,\,\rho^3,\,u(u-\rho^2)).
\end{equation}
Conversely, the parameter quadruple in \eqref{eq:canonical-normalization-parameters} has exactly this normalized collision data and satisfies \eqref{eq:canonical-normalization-equation}.
Thus, after the prescribed shift and weighted dilation, the forced balanced member is the canonical specialization $P_{-1,-1,1,0}=\Pi$.
No maximal-modulus or dominance hypothesis is part of this theorem; those spectral assertions are proved later only after the canonical algebraic specialization has been identified.
\end{theorem}

\begin{proof}
Apply the shift part of Lemma~\ref{lem:balanced-affine} with $\beta=u$ and then the scaling part with $L=\rho$.
The transformed polynomial is
\[
P_{a,c,d,g}(\rho\widetilde\lambda,u+\rho^2\widetilde y)
=
\rho^4
P_{a/\rho,
(c-2u)/\rho^2,
d/\rho^3,
(g+u(u-c))/\rho^4}
(\widetilde\lambda,\widetilde y).
\]
Because the change of variables \eqref{eq:canonical-normalization-change} is invertible and has nonzero Jacobian determinant \(\rho^3\), multiplicity of a root in the \(\lambda\)-fibre is preserved: for fixed \(y\), a point is a double root of the quartic in \(\lambda\) if and only if its normalized image is a double root of the normalized quartic in \(\widetilde\lambda\).
Definition~\ref{def:normalized-collision-data} is precisely the assertion that the original points $(\rho,u)$ and $(-\rho,u+\rho^2)$ become the normalized double roots $(1,0)$ and $(-1,1)$ under \eqref{eq:canonical-normalization-change}.
Thus the definition is equivalent to the displayed algebraic double-root constraints.
Therefore Lemma~\ref{lem:balanced-classification} forces the transformed parameter quadruple to be $(-1,-1,1,0)$.
Solving
\[
\frac a\rho=-1,
\qquad
\frac{c-2u}{\rho^2}=-1,
\qquad
\frac d{\rho^3}=1,
\qquad
\frac{g+u(u-c)}{\rho^4}=0
\]
gives exactly \eqref{eq:canonical-normalization-parameters}.
Conversely, if \eqref{eq:canonical-normalization-parameters} holds, then the displayed transformed parameter quadruple is $(-1,-1,1,0)$, which proves \eqref{eq:canonical-normalization-equation}.
Lemma~\ref{lem:balanced-classification} and its proof show that $P_{-1,-1,1,0}$ has double roots at $(1,0)$ and $(-1,1)$, and the inverse of \eqref{eq:canonical-normalization-change} carries them to $(\rho,u)$ and $(-\rho,u+\rho^2)$.
\end{proof}

\begin{lemma}[Real weighted pullback of positive-side dominance]\label{lem:real-weighted-pullback-dominance}
Let $u\in\RR$, let $\rho\in\RR^\times$, and suppose that
\[
P_{a,c,d,g}(\rho\widetilde\lambda,u+\rho^2\widetilde y)
=
\rho^4\Pi(\widetilde\lambda,\widetilde y).
\]
Assume that, for some interval $I\subset\RR$, a branch $\widetilde\lambda_*(\widetilde y)$ of the canonical fibre $\Pi(\cdot,\widetilde y)$ is simple and has strictly maximal modulus for every $\widetilde y\in I$.
This lemma has no independent canonical dominance input; it is only the invariance of already-proved modulus inequalities under the real weighted coordinate change.
Then
\[
\lambda_*(y)=\rho\widetilde\lambda_*\!\left(\frac{y-u}{\rho^2}\right),
\qquad
y\in u+\rho^2 I,
\]
is a simple strictly maximal-modulus branch of the original fibre $P_{a,c,d,g}(\cdot,y)$.
Moreover, any canonical real branch value $\widetilde y_0$ whose colliding pair is not of maximal modulus pulls back to $y_0=u+\rho^2\widetilde y_0$ with the same non-dominance property.
\end{lemma}

\begin{proof}
For fixed $\widetilde y$, the displayed identity gives a multiplicity-preserving bijection between roots of $\Pi(\cdot,\widetilde y)$ and roots of $P_{a,c,d,g}(\cdot,u+\rho^2\widetilde y)$ by $\lambda=\rho\widetilde\lambda$.
Indeed, multiplying the polynomial by the nonzero scalar $\rho^4$ does not change its roots, and the affine change in the fibre coordinate has nonzero derivative $\rho$.
Thus a simple canonical root maps to a simple original root.
For every two roots in the corresponding fibres, their moduli are both multiplied by the same positive factor $\abs\rho$.
Strict maximal-modulus inequalities are therefore preserved and reflected by the pullback.
The same comparison proves the final assertion: if a canonical colliding pair has modulus strictly smaller than another canonical root or pair at a real branch value, then after multiplying all roots by $\rho$ the same strict inequality holds in the original fibre over $u+\rho^2\widetilde y_0$.
\end{proof}

\begin{corollary}[Real weighted normalization and dominance]\label{cor:canonical-dominance-after-normalization}
In the situation of Theorem~\ref{thm:canonical-specialization-up-to-normalization}, suppose the chosen collision is the positive normalized one, so that after the change of variables \eqref{eq:canonical-normalization-change} the colliding point is $(\widetilde\lambda,\widetilde y)=(1,0)$ and the positive side is $\widetilde y>0$.
The double-root normalization itself is purely algebraic: it only identifies the normalized polynomial as $P_{-1,-1,1,0}$.
This corollary records only preservation of root and modulus comparisons under that algebraic normalization; the dominance content is Theorem~\ref{thm:canonical-branch-dominance}, which is the canonical citation for all branch-dominance claims.
For the canonical specialization, the collision at $(1,0)$ is the unique maximal-modulus square-root endpoint for the canonical branch on the positive side:
for every $\widetilde y>0$, the positive real root $\lambda_{\mathrm{dom}}(\widetilde y)$ of $P_{-1,-1,1,0}(\cdot,\widetilde y)$ is simple and satisfies
\[
\abs{\lambda_{\mathrm{dom}}(\widetilde y)}
>
\abs{\lambda}
\]
for every other root $\lambda$ of $P_{-1,-1,1,0}(\cdot,\widetilde y)$.
Equivalently, for this real nonzero scaling and only on the carried positive side, the original-variable sheet
\[
y=u+\rho^2\widetilde y,
\qquad
\lambda=\rho\lambda_{\mathrm{dom}}(\widetilde y)
\qquad(\widetilde y>0)
\]
is the unique maximal-modulus sheet of $P_{a,c,d,g}(\cdot,y)$ on the side $y>u$, and the endpoint $(\lambda,y)=(\rho,u)$ is dominant relative to its neighbouring positive-side fibres.
No assertion is made here for complex scalings, for arbitrary balanced members, or for the opposite side $y<u$.
The remaining normalized real branch values, $\widetilde y=1$ and $\widetilde y=\ySub$, are non-dominant in the sense recorded in Theorem~\ref{thm:canonical-branch-dominance}.
\end{corollary}

\begin{proof}
Theorem~\ref{thm:canonical-specialization-up-to-normalization} supplies only the algebraic normalization step: after the prescribed shift and weighted dilation, the polynomial is $P_{-1,-1,1,0}$, which is the specialized quartic $\Pi$.
No dominance assertion is used in that classification.
The endpoint statement at $(1,0)$ and the non-dominance of the other two real branch values are the separate canonical-specialization conclusions of Theorem~\ref{thm:canonical-branch-dominance}(2).
The strict maximal-modulus inequality for every $\widetilde y>0$ is Theorem~\ref{thm:canonical-branch-dominance}(4), equivalently Lemma~\ref{lem:positive-axis-dominance-criterion}.
Lemma~\ref{lem:real-weighted-pullback-dominance} then pulls those Theorem~D statements through the real weighted normalization \eqref{eq:canonical-normalization-change}, giving exactly the displayed original-variable sheet and endpoint.
\end{proof}

\subsection{Explicit elimination example}

The symbolic elimination that produces the ramification quintic \eqref{eq:balanced-quintic} can be displayed concretely.
Starting from \eqref{eq:balanced-ramification-eq} and \eqref{eq:balanced-weierstrass}, eliminate $Y$ as follows.
Equation \eqref{eq:balanced-ramification-eq} gives $Y=(3a\lambda^2+d)/(2\lambda)$, provided $\lambda\neq 0$ (the $\lambda=0$ case is trivial and leads again to $d=0$, excluded here).
Substituting into $Y^2=c^2-4a\lambda^3-4d\lambda-4g$ yields
\[
\frac{(3a\lambda^2+d)^2}{4\lambda^2}=c^2-4a\lambda^3-4d\lambda-4g.
\]
Clearing denominators produces
\[
(3a\lambda^2+d)^2=4\lambda^2\bigl(c^2-4a\lambda^3-4d\lambda-4g\bigr).
\]
Expanding both sides and collecting powers of $\lambda$ gives the degree-$5$ polynomial
\[
16a\lambda^5+9a^2\lambda^4+16d\lambda^3+(16g+6ad-4c^2)\lambda^2+d^2,
\]
which is exactly \eqref{eq:balanced-quintic}.
After the specialization \((a,c,d,g)=(-1,-1,1,0)\), this polynomial becomes
\[
-16\lambda^5+9\lambda^4+16\lambda^3-10\lambda^2+1
=
-(\lambda-1)(\lambda+1)(16\lambda^3-9\lambda^2+1),
\]
which is the factorization used later in \eqref{eq:ramification-poly}.
All central eliminations used below are displayed either here or in the proof component Appendix~\ref{app:disc}.  In particular, Corollary~\ref{cor:disc-factor} uses the expanded Sylvester determinant \eqref{eq:disc-expanded-polynomial}, not numerical sampling.

\subsection{Specialization to the quartic transfer family}

\begin{theorem}[Affine elliptic model and branch geometry]\label{thm:elliptic-reduction}
The spectral polynomial \(\Pi(\lambda,y)\) from \eqref{eq:spectral-poly} is the specialization
\[
\Pi(\lambda,y)=P_{-1,-1,1,0}(\lambda,y).
\]
Under the substitution
\begin{equation}\label{eq:xi-def}
\xi = y - \lambda^2, \qquad Y = 2\xi + 1 = 2y + 1 - 2\lambda^2,
\end{equation}
the affine spectral curve \(\Pi(\lambda,y)=0\) is isomorphic to the affine elliptic model
\begin{equation}\label{eq:elliptic-xi}
\xi^2 + \xi = \lambda^3 - \lambda,
\end{equation}
or equivalently
\begin{equation}\label{eq:elliptic-weierstrass}
Y^2 = 4\lambda^3 - 4\lambda + 1.
\end{equation}
Its Weierstrass invariants are
\begin{equation}\label{eq:weierstrass-invariants}
g_2 = 4, \qquad g_3 = -1, \qquad
\Delta = g_2^3 - 27g_3^2 = 37, \qquad
j = \frac{1728\,g_2^3}{\Delta} = \frac{110592}{37}.
\end{equation}
The function
\begin{equation}\label{eq:y-from-E}
y = \lambda^2 + \xi = \lambda^2 - \tfrac{1}{2} + \tfrac{1}{2}Y
\end{equation}
defines a degree-\(4\) map \(E\to \mathbf P^1_y\).
Its finite branch values are exactly
\begin{equation}\label{eq:branch-values}
y=0,\qquad y=1,\qquad c(y)=0,
\quad c(y)=256y^3+411y^2+165y+32.
\end{equation}
We shall write \(\ySub\) for the unique real root of \(c\), and \(\eta,\overline\eta\) for the two non-real roots of \(c\).
Thus
\begin{equation}\label{eq:branch-value-convention}
\Branch_{\mathrm{fin}}
=
\{0,1,\ySub,\eta,\overline\eta\},
\qquad
c(\ySub)=c(\eta)=c(\overline\eta)=0,
\qquad
\eta\notin\RR.
\end{equation}
This naming convention is used throughout the real classification and the positive-real maximal-modulus analysis.
\end{theorem}

\begin{proof}
Substituting \((a,c,d,g)=(-1,-1,1,0)\) into \eqref{eq:balanced-family} gives
\begin{equation}\label{eq:Pi-as-quadratic-in-y}
\Pi(\lambda,y)
=
y^2+(1-2\lambda^2)y+\lambda^4-\lambda^3-\lambda^2+\lambda.
\end{equation}
Equivalently,
\[
\Pi(\lambda,y)
=
(y-\lambda^2)^2+(y-\lambda^2)-\lambda^3+\lambda.
\]
Thus Proposition~\ref{prop:balanced-family} gives the affine isomorphism \eqref{eq:elliptic-xi}--\eqref{eq:elliptic-weierstrass} and the formula \eqref{eq:y-from-E}.

The smoothness condition follows from Corollary~\ref{cor:balanced-smooth}:
\[
64d^3+27a(c^2-4g)^2
=
64\cdot 1^3+27(-1)(1-0)^2
=
37\neq 0.
\]
Hence the projective completion is genus one.
The invariants in \eqref{eq:weierstrass-invariants} are read off directly from \(Y^2=4\lambda^3-4\lambda+1\).
The degree statement follows from Proposition~\ref{prop:balanced-family}.

For the finite ramification points, Corollary~\ref{cor:balanced-ramification} gives
\[
-16\lambda^5+9\lambda^4+16\lambda^3-10\lambda^2+1=0.
\]
Multiplying by \(-1\) and factoring yields
\begin{equation}\label{eq:ramification-poly}
(\lambda - 1)(\lambda + 1)(16\lambda^3 - 9\lambda^2 + 1)=0.
\end{equation}
The corresponding branch values are obtained from
\[
y = \lambda^2 + \xi = \frac{4\lambda^3 - 3\lambda^2 - 2\lambda + 1}{4\lambda}.
\]
At \(\lambda=1\), this gives \(y=0\); at \(\lambda=-1\), it gives \(y=1\).
For the cubic factor, the relation between \(\lambda\) and its image \(y\) is
\[
q(\lambda,y):=4y\lambda-(4\lambda^3-3\lambda^2-2\lambda+1)=0.
\]
Lemma~\ref{lem:cubic-branch-elimination} eliminates \(\lambda\) from \(16\lambda^3-9\lambda^2+1=0\) and \(q(\lambda,y)=0\).  Hence the remaining finite branch values are exactly the roots of \(c(y)=256y^3+411y^2+165y+32\).
\end{proof}

\begin{lemma}[Cubic branch elimination]\label{lem:cubic-branch-elimination}
Let
\[
p(\lambda)=16\lambda^3-9\lambda^2+1,
\qquad
q(\lambda,y)=4y\lambda-(4\lambda^3-3\lambda^2-2\lambda+1).
\]
Then
\begin{equation}\label{eq:cubic-branch-elimination-identity}
\Res_\lambda\bigl(p(\lambda),q(\lambda,y)\bigr)
=
-64\bigl(256y^3+411y^2+165y+32\bigr).
\end{equation}
Consequently, a point on the cubic ramification factor maps to the \(y\)-line only over the three roots of \(256y^3+411y^2+165y+32\), and each of those roots occurs as such an image.
\end{lemma}

\begin{proof}
The resultant in \eqref{eq:cubic-branch-elimination-identity} is the Sylvester determinant
\[
\det
\begin{pmatrix}
16&-9&0&1&0&0\\
0&16&-9&0&1&0\\
0&0&16&-9&0&1\\
-4&3&4y+2&-1&0&0\\
0&-4&3&4y+2&-1&0\\
0&0&-4&3&4y+2&-1
\end{pmatrix}.
\]
Expanding the displayed determinant by fraction-free elementary row-reduction over \(\ZZ[y]\) gives
\[
-16384y^3-26304y^2-10560y-2048
=
-64(256y^3+411y^2+165y+32),
\]
which proves the displayed identity.  This is the complete elimination criterion used here: the matrix is the Sylvester matrix of the two displayed polynomials, and no unrecorded computer-algebra normalization is being invoked.  By the defining property of the resultant, a common zero of \(p(\lambda)\) and \(q(\lambda,y)\) can occur only when this cubic in \(y\) vanishes.  Conversely, if the resultant vanishes, then the two positive-degree polynomials \(p\) and \(q\) have a common zero over the algebraic closure, so every root of the displayed cubic is the image of at least one point on the cubic ramification factor.
\end{proof}

\begin{lemma}[Simplicity of the finite ramification locus]\label{lem:finite-ramification-simple}
All five zeros of
\begin{equation}\label{eq:finite-ramification-framework}
R(\lambda):=(\lambda-1)(\lambda+1)(16\lambda^3-9\lambda^2+1)
\end{equation}
are simple.
Consequently the five finite ramification points of the map \(y:E\to\mathbf P^1_y\) are simple.
\end{lemma}

\begin{proof}
Let \(p(\lambda)=16\lambda^3-9\lambda^2+1\).
The two linear factors in \eqref{eq:finite-ramification-framework} are not factors of \(p\), since
\[
p(1)=8\neq0,
\qquad
p(-1)=-24\neq0.
\]
It remains to show that the cubic factor has no multiple zero.
Its derivative is
\[
p'(\lambda)=48\lambda^2-18\lambda=6\lambda(8\lambda-3).
\]
A common zero of \(p\) and \(p'\) would have \(\lambda=0\) or \(\lambda=3/8\), but
\[
p(0)=1\neq0,
\qquad
p\!\left(\frac38\right)=\frac{5}{32}\neq0.
\]
Thus \(\gcd(p,p')=1\), and no zero of \(p\) is multiple.
Together with \(p(\pm1)\neq0\), this proves \(\gcd(R,R')=1\), so the five roots of \(R\) are simple.

At a finite point of the smooth curve \(E\), ramification of \(y\) is given by \(\dd y=0\), and the displayed polynomial \(R\) is the resulting critical-point equation from Corollary~\ref{cor:balanced-ramification} after the specialization \((a,c,d,g)=(-1,-1,1,0)\).
Since its roots are simple, the differential \(\dd y\) has a simple zero at each of the five finite ramification points.
Equivalently each finite ramification index is \(2\), so every finite ramification point is simple.
\end{proof}

\begin{lemma}[Projective normalization of the quartic]\label{lem:projective-normalization}
Let \(\overline C\subset \mathbf P^2_{[\Lambda:W:Z]}\) be the projective closure of the affine quartic \(\Pi(\lambda,y)=0\), namely
\[
\overline C=\{F(\Lambda,W,Z)=0\},
\]
where
\begin{equation}\label{eq:projective-quartic}
F(\Lambda,W,Z)
=
\Lambda^4-\Lambda^3Z-(2WZ+Z^2)\Lambda^2+\Lambda Z^3+W(W+Z)Z^2.
\end{equation}
Then \(\overline C\) has a unique point at infinity, namely \([0:1:0]\), and this point is singular.
The morphism
\begin{equation}\label{eq:normalization-map}
\nu:E\longrightarrow \overline C,
\qquad
(\lambda,Y)\longmapsto
\left[\lambda:\lambda^2-\tfrac12+\tfrac12Y:1\right]
\end{equation}
extends across the point at infinity \(O\in E\), satisfies
\[
\nu^{-1}([0:1:0])=\{O\},
\]
and is finite and birational.
Consequently, \(E\) is the normalization of \(\overline C\).
\end{lemma}

\begin{proof}
Homogenizing \(\Pi(\lambda,y)\) gives \eqref{eq:projective-quartic}.
On the line \(Z=0\), one has \(F(\Lambda,W,0)=\Lambda^4\), so the only point at infinity is \([0:1:0]\).
The partial derivatives are
\[
F_\Lambda=4\Lambda^3-3\Lambda^2Z-2(2WZ+Z^2)\Lambda+Z^3,
\]
\[
F_W=-2Z\Lambda^2+(2W+Z)Z^2,
\]
\[
F_Z=-\Lambda^3-2(W+Z)\Lambda^2+3\Lambda Z^2+2W^2Z+3WZ^2,
\]
and all three vanish at \([0:1:0]\), so this point is singular.

On the affine chart \(Z=1\), the map \(\nu\) is inverse to the rational map
\[
(\lambda,y)\longmapsto (\lambda,Y),\qquad Y=2y+1-2\lambda^2,
\]
between the affine quartic \(\Pi(\lambda,y)=0\) and the affine cubic \(E:Y^2=4\lambda^3-4\lambda+1\).
Hence \(\nu\) is birational on dense affine opens.

Let \(O\in E\) be the point at infinity on the smooth projective cubic.
For a Weierstrass cubic one has
\[
\operatorname{ord}_O(\lambda)=-2,
\qquad
\operatorname{ord}_O(Y)=-3
\]
\cite{Silverman2009,Miranda1995}.
Since
\[
y=\lambda^2-\frac12+\frac12Y,
\]
it follows that
\[
\operatorname{ord}_O(y)=\min\{-4,-3,0\}=-4.
\]
Therefore \(\lambda/y\) and \(1/y\) are regular at \(O\) and vanish there.
Writing
\[
\nu=[\lambda:y:1]=[\lambda/y:1:1/y]
\]
shows that \(\nu\) extends to \(O\) and sends \(O\) to \([0:1:0]\).
Conversely, any point of \(E\) mapping to \(Z=0\) must be a pole of \(y\), and \(y\) has its unique pole at \(O\).
Thus \(\nu^{-1}([0:1:0])=\{O\}\).

Because \(E\) is projective and \(\nu\) is a nonconstant morphism onto the irreducible curve \(\overline C\), the map \(\nu\) is finite.
It is already birational on the affine loci, so \(\nu\) is a finite birational morphism.
Since \(E\) is smooth, it is normal, and therefore it is the normalization of \(\overline C\).
\end{proof}

\subsection{Riemann--Hurwitz consistency}\label{subsec:riemann-hurwitz}

The specialization above admits a strong geometric consistency check via the Riemann--Hurwitz formula \cite{Miranda1995}.

\begin{corollary}\label{cor:riemann-hurwitz}
The total ramification index of the degree-\(4\) map \(E \to \mathbf{P}^1_y\) equals \(8\), in agreement with the Riemann--Hurwitz formula.
\end{corollary}

\begin{proof}
The Riemann--Hurwitz formula for a degree-\(d\) map from a curve of genus \(g\) to \(\mathbf P^1\) gives
\[
2g-2=d(-2)+\sum_P(e_P-1).
\]
With \(g=1\) and \(d=4\), the total ramification must be \(8\).
At infinity the map has a single totally ramified point, contributing \(4-1=3\).
By Lemma~\ref{lem:finite-ramification-simple}, the five finite ramification points in \eqref{eq:ramification-poly} are simple, and each contributes \(1\).
Hence the finite contribution is \(5\), and \(3+5=8\).
\end{proof}

\subsection{Discriminant factorization as a corollary}\label{subsec:disc-corollary}

The branch locus of the degree-\(4\) covering coincides with the vanishing locus of the \(\lambda\)-discriminant.

\begin{lemma}[Finite repeated roots and ramification]\label{lem:repeated-root-ramification}
Let \(P=(\lambda_0,Y_0)\in E\) be a point with finite \(y\)-value \(y_0\).
Then the following are equivalent:
\begin{enumerate}
\item \(\lambda_0\) is a repeated root of the quartic \(\Pi(\cdot,y_0)\).
\item \(P\) is a finite ramification point of the map \(y:E\to \mathbf P^1_y\).
\end{enumerate}
\end{lemma}

\begin{proof}
On the affine quartic one has
\begin{equation}\label{eq:Pi-y-equals-Y}
\Pi_y(\lambda,y)=2y+1-2\lambda^2=Y.
\end{equation}
Away from \(Y=0\), implicit differentiation of \(\Pi(\lambda,y)=0\) therefore gives
\[
\frac{\dd y}{\dd\lambda}
=
-\frac{\Pi_\lambda(\lambda,y)}{\Pi_y(\lambda,y)}
=
-\frac{\Pi_\lambda(\lambda,y)}{Y}.
\]
Thus, whenever \(Y_0\neq 0\), the conditions \(\Pi(\lambda_0,y_0)=0=\Pi_\lambda(\lambda_0,y_0)\) and \(\dd y/\dd\lambda(P)=0\) are equivalent.

It remains to rule out the case \(Y_0=0\) at a finite repeated root.
If \(Y_0=0\), then \(2y_0+1=2\lambda_0^2\), hence
\[
\Pi_\lambda(\lambda_0,y_0)
=
4\lambda_0^3-3\lambda_0^2-2(2y_0+1)\lambda_0+1
=
-3\lambda_0^2+1.
\]
Therefore \(\Pi_\lambda(\lambda_0,y_0)=0\) would force \(\lambda_0=\pm 1/\sqrt3\).
But then
\[
Y_0^2=4\lambda_0^3-4\lambda_0+1
=
1\mp \frac{8}{3\sqrt3}\neq 0,
\]
contradicting \(Y_0=0\).
So \(Y_0\neq 0\) at every finite repeated root, and the equivalence follows.
\end{proof}

\begin{corollary}[Discriminant factorization]\label{cor:disc-factor}
The discriminant of \(\Pi(\lambda,y)\) with respect to \(\lambda\) factors as
\begin{equation}\label{eq:disc-factor-new}
\Disc_\lambda \Pi(\lambda,y) = -y(y-1)\bigl(256y^3 + 411y^2 + 165y + 32\bigr).
\end{equation}
\end{corollary}

\begin{proof}
By Lemma~\ref{lem:repeated-root-ramification}, a finite value \(y_0\) is a branch value of \(y:E\to \mathbf P^1_y\) if and only if \(\Pi(\cdot,y_0)\) has a repeated root.
The equivalence there includes the exclusion of the only possible denominator locus in the implicit derivative, namely \(Y=\Pi_y=0\), so no finite branch value is lost before passing to the resultant.
Equivalently, the finite branch values are exactly the zeros of \(\Disc_\lambda\Pi(\lambda,y)\).
Theorem~\ref{thm:elliptic-reduction} identifies them as \(y=0\), \(y=1\), and the roots of \(c(y)=256y^3+411y^2+165y+32\).
The computation in Appendix~\ref{app:disc} displays the Sylvester matrix for \(\Res_\lambda(\Pi,\partial_\lambda\Pi)\) and expands it in \eqref{eq:disc-expanded-polynomial} as \(-256y^5-155y^4+246y^3+133y^2+32y\), so \(\Disc_\lambda\Pi\) is a degree-\(5\) polynomial in \(y\) with leading coefficient \(-256\).
These roots and the leading coefficient determine the factorization uniquely, giving \eqref{eq:disc-factor-new}.
\end{proof}

\begin{corollary}[Canonical spectral-quartic specialization]\label{cor:canonical-theorem-a-specialization}
For the spectral quartic \(\Pi(\lambda,y)\), Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization} specializes as follows.
The normalized double-root conditions select the unique balanced member
\[
\Pi=P_{-1,-1,1,0}
\]
of the family \eqref{eq:balanced-family}.
The affine substitution
\[
\xi=y-\lambda^2,
\qquad
Y=2y+1-2\lambda^2
\]
identifies \(\Pi=0\) with the elliptic curve
\[
E:\quad Y^2=4\lambda^3-4\lambda+1,
\qquad
y=\lambda^2-\frac12+\frac12Y,
\]
and the projective closure of \(\Pi=0\) is normalized by this \(E\).
The map \(y:E\to\mathbf P^1_y\) has degree \(4\), finite branch values
\[
0,
\quad 1,
\quad \text{and the three roots of }256y^3+411y^2+165y+32,
\]
and discriminant factorization
\[
\Disc_\lambda\Pi(\lambda,y)=-y(y-1)(256y^3+411y^2+165y+32).
\]
Thus the canonical specialization, projective normalization, branch values, and discriminant factorization are the spectral-quartic realization of the balanced normalization theorem.
\end{corollary}

\begin{proof}
The double-root selection is Theorem~\ref{thm:canonical-specialization-up-to-normalization} applied with the fixed normalized collisions \((\lambda,y)=(1,0)\) and \((-1,1)\), equivalently its fixed-coordinate input Lemma~\ref{lem:balanced-classification}.
The affine model, the formula for \(y\), the degree of the cover, and the finite branch values are exactly Theorem~\ref{thm:elliptic-reduction}, which is the specialization of Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization} at \((a,c,d,g)=(-1,-1,1,0)\).
Theorem~\ref{thm:elliptic-reduction} gives the smooth cubic model \(E\), and Lemma~\ref{lem:projective-normalization} proves that the resulting morphism from \(E\) to the projective quartic closure is finite and birational with \(E\) normal, hence is the normalization.
Finally, Corollary~\ref{cor:disc-factor} identifies the zeros of \(\Disc_\lambda\Pi\) with the finite branch values and fixes the leading coefficient from the Sylvester determinant computation in Appendix~\ref{app:disc}, giving the displayed factorization.
These cited results prove precisely the four components listed in the statement.
\end{proof}

\begin{remark}\label{rem:genus-one-upgrade}
For the present transfer polynomial, the substitution \(\xi=y-\lambda^2\) is not an ad hoc trick.
It is the specialization of the balanced family \eqref{eq:balanced-family}, in which the same change of variables always removes the quartic obstruction and converts the curve to Weierstrass form.
This is the structural reason why the later branch-point and maximal-modulus branch analysis can be carried out exactly.
\end{remark}

