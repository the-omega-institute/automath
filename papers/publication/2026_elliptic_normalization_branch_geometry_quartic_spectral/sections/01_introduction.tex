\section{Introduction}

We study the polynomial family $\{Z_m(y)\}_{m\ge 0}$ defined by the rational generating function
\[
Z(t,y)=\sum_{m\ge 0} Z_m(y)t^m
=
\frac{1+yt+(y^2-y-1)t^2+(y^3-2y)t^3}
{1-t-(2y+1)t^2+t^3+y(y+1)t^4}.
\]
Equivalently, $\{Z_m(y)\}$ is generated by an order-$4$ companion transfer matrix with characteristic polynomial
\[
\Pi(\lambda,y)=\lambda^4-\lambda^3-(2y+1)\lambda^2+\lambda+y(y+1).
\]
The paper is organized as a complete exact analysis of one quartic spectral family.
We regard \(\Pi(\lambda,y)\) as a fixed algebraic-spectral object and ask for an exact description of its elliptic normalization, its real branch geometry, its positive-real maximal-modulus branch, and the zeros of \(Z_m\) nearest the maximal-modulus square-root endpoint at \(y=0\).

Section~\ref{sec:genus-one} places \(\Pi(\lambda,y)\) inside the balanced family
\[
P_{a,c,d,g}(\lambda,y)=\lambda^4+a\lambda^3+(c-2y)\lambda^2+d\lambda+y(y-c)+g,
\]
for which the substitution \(\xi=y-\lambda^2\) removes the quartic obstruction and produces a Weierstrass model.
Within that family we use only a normalized double-root statement: Lemma~\ref{lem:balanced-classification} shows that, once the colliding points are fixed at $(\lambda,y)=(1,0)$ and $(\lambda,y)=(-1,1)$, the balanced parameters are forced to be \((a,c,d,g)=(-1,-1,1,0)\).
Without the second prescription there remains a one-parameter family of balanced members; see Remark~\ref{rem:mu-parameter}.

For the specialization \((a,c,d,g)=(-1,-1,1,0)\), the projective closure of \(\Pi(\lambda,y)=0\) is normalized by the elliptic curve
\[
Y^2=4\lambda^3-4\lambda+1.
\]
This turns the problem into a degree-$4$ genus-one covering of the \(y\)-line.
As a result one can compute the finite branch values exactly, determine the real-spectral phase diagram, identify the maximal-modulus branch on \(y>0\), and derive explicit endpoint zero asymptotics near \(y=0\).
The endpoint theorem is proved by combining the localized two-branch oscillation lemma in Section~\ref{sec:positive-maximal-modulus-branch} with the algebraic expansion package in Appendix~\ref{app:endpoint-branching}; the appendix is part of the proof, not a numerical supplement.

\subsection{Relation to existing literature}

The ingredients used in the paper are classical.
The Yang--Lee papers and Fisher's edge-singularity paper provide the historical background for zero distributions in parameter-dependent polynomial families \cite{YangLee1952I,LeeYang1952II,Fisher1978}, while modern Beraha--Kahane--Weiss formulations organize limiting zero sets through dominant equimodularity \cite{BerahaKahaneWeiss1975,BerahaKahaneWeiss1978,Sokal2004,BrownOtto2025}.
Companion matrices and cyclicity are standard \cite{HornJohnson2012}; rational generating functions, linear recurrences, and finite Hankel rank belong to the general theory of rational sequences \cite{FlajoletSedgewick2009,Stanley2012,Partington1989}; and the reduction to Weierstrass form, the Riemann--Hurwitz count, and local Puiseux expansions are standard algebraic-curve tools \cite{Walker1950,Silverman2009,Miranda1995,BrieskornKnorrer1986}.

Within the recurrence-generated zero literature, the closest references are Tran's discriminant approach \cite{Tran2014,Tran2015}, the four-term work of de Bruin, Forg\'acs--Tran, Tran--Zumba, Adams, and Botta--Fornazieri \cite{deBruin1984,ForgacsTran2016,ForgacsTran2017,ForgacsTran2020,TranZumba2018,TranZumba2021,Adams2020,BottaFornazieri2023}, and the exact-support and Tran-conjecture papers of Ndikubwayo and of B{\o}gvad--Ndikubwayo--Shapiro \cite{Ndikubwayo2020,BogvadNdikubwayoShapiro2020}.
Related geometric viewpoints also appear in Goh--He--Ricci and in Wang--Zhang \cite{GohHeRicci2009,WangZhang2021}, while Ndikubwayo's later Toeplitz-based work treats a different recurrence regime \cite{Ndikubwayo2022}.

\subsubsection*{Contribution and scope}

The contribution here is the exact integration of several classical tools for one concrete quartic.
The paper carries the quartic from its rational generating function through its elliptic normalization, branch locus, real oval projection, maximal-modulus separation, endpoint two-branch oscillation law, and degree-growth law without numerical fitting or unproved genericity assumptions.
Section~\ref{sec:genus-one} records three balanced-family tools as finite infrastructure for this specialization: the uniform Weierstrass reduction (Proposition~\ref{prop:balanced-family}), the auxiliary finite projection criterion and comparison algorithm used to compute the specialized real image (Theorem~\ref{thm:balanced-real-projection} and Proposition~\ref{prop:balanced-finite-comparison-algorithm}), and the fixed-neighbouring-collision normalization (Theorem~\ref{thm:canonical-specialization-up-to-normalization}).
The reusable balanced-family statement delivered here is deliberately a finite algebraic criterion for each fixed smooth balanced member: it classifies the one-interval, adjacent-touching, and two-interval alternatives by isolating and comparing explicitly listed real algebraic numbers, but it does not claim a closed-form stratification of the entire balanced parameter space.
The family-level statements are not offered as a classification of balanced quartics; their role is to isolate the finite algebraic skeleton from which the present real phase diagram follows.
This article does not treat finite-fold inverse systems, Sturmian or rotation dynamics, automata-prime questions, or BEDC-type certificate technology.
Finite-fold inverse-system questions ask for descent and stability across changing covers, whereas the present covering is the single degree-$4$ map from one elliptic normalization to the \(y\)-line.
Sturmian and rotation-dynamical problems concern symbolic codings of circle rotations; here the only real dynamics is the ordering of real branches and their moduli along intervals of the parameter line.
DFA-prime or automata-prime manuscripts study regular languages and arithmetic recognition, while no automaton or prime-register structure enters the proofs below.
BEDC-style certificate technology concerns external boundary or Euler certificates; the certificates used here are internal algebraic ones: a Weierstrass reduction, a finite ramification skeleton, and a local two-branch dominance expansion.
Thus the present paper is only the exact elliptic normalization and branch/dominance analysis of the single quartic spectral family \(\Pi\), with the named theorem package below serving as the scope boundary.

For real $y>0$ we write
\[
r(y):=\max_{1\le j\le 4}\abs{\lambda_j(y)},
\qquad
f_{\mathrm{dom}}(y):=\log r(y).
\]
The maximal-modulus branch over $y>0$ is the connected component of the smooth spectral curve on which $\abs{\lambda}=r(y)$.
The problem is thus to determine this branch and the corresponding zero geometry explicitly for the fixed quartic \(\Pi(\lambda,y)\).

\paragraph{Named theorem package.}
The body of the paper proves the following five named results.
The list is a roadmap to the labelled body results where the full hypotheses and proofs appear.
\begin{enumerate}
\item \textbf{Theorem A: balanced elliptic normalization.}
The balanced family
\[
P_{a,c,d,g}(\lambda,y)=\lambda^4+a\lambda^3+(c-2y)\lambda^2+d\lambda+y(y-c)+g
\]
is reduced by \(\xi=y-\lambda^2\), \(Y=2\xi-c\), to the Weierstrass model
\[
Y^2=c^2-4a\lambda^3-4d\lambda-4g.
\]
This is the formal body theorem Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization}, whose constituent assertions are Proposition~\ref{prop:balanced-family}, Corollary~\ref{cor:balanced-smooth}, and Corollary~\ref{cor:balanced-ramification}.

\item \textbf{Theorem B: balanced real-projection skeleton and canonical image.}
For smooth real balanced members, Theorem~\ref{thm:balanced-real-projection} gives a finite componentwise criterion for the real \(y\)-image using the real cubic endpoints, the ramification polynomial \(R_{a,c,d,g}\), and the exceptional \(\lambda=0\) fibre in \eqref{eq:balanced-real-extrema-set}.
Proposition~\ref{prop:balanced-finite-comparison-algorithm} is the fixed-member closed criterion used in this article: it reduces the disjoint, adjacent, and overlapping alternatives to finitely many comparisons of explicitly listed real algebraic numbers, while Corollary~\ref{cor:finite-projection-auxiliary-scope} records that no stronger global chamber classification for arbitrary balanced parameters is part of the present theorem chain.
Corollary~\ref{cor:balanced-real-interval-alternatives} records the same trichotomy in interval language.
For the canonical specialization, Proposition~\ref{prop:quartic-real-projection-skeleton} and Corollary~\ref{cor:real-phase-diagram} give the two component images \([\ySub,1]\) and \([0,\infty)\).

\item \textbf{Theorem C: algebraic normalized double-root specialization.}
The normalized collision data of Definition~\ref{def:normalized-collision-data} are equivalent to the displayed algebraic double-root constraints, and these constraints fix both neighbouring normalized collisions and force \((a,c,d,g)=(-1,-1,1,0)\).
More generally, after the shift \(y=u+\rho^2\widetilde y\) and dilation \(\lambda=\rho\widetilde\lambda\), the parameters are exactly
\[
(a,c,d,g)=(-\rho,\,2u-\rho^2,\,\rho^3,\,u(u-\rho^2)),
\]
as proved in Theorem~\ref{thm:canonical-specialization-up-to-normalization} from Lemma~\ref{lem:balanced-affine} and Lemma~\ref{lem:balanced-classification}.
Remark~\ref{rem:mu-parameter} records immediately after that theorem that leaving the second eigenvalue free gives a residual one-parameter family not classified in this article.

\item \textbf{Theorem D: branch and dominance classification.}
Theorem~\ref{thm:consolidated-branch-dominance-package} gives the discriminant factorization, the exact finite branch values \(0\), \(1\), \(\ySub\), \(\eta\), and \(\overline\eta\), the real-root counts on the four real intervals, and the positive-real spectral-radius branch for every \(y>0\).
It also records that \(y=1\) and \(\ySub\) are subdominant for the maximal-modulus envelope.

\item \textbf{Theorem E: endpoint zero law.}
Theorem~\ref{thm:endpoint-zero-quantization} proves that at the maximal-modulus square-root endpoint \((\lambda,y)=(1,0)\), for each fixed \(k\ge1\) the corresponding endpoint zero satisfies
\[
y_{m,k}
=
-\frac{\pi^2(2k-1)^2}{2m^2}
-\frac{3\pi^2(2k-1)^2}{4m^3}
+O_k(m^{-4}).
\]
Equivalently, every fixed compact edge-scaling window \(y=-u^2/m^2\) contains exactly the corresponding finite set of endpoint zeros for all sufficiently large \(m\).
\end{enumerate}

The minimal spectral-closure classification in Corollary~\ref{cor:complete-minimal-closure-classification} is used as standing recurrence-side input: it verifies that the order-four companion problem is the correct generic closure and identifies the three reduced fibres.
It is not one of the five headline contributions above.
The degree law and the boundary zero at \(y=-1\) are kept as supporting algebraic consequences in Corollary~\ref{cor:supporting-algebraic-consequences}; they verify the recurrence side of the same quartic family without enlarging the headline elliptic-normalization package into a general support-boundary theory.

The proof dependencies among the named results are as follows:
\[
\begin{array}{ccl}
\text{spectral closure} &\longrightarrow& \text{Theorem A},\\
\text{Theorem A} &\longrightarrow& \text{Theorems B and C},\\
\text{Theorems A and B} &\longrightarrow& \text{Theorem D},\\
\text{Theorem D plus the local endpoint expansion} &\longrightarrow& \text{Theorem E}.
\end{array}
\]
The supporting recurrence consequences in Corollary~\ref{cor:supporting-algebraic-consequences} are downstream of the fixed quartic family and are not hypotheses for Theorems A--E.
The spectral-closure input used before entering the named chain is Theorem~\ref{thm:minimal-order}, its all-parameter closure is Corollary~\ref{cor:complete-minimal-closure-classification}, and the positive-axis no-cancellation consequence used by the dominance step is Corollary~\ref{cor:spectral-closure-support}.
Theorem A is the formal body result Theorem~\ref{thm:theorem-a-balanced-elliptic-normalization}; the spectral-quartic package is assembled in Corollary~\ref{cor:canonical-theorem-a-package} from Theorem~\ref{thm:elliptic-reduction}, Lemma~\ref{lem:projective-normalization}, and Corollary~\ref{cor:disc-factor}.
Theorem B is proved by the finite criterion Theorem~\ref{thm:balanced-real-projection}, the finite comparison algorithm Proposition~\ref{prop:balanced-finite-comparison-algorithm}, its finite connectedness corollary Corollary~\ref{cor:balanced-real-interval-alternatives}, the auxiliary-scope clarification Corollary~\ref{cor:finite-projection-auxiliary-scope}, the scope interface Proposition~\ref{prop:balanced-projection-main-chain-interface}, Proposition~\ref{prop:quartic-real-projection-skeleton}, and Corollary~\ref{cor:real-phase-diagram}.
Theorem C is Theorem~\ref{thm:canonical-specialization-up-to-normalization}, whose proof invokes Lemma~\ref{lem:balanced-affine} and Lemma~\ref{lem:balanced-classification}; its dominance upgrade is Corollary~\ref{cor:canonical-dominance-after-normalization} and Theorem~\ref{thm:consolidated-branch-dominance-package}(5).
Theorem D is Theorem~\ref{thm:positive-maximal-modulus-branch}, Proposition~\ref{prop:global-dominance}, and Theorem~\ref{thm:consolidated-branch-dominance-package}.
Theorem E is the fixed-window interface Proposition~\ref{prop:fixed-window-endpoint-interface} together with Theorem~\ref{thm:endpoint-zero-quantization}.

\begin{corollary}[Supporting algebraic consequences of the recurrence]\label{cor:supporting-algebraic-consequences}
For the same rational family, the recurrence polynomials have the following algebraic boundary data.
For every \(m\ge2\),
\[
\deg_y Z_m=\left\lfloor\frac{m+3}{2}\right\rfloor,
\]
with leading coefficients
\[
[y^{\deg Z_{2n+1}}]Z_{2n+1}=n\quad(n\ge1),
\qquad
[y^{\deg Z_{2n}}]Z_{2n}=\frac{n(n^2+5)}6\quad(n\ge1).
\]
Moreover \((y+1)^2\mid Z_m(y)\) for every \(m\ge2\).
These consequences are not additional branch-classification hypotheses: they follow from the recurrence once the spectral family has been fixed.
\end{corollary}

\begin{proof}
The degree formula and the two displayed leading-coefficient identities are exactly Theorem~\ref{thm:support-law}.
The divisibility statement is Proposition~\ref{prop:double-zero}.
Both results are proved by identities in the recurrence and generating function, and neither is used to add a branch value, a normalization constraint, or a new endpoint to Theorems A--E.
\end{proof}

\subsection{Summary of results}

For this quartic family we obtain the following.

\begin{enumerate}
\item \textbf{Balanced-family reduction, real-projection description, and double-root normalization} (Section~\ref{sec:genus-one}).
We identify a four-parameter balanced family \(P_{a,c,d,g}\) only as the algebraic scaffold needed to convert the specialized quartic curve to a Weierstrass model by the substitution \(\xi=y-\lambda^2\).
For real balanced parameters, Theorem~\ref{thm:balanced-real-projection} gives the finite componentwise projection criterion used to compute the real \(y\)-images of this specialization.
We then prove the normalized double-root statement of Lemma~\ref{lem:balanced-classification}: if the balanced polynomial has double roots at $(\lambda,y)=(1,0)$ and $(\lambda,y)=(-1,1)$, then \((a,c,d,g)=(-1,-1,1,0)\).
For the present quartic this yields the elliptic model with invariants \(g_2=4\), \(g_3=-1\), and \(\Delta=37\), together with the finite branch values \(y=0\), \(y=1\), and the three roots of \(256y^3+411y^2+165y+32\).

\item \textbf{Real branch-point classification and real-root counts} (Section~\ref{sec:ep-classification}).
Among the three real branch values, $y=0$ is the only one whose outgoing pair contains the maximal-modulus root for $y>0$ small; $y=1$ and the negative branch value \(y_{\mathrm{sub}}\) are non-dominant.
The real locus of the elliptic curve has component images
\[
[y_{\mathrm{sub}},1]
\qquad\text{and}\qquad
[0,\infty),
\]
so the total real image is \( [y_{\mathrm{sub}},\infty) \) with overlap on \([0,1]\).
This yields the exact count of real roots of \(\Pi(\cdot,y)\) for every real \(y\).

\item \textbf{Square-root branching and endpoint quantization} (Section~\ref{sec:positive-maximal-modulus-branch}).
We verify the local square-root branching, amplitude symmetry, and complex-uniform remainder bounds needed for the localized two-branch oscillation law at \(y=0\).
Consequently, for each fixed \(k\ge 1\), after fixing \(k\) and taking \(m\) sufficiently large, the \(k\)th negative zero nearest \(y=0\) satisfies
\[
y_{m,k}
=
-\frac{\pi^2(2k-1)^2}{2m^2}
-\frac{3\pi^2(2k-1)^2}{4m^3}
+O_k(m^{-4}).
\]

\item \textbf{Supporting algebraic consequences and a supplementary large-$y$ check} (Section~\ref{sec:support-law} and Appendix~\ref{app:projective-infinity}).
On the polynomial side, \(\deg_y Z_m=\lfloor(m+3)/2\rfloor\), and the top-degree coefficients are explicit and subexponential in \(m\).
At the opposite end of the positive real axis, the large-$y$ maximal-modulus root has a quarter-power splitting governed by a double tangency at projective infinity; this calculation is relegated to a supplementary appendix because it is not used in the local-to-global endpoint zero theorem.

\item \textbf{A rigid boundary value at \(y=-1\)} (Section~\ref{sec:boundary-zeros}).
For every \(m\ge 2\), the polynomial \(Z_m(y)\) has a zero of multiplicity at least two at \(y=-1\).
This gives a uniform algebraic boundary point for the whole sequence.
\end{enumerate}

Sections~\ref{sec:model}--\ref{sec:hankel} define the family and establish the generic order-$4$ minimality of the recurrence.
Sections~\ref{sec:genus-one}--\ref{sec:boundary-zeros} contain the explicit spectral and algebraic results for the quartic specialization.
Appendix~\ref{app:reproducibility-note} records supplementary numerical comparisons for the real-axis modulus ordering and the first endpoint zeros, Appendix~\ref{app:projective-infinity} records the large-\(y\) consistency check, and Section~\ref{sec:discussion-new} closes with the scope and limitations of the quartic analysis.

\paragraph{Novelty calibration by result.}
Table~\ref{tab:novelty-map} separates the classical input used in each step from the quartic-specific output obtained in this paper.
Most individual methods are standard, but the theorem chain is not a list of independent applications: the balanced reduction fixes the elliptic model, the finite projection skeleton proves the real oval images, the branch classification separates maximal-modulus from non-dominant collisions, and the resulting local model gives the endpoint zero law.
The balanced-family results are used only to support the specialization \((a,c,d,g)=(-1,-1,1,0)\); the paper does not pursue a general classification of balanced quartics.
The real-projection theorem of Section~\ref{sec:genus-one} is therefore framed as an auxiliary finite criterion whose specialization is the object used in the main chain.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}p{0.27\textwidth}p{0.23\textwidth}p{0.41\textwidth}@{}}
\toprule
Statement & Role in the theorem chain & Quartic-specific output \\
\midrule
Theorem~\ref{thm:minimal-order} & Recurrence input & Certifies order-$4$ and Hankel rank outside \(y=-1,0,1\). \\
Theorem~\ref{thm:balanced-real-projection} & Auxiliary skeleton & Reduces real oval images to finite algebraic comparisons. \\
Proposition~\ref{prop:quartic-real-projection-skeleton} & Main specialization & Computes the two oval images \([y_{\mathrm{sub}},1]\) and \([0,\infty)\). \\
Lemma~\ref{lem:balanced-classification} & Normalization input & Forces the quartic from two prescribed double roots. \\
Theorem~\ref{thm:elliptic-reduction} & Geometric core & Gives the elliptic normalization and exact branch polynomial. \\
Theorem~\ref{thm:ep-classification} & Dominance filter & Identifies which real branch collision controls the envelope. \\
Corollary~\ref{cor:real-phase-diagram} & Real-spectrum output & Gives exact real-root counts for all real \(y\). \\
Theorem~\ref{thm:positive-maximal-modulus-branch} and Proposition~\ref{prop:global-dominance} & Positive-axis branch and dominance & Parametrize the positive branch and prove maximal-modulus dominance on \(y>0\). \\
Proposition~\ref{prop:fixed-window-endpoint-interface} and Theorem~\ref{thm:endpoint-zero-quantization} & Asymptotic output & Gives the two-term fixed-index endpoint zero law near \(y=0\), restricted to fixed compact endpoint windows. \\
Corollary~\ref{cor:supporting-algebraic-consequences} & Supporting recurrence algebra & Determines degree growth, leading coefficients, and the boundary zero at \(y=-1\). \\
\bottomrule
\end{tabular}
\caption{Role map for the main theorem chain.  The table records how classical tools are integrated into the exact quartic analysis rather than assigning stand-alone novelty grades to the tools themselves.}
\label{tab:novelty-map}
\end{table}


