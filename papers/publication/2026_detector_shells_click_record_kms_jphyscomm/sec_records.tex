\section{Record, inverse, and finite-record results}\label{sec:records}

This section gives the record-side results in compressed form.  Each main
result states its own kernel and stationarity assumptions.  Between those
statements, all probabilities below are probabilities of the stationary visible
binary record generated by the sampled labelled kernels \(T_0,T_1\).
Throughout this section \((A_n)_{n\in\mathbb Z}\) denotes that two-sided
stationary extension.
One-sided strings such as \(A_0,\ldots,A_{N-1}\) are finite restrictions used
only for observed-record statistics; when a proof conditions on a past event
or shifts a renewal epoch, it is using the same stationary two-sided process.

\subsection{Model, benchmark, and renewal law}

\begin{proposition}[Killed-leakage sampled-boundary one-recovery-opportunity instrument and labelled kernels]\label{prop:instrument-kernels-main}
Fix \(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\).  With hidden states ordered as
\((R,D)\), let
\begin{equation}\label{eq:t0-t1-main}
T_0=
\begin{pmatrix}
p&0\\ b&s
\end{pmatrix},
\qquad
T_1=
\begin{pmatrix}
0&1-p\\0&a
\end{pmatrix},
\end{equation}
where
\begin{equation}\label{eq:p-c-main}
p=e^{-\Gamma\Delta\tau},
\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\end{equation}
and
\begin{equation}\label{eq:a-b-main}
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,
\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\end{equation}
For \(\kappa_{\mathrm r}\ne\Gamma\),
\[
a=\frac{\kappa_{\mathrm r}(1-p)-\Gamma(1-s)}
        {\kappa_{\mathrm r}-\Gamma},
\qquad
b=\frac{\kappa_{\mathrm r}(p-s)}
        {\kappa_{\mathrm r}-\Gamma},
\]
with continuous equal-rate limits.  The labelled-kernel convention is
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), and word probabilities are
\(\pi T_{u_0}\cdots T_{u_{m-1}}\mathbf1\) from the stationary hidden law.
\end{proposition}

\begin{proof}
From \(R\), no attempt occurs with probability \(p\), while any attempt causes
one visible click and resets the next hidden state to \(D\).  From \(D\), no
recovery before the boundary gives the \(s\) entry; recovery at time \(r\)
followed by no attempt gives the integrand for \(b\), and recovery followed
by at least one attempt gives the integrand for \(a\).  These disjoint cases
are exactly \eqref{eq:t0-t1-main}.  The displayed unequal-rate formulas are
the elementary evaluations of the two integrals.
\end{proof}

\begin{definition}[Reference criteria]\label{def:thermality-notions-main}
Detailed-balance thermality is the rate-ratio condition
\(\Gamma_+/\Gamma_-=e^{-\beta\Omega}\).  Gibbs thermalization is convergence
of the reduced detector state to \(e^{-\beta H_{\mathrm{det}}}/Z\).  The
record-side Parry benchmark is the Parry measure on the golden-mean shift,
the maximum-entropy law under \(11\notin\operatorname{supp}\mu\)
\cite{Parry1964,LindMarcus1995}.  Only the third criterion is a benchmark for
the binary record studied here.
\end{definition}

\begin{lemma}[Standard Parry benchmark for the stipulated ideal counter]\label{prop:ideal-main}
For the ideal one-step blanking counter with transition matrix
\[
P_\beta=
\begin{pmatrix}
p_\beta&1-p_\beta\\
1&0
\end{pmatrix},
\qquad p_\beta=e^{-\Gamma\Delta\tau},
\]
the sampled output is the Parry law on the golden-mean shift iff
\begin{equation}\label{eq:parry-threshold-main}
p_\beta=\varphi^{-1}
\quad\Longleftrightarrow\quad
\Gamma\Delta\tau=\log\varphi .
\end{equation}
\end{lemma}

\begin{proof}
The standard golden-mean Parry calculation gives
\(P^{\mathrm P}(0,0)=\varphi^{-1}\) and \(P^{\mathrm P}(1,0)=1\)
\cite{Parry1964,LindMarcus1995}.  The stipulated blanking counter has the
same support and the same forced transition from \(1\), so equality is
equivalent to \(p_\beta=\varphi^{-1}\).
\end{proof}

\begin{lemma}[Equal-rate and critical notation]\label{lem:equal-rate-critical-convention-main}
The survival factors are always
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and throughout the sampled-kernel algebra
\[
q=1-p .
\]
The hidden eigenvalue is
\begin{equation}\label{eq:lambda-hid-main}
\lambda_{\mathrm{hid}}
=p-b
=\frac{\kappa_{\mathrm r}s-\Gamma p}{\kappa_{\mathrm r}-\Gamma}
\quad(\kappa_{\mathrm r}\ne\Gamma),
\end{equation}
with continuous equal-rate value
\(\lambda_{\mathrm{hid}}=e^{-y}(1-y)\), where
\(y=\kappa_{\mathrm r}\Delta\tau=\Gamma\Delta\tau\) on the equal-rate
diagonal.  Put
\[
x=\Gamma\Delta\tau,\qquad y=\kappa_{\mathrm r}\Delta\tau .
\]
For \(x\ne y\),
\[
\lambda_{\mathrm{hid}}=0
\quad\Longleftrightarrow\quad
x e^{-x}=y e^{-y}.
\]
The equation \(x e^{-x}=y e^{-y}\) always has the formal solution \(x=y\).
For \(y\ne1\) this diagonal solution is only the removable zero of the
multiplied quotient numerator, not a zero of the continuous eigenvalue
\(\lambda_{\mathrm{hid}}\).  The critical rate is therefore the non-removable
branch
\begin{equation}\label{eq:gamma-crit-piecewise-main}
\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)
=\Delta\tau^{-1}
\begin{cases}
x_+(y),&0<y<1,\quad x_+(y)>1,\quad x_+(y)e^{-x_+(y)}=y e^{-y},\\
1,&y=1,\\
x_-(y),&y>1,\quad 0<x_-(y)<1,\quad x_-(y)e^{-x_-(y)}=y e^{-y}.
\end{cases}
\end{equation}
Equivalently, for \(y\ne1\) it is the unique positive solution of
\(x e^{-x}=y e^{-y}\) on the opposite side of \(1\) from \(y\), and the
removable equal-rate branch \(x=y\) is explicitly excluded.  Only at \(y=1\)
does the equal-rate branch become a genuine zero, giving
\(\Gamma_{\mathrm{crit}}=\kappa_{\mathrm r}=1/\Delta\tau\).
\end{lemma}

\begin{proof}
The identity \(p-b\) and the quotient form follow from
\eqref{eq:a-b-main}.  The equal-rate value is l'Hopital's rule, equivalently
\(b=y e^{-y}\) when \(\kappa_{\mathrm r}=\Gamma\), and hence
\(\lambda_{\mathrm{hid}}=e^{-y}(1-y)\).  For \(x\ne y\), multiplying the
quotient numerator by \(\Delta\tau\) gives the scalar equation
\(x e^{-x}=y e^{-y}\).  The function \(x\mapsto xe^{-x}\) is strictly
increasing on \((0,1)\), strictly decreasing on \((1,\infty)\), and has its
only maximum at \(1\).  Hence the level through \(y\ne1\) has exactly one
positive solution on the opposite side of \(1\), in addition to the removable
solution \(x=y\).  This gives the two non-removable branches in
\eqref{eq:gamma-crit-piecewise-main}.  When \(y=1\), the opposite branch
coalesces with the diagonal and the equal-rate limit \(e^{-1}(1-1)=0\) gives
\(\Gamma_{\mathrm{crit}}=1/\Delta\tau=\kappa_{\mathrm r}\).
\end{proof}

For later audits, the equal-rate diagonal
\(\Gamma=\kappa_{\mathrm r}\), \(y=\Gamma\Delta\tau\), \(p=e^{-y}\), is
summarized by the single dictionary
\begin{equation}\label{eq:equal-rate-dictionary-main}
\begin{aligned}
a&=1-(1+y)p,&
b&=yp,\\
\lambda_{\mathrm{hid}}&=p-b=(1-y)p,&
S_k&=\PP(G\ge k)=p^k(1+yk),\\
g_k&=p^k\bigl((1-p)(1+yk)-yp\bigr),&
h_k&=\frac{(1-p)+y(k(1-p)-p)}{1+yk},
\qquad k\ge0 .
\end{aligned}
\end{equation}

\begin{lemma}[Regeneration and zero-run law]\label{lem:renewal-gap-main}
Let \(G\) be the number of zeros between consecutive clicks in the stationary
sampled boundary-reset record.  Click times are regeneration times.  For
\(k\ge0\),
\begin{equation}\label{eq:gap-main}
g_k=\PP(G=k)=e_D^\top T_0^kT_1\mathbf1 .
\end{equation}
If \(\kappa_{\mathrm r}\ne\Gamma\), then
\begin{equation}\label{eq:gap-closed-main}
g_k=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma}.
\end{equation}
The gap law has full support on \(\mathbb N_0\) and an exponential tail.
\end{lemma}

\begin{proof}
After a click the next hidden state is \(D\), independently of the earlier
record.  Thus a gap of \(k\) zeros followed by a click has probability
\eqref{eq:gap-main}.  Since \(T_0\) is lower triangular,
\[
\textstyle\sum_{j=0}^{k-1}s^jp^{k-1-j}=0\quad(k=0),
\]
with the displayed sum interpreted as empty when \(k=0\), and
\[
T_0^k=
\begin{pmatrix}
p^k&0\\
b\textstyle\sum_{j=0}^{k-1}s^jp^{k-1-j}&s^k
\end{pmatrix}.
\]
Therefore
\[
e_D^\top T_0^kT_1\mathbf1
=b(1-p)\textstyle\sum_{j=0}^{k-1}s^jp^{k-1-j}+as^k,
\]
where the comma only separates the display from the following substitution.
For \(p\ne s\),
\[
\sum_{j=0}^{k-1}s^jp^{k-1-j}=\frac{p^k-s^k}{p-s}.
\]
Substituting
\[
a=\frac{\kappa_{\mathrm r}(1-p)-\Gamma(1-s)}
        {\kappa_{\mathrm r}-\Gamma},
\qquad
b=\frac{\kappa_{\mathrm r}(p-s)}
        {\kappa_{\mathrm r}-\Gamma}
\]
therefore gives
\[
b(1-p)\frac{p^k-s^k}{p-s}+as^k
=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma},
\]
which is \eqref{eq:gap-closed-main}.  If \(p=s\), equivalently
\(\kappa_{\mathrm r}=\Gamma\), the same identity is read by the continuous
equal-rate dictionary \eqref{eq:equal-rate-dictionary-main}.  All terms are
probabilities of disjoint paths, so they are positive, and the two survival
factors lie in \((0,1)\).
\end{proof}

\begin{corollary}[Support of finite binary words]\label{cor:word-support-main}
Every finite binary word has positive probability under the stationary
sampled boundary-reset record.
\end{corollary}

\begin{proof}
The gap law in \Cref{lem:renewal-gap-main} has \(g_k>0\) for every
\(k\ge0\).  Decompose any finite word into the observed portions of successive
zero runs between clicks, extending at the two ends if needed.
\end{proof}

\subsection{Exact dependence, hazard, and count diagnostics}

\begin{lemma}[Stationary boundary-reset recovery law]\label{lem:stationary-main}
For \(P=T_0+T_1=\begin{psmallmatrix}p&q\\ b&1-b\end{psmallmatrix}\), the
stationary hidden law is
\[
\pi=\left(\frac b{q+b},\frac{q}{q+b}\right).
\]
With \(q=1-p\) as fixed in \Cref{lem:equal-rate-critical-convention-main}, the
stationary click density is
\begin{equation}\label{eq:rho-main}
\rho=\pi T_1\mathbf1=\frac{q(1-s)}{q+b}.
\end{equation}
Moreover \(a<q(1-s)<q\), and therefore
\(a-\rho=-b(q-a)/(q+b)<0\).
\end{lemma}

\begin{proof}
Solving \(\pi P=\pi\) gives the displayed law.  Since
\(u=T_1\mathbf1=(q,a)^\top\) and \(a+b=1-s\), \(\rho=\pi u\) gives
\eqref{eq:rho-main}.  For \(r>0\),
\[
1-e^{-\Gamma(\Delta\tau-r)}<1-e^{-\Gamma\Delta\tau}=q,
\]
so the defining integral for \(a\) gives
\[
a<q\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}\,dr
=q(1-s)<q .
\]
The sign follows from
\[
a-\rho=-\frac{b(q-a)}{q+b}
\]
and the strict positivity of \(b\) and \(q-a\).
\end{proof}

\begin{lemma}[Covariance transfer through the hidden mode]\label{lem:covariance-transfer-main}
For the stationary sampled record,
\begin{equation}\label{eq:cov-transfer-main}
\PP(A_n=1\mid A_0=1)
=\rho+(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad
\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1}
\end{equation}
for \(n\ge1\).
\end{lemma}

\begin{proof}
With \(q=1-p\) and \(u=(q,a)^\top\),
\[
u-\rho\mathbf1=\frac{q-a}{q+b}\binom {q}{-b},
\qquad
P\binom {q}{-b}=(p-b)\binom {q}{-b}.
\]
Thus
\[
P^{n-1}u=\rho\mathbf1+
\lambda_{\mathrm{hid}}^{n-1}(u-\rho\mathbf1).
\]
A click at time \(0\) resets the next hidden boundary state to \(D\).  Hence
\[
\PP(A_n=1\mid A_0=1)
=e_D^\top P^{n-1}u
=\rho+\lambda_{\mathrm{hid}}^{n-1}(a-\rho),
\]
because the \(D\)-component of \(u-\rho\mathbf1\) is \(a-\rho\).  Multiplying
by \(\rho=\PP(A_0=1)\) and subtracting \(\rho^2\) gives the covariance in
\eqref{eq:cov-transfer-main}.
\end{proof}

\begin{lemma}[Critical-rate uniqueness and covariance phase]\label{lem:critical-rate-main}
For fixed \(\kappa_{\mathrm r},\Delta\tau>0\), the equation
\(\lambda_{\mathrm{hid}}(\Gamma)=0\) has the unique positive solution
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) defined by the
piecewise non-removable branch
\eqref{eq:gamma-crit-piecewise-main}.  When
\(\Gamma_{\mathrm{crit}}\ne\kappa_{\mathrm r}\), this is equivalently
\begin{equation}\label{eq:gamma-c-main}
\Gamma_{\mathrm{crit}}e^{-\Gamma_{\mathrm{crit}}\Delta\tau}
=\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}\Delta\tau}.
\end{equation}
The displayed scalar equation also has the diagonal solution
\(\Gamma_{\mathrm{crit}}=\kappa_{\mathrm r}\), but that solution is removable
for \(\lambda_{\mathrm{hid}}\) unless
\(\kappa_{\mathrm r}\Delta\tau=1\).  Thus on the diagonal
\(\Gamma=\kappa_{\mathrm r}\), the zero occurs only at
\(\Gamma=\kappa_{\mathrm r}=1/\Delta\tau\).
The sign of \(\operatorname{Cov}(A_0,A_n)\) is the sign of
\(-\lambda_{\mathrm{hid}}^{\,n-1}\).
\end{lemma}

\begin{proof}
The uniqueness is the branch statement in
\Cref{lem:equal-rate-critical-convention-main}.  The covariance sign follows
from \(\rho>0\) and \(a-\rho<0\) in \Cref{lem:stationary-main}.
\end{proof}

\begin{lemma}[Hidden-mode contraction]\label{lem:hidden-mode-contraction-main}
For every positive sampled-boundary setting,
\[
|\lambda_{\mathrm{hid}}|<1 .
\]
In particular the shifted geometric Fourier series in
\Cref{prop:spectrum-main} is absolutely convergent.
\end{lemma}

\begin{proof}
The transition matrix
\[
P=T_0+T_1=
\begin{pmatrix}
p&1-p\\ b&1-b
\end{pmatrix}
\]
is a two-state stochastic matrix with strictly positive off-diagonal entries
\(1-p>0\) and \(b>0\).  Its eigenvalues are \(1\) and
\(\lambda_{\mathrm{hid}}=p-b\).  The upper bound is immediate from \(p<1\)
and \(b>0\): \(\lambda_{\mathrm{hid}}<1\).  For the lower bound, \(p>0\) and
\[
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr
<\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}\,dr
=1-s<1 .
\]
Thus \(\lambda_{\mathrm{hid}}=p-b>-1\).  Hence
\(|\lambda_{\mathrm{hid}}|<1\).  Absolute convergence follows from
\(\sum_{n\ge1}|\lambda_{\mathrm{hid}}|^{n-1}<\infty\).
\end{proof}

\begin{lemma}[Hazard obstruction to finite symbolic memory]\label{lem:hazard-obstruction-main}
A renewal binary process with full gap support is a finite-order Markov chain
only if its gap hazard is eventually constant.
\end{lemma}

\begin{proof}
For a renewal process, after observing \(k\) consecutive zeros since the last
click, the conditional click probability is the zero-run hazard \(h_k\).
If an \(r\)-step Markov rule generated the same full-support law, then all
histories with \(k\ge r\) trailing zeros would have the same last \(r\) symbols
and hence the same click probability.  Therefore \(h_k\) would be constant for
all \(k\ge r\).

This condition concerns a transition rule conditioned on the most recent
symbols.  It is different from finite dependence of separated sigma-fields:
\(1\)-dependence says that events separated by one unobserved sample
factorize, while an \(r\)-step Markov rule says that every future one-step
transition probability is a function of only the last \(r\) symbols.  For a
renewal record with full gap support, the latter would force the click
probability after a long zero run to stop depending on the run length.
\end{proof}

\begin{lemma}[Cylinder factorization at the memory threshold]\label{lem:kernel-factorization-main}
If \(\lambda_{\mathrm{hid}}=0\), then \(P=T_0+T_1=\mathbf1\pi\).  More
explicitly, let \(u=(u_0,\ldots,u_{r-1})\) and
\(v=(v_0,\ldots,v_{\ell-1})\) be arbitrary finite binary words and write
\(T_u=T_{u_0}\cdots T_{u_{r-1}}\), \(T_v=T_{v_0}\cdots T_{v_{\ell-1}}\).
If the word \(u\) is observed on one block and \(v\) is observed on a second
block beginning two samples later, with the intervening sample unobserved,
then
\[
\PP(A_0^{r-1}=u,\ A_{r+1}^{r+\ell}=v)
=\pi T_u P T_v\mathbf1
=\bigl(\pi T_u\mathbf1\bigr)\bigl(\pi T_v\mathbf1\bigr).
\]
Consequently every two visible blocks separated by one unobserved sample are
independent, so the visible record is exact \(1\)-dependent.
\end{lemma}

\begin{proof}
The second eigenvalue of \(P\) is \(\lambda_{\mathrm{hid}}\), while the first is
\(1\).  At \(\lambda_{\mathrm{hid}}=0\), the primitive stochastic matrix
\(P\) has rank one and both rows equal \(\pi\), hence \(P=\mathbf1\pi\).
For fixed words \(u\) and \(v\), summing over the unobserved sample between
the two displayed blocks inserts \(T_0+T_1=P\).  The stationary cylinder
probability is therefore
\[
\pi T_u(T_0+T_1)T_v\mathbf1=\pi T_uPT_v\mathbf1.
\]
At the threshold,
\[
\pi T_uPT_v\mathbf1
=\pi T_u\mathbf1\,\pi T_v\mathbf1
=\PP(A_0^{r-1}=u)\PP(A_0^{\ell-1}=v),
\]
where the last equality is stationarity applied to the two single-block
cylinders.  Thus every pair of finite cylinder events supported on two
finite blocks with one omitted sample between them factorizes.  Translating
the indices gives the same statement for any blocks
\(\{i,\ldots,m\}\) and \(\{m+2,\ldots,j\}\).  Since finite cylinders generate
the corresponding finite-block sigma algebras, the two finite-block sigma
algebras are independent.  Letting the left and right block lengths increase
and using the monotone-class theorem gives independence of
\(\sigma(A_j:j\le m)\) and \(\sigma(A_j:j\ge m+2)\) for every \(m\).
\end{proof}

\begin{lemma}[Exact \texorpdfstring{\(1\)}{1}-dependence criterion]\label{lem:one-dep-criterion-main}
The sampled boundary-reset record is exact \(1\)-dependent iff
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).
\end{lemma}

\begin{proof}
The forward direction at the threshold is
\Cref{lem:kernel-factorization-main}.  Conversely, exact \(1\)-dependence
would make \(A_0\) and \(A_2\) independent.  But
\[
\operatorname{Cov}(A_0,A_2)=\rho(a-\rho)\lambda_{\mathrm{hid}},
\]
and the prefactor is nonzero by \Cref{lem:stationary-main}.
\end{proof}

\begin{lemma}[Survival tails and hazard monotonicity]\label{lem:survival-hazard-main}
For \(\kappa_{\mathrm r}\ne\Gamma\),
\begin{equation}\label{eq:survival-tail-main}
S_k=\PP(G\ge k)=
\frac{\kappa_{\mathrm r}p^k-\Gamma s^k}{\kappa_{\mathrm r}-\Gamma},
\end{equation}
and
\begin{equation}\label{eq:hazard-main}
h_k=\PP(G=k\mid G\ge k)=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}p^k-\Gamma s^k}.
\end{equation}
The equal-rate limit is
\begin{equation}\label{eq:equal-rate-survival-hazard-main}
\PP(G\ge k)=p^k(1+\Gamma\Delta\tau\,k),
\qquad
h_k=
\frac{(1-p)+\Gamma\Delta\tau(k(1-p)-p)}
     {1+\Gamma\Delta\tau\,k}.
\end{equation}
In all cases \(h_{k+1}>h_k\).
\end{lemma}

\begin{proof}
Summing \eqref{eq:gap-closed-main} over \(j\ge k\) gives
\eqref{eq:survival-tail-main}, and \(h_k=g_k/S_k\) gives
\eqref{eq:hazard-main}.  The equal-rate display is the continuous limit.
For the unequal-rate case, a direct subtraction gives
\[
h_{k+1}-h_k
=\frac{\Gamma\kappa_{\mathrm r}(p-s)^2p^ks^k}
        {(\kappa_{\mathrm r}p^k-\Gamma s^k)
         (\kappa_{\mathrm r}p^{k+1}-\Gamma s^{k+1})}
\]
and the denominator has positive product sign.  Indeed
\[
\kappa_{\mathrm r}p^m-\Gamma s^m
=(\kappa_{\mathrm r}-\Gamma)S_m,\qquad m=k,k+1,
\]
by \eqref{eq:survival-tail-main}.  Since \(S_m=\PP(G\ge m)>0\), both factors
have the sign of \(\kappa_{\mathrm r}-\Gamma\): they are both positive when
\(\kappa_{\mathrm r}>\Gamma\) and both negative when
\(\kappa_{\mathrm r}<\Gamma\).  Their product is therefore
\((\kappa_{\mathrm r}-\Gamma)^2S_kS_{k+1}>0\).  The numerator is also
positive because \(\Gamma,\kappa_{\mathrm r}>0\), \(p,s\in(0,1)\), and
\(p\ne s\) in the unequal-rate case.

On the equal-rate diagonal, write
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\) and
\(p=e^{-y}\).  The displayed equal-rate hazard can be rewritten as
\[
h_k
=1-p-\frac{y p}{1+y k}.
\]
Thus
\[
h_{k+1}-h_k
=yp\left(\frac1{1+yk}-\frac1{1+y(k+1)}\right)
=\frac{y^2p}{(1+yk)(1+y(k+1))}>0.
\]
\end{proof}

\begin{proof}[Proof of \Cref{thm:main-record-wrapper}]
The kernels are those of \Cref{prop:instrument-kernels-main}: \(T_0\) records a
zero, \(T_1\) records a click, \(P=T_0+T_1\) is stochastic, \(0<p,s<1\), and
\(a,b>0\) with \(a+b=1-s\).  A click sends the next boundary hidden state to
\(D\), so successive click epochs regenerate the future visible record.
\Cref{lem:renewal-gap-main} gives
\[
g_k=\PP(G=k)=e_D^\top T_0^kT_1\mathbf1
\]
and evaluates \(T_0^k\) to obtain \eqref{eq:gap-closed-main}.  The diagonal
display in the theorem is the continuous equal-rate line of
\eqref{eq:equal-rate-dictionary-main}.  Positivity of the path probabilities
gives full support of the gap law, and \Cref{cor:word-support-main} converts
positive gaps into support for every finite visible word.

\Cref{lem:survival-hazard-main} sums the gap law to
\[
S_k=\PP(G\ge k)=
\frac{\kappa_{\mathrm r}p^k-\Gamma s^k}{\kappa_{\mathrm r}-\Gamma}
\]
off the equal-rate diagonal and then divides \(g_k/S_k\) to obtain
\eqref{eq:hazard-main}.  The same lemma proves
\[
h_{k+1}-h_k
=\frac{\Gamma\kappa_{\mathrm r}(p-s)^2p^ks^k}
        {(\kappa_{\mathrm r}p^k-\Gamma s^k)
         (\kappa_{\mathrm r}p^{k+1}-\Gamma s^{k+1})}>0
\]
off the diagonal, and proves the positive continuous-limit increment in
\eqref{eq:equal-rate-survival-hazard-main} on the diagonal.

For the covariance identity, \Cref{lem:stationary-main} identifies the
stationary hidden law and click density \(\rho\).  With
\(u=T_1\mathbf1=(1-p,a)^\top\), \Cref{lem:covariance-transfer-main} writes
\[
u-\rho\mathbf1=\frac{1-p-a}{1-p+b}\binom{1-p}{-b}
\]
and diagonalizes the remaining hidden mode:
\[
P^{n-1}u=\rho\mathbf1+
\lambda_{\mathrm{hid}}^{\,n-1}(u-\rho\mathbf1).
\]
Conditioning on \(A_0=1\) again starts the next boundary state at \(D\), hence
\[
\PP(A_n=1\mid A_0=1)
=\rho+(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\]
and multiplying by \(\rho\) and subtracting \(\rho^2\) gives
\(\gamma_n=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1}\).

The exact \(1\)-dependence assertion is \Cref{lem:one-dep-criterion-main}.
When \(\lambda_{\mathrm{hid}}=0\), \(P=\mathbf1\pi\), and
\Cref{lem:kernel-factorization-main} factorizes all cylinder events separated
by one unobserved sample.  Conversely exact \(1\)-dependence would force
\(\gamma_2=0\), while
\(\gamma_2=\rho(a-\rho)\lambda_{\mathrm{hid}}\) and the prefactor is nonzero by
\Cref{lem:stationary-main}; hence \(\lambda_{\mathrm{hid}}=0\).
This finite sigma-field separation is not a finite-order Markov transition
rule: the former is a factorization statement across a gap, whereas the latter
would make the next-symbol probability depend only on a bounded suffix.

Finally, a renewal process with full gap support that were \(r\)-step Markov
would have constant click probability after every run of at least \(r\) zeros.
That is exactly the finite-memory obstruction in
\Cref{lem:hazard-obstruction-main}.  Since the hazard just proved is strictly
increasing, it is not eventually constant, so the visible process is not
finite-order Markov.
\end{proof}

\begin{lemma}[Discrete renewal count-variance identity]\label{lem:discrete-renewal-fano-main}
Let \((A_n)_{n\in\mathbb Z}\) be a stationary binary renewal process on the
sample lattice, with click epochs separated by i.i.d. integer cycle lengths
\(L_j\ge1\), mean \(\mu=\EE L_j<\infty\), variance
\(\sigma_L^2=\Var(L_j)<\infty\), and exponentially decaying tails.  For
\(N_m=\sum_{n=0}^{m-1}A_n\),
\begin{equation}\label{eq:discrete-renewal-fano-identity-main}
\lim_{m\to\infty}\frac{\Var(N_m)}{\EE N_m}
=\frac{\sigma_L^2}{\mu^2}.
\end{equation}
If, in addition, the autocovariances are summable and \(f(0)\) denotes the
zero-frequency spectral density
\(\operatorname{Cov}(A_0,A_0)+2\sum_{h\ge1}\operatorname{Cov}(A_0,A_h)\),
then the same limit equals \(f(0)/\rho\), where \(\rho=\EE A_0=1/\mu\).
\end{lemma}

\begin{proof}
Work first in Palm coordinates with a click at the left endpoint and write
\(S_k=L_1+\cdots+L_k\) and
\(K_m=\max\{k:S_k\le m\}\).  The stationary count \(N_m\) differs from the
corresponding Palm count only through the initial delayed cycle and the
terminal residual cycle.  The stationary residual cycle has the size-biased
cycle tail, which is still exponentially decaying, and the maximum of the
complete cycles intersecting \([0,m)\) is \(O_{\PP}(\log m)\).  Hence this
endpoint replacement changes \(m^{-1/2}(N_m-\EE N_m)\) by \(o_{\PP}(1)\), and
the squared normalized family is uniformly integrable.

At the renewal stopping index,
\[
S_{K_m}\le m<S_{K_m+1}.
\]
Thus, with \(W_j=L_j-\mu\),
\[
\mu\left(K_m-\frac m\mu\right)
=-\sum_{j=1}^{K_m}W_j+O_{\PP}(\log m).
\]
Since \(K_m/m\to1/\mu\) and the partial sums of the centered i.i.d. sequence
\((W_j)\) satisfy the usual random-time-change estimate,
\[
\sum_{j=1}^{K_m}W_j
=\sum_{j=1}^{\lfloor m/\mu\rfloor}W_j+o_{\PP}(\sqrt m).
\]
Therefore
\[
\frac{K_m-m/\mu}{\sqrt m}
=-\frac1\mu\,
\frac{\sum_{j=1}^{\lfloor m/\mu\rfloor}W_j}{\sqrt m}
+o_{\PP}(1),
\]
and uniform integrability yields
\[
\frac{\Var(N_m)}m
\longrightarrow
\frac{\sigma_L^2}{\mu^3}.
\]
Stationarity gives \(\EE N_m=m\rho=m/\mu\), which proves
\eqref{eq:discrete-renewal-fano-identity-main}.

For the spectral form, stationarity gives the exact sampled-window identity
\[
\Var(N_m)=
m\gamma_0+2\sum_{h=1}^{m-1}(m-h)\gamma_h,\qquad
\gamma_h=\operatorname{Cov}(A_0,A_h).
\]
Summability of \((\gamma_h)\) implies
\[
\frac{\Var(N_m)}m\longrightarrow
\gamma_0+2\sum_{h\ge1}\gamma_h=f(0).
\]
Dividing by \(\EE N_m/m=\rho\) gives the equivalent limit \(f(0)/\rho\).
\end{proof}

\begin{proposition}[Spectral density and classical sub-Poisson count noise for the killed-leakage sampled-boundary renewal record]\label{prop:spectrum-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), let \(T_0,T_1\) be the
killed-leakage labelled kernels in \eqref{eq:t0-t1-main} with
\(p=e^{-\Gamma\Delta\tau}\), \(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), and
the one-recovery-opportunity \(a,b\) in \eqref{eq:a-b-main}.  Thus
\(T_1(R,R)=T_1(D,R)=0\), \(P=T_0+T_1\) is stochastic, \(0<p,s<1\),
\(a>0\), \(b>0\), and \(a+b=1-s\).  Let
\((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process generated
by these kernels, put \(\rho=\PP(A_0=1)\) and
\(\lambda_{\mathrm{hid}}=p-b\), and interpret equal-rate expressions by
continuous limits when \(\Gamma=\kappa_{\mathrm r}\).  The stationary
covariance spectral density is
\begin{equation}\label{eq:spectrum-main}
f(\theta)=
\rho(1-\rho)+
2\rho(a-\rho)
\frac{\cos\theta-\lambda_{\mathrm{hid}}}
     {1-2\lambda_{\mathrm{hid}}\cos\theta+\lambda_{\mathrm{hid}}^2}.
\end{equation}
If \(N_m=\sum_{n=0}^{m-1}A_n\), then
\begin{equation}\label{eq:fano-main}
0\le
\lim_{m\to\infty}\frac{\Var(N_m)}{\EE N_m}
=\frac{\Var(G+1)}{\bigl(\EE(G+1)\bigr)^2}
=1-\rho+\frac{2(a-\rho)}{1-\lambda_{\mathrm{hid}}}
<1,
\end{equation}
with \(G\) distributed as in \Cref{lem:renewal-gap-main}.
\end{proposition}

\begin{proof}
Insert the covariance tail from \Cref{lem:covariance-transfer-main} into the Herglotz
Fourier series:
\[
f(\theta)
=\rho(1-\rho)
+2\rho(a-\rho)\sum_{n\ge1}
\lambda_{\mathrm{hid}}^{\,n-1}\cos(n\theta).
\]
The exponent is \(n-1\), not \(n\), because the covariance tail begins with
\(\operatorname{Cov}(A_0,A_1)=\rho(a-\rho)\).  Thus the relevant geometric
series is the shifted series below; using
\(\sum_{n\ge1}\lambda_{\mathrm{hid}}^{n}e^{in\theta}\) would insert an
extraneous factor of \(\lambda_{\mathrm{hid}}\) and give the wrong numerator.
Since \(|\lambda_{\mathrm{hid}}|<1\),
\[
\sum_{n\ge1}\lambda_{\mathrm{hid}}^{\,n-1}e^{in\theta}
=
\frac{e^{i\theta}}{1-\lambda_{\mathrm{hid}}e^{i\theta}},
\]
and taking real parts gives
\[
\sum_{n\ge1}\lambda_{\mathrm{hid}}^{\,n-1}\cos(n\theta)
=
\frac{\cos\theta-\lambda_{\mathrm{hid}}}
       {1-2\lambda_{\mathrm{hid}}\cos\theta+\lambda_{\mathrm{hid}}^2}.
\]
This includes the exact \(1\)-dependent case
\(\lambda_{\mathrm{hid}}=0\), where only the adjacent covariance contributes.
For this sampled count convention,
\Cref{lem:discrete-renewal-fano-main} gives, with \(L=G+1\),
\[
\lim_{m\to\infty}\frac{\Var(N_m)}{\EE N_m}
=\frac{\Var(L)}{(\EE L)^2}.
\]
The same lemma identifies this renewal-count limit with \(f(0)/\rho\), since
\(\rho=1/\EE L\) for the stationary renewal record.  Evaluating the displayed
spectral density at \(\theta=0\) gives the direct cycle-variance computation
\[
\frac{\Var(G+1)}{\bigl(\EE(G+1)\bigr)^2}
=\frac{f(0)}{\rho}
=1-\rho+\frac{2(a-\rho)}{1-\lambda_{\mathrm{hid}}}.
\]
The lower bound in \eqref{eq:fano-main} is therefore inherited from the
nonnegative variance quotient, not from the sign of the closed algebraic
correction.  The upper bound is direct: \(\rho>0\), \(a-\rho<0\) by
\Cref{lem:stationary-main}, and \(\lambda_{\mathrm{hid}}<1\) by
\Cref{lem:hidden-mode-contraction-main}.  Hence
\[
\frac{f(0)}{\rho}-1
=-\rho+\frac{2(a-\rho)}{1-\lambda_{\mathrm{hid}}}<0.
\]
\end{proof}

\begin{remark}[Population lag-two diagnostic and scope]\label{prop:finite-diagnostics-main}\label{rem:diagnostics-consistency-only-main}
For the fixed killed-leakage sampled-boundary kernels in
\eqref{eq:t0-t1-main}, the covariance paragraph in the proof of
\Cref{thm:main-record-wrapper} gives
\[
\gamma_2=\operatorname{Cov}(A_0,A_2)=\rho(a-\rho)\lambda_{\mathrm{hid}} .
\]
The prefactor is nonzero by \Cref{lem:stationary-main}.  Thus the population
diagnostic \(\gamma_2=0\) is exactly the hidden-mode threshold
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) under the
critical-rate convention of \eqref{eq:gamma-crit-piecewise-main}.

The hazard curve, the closed Fano factor, and the finite-record plots derived
from \Cref{lem:survival-hazard-main,prop:spectrum-main} are population formula
checks for the sampled boundary-reset law.  In this manuscript they are not
studentized fixed-setting long-record procedures.  The only diagnostic here with a complete
finite-record studentization theorem is the lag-two covariance statistic in
\Cref{cor:boundary-reset-cycle-clt-main}; hazard and Fano diagnostics should
therefore be read only as consistency checks unless a separate long-run
variance theory is added for the chosen empirical hazard or block-count
functional.
\end{remark}

\subsection{Renewal inclusion products and quotient inverse}

In Markovian-arrival-process language, the pair \((T_0,T_1)\) in
\eqref{eq:t0-t1-main} is a constrained two-state discrete-time MAP:
\(T_0+T_1\) is stochastic, \(T_0\) records a no-arrival transition, and
\(T_1\) records a one-arrival transition.  This places the model inside the
MAP/D-MAP family introduced and developed for correlated arrival streams
\cite{Neuts1979MAP,Lucantoni1991BMAP,AlfaNeuts1995DMAP,
AsanjaraniNazarathy2022MAP}.  The closest
representation comparators are the D-MAP construction of
\cite{AlfaNeuts1995DMAP} and the MAP2 canonical-form and weak-equivalence
results of
\cite{Ryden1996Aggregated,BodrogHeindlHorvathTelek2008MAP2Canonical,
RamirezCoboLilloWiper2010MAP2Nonidentifiability,
RamirezCoboLillo2012WeakEquivalentMAP}, which show that hidden MAP and
phase-type representations are generally nonunique and must be quotiented or
put in a specified representation chart.  The theorem below proves a different
and narrower statement for the sampled killed-leakage D-MAP(2) subfamily:
three visible inclusion probabilities reduce the renewal law to the quotient
coordinates \((p+s,ps)\), so the visible law identifies the unordered pair
\(\{p,s\}\) and no attempt/recovery label beyond it.

\begin{corollary}[Renewal inclusion and finite word law]\label{cor:renewal-principal-minor-word-law-main}
For a stationary renewal click set \(\varXi\) with intensity \(\rho\) and Palm
renewal masses \(u_n=\PP(n\in\varXi\mid0\in\varXi)\), every nonempty finite
ordered set \(B=\{i_1<\cdots<i_m\}\) satisfies
\begin{equation}\label{eq:renewal-inclusion-product-main}
\PP(B\subset\varXi)
=\rho\prod_{r=1}^{m-1}u_{i_{r+1}-i_r}.
\end{equation}
For the empty set this inclusion probability is \(1\).  Exact finite
configuration probabilities follow by finite inclusion-exclusion:
\begin{equation}\label{eq:finite-exact-configuration-probabilities-main}
\PP(\varXi\cap I=S)
=\sum_{C\subset I\setminus S}(-1)^{|C|}
\PP(S\cup C\subset\varXi).
\end{equation}
\end{corollary}

\begin{proof}
Condition on the leftmost click in \(B\).  Under the Palm law at that click,
the future click process renews, so the conditional probability of the
successive gaps \(i_{r+1}-i_r\) being occupied is
\(\prod_{r=1}^{m-1}u_{i_{r+1}-i_r}\).  Multiplying by the stationary click
intensity \(\rho\) gives \eqref{eq:renewal-inclusion-product-main}.  The
finite word formula is the ordinary inclusion-exclusion identity over the
unprescribed click sites \(I\setminus S\).
\end{proof}

\begin{lemma}[Finite LU product identity for renewal products]\label{prop:renewal-principal-minor-lu-main}\label{lem:renewal-principal-minor-main}
Let \(\varXi\) be a stationary renewal set on \(\mathbb Z\) with intensity
\(\rho\) and Palm renewal masses
\(u_n=\PP(n\in\varXi\mid0\in\varXi)\).  For
\(B=\{i_1<\cdots<i_m\}\),
\begin{equation}\label{eq:renewal-inclusion-product-algebra-main}
\PP(B\subset\varXi)
=\rho\prod_{r=1}^{m-1}u_{i_{r+1}-i_r},
\qquad \PP(\varnothing\subset\varXi)=1 .
\end{equation}
After this renewal product is known, define the finite matrix
\begin{equation}\label{eq:renewal-lu-product-matrix-main}
M_{rs}=
\begin{cases}
\rho,&s\le r,\\
\rho-u_{i_s-i_r},&s>r,
\end{cases}
\qquad 1\le r,s\le m,
\end{equation}
and set \(D_{\rm LU}(\varnothing)=1\).  The associated \emph{LU product
determinant} is the finite determinant
\begin{equation}\label{eq:renewal-lu-product-determinant-main}
D_{\rm LU}(B):=\det M(B)
=\rho\prod_{r=1}^{m-1}u_{i_{r+1}-i_r}.
\end{equation}
Equivalently, elementary LU elimination gives the displayed finite LU product
identity.  Thus \(D_{\rm LU}(B)=\PP(B\subset\varXi)\) for the renewal set.
This is only a compact rewriting of the renewal product, not a
determinantal-process assertion or construction.
\end{lemma}

\begin{proof}
The product identity is \Cref{cor:renewal-principal-minor-word-law-main}.
Write \(M=\rho L+U\), where \(L_{rs}=\mathbf1_{\{s\le r\}}\) and
\(U_{rs}=(\rho-u_{i_s-i_r})\mathbf1_{\{s>r\}}\).  Then \(L\) is unit lower
triangular, \(L^{-1}\) has diagonal \(1\) and subdiagonal \(-1\), and
\[
M=L(\rho I+L^{-1}U).
\]
The second factor is upper triangular with diagonal
\(\rho,u_{i_2-i_1},\ldots,u_{i_m-i_{m-1}}\).  Taking determinants gives the
displayed product.
\end{proof}

\begin{remark}[Noncanonical finite LU product identity]\label{rem:finite-determinant-language-main}\label{lem:principal_minor_gauge_nonuniqueness_main}
\Cref{prop:renewal-principal-minor-lu-main} does not make a determinantal
process assertion.  The mathematical content is the renewal inclusion product
already supplied by renewal conditioning; \(M(B)\) and \(D_{\rm LU}(B)\) are
set-indexed finite LU bookkeeping for that product.  No Hermitian,
positive-contraction, operator, nonsymmetric, or sampling point-process kernel
structure is asserted for \(M(B)\) or \(D_{\rm LU}(B)\).  In particular,
\(D_{\rm LU}\) is not DPP notation: no process is being defined by assigning
all inclusion probabilities to minors of an admissible kernel.
\end{remark}

\begin{lemma}[Boundary-reset specialization of the renewal product notation]\label{cor:boundary_reset_toeplitz_renewal_specialization}\label{cor:finite_window_principal_minor_word_law}\label{prop:finite_inclusion_tail_phase}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), and let
\((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process generated
by the killed-leakage kernels \eqref{eq:t0-t1-main} with
\(p=e^{-\Gamma\Delta\tau}\), \(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), the
positive \(a,b\) from \eqref{eq:a-b-main}, \(T_1(R,R)=T_1(D,R)=0\), and
continuous equal-rate interpretation on \(\Gamma=\kappa_{\mathrm r}\).  Then
\begin{equation}\label{eq:finite-inclusion-u-n-main}
u_n=\PP(A_n=1\mid A_0=1)
=\rho+(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad n\ge1.
\end{equation}
Thus the boundary-reset renewal inclusion products are obtained from
\eqref{eq:renewal-inclusion-product-main} by this \(u_n\).  The specialized
LU product determinant \(D_{{\rm LU},\Gamma}(B)\) is obtained from
\eqref{eq:renewal-lu-product-matrix-main} and
\eqref{eq:renewal-lu-product-determinant-main} after the inclusion product is
known, and finite word laws are given by
\eqref{eq:finite-exact-configuration-probabilities-main}.
\end{lemma}

\begin{proof}
The formula for \(u_n\) is \Cref{lem:covariance-transfer-main}; the renewal
inclusion product, finite LU product identity, and word formulas are
\Cref{prop:renewal-principal-minor-lu-main,cor:renewal-principal-minor-word-law-main}.
\end{proof}

\begin{lemma}[Symmetric-coordinate algebra for the quotient inverse]\label{lem:quotient-inverse-symmetric-algebra-main}
Let \(p\in(0,1)\), put \(q=1-p\), and let the sampled-boundary parameters satisfy
\[
a+b=1-s,\qquad
\lambda_{\mathrm{hid}}=p-b,\qquad
\rho=\frac{q(1-s)}{q+b}.
\]
Then
\begin{equation}\label{eq:quotient-inverse-s1-algebra-main}
1-a+\lambda_{\mathrm{hid}}=p+s
\end{equation}
and
\begin{equation}\label{eq:quotient-inverse-s2-algebra-main}
\rho(1-\lambda_{\mathrm{hid}})-a+\lambda_{\mathrm{hid}}=ps .
\end{equation}
\end{lemma}

\begin{proof}
Since \(a+b=1-s\), we have \(a=1-s-b\).  Therefore
\[
1-a+\lambda_{\mathrm{hid}}
=1-(1-s-b)+(p-b)
=p+s,
\]
which proves \eqref{eq:quotient-inverse-s1-algebra-main}.  For the second
identity, first note that
\[
1-\lambda_{\mathrm{hid}}=1-p+b=q+b.
\]
Thus
\[
\rho(1-\lambda_{\mathrm{hid}})
=\frac{q(1-s)}{q+b}\,(q+b)
=q(1-s).
\]
Combining this with \(a=1-s-b\) and \(\lambda_{\mathrm{hid}}=p-b\) gives
\[
\rho(1-\lambda_{\mathrm{hid}})-a+\lambda_{\mathrm{hid}}
=q(1-s)-(1-s-b)+(p-b)
=q(1-s)+p+s-1.
\]
Because \(p+q=1\), the last expression is
\[
q(1-s)+p+s-1
=s-qs
=ps,
\]
as claimed.
\end{proof}

\begin{lemma}[Exchange symmetry in dimensionless survival coordinates]\label{lem:gap-law-exchange-symmetry-main}
Put
\[
x=-\log p=\Gamma\Delta\tau,\qquad
y=-\log s=\kappa_{\mathrm r}\Delta\tau,
\qquad p=e^{-x},\qquad s=e^{-y}.
\]
For \(x\ne y\), the zero-run law in \eqref{eq:gap-closed-main} is
\begin{equation}\label{eq:symmetric-gap-law-main}
g_k=
\frac{y(1-p)p^k-x(1-s)s^k}{y-x},
\qquad k\ge0.
\end{equation}
The hidden eigenvalue in \eqref{eq:lambda-hid-main} is, in the same
coordinates,
\begin{equation}\label{eq:symmetric-lambda-hid-main}
\lambda_{\mathrm{hid}}
=\frac{y s-x p}{y-x}
=\frac{y e^{-y}-x e^{-x}}{y-x}.
\end{equation}
Both \eqref{eq:symmetric-gap-law-main} and
\eqref{eq:symmetric-lambda-hid-main} are invariant under the simultaneous
exchange \((x,p)\leftrightarrow(y,s)\).  The equal-rate laws are their
continuous limits.  Consequently the stationary visible renewal law is
unchanged by exchanging \(\Gamma\) and \(\kappa_{\mathrm r}\).
\end{lemma}

\begin{proof}
Multiplying numerator and denominator in \eqref{eq:gap-closed-main} by
\(\Delta\tau\) gives \eqref{eq:symmetric-gap-law-main}.  Similarly,
\eqref{eq:lambda-hid-main} becomes
\[
\lambda_{\mathrm{hid}}
=\frac{\kappa_{\mathrm r}s-\Gamma p}{\kappa_{\mathrm r}-\Gamma}
=\frac{y s-x p}{y-x}.
\]
Under \((x,p)\leftrightarrow(y,s)\), the numerator
\[
y(1-p)p^k-x(1-s)s^k
\]
and the denominator \(y-x\) both change sign, so \(g_k\) is unchanged.  The
same sign cancellation gives
\[
y s-x p\longmapsto x p-y s=-(y s-x p),
\qquad
y-x\longmapsto x-y=-(y-x),
\]
and hence \(\lambda_{\mathrm{hid}}\) is unchanged.  The diagonal formulas
follow by taking the continuous limits \(x\to y\).  Since
\Cref{lem:renewal-gap-main} proves that the visible law is the stationary
renewal law with gap probabilities \(g_k\), the unordered pair
\(\{(x,p),(y,s)\}\) determines the whole gap distribution.  It also determines
the stationary renewal intensity \(1/\EE(G+1)\).  Therefore the stationary
renewal measures generated before and after the exchange have the same
finite-dimensional visible distributions.
\end{proof}

\begin{convention}[Identifiable estimands and branch-map convention]\label{conv:branch-map-labelled-rates-main}
For one stationary visible record from the fixed killed-leakage
sampled-boundary family, the record-level estimands are
\[
(r_0,r_1,r_2),\qquad
\Phi(r)=(p+s,ps),\qquad
\gamma_2=\operatorname{Cov}(A_0,A_2),
\]
and their regenerative covariance matrices.  The inverse chart first requires
\[
D:=r_1-r_0^2\ne0 .
\]
It then identifies only the unordered survival-factor pair
\[
\{p,s\}=\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}.
\]
The ordered labelled vector \((\Gamma,\kappa_{\mathrm r})\) is not a
visible-law estimand without extra branch information.
Separated labelled roots and separated labelled rates are available only under
the additional conditions
\[
\Delta_\sigma=(p-s)^2>0
\]
and an externally supplied branch map
\[
\mathcal B:\{\text{the two roots of }z^2-\sigma_1z+\sigma_2=0\}\to
\{\text{attempt},\text{recovery}\}.
\]
In this paper, no branch map means no separated labelled rates.  Without such
a supplied map, every separated labelled-rate statement below is only a statement
about the unordered rate set
\[
\left\{-\frac{\log z_1}{\Delta\tau},
      -\frac{\log z_2}{\Delta\tau}\right\}
\]
or equivalently about the symmetric coordinates \((p+s,ps)\).  On the exchange
diagonal \(\sigma=(2z,z^2)\), \(0<z<1\), the two entries coincide and the
common numerical rate \(-\log z/\Delta\tau\) is an invariant visible-law
estimand; what fails there is only the separated ordered-branch chart.
\end{convention}

\begin{theorem}[Main Theorem 2: quotient inverse]\label{thm:toeplitz_image_inverse_duality}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and define
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\]
where
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,
\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
These are the killed-leakage labelled kernels:
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\) and
\(T_1(R,R)=T_1(D,R)=0\).  The row sum \(P=T_0+T_1\) is stochastic,
\(0<p,s<1\), \(a>0\), \(b>0\), and \(a+b=1-s\).  Let
\((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process generated by
these kernels, and interpret all formulas on the equal-rate diagonal
\(\Gamma=\kappa_{\mathrm r}\) by continuous limits.  Let
\[
r_0=\rho,\qquad r_1=\rho a,\qquad r_2=\rho u_2 .
\]
The population inverse is defined on the domain
\begin{equation}\label{eq:population-inverse-domain-main}
r_0>0,\qquad a-r_0\ne0
\quad
\left(\text{equivalently } r_0>0,\ r_1-r_0^2\ne0\right).
\end{equation}
We write the inverse-map denominator as
\begin{equation}\label{eq:inverse-map-denominator-main}
D:=r_1-r_0^2=r_0(a-r_0).
\end{equation}
For the sampled-boundary kernels this domain condition holds:
\(r_0=\rho>0\) and \(a-r_0=a-\rho<0\) by
\Cref{lem:stationary-main}.  On this domain,
\[
a=\frac{r_1}{r_0},\qquad
u_2=\frac{r_2}{r_0},\qquad
\lambda_{\mathrm{hid}}=\frac{u_2-r_0}{a-r_0}
=\frac{r_2-r_0^2}{r_1-r_0^2}.
\]
The symmetric coordinate vector is
\begin{equation}\label{eq:quotient-symmetric-coordinates-main}
\sigma_{\rm sym}=(\sigma_1,\sigma_2),\qquad
\sigma_1=1-a+\lambda_{\mathrm{hid}},\qquad
\sigma_2=r_0(1-\lambda_{\mathrm{hid}})-a+\lambda_{\mathrm{hid}}.
\end{equation}
They equal
\[
\sigma_1=p+s,\qquad \sigma_2=ps,
\]
so the visible law determines the unordered survival-factor pair
\[
\{p,s\}=\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\},
\]
or equivalently the unordered roots of \(z^2-\sigma_1z+\sigma_2=0\).  Its
root discriminant is
\[
\Delta_\sigma:=\sigma_1^2-4\sigma_2=(p-s)^2 .
\]
Within this constrained sampled D-MAP(2) subfamily, the labelled formulas with
\(p\) attached to \(\Gamma\) and \(s\) attached to \(\kappa_{\mathrm r}\) are
not themselves identified; only the symmetric polynomial coordinates are.
\Cref{conv:branch-map-labelled-rates-main} is the branch-map convention used
by all later labelled-rate statements.  The first
singularity is the inverse denominator
\[
D=r_1-r_0^2=r_0(a-r_0),
\]
which must be nonzero to recover \(\lambda_{\mathrm{hid}}\).  The second is
the labelled-root discriminant: the separated labelled-root chart requires
\(\Delta_\sigma>0\) and the external branch-map input specified in
\Cref{conv:branch-map-labelled-rates-main}.  The labelled
chart has denominator \(p-s\) and therefore fails at the
exchange diagonal \(p=s\), equivalently \(\Delta_\sigma=0\).  The dimensionless formulas
\eqref{eq:symmetric-gap-law-main}--\eqref{eq:symmetric-lambda-hid-main} show
that the stationary visible law generated by \((\Gamma,\kappa_{\mathrm r})\)
equals that generated by \((\kappa_{\mathrm r},\Gamma)\).  Thus the visible
law does not label which root is the attempt survival factor and which is the
recovery survival factor; by \Cref{conv:branch-map-labelled-rates-main}, that
label is an external input, not a statistic of one visible record.  No claim is
made that arbitrary MAP2 representations are identified by these three
coordinates.  At the exchange diagonal \(\sigma=(2z,z^2)\), \(0<z<1\), the
unordered pair collapses to the single survival factor \(z\), so the common
numerical rate \(-\log z/\Delta\tau\) remains identifiable even though no
separated ordered branch is.
For finite-record use of this inverse chart, restrict to stability
neighbourhoods where, for some \(\epsilon,\delta>0\),
\[
r_0\ge\epsilon,\qquad |a-r_0|\ge\epsilon,\qquad
0<p,s<1,\qquad \Delta_\sigma=(p-s)^2\ge\delta^2,
\]
unless only the projected symmetric coordinates are reported at the exchange
diagonal.  These hypotheses keep the inverse denominator away from zero and
separate the two roots when labelled rate charts are used.
Operational finite-record estimators are therefore not obtained by blindly
evaluating the rational inverse and the labelled-root chart on every sample
path.  The finite-record theorem below uses a thresholded inverse denominator,
a feasible projection in the symmetric coordinates, and a no-label rule when
the empirical projected discriminant is below its retention threshold.
\end{theorem}

\begin{proof}
\Cref{lem:gap-law-exchange-symmetry-main} proves the exchange invariance of
the stationary visible renewal law by rewriting both \(g_k\) and
\(\lambda_{\mathrm{hid}}\) in the coordinates
\((x,p)=(-\log p,p)\) and \((y,s)=(-\log s,s)\).  Conversely, from
\[
r_0=\rho,\qquad r_1=\PP(A_0=A_1=1)=\rho a,\qquad
r_2=\PP(A_0=A_2=1)=\rho u_2
\]
the domain condition \eqref{eq:population-inverse-domain-main} permits the
divisions below.  Hence one obtains \(a=r_1/r_0\), \(u_2=r_2/r_0\), and
\[
\lambda_{\mathrm{hid}}
=\frac{u_2-r_0}{a-r_0}
=\frac{r_2/r_0-r_0}{r_1/r_0-r_0}
=\frac{r_2-r_0^2}{r_1-r_0^2}.
\]
The last equality uses only scalar division by \(r_0>0\) and by
\(D=r_1-r_0^2=r_0(a-r_0)\ne0\), precisely the stated inverse domain.
Then the identities
\[
\sigma_1=1-a+\lambda_{\mathrm{hid}},
\qquad
\sigma_2=r_0(1-\lambda_{\mathrm{hid}})-a+\lambda_{\mathrm{hid}}
\]
recover \(p+s\) and \(ps\) by
\Cref{lem:quotient-inverse-symmetric-algebra-main}, since \(r_0=\rho\) for
the sampled-boundary law.  The unordered roots of
\(z^2-\sigma_1z+\sigma_2=0\) are therefore identified, but their labels are
not.  Finally, \Cref{lem:gap-law-exchange-symmetry-main} proves that swapping
the two roots also leaves the stationary renewal gap law, and hence all
finite-dimensional visible distributions, unchanged.  The inverse therefore
lands on the quotient by the exchange \((p,\Gamma)\leftrightarrow
(s,\kappa_{\mathrm r})\), not on separated labelled rates.  At a double root
the quotient itself contains the common numerical rate, as made explicit in
\Cref{lem:diagonal-common-rate-main}.
\end{proof}

\begin{corollary}[Three inclusion probabilities are complete quotient coordinates]\label{cor:three_inclusion_complete_coordinates}
For the stationary visible law of the killed-leakage sampled-boundary kernels
in \Cref{thm:toeplitz_image_inverse_duality}, with positive
\(\Gamma,\kappa_{\mathrm r},\Delta\tau\), \(T_1(R,R)=T_1(D,R)=0\), and
equal-rate formulas interpreted continuously, the three inclusion probabilities
\((r_0,r_1,r_2)=(\rho,\rho a,\rho u_2)\) determine the full stationary visible
law up to the exchange
\((\Gamma,\kappa_{\mathrm r})\leftrightarrow(\kappa_{\mathrm r},\Gamma)\).
\end{corollary}

\begin{proof}
They determine \(p+s\) and \(ps\) by the proof of
\Cref{thm:toeplitz_image_inverse_duality}; substituting the resulting
unordered pair into \eqref{eq:gap-closed-main} determines the renewal law and
therefore all finite-dimensional distributions.
\end{proof}

\begin{lemma}[Diagonal common-rate estimand]\label{lem:diagonal-common-rate-main}
On the exchange diagonal
\[
\sigma=(\sigma_1,\sigma_2)=(2z,z^2),\qquad 0<z<1,
\]
the visible quotient law identifies the common labelled numerical rate
\[
\gamma_{\rm diag}:=-\frac{\log z}{\Delta\tau}.
\]
Equivalently, it identifies the invariant ordered pair
\[
(\gamma_{\rm diag},\gamma_{\rm diag}).
\]
It does not identify a separated attempt/recovery branch, because the
separated-root map has derivative denominator \(p-s=0\).  Thus separated-root
Wald intervals are invalid on the diagonal, but reporting
\((\gamma_{\rm diag},\gamma_{\rm diag})\) is valid as a quotient invariant.
\end{lemma}

\begin{proof}
The polynomial with symmetric coordinate \((2z,z^2)\) is
\[
t^2-2zt+z^2=(t-z)^2,
\]
so both unordered roots equal \(z\).  Applying the fixed sampling map
\(w\mapsto-\log w/\Delta\tau\) to both entries gives the ordered pair
\((\gamma_{\rm diag},\gamma_{\rm diag})\).  Because the two entries are equal,
exchanging the hidden attempt and recovery labels leaves this pair unchanged,
so it is a visible-law quotient invariant.  By contrast, the separated-root
differential
\[
dw=\frac{w\,d\sigma_1-d\sigma_2}{2w-\sigma_1}
\]
has denominator \(2z-2z=0\) at the double root, equivalently \(p-s=0\).
Therefore the ordinary separated-root delta method and its Wald intervals do
not apply on the exchange diagonal; the available first-order finite-record
limit is the projected symmetric-coordinate limit in
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main}.
\end{proof}

\begin{proposition}[Identifiable estimands]\label{thm:identifiable-finite-record-estimands-main}
In the fixed-setting killed-leakage sampled-boundary model of
\Cref{thm:toeplitz_image_inverse_duality}, a stationary visible record
identifies only functionals of its stationary visible law.  The finite-record
CLT and covariance statements below use
\Cref{conv:branch-map-labelled-rates-main}.  In
particular, they are statements about the following estimands:
\[
(r_0,r_1,r_2),\qquad
\Phi(r)=(p+s,ps),\qquad
\gamma_2=\operatorname{Cov}(A_0,A_2),
\]
and their regenerative covariance matrices.  These are invariant under the
hidden-label exchange
\((p,\Gamma)\leftrightarrow(s,\kappa_{\mathrm r})\).

A separated labelled-rate vector
\[
(\Gamma,\kappa_{\mathrm r})
=\left(-\frac{\log p}{\Delta\tau},
       -\frac{\log s}{\Delta\tau}\right)
\]
is not an estimand of the visible law alone when \(p\ne s\).  Any
finite-record confidence statement for separated labelled rates is conditional
on two additional pieces of input: \(\Delta_\sigma>0\), so that the two roots
are separated, and a supplied branch map
\[
\mathcal B:\{\text{the two roots of }z^2-\sigma_1z+\sigma_2=0\}\to
\{\text{attempt},\text{recovery}\}
\]
which assigns the separated roots to the ordered pair \((p,s)\) on a
neighbourhood of the population point.  No result below supplies, estimates,
or validates \(\mathcal B\) from one visible record.  By
\Cref{conv:branch-map-labelled-rates-main}, without such a supplied branch map
the separated labelled-rate conclusions must be read only as conclusions for
the unordered rate set
\[
\left\{-\frac{\log z_1}{\Delta\tau},
      -\frac{\log z_2}{\Delta\tau}\right\},
\]
or, equivalently, for the symmetric coordinates \((p+s,ps)\).  If the
population point is instead on the exchange diagonal,
\(\sigma=(2z,z^2)\), the two ordered entries coincide and the common-rate
vector in \Cref{lem:diagonal-common-rate-main} is identified.
\end{proposition}

\begin{proof}
\Cref{thm:toeplitz_image_inverse_duality} identifies the quotient
coordinates \((p+s,ps)\) from \((r_0,r_1,r_2)\) and proves that the stationary
visible law is unchanged by the exchange
\((p,\Gamma)\leftrightarrow(s,\kappa_{\mathrm r})\).  Hence no statistic of
one visible record can distinguish which separated root is the attempt
survival factor and which is the recovery survival factor.  The listed
estimands are functions of the visible law and are therefore identifiable.
At separated roots, an ordered labelled-rate vector is obtained from the
unordered roots only after an externally supplied branch map chooses the
ordering, as in \Cref{conv:branch-map-labelled-rates-main}.  At a double root the
ordering is immaterial, and \Cref{lem:diagonal-common-rate-main} records the
identified common-rate vector.
\end{proof}

\noindent\fbox{%
\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{%
\textbf{Inverse and branch disclosure.}
The visible law identifies only
\[
\sigma=(\sigma_1,\sigma_2)=(p+s,ps).
\]
The labelled vector \((\Gamma,\kappa_{\mathrm r})\) is not a visible-law
estimand.  At separated roots, a labelled chart is conditional on an external
branch map \(\mathcal B\).  At \(p=s\), separated labelled branches are not
reported, but the common rate in
\Cref{lem:diagonal-common-rate-main} remains an identified invariant.  The
labelled-root differential below is therefore only the conditional
separated-branch differential
\[
dz=\frac{z\,d\sigma_1-d\sigma_2}{2z-\sigma_1}.
\]
At \(p=s\), the denominator vanishes.  The labelled-root delta method is then
invalid, and \((\widehat\Gamma,\widehat\kappa_{\mathrm r})\) must not be read
as an unconditional estimator.}}

\begin{lemma}[Separated-root labelled-rate delta method]\label{lem:separated-root-rate-delta-main}
Let \(r=(r_0,r_1,r_2)\) lie in the population inverse domain of
\Cref{thm:toeplitz_image_inverse_duality}, let
\[
\Phi(r)=\sigma_{\rm sym}=(\sigma_1,\sigma_2)=(p+s,ps),
\]
and suppose the two roots \(p\ne s\) both lie in \((0,1)\).  A labelled root
chart is a supplied branch map \(\mathcal B\), external to the visible record,
which assigns the two local roots to the attempt survival factor \(p\) and the
recovery survival factor \(s\).  On any neighbourhood where \(|p-s|\) is
bounded away from zero and \(\mathcal B\) is fixed, this chart is \(C^1\), and
its Jacobian with respect to the
symmetric coordinates is
\begin{equation}\label{eq:separated-root-jacobian-main}
J_{\mathrm{root}}
=
\begin{pmatrix}
\dfrac{p}{p-s} & -\dfrac1{p-s}\\[1.1em]
\dfrac{s}{s-p} & -\dfrac1{s-p}
\end{pmatrix}.
\end{equation}
Equivalently, for either labelled root \(z\in\{p,s\}\),
\[
dz=\frac{z\,d\sigma_1-d\sigma_2}{2z-\sigma_1}.
\]
For the fixed sampling interval \(\Delta\tau\), the labelled rate map
\[
R(p,s)=\left(-\frac{\log p}{\Delta\tau},
              -\frac{\log s}{\Delta\tau}\right)
=(\Gamma,\kappa_{\mathrm r})
\]
has Jacobian
\begin{equation}\label{eq:separated-rate-jacobian-main}
J_{\mathrm{rate}}
=
\begin{pmatrix}
-\dfrac1{\Delta\tau p} & 0\\[0.8em]
0 & -\dfrac1{\Delta\tau s}
\end{pmatrix}
J_{\mathrm{root}} .
\end{equation}
The full Jacobian from inclusion coordinates to labelled rates is therefore
\begin{equation}\label{eq:inclusion-to-rate-jacobian-main}
J_{\Gamma,\kappa}(r)
=J_{\mathrm{rate}}\,J_\Phi(r),
\qquad
J_\Phi(r)=D\Phi(r).
\end{equation}
Writing
\[
D=r_1-r_0^2,\qquad E=r_2-r_0^2,\qquad
a=\frac{r_1}{r_0},\qquad \lambda=\frac{E}{D},
\]
the entries of \(J_\Phi\) are explicit:
\begin{align}
\frac{\partial\lambda}{\partial r_0}
&=-\frac{2r_0(r_1-r_2)}{D^2},&
\frac{\partial\lambda}{\partial r_1}
&=-\frac{E}{D^2},&
\frac{\partial\lambda}{\partial r_2}
&=\frac1D, \label{eq:lambda-gradient-main}\\
D\Phi_1
&=
\left(
\frac{a}{r_0}+\lambda_{0},\
-\frac1{r_0}+\lambda_{1},\
\lambda_{2}
\right),\nonumber\\
D\Phi_2
&=
\left(
1-\lambda+\frac{a}{r_0}+(1-r_0)\lambda_{0},\
-\frac1{r_0}+(1-r_0)\lambda_{1},\
(1-r_0)\lambda_{2}
\right), \label{eq:phi-jacobian-main}
\end{align}
where \(\lambda_i=\partial\lambda/\partial r_i\).

Consequently, if
\[
\sqrt N(\widehat r_N-r)\Rightarrow \mathcal N(0,\Sigma_r),
\qquad
\widehat\Sigma_{r,N}\to\Sigma_r
\quad\text{in probability},
\]
and \(\widehat r_N\) remains in the same separated-root chart with probability
tending to one, then
\[
\sqrt N\{(\widehat p_N,\widehat s_N)-(p,s)\}
\Rightarrow
\mathcal N(0,\Sigma_{\mathrm{root}}),
\qquad
\Sigma_{\mathrm{root}}
=J_{\mathrm{root}}\,\Sigma_{\sigma}\,J_{\mathrm{root}}^\top,
\]
\[
\sqrt N\{(\widehat\Gamma_N,\widehat\kappa_{\mathrm r,N})
-(\Gamma,\kappa_{\mathrm r})\}
\Rightarrow
\mathcal N(0,\Sigma_{\Gamma,\kappa}),
\qquad
\Sigma_{\Gamma,\kappa}
=J_{\Gamma,\kappa}(r)\,\Sigma_r\,J_{\Gamma,\kappa}(r)^\top,
\]
where \(\Sigma_{\sigma}=J_\Phi(r)\Sigma_rJ_\Phi(r)^\top\).  The plug-in covariance
estimators obtained by replacing \(r,\Sigma_r,p,s\) with their separated-chart
estimates are consistent.

For retained-rank Wald reporting in the labelled rate chart, let
\(k=\operatorname{rank}\Sigma_{\Gamma,\kappa}\).  If the thresholded covariance
\(\widehat\Sigma_{\Gamma,\kappa,N}\) satisfies
\[
\|\widehat\Sigma_{\Gamma,\kappa,N}-\Sigma_{\Gamma,\kappa}\|_{\operatorname{op}}
=o_{\PP}(t_N),\qquad t_N\downarrow0,
\]
then the threshold retains exactly the \(k\) positive limiting eigenvalues
with probability tending to one, and
\[
N\,
\{(\widehat\Gamma_N,\widehat\kappa_{\mathrm r,N})
-(\Gamma,\kappa_{\mathrm r})\}^{\top}
\widehat\Sigma_{\Gamma,\kappa,N}^{+}
\{(\widehat\Gamma_N,\widehat\kappa_{\mathrm r,N})
-(\Gamma,\kappa_{\mathrm r})\}
\Rightarrow \chi_k^2 .
\]
This separated labelled-rate conclusion is conditional on the supplied branch
map \(\mathcal B\) in \Cref{conv:branch-map-labelled-rates-main}.  The visible
law alone still identifies only the unordered pair at separated roots, and the
common-rate vector at a double root, as stated in
\Cref{thm:identifiable-finite-record-estimands-main,lem:diagonal-common-rate-main}.
\end{lemma}

\begin{proof}
The implicit equation for a root is
\(z^2-\sigma_1z+\sigma_2=0\).  Differentiating gives
\[
(2z-\sigma_1)\,dz-z\,d\sigma_1+d\sigma_2=0.
\]
At separated roots \(2p-\sigma_1=p-s\) and \(2s-\sigma_1=s-p\), which gives
\eqref{eq:separated-root-jacobian-main}.  Thus the labelled-root differential
itself has a pole at \(p=s\); the quotient map \(\Phi\) remains the symmetric
coordinate map there, but the labelled-root and labelled-rate charts do not
extend through the exchange diagonal.  The logarithmic rate Jacobian is
the ordinary derivative of
\((p,s)\mapsto(-\log p/\Delta\tau,-\log s/\Delta\tau)\), proving
\eqref{eq:separated-rate-jacobian-main}.  The displayed formula for
\(\lambda=E/D\) is the quotient rule, and
\eqref{eq:phi-jacobian-main} follows by differentiating
\[
\Phi_1=1-a+\lambda,\qquad
\Phi_2=r_0-a+(1-r_0)\lambda .
\]
All denominators are bounded away from zero on the stated neighbourhood, so
the maps and plug-in Jacobians are continuous there.  The two Gaussian limits
are then the multivariate delta method, and covariance consistency follows by
continuous mapping.  The retained-rank statement is Weyl's eigenvalue
inequality plus Slutsky's theorem on the limiting range of
\(\Sigma_{\Gamma,\kappa}\).
\end{proof}

\subsection{Finite-record inference and the exchange diagonal}

This subsection is organized by inferential status.  First come generic
regenerative and batching tools, which apply to any bounded finite-window
observable of the renewal record.  The sampled-boundary model is then verified
against those hypotheses.  The inclusion-coordinate CLT and its covariance
estimator follow next.  The inverse statements are delta-method chart
statements for identifiable symmetric coordinates, with labelled rates only on
an externally supplied separated-root branch.  The only fully studentized
finite-record confidence interval in this paper is the scalar lag-two interval
in \Cref{cor:boundary-reset-cycle-clt-main}.

\paragraph{Generic regenerative tools.}
This block fixes the finite-record complete-cycle convention and records the
model-independent renewal tools used throughout the finite-record section:
bounded local calendar windows become measurable cycle rewards,
calendar-stopped separated batches have a consistent covariance rate, and
projected-Gaussian calibration handles the exchange diagonal.  These tools do
not by themselves assert a studentized interval.  They are standard
simulation-output devices, used here only after the killed-leakage renewal gap
law has verified the required exponential tail
\cite{GlynnIglehart1990STS,AsmussenGlynn2007,
VatsFlegalJones2019Multivariate}.

\begin{definition}[Cycle-index inclusion estimator]\label{def:cycle-index-inclusion-estimator-main}
For a finite observed record \(A_0,\ldots,A_{N-1}\), write the complete click
epochs in the observed window as
\[
\tau_0<\tau_1<\cdots<\tau_{K_N},\qquad K_N=\max\{k:\tau_k<N\}.
\]
If the observed window contains no click, set \(K_N=-1\) and every
all-complete-cycle index set below to the empty set.  Thus \(K_N+1\) is the
number of observed clicks, and \(K_N\) is one less than the number of click
epochs from which complete observed cycles can be formed.  Set
\(L_j=\tau_{j+1}-\tau_j\) whenever both endpoints are observed.  For
\(m=0,1,2\), put
\[
X_n^{(m)}=A_nA_{n+m},\qquad X_n^{(0)}=A_n,
\]
and define the complete-cycle uncentered reward
\[
U_j^{(m)}=\sum_{n=\tau_j}^{\tau_{j+1}-1}X_n^{(m)}.
\]
Set \(c_0=c_1=0\) and \(c_2=1\).  The admissible all-complete-cycle index set is
\[
\begin{aligned}
J_{m,N}
=\{0\le j\le K_N-c_m-1:\;&
\tau_j,\tau_{j+1}\in[0,N-1],\\
&\text{and every }n+m\text{ entering }U_j^{(m)}
\text{ lies in }[0,N-1]\}.
\end{aligned}
\]
The cycle-index estimator of the three inclusion coordinates
\[
r=(r_0,r_1,r_2)=(\PP(A_0=1),\PP(A_0=A_1=1),\PP(A_0=A_2=1))
\]
is
\[
\widehat r_N^{\mathrm{cyc}}
=\bigl(\widehat r_{0,N}^{\mathrm{cyc}},
\widehat r_{1,N}^{\mathrm{cyc}},
\widehat r_{2,N}^{\mathrm{cyc}}\bigr)^\top,\qquad
\widehat r_{m,N}^{\mathrm{cyc}}
=\frac1N\sum_{j\in J_{m,N}}U_j^{(m)} .
\]
Empty sums are \(0\).  Hence the estimator is a total finite-record statistic:
on records with too few observed clicks, or with no admissible terminal
look-ahead cycles for the requested lag, the corresponding coordinate is
defined to be \(0\).
For covariance estimation the three coordinates are evaluated on the common
complete-cycle set \(J_N=J_{0,N}\cap J_{1,N}\cap J_{2,N}\).  With plug-in
centering, the vector reward used in the separated batches is
\[
\widehat Y_j=
\bigl(U_j^{(0)}-\widehat r_{0,N}^{\mathrm{cyc}}L_j,\,
U_j^{(1)}-\widehat r_{1,N}^{\mathrm{cyc}}L_j,\,
U_j^{(2)}-\widehat r_{2,N}^{\mathrm{cyc}}L_j\bigr)^\top .
\]
\end{definition}

\begin{lemma}[Measurable local-window-to-cycle rewards]\label{lem:local-window-regenerative-decomposition-main}
Let \((A_n)_{n\in\mathbb Z}\) be a stationary binary renewal process with
click epochs \((\tau_j)_{j\in\mathbb Z}\), i.i.d. cycle lengths
\(L_j=\tau_{j+1}-\tau_j=G_j+1\), mean
\(\mu=\EE L_0\in(0,\infty)\), and \(\EE e^{\eta G_0}<\infty\) for some
\(\eta>0\).  Fix \(r,\ell\ge0\) and let
\[
Z_n=\psi(A_{n-r},\ldots,A_{n+\ell})\in\RR^{d_{\rm rew}},
\qquad
\bar z=\EE Z_0 ,
\]
where \(\psi\) is bounded.  Put
\[
q_*(r,\ell):=r+\max\{\ell-1,0\}.
\]
For \(g=(g_0,\ldots,g_{q_*})\in\mathbb N_0^{q_*+1}\), set
\[
S_0(g)=0,\qquad
S_m(g)=\sum_{i=0}^{m-1}(g_i+1),\quad 1\le m\le r+\ell,
\]
and define the finite local click set, in coordinates whose origin is the
start of cycle \(r\), by
\[
\mathcal C_{r,\ell}(g):=\{S_m(g)-S_r(g):0\le m\le r+\ell\}.
\]
Here \(S_{r+\ell}\) is defined by the displayed formula; when \(\ell=0\), no
gap beyond \(g_r\) is needed and \(q_*=r\).  Define the measurable maps
\begin{equation}\label{eq:local-window-cycle-map-main}
\begin{aligned}
F_{r,\ell}^{\rm raw}(g_0,\ldots,g_{q_*})
&:=\sum_{u=0}^{g_r}
\psi\bigl(
\mathbf1_{\{u-r\in\mathcal C_{r,\ell}(g)\}},\ldots,
\mathbf1_{\{u+\ell\in\mathcal C_{r,\ell}(g)\}}
\bigr),\\
F_{r,\ell}(g_0,\ldots,g_{q_*})
&:=F_{r,\ell}^{\rm raw}(g_0,\ldots,g_{q_*})-\bar z\,(g_r+1).
\end{aligned}
\end{equation}
For each cycle \(j\), define the anchor-to-cycle rewards
\[
Y_j^{\rm raw}:=U_j=\sum_{n=\tau_j}^{\tau_{j+1}-1}Z_n,\qquad
Y_j=U_j-\bar z\,L_j ,
\]
and the future-indexed version
\[
\widetilde Y_j^{\rm raw}:=U_{j+r},\qquad
\widetilde Y_j:=Y_{j+r}.
\]
Then the following hold.
\begin{enumerate}[label=\textup{(\alph*)},leftmargin=2.4em,itemsep=0.2em]
\item Palm inversion gives
\begin{equation}\label{eq:local-window-palm-inversion-main}
\EE U_j=\mu\,\bar z,\qquad \EE Y_j=0 .
\end{equation}
Equivalently, before centering,
\(\EE Y_j^{\rm raw}=\EE(L_0)\,\EE Z_0\).
\item The reward is exactly a measurable function of the displayed finite gap
block:
\begin{equation}\label{eq:local-window-future-block-main}
\widetilde Y_j^{\rm raw}
=F_{r,\ell}^{\rm raw}(G_j,\ldots,G_{j+q_*}),
\qquad
\widetilde Y_j
=F_{r,\ell}(G_j,\ldots,G_{j+q_*}).
\end{equation}
Equivalently,
\[
Y_j^{\rm raw}
=F_{r,\ell}^{\rm raw}(G_{j-r},\ldots,G_{j-r+q_*}),
\qquad
Y_j
=F_{r,\ell}(G_{j-r},\ldots,G_{j-r+q_*}).
\]
Thus the centered and uncentered future-indexed reward sequences are
\(q_*(r,\ell)\)-dependent.  A larger displayed range may be used later by
padding \(F_{r,\ell}\) with unused coordinates, but no informal reassignment
is involved.
\item For the observed window \([0,N)\), let
\[
j_-(N)=\min\{j:[\tau_j,\tau_{j+1})\cap[0,N)\ne\varnothing\},\qquad
j_+(N)=\max\{j:[\tau_j,\tau_{j+1})\cap[0,N)\ne\varnothing\}.
\]
Then
\begin{equation}\label{eq:local-window-endpoint-main}
\sum_{n=0}^{N-1}Z_n-\sum_{j=j_-(N)}^{j_+(N)}U_j=O_{\PP}(\log N).
\end{equation}
In the future-indexed notation the same assertion is
\[
\sum_{n=0}^{N-1}Z_n
-\sum_{j=j_-(N)-r}^{j_+(N)-r}\widetilde Y_j^{\rm raw}
=O_{\PP}(\log N).
\]
Consequently, if \(\bar z=0\), then
\begin{equation}\label{eq:local-window-centered-endpoint-main}
\sum_{n=0}^{N-1}Z_n-\sum_{j=j_-(N)}^{j_+(N)}Y_j
=O_{\PP}(\log N)=o_{\PP}(\sqrt N).
\end{equation}
The same two endpoint bounds hold when the upper or lower cycle index is
trimmed by any fixed number of look-ahead or look-back cycles.
\end{enumerate}
\end{lemma}

\begin{proof}
Work first under the Palm version with a click at \(\tau_0=0\).  The
stationary renewal measure is obtained by choosing a cycle with length-biased
law and then choosing the origin uniformly inside that cycle.  The local
observable may inspect neighbouring cycles, but it is bounded and is still a
shift-covariant function of the two-sided stationary renewal configuration.
Therefore renewal Palm inversion gives
\[
\EE Z_0
=\frac1{\mu}\,
\EE_{\mathrm{Palm}}\sum_{n=\tau_0}^{\tau_1-1}Z_n
=\frac1{\mu}\EE U_0 ,
\]
which proves \eqref{eq:local-window-palm-inversion-main}.  This is the
identity used below for lagged inclusion rewards; for \(Z_n=A_nA_{n+m}\), it
reads \(\EE U_j^{(m)}=\mu\,\PP(A_0=A_m=1)\).

Now fix \(j\) and write \(n=\tau_{j+r}+u\) with
\(0\le u\le G_{j+r}\).  In the coordinate system with origin
\(\tau_{j+r}\), the clicks visible to the window
\((A_{n-r},\ldots,A_{n+\ell})\) are precisely
\(\tau_{j+m}-\tau_{j+r}=S_m(G_j,\ldots,G_{j+q_*})-S_r(G_j,\ldots,G_{j+q_*})\)
for \(0\le m\le r+\ell\).  No earlier or later click can enter the window:
each cycle has length at least one, so a displacement \(r\) to the left crosses
at most \(r\) previous click epochs and a displacement \(\ell\) to the right
crosses at most \(\ell\) future click epochs.  Hence the summand at offset
\(u\) is exactly the \(u\)-summand in
\eqref{eq:local-window-cycle-map-main}, and summing over
\(u=0,\ldots,G_{j+r}\) gives
\eqref{eq:local-window-future-block-main}.  The map is measurable because it
is a finite sum of indicator functions on \(\mathbb N_0^{q_*+1}\).  If
\(|j-k|>q_*\), then the gap blocks
\((G_j,\ldots,G_{j+q_*})\) and \((G_k,\ldots,G_{k+q_*})\) are disjoint
functions of the i.i.d. gap sequence, proving \(q_*\)-dependence.

It remains to record the endpoint size.  Since \(G_0\) has an exponential
moment, so does \(L_0\), and for each fixed \(c\)
\[
\max_{|j|\le cN}L_j=O_{\PP}(\log N).
\]
Indeed, after decreasing \(\eta\) if needed, a union bound gives
\(\PP(\max_{|j|\le cN}L_j>x)\le C'N e^{-\eta x}\).  The renewal law gives
\(j_+(N)-j_-(N)+1=O_{\PP}(N)\), so with probability tending to one every cycle
which can affect a window anchored in \([0,N)\) lies in such a deterministic
index range.  The only terms not matched by complete assigned cycle rewards
belong to the initial incomplete cycle, the terminal incomplete cycle, and a
fixed number of adjacent cycles required by the two-sided local window.  Since
\(\psi\) is bounded, their total uncentered contribution is
\(O_{\PP}(\log N)\), proving \eqref{eq:local-window-endpoint-main}.  When
\(\bar z=0\), \(Y_j=U_j\), so the centered endpoint difference is the same
\(O_{\PP}(\log N)\), which is \(o_{\PP}(\sqrt N)\).  Trimming or adding a
fixed number of endpoint cycles changes the sum by at most the same maximum
cycle-length order.
\end{proof}

\begin{proposition}[Generic complete-cycle regenerative CLT]\label{prop:generic-complete-cycle-regenerative-clt-main}
Let \((A_n)_{n\in\mathbb Z}\) be a stationary binary renewal process with
click epochs \((\tau_j)\), i.i.d. gaps \(G_j\), cycle lengths
\(L_j=G_j+1\), mean \(\mu=\EE L_0\in(0,\infty)\), and
\(\EE e^{\eta G_0}<\infty\) for some \(\eta>0\).  Fix a finite dimension
\(d_{\rm rew}\) and a fixed look-ahead \(m_{\rm dep}\).  Suppose that
\[
Y_j=f(G_j,\ldots,G_{j+m_{\rm dep}})\in\RR^{d_{\rm rew}},\qquad
\EE Y_j=0,
\]
with
\[
\|f(g_0,\ldots,g_{m_{\rm dep}})\|
\le C_f\left(1+\sum_{i=0}^{m_{\rm dep}}g_i\right).
\]
Let \(K_N=\max\{k:\tau_k<N\}\), with \(K_N=-1\) if no click is observed, and
let \(J_N\) be any all-complete-cycle index set obtained from
\(\{0,\ldots,K_N-1\}\) by deleting only a fixed number of endpoint and
look-ahead indices and by requiring that every local window entering \(Y_j\)
is observed.  Empty sums are interpreted as \(0\).  Define
\[
\widehat\theta_N:=\frac1N\sum_{j\in J_N}Y_j .
\]
Then the empty-sum branch is used with probability \(o(1)\), the endpoint
replacement is
\[
\sum_{j\in J_N}Y_j-\sum_{j=0}^{K_N-1}Y_j=O_{\PP}(\log N)
=o_{\PP}(\sqrt N),
\]
and
\[
\sqrt N\,\widehat\theta_N
\Rightarrow
\mathcal N\left(0,\frac{\mathcal T_{m_{\rm dep}}}{\mu}\right),
\]
where
\[
\mathcal T_{m_{\rm dep}}
=\Var(Y_0)+
\sum_{h=1}^{m_{\rm dep}}
\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\}.
\]
Whenever \(Y_j\) is obtained from a bounded local calendar window
\(\psi(A_{n-r},\ldots,A_{n+\ell})\), the required representation is supplied
by \Cref{lem:local-window-regenerative-decomposition-main} with
\[
m_{\rm dep}=q_*(r,\ell)=r+\max\{\ell-1,0\},
\]
after the future-index shift used in that lemma.  A sharper
application-specific gap range may be used when the reward admits one.
\end{proposition}

\begin{proof}
The exponential moment implies, for each fixed \(c\),
\[
\max_{0\le j\le cN}L_j=O_{\PP}(\log N).
\]
Indeed, after decreasing \(\eta\) if needed, a union bound gives
\(\PP(\max_{0\le j\le cN}L_j>x)\le CNe^{-\eta x}\).  Since the renewal law
gives \(K_N=O_{\PP}(N)\), every endpoint or look-ahead cycle deleted from
the complete-cycle sum has length \(O_{\PP}(\log N)\).  The displayed growth
bound on \(f\) therefore gives
\[
\sum_{j\in J_N}Y_j-\sum_{j=0}^{K_N-1}Y_j=O_{\PP}(\log N),
\]
because \(J_N\) differs from the complete prefix by only a fixed number of
endpoint or look-ahead cycle indices.  For every fixed \(C\),
\(\PP(K_N<C)\to0\) by \Cref{lem:fixed-low-cycle-event-main}; after any fixed
endpoint deletion the same conclusion holds.  Hence the empty-sum convention
affects only an \(o(1)\)-probability event.

The sequence \((Y_j)\) is centered and \(m_{\rm dep}\)-dependent, and the
growth bound plus the exponential gap moment gives \(\EE\|Y_0\|^2<\infty\).
The multivariate CLT for fixed-range dependent sequences gives, for
deterministic \(m\to\infty\),
\[
\frac1{\sqrt m}\sum_{j=0}^{m-1}Y_j
\Rightarrow
\mathcal N(0,\mathcal T_{m_{\rm dep}}).
\]
Put \(m_N=\lfloor N/\mu\rfloor\).  The renewal CLT gives
\(|K_N-m_N|=O_{\PP}(\sqrt N)\).  For fixed \(m_{\rm dep}\)-dependent partial
sums, the maximal inequality
\[
\EE\max_{0\le q\le q_N}
\left\|\sum_{j=m_N}^{m_N+q-1}Y_j\right\|^2
\le C q_N
\]
holds for deterministic \(q_N\), by applying the usual martingale or blocking
proof to the \(m_{\rm dep}+1\) independent subsequences.  Taking
\(q_N=O(\sqrt N)\) on high-probability events shows that replacing the
deterministic index \(m_N\) by the random index \(K_N\) changes the normalized
partial sum by \(o_{\PP}(1)\).  Equivalently, this is Anscombe's random-index
replacement for \(m_{\rm dep}\)-dependent sums.

Therefore
\[
\frac1{\sqrt N}\sum_{j=0}^{K_N-1}Y_j
=
\sqrt{\frac{K_N}{N}}\,
\frac1{\sqrt{K_N}}\sum_{j=0}^{K_N-1}Y_j
\Rightarrow
\mathcal N\left(0,\frac{\mathcal T_{m_{\rm dep}}}{\mu}\right),
\]
because \(K_N/N\to1/\mu\).  The endpoint replacement then gives the same limit
for \(N^{-1/2}\sum_{j\in J_N}Y_j=\sqrt N\,\widehat\theta_N\).
\end{proof}

\begin{lemma}[Calendar-stopped triangular-array batch covariance rate]\label{lem:calendar-stopped-batch-cov-main}
Let \((Y_j,L_j)_{j\in\mathbb Z}\) be a stationary cycle sequence with
\(\EE Y_0=0\) in a fixed finite dimension,
\(\mu=\EE L_0\in(0,\infty)\), exponentially decaying tails for
\(L_0\), and a fixed nonnegative look-ahead \(m_{\rm dep}\) such that
\(Y_j=f(G_j,\ldots,G_{j+m_{\rm dep}})\), where the gaps \(G_j\) are i.i.d.
and
\[
\|f(g_0,\ldots,g_{m_{\rm dep}})\|
\le C_f\left(1+\sum_{i=0}^{m_{\rm dep}}g_i\right).
\]
Let \(b_N\to\infty\), \(b_N/N\to0\), \(d_N=b_N+m_{\rm dep}\), and set
\[
C_{\ell,N}=\sum_{j=(\ell-1)d_N}^{(\ell-1)d_N+b_N-1}Y_j,\qquad
V_{\ell,N}=b_N^{-1/2}C_{\ell,N}.
\]
Then \((V_{\ell,N})_{\ell\ge1}\) are independent within each row,
\(\sup_{\ell,N}\EE\|V_{\ell,N}\|^4<\infty\), and
\[
\EE[V_{\ell,N}V_{\ell,N}^{\top}]\longrightarrow
\mathcal T_{m_{\rm dep}}:=\Var(Y_0)+
\sum_{h=1}^{m_{\rm dep}}\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\}.
\]
Let \(R_N\) be any positive integer-valued random variable, possibly depending
on the same gaps and rewards, such that
\[
n_N:=\left\lfloor\frac{N}{\mu d_N}\right\rfloor\to\infty,\qquad
\left|\frac{R_N}{n_N}-1\right|
=O_{\PP}\left(\sqrt{\frac{d_N}{N}}\right).
\]
On the event \(R_N\ge2\), define
\[
S_N(R_N)=
\frac1{R_N-1}\sum_{\ell=1}^{R_N}
(V_{\ell,N}-\bar V_{R_N,N})(V_{\ell,N}-\bar V_{R_N,N})^{\top},
\qquad
\bar V_{R_N,N}=R_N^{-1}\sum_{\ell=1}^{R_N}V_{\ell,N}.
\]
Then
\begin{equation}\label{eq:calendar-stopped-batch-rate-main}
\left\|S_N(R_N)-\mathcal T_{m_{\rm dep}}\right\|_{\operatorname{op}}
=
O_{\PP}\left(b_N^{-1}+\sqrt{\frac{d_N}{N}}\right).
\end{equation}
Consequently, if
\(K_N=\max\{k:\tau_k<N\}\) and \(R_N=M_N\) is the number of all-complete-cycle
batches selected before the calendar time \(N\), with
\(\bar C_N=M_N^{-1}\sum_{\ell=1}^{M_N}C_{\ell,N}\), then
\[
\left\|
\frac{K_N}{N}\,\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}(C_{\ell,N}-\bar C_N)(C_{\ell,N}-\bar C_N)^{\top}
-\frac{\mathcal T_{m_{\rm dep}}}{\mu}
\right\|_{\operatorname{op}}
=
O_{\PP}\left(b_N^{-1}+\sqrt{\frac{b_N}{N}}\right).
\]
\end{lemma}

\begin{proof}
The separation by \(m_{\rm dep}\) omitted cycle rewards makes the gap sets entering
distinct \(C_{\ell,N}\)'s disjoint, so the row variables \(V_{\ell,N}\) are
independent.  The displayed growth bound and the exponential gap tail give the
uniform fourth-moment bound after the \(b_N^{-1/2}\) normalization.  Since
\((Y_j)\) is \(m_{\rm dep}\)-dependent,
\[
\frac1{b_N}\Var(C_{\ell,N})
=\Var(Y_0)+\sum_{h=1}^{m_{\rm dep}}\left(1-\frac{h}{b_N}\right)
\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\}
\]
and therefore
\begin{equation}\label{eq:batch-second-moment-bias-main}
\left\|\EE[V_{\ell,N}V_{\ell,N}^{\top}]
-\mathcal T_{m_{\rm dep}}\right\|_{\operatorname{op}}
=O(b_N^{-1}).
\end{equation}
For the deterministic prefix \(n_N\), the fixed dimension and the uniform
fourth-moment bound give
\[
\left\|
\frac1{n_N}\sum_{\ell=1}^{n_N}
\{V_{\ell,N}V_{\ell,N}^{\top}
-\EE[V_{\ell,N}V_{\ell,N}^{\top}]\}
\right\|_{\operatorname{op}}
=O_{\PP}(n_N^{-1/2})
=O_{\PP}\left(\sqrt{\frac{d_N}{N}}\right),
\]
and similarly
\[
\left\|\frac1{n_N}\sum_{\ell=1}^{n_N}V_{\ell,N}\right\|
=O_{\PP}(n_N^{-1/2})
=O_{\PP}\left(\sqrt{\frac{d_N}{N}}\right).
\]
The deterministic-prefix sample covariance therefore differs from
\(\mathcal T_{m_{\rm dep}}\) by
\[
O_{\PP}\left(b_N^{-1}+\sqrt{\frac{d_N}{N}}\right).
\]

It remains to show that the same rate survives the calendar-stopped prefix.
Put \(a_N=\sqrt{d_N/N}\).  Since
\(|R_N/n_N-1|=O_{\PP}(a_N)\), for each fixed large \(C\) the event
\[
\mathcal E_{N,C}=\{|R_N-n_N|\le Ca_Nn_N,\ n_N/2\le R_N\le2n_N\}
\]
has probability tending to one as \(C\to\infty\), uniformly for large \(N\).
On \(\mathcal E_{N,C}\), the terms added to or removed from the deterministic
prefix are contained in the deterministic window
\[
\{\ell:\ n_N-\lceil Ca_Nn_N\rceil\le \ell\le
n_N+\lceil Ca_Nn_N\rceil\}.
\]
Hence the difference between the \(R_N\)- and \(n_N\)-prefix second-moment
averages is bounded by the normalization change times
\(n_N^{-1}\sum_{\ell\le n_N}\|V_{\ell,N}\|^2\), plus \(2n_N^{-1}\) times the
sum of \(\|V_{\ell,N}\|^2\) over this deterministic window.  The first term is
\(O_{\PP}(a_N)\).  The second term is \(O_{\PP}(a_N)\) by Markov's inequality,
because the window has \(O(a_Nn_N+1)\) indices and
\(\sup_{\ell,N}\EE\|V_{\ell,N}\|^2<\infty\).  The same argument, with
\(\|V_{\ell,N}\|\) in place of \(\|V_{\ell,N}\|^2\), shows that the
empirical mean changes by \(O_{\PP}(a_N)\).  Thus the random-prefix sample
covariance has the rate in
\eqref{eq:calendar-stopped-batch-rate-main}; no independence between \(R_N\)
and the row variables is used.

For the renewal calendar stop, the renewal law gives \(K_N/N\to1/\mu\) in
probability, and the renewal CLT gives
\(K_N=N/\mu+O_{\PP}(\sqrt N)\).  The endpoint convention defining \(J_N\)
removes only the initial incomplete cycle, the terminal incomplete cycle,
\(O(m_{\rm dep})\) local look-ahead cycles, and one unfinished terminal batch of length
less than \(d_N\).  Exponential tails give the largest endpoint cycle length
\(O_{\PP}(\log N)\), so
\[
M_Nd_N=K_N+O_{\PP}(d_N+\log N+m_{\rm dep}).
\]
Consequently
\[
\left|\frac{M_N}{N/(\mu d_N)}-1\right|
=O_{\PP}\left(N^{-1/2}+\frac{d_N}{N}+\frac{\log N}{N}\right)
=O_{\PP}\left(\sqrt{\frac{d_N}{N}}\right),
\]
because \(d_N\to\infty\) and \(d_N=o(N)\).  The preceding random-prefix rate
applied with \(R_N=M_N\) gives
\[
\left\|
\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}(C_{\ell,N}-\bar C_N)(C_{\ell,N}-\bar C_N)^\top
-\mathcal T_{m_{\rm dep}}
\right\|_{\operatorname{op}}
=O_{\PP}\left(b_N^{-1}+\sqrt{\frac{d_N}{N}}\right).
\]
Multiplication by
\[
K_N/N=1/\mu+O_{\PP}(N^{-1/2})
\]
changes the target to \(\mathcal T_{m_{\rm dep}}/\mu\) and adds only
\(O_{\PP}(N^{-1/2})\), which is dominated by \(\sqrt{b_N/N}\).  Since
\(d_N=b_N+m_{\rm dep}\) and \(m_{\rm dep}\) is fixed, this proves the calendar-time covariance
rate.
\end{proof}

\begin{lemma}[Projected-Gaussian bootstrap at a moving half-space]\label{lem:projected-gaussian-bootstrap-halfspace-main}
For \(t\in(0,1)\), write
\[
H_t=\{h\in\RR^2:h_2\le th_1\}.
\]
Let \(z_N\to z\in(0,1)\) in probability and let
\(\widehat\Sigma_N\) be a symmetric nonnegative definite covariance estimator
such that
\[
\|\widehat\Sigma_N-\Sigma\|_{\operatorname{op}}=o_{\PP}(t_N),
\qquad t_N\downarrow0,
\]
for a fixed nonnegative definite \(2\times2\) matrix \(\Sigma\).  Let
\[
\widehat\Sigma_N^+
=\sum_{\widehat\lambda_{i,N}>t_N}
\widehat\lambda_{i,N}^{-1}
\widehat v_{i,N}\widehat v_{i,N}^{\top}
\]
be the thresholded Moore--Penrose inverse.  Suppose that
\[
X_N\Rightarrow X:=\Pi_{H_z}(Z),
\qquad Z\sim\mathcal N(0,\Sigma).
\]
Define
\[
T_N=X_N^\top\widehat\Sigma_N^+X_N
\]
and, conditionally on the observed record,
\[
Z_N^\ast\sim \mathcal N(0,\widehat\Sigma_N),\qquad
X_N^\ast=\Pi_{H_{z_N}}(Z_N^\ast),\qquad
T_N^\ast=(X_N^\ast)^\top\widehat\Sigma_N^+X_N^\ast .
\]
Then
\[
T_N\Rightarrow T:=X^\top\Sigma^+X,
\]
and the conditional law of \(T_N^\ast\) converges weakly in probability to
the law of \(T\).  Consequently, if \(c_{1-\alpha,N}^{\ast,-}\) is the lower
conditional \((1-\alpha)\)-quantile of \(T_N^\ast\) and the limiting quantile
\[
c_{1-\alpha}:=\inf\{c:\PP(T\le c)\ge1-\alpha\}
\]
is a continuity point of the distribution function of \(T\), then
\[
c_{1-\alpha,N}^{\ast,-}\to c_{1-\alpha}
\quad\text{in probability}.
\]
If in addition \(\PP(T\le c_{1-\alpha})=1-\alpha\), then
\[
\PP\{T_N\le c_{1-\alpha,N}^{\ast,-}\}\to1-\alpha .
\]

\end{lemma}

\begin{proof}
Weyl's inequality and the condition
\(\|\widehat\Sigma_N-\Sigma\|_{\operatorname{op}}=o_{\PP}(t_N)\) imply that,
with probability tending to one, every positive eigenvalue of \(\Sigma\) is
retained and every zero eigenvalue is removed by the threshold.  On that event
the retained spectral projection converges to the range projection of
\(\Sigma\), and
\[
\|\widehat\Sigma_N^+-\Sigma^+\|_{\operatorname{op}}\to0
\]
in probability.  Slutsky's theorem therefore gives
\[
X_N^\top\widehat\Sigma_N^+X_N
\Rightarrow X^\top\Sigma^+X .
\]

The remaining issue is conditional Gaussian convergence after the moving
projection.  The half-space projection has the explicit formula
\[
\Pi_{H_t}(u)
=u-\frac{(u_2-tu_1)_+}{1+t^2}\binom{-t}{1}.
\]
Thus \(u\mapsto\Pi_{H_t}(u)\) is one-Lipschitz and
\((t,u)\mapsto\Pi_{H_t}(u)\) is continuous, uniformly for \(t\) in compact
subintervals of \((0,1)\) and \(u\) in bounded sets.  Since
\(\widehat\Sigma_N\to\Sigma\) in probability, the conditional Gaussian laws
\(\mathcal N(0,\widehat\Sigma_N)\) converge weakly in probability to
\(\mathcal N(0,\Sigma)\).  Combining this Gaussian continuity with
\(z_N\to z\) and \(\widehat\Sigma_N^+\to\Sigma^+\) shows that, for every
bounded continuous \(\varphi:\RR\to\RR\),
\[
\EE^\ast\varphi(T_N^\ast)
\to
\EE\varphi\!\left((\Pi_{H_z}Z)^\top\Sigma^+\Pi_{H_z}Z\right)
\]
in probability, where \(\EE^\ast\) denotes conditional expectation given the
record.  This is conditional weak convergence in probability.  The quantile
conclusion is the standard consequence of weak convergence at a continuity
point of the limiting distribution function; the final coverage statement
then follows from the convergence of \(T_N\) and the random quantile
\(c_{1-\alpha,N}^{\ast,-}\).
\end{proof}

\begin{lemma}[Projected-Gaussian continuity and rank degeneracy]\label{thm:projected-gaussian-continuity-degeneracy-main}
Fix \(0<z<1\), put
\[
n_z=(-z,1)^\top,\qquad H_z=\{h:n_z^\top h\le0\},
\]
and let
\[
T_z=(\Pi_{H_z}Z)^\top\Sigma^+\Pi_{H_z}Z,
\qquad Z\sim\mathcal N(0,\Sigma),
\]
where \(\Sigma\) is a nonnegative definite \(2\times2\) matrix.  Then the
distribution of \(T_z\) is continuous in the following cases:
\[
\operatorname{rank}\Sigma=2,
\qquad\text{or}\qquad
\operatorname{rank}\Sigma=1
\text{ and }
\operatorname{Range}\Sigma\ne \operatorname{span}\{n_z\}.
\]
If \(\operatorname{rank}\Sigma=1\) and
\(\operatorname{Range}\Sigma=\operatorname{span}\{n_z\}\), then
\[
\PP(T_z=0)=\frac12
\]
and \(T_z\) has no atom on \((0,\infty)\).  If \(\Sigma=0\), then
\(T_z=0\) almost surely.  Consequently the continuity condition in
\Cref{lem:projected-gaussian-bootstrap-halfspace-main} is automatic except
for the zero covariance case and for the rank-one case in which the retained
Gaussian direction is normal to the exchange-diagonal tangent half-space; in
that rank-one normal case the only possible discontinuity is at the zero
quantile.
\end{lemma}

\begin{proof}
If \(\Sigma=0\), the assertion is immediate.  Suppose first that
\(\operatorname{rank}\Sigma=2\).  The Gaussian law has a Lebesgue density on
\(\RR^2\).  On \(H_z\), the statistic is the nonzero quadratic form
\(u^\top\Sigma^+u\).  On \(H_z^c\), the projection is the linear map
\[
L_z u=u-\frac{n_z^\top u}{\|n_z\|^2}n_z
\]
onto the boundary line \(n_z^\top h=0\), and the statistic is the quadratic
form \(u^\top L_z^\top\Sigma^+L_z u\), not identically zero on that half-plane.
For each fixed \(c\), the level set of either nonzero quadratic polynomial has
Lebesgue measure zero.  Hence \(\PP(T_z=c)=0\) for every \(c\).

Now let \(\operatorname{rank}\Sigma=1\).  Write
\(\Sigma=\lambda vv^\top\), where \(\lambda>0\) and \(\|v\|=1\).  Then
\(Z=\xi v\), with \(\xi\sim\mathcal N(0,\lambda)\), and
\[
\Sigma^+=\lambda^{-1}vv^\top .
\]
If \(n_z^\top v=0\), the line
\(\operatorname{span}\{v\}\) lies in the boundary of \(H_z\), so
\(T_z=\xi^2/\lambda\), which has a continuous \(\chi^2_1\) law.  If
\(n_z^\top v\ne0\), one half-line is already in \(H_z\), where again
\(T_z=\xi^2/\lambda\).  On the other half-line,
\[
\Pi_{H_z}(\xi v)=
\xi\left(v-\frac{n_z^\top v}{\|n_z\|^2}n_z\right).
\]
The corresponding coefficient in the seminorm \(\Sigma^+\) is
\[
\lambda^{-1}
\left\{v^\top\left(v-\frac{n_z^\top v}{\|n_z\|^2}n_z\right)\right\}^2 .
\]
This coefficient is positive unless \(v\) is parallel to \(n_z\).  Therefore,
when \(\operatorname{Range}\Sigma\ne\operatorname{span}\{n_z\}\), \(T_z\) is
a two-sided mixture of positive multiples of \(\xi^2\) and has no atoms.  When
\(\operatorname{Range}\Sigma=\operatorname{span}\{n_z\}\), the outside
half-line projects to the origin and contributes mass \(1/2\) at zero, while
the inside half-line contributes a continuous positive component.  This proves
the rank-one alternatives.
\end{proof}

\paragraph{Model verification.}
We now specialize the generic renewal tools to one fixed killed-leakage
sampled-boundary setting.  The exponential-tail lemma verifies the renewal
hypothesis for this model.  Main Theorem~3 then states the exact finite-record
statistic for the three inclusion coordinates and applies the generic
complete-cycle CLT to that statistic.

\begin{lemma}[Killed-leakage gap exponential moment]\label{lem:killed-leakage-gap-exponential-moment-main}
For the killed-leakage sampled-boundary kernels
\eqref{eq:t0-t1-main}, with \(p=e^{-\Gamma\Delta\tau}\),
\(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), set
\[
r=\max\{p,s\}<1 .
\]
Then there are finite constants \(C_0,C_1\), depending only on the fixed
sampled-boundary setting, such that
\[
g_k\le C_0r^k\qquad(k\ge0).
\]
Consequently, for every
\[
0<\eta<-\log r,
\]
the gap and cycle length satisfy
\[
\EE e^{\eta G}<\infty,\qquad
\EE e^{\eta (G+1)}<\infty .
\]
Thus the regenerative exponential-moment hypothesis used below is a verified
consequence of the killed-leakage model, not an additional assumption.
\end{lemma}

\begin{proof}
When \(p\ne s\), the displayed unequal-rate formula
\eqref{eq:gap-closed-main} gives
\[
|g_k|
\le
\left|\frac{\kappa_{\mathrm r}(1-p)}{\kappa_{\mathrm r}-\Gamma}\right|p^k
+
\left|\frac{\Gamma(1-s)}{\kappa_{\mathrm r}-\Gamma}\right|s^k
\le C_1(p^k+s^k)
\le 2C_1r^k .
\]
Since \(g_k\ge0\), this is the required tail bound.

On the equal-rate diagonal \(p=s=e^{-\Gamma\Delta\tau}\), write
\(y=\Gamma\Delta\tau\).  The equal-rate dictionary
\eqref{eq:equal-rate-dictionary-main} gives
\[
g_k=p^k\bigl((1-p)(1+yk)-yp\bigr).
\]
Hence \(|g_k|\le C_1(1+k)p^k\).  For any
\(\eta<-\log p\), the series
\[
\sum_{k\ge0}e^{\eta k}(1+k)p^k
\]
converges.  This proves \(\EE e^{\eta G}<\infty\) in the equal-rate case.
The unequal-rate case follows similarly from
\(\sum_{k\ge0}e^{\eta k}r^k<\infty\) whenever \(\eta<-\log r\).  Finally,
\(\EE e^{\eta(G+1)}=e^\eta\EE e^{\eta G}\).
\end{proof}

\begin{corollary}[Stationary killed-leakage three-inclusion complete-cycle CLT]\label{cor:three-inclusion-complete-cycle-clt-main}
In one fixed stationary killed-leakage sampled-boundary setting, let
\(\widehat r_N^{\mathrm{cyc}}\) be the exact finite-record statistic from
\Cref{def:cycle-index-inclusion-estimator-main}:
\[
\widehat r_{m,N}^{\mathrm{cyc}}
=\frac1N\sum_{j\in J_{m,N}}U_j^{(m)},\qquad m=0,1,2,
\]
with empty sums equal to \(0\).  Put
\[
r=(r_0,r_1,r_2)^\top,\qquad
r_m=\PP(A_0=A_m=1),\qquad
\mu=\EE(G+1).
\]
In Palm cycle coordinates,
\begin{equation}\label{eq:inclusion-cycle-reward-identities-main}
U_j^{(0)}=1,\qquad
U_j^{(1)}=\mathbf1_{\{G_j=0\}},\qquad
U_j^{(2)}
=\mathbf1_{\{G_j=1\}}+\mathbf1_{\{G_j=0,\ G_{j+1}=0\}},
\end{equation}
and the centered reward is
\[
Y_j=
\bigl(U_j^{(0)}-r_0L_j,\,
U_j^{(1)}-r_1L_j,\,
U_j^{(2)}-r_2L_j\bigr)^\top .
\]
Then \(Y_j=f(G_j,G_{j+1})\), \(\EE Y_j=0\), and the reward sequence is
\(1\)-dependent.  With
\[
\mathcal T_1^{(r)}
=\Var(Y_0)+\operatorname{Cov}(Y_0,Y_1)+\operatorname{Cov}(Y_1,Y_0),
\qquad
\Sigma_r=\frac{\mathcal T_1^{(r)}}{\mu},
\]
one has
\[
\sqrt N(\widehat r_N^{\mathrm{cyc}}-r)
\Rightarrow \mathcal N(0,\Sigma_r).
\]
The proof uses the \(O_{\PP}(\log N)\) endpoint replacement and the
renewal random-index replacement from
\Cref{prop:generic-complete-cycle-regenerative-clt-main}; the covariance
scale is exactly \(\mathcal T_1^{(r)}/\mu\).
\end{corollary}

\begin{proof}
The half-open cycle \([\tau_j,\tau_{j+1})\) contains only the click at
\(\tau_j\), so \(U_j^{(0)}=1\).  A one-step look-ahead is a click exactly
when the next click occurs one sampled step later, so
\(U_j^{(1)}=\mathbf1_{\{G_j=0\}}\).  For lag two, the contributing time must
again be the click at \(\tau_j\); the click two steps later occurs either
when \(G_j=1\), or when \(G_j=0\) and the following gap also has length zero.
This gives the displayed reward identities.

\Cref{lem:local-window-regenerative-decomposition-main}, applied to
\(Z_n^{(m)}=A_nA_{n+m}\), gives the Palm inversion identity
\[
\EE U_j^{(m)}=r_m\EE L_j,\qquad m=0,1,2.
\]
Thus \(\EE Y_j=0\).  The displayed identities show that \(Y_j\) is a
function only of \((G_j,G_{j+1})\), hence \(1\)-dependent, and
\[
\|Y_j\|\le C(1+G_j)
\]
for a fixed finite \(C\).  The exponential moment required by the generic
proposition is supplied by
\Cref{lem:killed-leakage-gap-exponential-moment-main}.

For each coordinate,
\[
\widehat r_{m,N}^{\mathrm{cyc}}-r_m
=\frac1N\sum_{j\in J_{m,N}}\{U_j^{(m)}-r_mL_j\}
+\frac{r_m}{N}\left\{\sum_{j\in J_{m,N}}L_j-N\right\}.
\]
The complete cycles used by \(J_{m,N}\) differ from the calendar window
\([0,N)\) by only the initial incomplete cycle, the terminal incomplete
cycle, and a fixed look-ahead cycle.  Their total length is
\(O_{\PP}(\log N)\), by the endpoint proof in
\Cref{prop:generic-complete-cycle-regenerative-clt-main}.  Therefore the
second displayed term is \(o_{\PP}(N^{-1/2})\), and the first term has the
same \(N^{1/2}\)-limit as \(N^{-1}\sum_{j=0}^{K_N-1}Y_j\).
\Cref{prop:generic-complete-cycle-regenerative-clt-main} then supplies the
deterministic \(1\)-dependent CLT, the endpoint replacement, and the
random-index replacement \(K_N/N\to1/\mu\).  The resulting calendar-time
covariance is \(\mathcal T_1^{(r)}/\mu\).
\end{proof}

\begin{theorem}[Main Theorem 3: fixed-record inference for the three inclusion coordinates]\label{thm:finite-record-inference-audit-main}\label{prop:three-coordinate-vector-batch-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and let
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}
\]
with
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,
\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
These killed-leakage labelled kernels satisfy \(T_1(R,R)=T_1(D,R)=0\),
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), \(P=T_0+T_1\) stochastic,
\(0<p,s<1\), \(a>0\), \(b>0\), and \(a+b=1-s\).  Equal-rate formulas are
understood by continuous limits when \(\Gamma=\kappa_{\mathrm r}\).  Let
\((A_n)\) be the stationary visible binary process generated by these kernels,
or use the following finite-start protocol.  Fix an arbitrary hidden initial
law \(\alpha\) on \(\{R,D\}\).  Generate the hidden-output chain from
\(H_0\sim\alpha\) for \(L_N+N\) sampled intervals, where \(L_N\) is
deterministic and \(L_N\to\infty\).  Discard the first
\(L_N\) intervals and reindex the retained post-burn-in record by
\[
A^{(N)}_n=A_{L_N+n},\qquad 0\le n<N .
\]
Thus \(N\) denotes the retained post-burn-in length, not the total generated
length.  The complete-cycle indices \(J_N\), \(K_N\), and all endpoint
conventions below are then formed from the retained block \([0,N)\) after this
reindexing.  No total-generated-length convention is used in the estimator
definitions in this theorem.
By \Cref{conv:branch-map-labelled-rates-main}, this theorem estimates
visible-law and quotient/symmetric-coordinate targets only; any separated
labelled-rate reporting is conditional on the external branch-map input
specified there.  On the exchange
diagonal, the common numerical rate in \Cref{lem:diagonal-common-rate-main} is
available only through the projected symmetric-coordinate limit, not through a
separated-root Wald chart; the induced common-rate confidence set is
\Cref{cor:exchange-diagonal-common-rate-confidence-main}.

\paragraph{Operational reporting rule.}
All covariance and Wald reporting attached to this theorem uses the following
deterministic choices.  Choose a batch exponent \(0<\beta<1\) and set
\[
b_N=\lfloor N^\beta\rfloor,\qquad
\omega_\beta=\min\{\beta,(1-\beta)/2\}.
\]
Choose a rank threshold
\[
t_N=N^{-\alpha},
\qquad
0<\alpha<\omega_\beta .
\]
The all-complete-cycle separated-batch covariance estimator is formed with
batch length \(b_N\) and the separation convention of
\Cref{eq:all-cycle-batch-cov-main}.  In every retained-rank Wald statement,
only empirical covariance eigenvalues strictly larger than \(t_N\) are
retained; the reported rank is the number of retained eigenvalues, and the
pseudoinverse is taken on this retained empirical range.  Scalar Wald
intervals are reported only for scalar functionals whose limiting
studentization variance is strictly positive.  When that limiting scalar
variance is zero, the theorem supplies only the corresponding degenerate
Gaussian limit, not a scalar Wald interval.

The exact finite-record statistic is the cycle-index inclusion estimator
\(\widehat r_N^{\mathrm{cyc}}\) of
\Cref{def:cycle-index-inclusion-estimator-main}:
\[
\widehat r_{m,N}^{\mathrm{cyc}}
=\frac1N\sum_{j\in J_{m,N}}U_j^{(m)},\qquad m=0,1,2,
\]
with the empty-sum convention on records with too few complete cycles or
missing terminal look-ahead cycles.  Let
\[
r=(r_0,r_1,r_2)^\top,\qquad
r_m=\PP(A_0=A_m=1),\qquad
\mu=\EE(G+1).
\]
Let \(Y_j\), \(\mathcal T_1^{(r)}\), and
\(\Sigma_r=\mathcal T_1^{(r)}/\mu\) be the explicit Palm reward and covariance
scale from \Cref{cor:three-inclusion-complete-cycle-clt-main}.  Then
\[
\sqrt N(\widehat r_N^{\mathrm{cyc}}-r)
\Rightarrow \mathcal N(0,\Sigma_r),
\]
and the all-complete-cycle separated-batch estimator in
\Cref{eq:all-cycle-batch-cov-main}, applied to the plug-in rewards
\(\widehat Y_j\) from \Cref{def:cycle-index-inclusion-estimator-main}, is
consistent for \(\Sigma_r\) at the rate stated in
\Cref{lem:three-coordinate-batch-rate-main}.

\paragraph{Consequences included in this finite-record theorem.}
The preceding inclusion-coordinate conclusion is the base part of this
theorem.  The following three finite-record reporting conclusions are part of
the same theorem and use the same retained block, the same complete-cycle
convention, and the operational reporting rule above.

\begin{enumerate}[label=\textup{(\roman*)},leftmargin=*]
\item \emph{Quotient inverse delta method.}
Assume the population inverse is in the stable chart
\[
r_0>0,\qquad D=r_1-r_0^2\ne0,
\]
equivalently \(r_0\) and \(|r_1/r_0-r_0|\) are bounded away from zero in the
local neighbourhood considered.  Define
\[
a(r)=\frac{r_1}{r_0},\qquad
u_2(r)=\frac{r_2}{r_0},\qquad
\lambda(r)=\frac{u_2(r)-r_0}{a(r)-r_0},
\]
and
\[
\Phi(r)=
\bigl(1-a(r)+\lambda(r),\,
r_0(1-\lambda(r))-a(r)+\lambda(r)\bigr).
\]
Let \(F=\{(x+y,xy):0\le x,y\le1\}\), let \(\Pi_F^{\rm tb}\) be a measurable
nearest-point projection onto \(F\), and form the total thresholded estimator
\(\widetilde\sigma_N=\Pi_F^{\rm tb}(\widehat\sigma_N^{\rm raw})\) with the
fallback and denominator thresholds of
\Cref{cor:finite_inverse_delta_boundary_main}.  Then
\[
\sqrt N\{\Phi(\widehat r_N^{\mathrm{cyc}})-\Phi(r)\}
\Rightarrow
\mathcal N(0,\Sigma_\sigma),
\qquad
\Sigma_\sigma=D\Phi(r)\Sigma_rD\Phi(r)^\top ,
\]
and at separated interior points of \(F\) the same limit holds with
\(\Phi(\widehat r_N^{\mathrm{cyc}})\) replaced by
\(\widetilde\sigma_N\).  The sandwich covariance
\[
\widehat\Sigma_{\sigma,N}
=\widehat J_{\Phi,N}\widehat\Sigma_{r,N}^{\mathrm{all}}
 \widehat J_{\Phi,N}^{\top}
\]
is consistent there.  For the killed-leakage sampled-boundary population,
\(\Phi(r)=(p+s,ps)\).  If, in addition, \(\Delta_\sigma=(p-s)^2>0\) and an
external branch map \(\mathcal B\) supplies the separated-root labels in
\Cref{conv:branch-map-labelled-rates-main}, the retained separated labelled
rates obey the labelled-rate delta method and the retained-rank Wald statistic
uses only empirical eigenvalues above \(t_N\).

\item \emph{Exchange-diagonal projection and common-rate reporting.}
At a population exchange-diagonal point
\(\sigma_z=(2z,z^2)\), \(0<z<1\), the separated-root labelled-rate chart is
not used.  Instead
\[
\sqrt N(\widetilde\sigma_N-\sigma_z)
\Rightarrow
\Pi_{H_z}(Z),\qquad
Z\sim\mathcal N(0,\Sigma_\sigma),\qquad
H_z=\{h\in\mathbb R^2:h_2\le zh_1\}.
\]
The diagonal confidence set is the projected-Gaussian half-space inversion
\[
\{(2z,z^2):0<z<1,\ Q_N(z)\le c^{-}_{1-\alpha,N}(z)\}
\]
with \(Q_N\), the thresholded pseudoinverse, and the conditional Gaussian
critical value defined in
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main}.  Its image
under \(z\mapsto-\log z/\Delta\tau\) is the finite-record confidence set for
the identified common numerical rate
\[
\gamma_{\rm diag}=-\frac{\log z}{\Delta\tau},
\]
or equivalently for the invariant ordered quotient pair
\((\gamma_{\rm diag},\gamma_{\rm diag})\).  This conclusion does not assert a
separated attempt/recovery branch on the diagonal.

\item \emph{Lag-two studentized scalar interval.}
For the scalar diagnostic
\[
\gamma_2=\operatorname{Cov}(A_0,A_2),
\]
use the all-complete-cycle lag-two estimator \(\widehat\gamma_{2,N}\) and its
observable separated-batch scalar studentizer
\(\widehat\sigma^2_{\gamma_2,N}\) from
\Cref{cor:boundary-reset-cycle-clt-main}.  The limiting variance
\(\sigma_{\gamma_2}^2\) is positive on the full positive sampled-boundary
parameter domain.  Hence
\[
\sqrt N(\widehat\gamma_{2,N}-\gamma_2)
\Rightarrow \mathcal N(0,\sigma_{\gamma_2}^2),
\qquad
\widehat\sigma^2_{\gamma_2,N}\xrightarrow{\PP}\sigma_{\gamma_2}^2,
\]
and the two-sided interval
\[
\left[
\widehat\gamma_{2,N}
-z_{1-\alpha_{\rm ci}/2}\frac{\widehat\sigma_{\gamma_2,N}}{\sqrt N},
\quad
\widehat\gamma_{2,N}
+z_{1-\alpha_{\rm ci}/2}\frac{\widehat\sigma_{\gamma_2,N}}{\sqrt N}
\right]
\]
has asymptotic coverage \(1-\alpha_{\rm ci}\) for every
\(0<\alpha_{\rm ci}<1\).  No other scalar Wald interval is
claimed from this theorem unless its limiting studentization variance is
strictly positive and consistently estimated.
\end{enumerate}
\end{theorem}

\begin{proof}
\Cref{lem:killed-leakage-gap-exponential-moment-main} verifies the
exponential renewal tail.  The stationary CLT for the exact statistic
\(\widehat r_N^{\mathrm{cyc}}\), including the Palm reward identities, the
\(O_{\PP}(\log N)\) endpoint replacement, the renewal random-index
replacement, and the covariance scale
\(\Sigma_r=\mathcal T_1^{(r)}/\mu\), is precisely
\Cref{cor:three-inclusion-complete-cycle-clt-main}.

For covariance estimation, use the common complete-cycle set \(J_N\) and the
plug-in rewards \(\widehat Y_j\) in
\Cref{def:cycle-index-inclusion-estimator-main}.  The just-proved CLT gives
\(\widehat r_N^{\mathrm{cyc}}-r=O_{\PP}(N^{-1/2})\), so
\(\widehat Y_j-Y_j=\Delta_NL_j\) with
\(\Delta_N=O_{\PP}(N^{-1/2})\) componentwise.  The hypotheses of
\Cref{lem:three-coordinate-batch-rate-main} therefore hold with
\(m_{\rm dep}=1\), and the estimator in
\Cref{eq:all-cycle-batch-cov-main} is consistent for
\(\Sigma_r=\mathcal T_1^{(r)}/\mu\).

It remains only to justify the finite-start protocol.  Let
\(\mathsf P_{\alpha,L}^{(N)}\) be the law of the
retained visible word \((A_L,\ldots,A_{L+N-1})\) when the hidden chain starts
from \(\alpha\), and let \(\mathsf P_{\pi}^{(N)}\) be the corresponding
stationary word law.  The hidden transition matrix
\[
P=T_0+T_1=
\begin{pmatrix}p&1-p\\ b&1-b\end{pmatrix}
\]
is primitive, has stationary law \(\pi\) from \Cref{lem:stationary-main}, and has second
eigenvalue \(\lambda_{\mathrm{hid}}=p-b\), with
\(|\lambda_{\mathrm{hid}}|<1\) by \Cref{lem:hidden-mode-contraction-main}.
For any signed row vector \(v\) with total mass zero,
\(vP^L=\lambda_{\mathrm{hid}}^L v\).  Hence, for
\(\theta=(1+|\lambda_{\mathrm{hid}}|)/2\in(0,1)\), there is a finite constant
\(C\) such that, uniformly in \(\alpha\) and \(L\ge0\),
\[
\|\alpha P^L-\pi\|_{\mathrm{TV}}\le C\theta^L .
\]
The visible future word is obtained from the hidden state at time \(L\) by a
Markov kernel, so total variation contracts:
\begin{equation}\label{eq:finite-start-tv-main}
\bigl\|\mathsf P_{\alpha,L}^{(N)}-\mathsf P_{\pi}^{(N)}
\bigr\|_{\mathrm{TV}}
\le C\theta^L ,
\qquad N\ge1 .
\end{equation}
With \(L=L_N\), this tends to zero.  Equivalently, by maximal coupling one can
construct the finite-start retained word and a stationary retained word on one
space so that they agree with probability \(1-o(1)\).  On the agreement event
every complete-cycle statistic formed from the reindexed retained block has
the stationary law.  On the complement, whose probability is \(o(1)\), the
distributional contribution is bounded by the coupling failure probability.
Thus every stationary weak limit and covariance conclusion above transfers to
the finite-start post-burn-in record with retained sample size \(N\).

The quotient inverse part is the delta-method consequence proved in
\Cref{cor:finite_inverse_delta_boundary_main}; its assumptions are exactly the
stable inverse chart, projection, threshold, retained-root, and branch-map
conditions stated in the theorem.  The exchange-diagonal projected law and
common-rate confidence set are
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main,cor:exchange-diagonal-common-rate-confidence-main}.
The scalar lag-two CLT, positive variance, consistent scalar studentizer, and
normal interval are \Cref{cor:boundary-reset-cycle-clt-main}.  These results
all use the same retained post-burn-in record convention and the same
deterministic batch/rank rule stated in the theorem, so they are consequences
of the fixed-record theorem rather than separate reporting rules assembled
from independent conventions.
\end{proof}

\paragraph{Inclusion-coordinate covariance estimation.}
The following block turns the preceding inclusion-coordinate CLT into an
operational covariance estimate.  The block-average lemma is only used to
control plug-in centering uniformly over all batches; the resulting estimator
feeds the symmetric inverse and projected-Gaussian constructions.  This
multivariate covariance estimate is not, by itself, a fully studentized
confidence interval for every derived diagnostic.

\begin{lemma}[Uniform block average with exponential moments]\label{lem:uniform-calendar-block-length-main}
Let \(L_0,L_1,\ldots\) be i.i.d. nonnegative random variables with
\(\EE e^{\eta L_0}<\infty\) for some \(\eta>0\).  Let
\(b_N=\lfloor N^\beta\rfloor\), \(0<\beta<1\).  For each \(N\), let
\(I_{\ell,N}\) be intervals of \(b_N\) consecutive indices, and suppose the
number \(M_N\) of intervals used by the statistic satisfies
\(M_N=O_{\PP}(N/b_N)\).  Then
\begin{equation}\label{eq:uniform-calendar-block-length-main}
\max_{1\le \ell\le M_N}
\frac1{b_N}\sum_{j\in I_{\ell,N}}L_j
=O_{\PP}(1).
\end{equation}
The maximum over an empty index set is interpreted as \(0\).
\end{lemma}

\begin{proof}
Fix \(c<\infty\) such that
\(\PP(M_N\le cN/b_N)\to1\) along a high-probability subsequence; the final
bound follows by taking \(c\) large in the definition of
\(O_{\PP}(N/b_N)\).  For \(x>\EE L_0\), the Cramer--Chernoff rate
\[
I(x)=\sup_{0<\theta\le\eta}
\{\theta x-\log \EE e^{\theta L_0}\}
\]
is positive after increasing \(x\) if necessary.  Chernoff's bound gives,
uniformly over all intervals of length \(b_N\),
\[
\PP\left(\sum_{j\in I_{\ell,N}}L_j\ge xb_N\right)
\le e^{-b_NI(x)} .
\]
Therefore
\[
\PP\left(
\max_{1\le\ell\le \lceil cN/b_N\rceil}
\sum_{j\in I_{\ell,N}}L_j\ge xb_N
\right)
\le
\left\lceil\frac{cN}{b_N}\right\rceil e^{-b_NI(x)}
\longrightarrow0,
\]
because \(b_N=N^{\beta+o(1)}\) and hence \(b_N\gg\log N\).  Combining this
union bound with \(\PP(M_N>cN/b_N)\) proves
\eqref{eq:uniform-calendar-block-length-main}.
\end{proof}

\begin{lemma}[Separated-batch covariance estimator]\label{lem:three-coordinate-batch-rate-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and let
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}
\]
with \(a,b\) defined by the one-recovery-opportunity integrals in
\eqref{eq:a-b-main}.  These killed-leakage labelled kernels satisfy
\(T_1(R,R)=T_1(D,R)=0\),
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), \(P=T_0+T_1\) stochastic,
\(0<p,s<1\), \(a>0\), \(b>0\), and \(a+b=1-s\).  Equal-rate formulas are
understood by continuous limits when \(\Gamma=\kappa_{\mathrm r}\).  Let
\((A_n)\) be the stationary visible binary process generated by these kernels.
Assume the exponential gap moment and fixed \(m_{\rm dep}\)-dependent centered
reward hypotheses of \Cref{thm:finite-record-inference-audit-main}.  Let the
separated batch length be deterministic,
\[
b_N=\lfloor N^\beta\rfloor,\qquad 0<\beta<1,
\]
and omit \(m_{\rm dep}\) complete cycle rewards between adjacent batches.  If
estimated centering is used, assume that the rewards entering the batches
satisfy, uniformly over all complete cycles used by the estimator,
\[
\widehat Y_j-Y_j=\Delta_NL_j,\qquad
L_j=G_j+1,\qquad
\Delta_N=O_{\PP}(N^{-1/2}),
\]
componentwise in the vector case.

Write \(K_N=\max\{k:\tau_k<N\}\), with \(K_N=-1\) if no click is observed,
put \(d_N=b_N+m_{\rm dep}\), and use only all-complete-cycle indices \(J_N\)
for which every local window entering \(Y_j\) is observed.  Let
\[
M_N=\max\{m\ge0: (m-1)d_N+b_N-1\in J_N\},
\qquad
I_{\ell,N}=\{(\ell-1)d_N,\ldots,(\ell-1)d_N+b_N-1\}.
\]
If the displayed set is empty, set \(M_N=0\).
For \(B_{\ell,N}=\sum_{j\in I_{\ell,N}}Y_j\) and
\(\bar B_N=M_N^{-1}\sum_{\ell=1}^{M_N}B_{\ell,N}\) on \(\{M_N\ge1\}\),
define
\begin{equation}\label{eq:all-cycle-batch-cov-main}
\widehat\Sigma_N^{\mathrm{all}}
=
\begin{cases}
\displaystyle
\frac{K_N}{N}\,
\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}(B_{\ell,N}-\bar B_N)(B_{\ell,N}-\bar B_N)^\top,
&M_N\ge2,\\[1.1ex]
0_{d_{\rm rew}\times d_{\rm rew}},
&M_N<2 .
\end{cases}
\end{equation}
Then \(M_N\ge2\) with probability tending to one and
\[
\|\widehat\Sigma_N^{\mathrm{all}}-\Sigma\|_{\operatorname{op}}
=
O_{\PP}\left(N^{-\beta}+N^{-(1-\beta)/2}\right),
\qquad
\Sigma=\frac{\mathcal T_{m_{\rm dep}}}{\mu}.
\]
Thus the all-complete-cycle covariance estimator with plug-in centering is
consistent.  With
\[
\omega_\beta:=\min\{\beta,(1-\beta)/2\},
\]
any rank-adaptive deterministic threshold \(t_N\) must satisfy
\[
\|\widehat\Sigma_N^{\mathrm{all}}-\Sigma\|_{\operatorname{op}}=o_{\PP}(t_N).
\]
A concrete admissible choice is
\[
t_N=N^{-\alpha},
\qquad
0<\alpha<\omega_\beta;
\]
for the balanced choice \(b_N=\lfloor N^{1/3}\rfloor\), this becomes any
\(0<\alpha<1/3\), for instance \(t_N=N^{-1/4}\).

For the three inclusion coordinates in
\Cref{def:cycle-index-inclusion-estimator-main}, the same separated-batch
estimator, using the plug-in rewards from that definition, estimates
\(\Sigma_r\) at the displayed rate.
\end{lemma}

\begin{proof}
For covariance estimation, first use true centering and the deterministic
cycle prefix \(n_N=\lfloor N/(\mu(b_N+m_{\rm dep}))\rfloor\).  With
\[
V_{\ell,N}=b_N^{-1/2}B_{\ell,N},
\]
the omitted \(m_{\rm dep}\) rewards make the \(V_{\ell,N}\)'s independent
within each triangular row.  The fourth moments are uniformly bounded by the
exponential moment and the fixed reward dimension.  Hence the empirical second
moment of the deterministic-prefix row differs from its expectation by
\[
O_{\PP}(n_N^{-1/2})=O_{\PP}\!\left(\sqrt{\frac{b_N}{N}}\right),
\]
and the empirical-mean subtraction contributes only \(O_{\PP}(n_N^{-1})\).
The fixed \(m_{\rm dep}\)-dependence calculation gives
\[
\EE[V_{\ell,N}V_{\ell,N}^{\top}]
=
\Var(Y_0)+\sum_{h=1}^{m_{\rm dep}}
\left(1-\frac{h}{b_N}\right)
\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\},
\]
so the finite-batch bias relative to \(\mathcal T_{m_{\rm dep}}\) is
\(O(b_N^{-1})\).

The random number \(M_N\) of used batches is determined by the same gaps and
rewards.  Since \(K_N/N\to1/\mu\) and \(d_N/N\to0\), the event \(M_N<2\)
has probability tending to zero; on that event
\eqref{eq:all-cycle-batch-cov-main} uses its zero-matrix fallback.  The
fixed low-cycle events \(\{K_N<C\}\), including the finitely many endpoint and
look-ahead deletions, are negligible by
\Cref{lem:fixed-low-cycle-event-main}.  \Cref{lem:calendar-stopped-batch-cov-main}, applied with
\(R_N=M_N\) and \(d_N=b_N+m_{\rm dep}\), gives directly
\[
\|\widehat\Sigma_N^{\mathrm{all}}-\Sigma\|_{\operatorname{op}}
=O_{\PP}\left(b_N^{-1}+\sqrt{\frac{b_N}{N}}\right).
\]
This use of the lemma includes the rate
\(M_N/(N/\{\mu(b_N+m_{\rm dep})\})-1=O_{\PP}(\sqrt{b_N/N})\), the endpoint
cycles omitted by the all-complete-cycle rule, the random-prefix change in the
empirical second moment, and the empirical-mean subtraction.  No independence
between \(M_N\) and the batch sums is assumed.

It remains to control the plug-in replacement on the event \(M_N\ge2\).  In
each used batch,
\[
\sum_{j\in I_{\ell,N}}(\widehat Y_j-Y_j)
=\Delta_N\sum_{j\in I_{\ell,N}}L_j .
\]
Write
\[
S_{\ell,N}=\sum_{j\in I_{\ell,N}}L_j,\qquad
P_{\ell,N}=\Delta_NS_{\ell,N},\qquad
\bar P_N=M_N^{-1}\sum_{\ell=1}^{M_N}P_{\ell,N}.
\]
The calendar-stopped batch count above satisfies
\(M_N=O_{\PP}(N/d_N)\), so
\Cref{lem:uniform-calendar-block-length-main} gives
\[
\max_{\ell\le M_N}\frac{S_{\ell,N}}{b_N}=O_{\PP}(1).
\]
Consequently
\[
\max_{\ell\le M_N}\|P_{\ell,N}\|
=O_{\PP}(b_NN^{-1/2}),
\qquad
\|\bar P_N\|=O_{\PP}(b_NN^{-1/2}).
\]
Let \(\widetilde B_{\ell,N}=B_{\ell,N}+P_{\ell,N}\) and
\(\bar{\widetilde B}_N=M_N^{-1}\sum_{\ell=1}^{M_N}\widetilde B_{\ell,N}\).
The plug-in covariance differs from the true-centered covariance by the two
cross terms
\[
\mathcal C_N
:=
\frac{K_N}{N}\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}
(B_{\ell,N}-\bar B_N)(P_{\ell,N}-\bar P_N)^\top
\quad\text{and}\quad
\mathcal C_N^\top
\]
and the square term
\[
\frac{K_N}{N}\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}
(P_{\ell,N}-\bar P_N)(P_{\ell,N}-\bar P_N)^\top .
\]
Put
\[
Q_{B,N}:=\frac{K_N}{N}\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}\|B_{\ell,N}-\bar B_N\|^2,
\]
and define \(Q_{P,N}\) by the same expression with \(P_{\ell,N}\) in place of
\(B_{\ell,N}\).  Cauchy's inequality gives
\[
\|\mathcal C_N\|_{\operatorname{op}}\le Q_{B,N}^{1/2}Q_{P,N}^{1/2}.
\]
The already proved true-centered covariance bound gives
\[
Q_{B,N}=O_{\PP}(1).
\]
Also \(Q_{P,N}^{1/2}=O_{\PP}(\sqrt{b_N/N})\), because
\(K_N/N=O_{\PP}(1)\) and
\[
\max_{\ell\le M_N}\|P_{\ell,N}-\bar P_N\|
=O_{\PP}(b_NN^{-1/2}).
\]
Thus
each cross term is \(O_{\PP}(\sqrt{b_N/N})\).  The square term is bounded by
\[
O_{\PP}(1)\cdot b_N^{-1}\max_{\ell\le M_N}
\|P_{\ell,N}-\bar P_N\|^2
=O_{\PP}(b_N/N).
\]
Since \(b_N/N\le\sqrt{b_N/N}\) for \(b_N\le N\), these plug-in terms are no
larger than the already displayed batch rate.  Substituting
\(b_N=\lfloor N^\beta\rfloor\), \(0<\beta<1\), therefore gives
\[
O_{\PP}\left(N^{-\beta}+N^{-(1-\beta)/2}\right).
\]
The threshold claim is immediate from Weyl's eigenvalue inequality: every
positive limiting eigenvalue stays above \(t_N\), and every zero limiting
eigenvalue falls below \(t_N\), with probability tending to one whenever
\(\|\widehat\Sigma_N^{\mathrm{all}}-\Sigma\|_{\operatorname{op}}=o_{\PP}(t_N)\).
The final assertion for the three inclusion coordinates uses the
\(d_{\rm rew}=3\), \(m_{\rm dep}=1\) reward vector identified in
\Cref{thm:finite-record-inference-audit-main}; its plug-in condition follows
from \(\widehat r_{m,N}^{\mathrm{cyc}}-r_m=O_{\PP}(N^{-1/2})\), proved by that
CLT coordinatewise.
\end{proof}

\paragraph{Inverse delta method and projected exchange-diagonal charts.}
The next propositions transfer the inclusion-coordinate CLT through the
quotient inverse.  The total estimator is thresholded and projected, so the
finite-record rule remains defined when denominators are small or empirical
roots leave the stable chart.  Labelled-rate statements remain conditional on
an external separated-root branch map; without that branch map only visible-law
and symmetric-coordinate estimands are reported.

\begin{proposition}[Symmetric-coordinate inverse delta method]\label{cor:finite_inverse_delta_boundary_main}\label{cor:finite-separated-root-rate-chart-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), with the killed-leakage labelled
kernels \(T_0,T_1\) of \eqref{eq:t0-t1-main}.  Thus
\(T_1(R,R)=T_1(D,R)=0\), \(P=T_0+T_1\) is stochastic, \(0<p,s<1\), \(a>0\),
\(b>0\), \(a+b=1-s\), and every equal-rate formula is interpreted by the
continuous dictionary \eqref{eq:equal-rate-dictionary-main}.  Let \((A_n)\) be
the stationary visible binary process generated by these kernels, and let
\(\widehat r_N\) be the all-complete-cycle inclusion estimator from
\Cref{def:cycle-index-inclusion-estimator-main}.  Suppose
\[
\sqrt N(\widehat r_N-r)\Rightarrow \mathcal N(0,\Sigma_r)
\]
with a covariance estimator \(\widehat\Sigma_{r,N}^{\mathrm{all}}\) satisfying
the rate conclusion of \Cref{lem:three-coordinate-batch-rate-main}.

On the population domain
\[
r_0>0,\qquad r_1/r_0\ne r_0,
\]
define
\[
a(r)=\frac{r_1}{r_0},\qquad
u_2(r)=\frac{r_2}{r_0},\qquad
\lambda(r)=\frac{u_2(r)-r_0}{a(r)-r_0},
\]
and
\begin{equation}\label{eq:finite-inverse-delta-map-main}
\Phi(r)=
\bigl(1-a(r)+\lambda(r),\,
r_0(1-\lambda(r))-a(r)+\lambda(r)\bigr).
\end{equation}
Let
\[
F=\{(x+y,xy):0\le x,y\le1\},\qquad \sigma^\circ=(1,1/4),
\]
and fix a measurable nearest-point projection \(\Pi_F^{\rm tb}\) onto \(F\),
with deterministic tie-breaking away from neighbourhoods where the projection
is unique.  Choose deterministic thresholds
\[
\varepsilon_N\downarrow0,\qquad \eta_N\downarrow0,\qquad
\delta_N\downarrow0,\qquad N^{1/2}\delta_N^2\to\infty .
\]
For every finite record, set
\[
\widehat D_N=\widehat r_{1,N}-\widehat r_{0,N}^2
\]
and define the total raw symmetric-coordinate estimator by
\[
\widehat\sigma^{\rm raw}_N=
\begin{cases}
\Phi(\widehat r_N),
&\widehat r_{0,N}\ge\varepsilon_N,\quad |\widehat D_N|\ge\varepsilon_N,\\
\sigma^\circ,&\text{otherwise.}
\end{cases}
\]
The reported symmetric estimator is
\[
\widetilde\sigma_N=\Pi_F^{\rm tb}(\widehat\sigma^{\rm raw}_N).
\]
Define also
\[
\widehat J_{\Phi,N}=
\begin{cases}
D\Phi(\widehat r_N),
&\widehat r_{0,N}\ge\varepsilon_N,\quad |\widehat D_N|\ge\varepsilon_N,\\
0,&\text{otherwise,}
\end{cases}
\qquad
\widehat\Sigma_{\sigma,N}
=\widehat J_{\Phi,N}\widehat\Sigma_{r,N}^{\mathrm{all}}
\widehat J_{\Phi,N}^{\top}.
\]
If the population point lies in a stability neighbourhood where
\(r_0\) and \(|a(r)-r_0|\) are bounded away from zero, then
\[
\sqrt N(\Phi(\widehat r_N)-\Phi(r))
\Rightarrow \mathcal N(0,\Sigma_{\sigma}),
\qquad
\Sigma_{\sigma}=D\Phi(r)\Sigma_rD\Phi(r)^\top .
\]
The same limit holds with \(\Phi(\widehat r_N)\) replaced by
\(\widetilde\sigma_N\) at separated interior points of \(F\), and the sandwich
\(\widehat\Sigma_{\sigma,N}\) is consistent there.  For the killed-leakage
sampled-boundary population, \(\Phi(r)=(p+s,ps)\).  At separated roots the
projection is asymptotically inactive.

For labelled reporting define the empirical projected discriminant
\[
\widehat\Delta_{\sigma,N}
=\widetilde\sigma_{1,N}^{\,2}-4\widetilde\sigma_{2,N}\ge0 .
\]
Labelled reporting also requires, as external data, a supplied separated-root
branch map \(\mathcal B\) as in
\Cref{thm:identifiable-finite-record-estimands-main}.  The map
\(\mathcal B\) is not estimated from the record; it assigns the two roots to
the ordered attempt/recovery pair \((p,s)\) on a neighbourhood of the
population point.  We write
\(\mathcal B(\mathcal R_{\mathrm{root}}(\sigma))\) for the resulting ordered
pair of roots when the assignment is unique.
The finite-record rule reports no separated labelled roots and no separated
labelled rates whenever
\(\widehat\Delta_{\sigma,N}<\delta_N^2\).  This is a no-branch decision, not a
claim that the diagonal common-rate estimand is absent.  If the population
point is \(\sigma_z=(2z,z^2)\), the target common rate is instead
\(-\log z/\Delta\tau\) and its finite-record calibration is the projected
boundary construction of
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main} pushed forward
by \Cref{cor:exchange-diagonal-common-rate-confidence-main}.  On the
complementary separated-root event, compute the two roots of
\(z^2-\widetilde\sigma_{1,N}z+\widetilde\sigma_{2,N}=0\).  If either root lies
outside \([\eta_N,1-\eta_N]\), or if the supplied branch map \(\mathcal B\) does
not assign the two empirical roots uniquely to the population attempt and
recovery branches, the rule withholds separated labelled-rate reporting under
\Cref{conv:branch-map-labelled-rates-main}.  Only on the retained event are
the separated labelled roots and rates recorded as
\[
(\widehat p_N,\widehat s_N)
=\mathcal B(\mathcal R_{\mathrm{root}}(\widetilde\sigma_N)),
\qquad
(\widehat\Gamma_N,\widehat\kappa_{\mathrm r,N})
=\left(-\frac{\log\widehat p_N}{\Delta\tau},
       -\frac{\log\widehat s_N}{\Delta\tau}\right).
\]
This is a chart-retention statement: if \(\Delta_\sigma=(p-s)^2>0\) and a
supplied branch map \(\mathcal B\) has fixed the separated-root chart, then the
retained event has probability tending to one and the empirical assignment is
the same chart with probability tending to one.  On that event, with
\(J_{\Gamma,\kappa}\) as in \Cref{lem:separated-root-rate-delta-main},
\[
\sqrt N\{(\widehat\Gamma_N,\widehat\kappa_{\mathrm r,N})
-(\Gamma,\kappa_{\mathrm r})\}
\Rightarrow
\mathcal N(0,\Sigma_{\Gamma,\kappa}),
\qquad
\Sigma_{\Gamma,\kappa}
=J_{\Gamma,\kappa}(r)\Sigma_rJ_{\Gamma,\kappa}(r)^\top .
\]
The plug-in covariance
\[
\widehat\Sigma_{\Gamma,\kappa,N}
=\widehat J_{\Gamma,\kappa,N}
\,\widehat\Sigma_{r,N}^{\mathrm{all}}\,
\widehat J_{\Gamma,\kappa,N}^{\top}
\]
is formed only on the retained separated-root event and is then consistent
with the same operator-norm rate.  If a deterministic rank threshold \(t_N\)
satisfies
\(\|\widehat\Sigma_{\Gamma,\kappa,N}-\Sigma_{\Gamma,\kappa}\|_{\operatorname{op}}
=o_{\PP}(t_N)\), then the separated labelled-rate retained-rank Wald statistic
converges to \(\chi_k^2\), \(k=\operatorname{rank}\Sigma_{\Gamma,\kappa}\).
\Cref{conv:branch-map-labelled-rates-main} applies: no separated labelled-rate
interval is asserted without this retained separated-root chart and supplied
branch map \(\mathcal B\).  If
\(\Delta_\sigma=0\), then
\(\widehat\Delta_{\sigma,N}=O_{\PP}(N^{-1/2})\), so the rule reports no
separated labelled rates with probability tending to one because
\(N^{1/2}\delta_N^2\to\infty\); the common-rate estimand is instead covered by
the projected diagonal common-rate statement just cited.
\end{proposition}

\begin{proof}
On the inverse stability neighbourhood, \(\Phi\) is \(C^1\).  Since
\(\widehat r_N\to r\) in probability, the threshold event
\(\{\widehat r_{0,N}\ge\varepsilon_N,\ |\widehat D_N|\ge\varepsilon_N\}\) has
probability tending to one, so the fallback value \(\sigma^\circ\) is
asymptotically unused.  The delta method therefore gives the displayed
Gaussian limit for \(\Phi(\widehat r_N)\).  The sandwich covariance inherits
the same operator-norm rate because \(D\Phi(\widehat r_N)\) is bounded and
converges in probability to \(D\Phi(r)\) on the same event.

The identity \(\Phi(r)=(p+s,ps)\) is exactly the quotient inverse calculation
in \Cref{thm:toeplitz_image_inverse_duality}.  At separated roots,
\(\Phi(r)\) lies in the relative interior of \(F\), so the nearest-point
projection is locally unique and inactive with probability tending to one;
therefore replacing \(\Phi(\widehat r_N)\) by \(\widetilde\sigma_N\) does not
change the limit.  If \(\Delta_\sigma>0\), then
\(\widehat\Delta_{\sigma,N}\to\Delta_\sigma\) in probability, while
\(\delta_N^2\to0\); the discriminant threshold is eventually passed.  The two
projected empirical roots converge to the two distinct population roots, which
lie in \((0,1)\), so the endpoint threshold is also eventually passed and the
fixed supplied branch map \(\mathcal B\) assigns the roots uniquely with
probability tending to one.  This proves chart retention and rules out
asymptotic branch flips.
\Cref{lem:separated-root-rate-delta-main} then applies on the retained chart
and gives the labelled-root and labelled-rate limits.  If
\(\Delta_\sigma=0\), \(\widetilde\sigma_N-\Phi(r)=O_{\PP}(N^{-1/2})\) by the
projected-coordinate limit below, hence
\(\widehat\Delta_{\sigma,N}=O_{\PP}(N^{-1/2})\); the condition
\(N^{1/2}\delta_N^2\to\infty\) makes the no-label decision occur with
probability tending to one.  The plug-in covariance formula follows by
continuous mapping on the retained chart.  The retained-rank Wald statement is
Weyl's eigenvalue inequality plus Slutsky's theorem on the limiting range of
\(\Sigma_{\Gamma,\kappa}\).
\end{proof}

\begin{corollary}[Exchange-diagonal projection and projected-Gaussian coverage]\label{cor:exchange-diagonal-projected-gaussian-confidence-main}\label{cor:projected-symmetric-wald-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and use the killed-leakage labelled kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\]
with \(a,b\) defined by \eqref{eq:a-b-main}.  Thus \(T_1(R,R)=T_1(D,R)=0\),
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), \(P=T_0+T_1\) is stochastic,
\(0<p,s<1\), \(a>0\), \(b>0\), \(a+b=1-s\), and equal-rate formulas are
interpreted continuously.  Let \((A_n)\) be the stationary visible binary
process generated by these kernels.
Assume the all-complete-cycle inclusion CLT and covariance consistency from
\Cref{thm:finite-record-inference-audit-main,lem:three-coordinate-batch-rate-main},
and form
\[
\widetilde\sigma_N=\Pi_F(\Phi(\widehat r_N)),\qquad
F=\{(x+y,xy):0\le x,y\le1\}.
\]
At an exchange-diagonal point \(\sigma_z=(2z,z^2)\), \(0<z<1\), where the
unordered sampled survival factors satisfy \(p=s=z\),
\[
\sqrt N(\widetilde\sigma_N-\sigma_z)
\Rightarrow
\Pi_{H_z}(Z),\qquad
Z\sim\mathcal N(0,\Sigma_{\sigma}),\qquad
H_z=\{h\in\RR^2:h_2\le zh_1\}.
\]
Define the limiting projected statistic and its lower quantile by
\[
T_z=(\Pi_{H_z}Z)^\top\Sigma_\sigma^+(\Pi_{H_z}Z),
\qquad
c_{1-\alpha}(z)=\inf\{c:\PP(T_z\le c)\ge1-\alpha\}.
\]
Thus ordinary chi-square reporting is asserted only at separated interior
points and only on the nondegenerate retained covariance range.  At a true
exchange-diagonal point the usable boundary confidence construction is the
projected-Gaussian half-space inversion of
\Cref{lem:projected-gaussian-bootstrap-halfspace-main}: with
\(\widehat\Sigma_{\sigma,N}^{+}\) formed after the deterministic threshold
\(t_N\), define
\begin{equation}\label{eq:boundary-projected-statistic-main}
Q_N(z)=
N(\widetilde\sigma_N-\sigma_z)^\top\widehat\Sigma_{\sigma,N}^{+}
(\widetilde\sigma_N-\sigma_z),
\end{equation}
and take \(c^{-}_{1-\alpha,N}(z)\) as the lower conditional
\((1-\alpha)\)-quantile
of
\[
\Pi_{H_z}(Z_N^\ast)^\top\widehat\Sigma_{\sigma,N}^{+}\Pi_{H_z}(Z_N^\ast),
\qquad
Z_N^\ast\mid A_0,\ldots,A_{N-1}\sim
\mathcal N(0,\widehat\Sigma_{\sigma,N}).
\]
The diagonal set
\begin{equation}\label{eq:boundary-projected-confidence-set-main}
\{(2z,z^2):0<z<1,\ Q_N(z)\le c^{-}_{1-\alpha,N}(z)\}
\end{equation}
has first-order projected-Gaussian coverage at the true diagonal point
\((2z,z^2)\) whenever
\[
\PP\{T_z\le c_{1-\alpha}(z)\}=1-\alpha .
\]
The quantile \(c^{-}_{1-\alpha,N}(z)\) converges in probability to
\(c_{1-\alpha}(z)\) at every continuity point of the distribution function of
\(T_z\).  The exact equality condition above is automatic if
\(\operatorname{rank}\Sigma_\sigma=2\), and also if
\(\operatorname{rank}\Sigma_\sigma=1\) but
\(\operatorname{Range}\Sigma_\sigma\ne\operatorname{span}\{(-z,1)^\top\}\).
If \(\operatorname{rank}\Sigma_\sigma=1\) and
\(\operatorname{Range}\Sigma_\sigma=\operatorname{span}\{(-z,1)^\top\}\), then
\(T_z\) has a single atom of mass \(1/2\) at zero and no atom on
\((0,\infty)\).  Consequently the exact first-order statement holds for
levels with \(1-\alpha>1/2\), whose limiting quantile is positive.  For
\(1-\alpha\le1/2\) in that rank-one normal case, and in the zero-covariance
case \(\Sigma_\sigma=0\), this corollary asserts the projected-Gaussian limit and
conservative reporting only, not exact equality to \(1-\alpha\).  A
conservative fallback in those zero-quantile subcases is to report the
degenerate limit together with a fixed positive critical buffer
\(\varepsilon\); then
\(\liminf_{N\to\infty}\PP\{Q_N(z)\le\varepsilon\}=1\), hence the displayed
diagonal set with \(c^{-}_{1-\alpha,N}(z)\) replaced by \(\varepsilon\) has
limiting coverage at least \(1-\alpha\).

Studentized scalar intervals are claimed only under a nondegenerate limiting
studentization variance.  For the lag-two covariance interval below,
\Cref{lem:lag-two-variance-nondegenerate-main} supplies the explicit
double/triple variance sum and proves \(\sigma_{\gamma_2}^2>0\) on the full
positive sampled-boundary parameter domain.  For any other scalar diagnostic,
if the relevant limiting variance is zero, only the degenerate Gaussian limit
is asserted.
\end{corollary}

\begin{proof}
At \(\sigma_z=(2z,z^2)\), the local projection and half-space limit are
exactly
\Cref{lem:local-projection-exchange-diagonal-main,lem:exchange-diagonal-projection-main}.
\Cref{lem:projected-gaussian-bootstrap-halfspace-main}, applied with
\(X_N=\sqrt N(\widetilde\sigma_N-\sigma_z)\), \(z_N=z\), and
\(\widehat\Sigma_N=\widehat\Sigma_{\sigma,N}\), gives the conditional
projected-Gaussian quantile convergence at continuity points.  The continuity
and degeneracy alternatives are exactly those of
\Cref{thm:projected-gaussian-continuity-degeneracy-main} with
\(\Sigma=\Sigma_\sigma\).  In the exceptional zero-quantile cases, the
limiting statistic has an atom at zero (or is identically zero), so the
displayed procedure is not reported as an exact-level interval; using a
fixed positive critical buffer gives the conservative statement recorded
above.  Scalar studentization divides by a consistent positive variance
estimate; hence it is asserted only when the limiting variance is
nondegenerate.
\end{proof}

\begin{corollary}[Common-rate confidence set on the exchange diagonal]\label{cor:exchange-diagonal-common-rate-confidence-main}
Use the notation and hypotheses of
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main}.  For each
\(0<z<1\), set
\[
\gamma(z)=-\frac{\log z}{\Delta\tau}.
\]
Let the projected diagonal confidence set in symmetric coordinates be
\[
\mathcal C_{\sigma,N}^{\rm diag}
=\{(2z,z^2):0<z<1,\ Q_N(z)\le c^{-}_{1-\alpha,N}(z)\},
\]
with the same thresholded covariance pseudoinverse and projected-Gaussian
critical value as in
\eqref{eq:boundary-projected-statistic-main}--\eqref{eq:boundary-projected-confidence-set-main}.
The corresponding finite-record confidence set for the identified common
numerical rate is the image
\begin{equation}\label{eq:common-rate-diagonal-confidence-set-main}
\mathcal C_{\gamma,N}^{\rm diag}
=\left\{-\frac{\log z}{\Delta\tau}:0<z<1,\ 
Q_N(z)\le c^{-}_{1-\alpha,N}(z)\right\}.
\end{equation}
Equivalently, the confidence set for the invariant ordered labelled-rate pair
on the quotient diagonal is
\[
\{(\gamma,\gamma):\gamma\in\mathcal C_{\gamma,N}^{\rm diag}\}.
\]
If the true diagonal point is
\(\sigma_{z_*}=(2z_*,z_*^2)\) and
\(\gamma_*=-\log z_*/\Delta\tau\), then for every \(N\)
\begin{equation}\label{eq:common-rate-coverage-transfer-main}
\{\gamma_*\in\mathcal C_{\gamma,N}^{\rm diag}\}
=
\{\sigma_{z_*}\in\mathcal C_{\sigma,N}^{\rm diag}\}.
\end{equation}
Hence the first-order projected-Gaussian coverage, exact-level alternatives,
rank-one zero-quantile qualifications, and conservative-buffer fallback in
\Cref{cor:exchange-diagonal-projected-gaussian-confidence-main} transfer
unchanged to \(\mathcal C_{\gamma,N}^{\rm diag}\).  In particular, whenever
\(\PP\{T_{z_*}\le c_{1-\alpha}(z_*)\}=1-\alpha\),
\[
\lim_{N\to\infty}
\PP\{\gamma_*\in\mathcal C_{\gamma,N}^{\rm diag}\}
=1-\alpha,
\]
and in the conservative-buffer subcases the same replacement gives
\[
\liminf_{N\to\infty}
\PP\{\gamma_*\in\mathcal C_{\gamma,N}^{\rm diag}(\varepsilon)\}
\ge 1-\alpha .
\]
Here \(\mathcal C_{\gamma,N}^{\rm diag}(\varepsilon)\) denotes
\eqref{eq:common-rate-diagonal-confidence-set-main} with
\(c^{-}_{1-\alpha,N}(z)\) replaced by the fixed positive buffer
\(\varepsilon\).
\end{corollary}

\begin{proof}
The map \(z\mapsto-\log z/\Delta\tau\) is a bijection from \((0,1)\) to
\((0,\infty)\).  Therefore the true common rate \(\gamma_*\) belongs to the
image set \(\mathcal C_{\gamma,N}^{\rm diag}\) if and only if its unique
preimage \(z_*\) satisfies \(Q_N(z_*)\le c^{-}_{1-\alpha,N}(z_*)\), which is
exactly the event that \((2z_*,z_*^2)\) belongs to
\(\mathcal C_{\sigma,N}^{\rm diag}\).  This proves the identity
\eqref{eq:common-rate-coverage-transfer-main}.  The stated limiting and
conservative coverage conclusions are therefore the corresponding conclusions
of \Cref{cor:exchange-diagonal-projected-gaussian-confidence-main} applied to
the identical event.
\end{proof}

\begin{lemma}[Local projection at the exchange diagonal]\label{lem:local-projection-exchange-diagonal-main}
Let
\[
F=\{(x+y,xy):0\le x,y\le1\},\qquad \sigma_z=(2z,z^2),\quad 0<z<1.
\]
There is \(\varepsilon>0\) such that Euclidean projection onto \(F\) is
single-valued on \(B_\varepsilon(\sigma_z)\).  In local coordinates
\(v=\sigma_z+h/\sqrt N\), feasibility is
\[
h_2\le zh_1+\frac{h_1^2}{4\sqrt N}.
\]
Hence the scaled feasible sets converge locally to the half-space
\[
T_{\sigma_z}F=\{h:h_2\le zh_1\}.
\]
\end{lemma}

\begin{proof}
Choose \(\delta>0\) so that \(2z-\delta>0\), \(2z+\delta<2\), and the other
box boundaries of \(F\) are outside \(B_\delta(\sigma_z)\).  In this ball the root
constraints \(0\le x,y\le1\) are inactive except for the exchange
discriminant.  Hence
\[
F\cap B_\delta(\sigma_z)
=\{(\xi_1,\xi_2)\in B_\delta(\sigma_z):\xi_2\le \xi_1^2/4\}.
\]
Let \(\Gamma_z=\{(t,t^2/4):|t-2z|<\delta\}\).  This \(C^2\) arc has a tubular
neighbourhood on which nearest-point projection onto \(\Gamma_z\) is
single-valued; equivalently, the arc has positive local reach
\cite{Shapiro1987MetricProjection}.  Shrinking \(\delta\) if necessary, a
point below the parabola is already in \(F\), while a point above the parabola
and sufficiently close to \(\sigma_z\) has its nearest feasible point on
\(\Gamma_z\), not on any remote part of \(F\).  This gives
\(\varepsilon>0\) such that the Euclidean projection onto the full, globally
nonconvex set \(F\) is single-valued on \(B_\varepsilon(\sigma_z)\).

For \(v=\sigma_z+h/\sqrt N\), the local feasibility condition is
\[
z^2+\frac{h_2}{\sqrt N}
\le
\frac{(2z+h_1/\sqrt N)^2}{4},
\]
which is exactly
\[
h_2\le zh_1+\frac{h_1^2}{4\sqrt N}.
\]
On every bounded set of \(h\)'s the last term converges uniformly to \(0\),
so the scaled feasible sets converge locally to
\(T_{\sigma_z}F=\{h:h_2\le zh_1\}\).
\end{proof}

\begin{lemma}[Projection and root splitting at the exchange diagonal]\label{lem:exchange-diagonal-projection-main}
If \(y_N=\sigma_z+Z_N/\sqrt N+o_{\PP}(N^{-1/2})\) and
\(Z_N\Rightarrow Z\), then
\[
\sqrt N\{\Pi_F(y_N)-\sigma_z\}\Rightarrow \Pi_{T_{\sigma_z}F}(Z).
\]
For the unordered roots of \(t^2-\sigma_1t+\sigma_2=0\), the corresponding root
splitting is \(N^{-1/4}\) at the diagonal unless the half-space limit lies on
the diagonal direction.
\end{lemma}

\begin{proof}
\Cref{lem:local-projection-exchange-diagonal-main} gives local uniqueness and
Painleve-Kuratowski convergence of the scaled feasible sets to the closed
convex half-space \(T_{\sigma_z}F\).  Continuity of projection under this local
positive-reach limit gives the displayed weak convergence by the continuous
mapping theorem.  The discriminant of the projected coordinate is
\((\sigma_1-2z)^2-4(\sigma_2-z^2)\); after projection it is nonnegative and is typically
of order \(N^{-1/2}\), so the two roots split at order \(N^{-1/4}\).
\end{proof}

\begin{corollary}[Exchange-diagonal self-dual inverse]\label{cor:critical_self_dual_inverse}
At the exchange diagonal \(p=s\), the visible quotient inverse returns the
self-dual unordered pair \(\{p,p\}\), and root splitting is governed by
\Cref{lem:exchange-diagonal-projection-main}.  This exchange-diagonal
self-duality is not, by itself, the exact-dependence critical point: along
\(p=s=e^{-\Gamma\Delta\tau}=e^{-\kappa_{\mathrm r}\Delta\tau}\),
\(\lambda_{\mathrm{hid}}=0\) only at
\(\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau=1\), equivalently
\(p=s=e^{-1}\).
\end{corollary}

\begin{proposition}[Hidden-label exchange non-identifiability]\label{prop:hidden_label_exchange_nonidentifiability}
No statistic of one visible sampled record can distinguish the two separated
ordered points \((\Gamma,\kappa_{\mathrm r})\) and
\((\kappa_{\mathrm r},\Gamma)\) without external labelling information.
\Cref{conv:branch-map-labelled-rates-main} records the resulting reporting
convention.  This exchange
non-identifiability does not remove the diagonal common-rate estimand in
\Cref{lem:diagonal-common-rate-main}, where the two entries are equal.
\end{proposition}

\begin{proof}
The two parameter points induce the same finite-dimensional visible law by
\Cref{thm:toeplitz_image_inverse_duality}; hence every visible-record
statistic has the same distribution under them.  This is the exchange
specialization inside the present sampled D-MAP(2) family, consistent with the
broader MAP2 lesson that a hidden arrival representation need not be uniquely
identified from the stationary observable arrival law
\cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability}.
\end{proof}

\subsection{Fully studentized lag-two covariance interval}

This subsection isolates the only finite-record interval in the manuscript
that is fully studentized.  The first two lemmas are generic finite-lag CLT
and batch-covariance tools, reused here for the scalar lag-two observable.
The remaining results verify the lag-two reward identity, prove the positivity
of the scalar variance, and state the observable
\(\widehat\sigma^2_{\gamma_2,N}\)-based interval
\eqref{eq:lag-two-studentized-interval-main}.  Other displayed diagnostics in
the paper should be read as population formulas, consistency statements, or
projected/sandwich covariance statements unless a separate scalar studentizer
is explicitly supplied.

\begin{lemma}[Finite-lag regenerative CLT]\label{thm:finite-lag-renewal-reward-main}\label{def:cycle-block-main}\label{lem:cycle-one-dep-main}\label{lem:renewal-reward-boundary-main}\label{lem:renewal-local-clt-main}\label{lem:deterministic-prefix-main}\label{lem:lag2-boundary-main}\label{thm:cycle-block-main}
Let \((A_n)\) be a stationary renewal indicator process with i.i.d. gaps
satisfying \(\PP(G\ge k)\le Ce^{-\eta k}\).  Let
\(Z_n=\psi(A_n,\ldots,A_{n+h_{\rm cal}})\in\RR^{d_{\rm rew}}\) be a bounded
centered local observable, with fixed reward dimension \(d_{\rm rew}\) and
fixed calendar look-ahead \(h_{\rm cal}\).  Let
\[
m_{\rm dep}:=q_*(0,h_{\rm cal})=\max\{h_{\rm cal}-1,0\},
\]
where \(q_*\) is the explicit local-window range from
\Cref{lem:local-window-regenerative-decomposition-main}.  Define the Palm
cycle reward by the measurable map
\[
Y_j:=F_{0,h_{\rm cal}}(G_j,\ldots,G_{j+m_{\rm dep}}).
\]
Equivalently,
\[
Y_j=f(G_j,\ldots,G_{j+m_{\rm dep}})\in\RR^{d_{\rm rew}},\qquad
\|f\|\le C_f\left(1+\sum_{i=0}^{m_{\rm dep}}G_{j+i}\right).
\]
Put \(\mu=\EE(G+1)\) and
\[
\mathcal T_{m_{\rm dep}}=
\Var(Y_0)+
\sum_{h=1}^{m_{\rm dep}}\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\}.
\]
Then the exact calendar-time empirical vector average satisfies
\[
\sqrt N\left(\frac1N\sum_{n=0}^{N-1}Z_n\right)
=\frac1{\sqrt N}\sum_{n=0}^{N-1}Z_n
\Rightarrow
\mathcal N\left(0,\frac{\mathcal T_{m_{\rm dep}}}{\mu}\right).
\]
In the scalar case this covariance reduces to \(\tau_{m_{\rm dep}}^2/\mu\),
where
\[
\tau_{m_{\rm dep}}^2=\Var(Y_0)+2\sum_{h=1}^{m_{\rm dep}}\operatorname{Cov}(Y_0,Y_h).
\]
The same conclusion holds after the finite-start burn-in in
\Cref{thm:finite-record-inference-audit-main}.

In the sampled-boundary applications below, \((A_n)\) is specifically the
stationary visible renewal record generated by the killed-leakage kernels
\eqref{eq:t0-t1-main}, with \(T_1(R,R)=T_1(D,R)=0\), fixed
\(\Delta\tau\), \(0<p,s<1\), \(a,b>0\), \(a+b=1-s\), and all equal-rate
formulas interpreted by the continuous dictionary
\eqref{eq:equal-rate-dictionary-main}.  No continuous-comparator or
growing-window perturbation statement is used in this lemma.
\end{lemma}

\begin{proof}
Write the click epochs as \(\tau_j\), put
\(L_j=\tau_{j+1}-\tau_j=G_j+1\).  Since the observable is centered,
\(\bar z=\EE Z_0=0\).  The explicit map
\(F_{0,h_{\rm cal}}\) in
\Cref{lem:local-window-regenerative-decomposition-main} gives the
complete-cycle representation
\[
\sum_{n=0}^{N-1}Z_n
=\sum_{j=0}^{K_N-m_{\rm dep}-1}Y_j+o_{\PP}(\sqrt N),
\qquad
K_N=\max\{k:\tau_k<N\}.
\]
Here the \(o_{\PP}(\sqrt N)\) term is the
\(O_{\PP}(\log N)\) endpoint error from the initial incomplete cycle, the
terminal incomplete cycle, and the fixed number of look-ahead cycles omitted
by the all-complete-cycle convention.

The variables \(Y_j=f(G_j,\ldots,G_{j+m_{\rm dep}})\) are centered and \(m_{\rm dep}\)-dependent,
by \Cref{lem:local-window-regenerative-decomposition-main}.  The displayed growth bound and the
exponential tail give a finite second moment.  The multivariate CLT for
centered \(m_{\rm dep}\)-dependent sequences, equivalently the scalar CLT applied by
Cramer--Wold, gives, for deterministic \(m\to\infty\),
\[
\frac1{\sqrt m}\sum_{j=0}^{m-1}Y_j
\Rightarrow
\mathcal N(0,\mathcal T_{m_{\rm dep}}),
\qquad
\mathcal T_{m_{\rm dep}}=\Var(Y_0)+
\sum_{h=1}^{m_{\rm dep}}\{\operatorname{Cov}(Y_0,Y_h)+\operatorname{Cov}(Y_h,Y_0)\}.
\]
The renewal law gives \(K_N/N\to1/\EE L\), where \(L=G+1\).  Anscombe's
random-index theorem, or the equivalent maximal inequality for
\(m_{\rm dep}\)-dependent partial sums, then replaces \(m\) by \(K_N-m_{\rm dep}\) and yields
\[
\frac1{\sqrt N}\sum_{n=0}^{N-1}Z_n
\Rightarrow
\mathcal N\left(0,\frac{\mathcal T_{m_{\rm dep}}}{\EE L}\right).
\]

The finite-start burn-in case is the final assertion of
\Cref{thm:finite-record-inference-audit-main}: the statistic is formed from the
retained post-burn-in block of length \(N\), and \(L_N\to\infty\) makes the
initial-law discrepancy \(C\theta^{L_N}\) vanish in total variation.
\end{proof}

\begin{lemma}[Finite-lag separated-batch covariance]\label{thm:finite-lag-separated-batch-main}\label{lem:batch-var-random-count-main}
Under the hypotheses and notation of
\Cref{thm:finite-lag-renewal-reward-main}, form all-complete-cycle batches of
cycle rewards \(Y_j\) with deterministic length
\[
b_N=\lfloor N^\beta\rfloor,\qquad 0<\beta<1,
\]
and leave \(m_{\rm dep}\) complete cycle rewards between consecutive batches.
Let \(K_N=\max\{k:\tau_k<N\}\), \(d_N=b_N+m_{\rm dep}\), and use only batch
indices for which every local calendar window entering the batched rewards is
observed.  With
\[
I_{\ell,N}=\{(\ell-1)d_N,\ldots,(\ell-1)d_N+b_N-1\},
\qquad
B_{\ell,N}=\sum_{j\in I_{\ell,N}}Y_j,
\]
and \(M_N\) the number of all-complete-cycle batches selected before calendar
time \(N\), with \(M_N=0\) when no such batch exists.  Define
\(\bar B_N=M_N^{-1}\sum_{\ell=1}^{M_N}B_{\ell,N}\) on
\(\{M_N\ge1\}\), and set
\[
\widehat\Sigma_N^{\rm batch}
=
\begin{cases}
\displaystyle
\frac{K_N}{N}\,
\frac1{b_N(M_N-1)}
\sum_{\ell=1}^{M_N}(B_{\ell,N}-\bar B_N)(B_{\ell,N}-\bar B_N)^\top,
&M_N\ge2,\\[1.1ex]
0_{d_{\rm rew}\times d_{\rm rew}},
&M_N<2 .
\end{cases}
\]
Then \(M_N\ge2\) with probability tending to one and
\begin{equation}\label{eq:finite-lag-separated-batch-rate-main}
\left\|\widehat\Sigma_N^{\rm batch}
-\frac{\mathcal T_{m_{\rm dep}}}{\mu}\right\|_{\operatorname{op}}
=O_{\PP}\left(b_N^{-1}+\sqrt{\frac{b_N}{N}}\right).
\end{equation}
Thus separated batches estimate the calendar-time covariance
\(\mathcal T_{m_{\rm dep}}/\mu\) at the displayed rate.

In the sampled-boundary applications below, \((A_n)\) is specifically the
stationary visible renewal record generated by the killed-leakage kernels
\eqref{eq:t0-t1-main}, with \(T_1(R,R)=T_1(D,R)=0\), fixed
\(\Delta\tau\), \(0<p,s<1\), \(a,b>0\), \(a+b=1-s\), and all equal-rate
formulas interpreted by the continuous dictionary
\eqref{eq:equal-rate-dictionary-main}.  No continuous-comparator or
growing-window perturbation statement is used in this lemma.
\end{lemma}

\begin{proof}
The future-block representation in
\Cref{lem:local-window-regenerative-decomposition-main} makes
\((Y_j)\) \(m_{\rm dep}\)-dependent.  Leaving \(m_{\rm dep}\) rewards between
successive batches therefore makes the gap blocks used by distinct
\(B_{\ell,N}\)'s disjoint, hence independent within each triangular row.  The
exponential gap tail and the linear reward bound give the fourth-moment
hypothesis in \Cref{lem:calendar-stopped-batch-cov-main}.  That lemma gives
\[
\left\|\widehat\Sigma_N^{\rm batch}
-\frac{\mathcal T_{m_{\rm dep}}}{\mu}\right\|_{\operatorname{op}}
=O_{\PP}\left(b_N^{-1}+\sqrt{\frac{b_N}{N}}\right),
\]
including the random calendar-stopped value of \(M_N\), the endpoint cycles
omitted by the all-complete-cycle rule, and the sample-mean subtraction.  The
formula \(b_N=\lfloor N^\beta\rfloor\) implies \(b_N\to\infty\) and
\(b_N/N\to0\), so \(M_N\ge2\) with probability tending to one.  On the
complementary event the definition uses the zero-matrix fallback, and that
branch does not affect the probability limit.  For every fixed \(C\), the
underlying low-cycle event \(\{K_N<C\}\), with any fixed endpoint or
look-ahead deletion, has vanishing probability by
\Cref{lem:fixed-low-cycle-event-main}.
\end{proof}

\begin{lemma}[Observed lag-two windows in the all-complete-cycle batches]\label{lem:all-cycle-lag-two-windows-main}
For lag-two rewards, using all-complete-cycle batches and leaving one complete
cycle after the last batched cycle ensures every term \(A_nA_{n+2}\) used by
the statistic is observed.
\end{lemma}

\begin{proof}
If \(n\in[\tau_j,\tau_{j+1}-1]\), then \(A_{n+2}\) is determined by the
click pattern up to time \(\tau_{j+1}+1\).  This time lies no later than the
end of cycle \(j+1\): if \(G_j\ge1\), then \(\tau_{j+1}+1\le\tau_{j+2}\);
if \(G_j=0\), then the cycle \(j\) interval contains only \(n=\tau_j\), and
\(A_{\tau_j+2}\) is determined by whether \(G_{j+1}=0\).  Thus every lag-two
term assigned to cycle \(j\) is determined by \(G_j\) and \(G_{j+1}\).  The
all-complete-cycle convention uses only batched indices for which cycle
\(j+1\) is complete and, at the terminal edge, leaves one complete cycle after
the last batched cycle.  Hence no term \(A_nA_{n+2}\) in a used batch reaches
outside the observed record.
\end{proof}

\begin{lemma}[Lag-two reward identity, centering, and one-gap dependence]\label{lem:lag-two-reward-main}
For \(Z_n=(A_n-\rho)(A_{n+2}-\rho)-\gamma_2\), the Palm cycle reward has the
explicit form
\begin{equation}\label{eq:lag-two-cycle-reward-main}
Y_j=f(G_j,G_{j+1}),
\end{equation}
where
\[
f(g,h)=
\mathbf1_{\{g=1\}}+\mathbf1_{\{g=0,\ h=0\}}
-\rho\bigl(1+\mathbf1_{\{g\ge1\}}+\mathbf1_{\{h=0\}}\bigr)
+(\rho^2-\gamma_2)(g+1).
\]
In particular \(\EE Y_j=0\), and \((Y_j)\) is \(1\)-dependent in the cycle
index.
\end{lemma}

\begin{proof}
Work in Palm coordinates with \(\tau_j=0\).  The cycle \(j\) contains the
times \(0,1,\ldots,G_j\), the next click is at \(G_j+1\), and the following
click is at \(G_j+G_{j+1}+2\).  Hence
\[
\sum_{n=0}^{G_j}A_n=1,
\]
while the shifted count in the same cycle is
\[
\sum_{n=0}^{G_j}A_{n+2}
=\mathbf1_{\{G_j\ge1\}}+\mathbf1_{\{G_{j+1}=0\}}.
\]
The lag-two product count is nonzero only when the click at the cycle start
is followed by another click exactly two sampled steps later:
\[
\sum_{n=0}^{G_j}A_nA_{n+2}
=\mathbf1_{\{G_j=1\}}+\mathbf1_{\{G_j=0,\ G_{j+1}=0\}}.
\]
Summing
\((A_n-\rho)(A_{n+2}-\rho)-\gamma_2\) over the cycle therefore gives
\[
\begin{aligned}
Y_j
&=\sum_{n=0}^{G_j}A_nA_{n+2}
-\rho\sum_{n=0}^{G_j}A_n
-\rho\sum_{n=0}^{G_j}A_{n+2}
+(\rho^2-\gamma_2)(G_j+1)  \\
&=f(G_j,G_{j+1}),
\end{aligned}
\]
which is \eqref{eq:lag-two-cycle-reward-main}.

The centering is not an additional assumption.  The stationary renewal record
has \(\EE[(A_0-\rho)(A_2-\rho)-\gamma_2]=0\), and Palm inversion gives
\[
\EE Y_j
=\EE L\,
\EE\bigl[(A_0-\rho)(A_2-\rho)-\gamma_2\bigr]
=0.
\]
Finally, \(Y_j\) is a function only of the adjacent pair
\((G_j,G_{j+1})\).  If \(|j-k|>1\), the pairs
\((G_j,G_{j+1})\) and \((G_k,G_{k+1})\) are disjoint functions of i.i.d. gaps,
so \(Y_j\) and \(Y_k\) are independent.  Thus the reward sequence is
\(1\)-dependent.
\end{proof}

\begin{lemma}[Explicit lag-two variance and finite-pair nondegeneracy certificate]\label{lem:lag-two-variance-nondegenerate-main}
Let \(g_k=\PP(G=k)\) be the full-support gap law from
\Cref{lem:renewal-gap-main}, let \(\mu=\EE(G+1)=1/\rho\), and let
\[
f(i,j)=
\mathbf1_{\{i=1\}}+\mathbf1_{\{i=0,\ j=0\}}
-\rho\bigl(1+\mathbf1_{\{i\ge1\}}+\mathbf1_{\{j=0\}}\bigr)
+(\rho^2-\gamma_2)(i+1),
\qquad i,j\ge0 .
\]
Equivalently, for independent gaps \(G_0,G_1,G_2\) with law \(g\),
the centered Palm reward for
\((A_n-\rho)(A_{n+2}-\rho)-\gamma_2\) is \(f(G_j,G_{j+1})\).  The
lag-two calendar-time variance in
\Cref{cor:boundary-reset-cycle-clt-main} is
\begin{equation}\label{eq:lag-two-probabilistic-variance-main}
\sigma_{\gamma_2}^2
=\frac{
\EE f(G_0,G_1)^2
+2\EE\{f(G_0,G_1)f(G_1,G_2)\}}
{\EE(G+1)} .
\end{equation}
It is also the absolutely convergent double/triple sum
\begin{equation}\label{eq:lag-two-explicit-variance-main}
\sigma_{\gamma_2}^2
=\frac1{\mu}\left[
\sum_{i,j\ge0}f(i,j)^2g_ig_j
+2\sum_{i,j,k\ge0}f(i,j)f(j,k)g_ig_jg_k
\right].
\end{equation}
Thus numerical verification of the studentizing variance is finite and
monotone: truncate the two sums to \(0\le i,j,k\le K\), bound the omitted tail
with the exponential envelope for \(g_k\) and the linear growth of \(f\), and
increase \(K\) until the tail bound is below the required tolerance.  The
closed nondegeneracy certificate below shows that this numerical recipe is a
check on arithmetic rather than an additional assumption.
Equivalently, with
\[
R_i=\sum_{j\ge0}f(i,j)g_j,\qquad
C_i=\sum_{j\ge0}f(j,i)g_j,\qquad
M=\sum_{i,j\ge0}f(i,j)g_ig_j,
\]
one has the sum-of-squares form
\begin{equation}\label{eq:lag-two-variance-squares-main}
\mu\,\sigma_{\gamma_2}^2
=\sum_{i\ge0}g_i(R_i+C_i-2M)^2
+\sum_{i,j\ge0}g_ig_j\bigl(f(i,j)-R_i-C_j+M\bigr)^2 .
\end{equation}
The finite certificate
\begin{equation}\label{eq:lag-two-finite-nondegeneracy-certificate-main}
f(0,0)-f(0,1)=1-\rho,\qquad
f(1,0)-f(1,1)=-\rho
\end{equation}
excludes the only possible zero-variance coboundary.  Hence
the expression in \eqref{eq:lag-two-probabilistic-variance-main} is strictly
positive.
In particular \(\sigma_{\gamma_2}^2>0\) for every
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\).
\end{lemma}

\begin{proof}
By \Cref{lem:lag-two-reward-main}, the centered Palm reward is
\(Y_j=f(G_j,G_{j+1})\), where the gaps are i.i.d. with law \(g\).  The
exponential tail of \(g\) and the linear growth of \(f(i,j)\) in \(i\) give
absolute convergence of all displayed second-moment sums.  Since
\(\EE Y_j=0\) and the sequence is \(1\)-dependent,
\[
\sigma_{\gamma_2}^2
=\frac{\Var(Y_0)+2\operatorname{Cov}(Y_0,Y_1)}{\mu}
=\frac{\EE(Y_0^2)+2\EE(Y_0Y_1)}{\mu},
\]
which is exactly \eqref{eq:lag-two-probabilistic-variance-main} and
\eqref{eq:lag-two-explicit-variance-main} after expanding
\[
\EE(Y_0^2)=\sum_{i,j\ge0}f(i,j)^2g_ig_j,\qquad
\EE(Y_0Y_1)=\sum_{i,j,k\ge0}f(i,j)f(j,k)g_ig_jg_k .
\]

It remains to make the nondegeneracy explicit rather than implicit.  The
unconditional mean \(M=\EE f(G_0,G_1)\) is zero, because
\(\EE Y_j=0\); it is kept in the formulas to display the orthogonal
decomposition.  The conditional row and column means of \(f(G_0,G_1)\) are
\(R_i\) and \(C_i\), and the centered residual
\[
E_{ij}=f(i,j)-R_i-C_j+M
\]
has zero conditional mean in each coordinate:
\[
\EE\{E_{G_0,G_1}\mid G_0\}=0,\qquad
\EE\{E_{G_0,G_1}\mid G_1\}=0 .
\]
For completeness we spell out the two conditional-expectation steps behind
the sum-of-squares identity.  With
\(X=f(G_0,G_1)\) and \(X'=f(G_1,G_2)\), independence of the gaps and the
Hoeffding decomposition
\[
X=M+(R_{G_0}-M)+(C_{G_1}-M)+E_{G_0,G_1}
\]
give, using \(M=0\),
\[
\EE X^2
=\EE E_{G_0,G_1}^2
 +\EE R_{G_0}^2+\EE C_{G_1}^2 ,
\qquad
\EE(XX')
=\EE\{\EE(X\mid G_1)\EE(X'\mid G_1)\}
=\EE\{C_{G_1}R_{G_1}\}.
\]
Therefore
\[
\EE(Y_0^2)+2\EE(Y_0Y_1)
=\EE E_{G_0,G_1}^2+\EE\bigl(R_{G_0}+C_{G_0}\bigr)^2
=\EE E_{G_0,G_1}^2+\EE\bigl(R_{G_0}+C_{G_0}-2M\bigr)^2,
\]
which is \eqref{eq:lag-two-variance-squares-main}.  Thus the variance can
vanish only if both square terms vanish.  Because the gap law has full support,
this is equivalent to
\[
f(i,j)=R_i+C_j-M,\qquad R_i+C_i=2M,\qquad i,j\ge0,
\]
or, after writing \(u_i=R_i-M\) and recalling \(M=0\), to the coboundary form
\[
f(i,j)=u_i-u_j,\qquad i,j\ge0 .
\]
That form is impossible for the lag-two reward.  Indeed, the difference
between columns \(0\) and \(1\) would have to be independent of the row index,
whereas the two finite comparisons in
\eqref{eq:lag-two-finite-nondegeneracy-certificate-main} give different
values.  Since \(0<\rho<1\), they cannot be equal.  Hence the zero-variance
coboundary case is excluded, and \(\sigma_{\gamma_2}^2>0\) throughout the
positive parameter domain.
\end{proof}

\begin{lemma}[Trimmed shifted-click centering]\label{lem:trimmed-shifted-click-centering-main}
Let \((A_n)_{n\in\mathbb Z}\) be a stationary renewal indicator process with
i.i.d. gaps \(G_j\) satisfying \(\PP(G\ge k)\le Ce^{-\eta k}\), click epochs
\((\tau_j)\), and click density \(\rho=\EE A_0\).  Fix
\(\ell\ge0\).  Let \(J_N^{(\ell)}\) be any all-complete-cycle index set
obtained from the complete observed cycles in \([0,N)\) by deleting only a
fixed number of endpoint cycles and by requiring that every term
\(A_{n+\ell}\), \(n\in[\tau_j,\tau_{j+1})\), is observed.  Then
\begin{equation}\label{eq:trimmed-shifted-click-centering-main}
\frac1N\sum_{j\in J_N^{(\ell)}}
\sum_{n=\tau_j}^{\tau_{j+1}-1}(A_{n+\ell}-\rho)
=O_{\PP}(N^{-1/2}) .
\end{equation}
Consequently, for any fixed finite set of shifts
\(\Lambda\subset\mathbb Z_{\ge0}\), fixed coefficients \(c_\ell\), and any
all-complete-cycle index set on which all the shifted terms are observed and
which differs from the complete-cycle set by only fixed endpoint trimming,
\begin{equation}\label{eq:trimmed-shifted-linear-combination-main}
\frac1N\sum_{j\in J_N}
\sum_{n=\tau_j}^{\tau_{j+1}-1}
\sum_{\ell\in\Lambda}c_\ell(A_{n+\ell}-\rho)
=O_{\PP}(N^{-1/2}) .
\end{equation}
In particular the conclusion holds for the lag-two all-complete-cycle set
\(J_{2,N}\) with \(\ell=0\) and \(\ell=2\), and for their sum.
\end{lemma}

\begin{proof}
Apply \Cref{lem:local-window-regenerative-decomposition-main} to the bounded
local observable \(Z_n=A_{n+\ell}-\rho\), with \(r=0\) and the fixed
look-ahead \(\ell\).  Since the process is stationary,
\(\EE Z_0=0\).  Palm inversion therefore centers the assigned cycle reward
\[
W_j^{(\ell)}
=\sum_{n=\tau_j}^{\tau_{j+1}-1}(A_{n+\ell}-\rho)
\]
by the explicit local-window map with \(r=0\):
\(\EE W_j^{(\ell)}=0\).  The reward is a function of the fixed future gap
block \(F_{0,\ell}(G_j,\ldots,G_{j+q_\ell})\), where
\(q_\ell=\max\{\ell-1,0\}\).  Moreover
\(|W_j^{(\ell)}|\le L_j+\ell+1\), so the exponential tail of \(G_j\) gives
\(\EE |W_j^{(\ell)}|^2<\infty\).  Hence the centered sequence
\((W_j^{(\ell)})\) is \(q_\ell\)-dependent with finite variance, and its
partial sums over any deterministic \(m=O(N)\) consecutive cycles are
\(O_{\PP}(\sqrt N)\).

The renewal law gives \(K_N=O_{\PP}(N)\) for the number of complete cycles
intersecting \([0,N)\).  Replacing the deterministic \(O(N)\) prefix by the
calendar-stopped prefix \(K_N\), or by a subset that differs from it by only a
fixed number of endpoint and look-ahead cycles, changes the preceding partial
sum by \(O_{\PP}(\log N)\) by the endpoint bound in
\Cref{lem:local-window-regenerative-decomposition-main}.  Thus the numerator
in \eqref{eq:trimmed-shifted-click-centering-main} is \(O_{\PP}(\sqrt N)\),
and division by \(N\) proves the claim.
The linear-combination form follows by applying the preceding bound to each
fixed shift after replacing \(J_N\) by the corresponding shift-specific
observed all-complete-cycle set; the finitely many discrepancies between these
sets are endpoint cycles, hence contribute only \(O_{\PP}(\log N)\) before
division by \(N\).  A fixed finite linear combination of
\(O_{\PP}(N^{-1/2})\) terms is again \(O_{\PP}(N^{-1/2})\).
\end{proof}

\begin{lemma}[Fixed low-cycle events vanish]\label{lem:fixed-low-cycle-event-main}
Let \((A_n)_{n\in\mathbb Z}\) be the stationary visible renewal record above,
with click density \(\rho>0\).  Let
\[
K_N=\max\{k:\tau_k<N\},
\]
with the convention \(K_N=-1\) if no click is observed in \([0,N)\).  For every
fixed integer \(C\ge0\),
\[
\PP(K_N<C)\longrightarrow0 .
\]
The same conclusion holds after deleting any fixed number of initial,
terminal, look-back, or look-ahead complete cycles from the observed
complete-cycle list.
\end{lemma}

\begin{proof}
The number of observed clicks is
\[
R_N=\sum_{n=0}^{N-1}A_n .
\]
By stationarity and ergodicity of the renewal indicator,
\(R_N/N\to\rho\) almost surely.  Since \(K_N+1=R_N\) under the convention above,
the event \(\{K_N<C\}\) is the event \(\{R_N\le C\}\), which is contained in
\(\{R_N/N\le \rho/2\}\) for all sufficiently large \(N\).  Its probability
therefore tends to \(0\).  If a fixed number \(c\) of endpoint or look-ahead
cycles is removed, the corresponding low-admissible-cycle event is contained
in \(\{K_N<C+c\}\), and the first part applies.
\end{proof}

\begin{corollary}[Killed-leakage sampled-boundary fixed-setting lag-two covariance CLT]\label{cor:boundary-reset-cycle-clt-main}\label{thm:sampled-finite-record-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), let \(T_0,T_1\) be the
killed-leakage labelled kernels \eqref{eq:t0-t1-main} with
\(T_1(R,R)=T_1(D,R)=0\), \(P=T_0+T_1\) stochastic, \(0<p,s<1\), \(a>0\),
\(b>0\), \(a+b=1-s\), and equal-rate formulas interpreted continuously.  Let
\((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process generated by
these kernels and observed in the fixed-setting long-record regime of
\Cref{thm:finite-record-inference-audit-main}.  Define the observed
complete-cycle lag-two covariance estimator by
\[
\widehat\gamma_{2,N}
=\frac1N\sum_{j\in J_{2,N}}\sum_{n=\tau_j}^{\tau_{j+1}-1}
(A_n-\widehat\rho_N)(A_{n+2}-\widehat\rho_N),
\qquad
\widehat\rho_N=\frac1N\sum_{n=0}^{N-1}A_n,
\]
where \(J_{2,N}\) contains exactly those all-complete-cycle indices for which
the lag-two windows lie in the observed record.

The scalar studentizer used for this statistic is the following observable
separated-batch estimator.  Let
\[
b_N=\lfloor N^\beta\rfloor,\qquad 0<\beta<1,\qquad d_N=b_N+1,
\]
and let
\[
M_{2,N}=\max\{m\ge0:(m-1)d_N+b_N-1\in J_{2,N}\},
\qquad
I^{\gamma}_{\ell,N}
=\{(\ell-1)d_N,\ldots,(\ell-1)d_N+b_N-1\},
\]
with \(M_{2,N}=0\) if the displayed set is empty.  For
\(j\in J_{2,N}\), define the plug-in lag-two cycle reward
\begin{equation}\label{eq:lag-two-plugin-cycle-reward-main}
\widehat Y^{\gamma}_{j,N}
=
\sum_{n=\tau_j}^{\tau_{j+1}-1}
\left\{(A_n-\widehat\rho_N)(A_{n+2}-\widehat\rho_N)
-\widehat\gamma_{2,N}\right\}.
\end{equation}
Put
\[
\widehat C^{\gamma}_{\ell,N}
=\sum_{j\in I^{\gamma}_{\ell,N}}\widehat Y^{\gamma}_{j,N},
\qquad
\bar{\widehat C}^{\gamma}_N
=M_{2,N}^{-1}\sum_{\ell=1}^{M_{2,N}}\widehat C^{\gamma}_{\ell,N}
\quad\text{on }\{M_{2,N}\ge1\}.
\]
The scalar lag-two variance estimator is
\begin{equation}\label{eq:lag-two-scalar-studentizer-main}
\widehat\sigma^2_{\gamma_2,N}
=
\begin{cases}
\displaystyle
\frac{K_N}{N}\,
\frac1{b_N(M_{2,N}-1)}
\sum_{\ell=1}^{M_{2,N}}
\left(\widehat C^{\gamma}_{\ell,N}
-\bar{\widehat C}^{\gamma}_N\right)^2,
&M_{2,N}\ge2,\\[1.1ex]
0,&M_{2,N}<2 .
\end{cases}
\end{equation}
Then
\[
\sqrt N(\widehat\gamma_{2,N}-\gamma_2)
\Rightarrow \mathcal N(0,\sigma_{\gamma_2}^2),
\]
where \(\sigma_{\gamma_2}^2\) is the explicit positive variance
\eqref{eq:lag-two-explicit-variance-main}, and
\[
\widehat\sigma^2_{\gamma_2,N}
\xrightarrow{\PP}\sigma_{\gamma_2}^2 .
\]
Consequently, for \(0<\alpha<1\), with
\(z_{1-\alpha/2}\) denoting the standard-normal \(1-\alpha/2\) quantile and
\(\widehat\sigma_{\gamma_2,N}=(\widehat\sigma^2_{\gamma_2,N})^{1/2}\),
the fixed-setting two-sided interval
\begin{equation}\label{eq:lag-two-studentized-interval-main}
\left[
\widehat\gamma_{2,N}
-z_{1-\alpha/2}\frac{\widehat\sigma_{\gamma_2,N}}{\sqrt N},
\quad
\widehat\gamma_{2,N}
+z_{1-\alpha/2}\frac{\widehat\sigma_{\gamma_2,N}}{\sqrt N}
\right]
\end{equation}
has limiting coverage \(1-\alpha\).
\end{corollary}

\begin{proof}
\Cref{lem:lag-two-reward-main} identifies the complete-cycle reward for the
true-centered statistic as a centered \(1\)-dependent reward, and
\Cref{lem:all-cycle-lag-two-windows-main} verifies that every lag-two term
used by \(J_{2,N}\) is observed.  Thus the \(m_{\rm dep}=1\) case of
\Cref{thm:finite-lag-renewal-reward-main} applies with
\[
\sigma_{\gamma_2}^2
=\frac{\Var(Y_0)+2\operatorname{Cov}(Y_0,Y_1)}{\EE(G+1)}.
\]
\Cref{lem:lag-two-variance-nondegenerate-main} identifies this quantity with
the double/triple sum \eqref{eq:lag-two-explicit-variance-main} and proves
that it is strictly positive on the full positive parameter domain.
Replacing \(\rho\) by \(\widehat\rho_N\) in the displayed estimator changes
the true-centered average by
\[
-(\widehat\rho_N-\rho)\frac1N
\sum_{j\in J_{2,N}}\sum_{n=\tau_j}^{\tau_{j+1}-1}
\bigl((A_n-\rho)+(A_{n+2}-\rho)\bigr)
+(\widehat\rho_N-\rho)^2\frac1N\sum_{j\in J_{2,N}}L_j .
\]
The stationarity and exponential renewal tail give the ordinary record mean
\(\widehat\rho_N-\rho=O_{\PP}(N^{-1/2})\).  The trimmed bracket is exactly the
linear-combination estimate
\eqref{eq:trimmed-shifted-linear-combination-main} with
\(\Lambda=\{0,2\}\) and \(c_0=c_2=1\):
\[
\frac1N
\sum_{j\in J_{2,N}}\sum_{n=\tau_j}^{\tau_{j+1}-1}
\bigl((A_n-\rho)+(A_{n+2}-\rho)\bigr)
=O_{\PP}(N^{-1/2}).
\]
Also \(N^{-1}\sum_{j\in J_{2,N}}L_j=O_{\PP}(1)\).  Therefore the two plug-in
terms are respectively \(O_{\PP}(N^{-1})\) and \(O_{\PP}(N^{-1})\), hence
\(o_{\PP}(N^{-1/2})\).  The CLT therefore has the same variance as the
true-centered reward.

It remains only to identify the displayed scalar studentizer with the
batch-estimated variance of that same limiting reward.  Let
\[
Y^\gamma_j
=\sum_{n=\tau_j}^{\tau_{j+1}-1}
\{(A_n-\rho)(A_{n+2}-\rho)-\gamma_2\},
\qquad
C^\gamma_{\ell,N}=\sum_{j\in I^\gamma_{\ell,N}}Y^\gamma_j .
\]
Write
\(\bar C^\gamma_N=M_{2,N}^{-1}\sum_{\ell=1}^{M_{2,N}}C^\gamma_{\ell,N}\)
on \(\{M_{2,N}\ge1\}\).
The \(m_{\rm dep}=1\) case of
\Cref{thm:finite-lag-separated-batch-main} applied to
\((Y^\gamma_j)\) gives consistency of
\[
\frac{K_N}{N}\,
\frac1{b_N(M_{2,N}-1)}
\sum_{\ell=1}^{M_{2,N}}
\left(C^\gamma_{\ell,N}-\bar C^\gamma_N\right)^2
\]
for \(\sigma_{\gamma_2}^2\), with the same zero fallback on
\(\{M_{2,N}<2\}\), an event whose probability tends to zero.

For the plug-in batch rewards, write
\[
\Delta_{\rho,N}=\widehat\rho_N-\rho,\qquad
\Delta_{\gamma,N}=\widehat\gamma_{2,N}-\gamma_2,
\]
and
\[
W_j=\sum_{n=\tau_j}^{\tau_{j+1}-1}
\bigl\{(A_n-\rho)+(A_{n+2}-\rho)\bigr\}.
\]
Then, for every used cycle,
\begin{equation}\label{eq:lag-two-plugin-cycle-difference-main}
\widehat Y^\gamma_{j,N}-Y^\gamma_j
=-\Delta_{\rho,N}W_j
+(\Delta_{\rho,N}^2-\Delta_{\gamma,N})L_j .
\end{equation}
The CLT already proved above gives
\(\Delta_{\gamma,N}=O_{\PP}(N^{-1/2})\), and the ordinary renewal mean gives
\(\Delta_{\rho,N}=O_{\PP}(N^{-1/2})\).  The uniform block-average lemma
\Cref{lem:uniform-calendar-block-length-main} gives
\[
\max_{\ell\le M_{2,N}}
\frac1{b_N}\sum_{j\in I^\gamma_{\ell,N}}L_j=O_{\PP}(1).
\]
Since \(|W_j|\le2L_j+4\), the same length bound gives directly
\[
\max_{\ell\le M_{2,N}}
\frac1{b_N}\left|\sum_{j\in I^\gamma_{\ell,N}}W_j\right|
=O_{\PP}(1).
\]
Consequently, with
\(P^\gamma_{\ell,N}:=\widehat C^\gamma_{\ell,N}-C^\gamma_{\ell,N}\),
\[
\max_{\ell\le M_{2,N}}|P^\gamma_{\ell,N}|
=O_{\PP}(b_NN^{-1/2}).
\]
Comparing the plug-in and true-centered scalar batch variances by the same
Cauchy bound used in
\Cref{lem:three-coordinate-batch-rate-main}, the two cross terms are
\(O_{\PP}(\sqrt{b_N/N})\) and the square term is
\(O_{\PP}(b_N/N)\).  Both are \(o_{\PP}(1)\) because \(b_N/N\to0\).
Thus
\(\widehat\sigma^2_{\gamma_2,N}\to_{\PP}\sigma_{\gamma_2}^2\).
Since the limiting variance is positive by
\Cref{lem:lag-two-variance-nondegenerate-main}, Slutsky's theorem gives the
studentized statistic and hence the interval
\eqref{eq:lag-two-studentized-interval-main}.
\end{proof}

\begin{proposition}[What one killed-leakage sampled-boundary fixed-setting long record estimates]\label{thm:one-record-estimates-main}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), with killed-leakage labelled
kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\qquad
p=e^{-\Gamma\Delta\tau},\quad s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
where \(a,b\) are the one-recovery-opportunity integrals in
\eqref{eq:a-b-main}.  Thus \(T_1(R,R)=T_1(D,R)=0\), \(P=T_0+T_1\) is
stochastic, \(0<p,s<1\), \(a>0\), \(b>0\), \(a+b=1-s\), and equal-rate
expressions are interpreted by continuous limits.  Let
\((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process generated by
these kernels.  A single fixed-setting record estimates only quantities of
that stationary visible law: the inclusion coordinates, the projected
symmetric coordinates \((p+s,ps)\), the fixed-setting lag-two covariance, and
their regenerative covariances under the assumptions above.  It does not label
two separated rates and does not identify any object outside the visible
stationary record law.  The branch-map convention is
\Cref{conv:branch-map-labelled-rates-main}; at \(\sigma=(2z,z^2)\) the common
numerical rate is the invariant exception described in
\Cref{lem:diagonal-common-rate-main}.
\end{proposition}

\begin{proof}
The positive estimands are exactly the CLT targets in
\Cref{thm:finite-record-inference-audit-main,thm:sampled-finite-record-main}.
The non-identifiability statement is
\Cref{prop:hidden_label_exchange_nonidentifiability}.  The record analysis
starts from \(T_0,T_1\) and therefore supplies no estimator for any externally
defined scalar map or its inverse images.
\end{proof}
