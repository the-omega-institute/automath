
\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{float}
\usepackage{graphicx}
\graphicspath{{generated/}}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\crefname{theorem}{theorem}{theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{lemma}{lemma}{lemmas}
\Crefname{lemma}{Lemma}{Lemmas}
\crefname{proposition}{proposition}{propositions}
\Crefname{proposition}{Proposition}{Propositions}
\crefname{corollary}{corollary}{corollaries}
\Crefname{corollary}{Corollary}{Corollaries}
\crefname{assumption}{assumption}{assumptions}
\Crefname{assumption}{Assumption}{Assumptions}
\crefname{definition}{definition}{definitions}
\Crefname{definition}{Definition}{Definitions}
\crefname{remark}{remark}{remarks}
\Crefname{remark}{Remark}{Remarks}
\crefname{importedinput}{imported input}{imported inputs}
\Crefname{importedinput}{Imported input}{Imported inputs}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage[mathlines,switch]{lineno}
\IfFileExists{orcidlink.sty}{\usepackage{orcidlink}}{}

\hypersetup{colorlinks=true,citecolor=blue!60!black,linkcolor=blue!60!black,urlcolor=blue!60!black}
\setstretch{1.07}
\setlength{\parskip}{0.38em}
\setlength{\parindent}{1.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem*{unnumberedlemma}{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{importedinput}[theorem]{Imported input}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\RR}{\mathbb{R}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Var}{\mathrm{Var}}

\title{Record laws for a stipulated killed-leakage sampled-boundary HMM: exact dependence, renewal LU product determinants, and fixed-setting long-record inference}
\author{Haobo Ma\thanks{CHRONOAI PTE. LTD., Singapore. Email: \href{mailto:auric@aelf.io}{auric@aelf.io}.}
\and
Wenlin Zhang\thanks{National University of Singapore, Singapore. Email: \href{mailto:e1327962@u.nus.edu}{e1327962@u.nus.edu}.}}
\date{}

\begin{document}
\modulolinenumbers[1]
\linenumbers
\maketitle

\noindent\fbox{%
\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{%
\textbf{Record-law scope.}
The main theorem chain concerns a classical sampled hidden Markov renewal law.
The inputs are the labelled stochastic kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\]
their stationary visible binary process, and fixed finite-record statistics
of that process.  Each main theorem below restates the local assumptions it
uses; this box is only a scope guide.  The parameters \(\Gamma\) and
\(\kappa_{\mathrm r}\) are record-side rate labels throughout the theorem
chain.}}

\begin{abstract}
This paper proves exact laws for a stipulated classical killed-leakage
sampled-boundary HMM renewal process.  Time is sampled in intervals of length
\(\Delta\tau\), the hidden state records readiness or recovery, the sampled
post-click boundary convention sets the return-to-ready entries of the click
kernel to zero, and the visible record is a binary sequence.  The theorem chain
is entirely record-level and concerns this sampled HMM only.  The appendix
contains a separate fixed-window total-variation comparison with a continuous
intra-bin hidden-recovery comparator; no stationary spectrum, Fano limit,
inverse law, or long-record CLT is transferred to that comparator.  The ideal one-step
blanking model is retained only as the standard Parry benchmark
\(\Gamma\Delta\tau=\log\varphi\). The main killed-leakage sampled-boundary
theorem treats the one-recovery-opportunity counter with attempt rate \(\Gamma\) and
recovery rate \(\kappa_{\mathrm r}\): its zero-run law has full support on
\(\mathbb N_0\), its zero-run hazard is strictly increasing, and its click
covariance has the hidden-eigenvalue tail
\(\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{n-1}\).
The visible record is exact \(1\)-dependent exactly at the unique rate
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) where
\(\lambda_{\mathrm{hid}}=0\).

A stationary-renewal lemma first constructs click-set probabilities from
renewal products and finite inclusion--exclusion.  As bookkeeping after those
probabilities are known, the resulting finite algebra can be encoded by an LU
product determinant; the boundary-reset law specializes it to explicit finite
Toeplitz matrices with
\(D_\Gamma(B)=\rho\prod_r u_{i_{r+1}-i_r}\). The same renewal record law has a quotient inverse:
three inclusion probabilities recover the symmetric coordinate vector, and hence at
the population level the unordered survival-factor pair
\(\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}\), equivalently
\(\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}\). Thus one
visible sampled record does not label which rate is the recovery
rate. Finite-record inverse inference is stated for the projected feasible
symmetric coordinates \(\sigma_{\rm sym}=(\sigma_1,\sigma_2)\); ordinary root and rate covariance
estimates require a separated-root hypothesis away from the exchange diagonal
\(e^{-\Gamma\Delta\tau}=e^{-\kappa_{\mathrm r}\Delta\tau}\). The finite-record
part then proves a fixed-setting lag-two covariance CLT and a complete-cycle
variance estimator for \(\gamma_2\) in this sampled-counter law.
\end{abstract}

\noindent\textbf{Keywords:} classical sampled HMM, renewal process, exact \(1\)-dependence, renewal LU product determinant, finite-record diagnostics

\section{Introduction}

This paper is about the record law of a classical sampled HMM.  The model has
an internal attempt rate \(\Gamma\), one hidden readiness bit, and a binary
output recording whether the sampling interval contains a click. Its visible
process has grammar, waiting-time, dependence, covariance, and counting-noise
observables that are invisible in a rate-only summary. The paper-level object
is therefore the sampled record itself: one stipulated interval instrument, its
stationary binary law, its dependence threshold, its renewal LU product
determinants for inclusion products, its quotient inverse, and its fixed-setting long-record
lag-two covariance estimator.

The scalar rate \(\Gamma\) is an internal parameter of the sampled counter.
All main-text claims below concern the induced binary record law.

The main text below is organized as four record-side theorem blocks, followed by
the theorem-by-theorem comparison in \Cref{sec:theorem-led-comparison}.
When the prose uses detector-characterization words such as click, dead time,
recovery, reset, and count, it uses them only as terminology for the stipulated
binary record model.  This convention is aligned with the single-photon
detector dictionary terminology of NIST IR 8486r1, where a detection event may
be called a count and dead time, reset time, afterpulse probability, timing
jitter, and related quantities are detector metrics
\cite{NISTIR8486r1SinglePhotonDictionary}.  The present theorem chain does not
model those full device metrics; it proves the law of the sampled kernels
stated below.

\subsection{Main killed-leakage sampled-boundary record theorem}\label{subsec:main-theorem-boundary}\label{subsec:scope-nonclaims}

For fixed
\((\Gamma,\kappa_{\mathrm r},\Delta\tau)\in(0,\infty)^3\) and the sampled
boundary-reset kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\]
the visible click process is a stationary renewal process with gap law \(g_k\),
increasing hazard \(h_k\), covariance
\[
\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad n\ge1,
\]
and exact \(1\)-dependence if and only if
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).  This is the main record
theorem.  Every renewal, covariance, finite-LU-encoding, inverse, and
fixed-setting long-record CLT statement in the main theorem chain is a consequence of
\(T_0,T_1\) and the associated stationary hidden Markov law.

The theorem chain has four main blocks, listed in
\Cref{tab:four-main-blocks}.  Lemmas, corollaries, and proof devices are
subordinated to one of these blocks.  In particular, the generic cycle-reward
lemmas prove Block~IV.

\subsection{Sampled boundary-reset convention}\label{subsec:model-validity-boundary}

The record theorem chain begins from the following sampled boundary convention;
\Cref{app:fixed-window-comparator} records only a fixed-window continuous
intra-bin comparison.

\begin{assumption}[Killed-leakage sampled-boundary regime]\label{ass:killed-leakage-boundary-reset-main}
The record theorem chain is stated for the sampled boundary-reset kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
Equivalently, a bin that contains a click passes the refractory state \(D\) to
the next sampling boundary:
\[
T_1(R,R)=T_1(D,R)=0 .
\]
The positive post-click return-to-ready leakage present in the continuous
intra-bin comparator is therefore not part of the sampled boundary convention.
Thus every stationary, spectral, Fano-factor, inverse, and CLT theorem below is
about these sampled kernels.  The continuous intra-bin comparator is used only
for the fixed-cylinder estimate isolated in \Cref{app:fixed-window-comparator}.
\end{assumption}

\begin{remark}[No growing-window transfer from the continuous comparator]\label{rem:no-continuous-growing-window-transfer-main}
The fixed-window comparison in \Cref{app:fixed-window-comparator} is not a
perturbation theorem for stationary or long-record quantities.  The main
record theorem chain does not assert that the continuous intra-bin comparator
has the same stationary spectrum, Fano limit, inverse map, exact-dependence
threshold, or fixed-setting long-record CLT as the sampled kernels.  Even a
record-window comparison for \(N\) observed bins would require at least a
vanishing full-window error such as \(N\eta_{x,y}\to0\), where
\(\eta_{x,y}\) is defined in \Cref{prop:finite-window-continuous-comparator-main}.
CLT-scale transfer would require stronger statistic-specific perturbation
control, for example bias and covariance errors small on the \(N^{-1/2}\)
scale for the centered statistic being transported.  No such growing-window
or CLT-scale perturbation theorem is used or proved here.
\end{remark}

\subsection{Four theorem blocks}\label{subsec:four-theorem-blocks}

The four blocks in \Cref{tab:four-main-blocks} are the theorem-level
contributions of the paper.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Four main theorem blocks; supporting lemmas are proof material for
these blocks.}
\label{tab:four-main-blocks}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.31\textwidth} >{\raggedright\arraybackslash}p{0.42\textwidth}}
\toprule
Block & Main result & Role \\
\midrule
I. Killed-leakage sampled-boundary model and gap law &
\Cref{prop:instrument-kernels-main,lem:renewal-gap-main,thm:recovery-main} &
Fixes one killed-leakage sampled-boundary one-recovery-opportunity readout and derives
the renewal gap law, full support, stationary density, increasing hazard, and
covariance tail for the classical sampled HMM. \\
\addlinespace
II. Exact \(1\)-dependence and diagnostics &
\Cref{lem:one-dep-criterion-main,prop:spectrum-main,prop:finite-diagnostics-main} &
Identifies the unique hidden-eigenvalue zero
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\), its sampled-record
covariance and count-noise formulas, and the fixed-setting long-record
diagnostics.  The spectral-density, Fano, and hazard displays are population
formulas for the sampled kernels only; the studentized finite-record theorem
below is only for the lag-two covariance unless separate variance theory is
supplied. \\
\addlinespace
III. Finite LU product determinant and inverse &
\Cref{thm:toeplitz_image_inverse_duality} &
Proves the quotient inverse.  The supporting
\Cref{prop:renewal-principal-minor-lu-main} is a finite LU identity used only
as algebraic bookkeeping for already constructed renewal inclusion products.
The inverse recovers only the unordered attempt/recovery pair. \\
\addlinespace
IV. Killed-leakage sampled-boundary fixed-setting long-record CLT &
\Cref{thm:finite-record-inference-audit-main,thm:sampled-finite-record-main,thm:one-record-estimates-main} &
Collects the stationarity/burn-in, exponential-tail, observed-window,
all-complete-cycle batching, plug-in centering, and rank-adaptive covariance
assumptions, then proves the fixed-setting lag-two covariance CLT and
one-record variance estimator. \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:main-jpa-comparison} gives the foundation-first theorem comparison.

\Cref{sec:operational} and \Cref{sec:records} define the record-level
observables, record the exact-clock Parry benchmark, and prove the four
record-side theorem blocks.  The comparison in
\Cref{sec:theorem-led-comparison} closes the main theorem narrative, and
Table~\ref{tab:notation-global} lists the recurring record-side symbols.

\section{Classical sampled-counter setting and record-level observables}\label{sec:operational}

We first consider only the sampled readout law. Proper time is divided into
intervals of length $\Delta\tau$, a classical counter carries a hidden readiness
state, and the visible output is the stationary two-sided binary process
\[
(A_n)_{n\in\mathbb Z}\in\{0,1\}^{\mathbb Z},
\]
where \(A_n=1\) denotes a click in the \(n\)th sampling interval of the
stationary extension.  All Palm, covariance, renewal, and independence
statements below refer to this two-sided stationary law.  A finite observed
record of length \(N\) is only the restriction
\[
A^{(N)}=(A_0,A_1,\ldots,A_{N-1}),
\]
or, after a deterministic burn-in, a translated restriction of the same
two-sided law.  Compressing such a finite record to the empirical click
frequency recovers only the one-point observable
\[
\widehat\rho_N=\frac{1}{N}\sum_{n=0}^{N-1}A_n.
\]
The full record, however, carries more. In particular, one may ask:
\begin{enumerate}[label=\textup{(\alph*)},leftmargin=2.0em,itemsep=0.25em]
\item what words belong to the support of the stationary law;
\item how the zero-run lengths between clicks are distributed;
\item whether the click hazard after a long run of zeros remembers the run length;
\item how the low-lag covariance sequence behaves;
\item what counting noise remains after coarse time averaging.
\end{enumerate}
\begin{definition}[Record benchmarks]\label{def:record-benchmarks-main}
For a stationary binary record $(A_n)_{n\in\mathbb Z}$, a \emph{zero-run length} is the number $G$ of consecutive zeros following a click and preceding the next click. If the zero-run law has full support, its hazard is
\[
h_k=\PP(G=k\mid G\ge k),\qquad k\ge0 .
\]
The ideal record is on the \emph{Parry benchmark} when its stationary law is the Parry measure on the golden-mean shift, i.e. the unique measure of maximal entropy subject to the grammar constraint $11\notin\operatorname{supp}\mu$. A stationary record is \emph{exact $1$-dependent} when, for every $m$, the sigma algebras generated by $\{A_j:j\le m\}$ and $\{A_j:j\ge m+2\}$ are independent.
Equivalently, one leaves the intermediate sample at time $m+1$ unobserved: a block ending at time $m$ is tested for independence from a block beginning at time $m+2$, not conditioned on the middle sample.
\end{definition}

These observables are not themselves rate observables, yet in the explicit
readout models below they reduce to sharp thresholds in the internal counter
rate $\Gamma$.

It is useful to keep the record construction in two logically separate steps.
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=2.2em,itemsep=0.2em]
\item A counter model determines a stationary word law and a record-level benchmark: the Parry law in the ideal case, exact $1$-dependence in the sampled boundary-reset case.
\item For the two models studied here, that benchmark is first equivalent to a scalar internal counter-rate condition, respectively $\Gamma\Delta\tau=\log\varphi$ and $\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$.  The memory condition is labelled only after the recovery rate has been independently identified: the inverse theorem gives
\[
\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}
\quad\text{and hence only}\quad
\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}.
\]
Without an external label the memory statement is made on the quotient by
$(\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)$.
\end{enumerate}
The two counter readout regimes studied in this work have intrinsically
different record thresholds. The ideal model imposes a hard symbolic grammar, so its
distinguished value is the entropy benchmark on the golden-mean shift. The
sampled one-recovery-opportunity boundary-reset model instead produces a
full-support renewal law, so its distinguished value is the exact
\(1\)-dependence threshold where the hidden eigenmode vanishes.

\begin{table}[H]
\centering
\small
\caption{Record-level observables that distinguish the two counter readout regimes.}
\label{tab:signatures}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.21\textwidth}}
\toprule
Regime & Output law & Distinguished threshold & Finite-record signature \\
\midrule
Ideal dead time & Markov chain on the golden-mean shift; $11$ forbidden & $\Gamma_+\Delta\tau=\log\varphi$ & No consecutive clicks; Parry law at threshold \\
Sampled one-recovery-opportunity boundary reset & Renewal process with infinite symbolic memory; zero-run law supported on all $k\ge0$, hence every finite binary word is allowed; renewal LU product determinant for the click-set inclusion products & With independently labelled \(\kappa_{\mathrm r}\), $\Gamma_+=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$; otherwise only the quotient under \((\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)\) is identified & Exact finite-window count formula at threshold; covariance and Fano formulas for this sampled record law \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{3pt}
\caption{Record-side notation used throughout the sampled-counter calculation.
Response-side couplings, spectra, and level-set notation are introduced locally
in \Cref{sec:kms-shells}.}
\label{tab:notation-global}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.17\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} >{\raggedright\arraybackslash}p{0.55\textwidth}}
\toprule
Layer & Symbols & Meaning and scope \\
\midrule
Sampled-model parameters &
\(\Delta\tau,\ \Gamma,\ \kappa_{\mathrm r}\) &
Sampling time, classical attempt rate, and recovery rate of the stipulated
sampled counter.  Bare \(\Gamma\) is a record-side parameter in all main-text
theorems. \\
\addlinespace[0.25ex]
Sampled-model local quantities &
\(p,\ q_0,\ s,\ a,\ b\) &
One-bin quantities local to the hidden Markov model:
\(p=e^{-\Gamma\Delta\tau}\), \(q_0=1-p\),
\(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), \(a,b\) are the
one-recovery-opportunity click/no-click probabilities.  The hidden transfer
eigenvalue is defined only where it is used in \Cref{sec:records}. \\
\addlinespace[0.25ex]
Covariance and spectral quantities &
\(\lambda_{\mathrm{hid}},\ \gamma_n,\ \gamma_2,\ f(\theta),\ F\) &
\(\lambda_{\mathrm{hid}}\) is the nontrivial hidden transfer eigenvalue.
\(\gamma_n=\operatorname{Cov}(A_0,A_n)\), in particular
\(\gamma_2=\operatorname{Cov}(A_0,A_2)\), \(f(\theta)\) is the stationary
binary-record spectral density, and \(F=f(0)/\rho\) is the asymptotic Fano
factor. \\
\addlinespace[0.25ex]
Palm-renewal quantities &
\(\varXi,\ \rho,\ u_n,\ g_k,\ S_k,\ h_k\) &
Click set, stationary click density, scalar Palm renewal mass
\(u_n=\PP(n\in\varXi\mid0\in\varXi)\), zero-run probability, zero-run survival
tail, and zero-run hazard. \\
\addlinespace[0.25ex]
Finite LU product determinant &
\(M_u(B),\ D_u(B),\ \delta_a\) &
Set-indexed finite matrices and determinants that represent renewal inclusion products after
those probabilities have already been constructed by renewal conditioning and
finite inclusion--exclusion.  \(M_u(B)\) is the general renewal LU matrix for a
finite ordered set \(B\), \(D_u(B)=\det M_u(B)\), its boundary-reset
specialization is denoted \(D_\Gamma(B)\), and \(\delta_a=\rho-a\).
Critical renewal LU witnesses are introduced locally at the critical theorem.
Their non-DPP boundary is fixed in
\Cref{subsec:scope-nonclaims,rem:finite-determinant-language-main}. \\
\addlinespace[0.25ex]
Three-inclusion inverse &
\(\begin{gathered}r_0,\ r_1,\ r_2,\ D,\ \Phi\\
J_\Phi,\ J_{\mathrm{root}},\ J_{\Gamma,\kappa}\\
\Sigma_r,\ \Sigma_{\sigma},\ \Sigma_{\Gamma,\kappa}\end{gathered}\) &
\(r_m=\PP(A_0=A_m=1)\) for \(m=0,1,2\), with \(r_0=\rho\).
The inverse-map denominator is \(D=r_1-r_0^2=r_0(a-r_0)\).  The map
\(\Phi\) sends the three inclusion coordinates to the symmetric survival
coordinate vector \(\sigma_{\rm sym}=(p+s,ps)\).  At separated roots, \(J_{\mathrm{root}}\) is the
chosen labelled-root Jacobian, \(J_{\Gamma,\kappa}\) is the inclusion-to-rate
Jacobian, and \(\Sigma_r,\Sigma_{\sigma},\Sigma_{\Gamma,\kappa}\) are the CLT
covariances before the inverse map, after \(\Phi\), and after the labelled-rate
map. \\
\addlinespace[0.25ex]
Finite-record covariance notation &
\(\begin{gathered}
d_{\rm rew},\ q_{\rm lag},\ Y_j\\
\mathcal T_{q_{\rm lag}},\ \Sigma\\
\sigma_{\gamma_2}^2,\ \widehat\Sigma_N^{\mathrm{all}},\ t_N\end{gathered}\) &
Inside the finite-record theorems only, \(d_{\rm rew}\) is the reward-vector
dimension and \(q_{\rm lag}\) is the cycle-reward dependence/look-ahead range.
The long-run cycle-reward covariance is \(\mathcal T_{q_{\rm lag}}\), the
calendar-time covariance is \(\Sigma=\mathcal T_{q_{\rm lag}}/\mu\), and
\(\sigma_{\gamma_2}^2\) is the explicit positive calendar-time variance of
the lag-two covariance statistic in
\Cref{lem:lag-two-variance-nondegenerate-main}.  The matrix
\(\widehat\Sigma_N^{\mathrm{all}}\) is the
all-complete-cycle separated-batch covariance estimator, and \(t_N\) is the
deterministic rank threshold. \\
\bottomrule
\end{tabular}
\end{table}

\begin{theorem}[Main killed-leakage sampled-boundary record theorem]\label{thm:main-record-wrapper}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and define
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,
\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
Let
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
These are the killed-leakage labelled kernels: a click sends the next
boundary state to \(D\), so \(T_1(R,R)=T_1(D,R)=0\).  The row sum
\(P=T_0+T_1\) is stochastic, \(0<p,s<1\), \(a>0\), \(b>0\), and
\(a+b=1-s\).
Let \((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process
generated by these labelled kernels, in the convention
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\).  Define
\[
\rho=\PP(A_0=1),\qquad
\lambda_{\rm hid}=p-b,
\]
with every expression containing \((\kappa_{\mathrm r}-\Gamma)^{-1}\)
interpreted by its continuous equal-rate limit, in particular
\(\lambda_{\rm hid}=e^{-y}(1-y)\) when
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\).  Then \((A_n)\) is a
stationary full-support renewal process.  Its zero-run law is
\[
g_k=e_D^\top T_0^kT_1\mathbf1,\qquad k\ge0,
\]
equivalently, for \(\kappa_{\mathrm r}\ne\Gamma\),
\[
g_k=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma},
\]
with the continuous equal-rate limit.  Its zero-run hazard is strictly
increasing and is given by \eqref{eq:hazard-main}, with the equal-rate
convention \eqref{eq:equal-rate-survival-hazard-main}.  Its covariance is
\[
\operatorname{Cov}(A_0,A_n)
=\rho(a-\rho)\lambda_{\rm hid}^{\,n-1},
\qquad n\ge1,
\]
and the visible record is exact \(1\)-dependent iff
\(\lambda_{\rm hid}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) under the
non-removable equal-rate convention of
\eqref{eq:gamma-crit-piecewise-main}.  In all parameter cases the visible
record is not a finite-order Markov chain.
\end{theorem}

\input{sec_records}

\section{Discussion}\label{sec:discussion}

The killed-leakage sampled-boundary theorem is the main mathematical result:
one classical sampled counter law produces a full-support renewal record with
strictly increasing hazard, suppressed adjacent-click conditional probability,
classical sub-Poisson count noise, a unique exact \(1\)-dependence threshold at
the zero of the hidden eigenmode, a Toeplitz renewal LU product determinant
encoding, a closed Fano law, and a three-inclusion inverse with
\(\Gamma\leftrightarrow\kappa_{\mathrm r}\) exchange duality. The ideal Parry
point remains the benchmark comparison rather than the main theorem.
All items in this list are sampled-record statements.  The appendix
continuous-recovery comparator is confined to fixed finite cylinders and does
not supply a stationary or asymptotic-transfer interpretation for these
formulas.

The theorem-family comparison in \Cref{tab:main-jpa-comparison} records how
this sampled-counter theorem chain differs from HMM record laws,
one-dependent determinant formulas, regenerative CLTs, trajectory models,
and memory-inference results.

\section{Theorem-by-Theorem Comparison}\label{sec:theorem-led-comparison}

\Cref{tab:main-jpa-comparison} is a narrow theorem-by-theorem comparison.  Each
row names one submitted result, one closest comparator, and the exact increment
supplied here.  It is not a foundations survey: broad Markov renewal,
phase-type, D-MAP, and hidden-Markov representation theory is used below only
to delimit identifiability claims.  The determinant and CLT rows are
deliberately modest: the LU determinant is a renewal inclusion identity, and
the renewal CLT ingredients are standard once the sampled record reward and
batching construction have been reduced to a regenerative problem.

\begingroup
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.08}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.25\textwidth} >{\raggedright\arraybackslash}p{0.26\textwidth}}
\caption{Theorem-by-theorem comparison with closest direct comparators.}
\label{tab:main-jpa-comparison}\\
\toprule
Submitted theorem/result & Direct comparator & Comparator content & Exact increment here \\
\midrule
\endfirsthead
\caption[]{Theorem-by-theorem comparison with closest direct comparators (continued).}\\
\toprule
Submitted theorem/result & Direct comparator & Comparator content & Exact increment here \\
\midrule
\endhead
\bottomrule
\endfoot
Main sampled-record law:
\Cref{thm:recovery-main}. &
Vollmar--Bebon--Schimpf--Flietel--Celiksoy--S\"onnichsen--Godec--Hugel
\cite{VollmarEtAl2024Memory}. &
This comparator gives a model-free memory-inference method for stochastic
trajectories.  It does not start from the two labelled kernels \((T_0,T_1)\)
used here or compute this record's gap, hazard, and covariance laws. &
For the killed-leakage sampled-boundary kernels, the submitted theorem derives
the stationary full-support renewal law, the closed zero-run probabilities,
strictly increasing hazard, stationary density, and one-mode covariance tail
\(\rho(a-\rho)\lambda_{\rm hid}^{n-1}\) in the same parameterization. \\
\addlinespace
Exact \(1\)-dependence threshold:
\Cref{lem:one-dep-criterion-main}. &
Pitman--You stationary \(1\)-dependent counting processes
\cite{PitmanYou2021}. &
This comparator analyzes stationary \(1\)-dependent counting processes through
runs and bivariate generating functions.  It does not identify a
one-dependence threshold inside a sampled killed-leakage D-MAP(2) subfamily. &
The submitted result proves that the full-support sampled renewal output is
exact \(1\)-dependent if and only if the hidden eigenvalue
\(\lambda_{\rm hid}=p-b\) is zero, and converts that algebraic condition into
the explicit rate threshold
\(\Gamma_{\rm crit}(\kappa_{\rm r},\Delta\tau)\). \\
\addlinespace
Spectral density and Fano law:
\Cref{prop:spectrum-main}. &
Kirchhoff--Hayman--Marais--Thayer--Barton-Grimley photon-counting deadtime
noise model \cite{KirchhoffEtAl2025Deadtime}. &
This comparator derives a deadtime noise model for atmospheric lidar photon
counting, with an observation scheme and device assumptions different from the
sampled-boundary kernels. &
For the stipulated sampled binary renewal record, the submitted proposition
computes the exact autocovariance series, spectral density, and asymptotic
count Fano factor, including the sub-Poisson expression inherited from the
renewal cycle variance. \\
\addlinespace
Renewal LU product determinant:
\Cref{cor:boundary_reset_toeplitz_renewal_specialization}. &
Poinas nonsymmetric DPP existence theorem \cite{Poinas2026NonsymmetricDPP}. &
This comparator concerns existence of determinantal point processes with
nonsymmetric kernels.  Its hypotheses are DPP kernel hypotheses, not renewal
conditioning hypotheses. &
The submitted determinant is only the finite LU product identity for renewal
inclusion probabilities already obtained by conditioning:
\(\rho\prod_r u_{i_{r+1}-i_r}\).  The increment is the explicit Toeplitz
specialization for the sampled renewal record and finite word probabilities by
inclusion--exclusion, with no DPP admissibility or sampling assertion. \\
\addlinespace
Visible-law quotient inverse:
\Cref{thm:toeplitz_image_inverse_duality}. &
Ram\'irez-Cobo--Lillo--Wiper two-state MAP nonidentifiability
\cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability}. &
This comparator proves nonidentifiability phenomena for two-state Markovian
arrival processes.  It does not give this sampled killed-leakage subfamily's
three-inclusion quotient inverse. &
The submitted theorem proves only the constrained-subfamily statement:
\((r_0,r_1,r_2)\) recover \((p+s,ps)\), hence the unordered pair
\(\{p,s\}\), while the hidden labels
\(\Gamma\) and \(\kappa_{\rm r}\) remain exchange-nonidentifiable. \\
\addlinespace
Finite-record inverse and covariance audit:
\Cref{thm:finite-record-inference-audit-main}. &
Singh--Shukla--Vats equal-batch inference for stochastic-gradient output
\cite{SinghShuklaVats2025EqualBatch}. &
This comparator studies the utility of equal batch sizes for inference in
stochastic-gradient output.  It is not a covariance theorem for a
calendar-stopped sampled renewal record. &
The submitted theorem builds the estimator for one binary sampled renewal
record: all-complete-cycle batches, \(q_{\rm lag}\)-separation, a
calendar-stopped random prefix rate, plug-in centering, rank-threshold
covariance estimation, separated-root delta charts, and projected Gaussian
inference at the exchange diagonal. \\
\addlinespace
Fixed-setting lag-two CLT:
\Cref{thm:sampled-finite-record-main}. &
Janson renewal central-limit theorem \cite{Janson2024RenewalCLT}. &
This comparator supplies general renewal CLT behavior; it does not identify
the lag-two click covariance reward or its variance for the sampled
killed-leakage law. &
The submitted theorem defines the exact lag-two cycle reward, proves the
finite-lag regenerative CLT for the fixed sampled setting, converts
cycle-index time to record length, and gives the explicit nondegenerate
variance certificate used by the lag-two threshold diagnostic. \\
\addlinespace
One-record estimability statement:
\Cref{thm:one-record-estimates-main}. &
Alexandrovich--Holzmann--Leister hidden-Markov identification and likelihood
\cite{AlexandrovichHolzmannLeister2016}. &
This comparator gives hidden-Markov identification and likelihood theory under
its own nonparametric HMM assumptions.  It does not address the
attempt/recovery exchange quotient of the sampled renewal law here. &
The submitted theorem states the exact scope for one long sampled binary
record in this model: population renewal coordinates, the unordered
survival-factor pair, and the lag-two threshold diagnostic are estimable under
the stated finite-record hypotheses; the hidden attempt/recovery label is not. \\
\end{longtable}
\endgroup

\paragraph{Classical representation boundary.}
The older Markov renewal and semi-Markov foundations
\cite{Pyke1961MarkovRenewal,Cinlar1969MarkovRenewal}, phase-type
representation and non-uniqueness results
\cite{Neuts1981MatrixGeometric,OCinneide1989PHNonuniqueness,
CommaultMocanu2003PhaseTypeRepresentations}, MAP/D-MAP representation
references \cite{Neuts1979MAP,Lucantoni1991BMAP,AlfaNeuts1995DMAP,
BodrogHeindlHorvathTelek2008MAP2Canonical,
RamirezCoboLilloWiper2010MAP2Nonidentifiability,
RamirezCoboLillo2012WeakEquivalentMAP}, and functions of finite Markov chains
\cite{BlackwellKoopmans1957,Gilbert1959,Dharmadhikari1963Functions,
BurkeRosenblatt1958MarkovianFunction,KemenySnell1960FiniteMarkovChains}
are the representation boundary for the claims above.  They explain why the
paper does not assert generic hidden-state identifiability.  The inverse result
is only the quotient inverse for the killed-leakage sampled-counter subfamily:
the visible renewal coordinates recover \((p+s,ps)\) and hence the unordered
sampled rates away from the stated denominator and separated-root boundaries.

\paragraph{Appendix fixed-window comparator.}
It compares the sampled killed-leakage kernels with a continuous intra-bin
hidden-recovery comparator only on a fixed finite cylinder, with total-variation
error at most \(m\eta_{x,y}\).  For a full record of length \(N\), this bound
even gives cylinder-level convergence only under an additional scaling such as
\(N\eta_{x,y}\to0\).  It does not transfer stationary laws, spectra, Fano
limits, inverse maps, or CLTs.

\appendix

\section{Fixed-Window Comparator for Killed-Leakage Sampling}\label{app:fixed-window-comparator}

This appendix records the fixed-cylinder comparison between the sampled
boundary-reset kernels and a continuous intra-bin hidden recovery model.  It is
not an input to the stationary, spectral, Fano-factor, inverse, or CLT
theorems; those theorems use the killed-leakage sampled-boundary kernels from
\Cref{ass:killed-leakage-boundary-reset-main}.

\begin{proposition}[Fixed-window comparison of killed-leakage sampled-boundary and continuous intra-bin recovery]\label{prop:model-validity-boundary-main}\label{prop:continuous-comparator-main}\label{prop:short-bin-boundary-reset-main}\label{prop:finite-window-continuous-comparator-main}
This appendix proposition is the precise fixed-cylinder comparison between the
killed-leakage sampled kernels and the continuous intra-bin comparator: the
dimensionless bin parameters
\(x=\Gamma\Delta\tau\) and \(y=\kappa_{\mathrm r}\Delta\tau\) are small, and
the comparison is only for a fixed finite output window.  The conclusion below
is a cylinder-law estimate; it is not a stationary-law, spectral, or
infinite-record approximation theorem, nor a Fano-factor or CLT transfer.
Put
\[
x=\Gamma\Delta\tau,\qquad y=\kappa_{\mathrm r}\Delta\tau,\qquad
p=e^{-x},\qquad s=e^{-y},
\]
and
\[
b=\int_0^1 y e^{-yt}e^{-x(1-t)}\,dt,\qquad
a=\int_0^1 y e^{-yt}\bigl(1-e^{-x(1-t)}\bigr)\,dt .
\]
Let \(T_0,T_1\) be the sampled boundary-reset kernels in
\Cref{ass:killed-leakage-boundary-reset-main}.  Let
\(\widetilde T_0,\widetilde T_1\) be the comparator kernels obtained by
letting the hidden state evolve continuously during a bin as
\[
R \xrightarrow{\ \Gamma\ } D,\qquad
D \xrightarrow{\ \kappa_{\mathrm r}\ } R,
\]
while the visible ancilla still records only whether at least one click occurred
before the boundary.  Then
\[
\widetilde T_0=T_0,\qquad
\widetilde T_1-T_1=
\begin{pmatrix}
\delta_R&-\delta_R\\
\delta_D&-\delta_D
\end{pmatrix},
\]
where
\[
\delta_R=
\frac{y(1-e^{-x})-x e^{-x}(1-e^{-y})}{x+y},
\qquad
\delta_D=
\frac{y}{x+y}\bigl(1-e^{-(x+y)}\bigr)-b.
\]
Moreover \(\delta_R>0\) and \(\delta_D>0\), whereas the sampled kernels set the
same two leakage entries to zero.

Let
\[
\eta_{x,y}:=\max\{\delta_R,\delta_D\}.
\]
For every initial hidden law \(\alpha\) and every word length \(m\ge1\), let
\(\mathsf P_{\alpha}^{(m)}\) and
\(\widetilde{\mathsf P}_{\alpha}^{(m)}\) be the two output laws on
\(\{0,1\}^m\).  Then
\begin{equation}\label{eq:finite-window-tv-comparator-main}
\bigl\|\mathsf P_{\alpha}^{(m)}
-\widetilde{\mathsf P}_{\alpha}^{(m)}\bigr\|_{\mathrm{TV}}
\le m\,\eta_{x,y}.
\end{equation}
Consequently, for every bounded function
\(F:\{0,1\}^m\to\mathbb R\),
\begin{equation}\label{eq:finite-window-test-function-comparator-main}
\left|
\EE_{\rm sampled}F(A_0,\ldots,A_{m-1})
-
\EE_{\rm cont}F(A_0,\ldots,A_{m-1})
\right|
\le
2\|F\|_\infty\,m\,\eta_{x,y}.
\end{equation}
Every fixed cylinder probability differs by at most \(m\eta_{x,y}\).
In the dimensionless small-bin regime \(x=\Gamma\Delta\tau\to0\) and
\(y=\kappa_{\mathrm r}\Delta\tau\to0\), with \(m\) fixed,
\[
\delta_R=\frac12xy+O((x+y)^3),
\qquad
\delta_D=\frac16xy^2+O((x+y)^4),
\]
where the constants in the two \(O\)-terms are uniform for
\(0\le x,y\le\varepsilon\), for each sufficiently small fixed
\(\varepsilon>0\).
Consequently,
\begin{equation}\label{eq:finite-window-tv-shortbin-main}
\bigl\|\mathsf P_{\alpha}^{(m)}
-\widetilde{\mathsf P}_{\alpha}^{(m)}\bigr\|_{\mathrm{TV}}
\le
\frac12mxy+O_m((x+y)^3).
\end{equation}
\end{proposition}

\begin{proof}
The no-click event occurs before any reset can matter, so the comparator and
the sampled model have the same output-\(0\) kernel.  The continuous hidden
boundary transition matrix is
\[
e^{Q\Delta\tau}
=\frac1{x+y}
\begin{pmatrix}
y+x e^{-(x+y)}&x(1-e^{-(x+y)})\\
y(1-e^{-(x+y)})&x+y e^{-(x+y)}
\end{pmatrix},
\]
where \(Q\) is the two-state generator with rates
\(\Gamma,\kappa_{\mathrm r}\).  Subtracting \(T_0\) gives
\(\widetilde T_1\), and subtracting the sampled \(T_1\) gives the displayed
rank-one correction.  The entries \(\delta_R,\delta_D\) are strictly positive:
from \(R\), there is positive probability of a click followed by recovery
before the boundary; from \(D\), there is positive probability of recovery,
click, and a second recovery before the boundary.

For a fixed current hidden state, the one-step total variation distance between
the two labelled laws on \(\{0,1\}\times\{R,D\}\) is at most
\(\eta_{x,y}\).  Coupling the two chains step-by-step by maximal one-step
couplings, the visible words can differ during the first \(m\) bins only if at
least one of the \(m\) labelled transitions disagrees.  The union bound gives
\eqref{eq:finite-window-tv-comparator-main}.  The test-function bound
\eqref{eq:finite-window-test-function-comparator-main} is the standard
total-variation dual inequality, and the cylinder bound follows by applying
\eqref{eq:finite-window-tv-comparator-main} to a cylinder indicator.  Taylor
expansion of the displayed dimensionless formulas gives the two leakage
expansions.  The quotient singularities at \(x+y=0\) are removable: for
example, \((1-e^{-(x+y)})/(x+y)=\int_0^1 e^{-u(x+y)}\,du\), and the remaining
terms are finite linear combinations of entire exponential functions and
one-dimensional exponential integrals.  Taylor's theorem on the compact square
\([0,\varepsilon]^2\) therefore gives the stated remainders uniformly for
\(0\le x,y\le\varepsilon\).  The displayed short-bin total-variation estimate
then follows from \eqref{eq:finite-window-tv-comparator-main}.
\end{proof}

\begin{corollary}[Fixed-window-only comparator scope and missing growing-window hypotheses]\label{cor:finite-window-only-comparator-main}
\Cref{prop:finite-window-continuous-comparator-main} has only the following
content.  For each fixed \(m\), fixed initial hidden law \(\alpha\), and bounded
test function on \(\{0,1\}^m\), the sampled boundary-reset output law and the
continuous intra-bin comparator output law differ by the total-variation and
expectation bounds
\[
\bigl\|\mathsf P_{\alpha}^{(m)}
-\widetilde{\mathsf P}_{\alpha}^{(m)}\bigr\|_{\mathrm{TV}}
\le m\eta_{x,y},
\qquad
\left|\EE_{\rm sampled}F-\EE_{\rm cont}F\right|
\le2\|F\|_\infty m\eta_{x,y}.
\]
Consequently every use of the proposition is confined to fixed cylinders and
fixed-window bounded observables; the later stationary, spectral, Fano-factor,
and CLT statements remain statements about the sampled kernels \(T_0,T_1\).

For a growing record window \(m_N\to\infty\), the same bound would imply
cylinder-level convergence only under the additional scaling condition
\[
m_N\eta_{x,y}\to0.
\]
For a full record of length \(N\), this reads \(N\eta_{x,y}\to0\).
This is only a law-level requirement for bounded functions of the whole
observed word.  It is not by itself a stationary, spectral, inverse, Fano-limit,
or CLT perturbation theorem.

For example, a CLT-scale transfer for a centered statistic
\[
S_N=N^{-1/2}\sum_{n=0}^{N-1}\psi(A_{n},\ldots,A_{n+\ell})
\]
would require, in addition to a valid growing-window coupling or an equivalent
operator perturbation argument, statistic-specific bounds such as
\[
\sqrt N\,\left|
\EE_{\rm sampled}\psi(A_0,\ldots,A_\ell)
-
\EE_{\rm cont}\psi(A_0,\ldots,A_\ell)
\right|\to0
\]
and convergence of the corresponding long-run covariance series on the scale
used by the CLT.  In a crude total-variation route, the window-level coupling
error would have to be \(o(N^{-1/2})\) after the statistic is centered, a
condition stronger than \(N\eta_{x,y}\to0\); under
\eqref{eq:finite-window-tv-comparator-main} for \(m=N\), this would read
\[
N\eta_{x,y}=o(N^{-1/2}),
\qquad\text{equivalently}\qquad
N^{3/2}\eta_{x,y}\to0.
\]
No such conditions are assumed in this paper and no continuous-comparator CLT,
Fano-limit, spectral-density limit, or inverse-law transfer is concluded.
\end{corollary}

\begin{proof}
The first display repeats the bounds in
\Cref{prop:finite-window-continuous-comparator-main}.  If \(m\) is fixed, the
right-hand side tends to zero in the small-bin regime.  If \(m=m_N\) grows, the
same bound is informative only under \(m_N\eta_{x,y}\to0\), and for \(m=N\)
this gives \(N\eta_{x,y}\to0\).  Applying the same window-level bound to a
\(\sqrt N\)-scaled centered statistic would multiply the window-law error by
\(\sqrt N\), yielding the displayed sufficient scaling
\(N^{3/2}\eta_{x,y}\to0\) for that crude route.  The covariance-series
condition is a separate requirement because a CLT limit is determined by
long-run second-order cumulants, not by a fixed-cylinder comparison alone.
The remaining claims follow because stationary laws, spectral densities, Fano
limits, inverse maps, and CLTs depend on limits in the window length, record
length, or parameter identification problem, not on a fixed cylinder
functional.
\end{proof}

\input{sec_response_short}

\section*{Funding}

No specific funding was received for this work.

\section*{Data availability}

No new data were created or analysed in this study. The article is
theoretical, and all calculations needed to reproduce the reported results are
contained in the submitted manuscript sources.  The record-side hazard,
covariance, spectrum, and Fano-factor plots are kept in the supplementary
material as deterministic formula sanity checks.  They are not simulations,
fits, validation data, or evidence for the theorem statements.  The displayed
formulas give the paper-local recipe for solving
$\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$ and regenerating those supplementary
checks.

\clearpage
\input{references.tex}

\end{document}
