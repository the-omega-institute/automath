
\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{float}
\usepackage{graphicx}
\graphicspath{{generated/}}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\crefname{theorem}{theorem}{theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{lemma}{lemma}{lemmas}
\Crefname{lemma}{Lemma}{Lemmas}
\crefname{proposition}{proposition}{propositions}
\Crefname{proposition}{Proposition}{Propositions}
\crefname{corollary}{corollary}{corollaries}
\Crefname{corollary}{Corollary}{Corollaries}
\crefname{assumption}{assumption}{assumptions}
\Crefname{assumption}{Assumption}{Assumptions}
\crefname{definition}{definition}{definitions}
\Crefname{definition}{Definition}{Definitions}
\crefname{remark}{remark}{remarks}
\Crefname{remark}{Remark}{Remarks}
\crefname{importedinput}{imported input}{imported inputs}
\Crefname{importedinput}{Imported input}{Imported inputs}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage[mathlines,switch]{lineno}
\IfFileExists{orcidlink.sty}{\usepackage{orcidlink}}{}

\hypersetup{colorlinks=true,citecolor=blue!60!black,linkcolor=blue!60!black,urlcolor=blue!60!black}
\setstretch{1.07}
\setlength{\parskip}{0.38em}
\setlength{\parindent}{1.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem*{unnumberedlemma}{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{importedinput}[theorem]{Imported input}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\RR}{\mathbb{R}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Var}{\mathrm{Var}}

\title{Exact record laws for a two-state sampled renewal HMM: dependence, quotient inverse, and fixed-setting long-record inference}
\author{Haobo Ma\thanks{CHRONOAI PTE. LTD., Singapore. Email: \href{mailto:auric@aelf.io}{auric@aelf.io}.}
\and
Wenlin Zhang\thanks{National University of Singapore, Singapore. Email: \href{mailto:e1327962@u.nus.edu}{e1327962@u.nus.edu}.}}
\date{}

\begin{document}
\modulolinenumbers[1]
\linenumbers
\maketitle

\begin{abstract}
This paper proves exact record laws for a stipulated classical two-state
sampled renewal HMM.  Time is sampled in intervals of length \(\Delta\tau\),
the hidden state is ready or recovering, the sampled post-event boundary
convention sets the return-to-ready entries of the event kernel to zero, and
the visible record is a stationary binary sequence.  The paper does not derive
this killed-leakage kernel pair from a quantum detector, KMS state,
thermodynamic model, or continuous-time measurement dynamics; those physical
inputs, if desired, enter only through an optional externally supplied scalar
calibration of the record-side rate.  The ideal one-step blanking model is
retained only as the standard Parry benchmark
\(\Gamma\Delta\tau=\log\varphi\).  The main killed-leakage sampled-boundary
theorem treats the one-recovery-opportunity HMM with attempt rate \(\Gamma\)
and recovery rate \(\kappa_{\mathrm r}\): its zero-run law has full support on
\(\mathbb N_0\), its zero-run hazard is strictly increasing, and its
binary-record covariance has the hidden-eigenvalue tail
\(\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{n-1}\).
The visible record is exact \(1\)-dependent exactly at the unique rate
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) where
\(\lambda_{\mathrm{hid}}=0\).

A stationary-renewal lemma first constructs click-set probabilities from
renewal products and finite inclusion--exclusion.  After those probabilities
are known, the same finite algebra gives a finite LU product identity for
that product; the boundary-reset law specializes the inclusion
product to \(\rho\prod_r u_{i_{r+1}-i_r}\).  The same renewal record law has
a quotient inverse: on the inverse domain \(D=r_1-r_0^2\ne0\), three inclusion
probabilities recover the symmetric coordinate vector
\(\sigma_{\rm sym}=(\sigma_1,\sigma_2)=(p+s,ps)\), and hence the unordered
survival-factor pair
\(\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}\).  The
discriminant
\(\Delta_\sigma=\sigma_1^2-4\sigma_2=(p-s)^2\) controls labelled recovery:
when \(\Delta_\sigma>0\), labelled roots and labelled rates are available only
after an external branch label chooses which root is the attempt survival
factor and which is the recovery survival factor; when
\(\Delta_\sigma=0\), the labelled-rate chart is singular.  Thus
\(\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}\), and one
visible sampled record identifies only the exchange quotient.  The finite-record
part proves a fixed-setting lag-two covariance CLT and a complete-cycle
variance estimator for \(\gamma_2\) in this sampled HMM law.
\end{abstract}

\noindent\textbf{Keywords:} classical sampled HMM, renewal process, exact \(1\)-dependence, renewal inclusion products, finite-record diagnostics

\section{Introduction}

This paper is about the record law of a two-state sampled renewal HMM.  The
model has an internal attempt rate \(\Gamma\), one hidden readiness bit, and a
binary output recording whether the sampling interval contains an event.  Its
visible stationary process has grammar, waiting-time, dependence, covariance,
and counting-noise observables that are invisible in a rate-only summary.  The
paper-level object is therefore the sampled record itself: one stipulated
interval law, its stationary binary distribution, its dependence threshold, its
renewal inclusion products, its quotient inverse, and its fixed-setting
long-record lag-two covariance estimator.

The scalar rate \(\Gamma\) is an internal parameter of the sampled counter.
All main-text claims below concern the induced binary record law.  The
continuous intra-bin recovery model mentioned later is only a fixed-window
comparator: it gives a total-variation bound on any preassigned finite
cylinder after the sampled kernels have been specified.  The external scalar
calibration at the end of the article is likewise only a composition
\(\Gamma=R_\Omega(x)\) with a supplied positive \(C^1\) function.  No
stationary-law, spectral, inverse, Fano-factor, or central-limit transfer from
the sampled record to a continuous comparator is proved or used, and no
quantum-detector, KMS, or thermodynamic theorem is needed for the record-law
results.

The main text below is organized around three paper-level main theorems:
the record law, the quotient inverse, and fixed-record inference.  Supporting
lemmas, propositions, and corollaries supply the renewal bookkeeping, generic
cycle-reward CLTs, separated-batch covariance estimates, and projected-Gaussian
boundary calculations needed by those three results.  The theorem-by-theorem
comparison in \Cref{sec:theorem-led-comparison} then separates these
contributions from standard HMM, renewal, and simulation-output theorems.  The
words event, click, recovery, reset, and count are names for variables and
transitions in this stipulated binary record model.  They do not add any
device-level assumptions beyond the sampled kernels displayed below.

\begin{remark}[Classical-kernel contract]\label{rem:classical-kernel-contract-main}
The mathematical object of the paper is the stationary visible law generated
by the displayed labelled kernels.  Consequently the record, inverse, and
finite-record inference theorems are classical sampled-HMM and renewal
theorems.  The paper intentionally takes the killed-leakage entries
\(T_1(R,R)=T_1(D,R)=0\) as part of the sampled boundary convention rather than
as an approximation derived from a detector-field Hamiltonian.  The optional
calibration corollary in \Cref{sec:kms-shells} only maps the two
record-selected constants through a prescribed scalar response profile and has
no converse: it neither constructs \(T_0,T_1\) nor validates the boundary
convention.
\end{remark}

\subsection{Main two-state sampled-renewal HMM theorem}\label{subsec:main-theorem-boundary}\label{subsec:scope-nonclaims}

For fixed
\((\Gamma,\kappa_{\mathrm r},\Delta\tau)\in(0,\infty)^3\) and the sampled
boundary-reset kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix},
\]
the visible click process is a stationary renewal process with gap law \(g_k\),
increasing hazard \(h_k\), covariance
\[
\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad n\ge1,
\]
and exact \(1\)-dependence if and only if
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).  This is
Main Theorem~1 and the sole theorem-level statement whose conclusion is the
stationary record law itself.  Main Theorem~2 is the quotient inverse from
visible inclusion probabilities to the exchange quotient
\((p+s,ps)\).  Main Theorem~3 is the fixed-record inference theorem for
complete-cycle inclusion statistics and their covariance scale.  The remaining
renewal, covariance, finite LU product identity, inverse-delta, batching,
projection, and lag-two interval statements are supporting lemmas,
propositions, or corollaries of \(T_0,T_1\) and the associated stationary
hidden Markov law.

The three main theorems and their supporting result groups are listed in
\Cref{tab:four-main-blocks}.  Lemmas, propositions, corollaries, and proof
devices are subordinated to one of these groups.  In particular, the generic
cycle-reward and separated-batch lemmas support the fixed-record inference
theorem rather than forming additional paper-level theorems.

\subsection{Sampled boundary-reset convention}\label{subsec:model-validity-boundary}

The record analysis begins from the following sampled boundary convention.

\begin{assumption}[Killed-leakage sampled-boundary regime]\label{ass:killed-leakage-boundary-reset-main}
The record results are stated for the sampled boundary-reset kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
Equivalently, a bin that contains a click passes the refractory state \(D\) to
the next sampling boundary:
\[
T_1(R,R)=T_1(D,R)=0 .
\]
Thus every stationary, spectral, Fano-factor, inverse, and finite-record
inference claim below is about these sampled kernels.  The continuous
intra-bin comparison in \Cref{app:fixed-window-comparator} is not part of this
assumption and supplies no long-record perturbation theorem.
\end{assumption}

\subsection{Three main theorems and supporting result groups}\label{subsec:four-theorem-blocks}

\Cref{tab:four-main-blocks} separates the three paper-level main theorems from
the supporting propositions, corollaries, and lemmas used to prove or apply
them.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Three main theorems and supporting result groups.}
\label{tab:four-main-blocks}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.31\textwidth} >{\raggedright\arraybackslash}p{0.42\textwidth}}
\toprule
Group & Result & Role \\
\midrule
Main Theorem 1: record law &
\Cref{thm:main-record-wrapper} &
States the publishable killed-leakage sampled-boundary record theorem: the
labelled kernels generate a stationary full-support renewal process with the
displayed gap law, increasing hazard, covariance tail, exact \(1\)-dependence
criterion, and non-Markov visible law. \\
\addlinespace
Kernel construction and proof details &
\Cref{prop:instrument-kernels-main,lem:renewal-gap-main,thm:recovery-main} &
Constructs the killed-leakage sampled-boundary one-recovery-opportunity readout
and proves the renewal gap law, full support, stationary density, increasing
hazard, and covariance tail used in the main theorem. \\
\addlinespace
Exact \(1\)-dependence and diagnostics &
\Cref{lem:one-dep-criterion-main,prop:spectrum-main,prop:finite-diagnostics-main} &
Identifies the unique hidden-eigenvalue zero
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\), its sampled-record
covariance and count-noise formulas, and the fixed-setting long-record
diagnostics.  The spectral-density, Fano, and hazard displays are population
formulas for the sampled kernels only; the studentized finite-record theorem
below is only for the lag-two covariance unless separate variance theory is
supplied. \\
\addlinespace
Main Theorem 2: quotient inverse &
\Cref{thm:toeplitz_image_inverse_duality} &
Proves the quotient inverse.  The supporting renewal inclusion and finite LU
product identity statements are bookkeeping for click-set probabilities already
constructed from the renewal law and finite inclusion--exclusion.  The inverse
uses \(D=r_1-r_0^2=r_0(a-r_0)\ne0\) and recovers only
\(\sigma_{\rm sym}=(\sigma_1,\sigma_2)=(p+s,ps)\), hence the unordered
attempt/recovery pair.  Labelled roots and rates require the separated-root
condition \(\Delta_\sigma=\sigma_1^2-4\sigma_2>0\) and an external branch
label; the labelled-rate chart fails at \(p=s\), equivalently
\(\Delta_\sigma=0\). \\
\addlinespace
Main Theorem 3: fixed-record inference &
\Cref{thm:finite-record-inference-audit-main} &
Gives the fixed-record complete-cycle CLT for inclusion statistics under the
post-burn-in retained-length protocol.  The generic finite-lag CLT, separated
batch estimator, projection calculations, inverse-delta chart, lag-two
studentized interval, and one-record estimand summary are supporting lemmas,
propositions, and corollaries rather than additional main theorems. \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:main-jpa-comparison} gives the recent closest-comparator theorem
comparison.

\Cref{sec:operational} and \Cref{sec:records} define the record-level
observables, record the exact-clock Parry benchmark, and prove the record-side
result groups.  The comparison in
\Cref{sec:theorem-led-comparison} closes the main theorem narrative, and
Table~\ref{tab:notation-global} lists the recurring record-side symbols and
scope conventions.

\section{Classical sampled-counter setting and record-level observables}\label{sec:operational}

We first consider only the sampled readout law. Proper time is divided into
intervals of length $\Delta\tau$, a classical counter carries a hidden readiness
state, and the visible output is the stationary two-sided binary process
\[
(A_n)_{n\in\mathbb Z}\in\{0,1\}^{\mathbb Z},
\]
where \(A_n=1\) denotes a click in the \(n\)th sampling interval of the
stationary extension.  All Palm, covariance, renewal, and independence
statements below refer to this two-sided stationary law.  A finite observed
record of length \(N\) is only the restriction
\[
A^{(N)}=(A_0,A_1,\ldots,A_{N-1}),
\]
of the stationary process.  The only nonstationary finite-start convention
used below is the post-burn-in retained-length convention: generate
\(L_N+N\) intervals from an arbitrary hidden initial law, discard the first
\(L_N\), and reindex the retained word as
\((A_{L_N},\ldots,A_{L_N+N-1})\), so \(N\) is always the retained sample size.
Compressing such a finite record to the empirical click
frequency recovers only the one-point observable
\[
\widehat\rho_N=\frac{1}{N}\sum_{n=0}^{N-1}A_n.
\]
The full record, however, carries more. In particular, one may ask:
\begin{enumerate}[label=\textup{(\alph*)},leftmargin=2.0em,itemsep=0.25em]
\item what words belong to the support of the stationary law;
\item how the zero-run lengths between clicks are distributed;
\item whether the click hazard after a long run of zeros remembers the run length;
\item how the low-lag covariance sequence behaves;
\item what counting noise remains after coarse time averaging.
\end{enumerate}
\begin{definition}[Record benchmarks]\label{def:record-benchmarks-main}
For a stationary binary record $(A_n)_{n\in\mathbb Z}$, a \emph{zero-run length} is the number $G$ of consecutive zeros following a click and preceding the next click. If the zero-run law has full support, its hazard is
\[
h_k=\PP(G=k\mid G\ge k),\qquad k\ge0 .
\]
The ideal record is on the \emph{Parry benchmark} when its stationary law is the Parry measure on the golden-mean shift, i.e. the unique measure of maximal entropy subject to the grammar constraint $11\notin\operatorname{supp}\mu$. A stationary record is \emph{exact $1$-dependent} when, for every $m$, the sigma algebras generated by $\{A_j:j\le m\}$ and $\{A_j:j\ge m+2\}$ are independent.
Equivalently, one leaves the intermediate sample at time $m+1$ unobserved: a block ending at time $m$ is tested for independence from a block beginning at time $m+2$, not conditioned on the middle sample.
\end{definition}

These observables are not themselves rate observables, yet in the explicit
readout models below they reduce to sharp thresholds in the internal counter
rate $\Gamma$.

It is useful to keep the record construction in two logically separate steps.
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=2.2em,itemsep=0.2em]
\item A counter model determines a stationary word law and a record-level benchmark: the Parry law in the ideal case, exact $1$-dependence in the sampled boundary-reset case.
\item For the two models studied here, that benchmark is first equivalent to a scalar internal counter-rate condition, respectively $\Gamma\Delta\tau=\log\varphi$ and $\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$.  The memory condition is labelled only after the recovery rate has been independently identified: the inverse theorem gives
\[
\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}
\quad\text{and hence only}\quad
\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}.
\]
Without an external label the memory statement is made on the quotient by
$(\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)$.
\end{enumerate}
The two counter readout regimes studied in this work have intrinsically
different record thresholds. The ideal model imposes a hard symbolic grammar, so its
distinguished value is the entropy benchmark on the golden-mean shift. The
sampled one-recovery-opportunity boundary-reset model instead produces a
full-support renewal law, so its distinguished value is the exact
\(1\)-dependence threshold where the hidden eigenmode vanishes.

\begin{table}[H]
\centering
\small
\caption{Record-level observables that distinguish the two counter readout regimes.}
\label{tab:signatures}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.21\textwidth}}
\toprule
Regime & Output law & Distinguished threshold & Finite-record signature \\
\midrule
Ideal dead time & Markov chain on the golden-mean shift; $11$ forbidden & $\Gamma_+\Delta\tau=\log\varphi$ & No consecutive clicks; Parry law at threshold \\
Sampled one-recovery-opportunity boundary reset & Renewal process with infinite symbolic memory; zero-run law supported on all $k\ge0$, hence every finite binary word is allowed; renewal inclusion products determine click-set and word probabilities & With independently labelled \(\kappa_{\mathrm r}\), $\Gamma_+=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$; otherwise only the quotient under \((\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)\) is identified & Exact finite-window count formula at threshold; covariance and Fano formulas for this sampled record law \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{3pt}
\caption{Consolidated notation and scope conventions for the sampled-counter
calculation.  Response-side couplings, spectra, and level-set notation are
introduced locally in \Cref{sec:kms-shells}.}
\label{tab:notation-global}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.17\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} >{\raggedright\arraybackslash}p{0.55\textwidth}}
\toprule
Layer & Symbols & Meaning and scope \\
\midrule
Scope convention &
\(T_0,T_1,\ (A_n)\) &
All main record, inverse, and finite-record theorems start from the stipulated
labelled kernels \(T_0,T_1\), their stationary visible binary process, and
statistics of that process.  The record-side rates
\(\Gamma,\kappa_{\mathrm r}\) are labels of this classical sampled HMM.  No
main theorem derives \(T_0,T_1\) from a quantum detector, KMS state,
thermodynamic model, or continuous-time measurement dynamics. \\
\addlinespace[0.25ex]
Sampled-model parameters &
\(\Delta\tau,\ \Gamma,\ \kappa_{\mathrm r}\) &
Sampling time, classical attempt rate, and recovery rate of the stipulated
sampled counter.  Bare \(\Gamma\) is a record-side parameter in all main-text
theorems. \\
\addlinespace[0.25ex]
Sampled-model local quantities &
\(p,\ q,\ s,\ a,\ b\) &
One-bin quantities local to the hidden Markov model:
\(p=e^{-\Gamma\Delta\tau}\), \(q=1-p\),
\(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), \(a,b\) are the
one-recovery-opportunity click/no-click probabilities.  The hidden transfer
eigenvalue is defined only where it is used in \Cref{sec:records}. \\
\addlinespace[0.25ex]
Covariance and spectral quantities &
\(\lambda_{\mathrm{hid}},\ \gamma_n,\ \gamma_2,\ f(\theta),\ F\) &
\(\lambda_{\mathrm{hid}}\) is the nontrivial hidden transfer eigenvalue.
\(\gamma_n=\operatorname{Cov}(A_0,A_n)\), in particular
\(\gamma_2=\operatorname{Cov}(A_0,A_2)\), \(f(\theta)\) is the stationary
binary-record spectral density, and \(F=f(0)/\rho\) is the asymptotic Fano
factor. \\
\addlinespace[0.25ex]
Equal-rate convention &
\(\Gamma=\kappa_{\mathrm r},\ y=\Gamma\Delta\tau\) &
Every expression with \((\kappa_{\mathrm r}-\Gamma)^{-1}\) is read by the
single continuous-limit dictionary in \Cref{lem:equal-rate-critical-convention-main}
and \eqref{eq:equal-rate-dictionary-main}; this includes \(a,b\),
\(\lambda_{\mathrm{hid}}\), \(S_k\), \(g_k\), and \(h_k\). \\
\addlinespace[0.25ex]
Palm-renewal quantities &
\(\varXi,\ \rho,\ u_n,\ g_k,\ S_k,\ h_k\) &
Click set, stationary click density, scalar Palm renewal mass
\(u_n=\PP(n\in\varXi\mid0\in\varXi)\), zero-run probability, zero-run survival
tail, and zero-run hazard. \\
\addlinespace[0.25ex]
Renewal inclusion bookkeeping &
\(M_u(B),\ D_u(B),\ \delta_a\) &
The inclusion product
\(\PP(B\subset\varXi)=\rho\prod_r u_{i_{r+1}-i_r}\) is the primary object.
After this renewal probability is known, \(M(B)\) and
\(D(B)=\det M(B)\) give a finite LU product identity for the same
number; its boundary-reset specialization is denoted \(D_\Gamma(B)\), and
\(\delta_a=\rho-a\).  Critical LU-product witnesses are introduced
locally at the critical theorem. \\
\addlinespace[0.25ex]
Three-inclusion inverse &
\(\begin{gathered}r_0,\ r_1,\ r_2,\ D,\ \Phi\\
J_\Phi,\ J_{\mathrm{root}},\ J_{\Gamma,\kappa}\\
\Sigma_r,\ \Sigma_{\sigma},\ \Sigma_{\Gamma,\kappa}\end{gathered}\) &
\(r_m=\PP(A_0=A_m=1)\) for \(m=0,1,2\), with \(r_0=\rho\).
The inverse-map denominator is \(D=r_1-r_0^2=r_0(a-r_0)\).  The map
\(\Phi\) sends the three inclusion coordinates to the symmetric survival
coordinate vector \(\sigma_{\rm sym}=(p+s,ps)\).  At separated roots
\(p\ne s\), equivalently
\(\Delta_\sigma=\sigma_1^2-4\sigma_2>0\), after an external branch label has been supplied,
\(J_{\mathrm{root}}\) is the chosen labelled-root Jacobian,
\(J_{\Gamma,\kappa}\) is the inclusion-to-rate Jacobian, and
\(\Sigma_r,\Sigma_{\sigma},\Sigma_{\Gamma,\kappa}\) are the CLT
covariances before the inverse map, after \(\Phi\), and after the labelled-rate
map.  These labelled-rate objects are not defined by the visible law alone and
their chart is singular at \(p=s\). \\
\addlinespace[0.25ex]
Finite-record covariance notation &
\(\begin{gathered}
d_{\rm rew},\ m_{\rm dep},\ Y_j\\
\mathcal T_{m_{\rm dep}},\ \Sigma\\
\sigma_{\gamma_2}^2,\ \widehat\sigma^2_{\gamma_2,N}\\
\widehat\Sigma_N^{\mathrm{all}},\ t_N\end{gathered}\) &
Inside the finite-record theorems only, \(d_{\rm rew}\) is the reward-vector
dimension and \(m_{\rm dep}\) is the cycle-reward dependence/look-ahead range.
The long-run cycle-reward covariance is \(\mathcal T_{m_{\rm dep}}\), the
calendar-time covariance is \(\Sigma=\mathcal T_{m_{\rm dep}}/\mu\), and
\(\sigma_{\gamma_2}^2\) is the explicit positive calendar-time variance of
the lag-two covariance statistic in
\Cref{lem:lag-two-variance-nondegenerate-main}.  The scalar
\(\widehat\sigma^2_{\gamma_2,N}\) is the plug-in separated-batch variance
estimator used in the lag-two interval
\Cref{eq:lag-two-scalar-studentizer-main,eq:lag-two-studentized-interval-main}.
The matrix
\(\widehat\Sigma_N^{\mathrm{all}}\) is the
all-complete-cycle separated-batch covariance estimator, and \(t_N\) is the
deterministic rank threshold. \\
\bottomrule
\end{tabular}
\end{table}

\begin{theorem}[Main Theorem 1: record law]\label{thm:main-record-wrapper}
Fix one positive sampled-boundary setting
\(\Gamma,\kappa_{\mathrm r},\Delta\tau>0\), set
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
and define
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,
\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
Let
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
These are the killed-leakage labelled kernels: a click sends the next
boundary state to \(D\), so \(T_1(R,R)=T_1(D,R)=0\).  The row sum
\(P=T_0+T_1\) is stochastic, \(0<p,s<1\), \(a>0\), \(b>0\), and
\(a+b=1-s\).
Let \((A_n)_{n\in\mathbb Z}\) be the stationary visible binary process
generated by these labelled kernels, in the convention
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\).  Define
\[
\rho=\PP(A_0=1),\qquad
\lambda_{\mathrm{hid}}=p-b,
\]
with every expression containing \((\kappa_{\mathrm r}-\Gamma)^{-1}\)
interpreted by its continuous equal-rate limit, in particular
\(\lambda_{\mathrm{hid}}=e^{-y}(1-y)\) when
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\).  The complete equal-rate
dictionary used by all covariance, finite-record CLT, batch-covariance, and
inverse-delta statements is the single convention
\eqref{eq:equal-rate-dictionary-main}.  Then \((A_n)\) is a stationary
full-support renewal process.  Its zero-run law is
\[
g_k=e_D^\top T_0^kT_1\mathbf1,\qquad k\ge0.
\]
No branch map, no labelled rates: the record law below is unchanged by the
hidden-label exchange unless an external branch map is supplied for later
labelled-rate reporting.
For \(\kappa_{\mathrm r}\ne\Gamma\),
\[
g_k=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma},
\]
while at \(\kappa_{\mathrm r}=\Gamma\), writing
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\) and \(p=e^{-y}\),
\[
g_k=p^k\bigl((1-p)(1+yk)-yp\bigr),\qquad k\ge0.
\]
Its zero-run hazard is strictly
increasing and is given by \eqref{eq:hazard-main}, with the equal-rate
convention \eqref{eq:equal-rate-survival-hazard-main}.  Its covariance is
\[
\operatorname{Cov}(A_0,A_n)
=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad n\ge1,
\]
and the visible record is exact \(1\)-dependent iff
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\) under the
non-removable equal-rate convention of
\eqref{eq:gamma-crit-piecewise-main}.  In all parameter cases the visible
record is not a finite-order Markov chain.
\end{theorem}

\input{sec_records}

\section{Discussion}\label{sec:discussion}

The killed-leakage sampled-boundary theorem is the main mathematical result:
one classical sampled counter law produces a full-support renewal record with
strictly increasing hazard, suppressed adjacent-click conditional probability,
classical sub-Poisson count noise, a unique exact \(1\)-dependence threshold at
the zero of the hidden eigenmode, renewal inclusion products with optional
finite LU product identity, a closed Fano law, and a three-inclusion inverse with
\(D=r_1-r_0^2\ne0\), \(\Gamma\leftrightarrow\kappa_{\mathrm r}\) exchange
duality, and no labelled-rate chart at \(p=s\). The ideal Parry
point remains the benchmark comparison rather than the main theorem.
All items in this list are sampled-record statements.  The appendix
continuous-recovery comparator is confined to fixed finite cylinders and does
not supply a stationary or asymptotic-transfer interpretation for these
formulas.

The theorem-family comparison in \Cref{tab:main-jpa-comparison} records how
these sampled-counter results differ from HMM record laws,
finite-dependence theory, regenerative CLTs, trajectory models, and
memory-inference results.

\section{Theorem-by-Theorem Comparison}\label{sec:theorem-led-comparison}

\Cref{tab:main-jpa-comparison} compares only theorem families whose
observables are hidden-state functions, visible transitions, waiting times,
semi-Markov kernels, or long-run statistics of a partially observed process.
The comparison is therefore at the same level as the submitted claims: observed
record law, identifiable coordinates, and fixed-setting inference.  It is not a
comparison with detector engineering, QFT response theory, or general
thermodynamic estimation except where those works use partially accessible
Markov-network or semi-Markov observations.

The submitted increment is deliberately narrow.  After imposing the
killed-leakage triangular sampled-boundary kernels and stationarity, the
unlabelled binary sampled record becomes a stationary renewal law with explicit
gap probabilities, inclusion products, covariance tail, Fano law, the quotient
inverse for \((p+s,ps)\), and fixed-record diagnostics for
\((r_0,r_1,r_2,\gamma_2)\).  Standard HMM theory supplies the general process
and likelihood background
\cite{BaumPetrie1966,BaumEtAl1970Maximization,Rabiner1989HMM,
AllmanMatiasRhodes2009,GassiatCleynenRobin2016,
AlexandrovichHolzmannLeister2016,BickelRitovRyden1998,
DoucMoulinesRyden2004}; Markov-renewal, semi-Markov, phase-type, and MAP/D-MAP
theory supplies the representation background
\cite{Pyke1961MarkovRenewal,Cinlar1969MarkovRenewal,
Neuts1979MAP,Neuts1981MatrixGeometric,Lucantoni1991BMAP,
AlfaNeuts1995DMAP,OCinneide1989PHNonuniqueness,
CommaultMocanu2003PhaseTypeRepresentations,
BodrogHeindlHorvathTelek2008MAP2Canonical,
RamirezCoboLilloWiper2010MAP2Nonidentifiability,
RamirezCoboLillo2012WeakEquivalentMAP}.  The table records what those
frameworks do not by themselves provide for this special record law, and what
the submitted theorems do not claim beyond it.

\begingroup
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.23\textwidth}
>{\raggedright\arraybackslash}p{0.34\textwidth}
>{\raggedright\arraybackslash}p{0.35\textwidth}}
\caption{Compact theorem-level comparison with closest observed-process
comparators.}
\label{tab:main-jpa-comparison}\\
\toprule
Submitted theorem family & Closest comparator theorem family & Exact separation of observable and target \\
\midrule
\endfirsthead
\caption[]{Compact theorem-level comparison with closest observed-process
comparators (continued).}\\
\toprule
Submitted theorem family & Closest comparator theorem family & Exact separation of observable and target \\
\midrule
\endhead
\bottomrule
\endfoot
Exact sampled-record law and quotient inverse:
\Cref{thm:main-record-wrapper,thm:toeplitz_image_inverse_duality}. &
Finite-HMM process, likelihood, identifiability, and asymptotic-normality
theorems for probabilistic functions of finite Markov chains
\cite{BaumPetrie1966,BaumEtAl1970Maximization,Rabiner1989HMM,
AllmanMatiasRhodes2009,GassiatCleynenRobin2016,
AlexandrovichHolzmannLeister2016,BickelRitovRyden1998,
DoucMoulinesRyden2004}. &
The comparator observable is a general finite-state HMM output law and the
target is generic likelihood inference or generic HMM identifiability, up to
its stated label and rank ambiguities.  The submitted observable is one binary
fixed-clock record from the triangular killed-leakage kernels.  The target is
the closed renewal law and the quotient inverse for \((p+s,ps)\); no general
HMM likelihood algorithm or labelled-rate identification is claimed. \\
\addlinespace
Renewal inclusion products, covariance tail, and visible-law
representation boundary:
\Cref{cor:boundary_reset_toeplitz_renewal_specialization,prop:spectrum-main}. &
Markov-renewal, semi-Markov, phase-type, and MAP/D-MAP representation theorems
\cite{Pyke1961MarkovRenewal,Cinlar1969MarkovRenewal,
Neuts1979MAP,Neuts1981MatrixGeometric,Lucantoni1991BMAP,
AlfaNeuts1995DMAP,OCinneide1989PHNonuniqueness,
CommaultMocanu2003PhaseTypeRepresentations,
BodrogHeindlHorvathTelek2008MAP2Canonical,
RamirezCoboLilloWiper2010MAP2Nonidentifiability,
RamirezCoboLillo2012WeakEquivalentMAP}. &
The comparator observable is a general Markov-renewal or matrix-analytic
arrival representation, often with known kernel classes and non-uniqueness.
The submitted observable is already reduced to a scalar binary renewal record.
The target is not a canonical MAP or phase-type representation; it is the
special killed-leakage specialization
\(u_n=\rho+(a-\rho)\lambda_{\rm hid}^{n-1}\), the resulting inclusion product,
and the visible quotient coordinates. \\
\addlinespace
Exact \(1\)-dependence threshold:
\Cref{lem:one-dep-criterion-main}. &
Stationary \(1\)-dependent counting-process generating-function theory
\cite{PitmanYou2021} and finite-dependence foundations such as
\cite{Diananda1955,HolroydLiggett2016}. &
The comparator assumes finite dependence or studies its consequences for an
already specified counting process.  The submitted theorem derives the
equivalence between exact \(1\)-dependence and the hidden-eigenmode collapse
\(\lambda_{\rm hid}=0\) inside the killed-leakage sampled HMM, yielding the
explicit \(\Gamma_{\rm crit}(\kappa_{\rm r},\Delta\tau)\) surface. \\
\addlinespace
Partially accessible Markov-network comparison for record laws:
\Cref{thm:main-record-wrapper,thm:toeplitz_image_inverse_duality}. &
Transition- and waiting-time-based inference for partially accessible Markov
networks, including thermodynamic inference from visible transitions
\cite{VanDerMeerErtelSeifert2022TransitionWTD,
HarunariDuttaPolettiniRoldan2022VisibleTransitions}, periodically driven
observable-transition inference
\cite{MaierDeguntherVanderMeerSeifert2024PeriodicTransitions}, hidden-path
reconstruction from observed transitions
\cite{MaierSeifertVanderMeer2025HiddenPaths}, and stroboscopic generator
reconstruction
\cite{BauerSeifertVanderMeer2025Stroboscopic}. &
The comparator observable is selected visible transitions, their waiting-time
distributions, or a full stroboscopic propagator; the target is kinetics,
hidden paths, entropy production, generator reconstruction, or thermodynamic
bounds.  The submitted observable is only the binary sampled click/no-click
record.  Its target is the stationary renewal law and visible quotient
coordinates, not hidden paths, cycle affinities, entropy production, or a
continuous-time generator. \\
\addlinespace
Finite-record inference from imperfect or partially observed records:
\Cref{thm:finite-record-inference-audit-main,thm:one-record-estimates-main}. &
Recent partially accessible Markov-network inference with imperfect
observations, finite temporal/spatial resolution, finite statistics, and random
transition-detection blackouts
\cite{FritzErtelSeifert2025ImperfectObservations,
VanDerMeerSaito2025FaultyCoarseGraining,
MaierHaslerSeifert2026Blackouts}. &
The comparator observable is an imperfectly observed transition record from an
underlying Markov network, and the target is entropy-estimator performance,
faulty-coarse-graining error correction, blackout compensation, or recovery of
waiting-time based estimators.  The submitted observable is a single unlabelled
binary sampled record generated by the stipulated triangular kernels.  Its
finite-record target is covariance estimation, projected Gaussian coverage, and
delta-method transport for renewal coordinates and the quotient inverse, not
correction of mistaken or missed transitions or thermodynamic estimation. \\
\addlinespace
Lag-two diagnostic and fixed-setting CLT:
\Cref{thm:sampled-finite-record-main,thm:finite-record-inference-audit-main}. &
Semi-Markov large-deviation and entropy-production inference for
coarse-grained or partially accessible systems
\cite{ErtelVanderMeerSeifert2022SemiMarkovTUR,
MaierFritzSeifert2026SemiMarkovLD}, together with generic regenerative and
simulation-output CLTs
\cite{Janson2024RenewalCLT,VatsFlegalJones2019Multivariate}. &
The comparator semi-Markov observable is an empirical semi-Markov kernel or
trajectory-level waiting-time statistic, and the target is a rate function,
entropy-production bound, or general output-analysis covariance.  The
submitted statistic is the explicit local reward for the binary renewal record:
\((r_0,r_1,r_2,\gamma_2)\), the scalar batched reward for \(\gamma_2\), and
the rank-adaptive covariance audit.  The theorem supplies model-specific
rewards and plug-in control, not a general semi-Markov large-deviation theorem.
\\
\end{longtable}
\endgroup

\paragraph{Determinant notation boundary.}
The finite LU product identity in
\Cref{prop:renewal-principal-minor-lu-main,rem:finite-determinant-language-main}
is only the determinant display inside a finite LU product identity for the
renewal inclusion product.  Determinantal
point-process existence theory
\cite{Macchi1975Fermion,Lyons2003DPP} uses admissible kernels, with the usual
Hermitian discrete criterion \(0\le K\le I\), to define an entire process from
determinantal inclusion probabilities.  No positive contraction kernel, DPP
sampling rule, repulsion property, negative association theorem, or
determinantal-process construction is asserted here.

\paragraph{Appendix fixed-window comparator.}
The appendix comparator is a fixed-cylinder total-variation check only.  It
does not transfer stationary laws, spectra, Fano limits, inverse maps, or CLTs
from the sampled record to a continuous intra-bin recovery process.

\appendix

\section{Fixed-Window Comparator}\label{app:fixed-window-comparator}

\begin{proposition}[Fixed-window comparison of killed-leakage sampled-boundary and continuous intra-bin recovery]\label{prop:model-validity-boundary-main}\label{prop:continuous-comparator-main}\label{prop:short-bin-boundary-reset-main}\label{prop:finite-window-continuous-comparator-main}
Put
\[
x=\Gamma\Delta\tau,\qquad y=\kappa_{\mathrm r}\Delta\tau,\qquad
p=e^{-x},\qquad s=e^{-y},
\]
and
\[
b=\int_0^1 y e^{-yt}e^{-x(1-t)}\,dt,\qquad
a=\int_0^1 y e^{-yt}\bigl(1-e^{-x(1-t)}\bigr)\,dt .
\]
Let \(T_0,T_1\) be the sampled boundary-reset kernels in
\Cref{ass:killed-leakage-boundary-reset-main}.  Let
\(\widetilde T_0,\widetilde T_1\) be the continuous intra-bin comparator:
within one bin the hidden state evolves as
\[
R \xrightarrow{\ \Gamma\ } D,\qquad
D \xrightarrow{\ \kappa_{\mathrm r}\ } R,
\]
and the visible output records only whether at least one click occurred before
the boundary.  Then
\[
\widetilde T_0=T_0,\qquad
\widetilde T_1-T_1=
\begin{pmatrix}
\delta_R&-\delta_R\\
\delta_D&-\delta_D
\end{pmatrix},
\]
where
\[
\delta_R=
\frac{y(1-e^{-x})-x e^{-x}(1-e^{-y})}{x+y},
\qquad
\delta_D=
\frac{y}{x+y}\bigl(1-e^{-(x+y)}\bigr)-b.
\]
The two entries \(\delta_R,\delta_D\) are the paths suppressed by the sampled
boundary-reset convention: click-then-recover within the same bin from \(R\),
and recover-click-recover within the same bin from \(D\).  They are positive.
Let \(\eta_{x,y}:=\max\{\delta_R,\delta_D\}\).
For every initial hidden law \(\alpha\), every fixed word length \(m\ge1\), let
\(\mathsf P_{\alpha}^{(m)}\) and
\(\widetilde{\mathsf P}_{\alpha}^{(m)}\) be the two output laws on
\(\{0,1\}^m\).  Then
\begin{equation}\label{eq:finite-window-tv-comparator-main}
\bigl\|\mathsf P_{\alpha}^{(m)}
-\widetilde{\mathsf P}_{\alpha}^{(m)}\bigr\|_{\mathrm{TV}}
\le m\,\eta_{x,y}.
\end{equation}
Consequently, for every bounded function
\(F:\{0,1\}^m\to\mathbb R\),
The same bound gives
\[
\left|
\EE_{\rm sampled}F(A_0,\ldots,A_{m-1})
-
\EE_{\rm cont}F(A_0,\ldots,A_{m-1})
\right|
\le
2\|F\|_\infty\,m\,\eta_{x,y}
\]
for every bounded \(F:\{0,1\}^m\to\mathbb R\).
\end{proposition}

\begin{proof}
The no-click kernel is unchanged, because no reset can occur before the first
recorded click.  The displayed correction is obtained by subtracting the
sampled \(T_0+T_1\) kernels from the one-bin transition law of the
two-state continuous-time chain with rates \(\Gamma,\kappa_{\mathrm r}\), then
separating the paths according to whether at least one click occurred.  The
one-step labelled total-variation distance is therefore at most
\(\eta_{x,y}\).  A stepwise maximal coupling and the union bound give
\eqref{eq:finite-window-tv-comparator-main}; the bounded-test-function
estimate is the total-variation dual inequality.
\end{proof}

\paragraph{Warning.}
\Cref{prop:finite-window-continuous-comparator-main} is only a fixed-cylinder
bound.  If the window grows, the displayed estimate is informative only under
an additional condition such as \(m_N\eta_{x,y}\to0\), and for a full
\(N\)-bin word this becomes \(N\eta_{x,y}\to0\).  This is only a statement
about the displayed finite-cylinder inequality.  It is not a stationary,
spectral, inverse, Fano-factor, or CLT perturbation theorem.  In particular,
the paper does not prove a stationary law for the continuous comparator close
to the sampled stationary law, does not transfer the sampled spectral density
or Fano limit, does not transfer the quotient inverse, and does not transfer
the fixed-record CLT or studentized intervals.  The main stationary and
finite-record theorems remain statements about the sampled kernels \(T_0,T_1\)
in \Cref{ass:killed-leakage-boundary-reset-main}.

\input{sec_response_short}

\section*{Funding}

No specific funding was received for this work.

\section*{Data availability}

No new data were created or analysed in this study. The article is
theoretical, and all calculations needed to reproduce the reported results are
contained in the submitted manuscript sources.  The record-side hazard,
covariance, spectrum, and Fano-factor plots are kept in the supplementary
material as deterministic formula sanity checks.  They are not simulations,
fits, validation data, or evidence for the theorem statements.  The displayed
formulas give the paper-local recipe for solving
$\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$ and regenerating those supplementary
checks.

\clearpage
\newcommand{\MAINARTICLEBIB}{}
\input{references.tex}

\end{document}
