
\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{float}
\usepackage{graphicx}
\graphicspath{{generated/}}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\crefname{theorem}{theorem}{theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{lemma}{lemma}{lemmas}
\Crefname{lemma}{Lemma}{Lemmas}
\crefname{proposition}{proposition}{propositions}
\Crefname{proposition}{Proposition}{Propositions}
\crefname{corollary}{corollary}{corollaries}
\Crefname{corollary}{Corollary}{Corollaries}
\crefname{assumption}{assumption}{assumptions}
\Crefname{assumption}{Assumption}{Assumptions}
\crefname{definition}{definition}{definitions}
\Crefname{definition}{Definition}{Definitions}
\crefname{convention}{convention}{conventions}
\Crefname{convention}{Convention}{Conventions}
\crefname{remark}{remark}{remarks}
\Crefname{remark}{Remark}{Remarks}
\crefname{importedinput}{imported input}{imported inputs}
\Crefname{importedinput}{Imported input}{Imported inputs}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage[mathlines,switch]{lineno}
\IfFileExists{orcidlink.sty}{\usepackage{orcidlink}}{}

\hypersetup{colorlinks=true,citecolor=blue!60!black,linkcolor=blue!60!black,urlcolor=blue!60!black}
\setstretch{1.07}
\setlength{\parskip}{0.38em}
\setlength{\parindent}{1.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem*{unnumberedlemma}{Lemma}
\newtheorem*{sublemma}{Sublemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{convention}{Convention}[section]
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{importedinput}[theorem]{Imported input}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\RR}{\mathbb{R}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\recordscopebox}[1]{%
\par\smallskip\noindent\fbox{%
\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{#1}}\par\smallskip}

\title{Exact record laws, quotient inverse, and finite-record inference for a constrained two-state sampled D-MAP}
\author{Haobo Ma\thanks{CHRONOAI PTE. LTD., Singapore. Email: \href{mailto:auric@aelf.io}{auric@aelf.io}.}
\and
Wenlin Zhang\thanks{National University of Singapore, Singapore. Email: \href{mailto:e1327962@u.nus.edu}{e1327962@u.nus.edu}.}}
\date{}

\begin{document}
\modulolinenumbers[1]
\linenumbers
\maketitle

\begin{abstract}
This paper proves three theorem families for a stipulated constrained
two-state sampled D-MAP.  Time is sampled at a fixed interval, the hidden state
has two values, the killed-leakage boundary constraint fixes the labelled
kernels, and the observed output is a stationary binary record.  The ideal
one-step blanking model is kept only as the standard Parry benchmark
\(\Gamma\Delta\tau=\log\varphi\), not as the main object.

For the killed-leakage one-recovery-opportunity boundary law, the visible
record is a full-support stationary renewal process whose zero-run hazard is
strictly increasing, whose covariance has a single hidden-mode tail, and whose
exact \(1\)-dependence occurs precisely at the unique hidden-eigenvalue zero.
A stationary-renewal construction gives all finite marked-set and word
probabilities from renewal products and finite inclusion--exclusion.

The same record law has an explicit quotient inverse: three inclusion
probabilities recover the symmetric survival-factor coordinates and hence the
unordered survival-factor pair, while an ordered labelled rate pair is
available only after a separated-root branch label is supplied.
At the exchange diagonal the separated-root chart is singular, but the common
numerical rate remains an invariant visible-law estimand.

For finite records, a single complete-cycle protocol fixes the batch length,
thresholds, covariance estimator, projection rule, and chart-retention rule
before proving the inclusion-coordinate CLT, the quotient-inverse delta method,
and the projected Gaussian limit on the exchange diagonal.  The diagonal
confidence construction is pointwise in the true common survival factor \(z\),
conditional on the retained covariance rank and its zero-quantile exceptions;
it is not a uniform-in-\(z\) diagonal band and is not a standard
chi-square/Wald set.  The only fully studentized scalar confidence interval
proved here is for
\(\gamma_2=\operatorname{Cov}(A_0,A_2)\); hazard, spectrum, and Fano formulas
are population identities, and no generic scalar studentization is proved.
\end{abstract}

\noindent\textbf{Keywords:} sampled D-MAP, renewal process, exact \(1\)-dependence, renewal inclusion products, finite-record diagnostics

\section{Introduction}

This paper is about a constrained two-state discrete-time Markovian-arrival
process (D-MAP) and the stationary binary renewal record it generates.  The
primitive object is the labelled kernel pair displayed in
\Cref{ass:constrained-dmap-record-main}: \(T_0+T_1\) is the hidden transition
matrix, \(T_1\) marks an observed \(1\), and the killed entries
\(T_1(R,R)=T_1(D,R)=0\) are part of the stipulated sampled-boundary constraint.
The parameters \((\Gamma,\kappa_{\mathrm r},\Delta\tau)\) are a positive
parametrization of this D-MAP.

The paper-level object is the sampled record itself: its stationary binary
distribution, dependence threshold, renewal inclusion products, quotient
inverse, and fixed-setting long-record estimators.  All main record, inverse,
and finite-record claims are read under
\Cref{ass:constrained-dmap-record-main}.

\begin{definition}[Constrained sampled D-MAP record]\label{ass:constrained-dmap-record-main}\label{rem:scope-nonclaims-main}
Fix \((\Gamma,\kappa_{\mathrm r},\Delta\tau)\in(0,\infty)^3\) and define
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
The record model is the two-state D-MAP with hidden states ordered as
\((R,D)\) and labelled kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
The convention is
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), the visible process
\((A_n)_{n\in\mathbb Z}\) is the stationary binary process generated by these
kernels, and formulas on \(\Gamma=\kappa_{\mathrm r}\) are interpreted by
continuous limits.  The killed entries \(T_1(R,R)=T_1(D,R)=0\) are a structural
constraint of this sampled D-MAP.
\end{definition}

The main text below is organized around three paper-level main theorems:
the record law, the quotient inverse, and fixed-record inference.  Supporting
lemmas, propositions, and corollaries supply the renewal bookkeeping, generic
cycle-reward CLTs, separated-batch covariance estimates, and projected-Gaussian
boundary calculations needed by those three results.  The theorem-by-theorem
comparison in \Cref{sec:theorem-led-comparison} then separates these
contributions from standard HMM, renewal, and simulation-output theorems.

\subsection{Main two-state sampled-renewal HMM theorem}\label{subsec:main-theorem-boundary}\label{subsec:scope-nonclaims}

Under the constrained sampled D-MAP record model of
\Cref{ass:constrained-dmap-record-main}, the visible binary process is a
stationary renewal process with gap law \(g_k\), increasing hazard \(h_k\),
covariance
\[
\operatorname{Cov}(A_0,A_n)=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1},
\qquad n\ge1,
\]
and exact \(1\)-dependence if and only if
\(\lambda_{\mathrm{hid}}=0\), equivalently
\(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).  This is
Main Theorem~1 and the sole theorem-level statement whose conclusion is the
stationary record law itself.  Main Theorem~2 is the quotient inverse from
visible inclusion probabilities to the exchange quotient
\((p+s,ps)\).  Main Theorem~3 is split into three fixed-record inference
statements: Theorem~3A gives the complete-cycle inclusion-coordinate CLT and
covariance estimator, Theorem~3B gives the quotient inverse delta method and
separated labelled rates only under \(\Delta_\sigma>0\) plus an external branch
map, and Theorem~3C gives the exchange-diagonal projected limit and pushes the
diagonal set forward to the common numerical rate.  The lag-two studentized
scalar interval is stated separately as a corollary.  The only fully
studentized scalar confidence interval proved here is for
\(\gamma_2=\operatorname{Cov}(A_0,A_2)\); hazard, spectrum, and Fano formulas
are population identities unless separately studentized.  The remaining renewal,
covariance, batching, projection, and estimand statements are
supporting lemmas, propositions, or corollaries of \(T_0,T_1\) and the
associated stationary hidden Markov law.

The three main theorems and their supporting result groups are listed in
\Cref{tab:four-main-blocks}.  Lemmas, propositions, corollaries, and proof
devices are subordinated to one of these groups.  In particular, the generic
cycle-reward and separated-batch lemmas support the fixed-record inference
theorem rather than forming additional paper-level theorems.

\subsection{Standing record model}\label{subsec:model-validity-boundary}

All record-side theorem statements use the constrained sampled D-MAP of
\Cref{ass:constrained-dmap-record-main}.  Later results may call the two
coordinates \(R,D\) and the two positive rates
\(\Gamma,\kappa_{\mathrm r}\) by the optional record labels introduced there,
but no theorem below changes the underlying pair \((T_0,T_1)\) or the
stationary visible law generated from it.

\subsection{Three main theorems and supporting result groups}\label{subsec:four-theorem-blocks}

\Cref{tab:four-main-blocks} separates the three paper-level main theorems from
the supporting propositions, corollaries, and lemmas used to prove or apply
them.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Three main theorems and supporting result groups.}
\label{tab:four-main-blocks}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.31\textwidth} >{\raggedright\arraybackslash}p{0.42\textwidth}}
\toprule
Group & Result & Role \\
\midrule
Main Theorem 1: record law &
\Cref{thm:main-record-wrapper} &
States the constrained sampled D-MAP record theorem: the labelled kernels
generate a stationary full-support renewal process with the displayed gap law,
increasing hazard, covariance tail, exact \(1\)-dependence criterion, and
non-Markov visible law. \\
\addlinespace
Kernel construction and proof details &
\Cref{prop:instrument-kernels-main,lem:renewal-gap-main,lem:stationary-main,lem:covariance-transfer-main,lem:survival-hazard-main} &
Records the constrained sampled D-MAP kernels and proves the renewal gap law,
full support, stationary density, increasing hazard, and covariance tail used
in the main theorem. \\
\addlinespace
Exact \(1\)-dependence and diagnostics &
\Cref{lem:one-dep-criterion-main,prop:spectrum-main,prop:finite-diagnostics-main} &
Identifies the unique hidden-eigenvalue zero
\(\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\), its sampled-record
covariance and count-noise formulas, and the fixed-setting long-record
diagnostics.  The spectral-density, Fano, and hazard displays are population
formulas for the sampled kernels only; the studentized finite-record theorem
below is only for the lag-two covariance unless separate variance theory is
supplied. \\
\addlinespace
Main Theorem 2: quotient inverse &
\Cref{thm:toeplitz_image_inverse_duality} &
Proves the quotient inverse.  The supporting renewal inclusion product
\(\PP(B\subset\varXi)=\rho\prod_r u_{i_{r+1}-i_r}\) and finite
inclusion--exclusion formulas construct all marked-set and word probabilities.
The inverse
uses \(D=r_1-r_0^2=r_0(a-r_0)\ne0\) and recovers only
\(\sigma_{\rm sym}=(\sigma_1,\sigma_2)=(p+s,ps)\), hence the unordered
survival-factor pair.  Separated labelled roots and rates require the
separated-root condition \(\Delta_\sigma=\sigma_1^2-4\sigma_2>0\) and an
external branch label; the separated-root labelled-rate chart fails at
\(p=s\), equivalently \(\Delta_\sigma=0\), while the invariant diagonal common
rate remains available. \\
\addlinespace
Main Theorem 3A--3C: fixed-record inference &
Theorems~\ref{thm:finite-record-inference-audit-main}--\ref{thm:finite-record-exchange-diagonal-main} &
Splits fixed-record inference under the post-burn-in retained-length protocol:
Theorem~3A gives the inclusion-coordinate CLT with covariance scale
\(\mathcal T_1^{(r)}/\mu\) and the finite-record covariance theorem
\Cref{thm:finite-record-covariance-main};
Theorem~3B applies the quotient inverse delta method and permits separated
labelled-rate Wald reporting only when \(\Delta_\sigma>0\) and an external
branch map is supplied; Theorem~3C gives only the exchange-diagonal
projected-Gaussian limit and the common-rate push-forward
\(z\mapsto-\log z/\Delta\tau\).  The fully studentized lag-two covariance
interval is \(\Cref{cor:finite-record-lag-two-studentized-main}\), not a
generic scalar-Wald claim. \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:classical-baselines-comparison,tab:recent-venue-comparison} give,
respectively, the classical baseline comparison and the strict
2021-07-04--2026-07-04 closest-comparator venue comparison.

\Cref{sec:operational} and \Cref{sec:records} define the record-level
observables, record the exact-clock Parry benchmark, and prove the record-side
result groups.  The comparison in
\Cref{sec:theorem-led-comparison} closes the main theorem narrative, and
Table~\ref{tab:notation-global} lists the recurring record-side symbols.

\section{Sampled D-MAP setting and record-level observables}\label{sec:operational}

We first consider only the sampled record law of
\Cref{ass:constrained-dmap-record-main}.  Time is indexed by sampling
intervals of length \(\Delta\tau\), the hidden chain has two states, and the
visible output is the stationary two-sided binary process
\[
(A_n)_{n\in\mathbb Z}\in\{0,1\}^{\mathbb Z},
\]
where \(A_n=1\) denotes a marked transition of the D-MAP in the \(n\)th
sampling interval.  All Palm, covariance, renewal, and independence
statements below refer to this two-sided stationary law.  A finite observed
record of length \(N\) is only the restriction
\[
A^{(N)}=(A_0,A_1,\ldots,A_{N-1}),
\]
of the stationary process.  The only nonstationary finite-start convention
used below is the post-burn-in retained-length convention: generate
\(L_N+N\) intervals from an arbitrary hidden initial law, discard the first
\(L_N\), and reindex the retained word as
\((A_{L_N},\ldots,A_{L_N+N-1})\), so \(N\) is always the retained sample size.
Compressing such a finite record to the empirical click
frequency recovers only the one-point observable
\[
\widehat\rho_N=\frac{1}{N}\sum_{n=0}^{N-1}A_n.
\]
The full record, however, carries more. In particular, one may ask:
\begin{enumerate}[label=\textup{(\alph*)},leftmargin=2.0em,itemsep=0.25em]
\item what words belong to the support of the stationary law;
\item how the zero-run lengths between clicks are distributed;
\item whether the click hazard after a long run of zeros remembers the run length;
\item how the low-lag covariance sequence behaves;
\item what counting noise remains after coarse time averaging.
\end{enumerate}
\begin{definition}[Record benchmarks]\label{def:record-benchmarks-main}
For a stationary binary record $(A_n)_{n\in\mathbb Z}$, a \emph{zero-run length} is the number $G$ of consecutive zeros following a click and preceding the next click. If the zero-run law has full support, its hazard is
\[
h_k=\PP(G=k\mid G\ge k),\qquad k\ge0 .
\]
The ideal record is on the \emph{Parry benchmark} when its stationary law is the Parry measure on the golden-mean shift, i.e. the unique measure of maximal entropy subject to the grammar constraint $11\notin\operatorname{supp}\mu$. A stationary record is \emph{exact $1$-dependent} when, for every $m$, the sigma algebras generated by $\{A_j:j\le m\}$ and $\{A_j:j\ge m+2\}$ are independent.
Equivalently, one leaves the intermediate sample at time $m+1$ unobserved: a block ending at time $m$ is tested for independence from a block beginning at time $m+2$, not conditioned on the middle sample.
\end{definition}

These observables are not themselves one-point rate observables, yet in the
explicit D-MAP family above they reduce to sharp thresholds in the parameter
\(\Gamma\).

It is useful to keep the record construction in two logically separate steps.
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=2.2em,itemsep=0.2em]
\item A sampled binary law determines a stationary word law and a record-level benchmark: the Parry law in the ideal blanking comparison, exact \(1\)-dependence in the constrained sampled D-MAP.
\item For these two sampled laws, that benchmark is first equivalent to a scalar parameter condition, respectively \(\Gamma\Delta\tau=\log\varphi\) and \(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).  The latter condition is an ordered-rate statement only after a branch map has labelled the two survival roots: the inverse theorem gives
\[
\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}
\quad\text{and hence only}\quad
\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}.
\]
Without an external branch label the statement is made on the quotient by
$(\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)$.
\end{enumerate}
The two sampled binary laws have intrinsically different record thresholds.
The ideal model imposes a hard symbolic grammar, so its distinguished value is
the entropy benchmark on the golden-mean shift.  The constrained sampled
D-MAP instead produces a full-support renewal law, so its distinguished value
is the exact \(1\)-dependence threshold where the hidden eigenmode vanishes.

\begin{table}[H]
\centering
\small
\caption{Record-level observables that distinguish the two counter readout regimes.}
\label{tab:signatures}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.18\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.21\textwidth}}
\toprule
Regime & Output law & Distinguished threshold & Finite-record signature \\
\midrule
Ideal dead time & Markov chain on the golden-mean shift; $11$ forbidden & $\Gamma_+\Delta\tau=\log\varphi$ & No consecutive clicks; Parry law at threshold \\
Constrained sampled D-MAP of \Cref{ass:constrained-dmap-record-main} & Renewal process with infinite symbolic memory; zero-run law supported on all \(k\ge0\), hence every finite binary word is allowed; renewal inclusion products determine marked-set and word probabilities & With an external branch label assigning the survival roots, \(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\); otherwise only the quotient under \((\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)\) is identified & Exact finite-window count variance at the \(1\)-dependence threshold; higher count laws from the finite-window generating polynomial; covariance, hazard, and Fano formulas for this sampled record law, population only unless separately studentized \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{3pt}
\caption{Consolidated notation for the sampled-record
calculation.  Response-side couplings, spectra, and level-set notation are
introduced locally in \Cref{sec:kms-shells}.}
\label{tab:notation-global}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.17\textwidth} >{\raggedright\arraybackslash}p{0.20\textwidth} >{\raggedright\arraybackslash}p{0.55\textwidth}}
\toprule
Layer & Symbols & Meaning and scope \\
\midrule
Scope convention &
\(T_0,T_1,\ (A_n)\) &
All main record, inverse, and finite-record theorems start from the stipulated
labelled kernels \(T_0,T_1\), their stationary visible binary process, and
statistics of that process.  The record-side rates
\(\Gamma,\kappa_{\mathrm r}\) parametrize the constrained sampled D-MAP; see
\Cref{ass:constrained-dmap-record-main}. \\
\addlinespace[0.25ex]
Sampled-D-MAP parameters &
\(\Delta\tau,\ \Gamma,\ \kappa_{\mathrm r}\) &
Sampling time and the two positive rate labels used to parametrize the
stipulated D-MAP.  Bare \(\Gamma\) is a record-side parameter in all main-text
theorems. \\
\addlinespace[0.25ex]
Sampled-model local quantities &
\(p,\ q,\ s,\ a,\ b\) &
One-bin quantities local to the hidden Markov model:
\(p=e^{-\Gamma\Delta\tau}\), \(q=1-p\),
\(s=e^{-\kappa_{\mathrm r}\Delta\tau}\), \(a,b\) are the
one-recovery-opportunity click/no-click probabilities.  The hidden transfer
eigenvalue is defined only where it is used in \Cref{sec:records}. \\
\addlinespace[0.25ex]
Covariance and spectral quantities &
\(\lambda_{\mathrm{hid}},\ \gamma_n,\ \gamma_2,\ f(\theta),\ F\) &
\(\lambda_{\mathrm{hid}}\) is the nontrivial hidden transfer eigenvalue.
\(\gamma_n=\operatorname{Cov}(A_0,A_n)\), in particular
\(\gamma_2=\operatorname{Cov}(A_0,A_2)\), \(f(\theta)\) is the stationary
binary-record spectral density, and \(F=f(0)/\rho\) is the asymptotic Fano
factor. \\
\addlinespace[0.25ex]
Equal-rate convention &
\(\Gamma=\kappa_{\mathrm r},\ y=\Gamma\Delta\tau\) &
Every expression with \((\kappa_{\mathrm r}-\Gamma)^{-1}\) is read by the
single continuous-limit dictionary in \Cref{lem:equal-rate-critical-convention-main}
and \eqref{eq:equal-rate-dictionary-main}; this includes \(a,b\),
\(\lambda_{\mathrm{hid}}\), \(S_k\), \(g_k\), and \(h_k\). \\
\addlinespace[0.25ex]
Palm-renewal quantities &
\(\varXi,\ \rho,\ u_n,\ g_k,\ S_k,\ h_k\) &
Click set, stationary click density, scalar Palm renewal mass
\(u_n=\PP(n\in\varXi\mid0\in\varXi)\), zero-run probability, zero-run survival
tail, and zero-run hazard. \\
\addlinespace[0.25ex]
Renewal inclusion bookkeeping &
\(B,\ I,\ S,\ u_n,\ \delta_a\) &
The inclusion product
\(\PP(B\subset\varXi)=\rho\prod_r u_{i_{r+1}-i_r}\) is the finite-law
object.  Exact word probabilities are then obtained by finite
inclusion-exclusion over \(I\setminus S\), and \(\delta_a=\rho-a\). \\
\addlinespace[0.25ex]
Three-inclusion inverse &
\(\begin{gathered}r_0,\ r_1,\ r_2,\ D,\ \Phi\\
J_\Phi,\ J_{\mathrm{root}},\ J_{\Gamma,\kappa}\\
\Sigma_r,\ \Sigma_{\sigma},\ \Sigma_{\Gamma,\kappa}\end{gathered}\) &
\(r_m=\PP(A_0=A_m=1)\) for \(m=0,1,2\), with \(r_0=\rho\).
The inverse-map denominator is \(D=r_1-r_0^2=r_0(a-r_0)\).  The map
\(\Phi\) sends the three inclusion coordinates to the symmetric survival
coordinate vector \(\sigma_{\rm sym}=(p+s,ps)\).  At separated roots
\(p\ne s\), equivalently
\(\Delta_\sigma=\sigma_1^2-4\sigma_2>0\), and under
\Cref{conv:branch-map-labelled-rates-main},
\(J_{\mathrm{root}}\) is the chosen labelled-root Jacobian,
\(J_{\Gamma,\kappa}\) is the inclusion-to-rate Jacobian, and
\(\Sigma_r,\Sigma_{\sigma},\Sigma_{\Gamma,\kappa}\) are the CLT
covariances before the inverse map, after \(\Phi\), and after the labelled-rate
map. \\
\addlinespace[0.25ex]
Finite-record covariance notation &
\(\begin{gathered}
d_{\rm rew},\ m_{\rm dep},\ Y_j\\
\mathcal T_{m_{\rm dep}},\ \Sigma\\
\sigma_{\gamma_2}^2,\ \widehat\sigma^2_{\gamma_2,N}\\
\widehat\Sigma_N^{\mathrm{all}},\ t_N\end{gathered}\) &
Inside the finite-record theorems only, \(d_{\rm rew}\) is the reward-vector
dimension and \(m_{\rm dep}\) is the cycle-reward dependence/look-ahead range.
The long-run cycle-reward covariance is \(\mathcal T_{m_{\rm dep}}\), the
calendar-time covariance is \(\Sigma=\mathcal T_{m_{\rm dep}}/\mu\), and
\(\sigma_{\gamma_2}^2\) is the explicit positive calendar-time variance of
the lag-two covariance statistic in
\Cref{lem:lag-two-variance-nondegenerate-main}.  The scalar
\(\widehat\sigma^2_{\gamma_2,N}\) is the plug-in separated-batch variance
estimator used in the lag-two interval
\Cref{eq:lag-two-scalar-studentizer-main,eq:lag-two-studentized-interval-main}.
The matrix
\(\widehat\Sigma_N^{\mathrm{all}}\) is the
all-complete-cycle separated-batch covariance estimator, and \(t_N\) is the
deterministic rank threshold. \\
\bottomrule
\end{tabular}
\end{table}

\begin{definition}[Constrained sampled-kernel law used by the main theorems]\label{def:main-sampled-kernel-law}
Let \(\mu_{\Gamma,\kappa_{\mathrm r},\Delta\tau}\) denote the stationary
visible binary law generated by the kernel pair in
\Cref{ass:constrained-dmap-record-main}.  Equivalently, that pair is
\[
\mathcal K(p,s)=
\left(
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\quad
T_1=\begin{pmatrix}0&1-p\\0&1-s-b\end{pmatrix}
\right),
\]
where
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},\qquad
b=\int_0^{\Delta\tau}
\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
Equivalently \(a=1-s-b\) is the lower-right one-labelled entry.  The main
record, quotient inverse, and finite-record
theorems concern only
\(\mu_{\Gamma,\kappa_{\mathrm r},\Delta\tau}\).  A later calibrated response
model may supply an external scalar map for \(\Gamma\); such a use is
composition with this stationary visible law, not a derivation of
\(\mathcal K(p,s)\).  No theorem below derives \((T_0,T_1)\) from a detector,
thermodynamic, KMS, or continuous-time physical model.
\end{definition}
\par\medskip

\begin{theorem}[Main Theorem 1: record law]\label{thm:main-record-wrapper}
Let \((A_n)_{n\in\mathbb Z}\) have law
\(\mu_{\Gamma,\kappa_{\mathrm r},\Delta\tau}\) from
\Cref{def:main-sampled-kernel-law}.  Thus the theorem concerns the stipulated
stationary visible law of the two labelled kernels only.  Let \(G\) be the
number of zeros between consecutive ones, and put
\[
\rho=\PP(A_0=1),\qquad
\lambda_{\mathrm{hid}}=p-b .
\]
Then \((A_n)\) is a full-support renewal process with Palm gap law
\[
g_k=\PP(G=k)=e_D^\top T_0^kT_1\mathbf1,\qquad k\ge0 .
\]
If \(\Gamma\ne\kappa_{\mathrm r}\), then
\[
g_k=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma},
\]
while on the equal-rate diagonal the formula is interpreted by the continuous
limit, equivalently for
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\) and \(p=e^{-y}\),
\[
g_k=p^k\bigl((1-p)(1+yk)-yp\bigr).
\]
The zero-run hazard \(h_k=\PP(G=k\mid G\ge k)\) is strictly increasing.  For
every \(n\ge1\),
\[
\gamma_n:=\operatorname{Cov}(A_0,A_n)
=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1}.
\]
The record is exact \(1\)-dependent iff \(\lambda_{\mathrm{hid}}=0\), and in no
positive parameter case is it a finite-order Markov chain.  The conclusion is a
population statement about this sampled record law; labelled rate reporting is
governed by \Cref{conv:branch-map-labelled-rates-main}.
\end{theorem}

\input{sec_records}

\section{Discussion}\label{sec:discussion}

The constrained sampled D-MAP theorem is the main mathematical result:
one stipulated two-state labelled kernel pair produces a full-support renewal
record with strictly increasing hazard, suppressed adjacent-one conditional
probability, classical sub-Poisson count noise, a unique exact
\(1\)-dependence threshold at the zero of the hidden eigenmode, renewal
inclusion products and finite inclusion--exclusion word laws, a closed Fano
law, and a three-inclusion inverse with
\(D=r_1-r_0^2\ne0\), \(\Gamma\leftrightarrow\kappa_{\mathrm r}\) exchange
duality, no separated labelled-rate chart at \(p=s\), and an identifiable
diagonal common rate there. The ideal Parry
point remains the benchmark comparison rather than the main theorem.

The comparison in
\Cref{tab:classical-baselines-comparison,tab:recent-venue-comparison} separates
the older finite-chain and renewal baselines from the strict
2021-07-04--2026-07-04 venue-facing comparison with recent partially observed
record, coarse-graining, and memory-inference papers.

\section{Theorem-by-Theorem Comparison}\label{sec:theorem-led-comparison}

The comparison is split deliberately into three categories:
\[
\begin{array}{l}
\text{Strict 2021-07-04 to 2026-07-04 comparators},\\
\text{Older mathematical baselines},\\
\text{Near-miss recent probability comparators}.
\end{array}
\]
The strict recent category answers the venue-facing last-five-years question in
\Cref{tab:recent-venue-comparison}.  The older-baseline category records the
mathematical ancestry in \Cref{tab:classical-baselines-comparison}: functions
of finite Markov chains, lumpability, MAP/phase-type representation theory,
renewal limit theory, and standardized-time-series covariance estimation.  The
near-miss category is kept in prose, because its closest item falls just outside
the strict submission-date window.  To keep the comparison readable under a
numeric bibliography style, each table row names one closest comparator only;
the surrounding prose lists secondary comparators.

\paragraph{Older mathematical baselines.}
The visible process is a function of a finite Markov chain.  The appropriate
classical baseline therefore includes Heller's characterization of processes
derived from Markov chains, Fox--Rubin and Erickson on functions of Markovian
states/chains, Gurvits--Ledoux on the Markov property of a function of a Markov
chain, and Spreij on finite hidden Markov chains
\cite{Heller1965,FoxRubin1968,Erickson1970,GurvitsLedoux2005,
FinessoGrassiSpreij2010}.  Burke--Rosenblatt's Markovian-function example, the
Kemeny--Snell finite-chain lumpability criterion, and Dharmadhikari's
finite-chain-function tests supply the surrounding finite-state context
\cite{BurkeRosenblatt1958MarkovianFunction,KemenySnell1960FiniteMarkovChains,
Dharmadhikari1963Functions,Dharmadhikari1963Sufficient}.  The present theorem
differs from those works by exploiting the triangular killed-leakage kernel to
obtain an exact renewal law and an explicit quotient inverse.

For Main Theorem~1 the closest classical comparator is Heller's finite-chain
function characterization because it addresses when a visible process is
derived from a finite Markov chain.  The secondary mathematical background is
Blackwell--Koopmans and Gilbert on identifiability of functions of finite
Markov chains, Burke--Rosenblatt on a Markovian function of a Markov chain,
Kemeny--Snell on lumpability, Dharmadhikari on functions of finite chains,
Fox--Rubin, Erickson, Gurvits--Ledoux, Spreij's finite-HMM line, Ryd\'en on
aggregated continuous-time chains and MAPs, and Asanjarani--Nazarathy on MAP
representation questions
\cite{BlackwellKoopmans1957,Gilbert1959,
BurkeRosenblatt1958MarkovianFunction,KemenySnell1960FiniteMarkovChains,
Dharmadhikari1963Functions,Dharmadhikari1963Sufficient,FoxRubin1968,
Erickson1970,GurvitsLedoux2005,FinessoGrassiSpreij2010,Ryden1996Aggregated,
AsanjaraniNazarathy2022MAP}.
For Main Theorem~2 the closest comparator is the MAP2 nonidentifiability result
of Ram\'irez-Cobo--Lillo--Wiper; secondary comparators include
Blackwell--Koopmans, Gilbert, Ryd\'en, and weak equivalence for MAP2/MAP3
\cite{BlackwellKoopmans1957,Gilbert1959,Ryden1996Aggregated,
RamirezCoboLillo2012WeakEquivalentMAP}.  For Main Theorem~3A--3C the closest
comparator is Glynn--Iglehart's standardized-time-series covariance estimator;
secondary inference baselines are Baum--Petrie, Baum--Petrie--Soules--Weiss,
Asmussen--Glynn, and Janson's renewal CLT
\cite{BaumPetrie1966,BaumEtAl1970Maximization,AsmussenGlynn2007,
Janson2024RenewalCLT}.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\renewcommand{\arraystretch}{1.15}
\caption{Classical baselines for the three submitted theorem families.  These
rows are the older finite-chain, MAP, lumpability, renewal, and simulation-output
results against which the exact sampled-record theorems should be read; recent
2021-07-04--2026-07-04 observed-record comparators are separated in
\Cref{tab:recent-venue-comparison}.}
\label{tab:classical-baselines-comparison}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.27\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.25\textwidth}}
\toprule
Submitted result & Classical baseline & Closest comparator & Difference in this paper \\
\midrule
Main Theorem 1: sampled-record law
\(\Cref{thm:main-record-wrapper}\). &
Functions of finite Markov chains, Markovian functions, lumpability,
aggregated-chain identifiability, and MAP representation criteria. &
Heller's characterization of stochastic processes derived from Markov chains
\cite{Heller1965}. &
For the triangular killed-leakage sampled D-MAP(2) subfamily, the theorem gives
the exact stationary binary renewal law: Palm gap formula, full support,
strictly increasing hazard, covariance tail, Fano law, and the unique exact
\(1\)-dependence threshold \(\lambda_{\rm hid}=0\).  It also proves, in the
same two-state model, that finite dependence at the threshold does not make the
visible process finite-order Markov. \\
\addlinespace
Main Theorem 2: quotient inverse
\(\Cref{thm:toeplitz_image_inverse_duality}\). &
Finite-chain-function identifiability and MAP nonuniqueness: different hidden
representations can produce the same visible finite-word law or arrival
statistics. &
Two-state MAP nonidentifiability
\cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability}. &
The theorem proves the exact visible-law inverse appropriate to this binary
record: three inclusion coordinates with \(D=r_1-r_0^2\ne0\) identify only
\(\sigma_{\rm sym}=(p+s,ps)\), hence the exchange quotient
\(\{p,s\}\).  \Cref{conv:branch-map-labelled-rates-main} fixes the separated
labelled-rate convention; at \(p=s\) only the common diagonal rate is valid. \\
\addlinespace
Main Theorem 3A--3C: fixed-record inference.
Theorems~\ref{thm:finite-record-inference-audit-main}--\ref{thm:finite-record-exchange-diagonal-main}. &
Inference for probabilistic functions of finite Markov chains, renewal CLTs,
regenerative simulation analysis, and standardized-time-series covariance
estimation. &
Standardized-time-series covariance estimation
\cite{GlynnIglehart1990STS}. &
For complete cycles of the same unlabelled binary renewal record, the split
theorems verify the exponential-tail regenerative hypotheses, prove the
inclusion-coordinate CLT, carry the quotient-inverse delta method, fix the
deterministic retained-rank reporting rule, and give the exchange-diagonal
projected common-rate set.  The only fully studentized scalar interval is the
separate lag-two corollary for \(\gamma_2\).  These results do not correct
missed transitions, compensate blackouts, or prove a general semi-Markov
large-deviation principle. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Strict 2021-07-04 to 2026-07-04 comparators.}
The recent literature comparison is theorem-indexed rather than topic-indexed:
the rows below separate the exact renewal law, quotient identifiability,
regenerative CLT, branch-labelled inverse inference, and coalescing-root
projection.  The strict window is 2021-07-04 through 2026-07-04.  Where the
nearest theorem-level mathematical comparator is older, the row says so
explicitly and uses the strict-window paper only as the closest recent
neighbouring problem, not as the mathematical baseline for the theorem.

Secondary recent applied comparators are kept in prose rather than in table
cells.  Projected observation and coarse-graining context includes Markov-state
holography, constrained HMM reconstruction, blurred-transition inference,
transition-based waiting-time inference, snippet statistics, imperfect
observations, faulty coarse graining, stroboscopic measurements, and blackout
models
\cite{ZhaoMakarovGodec2025MarkovStateHolography,
TancrediEtAl2024ConstrainedHMM,ErtelSeifert2024BlurredTransitions,
VanDerMeerErtelSeifert2022TransitionWTD,VanDerMeerDeguntherSeifert2023Snippets,
FritzErtelSeifert2025ImperfectObservations,
VanDerMeerSaito2025FaultyCoarseGraining,BauerSeifertVanderMeer2025Stroboscopic,
MaierHaslerSeifert2026Blackouts}.  Recent memory papers by Vollmar and
coauthors, Zhao--Hartich--Godec, Loos--Godec, Brandner, Meyer--Brandner, and
Maier--Fritz--Seifert concern emergence, diagnosis, or
general formalisms for memory in projected dynamics and semi-Markov systems
rather than the exact sampled renewal record, quotient inverse, or fixed-record
confidence statements proved here
\cite{VollmarEtAl2024Memory,ZhaoHartichGodec2024Memory,
LoosGodec2025NonMarkovianEffects,Brandner2025WeakMemoryPRE,
MeyerBrandner2026WeakMemory,MaierFritzSeifert2026SemiMarkovLD}.

\paragraph{Near-miss recent probability comparators.}
Pitman--You 2021 \cite{PitmanYou2021} is the closest direct probability
comparator for stationary \(1\)-dependent counting processes; although slightly
outside the strict five-year window on the submission date, it is included here
because it is mathematically closer to the count-record part of Theorem~2.2
than the physics-memory comparators.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{1.8pt}
\renewcommand{\arraystretch}{1.12}
\caption{Theorem-indexed recent venue comparison for the strict 2021-07-04
through 2026-07-04 window.  The rows follow the submitted theorem numbering:
Theorem~2.2, Theorem~3.24, and Theorems~3.40A--3.40C.  Broad physics-memory
and coarse-graining papers are not used as substitutes for theorem-level
mathematical comparators; older theorem-level baselines are named only in the
separate baseline column and are not counted as strict-window comparators.}
\label{tab:recent-venue-comparison}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.13\textwidth}
>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.19\textwidth}}
\toprule
Submitted theorem slice & Strict-window comparator status & Outside-window theorem-level baseline & Comparator result & Increment proved here \\
\midrule
Exact renewal law:
Theorem~2.2. &
No strict-window theorem-level comparator found that derives a full-support binary renewal law,
hazard monotonicity, covariance tail, exact \(1\)-dependence criterion, and
non-finite-order Markov conclusion for the same two-state sampled D-MAP. &
Heller's finite-chain-function baseline and the MAP/D-MAP construction
literature \cite{Heller1965,AlfaNeuts1995DMAP}. &
Recent transition-observation papers such as
\cite{VanDerMeerErtelSeifert2022TransitionWTD,HarunariDuttaPolettiniRoldan2022VisibleTransitions}
infer or bound quantities from partial observations; they do not give this
closed stationary renewal law. &
The triangular killed-leakage kernels yield the explicit Palm gap
\(g_k\), increasing hazard \(h_k\), covariance tail, finite-word support, and
the exact \(\lambda_{\mathrm{hid}}=0\) dependence threshold. \\
\addlinespace
Quotient identifiability:
Theorem~3.24. &
Asanjarani--Nazarathy 2022
\cite{AsanjaraniNazarathy2022MAP}. &
MAP2 nonidentifiability and weak-equivalence baselines
\cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability,
RamirezCoboLillo2012WeakEquivalentMAP}. &
The recent paper surveys stationary MAP representation results and open
problems, including nonunique hidden representations. &
For this constrained D-MAP(2), three visible inclusion probabilities with
\(D=r_1-r_0^2\ne0\) identify exactly the symmetric quotient
\((p+s,ps)\), not a labelled ordered pair. \\
\addlinespace
Regenerative finite-record CLT:
Theorem~3.40A. &
Janson 2024 \cite{Janson2024RenewalCLT}. &
Regenerative simulation and standardized-time-series covariance theory
\cite{AsmussenGlynn2007,GlynnIglehart1990STS}. &
The strict-window comparator proves a renewal CLT at the process level. &
The theorem verifies the concrete cycle rewards for the inclusion vector,
states \(b_N,t_N,\varepsilon_N,\eta_N,\delta_N\), and proves covariance
consistency for the separated complete-cycle batches used by the finite-record
protocol. \\
\addlinespace
Quotient inverse inference:
Theorem~3.40B. &
No strict-window theorem-level comparator gives a delta method for the explicit exchange
quotient for this two-root record inverse. &
Classical delta method for smooth finite-dimensional transforms, combined with
the MAP nonidentifiability baselines cited above. &
Recent hidden-path and stroboscopic papers
\cite{MaierSeifertVanderMeer2025HiddenPaths,
BauerSeifertVanderMeer2025Stroboscopic} infer hidden Markov paths or generators
under different observation models. &
The theorem transports the inclusion-coordinate CLT through the explicit
quotient inverse \(\Phi(r)=(p+s,ps)\).  The separate finite-record protocol
reports labelled \((\Gamma,\kappa_{\mathrm r})\) only on the retained separated
chart and only with the external branch map. \\
\addlinespace
Coalescing-root projection:
Theorem~3.40C. &
No strict-window theorem-level comparator is used as a proxy for the
directionally differentiable projection theorem. &
Shapiro's metric-projection differentiability theorem and Geyer's constrained
\(M\)-estimation theorem
\cite{Shapiro1987MetricProjection,Geyer1994ConstrainedM}. &
Fang--Santos gives the relevant directionally differentiable inference warning
and limit theory \cite{FangSantos2019DirectionalDifferentiability}; recent
memory papers do not replace it. &
The theorem simulates the limiting projected Gaussian law for the cone
\(\{h_2\le z h_1\}\) at \(p=s=z\), rather than using an ordinary bootstrap or a
smooth labelled-root delta method. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Supplementary fixed-window comparator.}
The fixed-window continuous intra-bin recovery comparator is given in the
supplementary material and is auxiliary to the standing sampled D-MAP record
model of \Cref{ass:constrained-dmap-record-main}.

\appendix

\input{sec_response_short}

\section*{Funding}

No specific funding was received for this work.

\section*{Data availability}

No new data were created or analysed in this study. The article is
theoretical, and all calculations needed to reproduce the reported results are
contained in the submitted manuscript sources.  The record-side hazard,
covariance, spectrum, and Fano-factor plots are kept in the supplementary
material as deterministic formula sanity checks.  They are not simulations,
fits, validation data, or evidence for the theorem statements.  The displayed
formulas give the paper-local recipe for solving
$\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)$ and regenerating those supplementary
checks.

\clearpage
\newcommand{\MAINARTICLEBIB}{}
\input{references.tex}

\end{document}
