\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{float}
\usepackage{graphicx}
\graphicspath{{generated/}}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\crefname{theorem}{theorem}{theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{lemma}{lemma}{lemmas}
\Crefname{lemma}{Lemma}{Lemmas}
\crefname{proposition}{proposition}{propositions}
\Crefname{proposition}{Proposition}{Propositions}
\crefname{corollary}{corollary}{corollaries}
\Crefname{corollary}{Corollary}{Corollaries}
\crefname{assumption}{assumption}{assumptions}
\Crefname{assumption}{Assumption}{Assumptions}
\crefname{definition}{definition}{definitions}
\Crefname{definition}{Definition}{Definitions}
\crefname{convention}{convention}{conventions}
\Crefname{convention}{Convention}{Conventions}
\crefname{remark}{remark}{remarks}
\Crefname{remark}{Remark}{Remarks}
\crefname{importedinput}{imported input}{imported inputs}
\Crefname{importedinput}{Imported input}{Imported inputs}
\crefname{algorithmiclemma}{algorithmic lemma}{algorithmic lemmas}
\Crefname{algorithmiclemma}{Algorithmic Lemma}{Algorithmic Lemmas}
\crefname{maintheorem}{theorem}{theorems}
\Crefname{maintheorem}{Theorem}{Theorems}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage[mathlines,switch]{lineno}
\IfFileExists{orcidlink.sty}{\usepackage{orcidlink}}{}

\hypersetup{colorlinks=true,citecolor=blue!60!black,linkcolor=blue!60!black,urlcolor=blue!60!black}
\setstretch{1.07}
\setlength{\parskip}{0.38em}
\setlength{\parindent}{1.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{algorithmiclemma}[theorem]{Algorithmic Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{maintheorem}[theorem]{Theorem}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{convention}{Convention}[section]
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{importedinput}[theorem]{Imported input}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\RR}{\mathbb{R}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\doiid}[1]{\href{https://doi.org/#1}{doi:\nolinkurl{#1}}}
\newcommand{\arxivid}[1]{\href{https://arxiv.org/abs/#1}{arXiv:\nolinkurl{#1}}}
\newcommand{\shapirodoi}{\href{https://doi.org/10.1090/S0002-9939-1987-0866441-7}{doi:10.1090/S0002-9939-\allowbreak 1987-0866441-7}}
\newcommand{\recordscopebox}[1]{%
\par\smallskip\noindent\fbox{%
\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{#1}}\par\smallskip}
\newcommand{\mainresultrow}[3]{\textup{#1} & #2 & #3\\}

\title{Exact record laws, quotient inverse, and fixed-setting retained-record asymptotics for a constrained two-state classical sampled-counter D-MAP}
\author{Haobo Ma\thanks{CHRONOAI PTE. LTD., Singapore. Email: \href{mailto:auric@aelf.io}{auric@aelf.io}.}
\and
Wenlin Zhang\thanks{National University of Singapore, Singapore. Email: \href{mailto:e1327962@u.nus.edu}{e1327962@u.nus.edu}.}}
\date{}

\begin{document}
\modulolinenumbers[1]
\linenumbers
\maketitle

\begin{abstract}
This paper gives a closed-form analysis of one constrained classical
sampled-counter D-MAP(2) subfamily.  The input is a classical sampled counter:
an attempt clock with rate \(\Gamma\), a recovery clock with rate
\(\kappa_{\mathrm r}\), and a sampling interval \(\Delta\tau\).  Sampling the
counter with the boundary-reset convention derives a pair of labelled
stochastic kernels \((T_0,T_1)\) with triangular killed-leakage form.  The
ideal one-step blanking law is mentioned only as a short Parry comparator, not
as the main object.

For this constrained D-MAP law, the visible record is a full-support
stationary renewal process whose zero-run hazard is strictly increasing, whose
covariance has a single hidden-mode tail, and whose exact \(1\)-dependence
occurs precisely at the unique hidden-eigenvalue zero.
A stationary-renewal construction gives all finite marked-set and word
probabilities from renewal products and finite inclusion--exclusion.

The same record law has an explicit quotient inverse: three inclusion
probabilities recover the symmetric survival-factor coordinates and hence the
unordered survival-factor pair, while an ordered labelled rate pair is
available only after a separated-root branch label is supplied.
At the exchange diagonal the separated-root chart is singular, but the common
numerical rate remains an invariant visible-law estimand.

For fixed-setting retained-record asymptotics, a single complete-cycle
protocol fixes the batch length, thresholds, covariance estimator, projection
rule, and chart-retention rule before proving the inclusion-coordinate CLT, the
quotient-inverse delta method, and the projected Gaussian limit on the exchange
diagonal.  The diagonal acceptance theorem is the fixed-candidate test at
\(z_0=1/2\), the only diagonal candidate with a printed branch certificate in
this manuscript.
The diagonal inference result in this manuscript is a fixed-candidate
asymptotic statement at \(z_0=1/2\).  It is not a simultaneous confidence
procedure over \(0<z<1\), not a data-dependent model-selection-valid diagonal
report, and not an executed finite-record accept/reject report unless a branch
certificate and a critical-value transcript are attached.
\end{abstract}

\noindent\textbf{Keywords:} constrained D-MAP, renewal process, exact \(1\)-dependence, renewal inclusion products, finite-record diagnostics

\section{Introduction}

This paper is about a constrained two-state discrete-time Markovian-arrival
process (D-MAP) and the stationary binary renewal record it generates.  The
physical input is deliberately modest: a classical sampled counter with two
internal states, \(R\) for ready and \(D\) for dead/recovering.  During one
sampling interval of length \(\Delta\tau\), a ready counter attempts to click
with exponential rate \(\Gamma\), and a dead counter recovers with exponential
rate \(\kappa_{\mathrm r}\).  A click is recorded as \(A_n=1\), and the
boundary-reset convention assigns the post-click state at the next sample to
\(D\).  This construction derives the labelled kernel pair displayed in
\Cref{ass:model-inference-contract-main}: \(T_0+T_1\) is the hidden transition
matrix, \(T_1\) marks an observed \(1\), and the killed entries
\(T_1(R,R)=T_1(D,R)=0\) encode the rule that every recorded click leaves the
counter dead at the next boundary.  Thus
\((\Gamma,\kappa_{\mathrm r},\Delta\tau)\) are not formal decoration; they are
the attempt rate, recovery rate, and sampling interval of this sampled-counter
realization of the D-MAP.

The paper-level contribution is therefore the closed-form record-law,
quotient-inverse, and fixed-setting retained-record asymptotic analysis of this
one constrained D-MAP(2) subfamily.  The following contract fixes the model used
by all paper-level theorem statements.

\begin{assumption}[Constrained sampled-counter D-MAP model]\label{ass:model-inference-contract-main}
Fix \((\Gamma,\kappa_{\mathrm r},\Delta\tau)\in(0,\infty)^3\) and define
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},
\]
\[
a=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr,\qquad
b=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
The record model is the two-state D-MAP with hidden states ordered as
\((R,D)\) and labelled kernels
\[
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\qquad
T_1=\begin{pmatrix}0&1-p\\0&a\end{pmatrix}.
\]
The convention is
\((T_i)_{XY}=\PP(A_n=i,H_{n+1}=Y\mid H_n=X)\), the visible process
\((A_n)_{n\in\mathbb Z}\) is the stationary binary process generated by these
kernels, and formulas on \(\Gamma=\kappa_{\mathrm r}\) are interpreted by
continuous limits.  The killed entries \(T_1(R,R)=T_1(D,R)=0\) are a structural
constraint of this D-MAP.
\end{assumption}

\subsection{Scope of finite-record reports}\label{subsec:finite-record-report-scope-main}

Finite-record reporting procedures are fixed-setting retained-record
asymptotics for the stationary law in
\Cref{ass:model-inference-contract-main}.  In every theorem that invokes this
scope convention, \((\Gamma,\kappa_{\mathrm r},\Delta\tau)\) is fixed at the
population point under discussion, \(\mathcal E_N\) denotes the named reporting
event for that procedure, and finite-record reports are formed only on
\(\mathcal E_N\).  If \(\mathcal E_N\) fails, the corresponding finite-record
report is withheld.
The paper does not assert finite-sample coverage, uniform coverage over
\((\Gamma,\kappa_{\mathrm r})\), or model-selection-valid coverage across
reporting branches.

The only model interpretation assumed by this contract is the classical
sampled-counter protocol of
\Cref{lem:classical-counter-kernel-construction-main}.  If the sampling
interval \(\Delta\tau\) is known, the visible stationary
record identifies the unordered survival-factor set
\(\{p,s\}=\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}\) and
hence the unordered numerical rate set
\(\{\Gamma,\kappa_{\mathrm r}\}\).  It does not identify which member is the
attempt rate and which is the recovery rate without side information.  If
\(\Delta\tau\) is not treated as a known design constant, the visible law
identifies only the unordered products
\(\{\Gamma\Delta\tau,\kappa_{\mathrm r}\Delta\tau\}\).

The diagonal retained-record theorem is the fixed-candidate statement at
\(z_0=1/2\), using the printed branch certificate in
\Cref{lem:z-half-diagonal-branch-certificate-main}; its precise scope is
restated immediately before Theorem~E.  Other diagonal candidates and
continuum displays remain diagnostics unless a later implementation extension
supplies its own printed finite certificate and critical-value transcript.
Ordered labelled rates require the separate branch-map convention of
\Cref{conv:branch-map-labelled-rates-main}.  The paper does not claim
simultaneous inversion over \(0<z<1\), nor identification of UDW, KMS,
thermodynamic, response-profile, or ordered-rate quantities unless the
particular result supplies the extra hypotheses and proof.

\subsection{Visible-record diagnostics for the model contract}\label{subsec:visible-record-model-diagnostics-main}

\Cref{ass:model-inference-contract-main} and
\Cref{subsec:finite-record-report-scope-main} form a conditional contract, not a
model-selection theorem.  The retained-record reports below are meaningful only
after the retained visible record is treated as coming from the killed-leakage
sampled-counter D-MAP with known \(\Delta\tau\).  No size, power, or
post-selection coverage theorem is proved for model diagnostics.  The
diagnostic protocol is therefore supplementary: complete-gap renewal checks,
inclusion-product residuals, and low-lag covariance residuals can falsify the
standing renewal D-MAP interpretation, but they do not certify it.  If those
checks show a material unexplained failure, the model-conditioned
finite-record reports in
\Cref{thm:finite-record-inference-audit-main,thm:finite-record-quotient-delta-main,thm:e-fixed-candidate-exchange-diagonal-main}
are withheld rather than reported with coverage language attached.


\begin{lemma}[Classical sampled-counter derivation of the D-MAP kernels]\label{lem:classical-counter-kernel-construction-main}
The kernels in \Cref{ass:model-inference-contract-main} are exactly the
one-step kernels of the following discrete sampled-counter protocol.  At the
start of a sampling interval of length \(\Delta\tau\), the hidden state is
either \(R\) or \(D\).  From \(R\), an independent attempt clock with rate
\(\Gamma\) either rings before the end of the interval, in which case the
visible output is \(1\) and the next state is \(D\), or it does not ring, in
which case the visible output is \(0\) and the next state remains \(R\).
From \(D\), an independent recovery clock with rate \(\kappa_{\mathrm r}\)
either does not ring, giving output \(0\) and next state \(D\), or rings at
time \(r\in(0,\Delta\tau)\).  Conditional on recovery at \(r\), the same
attempt clock is run only during the remaining time \(\Delta\tau-r\): no
attempt gives output \(0\) and next state \(R\), while an attempt gives output
\(1\) and next state \(D\).  This protocol gives
\[
T_0(R,R)=p,\quad T_1(R,D)=1-p,\quad T_0(D,D)=s,
\]
\[
T_0(D,R)=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
e^{-\Gamma(\Delta\tau-r)}\,dr=b,
\quad
T_1(D,D)=\int_0^{\Delta\tau}\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}
\bigl(1-e^{-\Gamma(\Delta\tau-r)}\bigr)\,dr=a,
\]
and the two killed entries \(T_1(R,R)=T_1(D,R)=0\).
\end{lemma}

\begin{proof}
The \(R\)-row is the survival and ringing decomposition of an exponential
clock over one interval.  For the \(D\)-row, no recovery before
\(\Delta\tau\) has probability \(s=e^{-\kappa_{\mathrm r}\Delta\tau}\) and
contributes to \(T_0(D,D)\).  If recovery occurs at \(r\), its density is
\(\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}\,dr\).  Survival of the attempt
clock for the remaining time \(\Delta\tau-r\) gives the \(T_0(D,R)\) integral,
and its complement gives the \(T_1(D,D)\) integral.  The construction never
allows a marked transition landing in \(R\), which gives the two killed
entries.  These mutually exclusive cases exhaust each row, since
\(s+a+b=1\) and \(p+(1-p)=1\).
\end{proof}

The main text below is organized around five paper-level theorem statements
for this constrained D-MAP(2) subfamily.  Supporting lemmas, propositions, and
corollaries are used only to prove or apply these five statements.

\subsection{Paper-level theorem block}\label{subsec:main-theorem-boundary}

Under \Cref{ass:model-inference-contract-main}, the acceptance target is the
following five-result block.  Other numbered statements support these rows.
Executable routines and certificate transcripts are supplementary, except for
the \(z_0=1/2\) branch transcript printed in
\Cref{app:self-contained-diagonal-branch-certificate-main}.

\begingroup
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.18}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.08\textwidth}
>{\raggedright\arraybackslash}p{0.39\textwidth}
>{\raggedright\arraybackslash}p{0.45\textwidth}}
\caption{The five paper-level results.  The right column gives the audit target
and the location of the retained certificate or protocol interface.}
\label{tab:main-theorem-block}\\
\toprule
Result & Statement & Where implemented or certified \\
\midrule
\endfirsthead
\caption[]{The five paper-level results (continued).}\\
\toprule
Result & Statement & Where implemented or certified \\
\midrule
\endhead
\bottomrule
\endlastfoot
\mainresultrow{A}
{\(\Cref{thm:main-record-wrapper}\): the triangular sampled-counter kernels
\((T_0,T_1)\) generate a stationary full-support renewal binary record with
explicit Palm gap law \(g_k\), strictly increasing hazard, hidden-mode
covariance tail, exact \(1\)-dependence iff \(\lambda_{\rm hid}=0\), and no
positive-parameter finite-order Markov representation.}
{Record observables and the Fano/covariance formulas are developed in
\Cref{sec:records}.}
\mainresultrow{B}
{\(\Cref{thm:toeplitz_image_inverse_duality}\): the three visible inclusion
coordinates \(r=(r_0,r_1,r_2)\) satisfy
\(\Delta_{\rm inv}=r_1-r_0^2\ne0\) and determine only the quotient
\(\Phi(r)=(p+s,ps)\), hence the unordered survival-factor pair and complete
visible renewal law.}
{The algebra is proved in \Cref{sec:records}.  Ordered labelled rates are
withheld unless the side-data branch convention
\Cref{conv:branch-map-labelled-rates-main} is explicitly assumed.}
\mainresultrow{C}
{\(\Cref{thm:finite-record-inference-audit-main}\): the retained complete-cycle
inclusion estimator obeys
\(\sqrt N(\widehat r_N-r)\Rightarrow\mathcal N(0,\Sigma_r)\), with
\(\Sigma_r=\mathcal T_1^{(r)}/\mu\), and the separated-batch covariance has the
stated consistency rate on the retained reporting event.}
{Coverage language is governed by
\Cref{conv:boxed-retained-coverage-main}; Algorithm~1 is supplementary.}
\mainresultrow{D}
{\(\Cref{thm:finite-record-quotient-delta-main}\): on the stable quotient
chart, the delta method gives
\(\sqrt N\{\Phi(\widehat r_N)-\Phi(r)\}\Rightarrow
\mathcal N(0,D\Phi\,\Sigma_r\,D\Phi^\top)\) and reports unordered roots/rates,
not ordered hidden labels.}
{The projection/chart-retention recipe is Algorithm~2 in the supplement.
Separated labelled Wald output remains side-data-only.}
\mainresultrow{E}
{\(\Cref{thm:e-fixed-candidate-exchange-diagonal-main}\):
at the printed certified candidate \(z_0=1/2\),
\[
\PP_{\sigma_0}\{E_N\cap[Q_N\le c_{1-\alpha,N}^{-}]\}\to1-\alpha .
\]
This is a fixed-null asymptotic event only, interpreted through
\Cref{conv:boxed-retained-coverage-main}.}
{The branch certificate is
\Cref{lem:z-half-diagonal-branch-certificate-main}.  Finite report formats are
the ideal-CDF bracket and canonical DKW route in
\Cref{def:finite-diagonal-report-main,prop:finite-diagonal-report-master-main}.}
\end{longtable}
\endgroup

The dependencies are linear: A supplies the renewal law, B the quotient
inverse, C the retained-record Gaussian input, D the quotient delta method, and
E the local projection calculus at the certified candidate.

\Cref{tab:theorem-comparison} gives the compact external comparison by
Theorems~A--E: renewal-binary asymptotics, MAP/D-MAP representation theory,
quotient identifiability, projection inference, and the paper's actual
mathematical increment.

\Cref{sec:operational} and \Cref{sec:records} define the record-level
observables and prove the record-side result groups.  The comparison in
\Cref{sec:theorem-led-comparison} closes the paper-level theorem narrative, and
Table~\ref{tab:notation-global} lists the recurring record-side symbols.

\section{D-MAP setting and record-level observables}\label{sec:operational}

We first consider only the stationary D-MAP record law of
\Cref{ass:model-inference-contract-main}.  The hidden chain has two states, the
output alphabet is \(\{0,1\}\), and the visible output is the stationary
two-sided binary process
\[
(A_n)_{n\in\mathbb Z}\in\{0,1\}^{\mathbb Z},
\]
where \(A_n=1\) denotes a marked transition of the D-MAP in the \(n\)th
discrete step.  All Palm, covariance, renewal, and independence
statements below refer to this two-sided stationary law.  A finite observed
record of length \(N\) is only the restriction
\[
A^{(N)}=(A_0,A_1,\ldots,A_{N-1}),
\]
of the stationary process.  The only nonstationary finite-start convention
used below is the post-burn-in retained-length convention: generate
\(L_N+N\) intervals from an arbitrary hidden initial law, discard the first
\(L_N\), and reindex the retained word as
\((A_{L_N},\ldots,A_{L_N+N-1})\), so \(N\) is always the retained sample size.
Compressing such a finite record to the empirical click
frequency recovers only the one-point observable
\[
\widehat\rho_N=\frac{1}{N}\sum_{n=0}^{N-1}A_n.
\]
The full record, however, carries more. In particular, one may ask:
\begin{enumerate}[label=\textup{(\alph*)},leftmargin=2.0em,itemsep=0.25em]
\item what words belong to the support of the stationary law;
\item how the zero-run lengths between clicks are distributed;
\item whether the click hazard after a long run of zeros remembers the run length;
\item how the low-lag covariance sequence behaves;
\item what counting noise remains after coarse time averaging.
\end{enumerate}
\begin{definition}[Record observables]\label{def:record-benchmarks-main}
For a stationary binary record $(A_n)_{n\in\mathbb Z}$, a \emph{zero-run length} is the number $G$ of consecutive zeros following a click and preceding the next click. If the zero-run law has full support, its hazard is
\[
h_k=\PP(G=k\mid G\ge k),\qquad k\ge0 .
\]
A stationary record is \emph{exact $1$-dependent} when, for every $m$, the sigma algebras generated by $\{A_j:j\le m\}$ and $\{A_j:j\ge m+2\}$ are independent.
Equivalently, one leaves the intermediate sample at time $m+1$ unobserved: a block ending at time $m$ is tested for independence from a block beginning at time $m+2$, not conditioned on the middle sample.
\end{definition}

These observables are not themselves one-point rate observables, yet in the
explicit D-MAP family above they reduce to sharp thresholds in the coordinate
\(\Gamma\).

It is useful to keep the D-MAP record construction in two logically separate
steps.
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=2.2em,itemsep=0.2em]
\item The labelled kernels determine the stationary word law, the zero-run
hazard, and the covariance sequence of the visible binary record.
\item Exact \(1\)-dependence for this D-MAP is first equivalent to the scalar
coordinate condition \(\Gamma=\Gamma_{\mathrm{crit}}(\kappa_{\mathrm r},\Delta\tau)\).
That condition is labelled according to \Cref{ass:model-inference-contract-main}:
the inverse theorem gives
\[
\mu_{\Gamma,\kappa_{\mathrm r}}=\mu_{\kappa_{\mathrm r},\Gamma}
\quad\text{and hence only}\quad
\{e^{-\Gamma\Delta\tau},e^{-\kappa_{\mathrm r}\Delta\tau}\}.
\]
Thus, without a supplied branch label, the statement is made on the quotient by
\((\Gamma,\kappa_{\mathrm r})\sim(\kappa_{\mathrm r},\Gamma)\).
\end{enumerate}
The constrained D-MAP produces a full-support renewal law.  Its distinguished
record threshold in the main theorem chain is therefore the exact
\(1\)-dependence threshold where the hidden eigenmode vanishes.

\phantomsection\label{tab:signatures}\phantomsection\label{tab:notation-global}
The regime summary and consolidated notation table are in the supplementary
material.  The main theorem chain below keeps only the symbols needed for the
D-MAP law and proofs.  In the main finite-record proofs, \(K_N\) is the
calendar-stopped complete-cycle index and \(J_{m,N}\) is the corresponding
admissible complete-cycle set for the lag-\(m\) reward; the supplementary
diagnostic notation \(\widehat K_N\) is reserved for raw observed click-time
indices before those retained-cycle deletions are applied.

\begin{definition}[Constrained D-MAP law used by Theorems A--E]\label{def:main-sampled-kernel-law}
Let \(\mu_{\Gamma,\kappa_{\mathrm r},\Delta\tau}\) denote the stationary
visible binary law generated by the kernel pair in
\Cref{ass:model-inference-contract-main}.  Equivalently, that pair is
\[
\mathcal K(p,s)=
\left(
T_0=\begin{pmatrix}p&0\\ b&s\end{pmatrix},
\quad
T_1=\begin{pmatrix}0&1-p\\0&1-s-b\end{pmatrix}
\right),
\]
where
\[
p=e^{-\Gamma\Delta\tau},\qquad
s=e^{-\kappa_{\mathrm r}\Delta\tau},\qquad
b=\int_0^{\Delta\tau}
\kappa_{\mathrm r}e^{-\kappa_{\mathrm r}r}e^{-\Gamma(\Delta\tau-r)}\,dr .
\]
Equivalently \(a=1-s-b\) is the lower-right one-labelled entry.  This is the
kernel pair derived from the sampled-counter model and fixed by
\Cref{ass:model-inference-contract-main}.
\end{definition}
\par\medskip

\begin{theorem}[Theorem A: constrained D-MAP record law]\label{thm:main-record-wrapper}
Let \((A_n)_{n\in\mathbb Z}\) have law
\(\mu_{\Gamma,\kappa_{\mathrm r},\Delta\tau}\) from
\Cref{def:main-sampled-kernel-law}.  Let \(G\) be the
number of zeros between consecutive ones, and put
\[
\rho=\PP(A_0=1),\qquad
\lambda_{\mathrm{hid}}=p-b .
\]
Then \((A_n)\) is a full-support renewal process with Palm gap law
\[
g_k=\PP(G=k)=e_D^\top T_0^kT_1\mathbf1,\qquad k\ge0 .
\]
If \(\Gamma\ne\kappa_{\mathrm r}\), then
\[
g_k=
\frac{\kappa_{\mathrm r}(1-p)p^k-\Gamma(1-s)s^k}
     {\kappa_{\mathrm r}-\Gamma},
\]
while on the equal-rate diagonal the formula is interpreted by the continuous
limit, equivalently for
\(y=\Gamma\Delta\tau=\kappa_{\mathrm r}\Delta\tau\) and \(p=e^{-y}\),
\[
g_k=p^k\bigl((1-p)(1+yk)-yp\bigr).
\]
Writing
\[
S_k=\PP(G\ge k),\qquad h_k=\PP(G=k\mid G\ge k)=g_k/S_k ,
\]
the survival tail, hazard, and hazard increment are explicit.  Off the
equal-rate diagonal,
\[
S_k=
\frac{\kappa_{\mathrm r}p^k-\Gamma s^k}{\kappa_{\mathrm r}-\Gamma},
\qquad
h_k=
1-\frac{\kappa_{\mathrm r}p^{k+1}-\Gamma s^{k+1}}
        {\kappa_{\mathrm r}p^k-\Gamma s^k},
\]
and
\[
h_{k+1}-h_k
=
\frac{
  \Gamma\kappa_{\mathrm r}\,
  (p-s)^2\,p^k s^k
}{
  \bigl(\kappa_{\mathrm r}p^k-\Gamma s^k\bigr)
  \bigl(\kappa_{\mathrm r}p^{k+1}-\Gamma s^{k+1}\bigr)
}>0 .
\]
On the equal-rate diagonal,
\[
S_k=p^k(1+yk),\qquad
h_k=1-p-\frac{yp}{1+yk},
\qquad
h_{k+1}-h_k=
\frac{y^2p}{(1+yk)(1+y(k+1))}>0 .
\]
Thus the zero-run hazard is strictly increasing.  For every \(n\ge1\),
\[
\gamma_n:=\operatorname{Cov}(A_0,A_n)
=\rho(a-\rho)\lambda_{\mathrm{hid}}^{\,n-1}.
\]
The record is exact \(1\)-dependent iff \(\lambda_{\mathrm{hid}}=0\), and in no
positive parameter case is it a finite-order Markov chain.
\end{theorem}

\input{sec_records}

\section{Discussion}\label{sec:discussion}

The paper gives a closed-form record-law and fixed-setting inference analysis
for one constrained classical sampled-counter D-MAP(2) subfamily.  The visible
record identifies the unordered survival/rate coordinates, not hidden labels
or response-side thermodynamic parameters without external calibration.  The
finite-record diagonal statement is likewise deliberately narrow: in the main
article it is the fixed \(z_0=1/2\) candidate with its printed branch
certificate and the retained-record coverage convention of
\Cref{conv:boxed-retained-coverage-main}.

\Cref{tab:theorem-comparison} records the theorem-by-theorem literature
positioning and exact increment.

\section{Related Work and Mathematical Comparison}\label{sec:theorem-led-comparison}

The comparison in this section is a mathematical related-work comparison.  It
separates three roles for the cited literature: theorems imported as proof
inputs, papers proving nearby mathematical objects, and physics-facing or
statistical papers that motivate the same partial-observation problem but do
not prove the present row object.  This distinction is important because the
main contributions are not claims of bibliographic absence.  They are the
explicit constrained sampled-counter D-MAP formulas and finite-record
quotient/projection statements listed in the last column of
\Cref{tab:theorem-comparison}.

The closest direct probability comparator is Zamparo's stationary
renewal-binary theory \cite{Zamparo2022RenewalBinary}.  It supplies the
renewal-binary finite-dimensional and Gaussian limit inputs.  The increment
here is the derivation of that renewal record from the triangular
sampled-counter D-MAP kernels, together with closed formulas for the gap law,
hazard increments, hidden-mode covariance tail, and the zero-hidden-mode
\(1\)-dependence threshold.  The MAP literature provides the correct
arrival-process background: Neuts' Markovian point process
\cite{Neuts1979MAP}, Lucantoni's batch-MAP work
\cite{Lucantoni1991BMAP}, the non-renewal arrival process of
Lucantoni--Meier-Hellstern--Neuts
\cite{LucantoniMeierHellsternNeuts1990Vacations}, and the discrete-time MAP
construction of Alfa--Neuts \cite{AlfaNeuts1995DMAP}.  Recent structured-MAP
moment work \cite{AsanjaraniHautphenneNazarathy2026StructuredMAPMoments}
is close to the moment side of the paper, but it does not replace the
visible-renewal quotient inverse used here.

The inverse results should be read against two established facts.  First, MAP
and phase-type representations are not generally identifiable as labelled
matrix representations
\cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability,
OCinneide1989PhaseTypeNonuniqueness}.  Second, recent partial-observation
Markov-network and thermodynamic-inference papers study different estimands:
stroboscopic reconstruction \cite{BauerSeifertVanderMeer2025Stroboscopic},
visible-transition or waiting-time statistics
\cite{HarunariDuttaPolettiniRoldan2022VisibleTransitions,
VanDerMeerErtelSeifert2022TransitionWTD,
VanDerMeerDeguntherSeifert2023Snippets},
faulty or imperfect observations
\cite{ErtelSeifert2024BlurredTransitions,
VanDerMeerSaito2025FaultyCoarseGraining,
FritzErtelSeifert2025ImperfectObservations,
MaierHaslerSeifert2026Blackouts},
and coarse-grained Langevin or entropy-production inference
\cite{GhosalBisker2022PartialLangevin,DiTerlizziEtAl2024VarianceSumRule}.
The present inverse deliberately stops at the visible quotient:
three inclusion probabilities recover \((p+s,ps)\), the unordered
survival-root pair, and hence the complete visible renewal law.  They do not
identify an ordered hidden labelled D-MAP representation without the external
side information specified elsewhere in the paper.

For the finite-record and diagonal statements, the relevant statistical
comparators are renewal/regenerative CLTs
\cite{Zamparo2022RenewalBinary,Janson2024RenewalCLT}, simulation batch means
\cite{AsmussenGlynn2007,FlegalJones2010BatchMeansSpectral}, constrained-HMM
and coarse-graining inference
\cite{TancrediEtAl2024ConstrainedHMM,HilderSharma2024CoarseGraining}, and
projection or boundary inference
\cite{Shapiro1987MetricProjection,Chernoff1954LikelihoodRatio,
Geyer1994ConstrainedM,SelfLiang1987NonstandardLRT,
FangSantos2019DirectionalDifferentiability,Andrews1999BoundaryEstimation,
Andrews2001BoundaryTesting,CavaliereNielsenPedersenRahbek2022BoundaryBootstrap,
FangSeo2021ProjectionTests,Fang2021UnifyingShape,
ChenFang2019FirstOrderDegeneracy,WangYeChen2024NonconvexBoundary,
Li2025ProximalBootstrap}.  Fang--Seo's cone-projection framework is the
closest theorem-level comparator for the projection-limit component.  The
paper's additional object is narrower: a D-MAP quotient covariance, a branch
certificate for the exchange diagonal, and a fixed-candidate \(z_0=1/2\)
DKW-inflated projected-Gaussian route with an explicit finite transcript.
Mart\'inez--Bisker--Horowitz--Parrondo
\cite{MartinezBiskerHorowitzParrondo2019InferringBrokenDetailedBalance}
is retained as motivation for inference from partial observables without
visible currents.

\paragraph{Theorem-dependency summary.}
The compact comparison in \Cref{tab:theorem-comparison} lists, theorem by theorem, the imported prior theorem, closest venue comparator, overlap, and exact increment.  The surrounding literature is background; it is not used to enlarge the row-object claims.

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.18}
\begin{longtable}{>{\centering\arraybackslash}p{0.06\textwidth}
>{\raggedright\arraybackslash}p{0.25\textwidth}
>{\raggedright\arraybackslash}p{0.28\textwidth}
>{\raggedright\arraybackslash}p{0.31\textwidth}}
\caption{Compact theorem-dependency and venue-context comparison.  Imported proof inputs are separated from nearby venue comparators; the last column states the exact increment proved here.}
\label{tab:theorem-comparison}\\
\toprule
Our theorem & Imported prior theorem & Closest recent venue comparator & Exact increment \\
\midrule
\endfirsthead
\caption[]{Compact theorem-dependency and venue-context comparison (continued).}\\
\toprule
Our theorem & Imported prior theorem & Closest recent venue comparator & Exact increment \\
\midrule
\endhead
\bottomrule
\endlastfoot
\textup{A} & Zamparo stationary renewal-binary finite-dimensional law and CLT input \cite[Prop.~2.1, Thm.~4.3]{Zamparo2022RenewalBinary}; \doiid{10.1007/s10955-022-02893-8}. & Renewal, HMM, and MAP visible-process papers: Zamparo \cite{Zamparo2022RenewalBinary}; structured MAP moments \cite{AsanjaraniHautphenneNazarathy2026StructuredMAPMoments}, \doiid{10.1016/j.peva.2026.102562}; Lucantoni--Meier-Hellstern--Neuts \cite{LucantoniMeierHellsternNeuts1990Vacations}, \doiid{10.2307/1427464}. & Triangular D-MAP gap, hazard, covariance, Fano, exact \(1\)-dependence, and non-finite-order Markov formulas from the sampled-counter kernels. \\
\addlinespace
\textup{B} & None for the quotient inverse \((r_0,r_1,r_2)\mapsto(p+s,ps)\). & Structured MAP moments \cite{AsanjaraniHautphenneNazarathy2026StructuredMAPMoments}, \doiid{10.1016/j.peva.2026.102562}; MAP2 and phase-type nonidentifiability \cite{RamirezCoboLilloWiper2010MAP2Nonidentifiability,OCinneide1989PhaseTypeNonuniqueness}; stroboscopic reconstruction \cite{BauerSeifertVanderMeer2025Stroboscopic}, \doiid{10.1088/1751-8121/adbc52}. & Three-inclusion quotient inverse: \((r_2-r_0^2)/(r_1-r_0^2)=\lambda_{\rm hid}\), recovery of \((p+s,ps)\), unordered roots, and the complete visible renewal law. \\
\addlinespace
\textup{C} & Zamparo renewal-binary CLT \cite[Thm.~4.3]{Zamparo2022RenewalBinary}; regenerative and batch-means background \cite{AsmussenGlynn2007,FlegalJones2010BatchMeansSpectral}. & Janson renewal CLT \cite{Janson2024RenewalCLT}, \doiid{10.1016/j.spl.2023.109948}; visible-transition statistics \cite{HarunariDuttaPolettiniRoldan2022VisibleTransitions}, \doiid{10.1103/PhysRevX.12.041026}. & Retained-cycle covariance estimator for \((r_0,r_1,r_2)\), the all-complete-cycle reporting gate, and computable off-diagonal/diagonal covariance formulas. \\
\addlinespace
\textup{D} & Zamparo renewal-binary CLT plus the standard finite-dimensional delta method on \(\Delta_{\rm inv}\ne0\); \doiid{10.1007/s10955-022-02893-8}. & Constrained HMM inference \cite{TancrediEtAl2024ConstrainedHMM}, \doiid{10.1088/1367-2630/ad5def}; coarse graining \cite{HilderSharma2024CoarseGraining}, \doiid{10.1137/22M1473996}, \arxivid{2201.10256}. & Explicit unordered-root quotient transport through \(\Phi(r)=(p+s,ps)\), derivative bookkeeping, and side-data-only separated labelled-rate reporting. \\
\addlinespace
\textup{E} & Fang--Seo projection tests for the projection-limit component \cite{FangSeo2021ProjectionTests}, \doiid{10.3982/ECTA17764}; Shapiro metric projection background \cite{Shapiro1987MetricProjection}, \shapirodoi. & Boundary, projection, and shape-restriction inference: Fang \cite{Fang2021UnifyingShape}, \arxivid{2107.12494}; Chen--Fang \cite{ChenFang2019FirstOrderDegeneracy}, \arxivid{1901.04861}; Wang--Ye--Chen \cite{WangYeChen2024NonconvexBoundary}, \doiid{10.1093/biomet/asad062}; Li \cite{Li2025ProximalBootstrap}, \doiid{10.1016/j.jspi.2024.106245}. & D-MAP branch certificate, local exchange-diagonal projection calculus, fixed \(z_0=1/2\) projected-Gaussian theorem, and canonical DKW domination route for the simulated critical value. \\
\end{longtable}
\endgroup

The surrounding references are background, not additional theorem-level
comparators.  The fixed-window continuous intra-bin recovery comparator is
supplementary and auxiliary to \Cref{ass:model-inference-contract-main}.

\appendix

\phantomsection\label{app:literature-search-protocol-main}
\paragraph{Supplementary protocol boundary.}
Bibliographic-boundary prose, diagnostic protocols, and implementation
variants are collected in the supplementary material; the main article retains
the compact comparison in \Cref{tab:theorem-comparison} and the
proof-supporting certificate/protocol interfaces below.

\input{app_finite_record_tools}

For Theorem~E, the present manuscript certifies only the fixed candidate
\(z_0=1/2\).  All other diagonal candidates are diagnostics unless an
additional printed branch certificate and quantile transcript are supplied.

\input{app_diagonal_branch_certificate}

\begin{remark}[Parry comparator in the supplement]
The ideal one-step blanking Parry benchmark is a comparator only.  It is not
part of the D-MAP theorem package and is recorded in the supplementary
material rather than as a main-text theorem or appendix.
\end{remark}

\section*{Funding}

No specific funding was received for this work.

\section*{Data availability}

No new data were created or analysed in this study. The article is
theoretical.  The supplementary material contains deterministic formula
reproduction details for the record-side plots and examples; no simulations,
fits, or validation data are used as evidence for the theorem statements.

\clearpage
\newcommand{\MAINARTICLEBIB}{}
\input{references.tex}

\end{document}
