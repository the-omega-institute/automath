\subsection{Reduced determinants and finite parts}

Let \(z_\ast=\lambda^{-1}\). Since \(\lambda\) is a simple eigenvalue,
\(\zeta_A\) has a simple pole at \(z_\ast\), and we define the residue
constant
\begin{equation}\label{eq:residue-constant-def}
  C(A):=\lim_{z\to z_\ast}(1-\lambda z)\zeta_A(z).
\end{equation}
If \(\mu_2,\dots,\mu_d\) are the non-Perron eigenvalues, then
\begin{equation}\label{eq:reduced-det-poly}
  F_A(t):=\frac{\det(I-tA/\lambda)}{1-t}
  =\prod_{j=2}^d\left(1-\frac{\mu_j}{\lambda}t\right).
\end{equation}
We call \(F_A\) the \emph{reduced spectral polynomial}. Moreover,
\begin{equation}\label{eq:reduced-det-constant}
  C(A)=\det\nolimits'(I-A/\lambda)^{-1}=F_A(1)^{-1}.
\end{equation}
This reduced determinant calculation is only the elementary simple-pole
finite-matrix form of the Perron normalisation used in the zeta literature;
see \cite{ParryPollicott1990Zeta} for the dynamical normalisation and
\cite{Seneta2006NonnegativeMatrices} for the primitive-matrix spectral input.

\begin{lemma}[Reduced constant at a simple eigenvalue]
  \label{lem:simple-eigenvalue-reduced-constant}
  Let \(M\) be a complex square matrix and let
  \(\alpha\neq 0\) be a simple eigenvalue of \(M\). Define
  \[
    C_\alpha(M)
    :=\lim_{z\to \alpha^{-1}}
      (1-\alpha z)\det(I-zM)^{-1}.
  \]
  Then the limit exists and
  \[
    C_\alpha(M)
    =
    \prod_{\substack{\nu\in\spec(M)\\ \nu\neq\alpha}}
      \left(1-\frac{\nu}{\alpha}\right)^{-1},
  \]
  with algebraic multiplicities in the product. In particular, if \(A\)
  is primitive with Perron root \(\lambda\), then
  \(C(A)=C_\lambda(A)\).
\end{lemma}

\begin{proof}
  Since \(\alpha\) has algebraic multiplicity one,
  \[
    \det(I-zM)
    =(1-\alpha z)
      \prod_{\substack{\nu\in\spec(M)\\ \nu\neq\alpha}}(1-\nu z),
  \]
  where the remaining eigenvalues are listed with algebraic
  multiplicity. Multiplying by \(1-\alpha z\) after inverting and then
  taking \(z\to \alpha^{-1}\) gives the displayed formula. For a
  primitive matrix, Perron--Frobenius theory
  \cite{Seneta2006NonnegativeMatrices} makes the Perron root simple, so the
  definition agrees with \eqref{eq:residue-constant-def}.
\end{proof}

When \(\lambda>1\), define the primitive Abel generating series
\[
  P_A(r):=\sum_{n\ge 1}p_n(A)\lambda^{-n}r^n,
  \qquad 0\le r<1.
\]
By \Cref{lem:primitive-orbit-asymptotic},
\(p_n(A)\lambda^{-n}=1/n+O(\vartheta^n/n)\) for some
\(\vartheta\in(0,1)\), the renormalised expression
\[
  P_A(r)+\log(1-r)
\]
has a finite limit as \(r\uparrow 1\).

\begin{definition}[Abel finite-part constant]\label{def:abel-finite-part}
For a primitive matrix \(A\) with Perron root \(\lambda>1\), we write
\[
  \FP(A):=\lim_{r\uparrow 1}\bigl(P_A(r)+\log(1-r)\bigr).
\]
The associated orbit-Mertens constant is
\[
  \mathcal M(A):=\gamma+\FP(A).
\]
\end{definition}

\subsection{Auxiliary cyclic-lift notation}

This short subsection fixes the canonical fixed-matrix comparison notation
used later in the scalar/no-descent boundary.  For \(q\ge 1\) let \(P_q\) be the \(q\)-cycle permutation matrix and
define the cyclic lift
\begin{equation}\label{eq:cyclic-lift-def}
  A^{\langle q\rangle}:=A\otimes P_q.
\end{equation}
The basic spectral identity
\[
  \spec(A^{\langle q\rangle})
  =\{\mu\,\omega_q^j:\mu\in\spec(A),\ 0\le j\le q-1\}
\]
will be used repeatedly. In particular,
\(\rho(A^{\langle q\rangle})=\rho(A)\). For \(q>1\) this cyclic lift is
typically periodic rather than primitive, but \(\lambda=\rho(A)\) is
still a simple eigenvalue of \(A^{\langle q\rangle}\). We therefore use
the branch-lifted logarithmic record
\[
  \Psi_q(A):=
  -\sum_{j=2}^d
    \Log\!\left(1-\left(\frac{\mu_j}{\lambda}\right)^q\right)
\]
as a fixed-matrix comparison quantity. Since \(|\mu_j|<\lambda\), every
factor \(1-(\mu_j/\lambda)^q\) lies in the open right half-plane, so the
principal branch \(\Log\) is well defined here. By
\Cref{lem:simple-eigenvalue-reduced-constant},
\[
  \exp(\Psi_q(A))=q\,C_\lambda(A^{\langle q\rangle}),
\]
so the reduced constants determine only the exponentiated values unless
a branch choice is included as part of the data.
When interpreted as a \(\ZZ/q\ZZ\)-extension, this is the special
all-edges-label-\(1\) cocycle; it is not the same object as an arbitrary
cyclic quotient of a later finite-group cocycle.

\begin{definition}[Auxiliary fixed-matrix cyclic-lift notation]
  \label{def:spectral-fingerprints}
  In the fixed-matrix cyclic-lift comparison, the auxiliary cyclic-lift
  logarithmic sequence of \(A\) is
  \[
    \{\Psi_q(A)\}_{q\ge 1}.
  \]
  For each \(q\ge 1\), the auxiliary \(q\)-torsion scalar layer is
  \[
    \{F_A(\omega_q^j)\}_{0\le j\le q-1}.
  \]
  These are auxiliary scalar quantities of this one matrix \(A\). They are
  not invariants of a varying family and not class-profile data for a
  finite-group cocycle unless additional descent information is supplied. In
  this article they are bookkeeping for the scalar comparison boundary only:
  the record stores fixed-matrix data, and by itself it does not produce a
  cocycle class profile, a cyclic-lift invariant, or a stable invariant under
  changes of matrix presentation.
\end{definition}

\subsection{Notation table for finite-group coordinates}
\label{subsec:notation-table}

This table is the reference point for the notation used from
\S\ref{sec:chebotarev} onward.  The symbols are grouped by the four
coordinate systems that occur in the proof: periodic fixed-point data,
primitive orbit data, fixed-label Euler data, and Frobenius-class product
data.

\begin{center}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{L{0.23\textwidth}L{0.31\textwidth}L{0.36\textwidth}}
\toprule
\multicolumn{3}{c}{\textbf{Basic finite-state and representation notation}}\\
\midrule
Symbol & Meaning & Convention used in the paper\\
\midrule
\(\sigma\) & The left shift on the one-sided edge shift \(X_A^+\). &
Fixed points of \(\sigma^n\) are basepointed closed paths of length \(n\);
they are not cyclic primitive orbit classes.\\
\(\lambda\) and \(\rho(M)\) & Perron root of \(A\), and spectral radius of a
matrix \(M\). & The Greek letter \(\rho(\cdot)\) is never an irreducible
representation.\\
\(\varrho,\pi,\kappa\in\Irr(G)\) & Irreducible unitary representations of
the finite group \(G\). & We reserve \(\varrho,\pi,\kappa\) for
representation labels; primitive Peter--Weyl coefficients use the separate
symbol \(c_{n,\varrho}\).\\
\(A_\varrho\) & The twisted transition block
\((A_\varrho)_{ij}=\sum_{e:i\to j}\varrho(\tau(e))\). & Its traces are
periodic fixed-point sums in the \(\varrho\)-channel; its determinant
polynomial is \(P_\varrho(z)=\det(I-zA_\varrho)\).\\
\(\chi_\varrho\), \(\chi_\varrho^\ast(C)\) & Character of \(\varrho\), and
\(\chi_\varrho^\ast(C)=\overline{\chi_\varrho(C)}=\chi_\varrho(C^{-1})\). &
Forward class expansions use \(\chi_\varrho^\ast(C)\); inverse Fourier
recovery pairs with the unconjugated \(\chi_\pi(C)\).\\
\(\psi^m\phi\), \(a_{\varrho,\pi}^{(m)}\), \(\operatorname{Pow}_G\) & Adams operation
\((\psi^m\phi)(g)=\phi(g^m)\), and
\(\psi^m\chi_\varrho=\sum_\pi a_{\varrho,\pi}^{(m)}\chi_\pi\). & The
finite input is the character table together with the conjugacy-class power
maps \(\operatorname{Pow}_G=\{C\mapsto C^a:0\le a<\exp(G)\}\); all \(m\)
are reduced modulo \(\exp(G)\).\\
\bottomrule
\end{tabular}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{L{0.23\textwidth}L{0.31\textwidth}L{0.36\textwidth}}
\toprule
\multicolumn{3}{c}{\textbf{Periodic and primitive coordinates}}\\
\midrule
Symbol & Meaning & What changes under repetition\\
\midrule
\(\tau_n(x)\) & Label of a fixed point \(x\in\operatorname{Fix}(\sigma^n)\). &
If \(x\) is the \(r\)-fold repetition of a primitive orbit with label \(g\),
then \(\tau_n(x)\) is conjugate to \(g^r\).\\
\(T_{\phi,n}\) & Periodic class-function trace
\(\sum_{x\in\operatorname{Fix}(\sigma^n)}\phi(\tau_n(x))\). & This is the
fixed-point, or basepointed, quantity linearized by \(\Tr(A_\varrho^n)\).\\
\(\mathcal L_\phi(z)\) & Periodic Artin logarithm
\(\sum_{n\ge1}T_{\phi,n}z^n/n\). & It equals
\(\sum_{r\ge1}\Pi_{\psi^r\phi}(z^r)/r\); repetitions apply Adams powers to
the label.\\
\(\cP\) & Set of primitive orbits of the edge shift. & Orbits are unbased
cyclic classes; the \(1/n\) in primitive coefficient formulae appears only
after passing from fixed points to these classes.\\
\(\Pi_\phi(z)\) & Primitive fixed-label series
\(\sum_{\gamma\in\cP}\phi(\tau(\gamma))z^{|\gamma|}\). & Adams--M\"obius
inversion recovers it from the periodic logarithms:
\(\Pi_\phi(z)=\sum_{m\ge1}\mu(m)\mathcal L_{\psi^m\phi}(z^m)/m\).\\
\(c_{n,\varrho}(A,\tau)\) & Coefficient of \(\Pi_{\chi_\varrho}\):
\(\sum_{|\gamma|=n}\chi_\varrho(\tau(\gamma))\). & Its coefficient formula is
\(\frac1n\sum_{m\mid n}\mu(m)\sum_\pi a_{\varrho,\pi}^{(m)}
\Tr(A_\pi^{n/m})\).\\
\(p_{n,C}\) & Number of primitive length-\(n\) orbits whose label lies in
the conjugacy class \(C\). & It is recovered from the primitive channels by
\((|C|/|G|)\sum_\varrho\chi_\varrho^\ast(C)c_{n,\varrho}\).\\
\bottomrule
\end{tabular}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{L{0.23\textwidth}L{0.31\textwidth}L{0.36\textwidth}}
\toprule
\multicolumn{3}{c}{\textbf{Boundary and fixed-label determinant coordinates}}\\
\midrule
Symbol & Meaning & Boundary convention\\
\midrule
\(\Log_0\) & Determinant-logarithm germ normalized to vanish at the origin. &
At \(k=1\) it is continued only along the Abel path \(0\le t<1\); at
\(k\ge2\) it is evaluated at the ordinary interior point \(\lambda^{-k}\).\\
\(\Gamma_\pi\), \(m_{\pi,\zeta}\), \(s_\pi\) & Peripheral phases
\(\lambda\zeta\) of \(A_\pi\), their algebraic multiplicities, and
\(s_\pi=m_{\pi,1}\). & Only the \(\zeta=1\) multiplicity produces a
non-oscillatory logarithmic term.\\
\(D_{\partial}^{\mathrm{ren}}\) or
\(\mathfrak D_{\partial}^{\mathrm{ren}}(A,\tau;G)\) & Branch-normalized
Perron-boundary input tuple. & It consists of \(G\), the power maps,
\(\{P_\pi\}\), \(\lambda\), \(\Gamma_\pi\), \(m_{\pi,\zeta}\), and the branch
families \(\{\Log_{\pi,\zeta}\}\), \(\{\Log_{\partial,\pi}\}\).  The factor
\(Q_{\partial,\pi}\) is defined by
\(P_\pi(t/\lambda)=\prod_{\zeta\in\Gamma_\pi}(1-t\zeta)^{m_{\pi,\zeta}}
Q_{\partial,\pi}(t)\), \(Q_{\partial,\pi}(1)\ne0\).  The power-map component means the finite table
\(\operatorname{Pow}_G\) modulo \(\exp(G)\), not an infinite list.  It is
larger than the determinant polynomials alone.\\
\(\Log_{\pi,\zeta}\), \(\Log_{\partial,\pi}\) & Boundary logarithm branches. &
For \(0\le t<1\), \(\Log_{\pi,\zeta}(1-t\zeta)\) is the unique continuous
branch with value \(0\) at \(t=0\).  The value
\(\Log_{\partial,\pi}Q_{\partial,\pi}(1)\) is obtained by continuing
\(\Log Q_{\partial,\pi}(t)\), normalized at \(0\), along the same segment.
For \(k\ge2\), use the interior power-series value
\(\Log_0P_\pi(\lambda^{-k})^{-1}\).  Product constants use the real logarithm
on \(\mathbb R_{>0}\) after positivity, not a separate boundary datum.\\
\(B_{k,\pi}^{\mathrm{ren}}\) & Derived determinant-logarithm layer. & For
\(k=1\), subtract \(s_\pi\log(1-t)\) before taking the Abel finite part; for
\(k\ge2\), use \(\Log_0\det(I-\lambda^{-k}A_\pi)^{-1}\).\\
\(R_\varrho\) & Renormalized primitive finite part
\(\lim_{t\uparrow1}(\Pi_\varrho(t\lambda^{-1})+s_\varrho\log(1-t))\). &
It is the \(r=1\) primitive finite part in the Perron-boundary class-Mertens
formula.\\
\(F_\varrho(z)\) & Fixed-label Euler transform
\(\sum_{r\ge1}\Pi_\varrho(z^r)/r\). & It keeps the primitive label fixed in
all Euler repetitions and is not the periodic Artin logarithm.\\
\(F_\varrho^{\mathrm{ren}}\) & Renormalized boundary value of
\(F_\varrho\). & It is computed from
\(\sum_{r,m\ge1}\mu(m)\sum_\pi a_{\varrho,\pi}^{(m)}
B_{rm,\pi}^{\mathrm{ren}}/(rm)\).\\
\(\mathfrak D_{\mathrm{strict}}(A,\tau;G)\) & Strict-gap specialization of
\(\mathfrak D_{\partial}^{\mathrm{ren}}\). & Used only when
\(\eta=\max_{\varrho\ne\mathbf1}\rho(A_\varrho)<\lambda\); then
non-trivial \(k=1\) entries are ordinary regular Perron endpoint values and
the scalar Perron triple is deleted before evaluation.\\
\bottomrule
\end{tabular}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{L{0.23\textwidth}L{0.31\textwidth}L{0.36\textwidth}}
\toprule
\multicolumn{3}{c}{\textbf{Frobenius-class product coordinates}}\\
\midrule
Symbol & Meaning & Readout role\\
\midrule
\(\delta_C\) & Perron-boundary product exponent
\((|C|/|G|)\sum_\varrho\chi_\varrho^\ast(C)s_\varrho\). & In the strict-gap
case this reduces to \(|C|/|G|\); in general it records all
non-oscillatory boundary channels.\\
\(M_C^{\mathrm{ren}}\) & Renormalized class-Mertens constant
\((|C|/|G|)\sum_\varrho\chi_\varrho^\ast(C)(s_\varrho\gamma+R_\varrho)\). &
It is the constant term in
\(\sum_{n\le N}p_{n,C}\lambda^{-n}=\delta_C\log N+M_C^{\mathrm{ren}}+O(N^{-1})\).\\
\(P_C(N)\) & Frobenius-class Euler product
\(\prod_{\gamma\in\cP,\ |\gamma|\le N,\ \tau(\gamma)\in C}
(1-\lambda^{-|\gamma|})\). & Its logarithm splits into the \(r=1\)
class-Mertens sum plus the absolutely convergent \(r\ge2\) product tail.\\
\(K_C^{\mathrm{ren}}\) & Positive product constant in
\(P_C(N)=K_C^{\mathrm{ren}}N^{-\delta_C}(1+O(N^{-1}))\). & The positive
logarithm is
\(-(|C|/|G|)\sum_\varrho\chi_\varrho^\ast(C)
\bigl(s_\varrho\gamma+F_\varrho^{\mathrm{ren}}\bigr)\).\\
\(K_C\) & Strict-gap product constant. & It is the specialization of
\(K_C^{\mathrm{ren}}\) with \(\delta_C=|C|/|G|\) and
\(F_\varrho^{\mathrm{ren}}=F_\varrho(\lambda^{-1})\) for non-trivial
\(\varrho\).\\
\bottomrule
\end{tabular}
\end{center}

\subsection{Standing notation}

Throughout the paper \(\omega_q=e^{2\pi i/q}\) denotes a primitive
\(q\)th root of unity, and \(\Phi_q\) the \(q\)th cyclotomic
polynomial. We reserve the notation \(\varrho\) for irreducible
representations of finite groups and keep \(\rho(\cdot)\) for spectral
radii. Sections~\ref{sec:chebotarev} and~\ref{sec:shadows} use the
graph and cocycle picture for finite-group extensions. The later scalar
section, Section~\ref{sec:finite-part}, uses only the primitive matrix
formalism and cyclic lifts.


