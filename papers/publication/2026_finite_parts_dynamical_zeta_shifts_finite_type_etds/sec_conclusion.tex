\section{Discussion and open problems}\label{sec:conclusion}

\subsection{Summary of the main contribution}

The paper has two main results, both in the finite-group part. First,
twisted determinant data in the Peter--Weyl channels are converted to
primitive orbit data by Adams--M\"obius inversion. Second, under the
stated twisted spectral gap this primitive determinant calculus yields
the class-indexed constants \(\mathcal M_C(A,\tau;G)\) at the Perron
point; the accompanying Fourier formula is the standard
character-orthogonality readout of them
(\Cref{cor:class-function-adams-mobius,%
thm:class-mertens-explicit,%
thm:class-mertens-fourier}). Quotient functoriality then identifies the
abelianised primitive profile, and the linear span of cyclic quotient
observables is the abelian shadow of the full primitive class profile at
the Perron point, under \(\eta<\lambda\).
The root-of-unity layer belongs to the separate canonical cyclic-lift
model, not to arbitrary cyclic quotients of the abelianised cocycle
(\Cref{cor:abelian-cyclic-shadow,%
prop:canonical-cyclic-lift-root-layer,%
thm:abelian-shadow-defect,%
thm:cyclic-detection-boundary}).

The scalar finite-part formula and the canonical cyclic-lift comparison
remain auxiliary fixed-matrix inputs. They supply the trivial-channel
normalisation \(\mathcal M(A)\) and a separate scalar recovery model,
with the detailed cyclic-lift proofs collected in
\Cref{app:cyclic-lift-package}.

\subsection{Scope}

The paper treats one fixed finite-group cocycle and separates its
results into the three layers recorded in
\Cref{sec:intro-scope-of-results}: determinant and Adams--M\"obius
identities on the open Perron disc, Perron-point evaluation under the
twisted gap \(\eta<\lambda\), and auxiliary scalar inputs for
\(\mathcal M(A)\) together with a separate canonical cyclic-lift
comparison. Classical
prime-orbit, Chebotarev, reciprocity, and homology-class asymptotics
provide the surrounding context; the contribution here is the explicit
determinant-level primitive reconstruction and constant extraction after
that stage. In particular, the classwise constants
\(\mathcal M_C(A,\tau;G)\) should be read as finite-state secondary
terms for one fixed cocycle, not as a new Tauberian leading-density
theorem. The canonical cyclic-lift root-of-unity model remains a
comparison device for the scalar section and, for \(q>1\), lies outside
the twisted-gap regime.

\subsection{Open problems}

We close with three problems suggested by this framework.

\begin{enumerate}
\item \emph{Structural criteria beyond direct skew-product spectral
  testing.} The
  condition
  \(\eta=\max_{\varrho\neq\mathbf 1}\rho(A_\varrho)<\lambda\) is
  assumed in \Cref{thm:class-mertens-explicit}. By
  \Cref{lem:twisted-gap-skew-product-peripheral}, this is already a
  finite-dimensional condition in the present setting: it is equivalent
  to saying that the skew-product adjacency \(\widetilde A\) has no
  non-trivial peripheral spectrum, equivalently that the peripheral
  spectral multiset of \(\widetilde A\) is the single simple eigenvalue
  \(\lambda\), or equivalently that \(\widetilde A\) has a simple
  strictly dominant Perron eigenvalue \(\lambda\); if \(\widetilde A\)
  is irreducible, it is equivalent to primitivity of \(\widetilde A\).
  Thus one direct verification route is simply to build the
  skew-product matrix (or the twisted blocks \(A_\varrho\)) and test
  strict Perron dominance. The canonical cyclic lift of
  \Cref{rem:abelian-toy-model-cyclic,prop:canonical-cyclic-lift-root-layer}
  already gives such a failure:
  \(A_{\chi_j}^{\tau_q}=\omega_q^jA\) and
  \(\rho(A_{\chi_j}^{\tau_q})=\lambda\) for every \(j\neq0\), so for
  \(q>1\) this comparison model lies outside the twisted-gap regime.
  For non-abelian extensions the analogous condition was also assumed
  without derivation in \cite{ParryPollicott1986Chebotarev}. The open
  problem is therefore not computability of the condition itself but
  the search for structural or easily checkable hypotheses on the
  cocycle \(\tau:E_A\to G\) that guarantee it without first carrying
  out the full finite-matrix test. Noorani's analysis of homogeneous
  extensions and Markov-shift applications
  \cite{Noorani1997HomogeneousExtensionsMarkov}, the analysis of Parry and
  Liv\v{s}ic obstructions in
  \cite{BoyleSchmieding2017FiniteGroupExtensions}, together with
  the finite-action periodic-data viewpoint of
  \cite{Fiebig1993PeriodicFiniteGroupActions} and the \(G\)-SFT
  flow-equivalence framework of
  \cite{BoyleCarlsenEilers2020FlowEquivalenceGSFT}, gives a partial
  structural context. Parry's non-abelian periodic-point theorem
  \cite{Parry1999LivsicNonAbelian} gives another piece of this picture,
  and the \(S_3\)-model of \Cref{prop:s3-example-channels} (where
  \(\rho(A_\rho)=1<2=\lambda\)) provides one explicit verification.

\item \emph{Non-abelian analogues of cyclic arithmetic.} The scalar
  root-of-unity refinements separated in \Cref{app:arithmetic} are
  abelian in nature. It would be of interest to know whether any
  boundary-multiplicity or residue-class information has a natural
  higher-dimensional twisted-channel analogue.

\item \emph{Transfer-operator extensions.} The present paper works
  entirely at the level of finite matrices and algebraic determinants.
  For countable-state shifts or for the Ruelle transfer operator acting
  on spaces of H\"older-continuous functions, the finite-part constant
  and the Adams--M\"obius mechanism should have natural analogues
  involving the Fredholm determinant of the transfer operator. Relating
  \(\FP(A)\) to the spectral data of the transfer operator on an
  appropriate Banach space would connect the present results to the
  broader programme of Baladi
  \cite{Baladi2000PositiveTransfer,Baladi2018ZetaDeterminants}, the
  regularity results of J\'ez\'equel
  \cite{Jezequel2019ParameterRegularity}, and the
  Ruelle--Pollicott resonance theory.
\end{enumerate}
