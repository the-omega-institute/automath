\section{Verification note}\label{sec:exactness}

The source package contains the exact-arithmetic verifier
\path{artifacts/verify_a5_results.py} and the focused tests in
\path{artifacts/test_verify_a5_results.py}.  They check the \(S_3\) determinant
identities and strict gaps, the rational logarithmic inequalities used for the
sign witness, the quotient split-orbit identity through degree \(16\), and the
Fourier inversion conventions.  The expected final line is
\texttt{STATUS: PASS}; commands are recorded in \path{REPRODUCE.md}.  The
printed inequalities remain the proof and do not depend on execution of the
program.
