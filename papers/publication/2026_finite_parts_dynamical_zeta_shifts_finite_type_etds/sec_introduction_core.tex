\section{Introduction}\label{sec:introduction}

The problem addressed in this paper is primitive orbit determinant readout
for finite-group extensions of mixing subshifts of finite type. Let
\((X_A^+,\sigma)\) be a mixing subshift with primitive adjacency matrix
\(A\), let \(\lambda=\rho(A)\), and let \(\tau:E_A\to G\) be a cocycle
into a finite group. For every irreducible representation \(\varrho\)
of \(G\), the twisted transition matrix \(A_\varrho\) records periodic
labels through its traces. These traces are periodic data. The
primitive labels are subtler: when a primitive orbit with label \(g\)
is repeated \(m\) times, its label becomes \(g^m\). Thus primitive
extraction is not ordinary M\"obius inversion in the non-abelian
setting; it must be coupled to the Adams operations
\(\psi^m\phi(g)=\phi(g^m)\) on class functions.

The headline problem is inverse rigidity: what does the full family of
twisted determinant polynomials determine about the cocycle up to
Liv\v{s}ic cohomology?  Our answer is sharp.  The determinant family
determines exactly the unmarked primitive length--conjugacy-class profile,
but does not in general determine the marked cocycle.  The least
counterexample occurs for \(G=\ZZ/2\ZZ\) over the full two-shift and persists
despite primitivity of both skew products and semisimplicity of every twisted
block.  The exact intrinsic obstruction is the spectral cohomology
multiplicity \(\mu_{\Gamma,G}\), a finite fiber cardinality on
\(\operatorname{Hom}(\pi_1(|\Gamma|),G)/G\): determinant rigidity holds
exactly when \(\mu_{\Gamma,G}=1\).  On an \(m\)-loop bouquet with finite
abelian group, it has the closed form \(m!/\prod_g n_g!\), so every primitive
non-trivial abelian bouquet extension is non-rigid.  Perron-peripheral data
alone cannot determine this multiplicity, even with graph and group fixed.
On an edge-resolving graph, equality also forces a vertex gauge when the
paired semisimple spectral projectors admit the stated common
vertex-compatible certificate; this remains a useful sufficient witness
that avoids full enumeration.

The forward machinery behind this inverse theorem is the Adams--M\"obius
passage from twisted dynamical determinants to primitive Peter--Weyl channels
and then to the renormalised constant coordinates in Frobenius-class Euler
products.  Its maximal analytic form is the Perron-boundary version: all
twisted eigenvalues \(\lambda\zeta\) on the Perron circle are retained, the multiplicities
\(s_\varrho=m_{\varrho,1}\) determine the logarithmic class exponents
\(\delta_C\), and the constants are computed from reduced determinant finite
parts in the boundary blocks.  The internal product-tail argument gives the
Frobenius-class asymptotic
\[
  P_C(N)=K_C^{\mathrm{ren}} N^{-\delta_C}(1+O(N^{-1})).
\]
When the only boundary collision is the scalar trivial channel this reduces
to the strict-gap specialization with exponent \(|C|/|G|\).  Classical
Frobenius-class product theorems are cited only to compare the same exponent
and conventions in that specialization.  In the general boundary case the
non-trivial Fourier coordinates are given by the renormalised fixed-label
Euler transform
\[
  F_\varrho^{\mathrm{ren}}
  =\lim_{t\uparrow1}
    \left(F_\varrho(t\lambda^{-1})+s_\varrho\log(1-t)\right),
\]
not by the periodic Artin logarithm \(\mathcal L_\varrho^{\mathrm{ren}}\).
Replacing \(F_\varrho^{\mathrm{ren}}\) by
\(\mathcal L_\varrho^{\mathrm{ren}}\) is already false in the finite
\(S_3\) model of \Cref{app:s3-model-computations}.  The contribution is
therefore the Perron-boundary Adams-corrected fixed-label determinant formula
for \(K_C^{\mathrm{ren}}\), its strict-gap recovery, and the obstruction to
Artin substitution.  The scalar, abelian-shadow, and cyclic-lift results later
in the paper supply the projection and normalization data used by this formula.

The finite-group argument is organized around the inverse theorem, its
forward determinant inversion, and one Perron-boundary package with a
strict-gap specialization.  The theorem summary below gives the formulas;
\Cref{subsec:scope-normalizations-support} records the boundary data, branch
normalization, scalar convention, and supporting sections in one place.  The
class transform used throughout is the Hermitian character-orthogonality
convention:
\[
  \chi_\varrho^\ast(C)
  :=\overline{\chi_\varrho(C)}
  =\chi_\varrho(C^{-1})
\]
for unitary characters, while inverse readout of a non-trivial Fourier
coordinate uses \(\chi_\varrho(C)\) without the star.

\subsection{Finite-group main results}

Let \(\tau:E_A\to G\) be a cocycle into a finite group, and let
\(A_\varrho\) be the twisted transition matrices. For a class function
\(\phi:G\to\CC\) write
\[
  T_{\phi,n}:=\sum_{x\in\operatorname{Fix}(\sigma^n)}
    \phi(\tau_n(x)),
  \qquad
  \mathcal L_\phi(z):=\sum_{n\ge 1}\frac{T_{\phi,n}}{n}z^n,
\]
and
\[
  \Pi_\phi(z):=\sum_{\gamma\in\cP}\phi(\tau(\gamma))z^{|\gamma|}.
\]
For a conjugacy class \(C\subset G\), let \(p_{n,C}\) denote the number
of primitive orbits of length \(n\) whose cocycle label lies in \(C\).
The scalar orbit-Mertens constant \(\mathcal M(A)\) is the
trivial-channel constant defined in \Cref{def:abel-finite-part}; the
  finite-matrix formula behind this normalisation is the calculation in
  \Cref{lem:finite-part-primitive-closed-form}.

\subsection{Contributions}\label{sec:intro-scope-of-results}

The paper solves the following finite-state problem.  For a fixed matrix
extension \((A,\tau,G)\), determine from the open-disc twisted determinant
family
\(\{\det(I-zA_\varrho):\varrho\in\Irr(G)\}\), and from the
branch-normalized Perron-boundary coordinate package
\(\mathfrak D_\partial^{\mathrm{ren}}(A,\tau;G)\) when boundary values are
needed, the primitive Peter--Weyl coordinates at the Perron point and hence
the constants in conjugacy-class Mertens sums and Frobenius-class Euler
products.

\begin{remark}[Operational form of the gap hypothesis]
  \label{rem:intro-operational-gap}
  If \(G\) is trivial we set \(\eta=0\).  Otherwise the strict twisted
  spectral gap is the finite check
  \(
    \eta=\max_{\varrho\ne\mathbf1}\rho(A_\varrho)<\lambda
  \).
  Equivalently, by \Cref{lem:twisted-gap-skew-product-peripheral}, the
  skew-product adjacency matrix \(\widetilde A\) has peripheral spectral
  multiset exactly the single simple eigenvalue \(\lambda\); when
  \(\widetilde A\) is irreducible this is the same as primitivity of
  \(\widetilde A\).  This condition is not automatic from primitivity of
  \(A\).  It is a separate finite spectral exclusion on the cocycle.
\end{remark}

The following summary records the primary formulas in one place before the
proofs.
\begin{theoremsummary}[Headline inverse theorem and forward Theorems A--B]
  \summaryrefstep\label{stmt:main-theorem-spine}
  Let \(A\) be the primitive non-negative integer adjacency matrix of a finite
  directed multigraph and let \(\tau:E_A\to G\) be a finite-group edge cocycle.

  \emph{Headline Main Theorem: twisted-determinant inverse rigidity.}
  On Liv\v{s}ic cohomology classes, the fiber of
  \[
    [\tau]_{\mathrm{Liv}}\longmapsto
    \bigl(\det(I-zB_\varrho(\tau))\bigr)_{\varrho\in\Irr(G)}
  \]
  is exactly equality of all unmarked primitive counts \(p_{n,C}\).
  Continuous Liv\v{s}ic cohomology between one-step edge cocycles is exactly
  vertex-gauge cohomology, and the determinant fiber cardinality is the
  intrinsic computable integer \(\mu_{\Gamma,G}\) on
  \(\operatorname{Hom}(\pi_1(|\Gamma|),G)/G\).  Hence rigidity holds if and
  only if \(\mu_{\Gamma,G}=1\).  For an \(m\)-loop bouquet and finite abelian
  \(G\),
  \[
    \mu_{\Gamma,G}=\frac{m!}{\prod_{g\in G}n_g!};
  \]
  in particular, every primitive non-trivial abelian bouquet extension is
  non-rigid.  If all paired blocks are semisimple, determinant equivalence is
  independent similarity in every irreducible channel; on an edge-resolving
  graph a common vertex-compatible spectral-projector certificate promotes
  those similarities to one vertex gauge.  Without marked compatibility,
  injectivity fails even for mixing skew products: over the full two-shift the
  \(\ZZ/2\ZZ\)-cocycles \((0,1)\) and \((1,0)\) have determinant family
  \((1-2z,1)\) and are not Liv\v{s}ic cohomologous.  No counterexample has a
  smaller non-trivial group or fewer named edges.

  \emph{Main Theorem A: open-disc Adams--M\"obius determinant inversion.}
  For every class function \(\phi\), the primitive series is recovered on
  \(|z|<\lambda^{-1}\) by
  \[
    \Pi_\phi(z)=
    \sum_{m\ge1}\frac{\mu(m)}{m}\,
      \mathcal L_{\psi^m\phi}(z^m),
    \qquad
    \mathcal L_\phi(z)=
    \sum_{\varrho\in\Irr(G)}\widehat\phi(\varrho)
      \log\det(I-zA_\varrho)^{-1}.
  \]
  Equivalently the primitive class counts are
  \[
    p_{n,C}=
    \frac{|C|}{|G|\,n}
    \sum_{\varrho\in\Irr(G)}\chi_\varrho^\ast(C)
    \sum_{m\mid n}\mu(m)
      \sum_{\pi\in\Irr(G)}a_{\varrho,\pi}^{(m)}
        \Tr(A_\pi^{n/m}).
  \]
  \emph{Main Theorem B: Perron-boundary renormalized class and product
  theorem.}
  The Perron-boundary theorem replaces the strict exclusion of boundary
  eigenvalues by the finite data
  \[
    \Gamma_\pi,\quad m_{\pi,\zeta},\quad s_\pi=m_{\pi,1},\quad
    \Lambda_\perp,\quad \Lambda_\partial .
  \]
  With these algebraic multiplicities,
  \[
    c_{n,\varrho}(A,\tau)
    =
    \frac{\lambda^n}{n}
      \sum_{\zeta\in\Gamma_\varrho}
        m_{\varrho,\zeta}\zeta^n
    +O(\Lambda_\partial^n/n).
  \]
  Hence
  \[
    \sum_{n\le N}\frac{p_{n,C}}{\lambda^n}
    =
    \delta_C\log N+M_C^{\mathrm{ren}}+O(N^{-1}),
  \]
  with
  \[
    \delta_C=
    \frac{|C|}{|G|}
      \sum_{\varrho\in\Irr(G)}\chi_\varrho^\ast(C)s_\varrho,
    \qquad
    M_C^{\mathrm{ren}}=
    \frac{|C|}{|G|}
      \sum_{\varrho\in\Irr(G)}
        \chi_\varrho^\ast(C)(s_\varrho\gamma+R_\varrho).
  \]
  The fixed-label Euler transform is renormalized by
  \[
    F_\varrho^{\mathrm{ren}}
    =
    \lim_{t\uparrow1}
      \left(F_\varrho(t\lambda^{-1})+s_\varrho\log(1-t)\right),
  \]
  and is evaluated by the reduced determinant formula
  \[
    F_\varrho^{\mathrm{ren}}
    =
    \sum_{r,m\ge1}\frac{\mu(m)}{rm}
      \sum_{\pi\in\Irr(G)}
        a_{\varrho,\pi}^{(m)}
        B_{rm,\pi}^{\mathrm{ren}},
  \]
  where \(k=1\) terms are reduced Perron-boundary finite parts and
  \(k\ge2\) terms are ordinary interior determinant logarithms.  The
  Frobenius-class product theorem is
  \[
    P_C(N)=K_C^{\mathrm{ren}}\,N^{-\delta_C}\bigl(1+O(N^{-1})\bigr),
  \]
  where
  \[
    K_C^{\mathrm{ren}}
    =
    \exp\!\left(
      -\frac{|C|}{|G|}
        \sum_{\varrho\in\Irr(G)}
          \chi_\varrho^\ast(C)
          (s_\varrho\gamma+F_\varrho^{\mathrm{ren}})
    \right).
  \]
  \emph{Corollary C: strict-gap specialization.}
  The strict-gap formula with exponent \(|C|/|G|\) is recovered when
  \(s_{\mathbf1}=1\), \(s_\pi=0\) for \(\pi\ne\mathbf1\), and no non-trivial
  peripheral phase occurs.  In that case
  \(\delta_C=|C|/|G|\) and
  \(F_\rho^{\mathrm{ren}}=F_\rho(\lambda^{-1})\) for every non-trivial
  \(\rho\).

  \emph{Corollary D: product-coordinate inversion and Artin obstruction.}
  The product logarithms give the shifted coordinates
  \(s_\pi\gamma+F_\pi^{\mathrm{ren}}\):
  \[
    H_C^{\mathrm{ren}}:=
    -\frac{|G|}{|C|}\log K_C^{\mathrm{ren}}
    =
    \sum_\pi \chi_\pi^\ast(C)(s_\pi\gamma+F_\pi^{\mathrm{ren}}),
  \]
  while the exponent profile gives the boundary multiplicities by the
  finite Fourier inversion
  \[
    s_\pi=\sum_C\delta_C\chi_\pi(C).
  \]
  Consequently \((K_C^{\mathrm{ren}})_C\) together with
  \((\delta_C)_C\), equivalently \((s_\pi)_\pi\), recovers the reduced
  fixed-label coordinates:
  \[
    F_\pi^{\mathrm{ren}}
    =
    \sum_C\frac{|C|}{|G|}
      H_C^{\mathrm{ren}}\chi_\pi(C)-s_\pi\gamma .
  \]
  Thus the pair
  \(\bigl((K_C^{\mathrm{ren}})_C,(\delta_C)_C\bigr)\), equivalently
  \(\bigl((K_C^{\mathrm{ren}})_C,(s_\pi)_\pi\bigr)\), is the lossless
  product-coordinate readout for the reduced fixed-label vector after the
  positive logarithm branch is fixed.  Positive product constants without
  \((\delta_C)_C\) recover only the shifted vector
  \(s_\pi\gamma+F_\pi^{\mathrm{ren}}\).  The comparison with periodic Artin
  logarithms is the obstruction statement of
  \Cref{thm:renormalized-artin-obstruction}.
\end{theoremsummary}
The inverse headline is proved in
\Cref{thm:inverse-rigidity-main,thm:sharp-spectral-cohomology-classification,thm:inverse-rigidity-under-certificate,prop:minimal-twisted-determinant-counterexample}.
The forward summary is proved by the Perron-boundary theorem
\Cref{thm:perron-boundary-determinant-product}, the strict-gap specialization
\Cref{cor:strict-gap-specialization}, the product-coordinate readout
\Cref{cor:renormalized-coordinate-readout}, and the renormalized Artin
obstruction \Cref{thm:renormalized-artin-obstruction}.  The finite \(S_3\)
rational bounds later give a concrete example of the obstruction.

\subsection{Normalizations and dependency spine}
\label{subsec:scope-normalizations-support}

The main object is a fixed finite SFT cocycle \((A,\tau,G)\).  The headline
inverse theorem is \Cref{thm:inverse-rigidity-main}; its exact intrinsic
classification, positive certificate, and minimal obstruction are
\Cref{thm:sharp-spectral-cohomology-classification,thm:inverse-rigidity-under-certificate,prop:minimal-twisted-determinant-counterexample}.
Main Theorem~A is the open-disc Adams--M\"obius primitive inversion assembled in
\Cref{prop:main-theorem-i-assembly-map}.  Main Theorem~B is the
Perron-boundary determinant and product theorem
\Cref{thm:perron-boundary-determinant-product}.  Corollary~C is the
strict-gap specialization \Cref{cor:strict-gap-specialization}.  Corollary~D
is the product-coordinate inversion and Artin-substitution obstruction
\Cref{cor:renormalized-coordinate-readout,thm:renormalized-artin-obstruction}.
At the Perron boundary the input is the branch-normalized finite tuple
\(\mathfrak D_\partial^{\mathrm{ren}}(A,\tau;G)\): determinant polynomials,
class-power maps, peripheral phases and multiplicities, and the explicit Abel
branches \(\{\Log_{\pi,\zeta}\}\) and \(\{\Log_{\partial,\pi}\}\).  The reduced
determinant finite parts \(B_{k,\pi}^{\mathrm{ren}}\), fixed-label coordinates
\(F_\pi^{\mathrm{ren}}\), exponents \(\delta_C\), and product constants
\(K_C^{\mathrm{ren}}\) are outputs of that tuple.

The product asymptotic is derived from the renormalized class-Mertens estimate
and the explicit product-tail reduction.  Its exponent is
\(\delta_C\), and its positive constant is the Hermitian class Fourier
transform of
\((s_\varrho\gamma+F_\varrho^{\mathrm{ren}})_\varrho\).  Scalar finite parts
fix the trivial-channel normalization; the eta-gap diagnostic identifies the
strict-gap branch; abelian and cyclic shadow statements are projection facts;
and the \(S_3\) rational certificates are exact finite witnesses for the
Adams correction and for the failure of replacing
\(F_\varrho^{\mathrm{ren}}\) by
\(\mathcal L_\varrho^{\mathrm{ren}}\).

For the finite-group theorems, \(A\) is the primitive integer adjacency matrix
of a finite directed multigraph.  Only the scalar normalisation material allows
an arbitrary primitive non-negative real matrix, where \(p_n(A)\) denotes the
algebraic M\"obius coefficient.  The scalar finite part enters only through the
trivial representation channel.  The cyclic root-of-unity layer is attached to
the canonical cyclic lift, not to arbitrary cyclic quotients of the fixed
cocycle.

\begin{center}
\begin{tabular}{L{0.20\textwidth}L{0.38\textwidth}L{0.34\textwidth}}
  \toprule
  \textbf{Result here} & \textbf{Exact classical antecedents} & \textbf{Difference in this paper}\\
  \midrule
  Inverse-rigidity headline & Liv\v{s}ic periodic-data and finite-group
  extension theory
  \cite{Parry1999LivsicNonAbelian,ParryPollicott1997LivsicCompactLie,BoyleSchmieding2017FiniteGroupExtensions}
  & The exact obstruction is the computable full-Wedderburn cohomology-fiber
  multiplicity.  It has a sharp multinomial formula for finite abelian
  bouquets; a common spectral-projector certificate remains a sufficient
  witness, while fixed primitive examples prove that Perron-peripheral data,
  mixing, and semisimplicity alone do not suffice.\\[0.5ex]
  Main Theorem~A & Finite-extension and graph-Artin zeta factorizations of
  Fried, Parry--Pollicott, Hashimoto, Stark--Terras, and Noorani--Parry
  \cite{Fried1983PeriodicPointsTwistedCoefficients,ParryPollicott1990Zeta,Hashimoto1989ZetaFiniteGraphs,StarkTerras1996FiniteGraphsCoverings,NooraniParry1992ChebotarevShifts}
  & Primitive labels are extracted by Adams--M\"obius inversion in the full
  finite class-function ring, not only by periodic trace factorisation.\\[0.5ex]
  Perron-boundary Mertens theorem & Chebotarev orbit-counting and Frobenius-class Mertens
  estimates for finite extensions and hyperbolic systems
  \cite{ParryPollicott1986Chebotarev,NooraniParry1992ChebotarevShifts,MeslizaNoorani1999FrobeniusMertens}
  & The theorem keeps all Perron-boundary phases, gives \(\delta_C\) and
  \(M_C^{\mathrm{ren}}\), and recovers \(\eta<\lambda\) only as the
  scalar-collision specialization.\\[0.5ex]
  Renormalized Frobenius product & Frobenius-class Mertens products and
  closed-orbit Mertens normalisations of Mesliza--Noorani and Sharp
  \cite{MeslizaNoorani1999FrobeniusMertens,Sharp1991Mertens}
  & The invariant is the Adams-corrected reduced fixed-label product constant
  vector \(F_\pi^{\mathrm{ren}}\) and the constants
  \(K_C^{\mathrm{ren}}\), computed from the branch-dependent finite boundary
  datum rather than from ordinary periodic Artin logarithms.\\
  \bottomrule
\end{tabular}
\end{center}

\begin{center}
\begin{tabular}{L{0.22\textwidth}L{0.34\textwidth}L{0.36\textwidth}}
  \toprule
  \textbf{Adjacent topic} & \textbf{Recent comparison work} & \textbf{Role here}\\
  \midrule
  Finite group actions and \(G\)-SFT classification &
  Adler--Kitchens--Marcus, Fiebig, Silver--Williams, Boyle--Schmieding,
  Boyle--Carlsen--Eilers, Matsumoto, and Epperlein
  \cite{AdlerKitchensMarcus1985FiniteGroupActions,Fiebig1993PeriodicFiniteGroupActions,SilverWilliams2005InvariantFiniteGroupActions,BoyleSchmieding2017FiniteGroupExtensions,BoyleCarlsenEilers2020FlowEquivalenceGSFT,Matsumoto2016ExtensionsSubshiftsFiniteGroups,Epperlein2026FreeInertGSFTs}
  & These works locate finite-group extensions and equivariant symbolic
  invariants.  They are not used to prove the Adams--M\"obius determinant
  readout or the Perron-boundary product constants.\\[0.5ex]
  Group languages, noncommutative skew products, and growth on extensions &
  Berth\'e--Goulet-Ouellet--Perrin, Berth\'e--Goulet-Ouellet--Nyberg-Brodda--Perrin--Petersen,
  Crismale--Del Vecchio--Griseta--Rossi, and Dougall--Sharp
  \cite{BertheGouletOuelletPerrin2025RationalLanguagesDensity,BertheGouletOuelletNybergBroddaPerrinPetersen2026GroupLanguages,CrismaleDelVecchioGrisetaRossi2025NoncommutativeSkewProduct,DougallSharp2021AnosovGrowthGroupExtensions}
  & These provide modern context for labelled languages, skew products, covers,
  and growth-rate questions.  The present theorem is instead a finite
  determinant-to-product-coordinate formula for a fixed cocycle.\\[0.5ex]
  Finite-data, resonance, and periodic-length rigidity &
  Coles, Humbert, O'Hare, De Jong, and Nordin--Noorani--Mohd
  \cite{Coles2025VassilievWritheAxiomA,Humbert2025FirstRuelleResonance,OHare2025FiniteDataRigidity,DeJong2026PeriodicOrbitLengths,NordinNoorani2021BouquetDyckOrbitGrowth,NordinNooraniMohd2024OrbitGrowthSoficPFT,NordinNooraniMohd2025SoficDyckOrbitGrowth}
  & These are comparison-only citations for neighbouring finite-data and
  orbit-growth problems.  They do not supply hypotheses, branch conventions,
  or proof inputs for Main Theorem~B.\\
  \bottomrule
\end{tabular}
\end{center}

The later sections use this coordinate package in three ways: strict-gap
specialization of \Cref{thm:perron-boundary-determinant-product}, projection
boundaries in
\Cref{thm:abelian-shadow-defect,thm:cyclic-detection-boundary,thm:finite-part-scalar-interface-no-descent},
and the \(S_3\) witness
\Cref{thm:s3-certified-nonabelian-defect}.
