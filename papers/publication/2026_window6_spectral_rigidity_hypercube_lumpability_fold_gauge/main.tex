\documentclass[12pt,a4paper]{article}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{enumitem}
\usepackage{fvextra}
\usepackage[hidelinks]{hyperref}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{microtype}
\usepackage{geometry}
\geometry{margin=2.8cm}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\numberwithin{equation}{section}
\DeclareMathOperator{\Spec}{Spec}
\newcommand{\Fold}{\operatorname{Fold}}
\newcommand{\CC}{\mathbb C}
\newcommand{\RR}{\mathbb R}
\newcommand{\ZZ}{\mathbb Z}
\newcommand{\card}[1]{\lvert #1\rvert}

\title{Involution-Admissible Zeckendorf Folds: Dimension Classification and the
6-Cube Refinement}
\author{Haobo Ma\thanks{AELF PTE LTD., Singapore. Email: \texttt{auric@aelf.io}}
  \and Wenlin Zhang\thanks{National University of Singapore, Singapore. Email: \texttt{e1327962@u.nus.edu}}
}
\date{}

\begin{document}
\maketitle

\begin{abstract}
We classify the dimensions in which the finite Zeckendorf-prefix fold \(\Fold_m\) of the
\(m\)-dimensional hypercube is preserved by one of the affine coordinate involutions
\(\sigma_{i,j}^{(m)}\).  For \(m\ge3\), the involution-admissible dimensions are exactly
\(m\in\{3,6,8,9\}\).  The proof combines a fold-preservation criterion established here
with the complete effective binary-digit theorem of Bugeaud, Cipu, and Mignotte that the
only Fibonacci numbers expressible as sums of two distinct powers of two are
\(F_4=3\), \(F_5=5\), \(F_9=34\), and \(F_{12}=144\).  In each admissible dimension the
corresponding involution has an equitable orbit partition with \(2^{m-1}\) singleton cells
and \(2^{m-2}\) two-element cells, hence \(3\cdot2^{m-2}\) cells in total: respectively
\(6,48,192,384\).

As the worked case, we analyze \(m=6\) completely.  The \(21\)-cell \(\Fold_6\)-partition
is not equitable, and its unique coarsest equitable refinement is the \(48\)-cell orbit
partition, with \(32\) singletons and \(16\) pairs.  Consequently every equitable hidden
realization through which \(\Fold_6\) factors has at least \(48\) states, with equality
unique up to relabeling.  The quotient adjacency spectrum at eigenvalues
\(6,4,2,0,-2,-4,-6\) has multiplicities \((1,5,11,14,11,5,1)\), while the discarded
\(16\)-dimensional sector carries the adjacency operator of \(Q_4\).  The dimension
classification is complete for the stated involutions and rests on the imported effective
Diophantine theorem; we do not claim a classification of arbitrary equitable refinements
in all dimensions.  The accompanying supplement supplies the finite data needed to
recompute the finite claims.
\end{abstract}

\section{Introduction}\label{sec:introduction}
Equitable partitions are the exact combinatorial form of lumpability for a random walk:
each vertex in a cell must have the same number of neighbors in every target cell.
In the Hamming-graph and hypercube literature, an equitable partition is also called a
\emph{perfect colouring} \cite{fonderflaass2007perfect,vasileva2019local}.
They are central in algebraic graph theory, color refinement, and finite Markov-chain
reduction \cite{biggs1993algebraic,kemeny1976finite,buchholz1994exact,krebs2015universal}.
The hypercube is a particularly transparent test graph because its Walsh characters give
the complete spectrum and because every local neighbor count is integral.

The six-dimensional partition belongs to a sparse dimension-dependent phenomenon.  To
state it, for \(a=(a_1,\ldots,a_m)\in\Omega_m\) put
\[
 N_m(a)=\sum_{r=1}^m a_r2^{m-r},
 \qquad
 \Fold_m(a)=(z_1(N_m(a)),\ldots,z_m(N_m(a))),
\]
where \(N=\sum_{r\ge1}z_r(N)F_{r+1}\) is the greedy Zeckendorf expansion.  For
\(1\le i<j\le m\), let \(\sigma_{i,j}^{(m)}\) interchange coordinates \(i,j\) and
complement both:
\[
 (\sigma_{i,j}^{(m)}a)_i=1-a_j,
 \qquad (\sigma_{i,j}^{(m)}a)_j=1-a_i,
 \qquad (\sigma_{i,j}^{(m)}a)_r=a_r\quad(r\ne i,j).
\]
Thus \(00\) and \(11\) are exchanged in the selected coordinates, whereas \(01\) and
\(10\) are fixed.  Call \(m\)
\emph{involution-admissible} when \(\Fold_m\sigma_{i,j}^{(m)}=\Fold_m\) for some pair
\(i<j\).

\begin{proposition}[Characterisation of involution-admissible dimensions]
\label{prop:admissible-dimensions}
Put \(S_{i,j}=2^{m-i}+2^{m-j}\).  If \(S_{i,j}=F_k\), then
\(\Fold_m\sigma_{i,j}^{(m)}=\Fold_m\) if and only if
\[
 k-1>m
 \quad\text{and}\quad
 \bigl(k-2>m\ \text{ or }\ z_{k-2}(N)=0\text{ for every admissible }N\bigr),
\]
where \(0\le N<2^m\) is admissible when its binary digits of weights \(2^{m-i}\) and
\(2^{m-j}\) are both zero.  If \(S_{i,j}\) is not a Fibonacci number, the
involution does not preserve \(\Fold_m\).

For the classification below we import the complete binary-digit theorem of
Bugeaud, Cipu, and Mignotte~\cite{bugeaud2013representation}; it implies that the
only Fibonacci numbers that are sums of two distinct powers of two are
\[
 F_4=3,\qquad F_5=5,\qquad F_9=34,\qquad F_{12}=144.
\]
Then the involution-admissible dimensions \(m\ge3\) are exactly
\[
 m\in\{3,6,8,9\}.
\]
The preserving involutions at those four dimensions are, respectively,
\[
 \sigma_{1,3}^{(3)},\qquad \sigma_{1,5}^{(6)},\qquad
 \sigma_{1,4}^{(8)},\qquad \sigma_{2,5}^{(9)}.
\]
Independently, an exhaustive neighbor-signature computation for \(3\le m\le16\)
establishes that the coarsest equitable refinement of the \(\Fold_m\)-partition is
discrete outside these four dimensions and is the corresponding orbit partition at
them.  Each such partition has \(2^{m-1}\) singleton cells and \(2^{m-2}\) two-element
cells.  Its discarded sector has dimension \(2^{m-2}\), and the restriction of \(A_m\)
to that sector is isomorphic to \(A_{m-2}\).
\end{proposition}

\begin{proof}
The involution changes the binary value by \(+S_{i,j}\) on \(00\), by
\(-S_{i,j}\) on \(11\), and by zero on \(01,10\).  The uniqueness and normalization
rules for greedy Zeckendorf expansion force a fold-invisible nonzero increment of this
form to be a single Fibonacci number, say \(S_{i,j}=F_k\).  It is enough to consider
the \(00\) inputs, which are precisely the admissible integers \(N\): the corresponding
\(11\) input has value \(N+F_k\), and the other two patterns are fixed.

Adding \(F_k\) places a new Zeckendorf digit at position \(k-1\).  This digit is
invisible exactly when \(k-1>m\).  If the Zeckendorf expansion of \(N\) already has a
digit at position \(k-2\), the new digit is adjacent to it and the normalization
\[
 F_{k-1}+F_k=F_{k+1}
\]
consumes the digit at position \(k-2\); the normalization rules introduce no other
change below that position.  Thus the first \(m\) digits remain unchanged for every
admissible \(N\)
exactly when the new digit lies above the window and either the consumed position also
lies above the window or no admissible \(N\) occupies it.  This proves the stated
criterion, including necessity by taking \(N=0\) for the first condition and an
admissible \(N\) with \(z_{k-2}(N)=1\) for the second.

The list of four Fibonacci numbers is not proved here; it is the specialization to
exactly two nonzero binary digits of the complete classification in
Bugeaud--Cipu--Mignotte~\cite{bugeaud2013representation}.  Write
\(F_k=2^a+2^b\), with \(a>b\).  The coordinates are forced to be \(i=m-a\) and
\(j=m-b\), so \(m\ge a+1\); the first condition gives \(m\le k-2\), and the second
normally gives \(m\le k-3\).  Applying the criterion to the imported list gives
\[
\begin{array}{c|c|c}
\text{weight sum}&(i,j)&\text{admissible }m\ge3\\ \hline
F_4=3&(m-1,m)&\varnothing\\
F_5=5&(m-2,m)&3\\
F_9=34&(m-5,m-1)&6\\
F_{12}=144&(m-7,m-4)&8,9.
\end{array}
\]
The exceptional branch of the second condition occurs only for \(F_5\) at \(m=3\):
the admissible class is \(\{0,2\}\), and neither integer has a digit at position
\(k-2=3\).  For \(F_9\), dimension six survives because position \(k-2=7\) lies above
the window, whereas at \(m=7\) the admissible integer \(N=21=F_8\) occupies that now
visible position.  For \(F_{12}\), dimensions eight and nine survive, whereas at
\(m=10\) the admissible integer \(N=97=F_{11}+F_6\) occupies position \(k-2=10\).
All larger dimensions are excluded by the first condition.  This proves the global
classification of the stated involutions from the criterion and the imported
Diophantine list.  Separately, iterated neighbor-signature refinement over all vertices
for \(3\le m\le16\) gives the discrete partition outside these four dimensions and the
listed orbit partitions at them.

It remains to identify the uniform structure, which does not require computation.
Each \(\sigma_{i,j}^{(m)}\) is a cube automorphism.  It has \(2^{m-1}\) fixed points
(the selected coordinates are \(01\) or \(10\)) and \(2^{m-2}\) two-element orbits
(pairing \(00\) with \(11\)).  Its orbit partition is therefore equitable.  On an
antisymmetric orbit difference, the two selected cube directions cancel, while the
other \(m-2\) flips act as the coordinate flips of \(Q_{m-2}\).  Hence the discarded
space has dimension \(2^{m-2}\) and carries \(A_{m-2}\).
\end{proof}

The criterion is proved here, while the global list of involution-admissible dimensions
depends on the cited Diophantine scarcity theorem rather than on the endpoint of a
search.  The stronger statement about the full coarsest equitable refinement remains a finite
computation for \(3\le m\le16\); no classification of arbitrary equitable refinements is
asserted beyond that range.

Dimension six is therefore neither a minimal obstruction nor an isolated computation:
the first non-equitable case is \(m=3\), while \(m=6\) is one of the four
involution-admissible dimensions.  We focus on it because it is the audited
instance for which a complete 48-cell classification can be printed.  Its involution
comes from \(F_9=34=2^5+2^1\), and the general cancellation argument above explains
why its discarded sector is \(Q_4\).  The resulting classification determines an exact
state complexity for graph quotients: any exact Markov lumping retaining the 21 visible
labels needs at least 48 states.

\paragraph{Combinatorial classification (Theorem~\ref{thm:headline}).}
If \(H:\Omega_6\twoheadrightarrow Y\) has equitable fibers and
\(\Fold_6=\rho\circ H\) for a surjection \(\rho:Y\twoheadrightarrow X_6\), then
\(\card{Y}\ge48\).  Equality is attained uniquely up to relabeling by the orbit map
of \(\sigma_{\rm geo}(a_1,\ldots,a_6)=(1-a_5,a_2,a_3,a_4,1-a_1,a_6)\).

\paragraph{Spectral structure (Lemma~\ref{lem:spectral-carrier}).}
For this unique 48-state quotient, the spectrum has multiplicities
\((1,5,11,14,11,5,1)\), and the orthogonal complement of its function space carries
the adjacency operator of the 4-cube.

The proof separates two questions.  Section~\ref{sec:fold6} fixes the finite partition
and records its fibers.  Section~\ref{sec:residual} gives the elementary interval
criterion for approximate intertwinings and the sharp Fold$_6$ residual.  Theorem
\ref{thm:headline} is proved in Section~\ref{sec:repair} by printing all 48
neighbor-signature cells.  Section~\ref{sec:spectrum} then derives the quotient
spectrum and the discarded \(Q_4\) carrier.  The final section describes the independent
finite checks for both the six-dimensional classification and the dimension sweep.

\section{The Fold$_6$ partition}\label{sec:fold6}
Let \(Q_m\) be the hypercube on \(\Omega_m=\{0,1\}^m\), with adjacency matrix
\(A_m\) and walk matrix \(T_m=A_m/m\).  A surjection \(F:\Omega_m\twoheadrightarrow X\)
has block-indicator matrix \(M\), whose column indexed by \(x\) is the indicator of
\(F^{-1}(x)\).  The partition is equitable exactly when \(A_mM=MB\) for some \(B\),
or equivalently when every vertex in a source fiber has the same number of neighbors
in each target fiber.

\begin{definition}[Audited Fold$_6$]\label{def:fold6}
Set
\[
X_6=\{x\in\{0,1\}^6:x_ix_{i+1}=0\ (1\le i<6)\}.
\]
For \(\omega=(a_1,\ldots,a_6)\), put
\(N(\omega)=\sum_{i=1}^6a_i2^{6-i}\).  If
\(N=\sum_{r\ge1}z_r(N)F_{r+1}\) is its greedy Zeckendorf expansion, define
\(\Fold_6(\omega)=(z_1(N),\ldots,z_6(N))\).  The fibers, in lexicographic order of
\(X_6\), are the following.
\[
\begin{array}{c|l}
000000&000000,010101,100010,110111\\
000001&001101,101111\\
000010&001000,011101,101010,111111\\
000100&000101,011010,100111,111100\\
000101&010010,110100\\
001000&000011,011000,100101,111010\\
001001&010000,110010\\
001010&001011,100000,101101\\
010000&000010,010111,100100,111001\\
010001&001111,110001\\
010010&001010,011111,101100\\
\end{array}
\]
\[
\begin{array}{c|l}
010100&000111,011100,101001,111110\\
010101&010100,110110\\
100000&000001,010110,100011,111000\\
100001&001110,110000\\
100010&001001,011110,101011\\
100100&000110,011011,101000,111101\\
100101&010011,110101\\
101000&000100,011001,100110,111011\\
101001&010001,110011\\
101010&001100,100001,101110
\end{array}
\]
\end{definition}

The fiber-size vector is
\[
(4,2,4,4,2,4,2,3,4,2,3,4,2,4,2,3,4,2,4,2,3).
\]
The count can be obtained without enumerating binary words one by one.  If
\(V(x)=\sum_{r=1}^6x_rF_{r+1}\), let \(\mathcal T_7^{(0)}=\mathcal T_7^{(1)}=\{0\}\)
and, working backwards through the remaining Zeckendorf positions, set
\[
\mathcal T_r^{(1)}=\mathcal T_{r+1}^{(0)},\qquad
\mathcal T_r^{(0)}=\mathcal T_{r+1}^{(0)}\cup
 (F_{r+1}+\mathcal T_{r+1}^{(1)}).
\]
Then
\[
\Fold_6^{-1}(x)=\{\omega:N(\omega)-V(x)\in\mathcal T_7^{(x_6)}\},
\]
intersected with \([0,63]\).  This tail recursion is useful because the same finite sets
also compute every neighbor count used in Section~\ref{sec:residual}.

\begin{proposition}[Finite enumeration of the Fold fibers]\label{prop:fiber-enumeration}
The map in Definition~\ref{def:fold6} is onto \(X_6\), and the displayed 21 rows
partition all 64 vertices.  In particular \(|X_6|=F_8=21\), and the fiber-size vector
is the one displayed above.
\end{proposition}
\begin{proof}
The recurrence for admissible words gives \(|X_m|=F_{m+2}\).  Every legal word \(x\)
has value \(V(x)\le F_8-1=20\), so it occurs as the first six digits of the greedy
expansion of some integer in \([0,63]\).  The tail recursion then gives the listed
preimages.  Their rows are disjoint and their sizes sum to 64, proving the partition
claim.
\end{proof}

The involution in Theorem~\ref{thm:headline} preserves every row of this table.
Indeed, direct substitution into the 64-word definition gives
\(\Fold_6\circ\sigma_{\rm geo}=\Fold_6\).  This identity will also explain the
16-dimensional discarded sector.

\section{Visible nonlumpability and the sharp residual}\label{sec:residual}
For \(\omega\in\Omega_m\) and \(y\in X\), write
\[
c_\omega(y)=\#\{1\le i\le m:F(\omega+e_i)=y\}.
\]
The following elementary lemma is the residual calculation used below.

\begin{lemma}[Neighbor-count intervals and residual radius]\label{lem:residual}
For a finite fold \(F:\Omega_m\twoheadrightarrow X\), a fixed matrix \(P\) satisfies
\(\|T_mM-MP\|_\infty\le\varepsilon\) if and only if, for every \(x,y\in X\),
\[
P(x,y)\in\bigcap_{\omega\in F^{-1}(x)}
\bigl[c_\omega(y)/m-\varepsilon,\ c_\omega(y)/m+\varepsilon\bigr].
\]
Consequently
\[
\inf_{P\in\RR^{X\times X}}\|T_mM-MP\|_\infty
 =\frac{1}{2m}\max_{x,y}\left(\max_{\omega\in F^{-1}(x)}c_\omega(y)-\min_{\omega\in F^{-1}(x)}c_\omega(y)\right).
\]
The same lower bound holds when \(P\) is required to be row-stochastic.
\end{lemma}
\begin{proof}
The \((\omega,y)\)-entry of \(T_mM\) is \(c_\omega(y)/m\), while the corresponding
entry of \(MP\) is \(P(x,y)\) for every \(\omega\in F^{-1}(x)\).  The residual bound
for a fixed \(P\) is therefore exactly the displayed membership condition.  If \(P\) is
allowed to vary over all real matrices, its entries may be chosen independently, so a
feasible \(P\) exists exactly when every displayed intersection is nonempty.  A finite
family of intervals intersects exactly when its largest left endpoint is no larger than
its smallest right endpoint.  The least possible radius is consequently half the
largest normalized count diameter.  Restricting the feasible set to stochastic rows
cannot decrease the infimum.
\end{proof}

\begin{proposition}[Fold$_6$ residual certificate]\label{prop:residual-fold6}
For the partition of Definition~\ref{def:fold6}, the largest neighbor-count diameter is
\(\Delta_6=2\).  Hence the sharp real and row-stochastic residual radii are both
\(1/6\).

The two vertices \(000000\) and \(010101\) lie in the fiber \(000000\), but for
the target \(000100\) their neighbor counts are 0 and 1.  A diameter-attaining example
is the fiber \(100000\) and target \(010101\), where the count list is \((0,2,0,0)\);
the averaged pushforward therefore has residual entry \(\frac16(2-\frac12)=\frac14\).
\end{proposition}
\begin{proof}
Flipping each coordinate in the displayed fiber table gives the finite count lists; the
complete rowwise census is printed in the supplement.  Its maximum is 2, so
Lemma~\ref{lem:residual} gives the unrestricted radius \(2/(2\cdot6)=1/6\).  The list
\((0,2,0,0)\) gives the stated \(1/4\) entry, and the unequal counts in one fiber rule
out exact intertwining.

It remains to prove the upper bound under the stochastic constraint.  At
\(\varepsilon=1/6\), set
\[
L_{xy}=\max\left\{0,\frac{\max_{\omega\in F^{-1}(x)}c_\omega(y)-1}{6}\right\},
\qquad
U_{xy}=\min\left\{1,\frac{\min_{\omega\in F^{-1}(x)}c_\omega(y)+1}{6}\right\}.
\]
Because every count diameter is at most 2, \([L_{xy},U_{xy}]\) is nonempty and is
exactly the admissible interval after imposing \(0\le P(x,y)\le1\).  The same count
enumeration gives the uniform endpoint-sum bounds
\[
\max_{x\in X_6}\sum_{y\in X_6}L_{xy}=\frac12,
\qquad
\min_{x\in X_6}\sum_{y\in X_6}U_{xy}=\frac72.
\]
Hence each coordinate box \(\prod_y[L_{xy},U_{xy}]\) meets the row simplex: start at
the lower endpoints and distribute the remaining mass among the available capacities
\(U_{xy}-L_{xy}\).  Choosing such a row for every \(x\) produces a row-stochastic
matrix with residual at most \(1/6\).  Together with the unrestricted lower bound, this
proves both asserted equalities.
\end{proof}

\section{Unique minimal equitable refinement}\label{sec:repair}
We now prove Theorem~\ref{thm:headline}.  For each vertex define its Fold-neighbor
signature
\[
s(\omega)=\bigl(c_\omega(y)\bigr)_{y\in X_6}.
\]
Refine each visible fiber by equality of these signatures.  Here is one nontrivial row
of the calculation.  In the visible fiber
\[
\Fold_6^{-1}(100000)=\{000001,010110,100011,111000\},
\]
record only the nonzero coordinates of \(s(\omega)\), using a superscript to mark a
count larger than one.  Directly flipping the six bits gives
\[
\begin{array}{c|l}
000001&000000,000100,001000,100010,101001,101010\\
010110&000101,010000,010101^{(2)},100010,100100\\
100011&000000,000100,001000,100010,101001,101010\\
111000&000100,001000^{(2)},010000,100001,100100.
\end{array}
\]
Thus equality of visible signatures splits this fiber as
\[
\{000001,100011\},\qquad \{010110\},\qquad \{111000\}.
\]
The affine involution explains the split: it exchanges \(000001\) and \(100011\), while
fixing \(010110\) and \(111000\).  As a sample refined-target check, take
\(C=\{000000,100010\}\), the first signature cell inside visible state \(000000\).
The numbers of neighbors in \(C\), in the source order above, are \((1,0,1,0)\).
They agree on the paired source cell, while the two remaining source cells are
singletons.  The same calculation for every refined target gives the table below.

The resulting cells are the following; this is the 48-cell table used by the proof.
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{c|l}
Fold state&neighbor-signature cells\\ \hline
000000&\{000000,100010\},\{010101,110111\}\\
000001&\{001101,101111\}\\
000010&\{001000,101010\},\{011101,111111\}\\
000100&\{000101,100111\},\{011010\},\{111100\}\\
000101&\{010010\},\{110100\}\\
001000&\{000011\},\{011000,111010\},\{100101\}\\
001001&\{010000,110010\}\\
001010&\{001011\},\{100000\},\{101101\}\\
010000&\{000010\},\{010111\},\{100100\},\{111001\}\\
010001&\{001111\},\{110001\}\\
010010&\{001010\},\{011111\},\{101100\}\\
010100&\{000111\},\{011100,111110\},\{101001\}\\
010101&\{010100,110110\}\\
100000&\{000001,100011\},\{010110\},\{111000\}\\
100001&\{001110\},\{110000\}\\
100010&\{001001,101011\},\{011110\}\\
100100&\{000110\},\{011011\},\{101000\},\{111101\}\\
100101&\{010011\},\{110101\}\\
101000&\{000100,100110\},\{011001,111011\}\\
101001&\{010001,110011\}\\
101010&\{001100,101110\},\{100001\}
\end{tabular}
\end{center}

\setcounter{theorem}{22}
\begin{theorem}[Unique minimal hidden repair of Fold$_6$]\label{thm:headline}\label{thm:window6-minimal-equitable-refinement}
The signature refinement in the preceding table has 48 cells, is equitable, and is
exactly the orbit partition of \(\sigma_{\rm geo}\).  Every equitable partition refining
the Fold$_6$ fibers refines this 48-cell partition.  Therefore it is the unique coarsest
equitable refinement, and every equitable hidden realization through which Fold$_6$
factors has at least 48 states.
\end{theorem}
\begin{proof}
The identity \(\Fold_6\sigma_{\rm geo}=\Fold_6\) and the fact that \(\sigma_{\rm geo}\)
is a cube automorphism imply \(s(\omega)=s(\sigma_{\rm geo}\omega)\).  Comparing with
the table shows that the signature cells are exactly the orbits: 32 fixed points and
16 two-cycles.  Any orbit partition of a graph automorphism is equitable, since the
automorphism bijects neighbors in each target orbit.

Conversely, let \(\mathcal Q\) be any equitable refinement of the Fold fibers.  Two
vertices in one cell of \(\mathcal Q\) have equal neighbor counts into every cell of
\(\mathcal Q\).  Summing over the cells contained in any Fold fiber gives equal counts
into every visible target, so they have the same signature.  Thus \(\mathcal Q\) refines
the displayed partition.  This proves coarseness, uniqueness, and the lower bound.
\end{proof}

\section{Quotient spectrum and the discarded $Q_4$ carrier}\label{sec:spectrum}
Let \(V=\CC^{\Omega_6}\), and let \(V^\pm\) be the \(\pm1\)-eigenspaces of the
permutation induced by \(\sigma_{\rm geo}\).  The 16 two-cycles give \(\dim V^-=16\).

We first recall the spectral input.  If a partition of \(Q_m\) has block matrix \(M\)
and \(A_mM=MB\), then every eigenvalue of \(B\) is an eigenvalue of \(A_m\): an
eigenvector \(v\) of \(B\) gives the nonzero eigenvector \(Mv\) of \(A_m\).  The Walsh
characters \(\chi_S(\omega)=(-1)^{\sum_{i\in S}\omega_i}\) form an orthogonal basis and
\(A_m\chi_S=(m-2|S|)\chi_S\).  Therefore any equitable quotient walk has eigenvalues
in \(\{1-2k/m:0\le k\le m\}\).  This standard inclusion is used only to interpret
the quotient spectrum below; the minimality argument itself is purely local.

\begin{lemma}[Spectral carrier of the minimal quotient]\label{lem:spectral-carrier}
The antisymmetric space \(V^-\) is invariant under \(A_6\) and
\(A_6|_{V^-}\cong A_4\).  The invariant space \(V^+\), which is the function space of
the 48-cell orbit quotient, has adjacency eigenvalue multiplicities
\[
(1,5,11,14,11,5,1)
\]
at eigenvalues \(6,4,2,0,-2,-4,-6\), respectively.  Hence the quotient walk has
the multiplicities in Theorem~\ref{thm:headline}.
\end{lemma}
\begin{proof}
For each assignment \(u\in\{0,1\}^4\) to coordinates \(2,3,4,6\), let
\(\omega^{00}(u)\) and \(\omega^{11}(u)\) be the vertices with coordinates 1 and 5
equal to 00 and 11.  The 16 differences
\(\mathbf1_{\omega^{00}(u)}-\mathbf1_{\omega^{11}(u)}\) form a basis of \(V^-\).
Flips in coordinates 1 and 5 cancel on each difference; the remaining four flips act
exactly as the four cube directions on \(u\).  This gives the isomorphism with \(A_4\).

For the invariant space, use Walsh characters \(\chi_S(\omega)=(-1)^{\sum_{i\in S}\omega_i}\).
For each \(J\subseteq\{2,3,4,6\}\), the three invariant vectors
\[
\chi_J,\qquad \chi_{J\cup\{1,5\}},\qquad
\chi_{J\cup\{1\}}-\chi_{J\cup\{5\}}
\]
have Walsh degrees \(|J|,|J|+2,|J|+1\).  These \(3\cdot16=48\) vectors are linearly
independent, since their Walsh supports are disjoint across the displayed choices, and
hence they form a basis of the 48-dimensional space \(V^+\).  Thus the multiplicity
polynomial is
\[
(1+q)^4(1+q+q^2)=1+5q+11q^2+14q^3+11q^4+5q^5+q^6.
\]
Dividing the adjacency eigenvalues by six gives the quotient-walk spectrum.
\end{proof}

\section{Final remarks and reproducibility}\label{sec:final}
The result is a finite, exact statement about the audited Fold$_6$ partition.  It does
not claim that every folded hypercube or every pushforward construction has the same
repair.  The proof is independently checkable from the fiber table: recompute the six
flips of each of the 64 vertices, group equal signatures, and then check neighbor counts
between the resulting cells.

The supplement contains the complete canonical streams for the Fold$_6$ fibers, the
directed edge-count matrix, the involution and residual witnesses, the exact determinant
certificate for the visible pushforward, and the scripts
\path{supplement/verify_window6_streams.py} and
\path{artifacts/verify_hidden_refinement.py}.  The first script checks the printed
finite streams and their hashes; the second recomputes the 48 signature cells, compares
them with the \(\sigma_{\rm geo}\)-orbits, and verifies equitability and the 32/16 orbit
size split.  These checks validate the finite data and the stated partition identities;
they do not replace the proofs of the interval lemma or the spectral-carrier argument.

The computational parts of Proposition~\ref{prop:admissible-dimensions} are reproduced
by four scripts in \path{artifacts/}.  The script \path{verify_refinement_family.py}
performs the full refinement sweep through \(m=16\);
\path{verify_involution_mechanism.py} enumerates Fibonacci weight sums below \(F_{90}\)
and their candidate involutions; \path{verify_admissible_dimensions.py} streams the
candidate tests through \(m=22\).

Finally, \path{verify_preservation_criterion.py} compares the proved criterion with
brute force on all 49 Fibonacci-weight candidates for
\(2\le m\le16\), with no disagreement.  These finite checks corroborate, but do not
replace, either the criterion or the cited Diophantine classification.

\bibliographystyle{plain}
\bibliography{references}
\end{document}
