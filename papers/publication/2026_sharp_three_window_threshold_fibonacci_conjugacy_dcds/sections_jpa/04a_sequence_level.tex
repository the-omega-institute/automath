\section{Overlap presentations and the sharp threshold}\label{sec:stab:conjugacy}

The local fibers become a dynamical problem only after overlap is imposed. The basic overlap presentation, sharp injectivity threshold, two-window branch classification, and entropy defect formula are proved in \cite{MaZhang2026Nonlinearity}; this section recalls those ingredients by citation and isolates the DCDS-level phase-transition synthesis in Theorem~\ref{thm:phase-transition}. The terminology for sofic shifts and labeled graph presentations is standard; see \cite{LindMarcus1995,Kitchens1998,Nasu1995}.

\begin{definition}[Sliding stabilized-window process]\label{def:Phi}
See \cite{MaZhang2026Nonlinearity}, Definition~4.1.
\end{definition}

\begin{definition}[The overlap graph]\label{def:overlap-graph}
See \cite{MaZhang2026Nonlinearity}, the definition of the overlap graph immediately before Theorem~4.1.
\end{definition}

\begin{theorem}[Finite-state lift and bounded multiplicity]\label{thm:finite-state-lift}
See \cite{MaZhang2026Nonlinearity}, Theorem~4.1.
\end{theorem}

\begin{remark}
The cited theorem supplies the finite-state lift used below only as an input; the contribution of the present paper is the phase-transition synthesis in Theorem~\ref{thm:phase-transition} and its downstream DCDS interpretation.
\end{remark}

The arithmetic content of the threshold is already visible at $m=3$. The three output residues $r_0,r_1,r_2$ determine the tail bit $a_4$ except for one elementary branch, and that branch is separated by the pair $(r_0,r_2)$. The remaining bits are then recovered from the injective map $(u,v)\mapsto u+2v$ on $\{0,1\}^2$.

\begin{lemma}[Explicit three-window decoder]\label{lem:m3-decoder}
The map
\[
\Phi_{3,3}:\{0,1\}^{5}\longrightarrow X_3^3
\]
is injective.
\end{lemma}

\begin{proof}
Write the raw block as $a_0a_1a_2a_3a_4$ and let
\[
M:=F_5=5,
\qquad
r_t:=\Val_3\bigl(\Fold_3(a_ta_{t+1}a_{t+2})\bigr)\in\{0,1,2,3,4\},
\qquad 0\le t\le 2.
\]
Because $\Val_3|_{X_3}$ is a bijection, the stabilized output triple is equivalent to the residue triple $(r_0,r_1,r_2)$.

Using
\[
r_t\equiv a_t+2a_{t+1}+3a_{t+2}\pmod 5,
\]
set
\[
q:=r_0-r_1-r_2\pmod 5.
\]
The Fibonacci recurrence gives
\[
q\equiv a_0+a_1+2a_4\pmod 5.
\]
Since $a_0+a_1\in\{0,1,2\}$ and $2a_4\in\{0,2\}$, one has:
\[
q\in\{0,1\}\Longrightarrow a_4=0,
\qquad
q\in\{3,4\}\Longrightarrow a_4=1.
\]
Only the case $q=2$ requires an additional branch distinction. Then the two possible triples $(a_0,a_1,a_4)$ are
\[
(1,1,0)\qquad\text{or}\qquad (0,0,1).
\]
The corresponding diagnostic residues are summarized in Table~\ref{tab:m3-decoder}.

\begin{table}[t]
\centering
\caption{Diagnostic residues for the exceptional branch $q=2$ in Lemma~\ref{lem:m3-decoder}.}
\label{tab:m3-decoder}
\begin{tabular}{ccc}
\toprule
$(a_0,a_1,a_4)$ & possible $r_0$ & if $r_0=3$, possible $r_2$ \\
\midrule
$(1,1,0)$ & $\{1,3\}$ & $\{0,2\}$ \\
$(0,0,1)$ & $\{0,3\}$ & $\{1,4\}$ \\
\bottomrule
\end{tabular}
\end{table}

Indeed, in the first branch one has
\[
r_0\equiv 3+3a_2\pmod 5,
\qquad
r_2\equiv a_2+2a_3\pmod 5,
\]
whereas in the second branch
\[
r_0\equiv 3a_2\pmod 5,
\qquad
r_2\equiv a_2+2a_3+3\pmod 5.
\]
Thus $(q,r_0,r_2)$ determines $a_4$ uniquely. Outside $q=2$ this follows from the disjoint residue ranges above. Inside $q=2$, the values $r_0=1$ and $r_0=0$ identify the first and second branch respectively, while the only common value $r_0=3$ is separated by the disjoint alternatives $r_2\in\{0,2\}$ and $r_2\in\{1,4\}$.

Once $a_4$ is known, define
\[
t:=r_2-3a_4\pmod 5.
\]
Then
\[
t\equiv a_2+2a_3\pmod 5.
\]
Because the map
\[
\{0,1\}^2\to\{0,1,2,3\},\qquad (u,v)\mapsto u+2v
\]
is injective, $(a_2,a_3)$ is uniquely determined by $t$. Finally define
\[
u:=r_0-3a_2\pmod 5.
\]
Then
\[
u\equiv a_0+2a_1\pmod 5,
\]
and the same injectivity recovers $(a_0,a_1)$. Hence $a_0a_1a_2a_3a_4$ is uniquely determined by $(r_0,r_1,r_2)$, proving injectivity.
\end{proof}

\begin{theorem}[Sharp three-window threshold]\label{thm:block-invertibility}
See \cite{MaZhang2026Nonlinearity}, Theorem~4.3.
\end{theorem}

\begin{remark}
The cited theorem is the sharp finite-block injectivity result. In the present paper it is used as the threshold input for the phase-transition statement below.
\end{remark}

\begin{corollary}[Explicit sliding-block decoder above threshold]\label{cor:explicit-decoder}
See \cite{MaZhang2026Nonlinearity}, Corollary~4.4.
\end{corollary}

\begin{proposition}[Sharp low-window counterexamples]\label{prop:low-window-counterexamples}
See \cite{MaZhang2026Nonlinearity}, Proposition~4.5.
\end{proposition}

\begin{theorem}[Complete classification of the two-window branch locus]\label{thm:m2-branch-locus}
See \cite{MaZhang2026Nonlinearity}, Theorem~4.6.
\end{theorem}

\begin{corollary}[Exact entropy and KL defect formulas at the two-window threshold]\label{cor:m2-defect-formulas}
See \cite{MaZhang2026Nonlinearity}, Corollary~4.7. The present version also records the boundary convention that, if the source law assigns positive mass to the branch fiber while the comparison law assigns none, both raw and pushed-forward divergences are infinite and no finite defect identity is asserted.
\end{corollary}

\begin{remark}
The added boundary convention is a qualitative endpoint clarification of the same branch-fiber formula in \cite{MaZhang2026Nonlinearity}; it does not change the finite-defect identity on its stated finite range.
\end{remark}

\begin{corollary}[Asymptotic sufficiency off the branch fiber]\label{cor:m2-asymptotic-sufficiency}
See \cite{MaZhang2026Nonlinearity}, Corollary~4.8.
\end{corollary}

\begin{corollary}[Mod-zero invertibility at the two-window threshold]\label{cor:m2-mod-zero}
See \cite{MaZhang2026Nonlinearity}, Corollary~4.9.
\end{corollary}

\begin{corollary}[Full block complexity of the one-sided stabilized image]\label{cor:one-sided-complexity}
Let
\[
p_{Y_m^+}(n):=\#\mathcal L_n(Y_m^+)
\]
denote the length-$n$ word complexity of the one-sided stabilized image
\[
Y_m^+:=\Phi_m^+(\{0,1\}^{\mathbb N}),
\qquad
(\Phi_m^+(a))_t:=\Fold_m(a_t\ldots a_{t+m-1}).
\]
If $m\ge 3$ and $n\ge m$, then
\[
p_{Y_m^+}(n)=2^{n+m-1}.
\]
\end{corollary}

\begin{proof}
The domain of $\Phi_{m,n}$ has cardinality $2^{n+m-1}$. By Theorem~\ref{thm:block-invertibility}, $\Phi_{m,n}$ is injective, so its image has the same cardinality. That image is precisely the set of length-$n$ blocks appearing in $Y_m^+$.
\end{proof}

\begin{corollary}[One-sided stabilized-window conjugacy]\label{cor:one-sided-conjugacy}
See \cite{MaZhang2026Nonlinearity}, Corollary~4.11.
\end{corollary}

\begin{theorem}[Two-sided finite-memory conjugacy and finite-type closure]\label{thm:two-sided-conjugacy}
See \cite{MaZhang2026Nonlinearity}, Theorem~4.12.
\end{theorem}

\begin{theorem}[Three-window phase transition]\label{thm:phase-transition}
For the sliding stabilized-window factors $\Phi_m$, $m\ge2$, there are exactly
two regimes.
\begin{enumerate}[label=(\roman*),leftmargin=*,itemsep=2pt]
  \item At $m=2$ the unique non-singleton path-space fiber is the branch pair
  \[
  \Phi_2^{-1}\bigl((\ldots,00,00,\ldots)\bigr)
  =
  \{0^{\mathbb Z},1^{\mathbb Z}\}.
  \]
  Every other point of $Y_2$ has a singleton preimage.
  \item For every $m\ge3$ the map
  \[
  \Phi_m:\bigl(\{0,1\}^{\mathbb Z},\sigma\bigr)\to(Y_m,\sigma)
  \]
  is a topological conjugacy onto a subshift of finite type. Its inverse is a
  sliding-block code with memory $0$ and anticipation $m-1$.
\end{enumerate}
Consequently the stabilized-window dynamics has no intermediate degeneration:
in the whole Fibonacci family $m\ge2$, failure of path-space conjugacy occurs
only at $m=2$, and there it consists of the single branch orbit pair displayed
above.
\end{theorem}

\begin{remark}
The ingredients are Theorems~4.6 and~4.12 of \cite{MaZhang2026Nonlinearity}. The increment here is the exhaustive DCDS phase-transition formulation: across all $m\ge2$ the stabilized-window factor has only the single two-window branch regime and the conjugacy/SFT regime, with no intermediate finite-to-one non-conjugate case.
\end{remark}

\begin{proof}
By Theorem~4.6 of \cite{MaZhang2026Nonlinearity}, the two-window path-space
fiber over the constant $00$ output is exactly
$\{0^{\mathbb Z},1^{\mathbb Z}\}$, and every other point of $Y_2$ has a
singleton preimage. Since both preimages are fixed points, this is a single
shift-orbit pair.

By Theorem~4.12 of \cite{MaZhang2026Nonlinearity}, every $m\ge3$ gives a
topological conjugacy onto $Y_m$, the image is a subshift of finite type, and
the inverse is a sliding-block code with memory $0$ and anticipation $m-1$.
These two alternatives exhaust all integers $m\ge2$. Thus there is no remaining
value of $m$ at which a nontrivial finite-to-one, non-conjugate, or non-SFT
intermediate degeneration can occur.
\end{proof}

\begin{remark}[Decoder complexity]\label{rem:decoder-complexity}
The inverse code supplied by Corollary~\ref{cor:explicit-decoder} reads a sliding window of length $m$ and outputs one raw bit. Equivalently, the decoder may be implemented by maintaining an overlap state of size $m-1$. The arithmetic cost is $O(m)$ per output symbol, since each decoding step involves at most $m$ Fibonacci-weighted additions modulo $F_{m+2}$.
\end{remark}

\begin{proposition}[Standard dynamical corollaries above threshold]\label{prop:conjugacy-consequences}
Assume $m\ge 3$.
\begin{enumerate}[label=(\roman*),leftmargin=*,itemsep=2pt]
  \item For every $n\ge 1$,
  \[
  \#\operatorname{Fix}(\sigma^n|_{Y_m})=2^n,
  \qquad
  \zeta_{Y_m}(z)=\frac{1}{1-2z}.
  \]
  \item Pushforward by $\Phi_m$ induces a bijection between the shift-invariant Borel probability measures on $\{0,1\}^{\mathbb Z}$ and those on $Y_m$.
  \item Ergodicity, weak mixing, mixing, measure-theoretic entropy, and the Bernoulli and $K$ properties are transported exactly through the conjugacy.
\end{enumerate}
\end{proposition}

\begin{proof}
All three items are standard consequences of the conjugacy and inverse sliding-block code in Theorem~\ref{thm:two-sided-conjugacy}. Fixed-point counts and the Artin--Mazur zeta function are conjugacy invariants. The inverse sliding-block code gives the bijection of invariant measures. The dynamical statements in (iii) are invariants of topological or measure-theoretic conjugacy; see \cite{Walters1982}.
\end{proof}

\begin{remark}\label{rem:exact-recoding}
For $m\ge3$ the stabilized image is a subshift of finite type topologically conjugate to the full two-shift by a finite-memory code. The only obstruction below threshold is the branch locus in Theorem~\ref{thm:m2-branch-locus}.
\end{remark}

\begin{remark}\label{rem:sofic-comparison}
In the terminology of \cite{LindMarcus1995}, $\mathcal G_m$ is a right-resolving presentation of the sofic shift $Y_m$, and $\Phi_m$ has degree at most $2^{m-1}$ by Theorem~\ref{thm:finite-state-lift}. A right-resolving factor of degree~1 between irreducible shifts of finite type is a conjugacy \cite[Theorem~8.1.16]{LindMarcus1995}; see also \cite{Ashley1991} for the equal-entropy resolving theory and \cite{Boyle1983} for the lower-entropy obstruction. The threshold theorem says that degree~1 is achieved exactly at $m=3$. This is an arithmetic fact, not a formal consequence of the sofic framework, and it uses the explicit decoder of Lemma~\ref{lem:m3-decoder}.
\end{remark}

\begin{proposition}[Measure-theoretic entropy preservation under the stabilized-window factor]\label{prop:measure-entropy-preservation}
Let $m\ge 2$, let $\nu$ be a shift-invariant Borel probability measure on $\{0,1\}^{\mathbb Z}$, and let
\[
\eta:=(\Phi_m)_*\nu
\]
be its image on $Y_m\subset (X_m)^{\mathbb Z}$. Then
\[
h_\eta(\sigma)=h_\nu(\sigma).
\]
\end{proposition}

\begin{proof}
For $n\ge 1$, let
\[
A_{0:n+m-2}:=(A_0,\dots,A_{n+m-2})
\]
denote the raw binary block of length $n+m-1$, and let
\[
Y_{0:n-1}:=(Y_0,\dots,Y_{n-1}),\qquad Y_t=\Fold_m(A_t\cdots A_{t+m-1}),
\]
denote the stabilized block of length $n$.

Since $Y_{0:n-1}$ is a deterministic function of $A_{0:n+m-2}$, one has
\[
H_\eta(Y_{0:n-1})\le H_\nu(A_{0:n+m-2}).
\]
Conversely, by the finite-state lift in Theorem~\ref{thm:finite-state-lift}, once the initial overlap state
\[
(A_0,\dots,A_{m-2})\in\{0,1\}^{m-1}
\]
is specified, the entire raw block $A_{0:n+m-2}$ is uniquely determined by the stabilized block $Y_{0:n-1}$. Therefore
\[
H_\nu(A_{0:n+m-2}\mid Y_{0:n-1})\le (m-1)\log 2.
\]
By the chain rule,
\[
H_\nu(A_{0:n+m-2})
=
H_\eta(Y_{0:n-1})
+
H_\nu(A_{0:n+m-2}\mid Y_{0:n-1}),
\]
hence
\[
0\le H_\nu(A_{0:n+m-2})-H_\eta(Y_{0:n-1})\le (m-1)\log 2.
\]
Divide by $n$ and let $n\to\infty$. The additive discrepancy is $O(1)$, so it disappears after normalization. The left-hand side converges to $h_\nu(\sigma)$ because shifting the block length by the fixed amount $m-1$ does not affect the entropy rate, while the right-hand side converges to $h_\eta(\sigma)$. Thus
\[
h_\eta(\sigma)=h_\nu(\sigma).
\]
\end{proof}

\begin{corollary}[Sequence-level entropy preservation]\label{cor:entropy-preservation}
For every $m\ge 2$,
\[
h_{\mathrm{top}}(Y_m)=\log 2.
\]
\end{corollary}

\begin{proof}
The map $\Phi_m$ is a continuous factor from the full two-shift onto $Y_m$, so
\[
h_{\mathrm{top}}(Y_m)\le h_{\mathrm{top}}(\{0,1\}^{\mathbb Z})=\log 2.
\]
Let
\[
\nu_{1/2}:=\Bigl(\tfrac12,\tfrac12\Bigr)^{\otimes \mathbb Z}
\]
be the Bernoulli measure on the full two-shift, and set
\[
\eta_{1/2}:=(\Phi_m)_*\nu_{1/2}.
\]
By Proposition~\ref{prop:measure-entropy-preservation},
\[
h_{\eta_{1/2}}(\sigma)=h_{\nu_{1/2}}(\sigma)=\log 2.
\]
Since $\eta_{1/2}$ is a shift-invariant Borel probability measure on $Y_m$, the variational principle \cite{Walters1982} gives
\[
h_{\mathrm{top}}(Y_m)\ge \log 2.
\]
Thus equality holds.
\end{proof}

\begin{remark}
For every $m\ge2$ the stabilized-window factor preserves entropy. Once $m\ge3$, the structure is stronger: $Y_m$ is conjugate to the full two-shift. Preservation of periodic data and invariant measures is then a consequence of the conjugacy, not an additional source of arithmetic content. Proposition~\ref{prop:low-window-counterexamples} shows that the threshold cannot be lowered.
\end{remark}
