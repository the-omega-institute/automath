\section{Conclusion}\label{sec:conclusion}

The total-variation theorem describes the sampled continued fraction beyond
its scalar normalization.  It gives simultaneous limits for the two
contexts, the condensate defect and location, and the denominator
factorization.  A balanced cut and the exact boundary terms in continuant
concatenation give the sharp \(n^{-1}\) total-variation rate and its limiting
constant.  The summable two-context estimate is the arithmetic step that
makes these joint conclusions possible for the nonmultiplicative continuant
weight.

Its scalar denominator-layer asymptotic supplies the critical cost tail in
Weinstein's generator coordinates.  The exact orbit count turns a classical
Fibonacci layer into a renewal sum, and the tail then produces the unbounded
sublinear term of order \(m^{3-\sigma_0}\).  In the finite-size window
\(s_m-\sigma_0=O(m^{-1})\), the defective renewal mass becomes an exponential
tilt of the macroscopic generator location, while the triangular letter
array retains the critical stable fluctuation.  This yields the joint
crossover law, with the uniform critical theorem as its central member.
At fixed exponents, the two sides of the transition instead give a tight
generator cost and a boundary-localized cost with Gaussian letter-count
fluctuations.

The remaining question is whether analogous finite-size crossover laws hold
for other nonmultiplicative continued-fraction families.  Such an extension
would require an arithmetic generator identity and a local tail estimate
adapted to the family.

\section*{Reproducibility}
All numerical tables and finite checks for this paper are generated from the
scripts in \path{artifacts/}.  The verified scripts are: \path{artifacts/test_verify_critical_constants.py},
which runs unit tests for the critical-constant calculation; \path{artifacts/test_verify_critical_gibbs_geometry.py},
which runs unit tests for the finite Gibbs-geometry check and its controls;
and \path{artifacts/test_verify_finite_size_crossover.py}, which runs unit tests
for the finite-size crossover calculation.  The script
\path{artifacts/verify_condensed_split.py} compares the condensed and
noncondensed split by exact enumeration; \path{artifacts/verify_context_rate.py}
checks the two-context rate identities by exact enumeration; and
\path{artifacts/verify_continuant_multiplicity.py} checks continuant
multiplicities and the associated normalization sum.  The script
\path{artifacts/verify_correction_form.py} evaluates the competing finite-size
correction forms; \path{artifacts/verify_cost_is_stern_brocot_depth.py}
computes the depth-truncated Stern--Brocot cost; and
\path{artifacts/verify_critical_constants.py} evaluates the critical point and
the corresponding constants.  The script
\path{artifacts/verify_critical_gibbs_geometry.py} checks the finite Gibbs
geometry with negative controls and writes its check file;
\path{artifacts/verify_critical_point_bijection.py} checks the critical-point
bijection; and \path{artifacts/verify_critical_tail_constant.py} estimates the
critical tail constant from finite layers.  The script
\path{artifacts/verify_crossover_theta_dependence.py} compares measured and
predicted crossover parameters; \path{artifacts/verify_dushistova_mechanism.py}
checks the mechanism behind the Dushistova-coefficient discrepancy;
\path{artifacts/verify_finite_size_crossover.py} performs the finite-size
crossover computation; and \path{artifacts/verify_layer_partition_sum.py}
enumerates the layer partition sum.  Finally,
\path{artifacts/verify_resolvent_recurrence.py} checks the resolvent recurrence,
and \path{artifacts/verify_Zn_large_n.py} computes the large-$n$ partition sum
by a Stern--Brocot walk together with its truncation-error bound.
The one-click instructions are in \path{artifacts/REPRODUCE.md}, and the
artifact hashes are recorded in \path{artifacts/SHA256SUMS}.  These finite
checks corroborate, but do not replace, the proofs and asymptotic arguments
given above.
