\section{Weighted partition functions: a fourth-order minimal recurrence, Hankel rank, and Lee--Yang branching}
\label{sec:partition}

\subsection{Fold fibers and a weighted partition function}
For \(x\in X_m\), define the fold-fiber multiplicity
\[
d_m(x):=\#\Fold_m^{-1}(x).
\]
We introduce the weighted partition function
\begin{equation}\label{eq:Zm}
Z_m(y):=\sum_{x\in X_m} y^{\abs{x}_1}\, d_m(x),
\end{equation}
and the associated generating function
\[
Z(t;y):=\sum_{m\ge 0} Z_m(y)\,t^m.
\]

\subsection{A fourth-order minimal recurrence and Hankel-rank collapse}
\begin{theorem}[Fourth-order recurrence]\label{thm:recurrence}
The sequence \((Z_m(y))_{m\ge 0}\) satisfies a linear recurrence of order \(4\). Equivalently, its generating function is rational with denominator
\begin{equation}\label{eq:Dty}
D(t;y)=1-t-(2y+1)t^2+t^3+y(y+1)t^4.
\end{equation}
Equivalently, the minimal characteristic polynomial can be taken as
\begin{equation}\label{eq:Pi}
\Pi(\lambda,y)=\lambda^4-\lambda^3-(2y+1)\lambda^2+\lambda+y(y+1).
\end{equation}
For generic \(y\), the Hankel rank of \((Z_m(y))\) equals \(4\).
\end{theorem}

\begin{remark}
The recurrence and Hankel-rank statement can be derived from a finite-state transfer-matrix description of fold fibers. We outline a proof route in Appendix~\ref{app:recurrence}.
\end{remark}

\subsection{Discriminant factorization and Lee--Yang type criticality}
The free energy (pressure)
\[
f(y):=\lim_{m\to\infty}\frac{1}{m}\log Z_m(y)
\]
is governed by the dominant root \(\lambda_\star(y)\) of \(\Pi(\lambda,y)\). Singularities arise at parameter values where this root ceases to be simple, i.e., where the discriminant vanishes.

\begin{theorem}[Discriminant factorization]\label{thm:discriminant}
The discriminant of \(\Pi(\lambda,y)\) with respect to \(\lambda\) factors as
\begin{equation}\label{eq:disc}
\Disc_{\lambda}\,\Pi(\lambda,y)
=-y(y-1)\bigl(256y^3+411y^2+165y+32\bigr).
\end{equation}
The unique negative real root of the cubic factor,
\[
y_{\mathrm{LY}}\approx -1.13445,
\]
is the nearest negative real branch point for \(f(y)\) (a Lee--Yang type critical point).
\end{theorem}

\subsection{Weight statistics under the fold pushforward}
Let \(u_{\Om_m}\) be the uniform measure on \(\Om_m\), and let \(w_m:=\Fold_m\push u_{\Om_m}\) be its pushforward to \(X_m\). For \(X\sim w_m\), set \(H_m:=\abs{X}_1\).

\begin{theorem}[LLN/CLT for \(\abs{X}_1\) under \(w_m\)]\label{thm:weight-stats}
Under \(w_m\),
\[
\frac{H_m}{m}\xrightarrow{\ \PP\ }\frac{5}{18},
\qquad
\frac{H_m-\frac{5}{18}m}{\sqrt m}\Rightarrow \mathcal{N}\!\left(0,\frac{67}{972}\right).
\]
Moreover, \(\frac{H_m}{m}\) satisfies a large deviation principle with an explicit Legendre--Fenchel rate function.
\end{theorem}

