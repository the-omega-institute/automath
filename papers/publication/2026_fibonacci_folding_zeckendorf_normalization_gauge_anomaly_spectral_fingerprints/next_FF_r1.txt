You are refereeing the attached 34-page paper for a specialist dynamics/combinatorics journal.
One question only. Do not summarize the paper and do not answer a different question.

BACKGROUND YOU DO NOT NEED TO RE-DERIVE. Earlier versions claimed that because each Zeckendorf
rewrite 011 -> 100 has support 3, normalization is a "span-3 local normalizer" whose pair language
has memory 2. That is false -- Fibonacci rewrite cascades are unbounded -- and it has been removed.
The bounded-cascade property is now clause (ii) of Definition 3.x, and the manuscript states that
Fibonacci does not satisfy it. I have separately established, by exhaustive search over local
rewrite systems, that the class defined that way contains nothing resembling Fold_m, and that the
culprit is the "terminating confluent scan" wording rather than clause (ii); that part is settled
and is not what I am asking about.

THE QUESTION. The four-state pair graph is now derived from an explicit arithmetic carry
transducer instead of from the rewrite span. At a cut immediately before weight F_k, the state is
(P, y^-), where P is the raw-minus-normalized prefix imbalance and y^- is the preceding normalized
digit; the four states are listed as P = 0 with y^- = 0, P = -F_{k+1}, P = -F_k with y^- = 0, and
P = 0 with y^- = 1. Check that derivation, and check the two inclusions separately -- one direction
is where the previous proof failed.

Specifically, verify or refute:

(i) P takes only those finitely many values along any scan. I want an invariant that is PROVED to
    be preserved, not a range that was observed on examples. If the proof in the paper only exhibits
    the values rather than closing them under the scan step, say so.

(ii) The state (P, y^-) is sufficient: two scans in the same state have identical future pair
     languages. This is the direction where an unbounded cascade would reappear, as an unbounded P.

(iii) The resulting graph is right-resolving and synchronized, and it presents the whole interior
      pair language rather than a subshift of it.

ONE WARNING, because it cost me a wasted verification. The pair process here is the REVERSED shift
and Y_t is the BULK-LIMIT normalized digit, not the normalized digit of any finite window. A check
that reads left to right, or that uses the last digit of a finite-window label, computes a
different object and proves nothing either way. If you compute anything, state which object you
computed.

Give a verdict on each of (i), (ii), (iii).
