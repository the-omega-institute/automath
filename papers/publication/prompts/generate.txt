You are Stage G of the Omega distillation pipeline.

Raw research JSON:
{raw_research}

Deterministic section matches:
{section_matches}

Return only one JSON object matching this schema:
{schema}

Task:
Assemble a dual payload for Omega:
1. knowledge_payload: compact durable knowledge node for the backflow registry.
2. navigation_payload: routing and search material that can drive future discovery.

Requirements:
- Preserve the strongest method_operators, object mappings, theorem families, induction templates, and failure modes from raw research.
- Use section_matches to choose primary_targets; prefer high-score matches but reject weak semantic fits.
- Include primary_targets as a top-level list of core section slugs.
- Include expansion_queue as concrete future kernels with target_sections and status.
- Keep every target tied to a reason visible in the raw research or section matches.
- Do not invent already-integrated writebacks.

Output:
Valid JSON only. No markdown fence. No prose outside JSON.
