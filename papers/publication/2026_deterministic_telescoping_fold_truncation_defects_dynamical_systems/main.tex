\documentclass[11pt,reqno]{amsart}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{microtype}

\numberwithin{equation}{section}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\NN}{\mathbb{N}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\ZZ}{\mathbb{Z}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Xinf}{X_\infty}
\newcommand{\Unif}{\mathsf{Unif}}
\DeclareMathOperator{\Fold}{Fold}
\DeclareMathOperator{\Law}{Law}

\title[Truncation defects in finite-window Zeckendorf normalization]{Truncation defects in finite-window Zeckendorf normalization}
\author{Haobo Ma}
\address{AELF PTE LTD., \#14-02, Marina Bay Financial Centre Tower 1, 8 Marina Blvd, Singapore 018981}
\email{auric@aelf.io}
\author{Wenlin Zhang}
\thanks{Corresponding author.}
\address{National University of Singapore, Singapore}
\email{e1327962@u.nus.edu}
\subjclass{Primary 11B39; Secondary 37B10, 68Q45}
\keywords{Zeckendorf expansion, Fibonacci numeration, finite-window normalization, truncation defect}
\date{}

\begin{document}
\begin{abstract}
For $m\ge 1$, let $\Fold_m$ map a binary word of length $m$ to the first $m$
digits of the Zeckendorf expansion of its Fibonacci value.  For $n\ge m$ we
compare $\Fold_m(r_{n\to m}(\omega))$ with $\pi_{n\to m}(\Fold_n(\omega))$
and write $D_{n\to m}$ for the resulting truncation defect.  We prove that the
adjacent defect at scale $m+1\to m$ is nonzero exactly when the appended digit
is $1$ and the prefix value is at least $F_{m+1}$; this yields a telescoping
formula expressing $D_{n\to m}$ as the xor-sum of the projected adjacent
defects.  The main deterministic result is a one-bit tail collapse: if
$\omega=(p,q)\in\Omega_m\times\Omega_{n-m}$, then
$D_{n\to m}(p,q)=\kappa_{m+1\to m}(p,\beta(q))$ for an explicit bit
$\beta(q)\in\{0,1\}$.  Consequently
\[
\mu_{n,m}=(1-\rho_{n-m})\delta_0+\rho_{n-m}\lambda_m,
\qquad
\rho_\ell=\frac{2^\ell-(-1)^\ell}{3\cdot 2^\ell},
\]
where $\mu_{n,m}$ is the law of $D_{n\to m}$ under $\Unif_n$ and $\lambda_m$
is the adjacent defect law with top digit fixed to $1$.  In particular,
\[
\PP_{\Unif_n}[D_{n\to m}\ne 0]
=
\frac{(2^{n-m}-(-1)^{n-m})\lfloor 2^{m+1}/3\rfloor}{3\cdot 2^n}.
\]
The tail bit is the parity of the initial run of $1$'s in the upper block and
is the minimal exact tail statistic for the full defect family.  We also give
an explicit support-and-multiplicity formula for the adjacent law $\lambda_m$.
At the process level, the sliding-window defect factors have two
infinite-tail limits, indexed by the parity of the window length.  For every
shift-invariant binary measure these limits coincide if and only if the
measure gives no mass to the all-one fixed point; otherwise their total
variation distance is exactly that atomic mass.
For every stationary finite-state Markov factor of the full two-shift, the
defect law is obtained by adjoining a four-state tail automaton to the Markov
presentation.  In the one-step binary specialization this automaton has live
eigenvalue $-p_{11}$, giving coefficients
$p_{a1}(1-(-p_{11})^\ell)/(1+p_{11})$ and exponential convergence to the
limiting law.
\end{abstract}

\maketitle

\input{sections/01_introduction}
\input{sections/02_adjacent_defects}
\input{sections/03_telescoping_identities}
\input{sections/04_tail_collapse}
\input{sections/05_tail_factors}
\input{sections/06_markov_laws}
\input{sections/07_examples}

\bibliographystyle{plain}
\bibliography{references}

\end{document}
