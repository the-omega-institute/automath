\documentclass[runningheads]{llncs}

\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{array}
\usepackage{tabularx}

\title{Auditable Theory-to-Paper Pipelines for AI-Assisted Formal Mathematics}
\titlerunning{Auditable Theory-to-Paper Pipelines}
\author{Haobo Ma\inst{1} \and Wenlin Zhang\inst{2}}
\authorrunning{H. Ma and W. Zhang}
\institute{AELF PTE LTD. \email{auric@aelf.io} \and
National University of Singapore \email{e1327962@u.nus.edu}}

\begin{document}
\maketitle

\begin{abstract}
AI-assisted formalization is often described as a loop in which a model
proposes Lean code and a proof assistant accepts or rejects it.  That loop is
necessary, but it is too small for sustained research.  A live project must
also decide whether a generated object is a proposal, a source-theory claim, a
checked formal interface, a finite artifact witness, a publication claim, or a
human decision boundary.  We describe a theory-to-paper pipeline, developed
across the \emph{newmath} and \emph{automath} workspaces, that makes those
claim types explicit.  Generated material is routed through six records:
source object, formal interface, finite evidence row, advisory agent action,
deterministic gate, and human promotion boundary.  The system lets agents
search, repair, and criticize, while certification remains in typed records,
mechanical gates, and human promotion decisions.  The contribution is a portable
six-role audit boundary for AI-assisted formal mathematics: it explains how a
pipeline promotes a generated object into a bounded publication claim without
conflating source theory, formal interfaces, finite artifacts, advisory search,
deterministic checks, and human review.  The compact theorem spine is
conditional on the accepted record-gate-soundness row or hypothesis and is
schema-relative and finite-record only.  It covers the six-role interface,
record-gate certificate safety, finite-record boundary classification,
projection-contract support for named scripts, and four historical/path-verified
case rows; it is not a new theorem prover, an end-to-end correctness claim for
automath/newmath, a fresh artifact rerun, or an automatic venue-acceptance
system.
\end{abstract}

\paragraph{Route and package.}
This file is the public presentation paper for the workshop route.  The submitted
package also contains \path{main.tex} and review-bundle surfaces, but those files
serve only as finite-record supplementary support for the labelled obligations
named here.  They do not change the public article into a technical supplement
and they do not assert fresh source rebuilds, dynamic artifact validation,
external archive status, venue acceptance, or end-to-end workflow correctness.

\section{Problem}
Proof assistants are excellent at checking obligations they are given.  They do
not decide whether an AI-generated theorem statement is mathematically
substantive, whether it duplicates a submitted sibling, whether an artifact row
is being overread, or whether an intake note has become an active manuscript.
Those are routing failures: the local object may be coherent, but the global
claim status is wrong.

\emph{Newmath} is our structured-source instance.  It separates mathematical
source, Lean-facing interfaces, and finite substrate witnesses.  Lean markers
record one verification axis, but Lean acceptance is not treated as semantic
closure for every BEDC claim.  \emph{Automath} is the research-publication
instance.  It coordinates theoremization, review, overlap checks, novelty
checks, manuscript assembly, and submission packets.  Both workspaces use the
same principle: a generated increment is evidence only after its allowed claim
type has been recorded and gated.

\section{Audit interface}
The pipeline compiles each accepted increment into six roles: the source object
being changed, the formal interface on which it is checked, the finite evidence
row supporting the claim, the advisory agent action that proposed or repaired
it, the deterministic gate that accepted it, and the human promotion boundary
that says what may be claimed to readers.  If one role is missing, the
increment remains a proposal.

This LLNCS file, \path{submission_abstract.tex}, is the public presentation
article.  For this route \path{main.tex} is submitted as technical supplementary
material required to discharge the labelled proof obligations named by the
compact theorem spine, while the compact article remains the public review
object.  A supplement coordinate written below as \texttt{thm:...},
\texttt{prop:...}, \texttt{lem:...}, or \texttt{cor:...} means the unique TeX
source label \verb|\label{...}| in \path{main.tex}; it is a reader locator for
the corresponding finite-record proof obligation, not an additional claim that
the whole workflow implementation, source workspace, or artifact suite has been
semantically verified.

\paragraph{Finite-record interface theorem.}
The public result of this presentation is a single conditional RGS finite-record
interface claim, not a catalogue of independent workflow-correctness theorems: every
load-bearing sentence below is read through the bounded record named in the left
column and through the corresponding \path{main.tex} source label in the right
column.
\begin{center}
\small
\begin{tabularx}{\linewidth}{p{0.28\linewidth}X}
\toprule
Public interface claim & Conditional finite-record/RGS support coordinate \\
\midrule
Conditional six-role method interface &
\path{main.tex}, label
\texttt{thm:six-role-publication-master-interface}, with the support ledger
compressed at label \texttt{thm:master-spine-support-coordinate-compression} \\
RGS-conditional certificate safety &
\path{main.tex}, labels \texttt{thm:publication-safety-interface} and
\texttt{thm:record-acceptance-exactness}; this row is conditional on the external
record-gate-soundness hypothesis for the submitted finite rows \\
No free stronger reading &
\path{main.tex}, labels
\texttt{cor:primary-presentation-weak-interface-compression} and
\texttt{cor:no-finite-record-gluing-upgrade}; missing coordinates remain bad
subrecords until supplied and gated \\
Finite-script projection-contract support, not implementation soundness &
Projection-contract support only for the parsed finite fields of named scripts;
concrete script implementation soundness requires a separate upgrade coordinate,
via \path{main.tex}, labels \texttt{thm:finite-script-projection-contract-support}
and \texttt{thm:six-coordinate-gate-implementation-soundness-skeleton} \\
Four case rows &
Historical/path-verified gate examples only, via
\path{main.tex}, labels \texttt{prop:presentation-case-matrix-sufficiency} and
\texttt{thm:finite-row-case-evidence-closure} \\
\bottomrule
\end{tabularx}
\end{center}
Thus the compact article claims a portable routing interface: generated objects
may be promoted only to the strongest finite-record claim whose source,
interface, evidence row, advisory action, gate, and human-boundary coordinates
are present.  It does not assert whole-program implementation soundness, proof
truth, fresh formal rebuilding, dynamic artifact semantics, live benchmark
behaviour, upload/archive status, venue acceptance, or automatic paper
acceptance.

\begin{center}
\small
\begin{tabularx}{\linewidth}{p{0.20\linewidth}p{0.24\linewidth}p{0.24\linewidth}X}
\toprule
Gate & Observed issue & Evidence status & Safe lesson \\
\midrule
Promotion/intake isolation & intake seed treated as active paper & historical/path-verified row; no live daemon rerun & active-paper status requires explicit human promotion \\
Overlap/submitted blocking & route races a submitted sibling & finite overlap and submission-state records; no venue decision claim & publication routing cannot bypass submitted-sibling state \\
Fake-extension rejection & extension-like churn without theorem growth & theorem-inventory and review rows; no claim of object-level novelty proof & compilation or file growth is not mathematical discovery by itself \\
Artifact-limitation disclosure & manifest row overread as semantic validity & bounded witness and local digest rows; no Rule~110/GroundCompiler semantic rerun & artifacts support only declared finite witnesses \\
\bottomrule
\end{tabularx}
\end{center}

\section{Positioning}
Proof-assistant infrastructure such as Lean~\cite{deMouraKADR2015Lean} supplies
trusted checking for obligations that have already been routed to an appropriate
formal interface.  LLM theorem-proving systems, including
LeanDojo~\cite{YangEtAl2023LeanDojo}, draft--sketch--prove
workflows~\cite{JiangEtAl2023DraftSketchProve}, Lean-oriented LLM
provers~\cite{XinEtAl2024DeepSeekProver}, and mathematical language
models~\cite{AzerbayevEtAl2023Llemma}, improve proposal, search, and repair.
Formal-library and proof-engineering work, including AFP
mining~\cite{BlanchetteHMN2015AFP} and Isabelle/AFP infrastructure
accounts~\cite{Wenzel2019IsabelleAFP}, studies how formal corpora are curated
and maintained.  Artifact and reproducibility norms, such as ACM artifact
badging~\cite{ACM2020ArtifactBadging} and systems-repeatability
studies~\cite{CollbergProebsting2016Repeatability}, state what computational
objects and reruns can support.

This paper sits at a different boundary.  It is not a Lean hammer, not an AI
safety framework, not a complete BEDC theory exposition, and not an
artifact-validation paper.  Its contribution is the finite audit interface around
those activities: a pipeline rule for deciding what kind of claim a generated
object is allowed to become, and a missing-coordinate classification when a
stronger claim is requested.

The public source surface for the BEDC instance is the newmath repository
\cite{bedc-newmath-source}, pinned for this submission to commit
\path{3fb3d6a0641767388a401883062aa522ea0b397b}.  The stable packaging policy
for this route is: \path{submission_abstract.tex} is the presentation article,
while \path{main.tex}, the source-interface record, theorem inventory, digest
manifest, and review bundle are the submitted supplementary support surfaces for
the labelled finite-record proof obligations.  They support finite-record audit
predicates only;
they are not, by themselves, claims of clean-machine rebuild, complete source
import, dynamic rerun of every artifact, external byte-identity of an uploaded
archive, or venue acceptance.  The terminal support coordinates are therefore
local path-verified support, or uploaded/archive-equivalent support only when a
separate upload or archive locator is present.

\section{Conclusion}
The useful abstraction is not ``the model proves the theorem.''  It is
``the system routes each generated object to the strongest claim it is entitled
to support.''  Making that routing explicit gives AI agents room to search and
repair without letting polished local output masquerade as accepted
mathematical or publication evidence.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
