\documentclass[runningheads]{llncs}

\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xurl}
\usepackage{array}
\usepackage{tabularx}
\usepackage{lineno}

\makeatletter
\def\UrlBreaks{}
\def\UrlBigBreaks{}
\makeatother
\newcommand{\stableid}[1]{\mbox{\normalfont\scshape #1}}
\newcommand{\auditpath}[1]{\mbox{\begingroup\Urlmuskip=0mu\path{#1}\endgroup}}
\newcommand{\auditcode}[1]{\mbox{\normalfont\ttfamily\detokenize{#1}}}
\newcommand{\artifactpath}[1]{\auditpath{#1}}
\newcommand{\rawBeginTokenSubset}{\mathsf{rawBeginTokenSubset}}
\newcommand{\rawBeginTokenWitness}{\mathsf{rawBeginTokenWitness}}
\newcommand{\qlex}{\rawBeginTokenWitness}
\newcommand{\qrgs}{q_{\mathrm{rgs}}}
\newcommand{\qsrc}{q_{\mathrm{src}}}
\newcommand{\qart}{q_{\mathrm{art}}}
\newcommand{\qext}{q_{\mathrm{ext}}}
\newcommand{\qven}{q_{\mathrm{ven}}}

\title{Publication-Coordinate Design Patterns and Finite Checker Specifications for Generated Formal-Mathematics Artifacts}
\titlerunning{Publication-Coordinate Checker Specifications}
\author{Haobo Ma\inst{1} \and Wenlin Zhang\inst{2}}
\authorrunning{H. Ma and W. Zhang}
\institute{AELF PTE LTD. \email{auric@aelf.io} \and
National University of Singapore \email{e1327962@u.nus.edu}}

\begin{document}
\maketitle
\linenumbers

\begin{abstract}
Generated formal-mathematics artifacts need a publication interface that records
which finite evidence rows support which public claims.  We present a
publication-coordinate design pattern and finite checker specification for a
concrete \emph{newmath}/\emph{automath} theory-to-paper package.  The design
separates public coordinates, typed evidence surfaces, admissible pointers,
deterministic gates, and human promotion rows.  The package instance is a
six-coordinate replay-kernel specification.  Its PDF-level claim is only the
printed finite specification and Horn calculation; the present PDF-only object
does not instantiate a reviewer-executable artifact claim.  A later executable
replay reading would require the non-PDF archive contract below plus a separate
branch-by-branch \(\mathsf{ScriptOK}_{\mathrm{stage\_a}}\) conformance theorem.

\textbf{Contributions.}  First, the paper specifies six publication roles:
source object, formal interface, finite evidence row, advisory agent action,
deterministic gate, and human promotion boundary.  Second, it packages the
specified bytes through a finite replay-kernel relation consisting of a
byte-to-atom compiler, fixed Horn schema, replayable certificate, replay report,
raw begin-token witness ledger, and digest bundle.  Third, under the archive,
archive-format normalization and alias-rejection, ordinary-file, byte-equality,
replay-environment, stable-locator, and
\(\mathsf{ScriptOK}_{\mathrm{stage\_a}}\) assumptions, a concrete script run
may be cited for the conditional finite package, whose mathematical relation
has exactly two positive coordinates.  The first is the public
coordinate
\(\qlex\), whose legacy artifact spelling is \(\texttt{qinv}\) and whose older
paper spelling was \(q_{\mathrm{lex}}\); it is read only as a
\(\rawBeginTokenSubset\) raw-substring witness for the declared five literal
\LaTeX{} begin-token names.  The second coordinate is bounded
fixed-replay record-gate soundness.  Fourth, case rows for intake
isolation, overlap blocking, fake-extension rejection, and artifact-limitation
disclosure illustrate the same method at a historical, path-complete snapshot
level.  The replay-support surface is conditional on a non-PDF artifact
submission whose archive-format checks, ordinary files, SHA--256 rows,
replay-environment descriptor, stable locator, and script-conformance theorem
satisfy the archive contract.  A
single scope register below records the stronger coordinates that require their
own upgrade rows.
\end{abstract}

\paragraph{Dependency diagram.}
\[
\begin{array}{c}
\text{six-role records}
\longrightarrow
\text{finite gates and ledgers}
\longrightarrow
\text{replay certificate}
\longrightarrow
\varnothing\ \text{for the PDF-only base; }\{\qlex\}\ \text{with scanner success; }
\{\qlex,\qrgs\}\ \text{with }
\mathsf{ScriptOK}_{\mathrm{stage\_a}}
\end{array}
\]
The upgrade coordinates are governed by the scope register in
Section~\ref{sec:compact-scope-register}.

\paragraph{Notation key.}
The public coordinate names used below are
\[
(\qlex,\qrgs,\qsrc,\qart,\qext,\qven).
\]
The legacy artifact atom \texttt{qinv} is always the public coordinate
\(\qlex=\rawBeginTokenWitness\), the raw begin-token substring witness.  The local atom \texttt{qrgs}
is the public coordinate \(\qrgs\), bounded fixed-replay record-gate soundness.
\texttt{stage\_a} in paths and JSON keys is rendered in prose as Stage--A.
``Current bytes'' means the exact ordinary-file bytes named by the manifest and
digest table; changing any listed file requires a new digest/replay tuple.
``Support coordinate'' means one component of the displayed six-coordinate
product together with its finite input rows, not an informal promotion by a
ledger, path, or script name.

\paragraph{Audit-string typography.}
Path names, command fragments, digests, and code identifiers that are part of
audit rows are printed as atomic strings.  The manuscript disables
discretionary URL/path line breaks for \verb|\path|, \verb|\auditpath|,
\verb|\artifactpath|, and \verb|\auditcode|, so
\auditpath{certificates/stage_a_manifest.json},
\auditpath{str.splitlines()}, and \auditcode{forward_chain} are literal strings;
page layout never inserts a byte, blank, slash, underscore, or identifier
character into them.

\paragraph{Stable audit labels.}
The supplement assigns theorem-number-independent labels to the load-bearing
interfaces:
\[
\begin{array}{rcl}
\stableid{S0}&:&\text{conditional split-route typed appendix interface},\\
\stableid{R0a}&:&\text{punctured internal absolute-value row},\\
\stableid{R0}&:&\text{Richardson verified-triple domain preservation},\\
\stableid{R1}&:&\text{conditional printed-name specialization under (HW)},\\
\stableid{L1}&:&\text{valuation-transport protocol},\\
\stableid{O1}&:&\text{one-orbit ambient-product obstruction},\\
\stableid{H1}&:&\text{EKMP height-formula boundary},\\
\stableid{C0}&:&\text{cover-multiplicity freedom lemma},\\
\stableid{C1}&:&\text{factorial-spike cover stress test},\\
\stableid{C2}&:&\text{auxiliary-cover moment boundary},\\
\stableid{T0}&:&\text{finite-state transfer exponential bound},\\
\stableid{F0}&:&\text{finite-record replay foreground closure}.
\end{array}
\]
The optional split-route appendix dependency graph is
\[
\stableid{S0}\to
\{\stableid{R0a},\stableid{R0},\stableid{L1},\stableid{O1},\stableid{H1},\stableid{C0},\stableid{C1},\stableid{C2},\stableid{T0}\},
\qquad
\stableid{R0a}\to\stableid{R0},\quad
\stableid{R1}\to\stableid{R0}\ \text{only under (HW)}.
\]
The primary pipeline route is the separate chain
\[
\text{six-role records}\to\text{finite gates and ledgers}\to
\text{replay certificate}\to\stableid{F0}.
\]
These stable identifiers, not section theorem numbers, are the audit keys for
dependency discussions.
Only \(\stableid{F0}\), read as the finite replay-kernel closure for
\(\{\qlex,\qrgs\}\), is in the foreground public theorem inventory of
the compact article.  The optional split-route labels
\(\stableid{S0},\stableid{R0a},\stableid{R0},\stableid{R1},\stableid{L1},
\stableid{O1},\stableid{H1},\stableid{C0},\stableid{C1},\stableid{C2}\), and
\(\stableid{T0}\) are support or conditional appendix interfaces.  They may be
cited to explain boundaries and hypotheses, but they do not add public
coordinates beyond \(\qlex\) and \(\qrgs\).

\paragraph{Version and artifact status.}
Version date: 2026-06-17.  Repository locator:
\url{https://github.com/the-omega-institute/newmath} for the bounded imported
source surface.  Submitted package status: compact article plus technical
supplement plus a named finite support archive contract
\path{cicm_supplement_2026_auditable_theory_to_paper_pipeline.zip}.  The present
text does not assert that this archive has been supplied with matching bytes, a
DOI, SWHID, venue-supplied supplement identifier, or equivalent immutable
locator.  Therefore the PDF-only submission instantiates no
reviewer-executable artifact claim.  External, archive-equivalence, DOI,
one-command \path{make verify}, clean-machine replay, and venue-upload readings
are governed by the scope register in Section~\ref{sec:compact-scope-register}.

\section{Motivation}
Proof assistants are excellent at checking obligations they are given.  They do
not decide whether a generated theorem statement is mathematically new, whether
it duplicates a submitted sibling, whether an artifact row is being overread, or
whether an intake note has become an active manuscript.  These are publication
routing failures: a local object may be coherent while the global claim status
is wrong.

\emph{Newmath} is our structured-source instance.  It separates mathematical
source, Lean-facing interfaces, and finite substrate witnesses.  Lean markers
record one verification axis, but Lean acceptance is not treated as semantic
closure for every BEDC claim.  \emph{Automath} is the research-publication
instance.  It coordinates theoremization, review, overlap checks, novelty
checks, manuscript assembly, and submission packets.  Both workspaces use the
same rule: a generated increment becomes evidence only after its permitted claim
type has been recorded and gated.

\section{Six-role audit interface}
Each accepted increment is compiled into six coordinates:
\emph{source object}, \emph{formal interface}, \emph{finite evidence row},
\emph{advisory agent action}, \emph{deterministic gate}, and \emph{human
promotion boundary}.  If a coordinate is missing, the increment remains a
proposal.  If all coordinates are present only for a finite record, the public
claim is finite-record only.

The technical supplement specifies the foreground checker package as a
proof-carrying Stage--A replay-kernel closure, with the canonical finite-audit
basis used as its supporting normal form.  That basis proves that each monotone
acceptance predicate is determined by a unique finite antichain of minimal
accepting records, and that unsupported Stage--A upgrades have a canonical
bad-subrecord basis.  In an archive-instantiated replay, the ordinary files
\path{certificates/stage_a_manifest.json},
\path{certificates/stage_a_horn_schema.json},
\path{certificates/stage_a_horn_audit_certificate.json},
\path{certificates/stage_a_replay_report.json},
\path{certificates/stage_a_digest_table.json}, and
\path{certificates/replay_stage_a_audit.py} would provide the canonical byte
manifest, the exact byte-to-atom compiler, the fixed Horn schema, the
replayable certificate, the digest table, and the deterministic replay check.
Only under the archive, byte-equality, replay-environment, stable-locator,
explicit \(\mathsf{ScanOK}\), and
\(\mathsf{ScriptOK}_{\mathrm{stage\_a}}\) premises would running
\path{python certificates/replay_stage_a_audit.py} recompute the conditional finite vector
\[
\qlex=\mathsf{true}\quad(\text{raw substring witness; legacy artifact atom }\texttt{qinv}),\quad
\qrgs=\mathsf{true},\quad
\qsrc=\qart=\qext=\qven=
\mathsf{false}.
\]
Here \(\qrgs\) is bounded fixed-replay record-gate soundness for the
fixed Stage--A schema, manifest, replayable certificate, replay report, and
digest bundle.  Older six-role, conditional RGS, theorem-inventory, and
local-support closure results in the supplement are support normal forms for this
vector.  Case-study rows are historical, path-complete method witnesses unless a
fresh command, environment, \path{source_commit}, exit-code, and log row is
supplied for a live validation claim.  The separate case-row replay command reconstructs the
four printed finite rows only; it is not a rerun of the original workflow
pipelines or an artifact-semantic validation.  Its sampling frame is the fixed
compiler case set: Newmath intake isolation, upper-fibers overlap blocking,
fake-extension blocking, and Rule110 limitation disclosure.  A row is eligible
only when the manifest case name is in that set, local paths and submitted
snapshots are paired, every submitted snapshot path is relative and present, and
the boundary declares historical or path-verified scope.  The expected JSON also
contains a negative control: a false fifth row that tries to infer Rule110
dynamic semantic validation from the limitation snapshot is rejected by the same
verifier because it is outside the fixed compiler case set and lacks the
command-run/artifact-semantic coordinates.

This distinction is the main engineering lesson.  The same package may contain
proposal artifacts, repair logs, literature-search records, venue notes, and
generated theorem wrappers, while certification is reserved for deterministic
gates and human promotion decisions.  In practice this prevents four common
failures: treating intake material as active work, routing a submitted sibling
as a fresh paper, accepting file growth as theorem growth, and reading a bounded
artifact manifest as semantic validation.

\subsection{Scope and nonconsequence register}
\label{sec:compact-scope-register}
This subsection is the compact article's single register for stronger readings.
Later tables repeat an exclusion only when a local hypothesis would otherwise be
easy to misquote.

\begin{table}
\caption{Compact scope register.}
\label{tab:compact-scope-register}
\scriptsize
\begin{tabularx}{\linewidth}{p{0.26\linewidth}X}
\toprule
Surface & Accepted reading and upgrade rule\\
\midrule
\(\qlex\) & The legacy artifact atom \(\texttt{qinv}\) is read only as the
\(\rawBeginTokenWitness\) coordinate: a \(\rawBeginTokenSubset\) raw-substring
witness for the declared literal source substrings
\path{\textbackslash begin\{definition\}},
\path{\textbackslash begin\{lemma\}},
\path{\textbackslash begin\{proposition\}},
\path{\textbackslash begin\{theorem\}}, and
\path{\textbackslash begin\{corollary\}}.  It is not displayed \TeX{} content,
theorem-inventory coverage, dependency, proof-checking, scope-adequacy,
audit-completeness, or semantic-completeness coverage.\\
\addlinespace
\(\qrgs\) & Bounded fixed-replay record-gate soundness for the
printed finite Horn replay rows, and for archive bytes only after the archive
contract is supplied.  General gate-implementation soundness
requires its own conformance row or theorem.\\
\addlinespace
Upgrade coordinates & Fresh formal-source rebuild, dynamic artifact semantics,
external archive equality, and venue/upload status remain false until accepted
finite rows promote \(\qsrc,\qart,\qext\), or
\(\qven\).\\
\addlinespace
Scripts, digests, inventories, review rows & Evidence only for the coordinate
named by the gate; no paraphrase promotes them into proof-assistant soundness,
whole-program soundness, semantic artifact validation, or external publication
evidence.\\
\addlinespace
Optional mathematical appendix & Richardson, printed-name EML rows, one-orbit,
height, Zeckendorf, and cover material are support interfaces.  Richardson uses
the verified triple on the preserved source-indexed real domain; printed
\(U47,U111\) names require \((HW)\); \(U126\) is not a Richardson input; ordinary
maximum height uses \((\mathrm{EKMP})\).\\
\addlinespace
Public theorem inventory & The foreground surface is \(\stableid{F0}\) and the
two accepted coordinates \(\{\qlex,\qrgs\}\), not the list of all
theorem-like environments in the supplement.\\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}
\caption{Artifact certificate boundary.}
\label{tab:artifact-certificate-boundary}
\scriptsize
\begin{tabularx}{\linewidth}{p{0.28\linewidth}X}
\toprule
Record & Scope \\
\midrule
\path{certificates/pure_eml_syntax_certificate.json} & The optional U47/U111/U126
printed-root certificate is a split-route syntax record associated with
\path{arXiv:2603.21852v2} and \path{10.48550/arXiv.2603.21852}.  Those locators
name source provenance only.  Its paper-local archive is
\path{certificates/eml_witness_catalogue.json}, whose SHA--256 is
\path{aed9739e9e6d2e044868ec9c85a95b41c1e39d67cb602c7c135edd21cc432440};
the checker is \path{certificates/check_eml_witnesses.py}, whose SHA--256 is
\path{2ef35fd5e765978a9fa4fbf17e12a14dd325a52869d84034b6ea266b17a60aaf}.
The reviewable command is
\path{python certificates/check_eml_witnesses.py certificates/eml_witness_catalogue.json certificates/pure_eml_syntax_certificate.json --format json},
with \path{--format csv} for the CSV output.  It checks finite pure-EML grammar
membership, counts, depths, and SHA--256 hashes for the displayed catalogue
strings. \\
Boundary & The parser proves finite pure-EML syntax only; it proves no
real-domain identities.  It proves neither \(U47=\pi\), nor
\(U111(x)=\sin x\), nor any square-root identity for \(U126\), and no theorem in
this presentation paper depends on those optional printed roots.  In particular,
\(U126\) is an endpoint square-root source-lock row and is not used in any
Richardson obstruction.  A printed-name Richardson specialization exists only
under the separate \((HW)\) hypothesis
\(U47=\pi\) and \(U111(x)=\sin x\) on their stated real domains; without
\((HW)\), that specialization is deleted rather than inferred from syntax.
The appendix Richardson interface is only the conditional verified-triple
schema for already-proved or explicitly assumed rows \((P,S,A)\), not the
printed \(U47/U111/U126\) catalogue. \\
\midrule
\path{certificates/scan_tex_source_labels.py} & The source-level static scan is
the reviewable duplicate-label and stale-endpoint-token check.  The command
\path{python certificates/scan_tex_source_labels.py --write certificates/static_source_label_scan.json}
lexically scans \path{main.tex}, \path{submission_abstract.tex}, and
\path{appendix_full_technical_ledger.tex}; it does not evaluate TeX
conditionals, comments, includes, or macros, so inactive branches are included.
The recorded result in \path{certificates/static_source_label_scan.json} has
label counts \(13,3,351\), zero duplicate labels within each source, zero
duplicate labels across the three-source package, and zero occurrences of the
stale endpoint-certificate tokens listed in that JSON report: the three legacy
rational constants, the former shared-boundary equation, and the two obsolete
root-inequality phrases.  This proves only source-level label hygiene and
absence of the stale printed endpoint/central certificate tokens; it does not
prove any theorem statement or semantic interval inequality. \\
\midrule
\path{certificates/replay_stage_a_audit.py} & The Stage--A replay bundle is an
archive contract, not an instantiated claim of the current PDF-only article.
An archive-instantiated bundle must contain
\path{certificates/stage_a_manifest.json},
\path{certificates/stage_a_horn_schema.json},
\path{certificates/stage_a_horn_audit_certificate.json},
\path{certificates/stage_a_replay_report.json},
\path{certificates/stage_a_digest_table.json},
\path{certificates/stage_a_replay_environment.json}, and
\path{certificates/replay_stage_a_audit.py} under the archive-format
normalization contract: byte-preserving extraction, exact POSIX path names,
case-sensitive comparison, no Unicode normalization, symlink and hard-link
rejection, deterministic extraction order, distinct post-unpack identities, and
ordinary non-symlink files under a stable locator with matching SHA--256 rows.
The digest table then binds the
manifest, schema, certificate, report, environment descriptor, and replay script
by SHA--256 rows.  Under those additional premises, the finite relation checks
those rows, applies the displayed atom compiler to the manifest, forward-chains
the submitted Horn rules, validates the certificate DAG objects, and compares the
six-coordinate vector with the replay report.  A reviewable command
\path{python certificates/replay_stage_a_audit.py} may be cited for that relation
only if a separate branch-by-branch
\(\mathsf{ScriptOK}_{\mathrm{stage\_a}}\) theorem is supplied. \\
Boundary & Under the archive contract, the mathematical relation proves only
fixed Stage--A Horn replay for the conditional finite rows.  It does not run
Lean/BEDC, prove source-theory
correctness, validate artifact semantics, instantiate external upload or
archive equality, verify venue status, prove script conformance, or prove
gate-program implementation soundness.  Its positive vector is exactly
\(\qlex=\qrgs=\mathsf{true}\) and
\(\qsrc=\qart=\qext=\qven=
\mathsf{false}\). \\
\bottomrule
\end{tabularx}
\end{table}

\section{Positioning}
Proof-assistant infrastructure such as Lean~\cite{deMouraKADR2015Lean} supplies
trusted checking for obligations that have already been routed to an appropriate
formal interface.  Proof-carrying code and foundational proof certificates
\cite{Necula1997PCC,Appel2001FoundationalPCC,AppelMcAllester2001FPCCRecursiveTypes,ChihaniMillerRenaud2013FPC}
motivate finite, independently checkable certificate objects; here the
certificate is only the Stage--A Horn object for
\(\{\qlex,\qrgs\}\), with \(\qlex\) stored in the artifact as
\(\texttt{qinv}\).  LLM theorem-proving systems,
including LeanDojo~\cite{YangEtAl2023LeanDojo}, Lean
Copilot~\cite{SongYangAnandkumar2024LeanCopilot}, draft--sketch--prove
workflows~\cite{JiangEtAl2023DraftSketchProve}, Lean-oriented LLM
provers~\cite{XinEtAl2024DeepSeekProver}, and mathematical language
models~\cite{AzerbayevEtAl2023Llemma}, improve proposal, search, tactic
assistance, and repair.  They are advisory sources in this paper, not proof or
artifact evidence.  Formal-library and proof-engineering work, including AFP
mining~\cite{BlanchetteHMN2015AFP} and Isabelle/AFP infrastructure
accounts~\cite{Wenzel2019IsabelleAFP}, studies how formal corpora are curated
and maintained.  Artifact and reproducibility norms, such as ACM artifact
badging~\cite{ACM2020ArtifactBadging}, ReproZip-style packaging
\cite{ChirigatiRampinShashaFreire2016ReproZip}, and systems-repeatability
studies~\cite{CollbergProebsting2016Repeatability}, state what computational
objects and reruns can support; here replay is only the digest-bound finite-row
predicate printed in the certificate.

\subsection{Related work by interface}
The comparison below separates cited support by the interface it informs:
Richardson-style undecidability and simplification, EML representation rows,
Zeckendorf arithmetic, finite automata and rational-series/C-finite models, and
CICM-style formalization or software-artifact practice.  The CICM comparison is
not a free-form sample: the screened corpus is the official CICM event years
2021--2025 main-conference proceedings plus citable CICM workshop volumes in
that window.  For CICM 2025 the item labels below use the event year, while the
Springer volume metadata carries 2026 copyright information.
The screening strings were the venue/year terms together with
\path{formalization}, \path{certificate}, \path{artifact}, \path{LLM},
\path{library}, \path{database}, and \path{algorithm}.  Rows include only the
closest corpus item under interface similarity; the final column states why no
closer corpus result was used.

\begin{table}
\caption{Screened theorem-by-theorem comparison with recent CICM and
CICM-workshop mathematical-software work.}
\label{tab:related-work-comparison}
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabularx}{\linewidth}{p{0.14\linewidth}p{0.20\linewidth}p{0.16\linewidth}p{0.15\linewidth}p{0.17\linewidth}X}
\toprule
Theorem package here & Closest screened corpus item & Claim type there & Artifact status there & No-closer-result screen & Precise novelty increment here \\
\midrule
Finite replay theorem and six-coordinate public scope &
Baanen et al., ``Growing Mathlib'' (CICM 2025 event; Springer copyright 2026)~\cite{BaanenEtAl2026GrowingMathlib};
Yamamichi--Shigenaka--Nakash\=o--Wasaki, ``A Web Platform for Hosting the Mizar
Mathematical Library'' (CICM-WS/FMM 2021)~\cite{YamamichiEtAl2021MizarPlatform};
Kohlhase--M{\"u}ller--Schaefer, ``sTeX'' (CICM-WS/NatFoM
2021)~\cite{KohlhaseMuellerSchaefer2021STeX};
proof-carrying-code and foundational-certificate work
\cite{Necula1997PCC,Appel2001FoundationalPCC,ChihaniMillerRenaud2013FPC}. &
Library-maintenance, hosting, and semantic \LaTeX{} infrastructure for formal corpora. &
System and library artifacts are described; this paper does not audit their
bytes or replay logs. &
No closer corpus item was found with a public claim-coordinate vector, negative
upgrade coordinates, and a finite Horn replay certificate; these are nearest only
on formal-library maintenance, hosting, and semantic document interfaces. &
Adds a publication-coordinate finite-checker projection to the proof-carrying
pattern: the printed finite Horn relation, and any later archive bytes satisfying
the stated contract, have exactly the positive coordinates
\(\qlex\) and \(\qrgs\), while source rebuild, artifact semantics, external
archive, and venue coordinates remain negative. \\
\addlinespace
Advisory-agent gate and no-free-upgrade boundary &
Horowitz et al., ``Exploring proof autoformalization with Mistral on Herald''
(CICM 2025 event; Springer copyright 2026)~\cite{HorowitzEtAl2026Herald}; LeanDojo and Lean
Copilot~\cite{YangEtAl2023LeanDojo,SongYangAnandkumar2024LeanCopilot}. &
LLM autoformalization and Lean-facing copilot/tactic-assistance workflows. &
Dataset/model evaluation is reported; no claim is made here that its artifacts
are part of the submitted replay surface. &
No closer corpus item was found whose generated proof suggestions become
publication evidence only after deterministic gates and human-promotion rows. &
Uses generated-formal-artifact work only as adjacent context; the new increment
here is a finite publication-coordinate rule under which proposal and repair
artifacts become load-bearing only after deterministic finite gates and human
promotion records; copilot suggestions are advisory until that finite row is
present. \\
\addlinespace
Conditional typed appendix and Richardson branch &
Karatarakis, ``A Formalization of All Notions in the Statement of a Theorem by
Deligne'' (CICM 2024)~\cite{Karatarakis2024Deligne}. &
Partial formalization of the notions appearing in a major theorem statement. &
Lean development is linked by the paper; this submission imports none of those
definitions or proof obligations. &
No closer corpus item was found for Richardson equality-reflection or normalizer
obstruction; this is a weak match chosen only for the formal-interface boundary. &
Uses a typed interface theorem for an optional appendix branch: Richardson
consequences require a verified \(\Trip(P,S,A)\), and printed \(U47,U111\)
names require the separate \((HW)\) identities. \\
\addlinespace
\(L1\) Zeckendorf protocol and software-facing normalizer &
Brinich--Johnson, ``Generating Formally Verified Quantum Fourier Transform
Algorithms'' (CICM 2024)~\cite{BrinichJohnson2024QFT}; Bartoletti--Bonzio--Ferrara,
``Certified Algorithms for Numerical Semigroups in Rocq'' (CICM 2025 event;
Springer copyright 2026)~\cite{BartolettiBonzioFerrara2026Semigroups}. &
Verified algorithm generation or certified computation inside a proof
assistant. &
Proof-assistant developments are part of those papers' claims; this paper's
\(L1\)-Z block is only executable pseudocode plus totality and size bounds. &
No closer corpus item was found for this valuation-transport Zeckendorf protocol;
the selected items are nearest only on verified algorithm/computation interfaces. &
Defines \(\NF,\Val,\Add,\Mul,\Phi^S_0,\Phi^S_1\) with typed transport
operations \(\odot_S,\bullet_S\), explicitly not an efficient online
Zeckendorf arithmetic package. \\
\addlinespace
Finite-record certificates versus imported data &
Bauer et al., ``Incorporating a Database of Graphs into a Proof Assistant''
(CICM 2024)~\cite{BauerEtAl2024LeanHoG}; ACM/ReproZip/repeatability
artifact-replay context
\cite{ACM2020ArtifactBadging,ChirigatiRampinShashaFreire2016ReproZip,CollbergProebsting2016Repeatability}. &
Proof-assistant integration of curated graph data, generated certificates, and
external SAT solving; packaging norms for rerunnable computational artifacts. &
Lean-HoG and external certificates are described there; this paper does not
validate House-of-Graphs data, LeanSAT runs, or clean-machine reproduction. &
No closer corpus item was found that makes pointer admissibility, digest-bound
local records, and non-upgrade semantics the checked theorem object. &
Turns the interface question around: finite records certify which publication
coordinates a local artifact row may support, without upgrading to artifact
semantic correctness. \\
\addlinespace
Auxiliary-cover \(L2\) and fixed-transfer obstruction &
Bentkamp--Avigad, ``Verified Optimization'' (CICM-WS/FMM
2021)~\cite{BentkampAvigad2021VerifiedOptimization}; Brinich--Johnson
2024~\cite{BrinichJohnson2024QFT}. &
Verified reductions or rewrite rules for mathematical software pipelines. &
Work-in-progress or proof-assistant artifacts are described in their own
systems; not imported here. &
No closer corpus item was found for the artificial factorial-spike
auxiliary-cover stress test or fixed-transfer growth comparison. &
States only a cover-relative stress test for declared computable covers and
fixed-field transfer sequences; arbitrary one-spike covers are not intrinsic
Foldm behaviour and do not separate finite-state, local, or grammar-generated
cover classes. \\
\addlinespace
Related-work boundary for exact-real, automata, and artifact context &
Richardson/Caviness/Wang~\cite{Richardson1968,Caviness1970,Wang1974},
Zeckendorf arithmetic~\cite{Zeckendorf1972,Frougny1999,AhlbachUsatineFrougnyPippenger2013,Idziaszek2020},
and automata/rational-series references~\cite{AlloucheShallit2003,KauersPaule2011}
plus the CICM items above. &
Background theorems and software traditions, not matching recent CICM theorem
claims. &
All remain cited support unless a finite row in the submitted package names a
stronger artifact. &
No closer screened CICM item was used because these are deliberately
outside-corpus background streams rather than corpus hits. &
Prevents the table itself from importing an equality normalizer, digit-local
Zeckendorf algorithm, automatic-sequence theorem, or C-finite classification. \\
\bottomrule
\end{tabularx}
\end{table}

Our contribution is the publication-coordinate design pattern and finite checker
boundary around those activities.  The public BEDC
source surface is the \emph{newmath} repository~\cite{bedc-newmath-source},
pinned here to commit
\path{3fb3d6a0641767388a401883062aa522ea0b397b}.  The submitted package contains
this two-page presentation note, a longer technical supplement, a Stage--A
manifest, Horn schema, audit certificate, replay report, and a review bundle of
source-interface records, theorem-inventory records, digest manifests, and
case-evidence rows.  These surfaces support the bounded finite-record claims in
the replay-kernel vector under Section~\ref{sec:compact-scope-register}.

\section{Conclusion}
The useful abstraction is not a claim about tool performance.  It is the finite
route that assigns each generated or machine-maintained object to the strongest
publication coordinate it is entitled to support.  The unconditional PDF-level
claim is the displayed six-coordinate finite specification and digest/file-record
closure
\[
\operatorname{Cl}_{H_{\mathrm{Horn}}}(A_{\mathrm{rec}})\cap Q=\varnothing,
\]
so all six base coordinates are false.  Adjoining the explicit scanner-success
premise gives the intermediate finite relation with \(\qlex\) positive only as
a raw begin-token substring witness and the other five coordinates false.  The two-positive-coordinate replay reading
\((\qlex,\qrgs)=(\mathsf{true},\mathsf{true})\) is conditional on the
archive, byte-equality, environment, stable-locator, and branch-by-branch
\(\mathsf{ScriptOK}_{\mathrm{stage\_a}}\) premises; without that conformance
theorem, the scripts are only named support files and the paper asserts only the
unconditional mathematical specification plus those conditional relations.  Making
that route explicit permits proposal and repair
artifacts to remain visible without letting polished local output masquerade as
source-rebuild, artifact-semantic, external-archive, venue-status, or
whole-implementation evidence.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
