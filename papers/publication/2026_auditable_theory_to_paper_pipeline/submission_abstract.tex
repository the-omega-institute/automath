\documentclass[runningheads]{llncs}

\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{array}
\usepackage{tabularx}

\title{Auditable Theory-to-Paper Pipelines for AI-Assisted Formal Mathematics}
\titlerunning{Auditable Theory-to-Paper Pipelines}
\author{Haobo Ma\inst{1} \and Wenlin Zhang\inst{2}}
\authorrunning{H. Ma and W. Zhang}
\institute{AELF PTE LTD. \email{auric@aelf.io} \and
National University of Singapore \email{e1327962@u.nus.edu}}

\begin{document}
\maketitle

\begin{abstract}
AI-assisted formalization is often described as a loop in which a model
proposes Lean code and a proof assistant accepts or rejects it.  That loop is
necessary, but it is too small for sustained research.  A live project must
also decide whether a generated object is a proposal, a source-theory claim, a
checked formal interface, a finite artifact witness, a publication claim, or a
human decision boundary.  We describe a theory-to-paper pipeline, developed
across the \emph{newmath} and \emph{automath} workspaces, that makes those
claim types explicit.  Generated material is routed through six records:
source object, formal interface, finite evidence row, advisory agent action,
deterministic gate, and human promotion boundary.  The system lets agents
search, repair, and criticize, while certification remains in typed records,
mechanical gates, and human promotion decisions.  The compact theorem spine is
conditional and finite-record only: six-role interface, record-gate soundness,
finite-record exactness, no-free-upgrade, and four methodological case rows.  The
case rows are historical/path-verified gate examples rather than fresh daemon or
artifact reruns, and the finite-script support result is projection-contract
soundness for named scripts rather than semantic soundness of the implementation
stack.  The contribution is a portable audit boundary for AI-assisted formal
mathematics, not a new theorem prover, an end-to-end correctness claim for
automath/newmath, or an automatic venue-acceptance system.
\end{abstract}

\section{Problem}
Proof assistants are excellent at checking obligations they are given.  They do
not decide whether an AI-generated theorem statement is mathematically
substantive, whether it duplicates a submitted sibling, whether an artifact row
is being overread, or whether an intake note has become an active manuscript.
Those are routing failures: the local object may be coherent, but the global
claim status is wrong.

\emph{Newmath} is our structured-source instance.  It separates mathematical
source, Lean-facing interfaces, and finite substrate witnesses.  Lean markers
record one verification axis, but Lean acceptance is not treated as semantic
closure for every BEDC claim.  \emph{Automath} is the research-publication
instance.  It coordinates theoremization, review, overlap checks, novelty
checks, manuscript assembly, and submission packets.  Both workspaces use the
same principle: a generated increment is evidence only after its allowed claim
type has been recorded and gated.

\section{Audit interface}
The pipeline compiles each accepted increment into six roles: the source object
being changed, the formal interface on which it is checked, the finite evidence
row supporting the claim, the advisory agent action that proposed or repaired
it, the deterministic gate that accepted it, and the human promotion boundary
that says what may be claimed to readers.  If one role is missing, the
increment remains a proposal.

This LLNCS file is the compact presentation article.  The larger file
\path{main.tex}, when distributed with the source package or uploaded as
supplementary material, is a technical supplement rather than the public review
object.  A supplement coordinate written below as \texttt{thm:...},
\texttt{prop:...}, \texttt{lem:...}, or \texttt{cor:...} means the unique TeX
source label \verb|\label{...}| in \path{main.tex}; it is a reader locator for
the corresponding finite-record proof obligation, not an additional claim that
the whole workflow implementation, source workspace, or artifact suite has been
semantically verified.

\begin{proposition}[Compact public theorem spine]
\label{prop:compact-public-theorem-spine}
The public claims in this compact presentation article are exactly the following
bounded finite-record claims, discharged in the technical supplement at the
indicated \path{main.tex} source-label coordinates.
\begin{center}
\small
\begin{tabularx}{\linewidth}{p{0.28\linewidth}X}
\toprule
Public claim & Supplement discharge \\
\midrule
Six-role method interface &
\path{main.tex}, label
\texttt{thm:six-role-publication-master-interface}, with support compressed at
label \texttt{thm:master-spine-support-coordinate-compression} \\
Record-gate certificate safety &
\path{main.tex}, labels \texttt{thm:publication-safety-interface} and
\texttt{thm:record-acceptance-exactness}; this is conditional on the explicit
record-gate-soundness hypothesis for the submitted finite rows \\
No free stronger reading &
\path{main.tex}, labels
\texttt{cor:primary-presentation-weak-interface-compression} and
\texttt{cor:no-finite-record-gluing-upgrade} \\
Finite-script projection-contract support &
Projection-contract support only for named scripts, via \path{main.tex}, label
\texttt{thm:concrete-finite-script-implementation-soundness}; the legacy label
text is not adopted here as a claim of verified implementation correctness \\
Four case rows &
Historical/path-verified gate examples only, via
\path{main.tex}, labels \texttt{prop:presentation-case-matrix-sufficiency} and
\texttt{thm:finite-row-case-evidence-closure} \\
\bottomrule
\end{tabularx}
\end{center}
No sentence in the compact article asserts whole-program implementation
soundness, proof truth, fresh formal rebuilding, dynamic artifact semantics,
live benchmark behaviour, upload/archive status, venue acceptance, or automatic
paper acceptance.
\end{proposition}

\begin{proof}
Each row names the finite record coordinate through which the corresponding
public sentence is read.  The six-role row is the master method claim; the
certificate row is conditional on the submitted finite records and the stated
record-gate hypothesis, which is external to this compact article unless a
specific row records the corresponding gate predicate; the weak-interface row is
the classified missing-upgrade obstruction; the finite-script row is explicitly
restricted to parsed finite projections; and the case row is
historical/path-verified evidence.  Any stronger reading listed in the last
sentence changes one of these finite coordinates, so the supplement routes it to
a separate positive upgrade coordinate rather than to the compact public claim.
\end{proof}

\begin{center}
\small
\begin{tabularx}{\linewidth}{p{0.23\linewidth}X X}
\toprule
Gate & Observed issue & Safe lesson \\
\midrule
Promotion/intake isolation & intake seed treated as active paper & active-paper status needs explicit human promotion \\
Overlap/submitted blocking & route races a submitted sibling & publication routing cannot bypass submission state \\
Fake-extension rejection & extension-like churn without theorem growth & compilation or file growth is not discovery by itself \\
Artifact-limitation disclosure & manifest row overread as semantic validity & artifacts support only declared finite witnesses \\
\bottomrule
\end{tabularx}
\end{center}

\section{Positioning}
The method is complementary to proof-assistant and LLM-proving infrastructure
such as Lean~\cite{deMouraKADR2015Lean}, LeanDojo~\cite{YangEtAl2023LeanDojo},
draft--sketch--prove workflows~\cite{JiangEtAl2023DraftSketchProve}, recent
Lean-oriented LLM provers~\cite{XinEtAl2024DeepSeekProver}, and mathematical
language models~\cite{AzerbayevEtAl2023Llemma}.  It is also adjacent to
formal-library and proof-engineering practice, including AFP
mining~\cite{BlanchetteHMN2015AFP}, Isabelle/AFP infrastructure
accounts~\cite{Wenzel2019IsabelleAFP}, and artifact/reproducibility norms such
as ACM artifact badging~\cite{ACM2020ArtifactBadging} and systems-repeatability
studies~\cite{CollbergProebsting2016Repeatability}.  Those systems and norms
improve production, checking, curation, or review of formal and computational
objects.  Our interface addresses the surrounding audit-boundary layer: how a
pipeline decides what kind of claim a generated object is allowed to become,
and which finite coordinate is missing when a stronger claim is requested.

The public source surface for the BEDC instance is the newmath repository
\cite{bedc-newmath-source}, pinned for this submission to commit
\path{3fb3d6a0641767388a401883062aa522ea0b397b}.  The stable packaging policy
for this source package is: \path{submission_abstract.tex} is the presentation
article, while \path{main.tex}, the source-interface record, theorem inventory,
digest manifest, and review bundle are support surfaces when they are included
in the submitted supplement bundle, and otherwise remain local/path-verified
records.  In both readings they support finite-record audit predicates only;
they are not, by themselves, claims of clean-machine rebuild, complete source
import, dynamic rerun of every artifact, external byte-identity of an uploaded
archive, or venue acceptance.  The terminal support coordinates are therefore
local path-verified support, or uploaded/archive-equivalent support only when a
separate upload or archive locator is present.

\section{Conclusion}
The useful abstraction is not ``the model proves the theorem.''  It is
``the system routes each generated object to the strongest claim it is entitled
to support.''  Making that routing explicit gives AI agents room to search and
repair without letting polished local output masquerade as accepted
mathematical or publication evidence.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
