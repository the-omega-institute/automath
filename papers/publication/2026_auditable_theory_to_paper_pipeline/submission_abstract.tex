\documentclass[runningheads]{llncs}

\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{array}
\usepackage{tabularx}

\title{Auditable Theory-to-Paper Pipelines for AI-Assisted Formal Mathematics}
\titlerunning{Auditable Theory-to-Paper Pipelines}
\author{Haobo Ma\inst{1} \and Wenlin Zhang\inst{2}}
\authorrunning{H. Ma and W. Zhang}
\institute{AELF PTE LTD. \email{auric@aelf.io} \and
National University of Singapore \email{e1327962@u.nus.edu}}

\begin{document}
\maketitle

\begin{abstract}
AI-assisted formalization is often presented as a local loop: a model proposes
Lean code and a proof assistant accepts or rejects it.  That loop is necessary,
but it is not enough for sustained mathematical research.  A project also needs
to know whether a generated object is source theory, a checked interface, a
finite artifact witness, an advisory search result, a publication claim, or a
human decision.  We describe a theory-to-paper pipeline, developed across the
\emph{newmath} and \emph{automath} workspaces, that makes these roles explicit.
The pipeline routes generated material through six records: source object,
formal interface, finite evidence row, advisory agent action, deterministic
gate, and human promotion boundary.  This gives agents room to search, repair,
and criticize, while certification remains in typed records, mechanical gates,
and human review.  The contribution is a replay-kernel interface, supported by a
canonical finite-antichain and obstruction-basis audit calculus, that gives a
proof-carrying finite audit kernel for AI-assisted formal mathematics: for the
submitted package the current-byte vector accepts theorem-inventory closure and
bounded fixed-replay record-gate soundness for the fixed Stage--A schema,
manifest, replayable certificate, replay report, and digest bundle, while
leaving fresh formal-source rebuild, dynamic artifact semantics, external
archive equality, general implementation soundness without an implementation-audit
row, and venue status unclaimed.
\end{abstract}

\section{Motivation}
Proof assistants are excellent at checking obligations they are given.  They do
not decide whether an AI-generated theorem statement is mathematically new,
whether it duplicates a submitted sibling, whether an artifact row is being
overread, or whether an intake note has become an active manuscript.  These are
routing failures: a local object may be coherent while the global claim status is
wrong.

\emph{Newmath} is our structured-source instance.  It separates mathematical
source, Lean-facing interfaces, and finite substrate witnesses.  Lean markers
record one verification axis, but Lean acceptance is not treated as semantic
closure for every BEDC claim.  \emph{Automath} is the research-publication
instance.  It coordinates theoremization, review, overlap checks, novelty
checks, manuscript assembly, and submission packets.  Both workspaces use the
same rule: a generated increment becomes evidence only after its permitted claim
type has been recorded and gated.

\section{Six-role audit interface}
Each accepted increment is compiled into six coordinates:
\emph{source object}, \emph{formal interface}, \emph{finite evidence row},
\emph{advisory agent action}, \emph{deterministic gate}, and \emph{human
promotion boundary}.  If a coordinate is missing, the increment remains a
proposal.  If all coordinates are present only for a finite record, the public
claim is finite-record only.  If an implementation-soundness coordinate is absent,
the system may cite a script as a parsed support surface, but not as a verified
implementation theorem.  Fixed replay is not a substitute for that missing
implementation-audit row.

The technical supplement's foreground theorem package is the proof-carrying
Stage--A replay-kernel closure, with the canonical finite-audit basis used as
its supporting normal form.  That basis proves that each monotone acceptance
predicate is determined by a unique finite antichain of minimal accepting
records, and that unsupported Stage--A upgrades have a canonical bad-subrecord
basis.  A canonical byte manifest, a byte-to-atom compiler, a fixed Horn schema,
a replayable certificate, and a deterministic replay report produce the finite vector
\[
q_{\mathrm{inv}}=\mathsf{true},\quad
q_{\mathrm{rgs}}=\mathsf{true},\quad
q_{\mathrm{src}}=q_{\mathrm{art}}=q_{\mathrm{ext}}=q_{\mathrm{ven}}=
\mathsf{false}.
\]
Here \(q_{\mathrm{rgs}}\) is only bounded fixed-replay record-gate soundness for
the fixed Stage--A schema, manifest, replayable certificate, replay report, and
digest bundle; it is not a theorem about the browser, daemon, end-to-end
implementation, proof assistant, artifacts, external support upload, or venue
outcome.  Older six-role, conditional RGS, theorem-inventory, and local-support
closure results in the supplement are support normal forms for this vector, not
additional public theorem surfaces.  Case-study rows are used in the same bounded
way: they are historical, path-complete method witnesses until a fresh command,
environment, source state, exit-code, and log row is supplied for any live
validation claim.

This distinction is the main engineering lesson.  The same pipeline can let
agents propose theorem statements, repair manuscripts, search literature, and
criticize venue fit, while reserving certification for deterministic gates and
human promotion decisions.  In practice this prevents four common failures:
treating intake material as active work, routing a submitted sibling as a fresh
paper, accepting file growth as theorem growth, and reading a bounded artifact
manifest as semantic validation.

\section{Positioning}
Proof-assistant infrastructure such as Lean~\cite{deMouraKADR2015Lean} supplies
trusted checking for obligations that have already been routed to an appropriate
formal interface.  LLM theorem-proving systems, including
LeanDojo~\cite{YangEtAl2023LeanDojo}, draft--sketch--prove
workflows~\cite{JiangEtAl2023DraftSketchProve}, Lean-oriented LLM
provers~\cite{XinEtAl2024DeepSeekProver}, and mathematical language
models~\cite{AzerbayevEtAl2023Llemma}, improve proposal, search, and repair.
Formal-library and proof-engineering work, including AFP
mining~\cite{BlanchetteHMN2015AFP} and Isabelle/AFP infrastructure
accounts~\cite{Wenzel2019IsabelleAFP}, studies how formal corpora are curated
and maintained.  Artifact and reproducibility norms, such as ACM artifact
badging~\cite{ACM2020ArtifactBadging} and systems-repeatability
studies~\cite{CollbergProebsting2016Repeatability}, state what computational
objects and reruns can support.

Our contribution is the audit boundary around those activities.  The public BEDC
source surface is the \emph{newmath} repository~\cite{bedc-newmath-source},
pinned here to commit
\path{3fb3d6a0641767388a401883062aa522ea0b397b}.  The submitted package contains
this two-page presentation note, a longer technical supplement, a Stage--A
manifest, Horn schema, audit certificate, replay report, and a review bundle of
source-interface records, theorem-inventory records, digest manifests, and
case-evidence rows.  These surfaces support bounded finite-record claims and the
fixed replay-kernel vector above.  They do not claim a clean-machine rebuild of
all source files, a fresh dynamic rerun of every artifact, whole-program
implementation soundness, venue acceptance, or automatic mathematical novelty
judgment.

\section{Conclusion}
The useful abstraction is not ``the model proves the theorem.''  It is
``the system routes each generated object to the strongest claim it is entitled
to support.''  Making that routing explicit gives AI agents room to search and
repair without letting polished local output masquerade as accepted
mathematical or publication evidence.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
