| label | kind | class | closest-prior | citation-key | increment | hypothesis-status |
| --- | --- | --- | --- | --- | --- | --- |
| `lem:max-value-admissible-prefix` | lemma | classical-input | Lekkerkerker (1952, Zeckendorf representation theorem) | `Lekkerkerker1952` | no increment; used as building block | `m>=1` is verifiable; the manuscript accurately treats the range bound as a fully explicit finite check |
| `prop:fold-residue-characterization` | proposition | conditional-corollary | Frougny (1991, automaton normalization construction for Fibonacci representations) | `Frougny1991` | specializes classical Fibonacci normalization to the finite-window Fold map by showing that the truncated output is determined by the residue class of `N(\omega)` mod `F_{m+2}` | `m>=1` and `\omega\in\Omega_m` are verifiable; the manuscript accurately presents the claim as a deterministic specialization |
| `lem:pi-functorial-golden` | lemma | classical-input | Lind--Marcus (1995, prefix-factor setup in §1.3) | `LindMarcus1995` | no increment; used as building block | `n>=k>=m` and `u,v\in\{0,1\}^n` are verifiable; the manuscript accurately treats the statement as formal |
| `def:fold-local-curvature-defect` | definition | novel-increment | Berthé--Frougny--Rigo--Sakarovitch (2020, Def. 3.1) | `BertheFrougnyRigoSakarovitch2020` | replaces the scalar successor carry observable with an xor-valued adjacent non-commutation observable comparing truncate-then-fold against fold-then-project | `m>=1` and `\eta\in\Omega_{m+1}` are verifiable by construction; the manuscript accurately discloses that there are no hidden hypotheses |
| `prop:adjacent-defect-threshold` | proposition | novel-increment | Berthé--Frougny--Rigo--Sakarovitch (2020, Cor. 5.39) | `BertheFrougnyRigoSakarovitch2020` | gives an exact iff threshold and an explicit xor formula for adjacent non-commutation at scale `m+1\to m`, rather than an averaged carry statistic for the successor map | `m>=1`, `p\in\Omega_m`, and `b\in\{0,1\}` are verifiable; the manuscript accurately presents all hypotheses as explicit |
| `def:fold-global-stokes-defect` | definition | novel-increment | none known | none; searched `Frougny1996Successor`, `Frougny1999`, `BertheFrougnyRigoSakarovitch2020`, `LindMarcus1995`, and `Kitchens1998` and found no prior xor-valued band defect for Fibonacci normalization/truncation | introduces a resolution-band defect vector that aggregates adjacent non-commutation across all intermediate scales | `n>=m` and `\omega\in\Omega_n` are verifiable by construction; the manuscript accurately discloses that there are no hidden hypotheses |
| `lem:fold-defect-adjacent-recursion` | lemma | conditional-corollary | none known | none; searched the carry-propagation and symbolic-dynamics sources above and found no separately stated recursion for this paper-specific defect observable | expresses the level-`j-1` defect exactly as one new local defect plus the projected level-`j` defect, which is the bookkeeping step needed for the later telescoping sum | `m<j<=n` and `\omega\in\Omega_n` are verifiable; the manuscript accurately presents the statement as formal |
| `thm:fold-discrete-stokes-defect` | proposition | conditional-corollary | none known | none; searched `Frougny1996Successor`, `Frougny1999`, `BertheFrougnyRigoSakarovitch2020`, `LindMarcus1995`, and `Kitchens1998` and found no prior exact xor-telescoping formula for normalization-versus-truncation defects | packages the global defect as an exact xor sum of projected adjacent defects across every intermediate scale | `n>m` and `\omega\in\Omega_n` are verifiable; the manuscript accurately treats the result as unconditional |
| `prop:uniform-adjacent-defect-counts` | proposition | conditional-corollary | Ahlbach--Usatine--Frougny--Pippenger (2013, §2 counting/normalization algorithms) | `AhlbachUsatineFrougnyPippenger2013` | turns the threshold criterion into an exact dynamic program for counting raw prefixes by Fibonacci value and hence computing the one-step defect event under the uniform law | `j>=0`, `t\in\mathbb{Z}`, and the uniform law on `\Omega_{j+1}` are verifiable definitions; the manuscript accurately presents them as explicit |
| `thm:fold-defect-count-explicit` | theorem | novel-increment | Berthé--Frougny--Rigo--Sakarovitch (2020, Cor. 5.39) | `BertheFrougnyRigoSakarovitch2020` | extracts the exact closed form `\lfloor 2^{m+1}/3\rfloor / 2^{m+1}` for the adjacent defect probability under the uniform raw-word law, not merely existence or asymptotic carry propagation | `m>=1` and the uniform law on `\Omega_{m+1}` are verifiable; the manuscript accurately discloses that the theorem is unconditional |
| `prop:fold-defect-cocycle` | proposition | conditional-corollary | none known | none; searched `LindMarcus1995`, `Kitchens1998`, `WaltersErgodicTheory`, and the Fibonacci-carry sources and found no prior cocycle identity for this defect observable | factorizes the defect across an intermediate scale and adds the resulting exact Hamming-triangle inequality for the same observable | `m<k<n` and `\omega\in\Omega_n` are verifiable; the manuscript accurately presents the result as formal |
| `prop:exact-vector-recursion` | proposition | conditional-corollary | none known | none; searched the cited automata and carry-propagation literature and found no prior exact recursion for the full defect-vector law `\mu_{n,m}` | records the exact finite-`n` enumeration identity for the full xor-valued defect distribution rather than only the scalar event `P_{n,m}` | `n>m` and `v\in\{0,1\}^m` are verifiable; the manuscript accurately presents the statement as unconditional |
| `prop:fold-naive-prefix-lift` | proposition | conditional-corollary | Lind--Marcus (1995, inverse-limit compatibility of one-sided shift prefixes in §1.3) | `LindMarcus1995` | specializes the standard compatibility-of-prefixes criterion to the Fold-generated sequence `x_m` by expressing liftability entirely in terms of eventual vanishing of the adjacent discrepancies `\delta_m` | `\omega^\infty\in\{0,1\}^{\mathbb{N}}` and `M>=1` are verifiable; the manuscript accurately presents eventual compatibility as the only hypothesis |
