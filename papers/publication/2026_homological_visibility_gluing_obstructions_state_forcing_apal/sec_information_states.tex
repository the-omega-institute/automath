\section{Information States and State Forcing}\label{sec:information-states}

This section defines the basic semantics used throughout. A state records a context together with the realizations still compatible with the information available. The construction is intentionally weaker than the stochastic relational and dynamic presheaf semantics developed for contextuality by Kishida \cite{Kishida2014}: here the state layer supplies only the lower forcing relation over which later local-object data will be conservatively added.

\subsection{States, refinement, and forcing}

Fix a many-sorted first-order signature
\[
\Sigma=(S,F,R).
\]
When this state-forcing construction is used as the layer $\mathbb L_1$ of the paper, the signature is the reference-aware signature $\Sigma_1$ fixed in \Cref{def:reference-aware-boundary}. Thus the sorts $\mathsf{Ref}_s$ and predicates $\Adm_s$ are already ordinary symbols of the state-forcing language in that use; the later local-object layer changes neither their domains nor their interpretations.
For a model $M$, write $D_s^M$ for the interpretation of a sort $s\in S$. A \emph{context} is a finite typed list
\[
\Gamma=(x_1:s_1,\dots,x_n:s_n),
\]
and its realization set in $M$ is
\[
\Val_M(\Gamma):=\{\rho\mid \rho(x_i)\in D_{s_i}^M\text{ for }1\le i\le n\}.
\]
If $\Gamma\preceq\Delta$, every $\sigma\in\Val_M(\Delta)$ restricts to an element $\sigma|_{\Gamma}\in\Val_M(\Gamma)$.

\begin{definition}[Information state]\label{def:information-state}
See \cite{HomologicalVisibilityGluingObstructions2026}, Definition \texttt{def:information-state}.
\end{definition}

\begin{definition}[Refinement]\label{def:refinement}
See \cite{HomologicalVisibilityGluingObstructions2026}, Definition \texttt{def:refinement}.
\end{definition}

Refinement therefore combines two monotonicities: the context may become richer, and the set of still-admissible realizations may become smaller.

\begin{definition}[State forcing]\label{def:state-forcing}
See \cite{HomologicalVisibilityGluingObstructions2026}, Definition \texttt{def:state-forcing}.
\end{definition}

\begin{definition}[State theory]\label{def:state-theory}
For $p\in\St(M)$, define
\[
\Th_M(p):=\{\varphi\mid M,p\Vdash\varphi\}.
\]
\end{definition}

\begin{definition}[Decidedness]\label{def:decidedness}
See \cite{HomologicalVisibilityGluingObstructions2026}, Definition \texttt{def:decidedness}.
\end{definition}

\begin{proposition}[Persistence of forcing]\label{prop:forcing-persistence}
See \cite{HomologicalVisibilityGluingObstructions2026}, Proposition \texttt{prop:forcing-persistence}.
\end{proposition}

\begin{corollary}[Monotone growth of state theories]\label{cor:state-theory-monotonicity}
If $q\le p$, then $\Th_M(p)\subseteq \Th_M(q)$.
\leanverified{paper_information_state_theory_monotonicity_seeds}
\end{corollary}

\begin{proof}
Immediate from \Cref{prop:forcing-persistence}.
\end{proof}

\begin{proposition}[Singleton conservativity]\label{prop:singleton-conservativity}
See \cite{HomologicalVisibilityGluingObstructions2026}, Proposition \texttt{prop:singleton-conservativity}.
\end{proposition}

\subsection{Lifted consequence}

\begin{definition}[State semantic consequence]\label{def:state-semantic-consequence}
For formulas $\Phi\cup\{\varphi\}$, define
\[
\Phi\models_{\mathrm{S}}\varphi
\]
to mean that for every model $M$ and every state $p\in\St(M)$,
\[
M,p\Vdash\Phi
\quad\Longrightarrow\quad
M,p\Vdash\varphi.
\]
\end{definition}

\begin{proposition}[Lifted soundness of pointwise reasoning]\label{prop:lifted-pointwise-soundness}
Let $\vdash_*$ be any derivability relation on formulas that is sound for ordinary many-sorted satisfaction. Then
\[
\Gamma\vdash_*\varphi
\quad\Longrightarrow\quad
\Gamma\models_{\mathrm{S}}\varphi.
\]
\leanverified{paper_information_state_lifted_pointwise_soundness_seeds}
\end{proposition}

\begin{proof}
Assume $\Gamma\vdash_*\varphi$, and let $M$ and $p=(\Gamma_p,R_p)$ be such that $M,p\Vdash\Gamma$. By definition, every realization $\rho\in R_p$ satisfies every formula in $\Gamma$ pointwise. Since $\vdash_*$ is sound for ordinary satisfaction, each such $\rho$ also satisfies $\varphi$. Therefore $M,p\Vdash\varphi$. As $M$ and $p$ were arbitrary, $\Gamma\models_{\mathrm{S}}\varphi$.
\end{proof}

\Cref{prop:lifted-pointwise-soundness} is the bridge from ordinary satisfaction to state forcing. It shows that familiar sound rules continue to apply once formulas are evaluated against all realizations compatible with a state.
