\subsection{Eighth-order defect layer extracted from the finite-order coefficient calculus}%
\label{subsec:eighth-order-defects}

The analytic input for the eighth-order coefficient identity is
Theorem~\ref{thm:poisson-kl-even-orders} with \(N=4\).  The finite centred
sixth absolute moment is the algebraic threshold for evaluating the displayed
universal moment-polynomial coefficient \(A_8\).  The displayed quantitative
\(O_\nu(t^{-9})\) remainder uses
the positive-margin retained-order theorem with \(N=4\) and \(\eta=1\),
and therefore assumes a finite centred seventh absolute moment.

The explicit notation for this layer is consolidated below, immediately
after the Laurent computation and before the coefficient-identification
theorem.  That block fixes \(A_6,A_8,\Delta_6,\Delta_8\) once and for all.

\begin{lemma}[Normalized Laurent representatives through order six]%
\label{lem:n4-normalized-laurent-representatives}
Let \(\omega(\dd y)=\pi^{-1}(1+y^2)^{-1}\dd y\), let
\(I(n_1,\ldots,n_r)\) be the Cauchy mode integral
\eqref{eq:mode-integral-definition}, and define \(Q_n\) by
\eqref{eq:mode-laurent-polynomial-definition}.  For the modes needed through
order eight, use the Laurent normalization summarized in
Table~\ref{tab:cayley-phase-normalization}, namely
\(\cos(2\ell\theta)=(z^\ell+z^{-\ell})/2\) and
\(\sin(2\ell\theta)=(z^\ell-z^{-\ell})/(2\ii)\).  Then
\begin{align*}
Q_2(z)&=-\frac14\bigl(z+z^{-1}+z^2+z^{-2}\bigr),\\
Q_3(z)&=-\frac1{8\ii}\bigl((z-z^{-1})+2(z^2-z^{-2})+(z^3-z^{-3})\bigr),\\
Q_4(z)&=\frac1{16}\bigl((z+z^{-1})+3(z^2+z^{-2})
+3(z^3+z^{-3})+(z^4+z^{-4})\bigr),\\
Q_5(z)&=\frac1{32\ii}\bigl((z-z^{-1})+4(z^2-z^{-2})
+6(z^3-z^{-3})+4(z^4-z^{-4})+(z^5-z^{-5})\bigr),\\
Q_6(z)&=-\frac1{64}\bigl((z+z^{-1})+5(z^2+z^{-2})
+10(z^3+z^{-3})\\
&\hspace{6.5em}
+10(z^4+z^{-4})+5(z^5+z^{-5})+(z^6+z^{-6})\bigr).
\end{align*}
Thus the relevant Laurent supports are
\[
S_j:=\operatorname{supp} Q_j=\{\pm1,\ldots,\pm j\},
\qquad 2\le j\le6.
\]
Writing \(Q_n(z)=\sum_{k=-n}^nq_{n,k}z^k\), \(q_{n,0}=0\), the coefficient
vectors are, on positive exponents,
\[
\begin{array}{c|c|c}
n & (q_{n,1},\ldots,q_{n,n}) & q_{n,-m}\ (1\le m\le n)\\
\hline
2 & -\frac14(1,1) & q_{2,m}\\
3 & \frac{\ii}{8}(1,2,1) & -q_{3,m}\\
4 & \frac1{16}(1,3,3,1) & q_{4,m}\\
5 & -\frac{\ii}{32}(1,4,6,4,1) & -q_{5,m}\\
6 & -\frac1{64}(1,5,10,10,5,1) & q_{6,m}.
\end{array}
\]
\end{lemma}

\begin{proof}
The formulas are the special cases \(n=2,\ldots,6\) of
\eqref{eq:mode-laurent-even}--\eqref{eq:mode-laurent-odd}.  The
normalization is the one fixed in
Theorem~\ref{thm:finite-fourier-mode-integral-algorithm}: after setting
\(z=e^{2\ii\theta}\), each occurrence of \(\cos(2\ell\theta)\) or
\(\sin(2\ell\theta)\) contributes the additional Laurent factor \(1/2\) or
\(1/(2\ii)\).  For example,
\[
u_2(\tan\theta)=-\frac12(\cos2\theta+\cos4\theta)
\]
therefore gives
\[
Q_2(z)=-\frac14(z+z^{-1}+z^2+z^{-2}).
\]
The remaining rows follow in the same way from the displayed finite Fourier
forms.
\end{proof}

\begin{lemma}[Complete constant-term table through order eight]%
\label{lem:eighth-order-coefficient-arithmetic}
Use the Laurent representatives of
Lemma~\ref{lem:n4-normalized-laurent-representatives}.  For every product
which can enter the entropy expansion through total order eight, the Cauchy
mode integral is the constant Laurent coefficient
\[
I(n_1,\ldots,n_r)=[z^0]\prod_{j=1}^r Q_{n_j}(z)
=\sum_{k_1+\cdots+k_r=0}q_{n_1,k_1}\cdots q_{n_r,k_r}.
\]
The complete list needed for the even entropy coefficients and the intervening
odd cancellations is
\[
\begin{array}{c|c|c}
\text{total order} & \text{product} & [z^0]\text{ of the product} \\
\hline
4 & Q_2^2 & \frac{4}{16}=\frac14\\
\hline
5 & Q_2Q_3 & 0\\
\hline
6 & Q_2Q_4 & -\frac{3+1+1+3}{64}=-\frac18\\
6 & Q_3^2 & \frac{2(1^2+2^2+1^2)}{64}=\frac{3}{16}\\
6 & Q_2^3 & -\frac{6}{64}=-\frac{3}{32}\\
\hline
7 & Q_2Q_5 & 0\\
7 & Q_3Q_4 & 0\\
7 & Q_2^2Q_3 & 0\\
\hline
8 & Q_2Q_6 & \frac{5+1+1+5}{256}=\frac{3}{64}\\
8 & Q_3Q_5 &
-\frac{2(1\cdot1+2\cdot4+1\cdot6)}{256}=-\frac{15}{128}\\
8 & Q_4^2 & \frac{2(1^2+3^2+3^2+1^2)}{256}=\frac{5}{32}\\
8 & Q_2^2Q_4 & \frac{4+6+12+2}{256}=\frac{3}{32}\\
8 & Q_2Q_3^2 & -\frac{2\cdot8+2\cdot1}{256}=-\frac{9}{128}\\
8 & Q_2^4 & \frac{36}{256}=\frac{9}{64}.
\end{array}
\]
Here \(I(2,3,3)\) denotes the row \(Q_2Q_3^2\), and similarly for the
other products.  These values are the only constant terms used in the
coefficient substitution below.
\end{lemma}

\begin{proof}
The constant-term identity is exactly
Theorem~\ref{thm:finite-fourier-mode-integral-algorithm}.  It reduces every
entry to a finite zero-sum convolution of the coefficient vectors in
Lemma~\ref{lem:n4-normalized-laurent-representatives}.

For binary products one pairs exponents \(k\) and \(-k\).  For \(Q_2^3\)
there are six ordered zero-sum triples, each contributing \(-1/64\).  For
\(Q_2^2Q_4\), the ordered pairs from \(Q_2^2\) have sums with absolute values
\(1,2,3,4\), giving the grouped numerator \(4+6+12+2\) over \(256\).  For
\(Q_2Q_3^2\), the total contribution for each \(Q_2\)-exponent \(k=\pm1\) is
\(-8/256\), and for each \(k=\pm2\) is \(-1/256\).  For \(Q_2^4\) there are
thirty-six ordered zero-sum quadruples, each contributing \(1/256\).  The
signs in the odd--odd rows come from the displayed Laurent coefficients,
equivalently from \(\ii^2=-1\).

The zero rows are the complete list of odd total-degree products appearing
before the even-power projection:
\[
B_5=a_2a_3,\qquad
B_7=a_2a_5+a_3a_4-\frac12a_2^2a_3 .
\]
They also vanish by parity, because each listed product contains an odd
number of odd modes and is odd under \(y\mapsto-y\).
\end{proof}

\begin{lemma}[Substitution into \(A_4\), \(A_6\), and \(A_8\)]%
\label{lem:n4-entropy-coefficient-substitution}
Let \(a_j=\mu_j u_j\) for \(2\le j\le6\), with \(\mu_2=\sigma^2\), and set
\[
d_t=t^{-2}a_2+t^{-3}a_3+t^{-4}a_4+t^{-5}a_5+t^{-6}a_6 .
\]
The coefficient polynomials before integration are
\[
\begin{array}{c|c}
\text{coefficient} & \text{polynomial before integration}\\
\hline
B_4 & \frac12a_2^2\\[0.25em]
B_6 & a_2a_4+\frac12a_3^2-\frac16a_2^3\\[0.25em]
B_8 &
a_2a_6+a_3a_5+\frac12a_4^2
-\frac12a_2^2a_4-\frac12a_2a_3^2+\frac1{12}a_2^4 .
\end{array}
\]
Using the constant terms in
Lemma~\ref{lem:eighth-order-coefficient-arithmetic}, their Cauchy integrals
are
\[
A_4=\frac12\mu_2^2I(2,2)=\frac{\sigma^4}{8},
\]
\[
A_6=\mu_2\mu_4I(2,4)+\frac12\mu_3^2I(3,3)
-\frac16\mu_2^3I(2,2,2)
=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\]
\[
\begin{aligned}
A_8={}&\mu_2\mu_6I(2,6)+\mu_3\mu_5I(3,5)
+\frac12\mu_4^2I(4,4)\\
&-\frac12\mu_2^2\mu_4I(2,2,4)
-\frac12\mu_2\mu_3^2I(2,3,3)
+\frac1{12}\mu_2^4I(2,2,2,2)\\
={}&\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\end{aligned}
\]
\end{lemma}

\begin{proof}
The Taylor expansion
\[
\Phi(s)=(1+s)\log(1+s)-s
=\frac12s^2-\frac16s^3+\frac1{12}s^4+O(s^5)
\]
and the fact that \(d_t=O(t^{-2})\) show that only powers \(d_t^2,d_t^3,d_t^4\)
can contribute through total order eight.  Expanding these powers gives
\begin{align*}
d_t^2
&=t^{-4}a_2^2+2t^{-5}a_2a_3
+t^{-6}(2a_2a_4+a_3^2) \\
&\quad
+t^{-7}(2a_2a_5+2a_3a_4)
+t^{-8}(2a_2a_6+2a_3a_5+a_4^2)+O(t^{-9}),\\
d_t^3
&=t^{-6}a_2^3+3t^{-7}a_2^2a_3
+t^{-8}(3a_2^2a_4+3a_2a_3^2)+O(t^{-9}),\\
d_t^4
&=t^{-8}a_2^4+O(t^{-9}).
\end{align*}
Collecting the \(t^{-4}\), \(t^{-6}\), and \(t^{-8}\) terms gives the three
displayed \(B\)-polynomials.  The \(t^{-5}\) and \(t^{-7}\) terms have zero
integral by the zero rows in
Lemma~\ref{lem:eighth-order-coefficient-arithmetic}.  Substituting the
constant terms from that lemma gives the displayed formulas for
\(A_4,A_6,A_8\).
\end{proof}

\begin{remark}[Normalization and two-point checks]
The representative \(Q_2\) in
Lemma~\ref{lem:n4-normalized-laurent-representatives} satisfies
\[
Q_2(1)=-1,
\]
agreeing with \(u_2(0)=-1\).  For the symmetric two-point law
\(\rho_\sigma=\tfrac12(\delta_{-\sigma}+\delta_\sigma)\), the moments are
\(\mu_2=\sigma^2\), \(\mu_3=\mu_5=0\), \(\mu_4=\sigma^4\), and
\(\mu_6=\sigma^6\).  Substitution into
Lemma~\ref{lem:n4-entropy-coefficient-substitution} gives
\[
A_6(\rho_\sigma)=\frac{1-8}{64}\sigma^6
=-\frac{7\sigma^6}{64},
\qquad
A_8(\rho_\sigma)=\frac{3-12+12+20}{256}\sigma^8
=\frac{23\sigma^8}{256}.
\]
These are sanity checks on the arithmetic, not additional inputs to the
coefficient-identification theorem.
\end{remark}

\paragraph{Notation block for the sixth/eighth-order layer.}%
\label{par:eighth-order-coefficient-notation}
For a centred moment jet
\[
\mu_j=\int_{\RR}(\gamma-\bar\gamma)^j\dd\nu(\gamma),
\qquad \mu_2=\sigma^2>0,
\]
we use one symbol for each coefficient and defect:
\begin{align}
A_6(\nu)
&:=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\label{eq:n4-notation-a6}\\
A_8(\nu)
&:=\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256},
\label{eq:n4-notation-a8}\\
\Delta_6(\nu)
&:=-\frac{7}{64}\sigma^6-A_6(\nu),
\label{eq:n4-notation-delta6}\\
\Delta_8(\nu)
&:=A_8(\nu)-\frac{23}{256}\sigma^8.
\label{eq:n4-notation-delta8}
\end{align}
Before a finite-moment entropy theorem is invoked, \(A_6,A_8,\Delta_6,\Delta_8\)
are only moment polynomials.  Under the hypotheses of
Theorem~\ref{thm:poisson-kl-even-orders} with \(N=3\), \(A_6(\nu)\) is the
actual \(t^{-6}\) entropy coefficient.  Under the hypotheses of
Theorem~\ref{thm:poisson-kl-eighth}, \(A_8(\nu)\) is the actual \(t^{-8}\)
entropy coefficient as well.  The symbols \(\Delta_6\) and \(\Delta_8\) are
then the corresponding two-point coefficient defects.  No separate
\(\mathrm{alg}\) or \(\mathrm{ent}\) superscript is used in this layer; the
surrounding hypothesis states whether the displayed polynomial is being used
before or after coefficient identification.

\begin{lemma}[Eighth coefficient polynomial before coefficient identification]%
\label{lem:formal-seventh-eighth-coefficient}
The universal degree-eight coefficient polynomial involves only
\(\mathfrak m_2,\ldots,\mathfrak m_6\).  Its evaluation at the moments of
\(\nu\) is the polynomial fixed in
\eqref{eq:n4-notation-a8}:
\begin{equation}\label{eq:formal-eighth-evaluated-polynomial}
A_8(\nu)
:=
\frac{3\mu_2^4-12\mu_2^2\mu_4+9\mu_2\mu_3^2
+12\mu_2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\end{equation}
At this stage \(A_8(\nu)\) is only a moment polynomial; its entropy-coefficient
status is supplied later by Theorem~\ref{thm:poisson-kl-eighth}.
\end{lemma}

\begin{proof}
In the formal definition \eqref{eq:universal-entropy-coefficient}, a
degree-eight monomial has the form
\(\mathfrak m_{n_1}\cdots\mathfrak m_{n_r}\) with \(r\ge2\),
\(n_j\ge2\), and \(n_1+\cdots+n_r=8\).  Hence every index satisfies
\(2\le n_j\le6\); no \(\mathfrak m_7\) or \(\mathfrak m_8\) can occur.
The degree-eight product before integration is
\[
a_2a_6+a_3a_5+\frac12a_4^2
-\frac12a_2^2a_4-\frac12a_2a_3^2+\frac1{12}a_2^4
\]
with \(a_j=\mu_j u_j\).  The coefficient table above gives its integral
\[
\begin{aligned}
&\mu_2\mu_6 I(2,6)+\mu_3\mu_5 I(3,5)
+\frac12\mu_4^2 I(4,4)
-\frac12\mu_2^2\mu_4 I(2,2,4)\\
&\qquad
-\frac12\mu_2\mu_3^2 I(2,3,3)
+\frac1{12}\mu_2^4 I(2,2,2,2),
\end{aligned}
\]
which is exactly \eqref{eq:formal-eighth-evaluated-polynomial} after
substitution of the listed mode integrals.

Thus the displayed finite computation identifies the evaluated polynomial
\(A_8(\nu)\).  No entropy-coefficient status is asserted in this lemma.
\end{proof}

\begin{theorem}[Explicit \(N=4\) universal polynomial coefficient identity at the sixth-moment threshold]%
\label{thm:poisson-kl-eighth}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred sixth absolute moment.  Then,
with
\[
X_c:=\gamma-\bar\gamma,
\qquad
\mu_n:=\int_{\RR}X_c^n\dd\nu(\gamma),
\]
and
\[
h_t:=P_t*\nu,
\qquad
g_t(x):=P_t(x-\bar\gamma),
\]
Theorem~\ref{thm:poisson-kl-even-orders}, applied with \(N=4\), gives
\[
D_{\mathrm{KL}}(h_t\|g_t)
=A_4(\nu)t^{-4}+A_6(\nu)t^{-6}
+A_8(\nu)t^{-8}+o(t^{-8}),
\]
and the explicit Laurent computation gives
\[
A_6(\nu)=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\]
\[
A_8(\nu)=
\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\]
The defect notation is the one fixed in
\eqref{eq:n4-notation-delta6}--\eqref{eq:n4-notation-delta8}.
This is the algebraic threshold for evaluating the displayed universal
moment-polynomial coefficient \(A_8\).
These coefficients use the Laurent normalization of
\(Q_2,\ldots,Q_6\) displayed in
Lemma~\ref{lem:n4-normalized-laurent-representatives}, the constant-term
table in Lemma~\ref{lem:eighth-order-coefficient-arithmetic}, and the
coefficient substitution in
Lemma~\ref{lem:n4-entropy-coefficient-substitution}.  The normalization and
symmetric two-point checks are recorded in the preceding remark.
With these coefficients one has, as $t\to\infty$,
\begin{equation}\label{eq:poisson-kl-eighth}
\begin{split}
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
\\
\quad
+\frac{A_8(\nu)}{t^8}
+o(t^{-8}).
\end{split}
\end{equation}
\end{theorem}

\paragraph{Threshold note.}
This theorem is the sixth-moment coefficient identification.  It supplies the
coefficient ladder through \(t^{-8}\) with an \(o(t^{-8})\) remainder; the
seventh moment is used only later, in
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}, to make the finite-time
\(O(t^{-9})\) remainder quantitative.

\begin{proof}
Theorem~\ref{thm:poisson-kl-even-orders} with \(N=4\) applies because
\(\nu\) has finite centred sixth absolute moment.  It gives the asymptotic
identity through order \(t^{-8}\) with remainder \(o(t^{-8})\).  It remains
only to evaluate the three retained coefficients.

Lemma~\ref{lem:n4-entropy-coefficient-substitution} evaluates the retained
universal coefficients:
\[
A_4(\nu)=\frac{\sigma^4}{8},\qquad
A_6(\nu)=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\]
and
\[
A_8(\nu)
=\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\]
The odd coefficients through order eight vanish by
Lemma~\ref{lem:mode-parity-vanish}, or equivalently by the parity statement
in Theorem~\ref{thm:universal-entropy-coefficients}.  Substituting these
computed values into the \(N=4\) universal-coefficient expansion proves
\eqref{eq:poisson-kl-eighth}.
\end{proof}

\begin{theorem}[Quantitative \(N=4\) remainder under the seventh moment]%
\label{thm:poisson-kl-eighth-seventh-remainder}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred seventh absolute moment.  Use the
notation of Theorem~\ref{thm:poisson-kl-eighth}; in particular
\(A_6(\nu)\) and \(A_8(\nu)\) are the
identified entropy coefficients there.
Let
\[
M_7(\nu):=\int_{\RR}|X_c|^7\,\dd\nu(\gamma),
\qquad
\mathcal M_{\nu,8}:=\sum_{n=2}^{6}(n+1)|\mu_n|,
\]
\[
\mathcal C_{\nu,8}:=110\,M_7(\nu),
\qquad
\mathcal S_{\nu,8}:=7\sum_{n=2}^{6}|\mu_n|+110\,M_7(\nu).
\]
Define the two explicit finite sums
\[
\mathcal P_{\nu,8}:=
\sum_{r=2}^{4}\frac{1}{r(r-1)}
\sum_{\ell=1}^{r}\binom r\ell
\mathcal M_{\nu,8}^{\,r-\ell}\mathcal C_{\nu,8}^{\,\ell}
\]
and
\[
\mathcal Q_{\nu,8}:=
\sum_{r=2}^{4}\frac{1}{r(r-1)}
\sum_{m=9}^{6r}
\sum_{\substack{2\le n_1,\ldots,n_r\le6\\
n_1+\cdots+n_r=m}}
\prod_{j=1}^{r}\bigl((n_j+1)|\mu_{n_j}|\bigr),
\]
with the convention that an empty inner sum is zero.  Finally set
\[
B_{\nu,8}:=\mathcal P_{\nu,8}+\mathcal Q_{\nu,8}
+2\mathcal S_{\nu,8}^{5},
\qquad
T_{\nu,8}:=\max\{1,(2\mathcal S_{\nu,8})^{1/2}\}.
\]
These constants are directly computable from the centred moments
\(\mu_2,\ldots,\mu_6\) and the seventh absolute moment \(M_7(\nu)\);
the numerical values \(7\) and \(110\) are the \(L=6\) closed mode and
density envelopes from
Lemma~\ref{lem:closed-cayley-mode-constant-envelopes}.  No optimized
intermediate constants \(P^{\rm opt}\), \(Q^{\rm opt}\), \(S^{\rm opt}\) are
inputs in this theorem; \(B_{\nu,8}\) and \(T_{\nu,8}\) are the displayed
finite functions of this moment data.  Then for every
\(t\ge T_{\nu,8}\),
\begin{equation}\label{eq:poisson-kl-eighth-remainder}
\begin{split}
\Bigg|D_{\mathrm{KL}}(h_t\|g_t)
&-\frac{\sigma^4}{8t^4}
-\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64\,t^6}
\\
&-\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256\,t^8}
\Bigg|
\le B_{\nu,8}t^{-9}.
\end{split}
\end{equation}
Equivalently, the coefficient-identifying expansion
\eqref{eq:poisson-kl-eighth} improves in this positive-margin regime to
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+O_\nu(t^{-9}).
\]
\end{theorem}

\begin{remark}
The finite centred sixth absolute moment is the \(N=4\) hypothesis
\(2N-2=6\), the algebraic threshold for evaluating the displayed universal
moment-polynomial coefficient \(A_8\).  The seventh moment enters
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} for the quantitative
\(O(t^{-9})\) remainder.
\end{remark}

\begin{proof}
The finite centred seventh absolute moment implies the finite centred sixth
absolute moment needed for Theorem~\ref{thm:poisson-kl-eighth}, so the
coefficient identity is already available.  The same seventh moment is the
\(N=4,\eta=1\) positive-margin hypothesis \(2N-2+\eta=7\) in
\S\ref{subsubsec:finite-moment-constants-variants}.
Lemma~\ref{lem:closed-cayley-mode-constant-envelopes} gives, for \(L=6\),
\[
B_6\le 7,\qquad C_6\le55,\qquad
C_{\nu,6,1}=2C_6M_7(\nu)\le110M_7(\nu)=\mathcal C_{\nu,8}.
\]
It also gives \(\|u_n\|_\infty\le n+1\), hence
\[
M_A^{\rm opt}\le\mathcal M_{\nu,8},
\qquad
S^{\rm opt}\le\mathcal S_{\nu,8},
\qquad
T^{\rm opt}\le T_{\nu,8}.
\]
For the retained-mode tail,
\[
\|u_{n_1}\cdots u_{n_r}\|_{L^1(\omega)}
\le \prod_{j=1}^{r}\|u_{n_j}\|_\infty
\le \prod_{j=1}^{r}(n_j+1),
\]
because \(\omega\) is a probability measure, so
\(Q_4^{\rm opt}(\mu,u)\le\mathcal Q_{\nu,8}\).  The same substitutions give
\[
P_4^{\rm opt}(C_\rho^{\rm opt},M_A^{\rm opt})
\le\mathcal P_{\nu,8},
\qquad
2(S^{\rm opt})^5\le2\mathcal S_{\nu,8}^5 .
\]
The specialized estimate
\eqref{eq:optimized-n4-seventh-envelope} therefore implies
\eqref{eq:poisson-kl-eighth-remainder} with the displayed closed choices of
\(B_{\nu,8}\) and \(T_{\nu,8}\).
\end{proof}

\begin{corollary}[Moment-jet factorization of the eighth-order coefficients]%
\label{cor:eighth-order-jet-factorization}
Let $\nu$ and $\widetilde\nu$ be probability measures on~$\RR$ with means
$\bar\gamma$ and $\widetilde{\bar\gamma}$, positive variances, and finite
centred sixth absolute moments.  Write
\[
\mu_n:=\int(\gamma-\bar\gamma)^n\dd\nu(\gamma),
\qquad
\widetilde\mu_n:=\int(\gamma-\widetilde{\bar\gamma})^n
\dd\widetilde\nu(\gamma).
\]
If
\[
\mu_n=\widetilde\mu_n,
\qquad 2\le n\le6,
\]
then the two matched-mean Poisson entropy expansions have the same retained
eighth-order coefficient jet:
\[
A_6(\nu)=A_6(\widetilde\nu),
\qquad
A_8(\nu)=A_8(\widetilde\nu).
\]
Equivalently, the entropy coefficients \(A_6\) and
\(A_8\) in Theorem~\ref{thm:poisson-kl-eighth}, and hence the
entropy-defect coordinates introduced below after coefficient identification,
factor through the finite centred moment jet $(\mu_2,\ldots,\mu_6)$.
\end{corollary}

\begin{proof}
Apply Theorem~\ref{thm:poisson-kl-eighth} to both laws.  The displayed
formula \eqref{eq:poisson-kl-eighth} expresses the $t^{-4}$ coefficient as
\(\mu_2^2/8\), the $t^{-6}$ coefficient as
\((\mu_2^3+6\mu_3^2-8\mu_2\mu_4)/64\), and the $t^{-8}$ coefficient as
\[
\frac{3\mu_2^4-12\mu_2^2\mu_4+9\mu_2\mu_3^2
+12\mu_2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\]
These three polynomials involve only the centred moments
\(\mu_2,\ldots,\mu_6\).  Under the hypothesis
\(\mu_n=\widetilde\mu_n\) for $2\le n\le6$, the retained coefficients in the
two expansions are therefore equal.

The expressions defining \(A_6\) and
\(A_8\) are precisely the \(t^{-6}\) and \(t^{-8}\)
coefficients just identified.  The entropy-defect variables below are
\[
\Delta_6(\nu)
:=-\frac{7}{64}\mu_2^3-A_6(\nu),
\qquad
\Delta_8(\nu)
:=A_8(\nu)-\frac{23}{256}\mu_2^4 .
\]
Hence these defect variables also depend only on $(\mu_2,\ldots,\mu_6)$.
\end{proof}

\begin{corollary}[Moment-jet equality with seventh-moment remainder]%
\label{cor:eighth-order-jet-factorization-seventh-remainder}
Let $\nu$ and $\widetilde\nu$ satisfy the hypotheses of
Corollary~\ref{cor:eighth-order-jet-factorization}.  Assume in addition that
both laws have finite centred seventh absolute moments.  If
\[
\mu_n=\widetilde\mu_n,
\qquad 2\le n\le6,
\]
then
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
-D_{\mathrm{KL}}(P_t*\widetilde\nu\|P_t(\cdot-\widetilde{\bar\gamma}))
=O_{\nu,\widetilde\nu}(t^{-9}).
\]
\end{corollary}

\begin{proof}
Corollary~\ref{cor:eighth-order-jet-factorization} gives equality of the
retained \(t^{-4}\), \(t^{-6}\), and \(t^{-8}\) coefficients.  The additional
finite centred seventh absolute moments allow
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} to be applied to both
laws.  Subtracting the two estimates \eqref{eq:poisson-kl-eighth-remainder},
and using
\(\max\{B_{\nu,8},B_{\widetilde\nu,8}\}\) after increasing the lower time
threshold if necessary, gives the stated
\(O_{\nu,\widetilde\nu}(t^{-9})\) difference.
\end{proof}

Section~\ref{sec:strip-lattice} records the optional finite-phase check for
the same normalized Laurent products and constant terms displayed in
Lemmas~\ref{lem:n4-normalized-laurent-representatives} and
\ref{lem:eighth-order-coefficient-arithmetic}.  That check uses the same
trigonometric/Laurent normalization as the coefficient computation above but
is not part of the entropy proof.

The next defect corollaries use
\(A_6(\nu)\) and \(A_8(\nu)\) from the
finite-sixth coefficient identity.  They are coefficient-level defect
consequences.

\begin{corollary}[A two-level defect ladder]%
\label{thm:poisson-defect-ladder}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred sixth absolute moment.  Use the
coefficient notation above, after the identifications of
Theorem~\ref{thm:poisson-kl-eighth}.
Let $X:=(\gamma-\bar\gamma)/\sigma$ on the probability space
$(\RR,\nu)$, set
\[
\beta_n:=\EE[X^n],\qquad n\in\NN,\ n\ge1,\qquad
\beta_3:=\EE[X^3],
\qquad
Q_2:=X^2-1-\beta_3X,
\]
and define
\[
Q_3:=XQ_2-\|Q_2\|_{L^2(\nu)}^2X-\frac{\beta_3}{4}Q_2.
\]
Then
\begin{equation}\label{eq:defect-c6}
\frac{64}{\sigma^6}A_6(\nu)
=-7-2\beta_3^2-8\|Q_2\|_{L^2(\nu)}^2,
\end{equation}
and
\begin{equation}\label{eq:defect-c8}
\frac{256}{\sigma^8}A_8(\nu)
=23
+12\|Q_3\|_{L^2(\nu)}^2
+32\|Q_2\|_{L^2(\nu)}^4
+\frac{61}{4}\beta_3^2\|Q_2\|_{L^2(\nu)}^2
+52\|Q_2\|_{L^2(\nu)}^2
+2\beta_3^4
+13\beta_3^2.
\end{equation}
In particular
\begin{equation}\label{eq:defect-c8-lower}
A_8(\nu)\ge \frac{23}{256}\sigma^8,
\end{equation}
and equality holds if and only if
\[
\nu=\frac12\delta_{\bar\gamma+\sigma}
+\frac12\delta_{\bar\gamma-\sigma}.
\]
The identity \eqref{eq:defect-c6} recovers the classical Pearson
moment relation, while \eqref{eq:defect-c8} gives the next higher-order
component in the same defect coordinates.
\end{corollary}

\begin{proof}
Write
\[
\kappa:=\|Q_2\|_{L^2(\nu)}^2=\EE[Q_2^2],
\qquad
L:=\EE[XQ_2^2],
\qquad
M:=\EE[Q_2^3].
\]
Since $Q_2=X^2-1-\beta_3X$, one has
\[
\kappa=\beta_4-1-\beta_3^2,
\]
and therefore
\[
\frac{64}{\sigma^6}A_6(\nu)
=1+6\beta_3^2-8\beta_4
=-7-2\beta_3^2-8\kappa,
\]
which is exactly \eqref{eq:defect-c6}.

The identity $X^2=1+\beta_3X+Q_2$ yields
\[
\beta_5=\beta_3(\beta_3^2+2+2\kappa)+L,
\]
\[
\beta_6=1+3\beta_3^2+3\kappa+\beta_3^4+3\beta_3^2\kappa+3\beta_3L+M.
\]
Hence, if
\[
P_8:=3-12\beta_4+9\beta_3^2+12\beta_6-30\beta_3\beta_5+20\beta_4^2,
\]
then a direct substitution gives
\[
P_8
=23+20\kappa^2+16\beta_3^2\kappa+64\kappa+2\beta_3^4+13\beta_3^2+6\beta_3L+12M.
\]
On the other hand,
using $\EE[XQ_2]=0$, $\EE[Q_2]=0$, $\EE[X^2Q_2^2]=\kappa+\beta_3L+M$, and
$\EE[XQ_2^2]=L$, the definition
$Q_3=XQ_2-\kappa X-(\beta_3/4)Q_2$ gives
\[
\|Q_3\|_{L^2(\nu)}^2
=M+\frac{\beta_3}{2}L+\kappa-\kappa^2+\frac{\beta_3^2}{16}\kappa.
\]
Equivalently,
\[
12\|Q_3\|_{L^2(\nu)}^2
=12M+6\beta_3L+12\kappa-12\kappa^2+\frac34\beta_3^2\kappa.
\]
Substituting this last line into the preceding expression for $P_8$ gives
the exact regrouping
\[
P_8=23+12\|Q_3\|_2^2+32\kappa^2
+\frac{61}{4}\beta_3^2\kappa+52\kappa+2\beta_3^4+13\beta_3^2,
\]
which is \eqref{eq:defect-c8}.  Since
$P_8=256A_8(\nu)/\sigma^8$, inequality
\eqref{eq:defect-c8-lower} follows.

If equality holds in \eqref{eq:defect-c8-lower}, then every non-negative
term on the right-hand side of \eqref{eq:defect-c8} vanishes.  In
particular $\beta_3=0$ and $\|Q_2\|_{L^2(\nu)}=0$, so $X^2=1$
$\nu$-almost everywhere.  Since $\EE[X]=0$ and $\EE[X^2]=1$, it follows
that $X=\pm 1$ with equal weights.  Thus
\[
\nu=\frac12\delta_{\bar\gamma+\sigma}
+\frac12\delta_{\bar\gamma-\sigma}.
\]
The converse is immediate.
\end{proof}

\begin{corollary}[Defect amplification]\label{thm:poisson-defect-amplification}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred sixth absolute moment.  Define
\[
\Delta_6(\nu)
:=-\frac{7}{64}\sigma^6-A_6(\nu),
\qquad
\Delta_8(\nu)
:=A_8(\nu)-\frac{23}{256}\sigma^8,
\]
with \(A_6\) and \(A_8\) as in the notation block.  Set
\[
X:=\frac{\gamma-\bar\gamma}{\sigma},
\qquad
\beta_3:=\EE[X^3],
\qquad
Q_2:=X^2-1-\beta_3X,
\]
\[
\kappa:=\|Q_2\|_{L^2(\nu)}^2,
\qquad
Q_3:=XQ_2-\kappa X-\frac{\beta_3}{4}Q_2.
\]
Then
\[
\Delta_6(\nu)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa,
\]
and
\begin{align}
\Delta_8(\nu)
&=
\frac{13\sigma^2}{8}\Delta_6(\nu)
\notag\\
&\quad
+\frac{\sigma^8}{256}\left(
12\|Q_3\|_{L^2(\nu)}^2
+32\kappa^2
+\frac{61}{4}\beta_3^2\kappa
+2\beta_3^4
\right). \label{eq:defect-amplification}
\end{align}
In particular,
\[
\Delta_8(\nu)\ge
\frac{13\sigma^2}{8}\Delta_6(\nu),
\]
and equality in this proved sixth/eighth-order defect layer holds if and only if
\[
\nu=\frac12\delta_{\bar\gamma+\sigma}
+\frac12\delta_{\bar\gamma-\sigma}.
\]
\end{corollary}

\begin{proof}
Equation~\eqref{eq:defect-c6} gives
\[
\Delta_6(\nu)
=-\frac{\sigma^6}{64}
\left(7+\frac{64}{\sigma^6}A_6(\nu)\right)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa.
\]
Likewise, subtracting $23$ from \eqref{eq:defect-c8} yields
\[
\Delta_8(\nu)
=\frac{\sigma^8}{256}\Bigl(
12\|Q_3\|_{L^2(\nu)}^2
+32\kappa^2
+\frac{61}{4}\beta_3^2\kappa
+52\kappa
+2\beta_3^4
+13\beta_3^2
\Bigr).
\]
By the preceding formula for \(\Delta_6(\nu)\),
\[
\frac{13\sigma^2}{8}\Delta_6(\nu)
=\frac{\sigma^8}{256}\bigl(52\kappa+13\beta_3^2\bigr),
\]
and substituting this identity proves \eqref{eq:defect-amplification}.

If equality holds in the lower bound, then every non-negative term in
\eqref{eq:defect-amplification} vanishes.  In particular,
$\beta_3=0$ and $\kappa=0$, hence $Q_2=X^2-1$ vanishes
$\nu$-almost everywhere.  Since $\EE[X]=0$ and $\EE[X^2]=1$, it follows
that $X=\pm 1$ with equal weights, which is exactly the stated
two-point law.  The converse is immediate.
\end{proof}

\begin{corollary}[Symmetric two-point entropy-defect rigidity]%
\label{cor:poisson-two-point-defect-rigidity}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred sixth absolute moment.  Use
\(A_6(\nu)\) and \(A_8(\nu)\) as in the notation
table.
Define
\[
\Delta_6(\nu)
:=-\frac{7}{64}\sigma^6-A_6(\nu),
\qquad
\Delta_8(\nu)
:=A_8(\nu)-\frac{23}{256}\sigma^8.
\]
Let $\nu_c$ be the centred law of $\gamma-\bar\gamma$ and set
\[
\rho_\sigma:=\frac12(\delta_{-\sigma}+\delta_\sigma).
\]
Then, among all laws with the same mean and variance in this moment class,
\[
A_6(\nu)\le -\frac{7}{64}\sigma^6,
\qquad
A_8(\nu)\ge \frac{23}{256}\sigma^8.
\]
Moreover the following conditions are equivalent:
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=*]
\item $\nu_c=\rho_\sigma$, equivalently
$\nu=\frac12\delta_{\bar\gamma-\sigma}
+\frac12\delta_{\bar\gamma+\sigma}$;
\item \(A_6(\nu)=-\frac{7}{64}\sigma^6\);
\item \(A_8(\nu)=\frac{23}{256}\sigma^8\);
\item \(\Delta_6(\nu)=
\Delta_8(\nu)=0\);
\item the amplification bound is sharp,
\[
\Delta_8(\nu)
=\frac{13\sigma^2}{8}\Delta_6(\nu).
\]
\end{enumerate}
\end{corollary}

\begin{remark}
This rigidity statement is only for the sixth/eighth-order layer proved in
this paper.  No all-order defect hierarchy is asserted here.
\end{remark}

\begin{proof}
Use the notation of Corollaries~\ref{thm:poisson-defect-ladder}
and~\ref{thm:poisson-defect-amplification}.  From
\eqref{eq:defect-c6},
\[
A_6(\nu)+\frac{7}{64}\sigma^6
=-\frac{\sigma^6}{32}\beta_3^2-\frac{\sigma^6}{8}\kappa
\le0,
\qquad
\kappa:=\|Q_2\|_{L^2(\nu)}^2.
\]
Hence \(A_6(\nu)\) is maximized by, and only by, laws for which
$\beta_3=0$ and $\kappa=0$.  In that case
$Q_2=X^2-1$ vanishes almost surely, so $X=\pm1$ almost surely; since
$\EE X=0$ and $\EE X^2=1$, the two signs have equal mass.  This is exactly
$\nu_c=\rho_\sigma$.  Conversely, for $\nu_c=\rho_\sigma$ one has
$\beta_3=0$ and $Q_2=0$, so equality holds in the displayed bound for
\(A_6(\nu)\).

The lower bound for \(A_8(\nu)\) and the characterization of equality
in that bound are precisely \eqref{eq:defect-c8-lower} and the equality
statement in Corollary~\ref{thm:poisson-defect-ladder}.  The definitions of
\(\Delta_6\) and \(\Delta_8\) convert equality
in the two coefficient bounds into
\(\Delta_6(\nu)=0\) and
\(\Delta_8(\nu)=0\), respectively.  Finally,
Corollary~\ref{thm:poisson-defect-amplification} states that equality in
\[
\Delta_8(\nu)\ge
\frac{13\sigma^2}{8}\Delta_6(\nu)
\]
holds if and only if $\nu_c=\rho_\sigma$.  Combining these equivalences
proves all the listed conditions equivalent.
\end{proof}

