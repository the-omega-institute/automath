% sec_introduction.tex -- Introduction
\section{Introduction}\label{sec:introduction}

The Cayley transform
\[
\iota(x)=\frac{1+\ii x}{1-\ii x}
\]
identifies the one-point compactification of \(\RR\) with \(\TT\), and pulls
normalized Haar measure back to the Cauchy law
\[
\omega(\dd x)=\frac{1}{\pi(1+x^{2})}\dd x=P_1(x)\dd x .
\]
This normalization makes the fixed-law Poisson comparison
\begin{equation}\label{eq:intro-main-functional}
D_{\rm KL}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
\end{equation}
a finite coefficient problem in Cayley--Chebyshev modes.  The input law
\(\nu\) is fixed, \(\bar\gamma\) is its mean, and \(t\to\infty\) is the
Poisson scale.

\subsection*{Main theorem statements}

The next six theorem statements are the theorem-level map of the paper.
They are stated with their operative assumptions and coefficients; proof
mechanisms and comparisons follow after the theorem block, while conditional
finite-time post-processing is separated into
Appendix~\ref{app:conditional-post-processing}.  The multidimensional coefficients are denoted
\(\mathcal A_m^{(d)}(\nu)\) throughout; the one-dimensional coefficients use
the unsuperscripted symbols \(A_k(\nu)\).

\begin{theorem}[Multidimensional finite-\(L\) Poisson entropy jet, \(L\ge d+1\)]%
\label{thm:intro-multidimensional-jet}
Let \(d\ge1\), let \(L\ge\max\{2,d+1\}\) be an integer, and let
\(\nu\) be a probability measure on \(\RR^d\) with mean \(\bar\gamma\) and
finite centred \(L\)th absolute moment.  Then
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=\sum_{m=4}^{L+2}\mathcal A_m^{(d)}(\nu)t^{-m}
+o(t^{-(L+2)}),
\]
where \(\mathcal A_m^{(d)}\) is given by the tensor formula
\[
\mathcal A_m^{(d)}(\nu)=
\sum_{r=2}^{\lfloor m/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{|\alpha_1|,\ldots,|\alpha_r|\ge2\\
|\alpha_1|+\cdots+|\alpha_r|=m\\
|\alpha_j|\le L}}
\mu_{\alpha_1}\cdots\mu_{\alpha_r}
\int_{\RR^d}\prod_{j=1}^rU_{\alpha_j}^{(d)}(y)\,\Omega_d(\dd y).
\]
Here \(U_\alpha^{(d)}\) are the Taylor modes of
\[
\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^{(d+1)/2},
\qquad
\mu_\alpha=\EE[X_c^\alpha],
\]
and \(\Omega_d=P_1^{(d)}(y)\dd y\).  Equivalently, the radial Poisson quotient
\((P_t^{(d)}*\nu)/P_t^{(d)}(\cdot-\bar\gamma)\) has the finite-moment
Taylor-mode expansion of
Theorem~\ref{thm:multidimensional-density-quotient}, and
Theorem~\ref{thm:multidimensional-poisson-entropy-jets} transfers it through
the KL integrand.
\end{theorem}

\begin{proposition}[Formal multidimensional covariance layer under finite covariance]%
\label{prop:intro-multidimensional-formal-covariance-layer}
\label{thm:intro-multidimensional-leading-coefficient}
Let \(d\ge1\), and let \(\nu\) be a probability measure on \(\RR^d\) with
finite centred second moment.  Put
\[
X_c=\gamma-\bar\gamma,
\]
where \(\bar\gamma=\int_{\RR^d}\gamma\,\dd\nu(\gamma)\), and let
\[
\Sigma=\EE[X_cX_c^{\mathsf T}],
\qquad
\Sigma_0=\Sigma-d^{-1}(\operatorname{tr}\Sigma)I_d .
\]
Then the formal quadratic covariance layer obtained by inserting the
degree-two density mode into the quadratic term of the KL integrand is
\begin{equation}\label{eq:intro-multidim-leading}
A_{4,\mathrm{quad}}^{(d)}(\nu)
=
c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2,
\end{equation}
where
\[
c_d^{\rm iso}=
\frac{3(d+1)(7d+9)}
{4d(d+3)(d+5)(d+7)},
\qquad
c_d^{\rm tr}=
\frac{3(d+1)(d+3)}
{4(d+5)(d+7)}.
\]
This is Proposition~\ref{prop:multidim-formal-covariance-layer}.  The coefficient
formula assumes only \(\EE|X_c|^2<\infty\); it is a covariance-layer identity
and depends on no higher tensor moments.  The formula is positive and vanishes
exactly when \(\Sigma=0\).  No KL asymptotic is asserted from finite covariance
alone.
\end{proposition}

\begin{corollary}[Leading multidimensional KL coefficient under \((\mathrm{MPJ}_{d,L})\)]%
\label{cor:intro-multidimensional-leading-kl-coefficient}
Let \(d\ge1\), let \(L\ge\max\{2,d+1\}\), and assume that
\((\nu,d,L)\) satisfies \((\mathrm{MPJ}_{d,L})\).  With
\(A_{4,\mathrm{quad}}^{(d)}(\nu)\) as in
\eqref{eq:intro-multidim-leading},
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=A_{4,\mathrm{quad}}^{(d)}(\nu)t^{-4}+o(t^{-4}).
\]
\end{corollary}

\begin{remark}[Multidimensional hypothesis barrier]
Theorem~\ref{thm:intro-multidimensional-jet},
Proposition~\ref{prop:intro-multidimensional-formal-covariance-layer}, and
Corollary~\ref{cor:intro-multidimensional-leading-kl-coefficient} have separate
hypothesis levels.  The finite-\(L\) theorem supplies the full entropy jet and
uses the dimension-dependent quotient-remainder hypothesis.  The proposition is
only the formal quadratic-layer calculation under finite covariance.  The
corollary is where the proposition's coefficient is inserted into the entropy
jet.  Thus neither a full finite jet nor the asymptotic statement
\(D_{\rm KL}=A_{4,\mathrm{quad}}^{(d)}t^{-4}+o(t^{-4})\) is inferred from
\(\EE|X_c|^2<\infty\); only the displayed covariance functional
\(A_{4,\mathrm{quad}}^{(d)}\) is a covariance-only identity.
\end{remark}

Before the one-dimensional hierarchy is stated, we separate its two
threshold statements.  The leading \(A_4\) scale has the sharp analytic
variance threshold in the next theorem.  For the higher finite hierarchy,
Theorem~\ref{thm:poisson-kl-even-orders} identifies fixed
Cayley--Chebyshev moment-polynomial coefficients; its scope is recorded once,
after that theorem, in the boxed paragraph on coefficient evaluability.

\begin{theorem}[Sharp leading tail threshold in dimension one]%
\label{thm:intro-sharp-leading-threshold}
Let \(\nu\) be a probability measure on \(\RR\) with finite mean
\(\bar\gamma\), and put
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad
H(t)=D_{\mathrm{KL}}(h_t\|g_t).
\]
If the centred variance \(\mu_2=\int(\gamma-\bar\gamma)^2\,\dd\nu(\gamma)\)
is finite, then
\[
\lim_{t\to\infty}t^4H(t)=\frac{\mu_2^2}{8}.
\]
If the centred variance is infinite, then
\[
\lim_{t\to\infty}t^4H(t)=+\infty.
\]
Equivalently, finite centred second moment is necessary and sufficient for a
finite leading \(t^{-4}\) matched-Poisson KL coefficient, and the finite value
is the universal coefficient \(A_4(\nu)=\mu_2^2/8\).  This is
Theorem~\ref{thm:sharp-leading-kl-variance-threshold}.  It is the analytic
threshold result in the paper; the higher \(A_{2N}\) statements below are
coefficient-evaluability statements for the displayed universal polynomials.
\end{theorem}

\begin{theorem}[One-dimensional coefficient identification at finite \((2N-2)\)-moment level]%
\label{thm:intro-one-dimensional-universal-expansion}
Let \(N\ge2\), let \(\nu\) be a probability measure on \(\RR\) with mean
\(\bar\gamma\), and assume that \(X_c=\gamma-\bar\gamma\) has finite
\((2N-2)\)th absolute moment.  Put
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad
\mu_n=\int X_c^n\,\dd\nu(\gamma).
\]
Then
\begin{equation}\label{eq:intro-one-dimensional-transfer}
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=
\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N}).
\end{equation}
Each \(A_{2m}\) is the finite-moment evaluation of a universal polynomial in
the centred moments, computed by the Cayley mode integrals in
Theorem~\ref{thm:universal-entropy-coefficients}; all retained odd
coefficients vanish.  More explicitly, for \(4\le k\le2N\),
\[
A_k(\nu)=
\sum_{r=2}^{\lfloor k/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{n_1+\cdots+n_r=k\\2\le n_j\le 2N-2}}
\mu_{n_1}\cdots\mu_{n_r}
\int_{\RR}u_{n_1}\cdots u_{n_r}\,\dd\omega .
\]
This is Theorem~\ref{thm:poisson-kl-even-orders}; the boxed paragraph
following it fixes the coefficient-evaluability scope.  The algebraic
obstruction to evaluating the displayed top polynomial from lower moments is
isolated in
Proposition~\ref{prop:poisson-kl-moment-threshold-sharpness}, while
Proposition~\ref{prop:log-tail-counterexample-top-moment} gives explicit
power-tail examples in which the missing top moment is replaced by a
tail-dependent \(t^{-2N}\log t\) entropy layer.
\end{theorem}

\begin{corollary}[Coefficient defect stability at the fourth-moment layer]%
\label{cor:intro-defect-certificate}
Let \(\nu\) have mean \(\bar\gamma\), variance \(\sigma^2>0\), and finite
centred fourth absolute moment.  The \(N=3\) case of
Theorem~\ref{thm:intro-one-dimensional-universal-expansion} identifies
\[
A_6(\nu)=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64}
\]
as the genuine \(t^{-6}\) coefficient.  Define the sixth-order defect
\[
\Delta_6(\nu)=-\frac{7}{64}\sigma^6-A_6(\nu).
\]
Corollary~\ref{cor:coefficient-defect-coupling} converts this fixed-variance
defect into a \(W_2\) bound against
\(\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma)\).  This is a
coefficient-level statement under the finite fourth absolute moment: no
finite-time KL value, eighth-order remainder bound, sixth absolute moment, or
approximation radius is part of the corollary.  The stronger \(N=4\) theorem
below is needed only for the \(A_8\) layer and for the seventh-moment
quantitative remainder.
\end{corollary}

\begin{theorem}[Explicit \(N=4\) sixth/eighth coefficient layer]%
\label{thm:intro-explicit-n4-layer}
Let \(\nu\) be a probability measure on \(\RR\) with mean \(\bar\gamma\),
variance \(\sigma^2>0\), and finite centred sixth absolute moment.  Then
Theorem~\ref{thm:poisson-kl-eighth} identifies
\[
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=
\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+o(t^{-8}),
\]
where
\[
A_6(\nu)
=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\]
and
\[
A_8(\nu)
=
\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\]
If the centred seventh absolute moment is finite, then
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} improves the last
remainder to \(O(t^{-9})\).  The coefficient computation is the finite
zero-mode Laurent arithmetic displayed in the paper from the representatives
\[
Q_n(w)=\sum_{1\le |k|\le n}q_{n,k}w^k,\qquad
q_{n,k}=\lambda_n\,\operatorname{sgn}(k)^{\,n\bmod 2}
\binom{n-1}{|k|-1}
\]
from Lemma~\ref{lem:n4-normalized-laurent-representatives}, together with the
zero-sum convolution rule for Laurent constant terms.
\end{theorem}

\begin{theorem}[Poisson quotient corollary of Hardy--Stein]%
\label{thm:intro-kl-tail-identity}
Let \(\nu\) be a probability measure on \(\RR\) with mean \(\bar\gamma\) and
finite centred second moment.  For
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad u_t=h_t/g_t,
\]
define
\[
I_\nu(t)=
\frac1\pi\iint_{\RR^2}
\frac{g_t(x)\left(
u_t(x)\log\!\bigl(u_t(x)/u_t(y)\bigr)-u_t(x)+u_t(y)
\right)}
{(x-y)^2}\dd x\dd y .
\]
Then \(I_\nu\in L^1_{\rm loc}(0,\infty)\) and
\[
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=\int_t^\infty I_\nu(s)\dd s,
\]
for every \(t>0\).  This is Theorem~\ref{thm:poisson-kl-doob-tail}.  The
underlying Bregman differential identity is the KL/Cauchy specialization of
the polarized Hardy--Stein framework; the paper's verification is the moving
quotient domain: Lemma~\ref{lem:poisson-kl-l1-ac-chain-rule} proves the
\(L^1\)-absolute-continuity chain rule for the KL path, and
Lemma~\ref{lem:kl-tail-chain-rule-cutoff-passage} proves the bounded quotient
range, cutoff passage, and \(L^1\) Bregman dominator for
\(u_t=(P_t*\nu)/P_t(\cdot-\bar\gamma)\).  Consequently
Proposition~\ref{prop:dissipation-tail-coefficient-expansion} transfers the
already proved entropy coefficient expansion to the dissipation tail.
\end{theorem}

\subsection*{Scope and nonclaims}

The results above concern the fixed-law matched Poisson KL functional
\eqref{eq:intro-main-functional}.  Theorem~\ref{thm:intro-sharp-leading-threshold}
is a genuine analytic iff threshold for the leading \(t^{-4}\) scale.  The
higher one-dimensional hierarchy is a coefficient-evaluability hierarchy in
the sense of the boxed paragraph after
Theorem~\ref{thm:poisson-kl-even-orders}.  Appendix~\ref{app:conditional-post-processing}
contains only conditional bookkeeping after independent coefficient and
remainder bounds have already been fixed.

\subsection*{Cayley coefficient mechanism}

After the rescaling \(x=\bar\gamma+ty\), a translate by \(X_c=\gamma-\bar\gamma\)
is encoded by
\[
R_\varepsilon(y)=\frac{1+y^2}{1+(y-\varepsilon)^2},
\qquad \varepsilon=X_c/t .
\]
The Cayley angle \(y=\tan\theta\) converts the Taylor modes of
\(R_\varepsilon\) into finite trigonometric, equivalently Laurent, modes.
Theorem~\ref{thm:cayley-chebyshev-mode} gives the mode formula,
Theorem~\ref{thm:universal-entropy-coefficients} defines the formal entropy
coefficients by finite Cauchy-weighted mode integrals, and
Theorem~\ref{thm:finite-fourier-mode-integral-algorithm} reduces those
integrals to Laurent constant-term extraction.

The analytic input is separate from the algebra.  The finite-moment quotient
remainder is proved in Theorem~\ref{thm:finite-moment-density-remainder} and
Lemma~\ref{lem:exact-moment-density-truncation}; the retained-order nonlinear
transfer through \((1+u)\log(1+u)-u\) is
Proposition~\ref{prop:density-to-entropy-descent} and
Theorem~\ref{prop:coefficient-identification-poisson-entropy-transfer}.  The
finite Laurent arithmetic needed for the explicit \(N=4\) layer is displayed
in Lemmas~\ref{lem:n4-normalized-laurent-representatives}--\ref{lem:n4-entropy-coefficient-substitution}.

\subsection*{Position relative to existing work}

The table below is indexed by the theorem statements in this introduction.
It is complementary to the method-organized comparison in
Appendix~\ref{app:related-work-scope}: the appendix groups the literature by
technique, while this table records the closest theorem-level comparator for
each of the paper's main assertions.

\begingroup
\footnotesize
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.08}
\begin{longtable}{p{0.24\linewidth}p{0.32\linewidth}p{0.30\linewidth}}
\hline
Present theorem-level assertion & Closest theorem-level comparator & Exact theorem-level separation\\
\hline
\endfirsthead
\hline
Present theorem-level assertion & Closest theorem-level comparator & Exact theorem-level separation\\
\hline
\endhead
Theorems~\ref{thm:intro-multidimensional-jet},
\ref{thm:multidimensional-density-quotient}, and
\ref{thm:multidimensional-poisson-entropy-jets}: finite-\(L\) expansion of
the radial Poisson quotient and the matched KL jet
\(D_{\rm KL}=\sum_{m=4}^{L+2}\mathcal A_m^{(d)}t^{-m}+o(t^{-(L+2)})\).
& Anker--Papageorgiou--Zhang
\cite[Theorem~1.2]{AnkerPapageorgiouZhang2023HeatSymmetricSpaces}
prove that a heat-equation solution on a noncompact symmetric space with
bi-\(K\)-invariant \(L^1\) initial datum converges in \(L^1\) to its mass
times the heat kernel, and that the statement fails in general without
bi-\(K\)-invariance.
& Their theorem is a linear heat-flow profile theorem for an evolving datum.
The present theorem is a fixed-law Poisson theorem: it expands the quotient
\((P_t^{(d)}*\nu)/P_t^{(d)}(\cdot-\bar\gamma)\), then applies the nonlinear
KL \(\Phi(s)=(1+s)\log(1+s)-s\) transfer and identifies the coefficient
tensors \(\mathcal A_m^{(d)}\).\\
\hline
Proposition~\ref{prop:intro-multidimensional-formal-covariance-layer} and
Corollary~\ref{cor:intro-multidimensional-leading-kl-coefficient}: the formal
quadratic covariance layer
\(A_{4,\mathrm{quad}}^{(d)}
=c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2\)
under finite covariance, and its use as the KL leading coefficient only under
\((\mathrm{MPJ}_{d,L})\).
& The closest current JFA stability comparator is
Chen--Lu--Tang \cite[Theorems~1.1 and~1.4]{ChenLuTang2023LogSobolevStability},
which proves sharp local stability for logarithmic Sobolev and Moser--Onofri
inequalities on the sphere, with distance to the optimizer manifold controlled
by the corresponding inequality deficit.
& That theorem controls distance to conformal extremizers from a
functional-inequality deficit.  The present proposition is not a stability
theorem and not a finite-covariance KL asymptotic: it is the explicit
beta-prime/spherical integral evaluation of the degree-two Poisson quotient
inside the quadratic KL layer; the KL asymptotic is asserted only after the
separate finite-jet hypothesis.\\
\hline
Theorem~\ref{thm:intro-sharp-leading-threshold}: finite centred variance is
necessary and sufficient for \(t^4D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma))\)
to have the finite limit \(\mu_2^2/8\); with infinite variance the same
quantity diverges to \(+\infty\).
& Bobkov--G{\"o}tze \cite[Theorem~1.2]{BobkovGotzeEsscher2025} give
necessary and sufficient Laplace-transform/Hessian conditions for
infinite-order R\'enyi convergence of normalized sums in the
multidimensional CLT under strict sub-Gaussian hypotheses.
& Both are iff statements for an information-theoretic limit.  The JFA theorem
concerns changing laws \(n^{-1/2}(X_1+\cdots+X_n)\) and a Gaussian reference
in \(D_\infty\).  The present theorem concerns one fixed input law, Poisson
scale \(t\), a matched Cauchy reference, and an exact second Cauchy-mode
projection that yields the sharp variance/infinite-variance dichotomy for
KL.\\
\hline
Theorem~\ref{thm:intro-one-dimensional-universal-expansion}: for finite
\((2N-2)\)-moment,
\(D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\sum_{m=2}^NA_{2m}(\nu)t^{-2m}+o(t^{-2N})\),
where \(A_{2m}\) are universal Cayley-mode moment polynomials.
& Entropic and Fisher-information CLT expansions of
Bobkov--Chistyakov--G{\"o}tze
\cite{BobkovChistyakovGotzeEdgeworth2013,
BobkovChistyakovGotzeBerryEsseen2014,BobkovChistyakovGotzeFisher2014}
and the JFA Esscher criterion \cite[Theorem~1.2]{BobkovGotzeEsscher2025}
are the closest theorem-level entropy/divergence asymptotics.
& Those theorems expand or characterize information convergence for
normalized sums toward Gaussian laws.  The present theorem does not involve a
CLT parameter or Gaussian reference; it identifies the fixed-law matched
Cauchy coefficients by finite Laurent constant terms
\([z^0]\prod_jQ_{n_j}(z)\), and it states a coefficient-evaluability moment
level rather than a universal analytic tail threshold.\\
\hline
Propositions~\ref{prop:analytic-first-evaluability-obstruction},
\ref{prop:poisson-kl-moment-threshold-sharpness}, and
\ref{prop:log-tail-counterexample-top-moment}: the universal \(A_{2N}\)
polynomial cannot be evaluated from lower moments, and explicit symmetric
power-tail laws replace the missing top coefficient by a tail-dependent
\(t^{-2N}\log t\) layer.
& Moment and transform conditions in the entropy/Fisher CLT papers above, and
the R\'enyi CLT results
\cite{BobkovGotzeRenyi2025,BobkovGotzeRenyiSurvey2025}, are the closest
theorem-level threshold comparisons.
& The existing theorems give conditions for convergence or rates along a
changing-law limit.  These propositions are fixed-law counterexamples at the
coefficient layer: the obstruction is the top monomial
\(\kappa_Nm_2m_{2N-2}\) and the analytic replacement is an explicit
heavy-tail entropy contribution.\\
\hline
Theorem~\ref{thm:intro-explicit-n4-layer}: the \(N=4\) sixth/eighth layer
with \(A_6=(\sigma^6+6\mu_3^2-8\sigma^2\mu_4)/64\) and the displayed
degree-eight polynomial \(A_8\) in
Theorem~\ref{thm:poisson-kl-eighth}.
& The same entropy/divergence asymptotic theorems for CLT and Esscher
transforms are the nearest theorem-level comparators; no current JFA theorem
known to us gives this fixed Cauchy coefficient arithmetic.
& The theorem is finite coefficient algebra, not a qualitative convergence
criterion: it evaluates the \(t^{-6}\) and \(t^{-8}\) KL coefficients by
zero-mode convolution of Cayley Laurent representatives and includes the
seventh-moment \(O(t^{-9})\) remainder upgrade.\\
\hline
Theorem~\ref{thm:intro-kl-tail-identity}: for the moving quotient
\(u_t=(P_t*\nu)/P_t(\cdot-\bar\gamma)\),
\(D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\int_t^\infty I_\nu(s)\dd s\),
with the displayed two-point logarithmic Bregman integrand.
& Bogdan--Gutowski--Pietruska-Pa{\l}uba
\cite[Theorems~3.1 and~4.1]{BogdanGutowskiPietruskaPaluba2025PolarizedHardyStein}
prove the Hardy--Stein identity for vector \(L^p\) functions and the
polarized Hardy--Stein identity for the canonical pairing of real \(L^p\)
functions, using Bregman co-divergences.  The earlier theorem of
Ba{\~n}uelos--Bogdan--Luks
\cite[Theorem~3.2]{BanuelosBogdanLuks2016HardyStein} gives the
Hardy--Stein identity for semigroups generated by symmetric L\'evy measures.
& The cited JFA theorem supplies the abstract Bregman identity.  The present
result is the Cauchy/KL quotient-domain corollary: it verifies the moving
denominator \(g_t\), the \(L^1\)-AC KL chain rule, bounded quotient range,
cutoff passage, Bregman domination, and the tail limit \(H(t)\to0\); the
coefficient consequences then come from the already proved Cayley expansion.\\
\hline
Corollary~\ref{cor:intro-defect-certificate}: the sixth coefficient defect
\(\Delta_6=-7\sigma^6/64-A_6\) controls
\(W_2(\nu_c,\frac12(\delta_{-\sigma}+\delta_\sigma))\) at the coefficient
level.
& Chen--Lu--Tang \cite[Theorems~1.1 and~1.4]{ChenLuTang2023LogSobolevStability}
prove sharp spherical log-Sobolev/Moser--Onofri stability, and
L{\'o}pez-Rivera--Shenfeld \cite[Theorems~1.2, 1.5, and~1.7]{LopezRiveraShenfeld2025PoissonTransport}
construct a \(1\)-Lipschitz Poisson transport map onto ultra-log-concave laws
on \(\mathbb N\) and derive modified log-Sobolev/\(\Phi\)-Sobolev
inequalities.
& The overlap is only the conclusion type, a distance controlled by a
defect or Poisson-kernel structure.  Here the defect is the explicit
Cayley--Poisson coefficient polynomial \(A_6\), and the target is the
symmetric two-point law; no conformal extremizer, log-Sobolev deficit, or
Poisson transport map is used.\\
\hline
\end{longtable}
\endgroup

Large-time heat and fractional-heat profile expansions, entropy and
\(\Phi\)-entropy methods, Bregman identities, Hardy--Stein formulae,
moment-threshold arguments, Cauchy information formulae, and stability results
each touch one component of the proof architecture.  The present paper
combines a fixed input law, a matched Cauchy/Poisson reference kernel,
quotient expansion, finite Cayley coefficient extraction, and a quotient
Hardy--Stein tail identity.  Appendix~\ref{app:related-work-scope} gives a
method-based comparison with the adjacent literature and identifies which
analytic or algebraic ingredient is supplied here in each comparison class.

\subsection*{Organization}

Section~\ref{sec:preliminaries} fixes notation.  Sections~\ref{sec:cayley-gate}
and~\ref{sec:haar-pullback} record the Cayley and Haar-pullback
normalizations.  Section~\ref{sec:entropy-asymptotics} proves the
multidimensional entropy jets, the one-dimensional coefficient hierarchy, the
sharp leading variance threshold, the explicit sixth/eighth-order layer, and
the coefficient-defect consequence.
Section~\ref{sec:doob-phi-entropy}
applies the KL Hardy--Stein identity to the Poisson quotient and proves the dissipation-tail coefficient
expansion.  Appendix~\ref{app:related-work-scope} gives related-work context;
Appendix~\ref{app:entropy-integrals} records the weighted trigonometric
normalization behind the Laurent arithmetic;
Appendix~\ref{app:haar-pullback-uniqueness}
records the endpoint-normalized Cayley uniqueness statement; and
Appendix~\ref{app:conditional-post-processing} records the conditional
finite-time error propagation used after the coefficient and remainder
constants have been fixed.
