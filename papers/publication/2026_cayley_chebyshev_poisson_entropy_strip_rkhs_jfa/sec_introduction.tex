% sec_introduction.tex -- Introduction
\section{Introduction}\label{sec:introduction}

The Cayley transform
\[
\iota(x)=\frac{1+\ii x}{1-\ii x}
\]
identifies the one-point compactification $\overline\RR$ of the line with
the circle~$\TT$.  Under this identification normalized Haar measure pulls
back to the Cauchy law
\[
\frac{1}{\pi(1+x^{2})}\dd x=P_1(x)\dd x .
\]
This normalization is the common coordinate system of the paper.  It puts
Poisson smoothing on~$\RR$, circle Fourier analysis, Cauchy-weighted
entropy integrals, and the auxiliary secant observation kernel into one
calculus.  The Cauchy law is used here as the Haar/Poisson normalization for
the single matched fixed-law functional
\[
D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma)).
\]
This is the paper's scope.  The input law is fixed, the time parameter
\(t\) tends to infinity, and the coefficients are universal
moment-polynomial entropy jets for this Poisson comparison.  The comparison
with normalized-sum entropy asymptotics, heat and fractional-flow profile
asymptotics, transport or functional-inequality stability, and parametric
Cauchy divergence formulae is made theorem by theorem below.

\subsection*{Main results}

The first main result extends beyond the Cayley coordinate.  For the radial
Poisson kernel on \(\RR^d\), Theorems
\ref{thm:multidimensional-poisson-entropy-jets} and
\ref{thm:multidim-leading-positive-defect} prove a finite-moment
entropy-jet expansion under an integer truncation
\(L\ge\max\{2,d+1\}\) and the corresponding finite centred \(L\)th absolute
moment for the fixed-law comparison between
\(P_t^{(d)}*\nu\) and the translate of \(P_t^{(d)}\) matched to the mean of
\(\nu\).  The leading \(t^{-4}\) coefficient is not just an extracted Taylor
term: it splits as a positive sum of the squared isotropic second-moment
defect and the squared traceless covariance.  For \(d\ge2\), this proved
leading \(\mathcal A_4^{(d)}\) asymptotic is still part of the same
\(L\ge d+1\) finite-moment theorem and is not a finite-covariance-only
statement.  The remaining coefficients are universal tensor-moment
polynomials formed from the multivariable Taylor coefficients of
\[
\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^{(d+1)/2}.
\]
Thus the analytic finite-moment passage is a multidimensional Poisson theorem
with a dimension-dependent tail hypothesis and an explicit positive
covariance-defect structure at the first entropy level; the scalar Laurent
computation below is its explicit one-dimensional specialization.

The one-dimensional Cayley theorem is the explicit computable specialization
of that entropy-jet mechanism for precisely the matched KL functional used in
the rest of the paper, with the input law kept fixed.  For every
\(N\ge2\), if a probability law~\(\nu\) has mean $\bar\gamma$ and finite
centred $(2N-2)$th absolute moment, then
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N})
\qquad(t\to\infty).
\]
This is Theorem~\ref{thm:poisson-kl-even-orders}.  The threshold statement is
only a coefficient-identification statement: by
Proposition~\ref{prop:poisson-kl-moment-threshold-sharpness}, the displayed
universal moment-polynomial coefficient \(A_{2N}\) contains a nonzero
top-moment term and therefore cannot be evaluated from the centred moments
of orders at most \(2N-3\) alone; the centred \((2N-2)\)nd moment is part of
the displayed universal coefficient.
No claim is made there that an unrelated analytic asymptotic under weaker
hypotheses must fail or have the same coefficient.

The second main result is the explicit eighth-order layer.  Theorem~\ref{thm:poisson-kl-eighth}
computes the \(t^{-4}\), \(t^{-6}\), and \(t^{-8}\) coefficients at the
finite-sixth-moment threshold, and
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} adds the finite-time
\(O(t^{-9})\) estimate under the corresponding seventh-moment hypothesis.
Those coefficients define the defect coordinates used in
Corollary~\ref{cor:coefficient-defect-coupling}, which bounds the
fixed-variance moment defect in \(W_2\).  The finite-time version,
Theorem~\ref{thm:finite-time-poisson-entropy-stability}, is deliberately
stated first on controlled seventh-moment shells: once the shell parameters
\(\sigma>0\) and \(M\ge\sigma^7\) are fixed, equivalently once the normalized
radius \(\widehat M=M/\sigma^7\ge1\) is fixed, the theorem displays the
constants \(C_{\sigma,M}^{(8)},B_{\sigma,M}^{(8)},T_{\sigma,M}^{(8)}\), and
the measured KL value is used only through
\(\mathfrak D_t^{(6)}(\nu)\).  A separate oracle fixed-law variant,
Theorem~\ref{thm:oracle-finite-time-poisson-entropy-stability}, records the
same algebra when certificate bounds \(C,B,T\) for the eighth coefficient and
the remainder are supplied for a particular law.
The density quotient expansion, entropy transfer theorem, explicit \(N=4\)
coefficient theorem, defect corollary, and Doob-tail theorem are summarized
before the entropy proofs in Subsection~\ref{sec:entropy-theorem-map}.

Thus the new ingredient has two layers.  The analytic layer is the
finite-moment Poisson entropy-jet theorem for radial Poisson kernels in
dimension \(d\), stated under \(L\ge\max\{2,d+1\}\) and
\(\EE|X_c|^L<\infty\), together with the exact positive decomposition of the
leading coefficient into isotropic and traceless covariance defects under
that same hypothesis.  The
one-dimensional layer is explicit finite-moment Laurent arithmetic for
arbitrary fixed input laws under Poisson smoothing: the
\(t^{-6}\) and \(t^{-8}\) coefficient table, the resulting sixth/eighth-order
defect ladder, and the conditional finite-time estimate obtained once
eighth-order data are supplied.

Two supporting tools keep the argument checkable without changing this
hierarchy.  The KL Doob-tail section gives the dissipation-tail
representation \(D_{\mathrm{KL}}=\int_t^\infty I_\nu(s)\dd s\) and the
corresponding tail-window coefficient expansion.  A final auxiliary-data
remark records, outside the entropy proof, the finite root-of-unity check for
Laurent arithmetic and the direct full-profile formula by which external
Poisson observation data can supply the finite moment jet used by the entropy
layer.

\subsection*{Cayley coefficient mechanism}

The comparison kernel
\[
R_\varepsilon(y):=\frac{1+y^2}{1+(y-\varepsilon)^2}
\]
measures the ratio between a translated Poisson profile and the reference
Cauchy density after the rescaling $x=\bar\gamma+ty$.  The Cayley angle
$y=\tan\theta$ converts the Taylor modes of $R_\varepsilon$ into finite
trigonometric expressions governed by Chebyshev polynomials of the second
kind.  Theorem~\ref{thm:cayley-chebyshev-mode} records this mode formula.
Theorem~\ref{thm:universal-entropy-coefficients} then defines the formal
coefficient polynomials $A_k^{\rm form}$ by finite Cauchy-weighted mode
integrals and proves the odd-order cancellation.  At this stage the
calculation is algebraic: a law is evaluated only through the moment
interface in
Proposition~\ref{prop:universal-entropy-coefficient-evaluation}.

The analytic content is the finite-moment passage from this algebraic
calculus to actual large-time entropy coefficients.
The main entropy-transfer theorem is the coefficient-identification statement.
Stronger-moment fixed-law rates, moment-ball uniformity, and the associated
constant bookkeeping are collected in the appendix.

Finite phase sampling is only an exactness check for finite Laurent
polynomials after the coefficient calculus has already been formed.  The main
entropy proof uses Haar/Laurent constant-coefficient extraction directly.
The short final auxiliary-data remark records the root-of-unity averaging
identity below the explicit Fourier-support barrier; no finite-grid theorem
is used to prove the entropy jets.

\subsection*{Full-profile moment-supply interface}

The entropy expansion and defect theorems are moment theorems.  External
observation data enter only by supplying the moment jet
\((\mu_2,\ldots,\mu_6)\).  The interface used here is not a finite-sample
observation theorem: it assumes full continuous knowledge of a Fourier
transform of a finite signed measure.  At a fixed observation time \(t_0\),
write
\[
\kappa_{t_0}:=\operatorname{Law}((\gamma-\bar\gamma)/t_0).
\]
When the first centred absolute moment is finite,
\(\lambda_{t_0}(\dd s):=s\,\kappa_{t_0}(\dd s)\) is a finite signed measure,
and only then do we set
\[
q_{t_0}(\xi)
=\pi\widehat{\lambda_{t_0}}(\xi)
=\pi\int_{\RR}s e^{-\ii s\xi}\kappa_{t_0}(\dd s).
\]
Full continuous knowledge of \(q_{t_0}\) determines \(\lambda_{t_0}\) by Fourier
uniqueness, hence determines \(\kappa_{t_0}\) away from zero and the atom at
zero by total mass.  If the sixth centred absolute moment is finite, then
\(q_{t_0}\in C^5(\RR)\) and differentiating the finite signed measure
Fourier transform gives
\[
\mu_j
=
t_0^j\,\frac{\ii^{\,j-1}}{\pi}\,
q_{t_0}^{(j-1)}(0),
\qquad 2\le j\le6 .
\]

A separate exact lattice reconstruction requires both integer-orbit samples
and the additional support condition
\(\nu\{\gamma:\gamma-\bar\gamma\in t_0\ZZ\}=1\).  This is a non-generic
certificate, not a replacement for the full-profile moment-supply interface:
away from that support class, integer samples are projection data rather than
the full continuous profile.  The detailed lattice calculation is
therefore kept out of the main entropy argument and appears only as a final
remark.

\subsection*{Theorem-by-theorem related work}

There are two comparison layers.  The first is conceptual and is not confined
to JFA: it separates the present fixed-law Poisson smoothing theorem from
stable-law entropy limits, fractional Fisher information, parametric Cauchy
divergence formulae, and M\"obius Cauchy parameter geometry.

\begin{center}
\begingroup
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.16}
\begin{tabular}{p{0.21\textwidth}p{0.30\textwidth}p{0.39\textwidth}}
\hline
Neighboring literature & Representative results & Separation from the present results\\
\hline
Stable-law entropic CLT and entropy asymptotics
&
\cite{Barron1986,CarlenSoffer1991,BobkovChistyakovGotze2013,BobkovChistyakovGotzeEdgeworth2013,BobkovChistyakovGotzeBerryEsseen2014,BobkovGotzeEsscher2025}
&
Those theorems change the law through normalized convolution, stable
normalization, Edgeworth correction, or Esscher tilting.  Here \(\nu\) is a
single fixed input law; the parameter \(t\) is the Poisson smoothing scale, and
the KL functional is always compared with the matched translated Cauchy
profile.\\
\hline
Fractional Fisher information for stable limits
&
\cite{JohnsonStableDeBruijn2013,Toscani2015,Toscani2016}
&
Fractional Fisher methods identify entropy production or strengthened CLT
monotonicity toward stable laws.  The results here do not use a stable-law
Fisher deficit; they compute finite-moment Laurent coefficients of a fixed
Poisson--Cauchy quotient and then convert selected coefficients into moment
defects.\\
\hline
Cauchy \(f\)-divergence and KL formulae
&
\cite{ChyzakNielsen2019CauchyKL,NielsenOkamura2023Cauchy,BouhlelRousseau2022MultivariateCauchyKL,Verdu2023Cauchy}
&
Those formulae are closed forms inside parametric Cauchy families, including
central multivariate Cauchy subfamilies.  In the present problem
\(P_t*\nu\) is generally not Cauchy; the theorem is an asymptotic
finite-moment expansion for a nonparametric Poisson-smoothed law.\\
\hline
M\"obius Cauchy geometry and information geometry
&
\cite{KatoJones2010MobiusCircle,KatoMcCullagh2020SphereCauchyMobius,Nielsen2020CauchyVoronoi}
&
Those works describe Cauchy families and their parameter geometry under
M\"obius or information-geometric structure.  Here the Cayley map is a
normalization that turns the Cauchy weight into Haar measure so that Laurent
mode extraction evaluates Poisson entropy coefficients.\\
\hline
\end{tabular}
\endgroup
\end{center}

The second layer is a same-journal theorem map: each row pairs one present
core result with recent JFA work that is closest in analytic role.
Appendix~\ref{app:related-work-scope} gives a fuller version of both layers.

\begin{center}
\begingroup
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{p{0.22\textwidth}p{0.28\textwidth}p{0.18\textwidth}p{0.24\textwidth}}
\hline
Present result & Recent JFA comparator & Comparator type &
Separation\\
\hline
Theorems~\ref{thm:multidimensional-poisson-entropy-jets} and
\ref{thm:multidim-leading-positive-defect}: radial Poisson entropy jets
on \(\RR^d\) under \(L\ge\max\{2,d+1\}\)
&
Anker--Papageorgiou--Zhang~\cite{AnkerPapageorgiouZhang2023HeatSymmetricSpaces};
Crismale--De Luca--Kubin--Ninno--Ponsiglione~\cite{CrismaleDeLucaKubinNinnoPonsiglione2023}
&
large-time heat or fractional-flow asymptotics
&
Those works describe evolving solutions or variational flow limits.  Here the
input law is fixed and the scalar KL number
\(D_{\rm KL}(P_t^{(d)}*\nu\|P_t^{(d)}(\cdot-\bar\gamma))\) is expanded into
tensor-moment entropy coefficients; the first coefficient is a positive
isotropic/traceless covariance defect under the same \(L\ge d+1\)
finite-moment hypothesis, not under finite covariance alone.\\
\hline
Theorem~\ref{thm:poisson-kl-even-orders}: one-dimensional finite-moment
entropy transfer
&
Bobkov--G\"otze~\cite{BobkovGotzeEsscher2025}
&
normalized-sum entropy asymptotics and Esscher transforms
&
The comparator results concern normalized sums, stable-law convergence,
Edgeworth-type entropy expansions, or Esscher transforms.  Here \(\nu\) is
not rescaled as a summand law; \(t\) is the Poisson smoothing scale and the
output is a finite-moment fixed-law coefficient hierarchy.\\
\hline
Theorems~\ref{thm:poisson-kl-eighth}--\ref{thm:poisson-kl-eighth-seventh-remainder}:
explicit \(t^{-6}\), \(t^{-8}\) coefficients and seventh-moment remainder
&
Bobkov--G\"otze~\cite{BobkovGotzeEsscher2025};
Lopez-Rivera--Shenfeld~\cite{LopezRiveraShenfeld2025PoissonTransport}
&
entropy asymptotics for sums; Poisson transport
&
Neither comparator gives the Laurent coefficient table for \(P_t*\nu\)
against the matched Cauchy profile.  The theorem computes \(A_6\), \(A_8\),
and an \(O(t^{-9})\) fixed-law remainder under explicit finite moment
assumptions.\\
\hline
Corollaries~\ref{thm:poisson-defect-ladder}--\ref{cor:coefficient-defect-coupling}:
coefficient defects and \(W_2\) consequences
&
Chen--Lu--Tang~\cite{ChenLuTang2023LogSobolevStability};
Brigati--Dolbeault--Simonov~\cite{BrigatiDolbeaultSimonov2024LogSobolev}
and Carlen~\cite{Carlen2025LogHLS}
&
functional-inequality stability
&
Those JFA comparators prove sharp or quantitative stability for functional
inequalities.  The present statements identify fixed-law Poisson KL
coefficients first, then convert coefficient defects into moment and \(W_2\)
information for the matched Poisson comparison.\\
\hline
Theorem~\ref{thm:finite-time-poisson-entropy-stability}: controlled-shell
finite-time estimate
&
Chen--Lu--Tang~\cite{ChenLuTang2023LogSobolevStability};
Gozlan--Herry--Peccati~\cite{GozlanHerryPeccati2021PointMeasureTransport}
&
quantitative stability and transport
&
The estimate is a controlled-shell consequence for the fixed Poisson
comparison.  The moment shell and constants are fixed before the measured KL
value is inserted.\\
\hline
Theorem~\ref{thm:poisson-kl-doob-tail} and
Proposition~\ref{prop:dissipation-tail-coefficient-expansion}: Cauchy Doob
tail identity and tail-window coefficients
&
Lopez-Rivera--Shenfeld~\cite{LopezRiveraShenfeld2025PoissonTransport};
Erbar--Huesmann--Jalowy--M\"uller~\cite{ErbarHuesmannJalowyMuller2025StationaryPointTransport}
&
Poisson transport and entropy gradient-flow structures
&
The row here is an exact KL dissipation-tail identity for the same quotient
\(h_t/g_t\) used in the coefficient theorem, followed by coefficient recovery
from entropy tails and finite multiplicative windows.\\
\hline
Appendix~\ref{sec:strip-lattice}: auxiliary finite-check and observation input
&
Romero--Ulanovskii--Zlotnikov~\cite{RomeroUlanovskiiZlotnikov2024}
&
Gaussian shift-invariant sampling
&
The auxiliary material records a finite root-of-unity check for Laurent
arithmetic, a moment-supply interface, and a non-generic exact lattice
certificate; the entropy theorems require only the displayed moments and
constants.\\
\hline
\end{tabular}
\endgroup
\end{center}

Across the rows, the added ingredient is the coefficient-level
finite-moment transfer: the quotient expansion is controlled in the weighted
spaces needed for KL, the Bregman Taylor series is converted into an actual
entropy asymptotic, and the one-dimensional Laurent extraction evaluates the
resulting universal coefficients.

\subsection*{Organization}

Section~\ref{sec:preliminaries} fixes notation and conventions.
Subsection~\ref{sec:cayley-gate} records the Cayley formulas, and
Subsection~\ref{sec:haar-pullback} records the Haar-pullback normalization.
Section~\ref{sec:entropy-asymptotics} proves the main entropy results: the
multidimensional Poisson entropy-jet theorem, its one-dimensional
Cayley--Chebyshev coefficient specialization, and the seventh-moment
eighth-order defect layer, followed by the coefficient-level and finite-time
moment-defect \(W_2\) consequences.  Section~\ref{sec:doob-phi-entropy}
records the KL dissipation-tail identity for the same Poisson comparison
quotient and derives the dissipation-tail coefficient expansion from the
main coefficient theorem.  Appendix~\ref{sec:strip-lattice} records optional
finite Laurent checks, full-profile moment-supply input, and the limited
exact lattice case.  Appendix~\ref{app:related-work-scope}
collects the related-work
comparison.  Appendix~\ref{app:entropy-integrals}
records the weighted trigonometric integrals and coefficient arithmetic used
in the eighth-order expansion.
Appendix~\ref{app:haar-pullback-uniqueness} records the endpoint-normalized
uniqueness statement for the Cayley chart.
Appendix~\ref{app:finite-time-defect-estimates} records the constant-regime
bookkeeping and controlled-shell uniform version for the finite-time
moment-defect estimates.
