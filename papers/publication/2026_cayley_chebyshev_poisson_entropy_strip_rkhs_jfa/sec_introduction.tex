% sec_introduction.tex -- Introduction
\section{Introduction}\label{sec:introduction}

The Cayley transform
\[
\iota(x)=\frac{1+\ii x}{1-\ii x}
\]
identifies the one-point compactification of \(\RR\) with \(\TT\), and pulls
normalized Haar measure back to the Cauchy law
\[
\omega(\dd x)=\frac{1}{\pi(1+x^{2})}\dd x=P_1(x)\dd x .
\]
This normalization makes the fixed-law Poisson comparison
\begin{equation}\label{eq:intro-main-functional}
D_{\rm KL}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
\end{equation}
a finite coefficient problem in Cayley--Chebyshev modes.  The input law
\(\nu\) is fixed, \(\bar\gamma\) is its mean, and \(t\to\infty\) is the
Poisson scale.

\subsection*{Main theorem statements}

The next five theorem statements are the theorem-level map of the paper.
They are stated with their operative assumptions and coefficients; proof
mechanisms, comparisons, finite-time consequences, and nonclaims follow after
the theorem block.  The multidimensional coefficients are denoted
\(\mathcal A_m^{(d)}(\nu)\) throughout; the one-dimensional coefficients use
the unsuperscripted symbols \(A_k(\nu)\).

\begin{theorem}[Multidimensional fixed-law Poisson entropy jet]%
\label{thm:intro-multidimensional-jet}
Let \(d\ge1\), let \(L\ge\max\{2,d+1\}\) be an integer, and let
\(\nu\) be a probability measure on \(\RR^d\) with mean \(\bar\gamma\) and
finite centred \(L\)th absolute moment.  Then
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=\sum_{m=4}^{L+2}\mathcal A_m^{(d)}(\nu)t^{-m}
+o(t^{-(L+2)}),
\]
where \(\mathcal A_m^{(d)}\) is given by the tensor formula
\[
\mathcal A_m^{(d)}(\nu)=
\sum_{r=2}^{\lfloor m/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{|\alpha_1|,\ldots,|\alpha_r|\ge2\\
|\alpha_1|+\cdots+|\alpha_r|=m\\
|\alpha_j|\le L}}
\mu_{\alpha_1}\cdots\mu_{\alpha_r}
\int_{\RR^d}\prod_{j=1}^rU_{\alpha_j}^{(d)}(y)\,\Omega_d(\dd y).
\]
Here \(U_\alpha^{(d)}\) are the Taylor modes of
\[
\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^{(d+1)/2},
\qquad
\mu_\alpha=\EE[X_c^\alpha],
\]
and \(\Omega_d=P_1^{(d)}(y)\dd y\).  Equivalently, the radial Poisson quotient
\((P_t^{(d)}*\nu)/P_t^{(d)}(\cdot-\bar\gamma)\) has the finite-moment
Taylor-mode expansion of
Theorem~\ref{thm:multidimensional-density-quotient}, and
Theorem~\ref{thm:multidimensional-poisson-entropy-jets} transfers it through
the KL integrand.
\end{theorem}

\begin{theorem}[Leading multidimensional covariance coefficient]%
\label{thm:intro-multidimensional-leading-coefficient}
Under the hypotheses of Theorem~\ref{thm:intro-multidimensional-jet}, let
\[
\Sigma=\EE[X_cX_c^{\mathsf T}],
\qquad
\Sigma_0=\Sigma-d^{-1}(\operatorname{tr}\Sigma)I_d .
\]
Then the leading coefficient in the multidimensional Poisson KL jet is
\begin{equation}\label{eq:intro-multidim-leading}
\mathcal A_4^{(d)}(\nu)
=
c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2,
\end{equation}
where
\[
c_d^{\rm iso}=
\frac{3(d+1)(7d+9)}
{4d(d+3)(d+5)(d+7)},
\qquad
c_d^{\rm tr}=
\frac{3(d+1)(d+3)}
{4(d+5)(d+7)}.
\]
This is Theorem~\ref{thm:multidim-leading-positive-defect}.  The formula is
positive and vanishes exactly when \(\Sigma=0\).  In dimensions \(d\ge2\),
the assumption \(L\ge d+1\) remains part of the theorem; the coefficient
formula itself depends only on covariance, but this paper does not assert a
finite-covariance-only leading asymptotic.
\end{theorem}

\begin{theorem}[One-dimensional finite-moment coefficient hierarchy]%
\label{thm:intro-one-dimensional-universal-expansion}
Let \(N\ge2\), let \(\nu\) be a probability measure on \(\RR\) with mean
\(\bar\gamma\), and assume that \(X_c=\gamma-\bar\gamma\) has finite
\((2N-2)\)th absolute moment.  Put
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad
\mu_n=\int X_c^n\,\dd\nu(\gamma).
\]
Then
\begin{equation}\label{eq:intro-one-dimensional-transfer}
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=
\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N}).
\end{equation}
Each \(A_{2m}\) is the finite-moment evaluation of a universal polynomial in
the centred moments, computed by the Cayley mode integrals in
Theorem~\ref{thm:universal-entropy-coefficients}; all retained odd
coefficients vanish.  More explicitly, for \(4\le k\le2N\),
\[
A_k(\nu)=
\sum_{r=2}^{\lfloor k/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{n_1+\cdots+n_r=k\\2\le n_j\le 2N-2}}
\mu_{n_1}\cdots\mu_{n_r}
\int_{\RR}u_{n_1}\cdots u_{n_r}\,\dd\omega .
\]
This is Theorem~\ref{thm:poisson-kl-even-orders}.  The displayed top
coefficient \(A_{2N}\) is evaluable exactly at the centred
\((2N-2)\)nd-moment level as a universal polynomial; the polynomial threshold
is isolated in
Proposition~\ref{prop:poisson-kl-moment-threshold-sharpness}.
\end{theorem}

\begin{theorem}[Explicit \(N=4\) sixth/eighth coefficient layer]%
\label{thm:intro-explicit-n4-layer}
Let \(\nu\) be a probability measure on \(\RR\) with mean \(\bar\gamma\),
variance \(\sigma^2>0\), and finite centred sixth absolute moment.  Then
Theorem~\ref{thm:poisson-kl-eighth} identifies
\[
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=
\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+o(t^{-8}),
\]
where
\[
A_6(\nu)
=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\]
and
\[
A_8(\nu)
=
\frac{3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2}{256}.
\]
If the centred seventh absolute moment is finite, then
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} improves the last
remainder to \(O(t^{-9})\).  The coefficient computation is reproducible
from the explicit Laurent representatives
\[
Q_n(w)=\sum_{1\le |k|\le n}q_{n,k}w^k,\qquad
q_{n,k}=\lambda_n\,\operatorname{sgn}(k)^{\,n\bmod 2}
\binom{n-1}{|k|-1}
\]
from Lemma~\ref{lem:n4-normalized-laurent-representatives}, together with the
zero-sum convolution rule for Laurent constant terms.
\end{theorem}

\begin{theorem}[KL Hardy--Stein tail identity]%
\label{thm:intro-kl-tail-identity}
Let \(\nu\) be a probability measure on \(\RR\) with mean \(\bar\gamma\) and
finite centred second moment.  For
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad u_t=h_t/g_t,
\]
define
\[
I_\nu(t)=
\frac1\pi\iint_{\RR^2}
\frac{g_t(x)\left(
u_t(x)\log\frac{u_t(x)}{u_t(y)}-u_t(x)+u_t(y)
\right)}
{(x-y)^2}\dd x\dd y .
\]
Then \(I_\nu\in L^1_{\rm loc}(0,\infty)\) and
\[
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=\int_t^\infty I_\nu(s)\dd s,
\]
for every \(t>0\).  This is Theorem~\ref{thm:poisson-kl-doob-tail}; the
quotient-domain chain rule and cutoff passage are proved in
Lemma~\ref{lem:kl-tail-chain-rule-cutoff-passage}.  Consequently
Proposition~\ref{prop:dissipation-tail-coefficient-expansion} transfers the
coefficient expansion to the dissipation tail.
\end{theorem}

\subsection*{Defect and finite-time consequences}

\begin{corollary}[Coefficient defect stability]%
\label{cor:intro-defect-certificate}
In the setting of Theorem~\ref{thm:intro-explicit-n4-layer}, the explicit
\(N=4\) coefficients define the sixth-order defect
\[
\Delta_6(\nu)=-\frac{7}{64}\sigma^6-A_6(\nu).
\]
Corollary~\ref{cor:coefficient-defect-coupling} converts this fixed-variance
defect into a \(W_2\) bound against
\(\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma)\).  If a finite-time KL
value is used, Appendix
Lemma~\ref{lem:controlled-shell-conditional-certificate} propagates the
resulting deterministic error only after the shell constants, the
eighth-order coefficient bound, the remainder bound, and the observation
error radius have been supplied independently.
\end{corollary}

\subsection*{Scope and nonclaims}

The results above concern the fixed-law matched Poisson KL functional
\eqref{eq:intro-main-functional}.  They are not normalized-sum entropy
asymptotics, parametric Cauchy-family divergence formulae, or
functional-inequality stability theorems.  The moment thresholds are
coefficient-evaluation thresholds for the displayed universal polynomials,
not assertions that every possible weaker-tail or renormalized entropy
asymptotic fails; Proposition~\ref{prop:coefficient-evaluation-not-analytic-necessity}
spells out that distinction.  The finite-time estimates are conditional
bookkeeping after independent coefficient and remainder bounds have already
been fixed.

\subsection*{Cayley coefficient mechanism}

After the rescaling \(x=\bar\gamma+ty\), a translate by \(X_c=\gamma-\bar\gamma\)
is encoded by
\[
R_\varepsilon(y)=\frac{1+y^2}{1+(y-\varepsilon)^2},
\qquad \varepsilon=X_c/t .
\]
The Cayley angle \(y=\tan\theta\) converts the Taylor modes of
\(R_\varepsilon\) into finite trigonometric, equivalently Laurent, modes.
Theorem~\ref{thm:cayley-chebyshev-mode} gives the mode formula,
Theorem~\ref{thm:universal-entropy-coefficients} defines the formal entropy
coefficients by finite Cauchy-weighted mode integrals, and
Theorem~\ref{thm:finite-fourier-mode-integral-algorithm} reduces those
integrals to Laurent constant-term extraction.

The analytic input is separate from the algebra.  The finite-moment quotient
remainder is proved in Theorem~\ref{thm:finite-moment-density-remainder} and
Lemma~\ref{lem:exact-moment-density-truncation}; the retained-order nonlinear
transfer through \((1+u)\log(1+u)-u\) is
Proposition~\ref{prop:density-to-entropy-descent} and
Theorem~\ref{prop:coefficient-identification-poisson-entropy-transfer}.  The
finite Laurent arithmetic needed for the explicit \(N=4\) layer is displayed
in Lemmas~\ref{lem:n4-normalized-laurent-representatives}--\ref{lem:n4-entropy-coefficient-substitution}.

\subsection*{Position relative to existing work}

The closest analytic background is finite-moment large-time profile expansion
for heat, fractional heat, and stable kernels
\cite{DuoandikoetxeaZuazua1992,EscobedoZuazua1991,KarchSchonbek2001,
IshigeKawakami2012,IshigeKawakamiMichihisa2017,
IshigeKawakamiMichihisa2022RefinedFractional,
AnkerPapageorgiouZhang2023HeatSymmetricSpaces,Papageorgiou2024FractionalLargeTime}.
Those results expand an evolved density, kernel, or operator profile.  Here
the density quotient expansion is only the input to a nonlinear scalar KL
expansion, whose coefficients contain products of moment modes.

The heavy-tail functional-inequality literature is used only for comparison
unless it supplies the specific nonlocal Bregman identity invoked in
Section~\ref{sec:doob-phi-entropy}.  Weighted Poincar\'e, weighted
logarithmic Sobolev, weak functional-inequality, and stable-law
entropy-dissipation results
\cite{BobkovLedoux2009WeightedCauchyPoincare,
CattiauxGozlanGuillinRoberto2010HeavyTailFunctionalIneq,
CattiauxGuillinWu2011WeightedLogSobolev,
Huguet2024GeneralisedCauchyPoincare,
Huguet2026GeneralisedCauchyLogSobolev,
BobkovChistyakovGotzeStableFisher2014,
JohnsonStableDeBruijn2013,Toscani2015,Toscani2016,
Toscani2019StablePoincare}
are therefore background references: they locate the heavy-tail Cauchy/stable
setting and the entropy-production vocabulary, but no stability or
log-Sobolev-type theorem is deduced from them.  The genuinely adjacent
references for the paper's KL-tail identity are the Hardy--Stein and
Sobolev--Bregman identities cited in the last row of
Table~\ref{tab:intro-jfa-theorem-comparison}.

\subsection*{Profile expansions versus nonlinear KL transfer}

The distinction from fractional heat profile expansions is structural rather
than terminological.  In the finite-moment profile literature, a typical
large-time statement has the density or solution form
\begin{equation}\label{eq:intro-heat-profile-comparison}
u(t,x)=
\sum_{|\alpha|\le K}M_\alpha(\varphi)\,\partial^\alpha p_t(x)
+R_K(t,x),
\end{equation}
under weighted \(L^1\) moment assumptions on the initial datum.  The
coefficients \(M_\alpha(\varphi)\) multiply derivatives of the linear kernel,
and the theorem controls the remaining profile \(R_K\).

The present paper first proves a quotient expansion and then transfers it
through the nonlinear KL integrand.  In the notation used below, this gives
\begin{equation}\label{eq:intro-kl-transfer-comparison}
D_{\rm KL}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma)\bigr)
=
\int_{\RR}
\Phi\!\left(
\sum_{j=2}^{L}D_j(y)t^{-j}+\rho_L(t,y)
\right)\,\dd\Omega(y),
\qquad
\Phi(s)=(1+s)\log(1+s)-s .
\end{equation}
The novelty used in the coefficient theorem is therefore not a new linear
heat-kernel profile expansion, but the nonlinear KL transfer from this quotient
expansion, where \(\Phi\) forces products of the retained modes.
Thus the novelty claimed here is not a new heat-kernel profile expansion.
It is the finite-moment transfer from a matched Poisson quotient expansion to
a scalar KL asymptotic.  The nonlinearity of \(\Phi\) is what forces the
coefficient products, for example the \(D_2D_4\), \(D_3^2\), and \(D_2^3\)
terms in the sixth-order coefficient and the longer product list in the
eighth-order coefficient.

\begin{center}
\captionof{table}{Theorem-level comparison with nearby JFA theorem classes.}
\label{tab:intro-jfa-theorem-comparison}
\begingroup
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.13}
\begin{tabular}{p{0.20\textwidth}p{0.25\textwidth}p{0.43\textwidth}}
\hline
Present theorem & Nearby theorem class & Precise separation\\
\hline
Theorems~\ref{thm:multidimensional-density-quotient}--\ref{thm:multidim-leading-positive-defect}
& Fixed-law Poisson KL asymptotics, separated from density-profile
asymptotics in recent JFA heat/fractional-flow papers
\cite{AnkerPapageorgiouZhang2023HeatSymmetricSpaces,
CrismaleDeLucaKubinNinnoPonsiglione2023}
& The density quotient is not the final object: after the radial Poisson
quotient expansion, the theorem transfers the quotient through \(\Phi\) and
extracts a scalar KL coefficient, including the positive
isotropic/traceless covariance decomposition \eqref{eq:intro-multidim-leading}.
Profile theorems instead control \(u(t,\cdot)\), \(p_t\)-derivative
expansions, or flow limits.\\
\hline
Theorem~\ref{thm:poisson-kl-even-orders}
& Fixed-law Poisson KL asymptotics, separated from entropic CLT asymptotics
\cite{CarlenSoffer1991,BobkovChistyakovGotze2013,
BobkovGotzeRenyi2025,BobkovGotzeEsscher2025}
& The probability law \(\nu\) is fixed and only the Poisson scale \(t\)
diverges.  The theorem gives universal moment-polynomial coefficients for
the matched Cauchy KL functional, not a normalized-sum R\'enyi,
Esscher-transform, or stable-law relative-entropy limit.\\
\hline
Theorem~\ref{thm:poisson-kl-eighth} and
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}
& Explicit fixed-law KL coefficients, separated from both density-profile
coefficients and parametric Cauchy divergence formulae
\cite{ChyzakNielsen2019CauchyKL,NielsenOkamura2023Cauchy,
BouhlelRousseau2022MultivariateCauchyKL,Verdu2023Cauchy}
& The sixth/eighth coefficients are zero-mode Laurent evaluations of products
created by \(\Phi\), with a seventh-moment remainder estimate.  They are not
linear profile coefficients \(M_\alpha\partial^\alpha p_t\) and not
closed-form divergences inside a finite-dimensional Cauchy family.\\
\hline
Corollary~\ref{cor:coefficient-defect-coupling} and
Appendix Lemma~\ref{lem:controlled-shell-conditional-certificate}
& Coefficient-level stability and conditional fixed-time bookkeeping,
separated from recent JFA
functional-inequality stability theorems and from heavy-tail Cauchy
functional inequalities
\cite{ChenLuTang2023LogSobolevStability,
BrigatiDolbeaultSimonov2024LogSobolev,Carlen2025LogHLS,
BobkovLedoux2009WeightedCauchyPoincare,
CattiauxGozlanGuillinRoberto2010HeavyTailFunctionalIneq,
CattiauxGuillinWu2011WeightedLogSobolev,
Huguet2024GeneralisedCauchyPoincare,
Huguet2026GeneralisedCauchyLogSobolev}
& The estimates use preassigned coefficient and remainder certificates and
then insert one KL statistic with a separate error certificate.  They do not
prove stability of extremizers for
log-Sobolev, Moser--Onofri, logarithmic Hardy--Littlewood--Sobolev, weighted
Poincar\'e, weighted logarithmic Sobolev, or weak heavy-tail inequalities.\\
\hline
Theorem~\ref{thm:poisson-kl-doob-tail}
& KL tail identity, separated from recent JFA Hardy--Stein identity theorems
\cite{BanuelosBogdanLuks2016HardyStein,
BanuelosKim2019HardySteinNonsymmetric,
BogdanGutowskiPietruskaPaluba2025PolarizedHardyStein}
& The Hardy--Stein/Bregman mechanism is used as a domain-checked identity for
the Cauchy generator and moving Doob quotient \(u_t=(P_t*\nu)/g_t\).  The
new result is the matched-KL tail representation and its coefficient-tail
transfer, not a general Hardy--Stein theorem.\\
\hline
\end{tabular}
\endgroup
\end{center}

Appendix~\ref{app:related-work-scope} records the broader citation clusters.
The strip-RKHS and integer-orbit sampling constructions are not inputs to the
entropy theorem map, the moment hypotheses, or the Laurent coefficient
identifications.

\subsection*{Organization}

Section~\ref{sec:preliminaries} fixes notation.  Sections~\ref{sec:cayley-gate}
and~\ref{sec:haar-pullback} record the Cayley and Haar-pullback
normalizations.  Section~\ref{sec:entropy-asymptotics} proves the
multidimensional entropy jets, the one-dimensional coefficient hierarchy, the
explicit sixth/eighth-order layer, and the defect consequences.
Section~\ref{sec:doob-phi-entropy}
proves the KL Hardy--Stein tail identity and the dissipation-tail coefficient
expansion.  Appendix~\ref{app:related-work-scope} gives related-work context;
Appendix~\ref{app:entropy-integrals} records the weighted trigonometric
integrals and Laurent arithmetic; Appendix~\ref{app:haar-pullback-uniqueness}
records the endpoint-normalized Cayley uniqueness statement; and
Appendix~\ref{app:finite-time-defect-estimates} records the constant regimes
and deterministic error-propagation lemma used by the finite-time certificate.
