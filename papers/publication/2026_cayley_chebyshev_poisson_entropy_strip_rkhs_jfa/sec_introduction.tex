% sec_introduction.tex -- Introduction
\section{Introduction}\label{sec:introduction}

The Cayley transform
\[
\iota(x)=\frac{1+\ii x}{1-\ii x}
\]
identifies the one-point compactification of \(\RR\) with \(\TT\), and pulls
normalized Haar measure back to the Cauchy law
\[
\omega(\dd x)=\frac{1}{\pi(1+x^{2})}\dd x=P_1(x)\dd x .
\]
This is the endpoint-normalized form of the classical Cauchy/wrapped-Cauchy
M{\"o}bius correspondence in circular statistics
\cite{KentTyler1988WrappedCauchy,McCullagh1992CauchyModels,
McCullagh1996CauchyMobius,
KatoJones2010MobiusCircle,KatoMcCullagh2020SphereCauchyMobius}.
This normalization makes the fixed-law Poisson comparison
\begin{equation}\label{eq:intro-main-functional}
D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar{\gamma})\bigr)
\end{equation}
the object whose large-\(t\) behaviour is studied in Cayley--Chebyshev
modes.  The input law \(\nu\) is fixed, \(\bar{\gamma}\) is its mean, and
\(t\to\infty\) is the Poisson scale.

The basic calculation is a quotient calculation.  Let
\[
X_c=\gamma-\bar{\gamma}
\]
be the centred translation variable.  With \(x=\bar{\gamma}+ty\), translation
by \(X_c\) is encoded by
\[
R_\varepsilon(y)=\frac{1+y^2}{1+(y-\varepsilon)^2},
\qquad \varepsilon=X_c/t .
\]
The Cayley angle \(y=\tan\theta\) turns the Taylor coefficients of
\(R_\varepsilon\) into finite trigonometric, equivalently Laurent, modes.
After the matched Cauchy profile has been divided out, the quotient expansion
is therefore reduced to finite mode algebra; the KL expansion is then the
ordinary Taylor expansion of \((1+u)\log(1+u)-u\) in
\(L^\infty\cap L^1(\omega)\).

\subsection*{Main contribution}

The paper proves six results about the matched Poisson KL functional
\eqref{eq:intro-main-functional}.  The first is a one-dimensional coefficient
hierarchy: finite centred \((2N-2)\)th absolute moment gives the expansion
through order \(t^{-2N}\), with coefficients obtained by Laurent
constant-term extraction from the Cayley modes.  The second is the sharp
leading threshold: finite centred variance is equivalent to a finite
\(t^{-4}\) matched-Poisson KL coefficient, and the value is \(\mu_2^2/8\).
The third is a borderline-tail replacement for the missing top moment,
formulated as a conditional transfer and then verified for exact power,
perturbative power, and monotone slowly varying subclasses.  The fourth is a
multidimensional tensor-jet theorem, together with the separate finite-moment
and covariance statements needed to read its leading term correctly.  The
fifth is an exact Cauchy-generator Bregman dissipation identity whose tail
integral recovers the KL value.  The sixth is a finite fourth-moment
coefficient-defect coupling for the symmetric two-point law, obtained from the
identified \(A_6\) coefficient.

The following theorem package states these results.  A guide immediately
after the statements records which hypotheses are assumptions, which are
verified consequences, and where the proof machinery enters.

\subsection*{Consolidated theorem package}

\begin{theorem}[Theorem A: one-dimensional coefficient hierarchy]%
\label{thm:intro-main-1d-coefficients}
Let \(N\ge2\), put \(L=2N-2\), and let \(\nu\) be a probability law on
\(\RR\) with finite mean \(\bar{\gamma}\) and finite centred \(L\)th absolute
moment.  With
\[
\begin{aligned}
  X_c&=\gamma-\bar{\gamma},\\
  \mu_n&=\int_{\RR} X_c^n\,\dd\nu(\gamma)\qquad(2\le n\le L),\\
  h_t&=P_t*\nu,\qquad
  g_t&=P_t(\cdot-\bar{\gamma}),
\end{aligned}
\]
one has
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N}).
\]
Each coefficient \(A_{2m}(\nu)\) is a universal polynomial in
\(\mu_2,\ldots,\mu_{2m-2}\), evaluated by the Cayley--Chebyshev Laurent
constant-term rule; explicitly it is the coefficient formula of
Theorem~\ref{thm:poisson-kl-even-orders}.  All retained odd coefficients
vanish, and the leading coefficient is
\[
A_4(\nu)=\frac{\mu_2^2}{8}.
\]
\end{theorem}

\begin{theorem}[Theorem B: sharp leading variance threshold]%
\label{thm:intro-main-variance-threshold}
Let \(\nu\) be a probability law on \(\RR\) with finite mean \(\bar{\gamma}\),
put \(X_c=\gamma-\bar{\gamma}\), and set
\[
H(t)=D_{\mathrm{KL}}\bigl(P_t*\nu\,\|\,P_t(\cdot-\bar{\gamma})\bigr)
\]
with the extended nonnegative convention for relative entropy.  Then
\[
\EE X_c^2<\infty
\quad\Longleftrightarrow\quad
\limsup_{t\to\infty}t^4H(t)<\infty .
\]
If the variance is finite, then \(H(t)<\infty\) for every \(t>0\) and
\[
\lim_{t\to\infty}t^4H(t)=\frac{(\EE X_c^2)^2}{8}.
\]
If the variance is infinite, then
\[
\lim_{t\to\infty}t^4H(t)=+\infty
\]
in the extended sense.  This is the only analytic if-and-only-if moment
threshold proved here.
\end{theorem}

\begin{theorem}[Theorem C\('\): conditional borderline-tail transfer]%
\label{thm:intro-main-power-tail}
Let \(N\ge3\), let \(p=2N-2\ge4\), and put
\[
  \Delta_t(y):=\frac{(P_t*\nu)(ty)}{P_t(ty)}-1 .
\]
Assume first that \(\nu\) is symmetric, has finite absolute moments of every
order \(q<p\), and satisfies the conditional quotient expansion
\[
  \Delta_t(y)
  =A_t(y)
  +u_p(y)t^{-p}M_p(t)+e_t(y),
  \qquad
  A_t(y):=\sum_{n=2}^{p-1}\mu_n u_n(y)t^{-n},
\]
with
\[
  \|e_t\|_\infty+\|e_t\|_{L^1(\omega)}
  =o(t^{-p}M_p(t)),\qquad
  M_p(t)\to\infty,\qquad M_p(t)=o(t^{p-2}).
\]
Then
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}
+I(2,p)\mu_2t^{-2N}M_p(t)
+o(t^{-2N}M_p(t)),
\]
where
\[
\mu_2=\int x^2\,\nu(\dd x),\qquad
I(2,p):=J(2,p)=(-1)^N(N-1)2^{-2N+2}.
\]
\end{theorem}

\begin{corollary}[Theorem C, exact power-tail verification]%
\label{cor:intro-exact-power-tail-verification}
Let \(N\ge3\), put \(p=2N-2\), fix \(R>0\) and \(c>0\), and let \(\nu\)
have symmetric probability density
\[
f(x)=g(x)+c|x|^{-p-1}\mathbf1_{\{|x|\ge R\}},
\]
where \(g\ge0\) is symmetric and compactly supported in \((-R,R)\),
\(f\ge0\), and \(\int_{\RR}f=1\).
Then the conditional quotient hypothesis of
Theorem~\ref{thm:intro-main-power-tail} holds with
\[
M_p(t)=\int_{|x|\le t}x^pf(x)\,\dd x=2c\log t+O(1).
\]
Consequently
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}
+2c\,I(2,p)\mu_2t^{-2N}\log t
+o(t^{-2N}\log t).
\]
\end{corollary}

\begin{corollary}[Theorem C, perturbative power-tail verification]%
\label{cor:intro-perturbative-power-tail-verification}
Let \(N\ge3\), put \(p=2N-2\), fix \(R>0\) and \(c>0\), and let \(\nu\)
have even probability density
\[
f(x)=g(x)+\bigl(c\lvert x\rvert^{-p-1}+r(x)\bigr)
      \mathbf1_{\{\lvert x\rvert\ge R\}},
\]
where \(g\ge0\) is symmetric and compactly supported in \((-R,R)\),
\(f\ge0\), \(\int_{\RR}f=1\), and \(r\) is even.  Require
\[
\int_{\lvert x\rvert\ge R}\lvert x\rvert^q\lvert r(x)\rvert\,\dd x
<\infty\qquad(0\le q<p),
\]
together with the three perturbative controls
\[
\int_{R\le\lvert x\rvert\le t}\lvert x\rvert^p r(x)\,\dd x=O(1),
\]
\[
t^{-(p+1)}
\int_{R\le\lvert x\rvert\le t}\lvert x\rvert^{p+1}\lvert r(x)\rvert\,\dd x
=O(t^{-p}),
\qquad
\int_{\lvert x\rvert>t}
\left(1+\frac{x^2}{t^2}\right)\lvert r(x)\rvert\,\dd x
=O(t^{-p}).
\]
Then the conditional quotient hypothesis of
Theorem~\ref{thm:intro-main-power-tail} holds with
\[
M_p(t)=\int_{|x|\le t}x^pf(x)\,\dd x=2c\log t+O(1),
\]
and therefore
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}
+2c\,I(2,p)\mu_2t^{-2N}\log t
+o(t^{-2N}\log t).
\]
No conclusion is asserted for perturbations that fail these quotient-scale
controls.
\end{corollary}

\begin{corollary}[Theorem C, monotone slowly varying verification]%
\label{cor:intro-slowly-varying-tail-verification}
Let \(N\ge3\), put \(p=2N-2\), fix \(R>0\) and \(c>0\), and let \(\nu\)
have symmetric probability density
\[
f(x)=g(x)+c|x|^{-p-1}L(|x|)\mathbf1_{\{|x|\ge R\}},
\]
where \(g\ge0\) is symmetric and compactly supported in \((-R,R)\),
\(f\ge0\), \(\int_{\RR}f=1\), and \(L\) is nonnegative, eventually positive,
eventually monotone, and slowly varying.  If
\[
M_p(t)=\int_{|x|\le t}x^pf(x)\,\dd x
\]
tends to \(+\infty\), then the conditional quotient hypothesis holds with
this \(M_p(t)\), and hence with the entropy layer
\(I(2,p)\mu_2t^{-2N}M_p(t)\):
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}
+I(2,p)\mu_2t^{-2N}M_p(t)
+o(t^{-2N}M_p(t)).
\]
\end{corollary}

The exact, perturbative, and monotone slowly varying corollaries are
verification results for the displayed quotient expansion in
Theorem~\ref{thm:intro-main-power-tail}; they are not an unrestricted
regular-variation principle.  In particular, the lower moments together with
\(M_p(t)\to\infty\) do not imply the logarithmic layer unless the quotient
error is \(o(t^{-p}M_p(t))\) in \(L^\infty(\RR)\cap L^1(\omega)\).

\begin{theorem}[Theorem D1: conditional jet transfer]%
\label{thm:intro-main-multidimensional}
\label{thm:intro-main-multidimensional-quotient}
Let \(d\ge1\) and let \(L\in\NN\) satisfy \(L\ge d+1\).  Assume that
\((\nu,d,L)\) satisfies the full matched Poisson quotient jet
\((\mathrm{MPJ}_{d,L})\), including the large-\(t\) entropy-existence clause
\((\mathrm{EX}_{d,L})\):
\[
\begin{aligned}
\delta_t^{(d)}(y)
&=\sum_{2\le\lvert\alpha\rvert\le L}
\mu_\alpha U_\alpha^{(d)}(y)t^{-\lvert\alpha\rvert}
+\rho_{L,t}^{(d)}(y),\\
\lVert\rho_{L,t}^{(d)}\rVert_\infty+
\lVert\rho_{L,t}^{(d)}\rVert_{L^1(\Omega_d)}
&=o(t^{-L}),\qquad
\lVert\delta_t^{(d)}\rVert_\infty=O(t^{-2}).
\end{aligned}
\]
Then, with \(h_t=P_t^{(d)}*\nu\) and
\(g_t=P_t^{(d)}(\cdot-\bar{\gamma})\),
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\sum_{m=4}^{L+2}\mathcal A_m^{(d)}(\nu)t^{-m}
+o(t^{-(L+2)}),
\]
where the tensor coefficients are those of
Theorem~\ref{thm:multidimensional-poisson-entropy-jets}; all retained odd
coefficients vanish.
\end{theorem}

\begin{theorem}[Theorem D2: moment supply]%
\label{thm:intro-main-multidimensional-finite-moment}
Let \(d\ge1\), let \(L\in\NN\) satisfy \(L\ge d+1\), and let \(\nu\) be a
probability measure on \(\RR^d\) with mean \(\bar{\gamma}\) and finite centred
\(L\)th absolute moment.  Then \((\nu,d,L)\) satisfies
\((\mathrm{MPJ}_{d,L})\), including \((\mathrm{EX}_{d,L})\), with the explicit
quotient remainder estimate of
Theorem~\ref{thm:multidimensional-density-quotient}.  This theorem supplies
the analytic and existence hypotheses in Theorem~D1; it is neither the KL
transfer nor the covariance calculation.
\end{theorem}

\begin{proposition}[Proposition D3: covariance algebra]%
\label{thm:intro-main-multidimensional-covariance}
Let \(d\ge1\), and suppose only that the centred covariance
\(\Sigma=\EE[X_cX_c^{\mathsf T}]\) is finite.  Define
\[
b_\Sigma(y)=
\frac{d+1}{2}\left(
-\frac{\operatorname{tr}\Sigma}{1+|y|^2}
+\frac{(d+3)y^{\mathsf T}\Sigma y}{(1+|y|^2)^2}
\right).
\]
Then the degree-two density mode has the algebraic contraction
\[
\mathcal Q_d(\Sigma)
=\frac12\int_{\RR^d}b_\Sigma(y)^2\,\Omega_d(\dd y)
=c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2,
\]
with
\[
c_d^{\rm iso}=
\frac{3(d+1)(7d+9)}
{4d(d+3)(d+5)(d+7)},
\qquad
c_d^{\rm tr}=
\frac{3(d+1)(d+3)}
{4(d+5)(d+7)} .
\]
This displayed identity is only covariance algebra.  It is not, by itself, a
KL asymptotic theorem and does not imply \((\mathrm{EX}_{d,L})\) or
\((\mathrm{MPJ}_{d,L})\).
\end{proposition}

\noindent\fbox{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}
\textbf{Finite covariance is not enough here.}
Proposition~D3 gives the number \(\mathcal Q_d(\Sigma)\), not the KL
asymptotic \(\mathcal Q_d(\Sigma)t^{-4}\).  Finite covariance would become
sufficient for the leading KL coefficient only after the following
second-order quotient condition is separately known:
\[
  \delta_t^{(d)}(y)=t^{-2}b_\Sigma(y)+r_t(y),\qquad
  \|r_t\|_\infty+\|r_t\|_{L^1(\Omega_d)}=o(t^{-2}),\qquad
  \|\delta_t^{(d)}\|_\infty=O(t^{-2}).
\]
\end{minipage}}

\medskip

\begin{corollary}[Corollary D4: actual leading KL coefficient]%
\label{thm:intro-main-multidimensional-leading-coefficient}
Let \(d\ge1\), and let \(\nu\) be a probability measure on \(\RR^d\) with
mean \(\bar{\gamma}\).  If
\[
\EE|X_c|^{d+1}<\infty,\qquad X_c=\gamma-\bar{\gamma},
\]
then, with
\[
h_t=P_t^{(d)}*\nu,\qquad g_t=P_t^{(d)}(\cdot-\bar{\gamma}),
\qquad
\Sigma=\EE[X_cX_c^{\mathsf T}],
\]
\[
D_{\mathrm{KL}}(h_t\|g_t)=\mathcal Q_d(\Sigma)t^{-4}+o(t^{-4}).
\]
Here \(\mathcal Q_d(\Sigma)\) is the algebraic quadratic form in
Proposition~D3.  Thus finite covariance alone gives the number
\(\mathcal Q_d(\Sigma)\), while the finite \((d+1)\)-moment hypothesis gives
the quotient jet and \((\mathrm{EX}_{d,d+1})\) through Theorem~D2 and only
then turns that number into the leading KL coefficient.
\end{corollary}
The hypothesis \(\EE|X_c|^{d+1}<\infty\) in Corollary~D4 is used only as a
sufficient condition to supply the quotient jet and entropy-existence
hypotheses.  No sharpness statement, converse, or dimension-\(d\) moment
threshold is asserted here.

\begin{theorem}[Theorem E: Cauchy Bregman tail identity]%
\label{thm:intro-main-bregman}
Let \(\nu\) be a probability measure on \(\RR\) with mean \(\bar{\gamma}\) and
finite centred second moment.  Put
\[
\begin{aligned}
h_t&=P_t*\nu,\\
g_t&=P_t(\cdot-\bar{\gamma}),\\
u_t&=h_t/g_t .
\end{aligned}
\]
The nonlocal domain is part of the statement.  For every \(0<a<b<\infty\),
write
\[
\begin{aligned}
\mu_2&:=\int_{\RR}(\gamma-\bar{\gamma})^2\,\dd\nu(\gamma),\\
m&:=\frac{1}{2(1+\mu_2/a^2)},\\
M&:=2+\frac{2\mu_2}{a^2},
\end{aligned}
\]
and
\[
  L:=3a^{-1}\bigl(1+\mu_2/a^2\bigr).
\]
Then, for all \(t\in[a,b]\) and \(x,y\in\RR\),
\begin{equation}\label{eq:intro-bregman-domain-quotient-bounds}
m\le u_t(x)\le M,\qquad
\lvert u_t(x)-u_t(y)\rvert\le L\lvert x-y\rvert .
\end{equation}
Define the Bregman kernel by
\[
\Lambda(\alpha,\beta)=\alpha\log\frac{\alpha}{\beta}-\alpha+\beta,
\]
and set
\[
\Psi(z):=\mathbf1_{\{\lvert z\rvert<1\}}
\,+\,\lvert z\rvert^{-2}\mathbf1_{\{\lvert z\rvert\ge1\}},
\qquad
C_{a,b,\nu}:=\frac{1}{2m}\max\{L^2,(M-m)^2\}.
\]
The compact-window bounds imply the pointwise Bregman majorant
\begin{equation}\label{eq:intro-bregman-domain-majorant}
0\le
\frac{g_t(x)\Lambda(u_t(x),u_t(y))}{(x-y)^2}
\le C_{a,b,\nu}\,g_t(x)\Psi(x-y)
\qquad (x\ne y,\ t\in[a,b]),
\end{equation}
and the same estimate holds with \(x\) and \(y\) interchanged.  Hence the
symmetrized kernel is dominated on \([a,b]\times\RR^2\) by
\[
C_{a,b,\nu}\{g_t(x)+g_t(y)\}\Psi(x-y),
\]
whose integral over \([a,b]\times\RR^2\) equals
\(8C_{a,b,\nu}(b-a)\).  The same one-sided majorant gives the compact-window
dissipation bound
\[
\int_a^b\mathcal I_\nu(s)\,\dd s
\le \frac{4C_{a,b,\nu}}{\pi}(b-a)
\]
once \(\mathcal I_\nu\) is defined below.  This domain clause makes the
following nonlocal double integral finite on compact time windows and
independent of the annular exhaustion used to construct it.

Define \(\mathcal I_\nu(t)\) by annular cutoffs, equivalently by the
nonnegative Lebesgue integral
\[
\mathcal I_\nu(t)
=\frac1\pi\iint_{\RR^2}
\frac{g_t(x)\Lambda(u_t(x),u_t(y))}{(x-y)^2}\dd x\dd y ,
\]
with the diagonal ignored.  Then \(\mathcal I_\nu\) is measurable and locally
integrable on \((0,\infty)\), and for every \(t>0\),
\[
D_{\mathrm{KL}}(h_t\|g_t)=\int_t^\infty \mathcal I_\nu(s)\,\dd s .
\]
\end{theorem}

\begin{theorem}[Theorem F: coefficient-defect coupling]%
\label{thm:intro-main-coefficient-defect}
\label{cor:intro-coefficient-defect-coupling}
Let \(\nu\) be a probability law on \(\RR\) with mean \(\bar{\gamma}\),
positive variance \(\sigma^2>0\), and finite centred fourth absolute moment.
Set
\[
  X_c=\gamma-\bar{\gamma},\qquad
  \mu_k=\EE X_c^k\quad (k=3,4),
\]
and define the sixth-order coefficient identified by the coefficient
hierarchy through order \(t^{-6}\) by
\[
A_6(\nu)=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64}.
\]
With the normalized variable
\[
  X=\frac{X_c}{\sigma},\qquad
  \beta_3=\EE X^3,\qquad
  Q_2=X^2-1-\beta_3X,
\]
the sixth-order two-point coefficient defect satisfies the identity
\[
\Delta_6(\nu)
:=-\frac{7}{64}\sigma^6-A_6(\nu)
=\sigma^6\left(\frac{\beta_3^2}{32}
+\frac{\EE Q_2^2}{8}\right).
\]
If
\[
  \nu_c=(\gamma-\bar{\gamma})_\#\nu,\qquad
  \rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma),
\]
then
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\]
Moreover \(\Delta_6(\nu)\ge0\), and equality holds if and only if
\(\nu_c=\rho_\sigma\).  This finite-moment coupling statement is proved as
Corollary~\ref{cor:coefficient-defect-coupling}; it is a consequence of the
identified \(A_6\) coefficient and does not assert optimizer stability for a
sharp functional inequality.
\end{theorem}

\subsection*{Guide to hypotheses and proof layers}
\label{subsec:intro-guide-hypotheses}

The hypotheses in the theorem package have three different roles.
\emph{Moment supply} hypotheses give the quotient expansion of
\((P_t*\nu)/P_t(\cdot-\bar{\gamma})\) and enough integrability to pass from
that expansion to KL.  \emph{Conditional quotient} hypotheses assume that
analytic expansion directly and then compute its entropy consequence.
\emph{Algebraic coefficient} statements compute the resulting constants once
the quotient layers are available.  The theorem statements above keep these
roles separate so that a reader need not infer which part of a proof supplies
which assumption.

For Theorems~A and~B the route is finite-dimensional.  The Cayley mode
identity for \(R_\varepsilon\), the universal entropy polynomial, and the
Laurent constant-term rule produce the coefficients.  The finite
\((2N-2)\)-moment assumption in Theorem~A supplies the quotient remainder
needed to retain terms through \(t^{-2N}\).  Theorem~B is the one place where
the paper proves an analytic if-and-only-if moment threshold: finite variance
is exactly the condition for the leading finite KL coefficient, and that
coefficient is \(A_4(\nu)=\mu_2^2/8\).

Theorem~C is not a general regularly varying theorem.  Its first half is a
conditional statement: for \(p=2N-2\), the quotient must satisfy
\[
  \Delta_t=A_t+u_p t^{-p}M_p(t)+e_t,\qquad
  \|e_t\|_\infty+\|e_t\|_{L^1(\omega)}
  =o(t^{-p}M_p(t)),
\]
with \(M_p(t)\to\infty\), \(M_p(t)=o(t^{p-2})\), symmetry, and finite
moments of all orders \(q<p\).  The exact power, perturbative power, and
monotone slowly varying cases are separate verification classes for this
quotient hypothesis.  In the perturbative class the conditions on \(r\),
including \(0\le q<p\) in the absolute-moment requirement and the three
displayed tail controls, are part of the verification rather than
consequences of regular variation alone.

Theorems~D1--D4 separate the multidimensional logic.  Theorem~D1 assumes the
matched Poisson quotient jet \((\mathrm{MPJ}_{d,L})\), including the
large-time entropy-existence clause \((\mathrm{EX}_{d,L})\), and transfers it
to tensor KL coefficients.  Theorem~D2 proves that finite centred
\(L\)-absolute moment, \(L\ge d+1\), is one sufficient route to that analytic
input.  Proposition~D3 is only covariance algebra: it computes
\(\mathcal Q_d(\Sigma)\) from finite covariance and does not assert a KL
asymptotic.  Corollary~D4 turns the same algebraic quantity into the actual
leading KL coefficient only after the finite \((d+1)\)-moment hypothesis has
supplied the quotient jet and \((\mathrm{EX}_{d,d+1})\).  No sharp
dimension-\(d\) moment threshold is claimed there.

Theorem~E has a different domain issue.  On each compact time window
\([a,b]\subset(0,\infty)\), finite centred variance gives two-sided bounds
and a Lipschitz bound for the moving quotient
\(u_t=(P_t*\nu)/P_t(\cdot-\bar{\gamma})\).  Those bounds produce the
Bregman majorant in the statement: the symmetrized kernel has integral at
most \(8C_{a,b,\nu}(b-a)\), while the one-sided dissipation satisfies
\(\int_a^b\mathcal I_\nu(s)\,\dd s\le4C_{a,b,\nu}(b-a)/\pi\).  With this
nonlocal domain under control, the tail identity follows from the cutoff
Green formula and \(H'(t)=-\mathcal I_\nu(t)\).

Theorem~F uses only the coefficient algebra through \(A_6\) and a finite
fourth absolute moment.  The order-eight Laurent certificate is recorded once
in Theorem~\ref{thm:poisson-kl-eighth}; the sixth-order part gives
\[
A_6(\nu)=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64}.
\]
After normalization, this identity yields the nonnegative defect
\(\Delta_6\) and then the elementary \(W_2\) coupling estimate in
Corollary~\ref{cor:coefficient-defect-coupling}.  It is a finite-moment
coefficient consequence, not a stability theorem for a sharp functional
inequality.

\subsection*{Proof map}

The detailed statements and proofs occur where their hypotheses are supplied
or used.  Section~\ref{sec:entropy-asymptotics} proves the Cayley modes, the
universal coefficient formula, the Laurent evaluation rule, the
one-dimensional finite-moment expansion, the sharp variance threshold, the
borderline-tail transfer and verification classes, the multidimensional
quotient jets, and the coefficient-defect consequence.
Section~\ref{sec:doob-phi-entropy} proves the compact-window Bregman domain
bounds and the Cauchy/KL dissipation-tail identity.
Appendix~\ref{app:entropy-integrals} contains the normalization and Laurent
arithmetic used by the coefficient calculations.

\subsection*{Relation to recent JFA work and scope}

The paper uses the extended nonnegative Kullback--Leibler functional of
\eqref{eq:extended-kl-definition}.  Appendix~\ref{app:related-work-scope}
first gives a recent-\emph{J. Funct. Anal.} comparison table, organized by
the theorem package above, and explicitly marks the rows for which no close
JFA analogue appears to exist.  Older entropy, heat-kernel, Hardy--Stein,
stability, EPI-stability, and transport references are then used only as
background or proof language, not as substitutes for the JFA comparison.

\subsection*{Organization}

Section~\ref{sec:preliminaries} fixes notation.  Sections~\ref{sec:cayley-gate}
and~\ref{sec:haar-pullback} record the Cayley and Haar-pullback
normalizations.  Section~\ref{sec:entropy-asymptotics} proves the
multidimensional entropy jets, the one-dimensional coefficient hierarchy, the
sharp leading variance threshold, the explicit coefficient layer through
order eight, and the sixth-order coefficient-defect consequence.
Section~\ref{sec:doob-phi-entropy}
derives the Cauchy/KL Bregman tail identity by cutoff Green calculus,
verifies the Poisson quotient hypotheses, and then proves the dissipation-tail
coefficient expansion.
Appendix~\ref{app:related-work-scope} gives the theorem-level related-work
comparison and consolidated scope discussion;
Appendix~\ref{app:entropy-integrals} records the weighted trigonometric
normalization behind the Laurent arithmetic;
Appendix~\ref{app:haar-pullback-uniqueness}
records the endpoint-normalized Cayley uniqueness statement.
