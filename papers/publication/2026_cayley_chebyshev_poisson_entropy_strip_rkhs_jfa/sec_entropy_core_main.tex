% sec_entropy_core_main.tex -- compact main entropy core
\section{Poisson Smoothing and Entropy Asymptotics}%
\label{sec:entropy-asymptotics}

This section contains only the entropy coefficient mechanism, the
multidimensional covariance calculation, the leading one-dimensional threshold,
and the coefficient-defect consequence.  Quantitative constant envelopes,
finite-grid substitutions, moment-shell dictionaries, and conditional
finite-time post-processing are not used in the proofs below.  The only
post-processing retained in the article is the conditional appendix statement,
which assumes its numerical and remainder inputs explicitly and is not needed
for the theorem hierarchy in this section.

\subsection{Cayley modes and coefficient algebra}

Throughout the one-dimensional part,
\[
  \omega(\dd y)=\frac{\dd y}{\pi(1+y^2)},\qquad
  X_c=\gamma-\bar\gamma,\qquad
  \mu_n=\int_{\RR}X_c^n\,\dd\nu(\gamma)
\]
whenever the moment exists.  For \(y\in\RR\) and \(\varepsilon\in\RR\), set
\[
R_\varepsilon(y):=\frac{1+y^2}{1+(y-\varepsilon)^2}.
\]
The Taylor coefficients at \(\varepsilon=0\) are denoted by \(u_n\):
\[
R_\varepsilon(y)=\sum_{n=0}^{L}u_n(y)\varepsilon^n
 +\varepsilon^{L+1}v_L(y,\varepsilon)
\]
for \(|\varepsilon|\le1\).  The finite-order estimate used below is the
following two-region form: for every \(L\ge2\) there is a constant \(C_L\)
such that, uniformly in \(y\in\RR\),
\begin{equation}\label{eq:one-dimensional-two-region-taylor-remainder}
\left|R_\varepsilon(y)-\sum_{n=0}^{L}u_n(y)\varepsilon^n\right|
\le C_L
\begin{cases}
|\varepsilon|^{L+1},& |\varepsilon|\le1,\\
|\varepsilon|^L,& |\varepsilon|>1.
\end{cases}
\end{equation}
Indeed, the first line follows from the finite Taylor remainder above with
\(\sup_{y,|\varepsilon|\le1}|v_L(y,\varepsilon)|<\infty\); the second follows
from \(R_\varepsilon(y)\le3(1+\varepsilon^2)\le6|\varepsilon|^L\) and the
boundedness of \(u_0,\ldots,u_L\).

\begin{table}[t]
\caption{Coefficient notation and Cayley phase convention used in the entropy core.}
\label{tab:coefficient-notation}
\centering
\begin{tabular}{p{0.25\textwidth}p{0.64\textwidth}}
\hline
Symbol & Meaning \\
\hline
\(\theta\) & Half-angle Cayley coordinate, \(y=\tan\theta\),
\(-\pi/2<\theta<\pi/2\).\\
\(z\) & Laurent phase \(z=e^{2\ii\theta}\).  Thus
\(\cos(2k\theta)\) and \(\sin(2k\theta)\) are \(z^{\pm k}\) modes.\\
\(\omega\) & Cauchy probability measure
\(\dd y/(\pi(1+y^2))=\dd\theta/\pi\), pushed to Haar measure by
\(z=e^{2\ii\theta}\).\\
\(Q_n\) & Laurent polynomial satisfying
\(Q_n(e^{2\ii\theta})=u_n(\tan\theta)\).\\
\(A_k^{\rm form}\) & Universal coefficient polynomial before a law is inserted.\\
\(A_k(\nu)\) & Evaluation of \(A_k^{\rm form}\) at the centred moments of
\(\nu\), and, after Theorem~\ref{thm:poisson-kl-even-orders}, the
corresponding KL coefficient.\\
\(\mathcal A_m^{(d)}(\nu)\) & Multidimensional retained entropy coefficient.\\
\hline
\end{tabular}
\end{table}

\begin{theorem}[Cayley--Chebyshev mode formula]%
\label{thm:cayley-chebyshev-mode}
Let \(U_n\) be the Chebyshev polynomial of the second kind.  If
\(y=\tan\theta\), then
\begin{equation}\label{eq:cayley-chebyshev-mode}
u_n(\tan\theta)=\cos^n\theta\,U_n(\sin\theta).
\end{equation}
Equivalently, for \(n\ge1\),
\[
u_n(\tan\theta)=
\cos^{n-1}\theta\,
\sin\!\left((n+1)\left(\frac{\pi}{2}-\theta\right)\right),
\]
and therefore
\begin{equation}\label{eq:cayley-mode-parity}
u_n(-y)=(-1)^nu_n(y).
\end{equation}
Moreover, for \(m\ge1\) and \(m\ge0\), respectively,
\[
u_{2m}(\tan\theta)=(-1)^m2^{-(2m-1)}
\sum_{j=0}^{2m-1}\binom{2m-1}{j}\cos(2(j+1)\theta),
\]
\[
u_{2m+1}(\tan\theta)=(-1)^m2^{-2m}
\sum_{j=0}^{2m}\binom{2m}{j}\sin(2(j+1)\theta).
\]
\end{theorem}

\begin{proof}
With \(y=\tan\theta\),
\[
R_\varepsilon(\tan\theta)
=\{1-2(\varepsilon\cos\theta)\sin\theta
+(\varepsilon\cos\theta)^2\}^{-1}.
\]
The generating function
\(\sum_{n\ge0}U_n(x)r^n=(1-2xr+r^2)^{-1}\)
\cite[Chapters~1 and~5]{MasonHandscomb2002} gives
\eqref{eq:cayley-chebyshev-mode}.  The trigonometric form follows from the
standard identity for \(U_n(\sin\theta)\), and parity is immediate.  The
finite Fourier formula is obtained from
\(1+e^{2\ii\theta}=2e^{\ii\theta}\cos\theta\) after taking real and imaginary
parts.
\end{proof}

For \(r\ge2\) and \(n_j\ge2\), define
\begin{equation}\label{eq:mode-integral-definition}
I(n_1,\ldots,n_r):=
\int_{\RR}u_{n_1}(y)\cdots u_{n_r}(y)\,\omega(\dd y).
\end{equation}
In the formal polynomial algebra
\(\QQ[\mathfrak m_2,\ldots,\mathfrak m_k]\), put
\begin{equation}\label{eq:universal-entropy-coefficient}
A_k^{\rm form}:=
\sum_{r=2}^{\lfloor k/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{n_1+\cdots+n_r=k\\ n_j\ge2}}
\mathfrak m_{n_1}\cdots\mathfrak m_{n_r}\,I(n_1,\ldots,n_r).
\end{equation}
When the required centred moments exist,
\begin{equation}\label{eq:universal-entropy-coefficient-evaluation}
A_k(\nu):=\operatorname{ev}_{\nu,k}(A_k^{\rm form}),
\qquad
\operatorname{ev}_{\nu,k}(\mathfrak m_n)=\mu_n .
\end{equation}

\begin{theorem}[Universal entropy coefficients]%
\label{thm:universal-entropy-coefficients}
The coefficient of \(t^{-k}\) in the formal expression
\[
\int_{\RR}\left\{
\left(1+\sum_{n=2}^{k}\mathfrak m_nu_n(y)t^{-n}\right)
\log\left(1+\sum_{n=2}^{k}\mathfrak m_nu_n(y)t^{-n}\right)
-\sum_{n=2}^{k}\mathfrak m_nu_n(y)t^{-n}
\right\}\omega(\dd y)
\]
is \(A_k^{\rm form}\).  If the moments needed in
\eqref{eq:universal-entropy-coefficient} exist for \(\nu\), then
\[
A_k(\nu)=
\sum_{r=2}^{\lfloor k/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{n_1+\cdots+n_r=k\\ n_j\ge2}}
\mu_{n_1}\cdots\mu_{n_r}\,I(n_1,\ldots,n_r).
\]
All odd formal coefficients vanish.
\end{theorem}

\begin{proof}
Use
\[
(1+s)\log(1+s)-s=\sum_{r=2}^{\infty}\frac{(-1)^r}{r(r-1)}s^r.
\]
The coefficient of total degree \(k\) is finite and is exactly
\eqref{eq:universal-entropy-coefficient}.  If \(k\) is odd, then each mode
product in the coefficient has odd total parity by
\eqref{eq:cayley-mode-parity}, hence integrates to zero against the even
measure \(\omega\).
\end{proof}

\begin{theorem}[Finite Fourier algorithm for mode integrals]%
\label{thm:finite-fourier-mode-integral-algorithm}
Define \(Q_n(e^{2\ii\theta})=u_n(\tan\theta)\).  Then
\begin{equation}\label{eq:mode-laurent-polynomial-definition}
Q_{2m}(z)=(-1)^m2^{-2m}
\sum_{j=0}^{2m-1}\binom{2m-1}{j}
\bigl(z^{j+1}+z^{-(j+1)}\bigr),
\end{equation}
and
\[
Q_{2m+1}(z)=(-1)^m\frac{2^{-2m}}{2\ii}
\sum_{j=0}^{2m}\binom{2m}{j}
\bigl(z^{j+1}-z^{-(j+1)}\bigr).
\]
Consequently
\begin{equation}\label{eq:mode-integral-constant-term}
I(n_1,\ldots,n_r)=[z^0]\prod_{j=1}^r Q_{n_j}(z).
\end{equation}
\end{theorem}

\begin{proof}
The formulas are the finite Fourier identities of
Theorem~\ref{thm:cayley-chebyshev-mode} after using
\(\cos(2\ell\theta)=(z^\ell+z^{-\ell})/2\) and
\(\sin(2\ell\theta)=(z^\ell-z^{-\ell})/(2\ii)\).  Since
\(\omega(\dd y)=\dd\theta/\pi\), the map \(z=e^{2\ii\theta}\) sends the
integral to Haar integration on \(\TT\), hence to constant-term extraction.
\end{proof}

\subsection{One-dimensional coefficient theorem}

\begin{lemma}[Exact-moment density truncation]%
\label{lem:exact-moment-density-truncation}
\label{thm:finite-moment-density-remainder}
Let \(L\ge2\), and let \(\nu\) have mean \(\bar\gamma\) and finite centred
\(L\)th absolute moment.  For
\[
\delta_t(y):=
\frac{(P_t*\nu)(\bar\gamma+ty)}{P_t(ty)}-1
\]
one has
\begin{equation}\label{eq:exact-moment-density-truncation}
\delta_t(y)=\sum_{n=2}^{L}\mu_nu_n(y)t^{-n}+\rho_{L,t}(y),
\qquad
\|\rho_{L,t}\|_{\infty}+\|\rho_{L,t}\|_{L^1(\omega)}=o(t^{-L}).
\end{equation}
Also \(\|\delta_t\|_\infty=O(t^{-2})\).
\end{lemma}

\begin{proof}
For \(x=\bar\gamma+ty\),
\[
\frac{P_t(x-\gamma)}{P_t(x-\bar\gamma)}
=R_{X_c/t}(y).
\]
Subtract the Taylor polynomial in \(X_c/t\) through order \(L\).  On
\(|X_c|\le t\), the small-translation part of
\eqref{eq:one-dimensional-two-region-taylor-remainder} gives
\[
C_Lt^{-L}\EE\!\left[|X_c|^L(|X_c|/t)\mathbf1_{\{|X_c|\le t\}}\right]
=o(t^{-L}).
\]
On \(|X_c|>t\), the large-translation part gives
\[
\left|R_{X_c/t}(y)-\sum_{n=0}^{L}u_n(y)(X_c/t)^n\right|
\le C_L|X_c/t|^L,
\]
and hence \(C_Lt^{-L}\EE[|X_c|^L\mathbf1_{\{|X_c|>t\}}]=o(t^{-L})\).  The same
uniform estimates prove both the \(L^\infty\) and \(L^1(\omega)\) bounds.  The
\(n=0\) term is removed by subtracting \(1\), and the \(n=1\) term vanishes
because \(\EE X_c=0\).  The \(O(t^{-2})\) bound follows from the same split
with \(L=2\).
\end{proof}

\begin{theorem}[Finite-moment Poisson entropy transfer and coefficient formula]%
\label{thm:poisson-kl-even-orders}
\label{prop:coefficient-identification-poisson-entropy-transfer}
\label{prop:density-to-entropy-descent}
Let \(N\ge2\), put \(L=2N-2\), and let \(\nu\) have mean \(\bar\gamma\) and
finite centred \(L\)th absolute moment.  With
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),
\]
\begin{equation}\label{eq:poisson-kl-even-orders}
D_{\mathrm{KL}}(h_t\|g_t)
=\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N}).
\end{equation}
For \(4\le k\le2N\),
\begin{equation}\label{eq:coefficient-identification-transfer-expansion}
A_k(\nu)=
\sum_{r=2}^{\lfloor k/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{n_1+\cdots+n_r=k\\2\le n_j\le2N-2}}
\mu_{n_1}\cdots\mu_{n_r}
\int_{\RR}u_{n_1}\cdots u_{n_r}\,\dd\omega .
\end{equation}
All retained odd \(A_k\) vanish.
\end{theorem}

\begin{proof}
Write \(\delta_t=d_t+\rho_t\), where
\[
d_t(y)=\sum_{n=2}^{L}\mu_nu_n(y)t^{-n}.
\]
Lemma~\ref{lem:exact-moment-density-truncation} gives
\(\|d_t\|_\infty=O(t^{-2})\) and
\(\|\rho_t\|_\infty+\|\rho_t\|_{L^1(\omega)}=o(t^{-L})\).  Since
\[
g_t(\bar\gamma+ty)t\,\dd y=\omega(\dd y),
\]
and \(\int\delta_t\,\dd\omega=0\),
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\int_{\RR}\Phi(\delta_t(y))\,\omega(\dd y),
\qquad
\Phi(s)=(1+s)\log(1+s)-s .
\]
For large \(t\),
\[
\Phi(\delta_t)=\sum_{r=2}^{N}\frac{(-1)^r}{r(r-1)}\delta_t^r
+O(|\delta_t|^{N+1}).
\]
The Taylor tail is \(O(t^{-2N-2})\).  For \(2\le r\le N\),
\[
\|\delta_t^r-d_t^r\|_{L^1(\omega)}
\le C_r\sum_{\ell=1}^{r}
\|d_t\|_\infty^{r-\ell}
\|\rho_t\|_\infty^{\ell-1}
\|\rho_t\|_{L^1(\omega)}
=o(t^{-(L+2)}).
\]
The worst term is \(r=2,\ell=1\), namely \(O(t^{-2})o(t^{-L})\).  Since
\(L+2=2N\), only pure powers of \(d_t\) remain at the retained order.  Their
coefficients are exactly \eqref{eq:coefficient-identification-transfer-expansion},
and odd total orders vanish by Theorem~\ref{thm:universal-entropy-coefficients}.
\end{proof}

\begin{lemma}[Positive-margin quotient remainder and entropy transfer]
\label{lem:positive-margin-quotient-remainder}
\label{rem:positive-margin-retained-order}
Let \(L\ge2\), \(0<\eta\le1\), and let \(\nu\) have mean
\(\bar\gamma\) and finite centred \((L+\eta)\)th absolute moment.  With
\[
\delta_t(y):=
\frac{(P_t*\nu)(\bar\gamma+ty)}{P_t(ty)}-1
\]
there is a remainder \(\rho_{L,t}^{(\eta)}\) such that
\begin{equation}\label{eq:positive-margin-density-remainder}
\delta_t(y)=
\sum_{n=2}^{L}\mu_nu_n(y)t^{-n}+\rho_{L,t}^{(\eta)}(y),
\qquad
\|\rho_{L,t}^{(\eta)}\|_{\infty}
+\|\rho_{L,t}^{(\eta)}\|_{L^1(\omega)}
=O_\nu(t^{-(L+\eta)}).
\end{equation}
Moreover \(\|\delta_t\|_\infty=O_\nu(t^{-2})\).

Consequently, if \(N\ge2\), \(L=2N-2\), and
\(\EE|X_c|^{2N-2+\eta}<\infty\), then
\begin{equation}\label{eq:positive-margin-retained-order-theorem}
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}
+O_\nu(t^{-(2N+\eta)}).
\end{equation}
\end{lemma}

\begin{proof}
The quotient identity is
\[
\frac{P_t(\bar\gamma+ty-\gamma)}{P_t(ty)}
=R_{X_c/t}(y).
\]
Subtract the Taylor polynomial
\(\sum_{n=0}^{L}u_n(y)(X_c/t)^n\) and split the expectation into
\(|X_c|\le t\) and \(|X_c|>t\).

On \(|X_c|\le t\), the first line of
\eqref{eq:one-dimensional-two-region-taylor-remainder} gives, uniformly in
\(y\),
\[
\left|
\EE\!\left[
\left(R_{X_c/t}(y)-\sum_{n=0}^{L}u_n(y)(X_c/t)^n\right)
\mathbf1_{\{|X_c|\le t\}}
\right]\right|
\le
C_Lt^{-(L+1)}
\EE\!\left[|X_c|^{L+1}\mathbf1_{\{|X_c|\le t\}}\right].
\]
Because \(0<\eta\le1\), the last term is bounded by
\[
C_Lt^{-(L+\eta)}
\EE\!\left[
|X_c|^{L+\eta}
\left(\frac{|X_c|}{t}\right)^{1-\eta}
\mathbf1_{\{|X_c|\le t\}}
\right]
\le
C_L\EE|X_c|^{L+\eta}\,t^{-(L+\eta)} .
\]

On \(|X_c|>t\), the second line of
\eqref{eq:one-dimensional-two-region-taylor-remainder} gives
\[
\left|
\EE\!\left[
\left(R_{X_c/t}(y)-\sum_{n=0}^{L}u_n(y)(X_c/t)^n\right)
\mathbf1_{\{|X_c|>t\}}
\right]\right|
\le
C_Lt^{-L}\EE\!\left[|X_c|^L\mathbf1_{\{|X_c|>t\}}\right].
\]
Since \(|X_c|>t\) implies
\(|X_c|^L\le t^{-\eta}|X_c|^{L+\eta}\), this is also
\[
O_\nu(t^{-(L+\eta)}).
\]
The same uniform bounds give the \(L^1(\omega)\) estimate because
\(\omega\) is a probability measure.  The \(n=0\) term cancels in
\(\delta_t\), and the \(n=1\) term vanishes after integration against
\(\nu\) because \(\EE X_c=0\).  This proves
\eqref{eq:positive-margin-density-remainder}.  The bound
\(\|\delta_t\|_\infty=O_\nu(t^{-2})\) follows from
Lemma~\ref{lem:exact-moment-density-truncation}, since
\(\EE|X_c|^{L+\eta}<\infty\) and \(L\ge2\).

Now put \(L=2N-2\) and
\[
d_t(y)=\sum_{n=2}^{L}\mu_nu_n(y)t^{-n}.
\]
As in the proof of Theorem~\ref{thm:poisson-kl-even-orders},
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\int_{\RR}\Phi(\delta_t(y))\,\omega(\dd y),
\qquad
\Phi(s)=(1+s)\log(1+s)-s .
\]
For all large \(t\), \(\|\delta_t\|_\infty\le1/2\), so
\[
\Phi(\delta_t)=
\sum_{r=2}^{N}\frac{(-1)^r}{r(r-1)}\delta_t^r
+O_\nu(|\delta_t|^{N+1}).
\]
The Taylor tail contributes \(O_\nu(t^{-2N-2})\), which is
\(O_\nu(t^{-(2N+\eta)})\).  For \(2\le r\le N\), the product expansion gives
\[
\|\delta_t^r-d_t^r\|_{L^1(\omega)}
\le
C_{r,\nu}\sum_{\ell=1}^{r}
t^{-2(r-\ell)}
\|\rho_{L,t}^{(\eta)}\|_\infty^{\ell-1}
\|\rho_{L,t}^{(\eta)}\|_{L^1(\omega)}
\]
\[
\le
C_{r,\nu}\sum_{\ell=1}^{r}
t^{-2(r-\ell)}t^{-(L+\eta)\ell}
=O_\nu(t^{-(L+\eta+2)}).
\]
Since \(L+\eta+2=2N+\eta\), every term containing the quotient remainder is
of the asserted order.  The pure powers of \(d_t\) have the same retained
coefficients as in Theorem~\ref{thm:poisson-kl-even-orders}, and this proves
\eqref{eq:positive-margin-retained-order-theorem}.
\end{proof}

\medskip
\phantomsection\label{par:coefficient-evaluability-scope}
\noindent\fbox{%
\begin{minipage}{0.94\linewidth}
\textbf{Scope of the finite-moment hierarchy.}
The finite \((2N-2)\)-moment hypothesis in
Theorem~\ref{thm:poisson-kl-even-orders} is the first level at which the
displayed universal polynomial \(A_{2N}\) can be evaluated from a fixed finite
moment jet.  It is not asserted here to be a sharp analytic tail threshold
for every possible non-polynomial asymptotic regime.  The only analytic
if-and-only-if tail threshold proved in this paper is the leading variance
threshold, Theorem~\ref{thm:sharp-leading-kl-variance-threshold}.
\end{minipage}}
\medskip

\begin{proposition}[First evaluability level for the top coefficient]%
\label{prop:poisson-kl-moment-threshold-sharpness}
\label{prop:analytic-first-evaluability-obstruction}
For \(N\ge3\), the universal top coefficient \(A_{2N}^{\rm form}\) contains
the nonzero monomial
\[
\kappa_N\,\mathfrak m_2\mathfrak m_{2N-2},
\qquad
\kappa_N=(-1)^N(N-1)2^{-2N+2}\ne0 .
\]
Consequently the displayed universal polynomial \(A_{2N}\) cannot be
evaluated from centred moments of order at most \(2N-3\) on the full class of
laws.
\end{proposition}

\begin{proof}
The monomial comes from the quadratic term
\(\frac12\int d_t^2\,\dd\omega\), using one \(u_2\) factor and one
\(u_{2N-2}\) factor.  Constant-term extraction from
Theorem~\ref{thm:finite-fourier-mode-integral-algorithm} gives the displayed
nonzero coefficient.  Symmetric atomic laws with finite moments below
\(2N-2\) and infinite \((2N-2)\)nd moment show that no lower-moment polynomial
can define the same universal coefficient on the full class.
\end{proof}

The logarithmic replacement layers below are the fixed-law Cauchy-smoothing
counterpart of a common borderline phenomenon in entropy asymptotics: at the
critical moment exponent the missing polynomial coefficient is replaced by a
tail-dependent logarithmic scale, rather than by a lower-order moment
polynomial.

\begin{proposition}[Logarithmic heavy-tail replacement at the missing top moment]%
\label{prop:log-tail-counterexample-top-moment}
Let \(N\ge3\), put \(p=2N-2\), and choose \(R>0\) and \(c>0\) such that
\[
  \frac{2c}{pR^p}<1 .
\]
Let \(g\) be any symmetric nonnegative compactly supported integrable
function on \((-R,R)\) with total mass \(1-2c/(pR^p)\), and define the symmetric
probability density
\begin{equation}\label{eq:normalized-borderline-tail-density}
  f(x)=g(x)+c|x|^{-(p+1)}\mathbf1_{\{|x|\ge R\}} .
\end{equation}
Let \(\nu\) be the law with density \(f\).  Then \(\nu\) is centred,
\[
\EE|X|^q<\infty\quad(q<p),\qquad \EE|X|^p=\infty ,
\]
and
\[
\EE\!\left[X^p\mathbf1_{\{|X|\le t\}}\right]=2c\log t+O(1).
\]
With \(H(t)=D_{\mathrm{KL}}(P_t*\nu\|P_t)\),
\begin{equation}\label{eq:core-log-tail-counterexample-asymptotic}
H(t)=\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}
    +2c\,\kappa_N\,\mu_2\,t^{-2N}\log t
    +o(t^{-2N}\log t),
\end{equation}
where
\[
\kappa_N=I(2,p)=(-1)^N(N-1)2^{-2N+2}\ne0
\]
is the quadratic cross coefficient, equivalently the coefficient of the
monomial
\(\mathfrak m_2\mathfrak m_{2N-2}\) in
Proposition~\ref{prop:poisson-kl-moment-threshold-sharpness}.
Thus the failure of the top moment is an analytic tail-regime phenomenon:
the missing \(A_{2N}t^{-2N}\) polynomial layer is replaced, for this explicit
regularly varying law, by the displayed tail-dependent logarithmic layer.
The case \(N=2\) is excluded from this statement.
\end{proposition}

\begin{proof}
The mass condition makes \eqref{eq:normalized-borderline-tail-density} a
probability density:
\[
  \int_{\RR}f(x)\,\dd x
  =\left(1-\frac{2c}{pR^p}\right)
    +2c\int_R^\infty x^{-p-1}\,\dd x
  =1 .
\]
The compact core is symmetric and nonnegative by construction, the tail is
symmetric, and therefore \(\nu\) is centred.  The moment assertions are
immediate from compact support of \(g\) and
\[
2c\int_R^\infty x^q x^{-p-1}\,\dd x<\infty
\quad\Longleftrightarrow\quad q<p.
\]
The truncated top moment is
\[
\EE\!\left[X^p\mathbf1_{\{|X|\le t\}}\right]
=O(1)+2c\int_R^t x^{-1}\,\dd x
=2c\log t+O(1).
\]

For \(x=ty\), write
\[
\Delta_t(y)=\frac{(P_t*\nu)(ty)}{P_t(ty)}-1
=\int_{\RR}\{R_{x/t}(y)-1\}f(x)\dd x .
\]
The Taylor expansion of \(R_\varepsilon(y)\) gives, uniformly in \(y\),
\[
R_{x/t}(y)-1=\sum_{n=1}^{p}u_n(y)(x/t)^n
             +O(|x/t|^{p+1})\qquad(|x|\le t),
\]
and the elementary quotient bound
\(|R_\varepsilon(y)-1|\le C(1+\varepsilon^2)\) controls \(|x|>t\).
Splitting the integral into the compact core \(|x|<R\), the truncated tail
\(R\le |x|\le t\), and the far tail \(|x|>t\) gives
\[
\Delta_t(y)=\sum_{n=2}^{p-1}\mu_nu_n(y)t^{-n}
            +2c\,u_p(y)t^{-p}\log t+\varepsilon_t(y),
\]
with
\(\|\varepsilon_t\|_\infty+\|\varepsilon_t\|_{L^1(\omega)}\le Ct^{-p}\).
Indeed, for \(n<p\),
\[
t^{-n}\int_{|x|>t}|x|^n c|x|^{-p-1}\dd x=O(t^{-p}),
\]
whereas the missing top mode contributes
\[
t^{-p}u_p(y)\int_{R\le |x|\le t}x^pc|x|^{-p-1}\dd x
=2c\,u_p(y)t^{-p}\log t+O(t^{-p}).
\]
The Taylor remainder on \(R\le |x|\le t\) is
\[
O\!\left(t^{-(p+1)}\int_R^{t}x^{p+1}cx^{-p-1}\dd x\right)
=O(t^{-p}),
\]
and the large-tail quotient bound gives
\[
\int_{|x|>t}|R_{x/t}(y)-1|f(x)\dd x
\le C\int_t^\infty (1+x^2/t^2)x^{-p-1}\dd x=O(t^{-p}),
\]
using \(p\ge4\).

Put
\[
A_t(y)=\sum_{n=2}^{p-1}\mu_nu_n(y)t^{-n},\qquad
B_t(y)=2c\,u_p(y)t^{-p}\log t .
\]
Since \(\|A_t\|_\infty=O(t^{-2})\), \(\|B_t\|_\infty=O(t^{-p}\log t)\), and
\(\Delta_t=A_t+B_t+\varepsilon_t\), the entropy identity
\[
H(t)=\int_{\RR}\Phi(\Delta_t)\,\dd\omega,\qquad
\Phi(s)=(1+s)\log(1+s)-s,
\]
may be expanded to degree \(N\).  Terms containing \(\varepsilon_t\) are
\(O(t^{-2}t^{-p})=O(t^{-2N})\) after integration, and terms containing
\(B_t\) nonlinearly are \(O(t^{-(p+4)}\log t)+O(t^{-2p}(\log t)^2)
=o(t^{-2N}\log t)\), because \(p\ge4\).  Hence the only logarithmic contribution
at order \(t^{-2N}\log t\) is the quadratic cross term
\[
\int_{\RR}\mu_2u_2(y)t^{-2}\,2c\,u_p(y)t^{-p}\log t\,\omega(\dd y)
=2c\mu_2I(2,p)t^{-2N}\log t .
\]
The pure \(A_t\) terms through total order \(2N-2\) are exactly
\(\sum_{m=2}^{N-1}A_{2m}(\nu)t^{-2m}\), and the remaining pure \(A_t\) terms
are \(O(t^{-2N})=o(t^{-2N}\log t)\).

It remains only to identify the coefficient.  Writing \(p=2m\),
\(m=N-1\), the Laurent formula
\eqref{eq:mode-laurent-polynomial-definition}, together with
\(Q_2(z)=-(z+z^{-1}+z^2+z^{-2})/4\), gives
\[
I(2,2m)=[z^0]Q_2Q_{2m}
=-\frac12(-1)^m2^{-2m}\!\left\{\binom{2m-1}{0}
  \binom{2m-1}{1}\right\}
=(-1)^N(N-1)2^{-2N+2}.
\]
Substitution proves \eqref{eq:core-log-tail-counterexample-asymptotic}.

The same conclusion remains valid under the following perturbative tail
replacement.  Let
\[
  f(x)=g(x)+\bigl(c|x|^{-p-1}+r(x)\bigr)\mathbf1_{\{|x|\ge R\}},
\]
where \(r\) is even and integrable on \(\{|x|>R\}\), the displayed density is
nonnegative, and the total mass is normalized to one.  Assume, in addition,
that the perturbation has the lower moments needed for the retained
coefficients,
\[
  \int_{|x|\ge R}|x|^q|r(x)|\,\dd x<\infty
  \qquad(0\le q<p),
\]
that it does not change the logarithmic top truncated moment,
\[
  \int_{R\le |x|\le t}|x|^p r(x)\,\dd x=O(1),
\]
and that the two estimates used in the quotient-remainder split hold:
\[
  t^{-(p+1)}\int_{R\le |x|\le t}|x|^{p+1}|r(x)|\,\dd x=O(t^{-p}),
  \qquad
  \int_{|x|>t}\left(1+\frac{x^2}{t^2}\right)|r(x)|\,\dd x=O(t^{-p}).
\]
The proof above uses no other perturbative information.  Evenness preserves
the centering and removes odd tail modes, normalization keeps \(f\) a
probability density, the lower absolute moments define the coefficients
\(A_{2m}\) with \(m<N\), the signed truncated-\(p\)-moment condition keeps
\(\EE[X^p\mathbf1_{\{|X|\le t\}}]=2c\log t+O(1)\), the local
\((p+1)\)-weighted estimate controls the Taylor remainder on
\(R\le |x|\le t\), and the last display is exactly the uniform far-tail
quotient bound used for \(|x|>t\).
\end{proof}

\begin{proposition}[The \(N=2\) borderline infinite-variance layer]
Let \(c>0\) satisfy \(c\mathrm e^{-2}<1\), and let \(\nu\) be the symmetric
probability law with density
\[
f(x)=\frac{1-c\mathrm e^{-2}}{2}\mathbf1_{\{|x|\le1\}}
      +c|x|^{-3}\mathbf1_{\{|x|\ge \mathrm e\}} .
\]
Then \(\nu\) has finite mean and infinite variance, and
\[
\EE\!\left[X^2\mathbf1_{\{|X|\le t\}}\right]\sim 2c\log t,
\]
while
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\frac{c^2}{2}\,t^{-4}(\log t)^2+O(t^{-4}\log t).
\]
Thus the borderline \(N=2\) law is not a \(t^{-4}\log t\) replacement; its
leading layer is \(t^{-4}(\log t)^2\), in agreement with the variance
threshold theorem below.
\end{proposition}

\begin{proof}
The same truncation computation with \(L=2\) gives
\[
\Delta_t(y)=2c\,u_2(y)t^{-2}\log t+O(t^{-2})
\]
in \(L^\infty\cap L^1(\omega)\).  Since
\(\|\Delta_t\|_\infty=O(t^{-2}\log t)\),
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t)
=\frac12\int_{\RR}\Delta_t(y)^2\,\omega(\dd y)+O(t^{-6}(\log t)^3).
\]
Using \(I(2,2)=\int u_2^2\,\dd\omega=1/4\), the quadratic term is
\[
\frac12(2c)^2I(2,2)t^{-4}(\log t)^2+O(t^{-4}\log t)
=\frac{c^2}{2}t^{-4}(\log t)^2+O(t^{-4}\log t).
\]
\end{proof}

\subsection{Sharp leading threshold}

\begin{lemma}[Second-mode transform of a Cauchy translate]%
\label{lem:second-mode-cauchy-translate-transform}
For \(\varepsilon\in\RR\),
\[
\int_{\RR}u_2(y)\{R_\varepsilon(y)-1\}\,\omega(\dd y)
=\frac{4\varepsilon^2}{(4+\varepsilon^2)^2}.
\]
\end{lemma}

\begin{proof}
Since \(\int_{\RR}u_2\,\dd\omega=0\), it is enough to compute
\[
\int_{\RR}u_2(y)R_\varepsilon(y)\,\omega(\dd y).
\]
Using
\[
u_2(y)=\frac{3y^2-1}{(1+y^2)^2},
\qquad
R_\varepsilon(y)=\frac{1+y^2}{1+(y-\varepsilon)^2},
\]
this integral is
\[
\frac1\pi\int_{\RR} f_\varepsilon(y)\,\dd y,\qquad
f_\varepsilon(y):=
\frac{3y^2-1}{(1+y^2)^2(1+(y-\varepsilon)^2)} .
\]
For \(\varepsilon=0\), the integrand is
\((3y^2-1)/(1+y^2)^3\), whose integral is zero after the substitution
\(y=\tan\theta\).  Assume \(\varepsilon\ne0\), and write
\[
F_\varepsilon(z)=
\frac{3z^2-1}
{(z-\ii)^2(z+\ii)^2(z-\varepsilon-\ii)(z-\varepsilon+\ii)} .
\]
Closing the contour in the upper half-plane gives the order-two pole
\(z=\ii\) and the order-one pole \(z=\varepsilon+\ii\).  Since
\(F_\varepsilon(z)=O(|z|^{-4})\),
\[
\frac1\pi\int_{\RR}f_\varepsilon(y)\,\dd y
=2\ii\left(
\operatorname{Res}_{z=\ii}F_\varepsilon
+\operatorname{Res}_{z=\varepsilon+\ii}F_\varepsilon
\right).
\]
The simple pole gives
\[
\operatorname{Res}_{z=\varepsilon+\ii}F_\varepsilon
=-\frac{\ii(3\varepsilon^2+6\ii\varepsilon-4)}
{2\varepsilon^2(\varepsilon+2\ii)^2}.
\]
For the double pole,
\[
\begin{aligned}
\operatorname{Res}_{z=\ii}F_\varepsilon
&=
\left.
\frac{\dd}{\dd z}
\frac{3z^2-1}
{(z+\ii)^2(z-\varepsilon-\ii)(z-\varepsilon+\ii)}
\right|_{z=\ii} \\
&=-\frac{\ii(\varepsilon^2+2\ii\varepsilon+4)}
{2\varepsilon^2(\varepsilon-2\ii)^2}.
\end{aligned}
\]
Adding the residues cancels the apparent \(\varepsilon^{-2}\) singularity:
\[
\operatorname{Res}_{z=\ii}F_\varepsilon
+\operatorname{Res}_{z=\varepsilon+\ii}F_\varepsilon
=-\frac{2\ii\varepsilon^2}{(4+\varepsilon^2)^2}.
\]
Therefore
\[
\frac1\pi\int_{\RR} f_\varepsilon(y)\,\dd y
=\frac{4\varepsilon^2}{(4+\varepsilon^2)^2}.
\]
This proves the claim.
\end{proof}

\begin{theorem}[Sharp analytic variance threshold for the leading KL coefficient]%
\label{thm:sharp-leading-kl-variance-threshold}
Let \(\nu\) have finite mean \(\bar\gamma\), and put
\[
H(t)=D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma)).
\]
Then
\[
\EE X_c^2<\infty
\quad\Longleftrightarrow\quad
\limsup_{t\to\infty}t^4H(t)<\infty .
\]
When the variance is finite,
\[
\lim_{t\to\infty}t^4H(t)=\frac{(\EE X_c^2)^2}{8},
\]
and when the variance is infinite,
\[
\lim_{t\to\infty}t^4H(t)=+\infty .
\]
\end{theorem}

\begin{proof}
Finite variance is Theorem~\ref{thm:poisson-kl-even-orders} with \(N=2\).
For the converse, Csisz{\'a}r--Pinsker
\cite{Pinsker1964,Csiszar1967,BolleyVillani2005} and
\(\|u_2\|_\infty\le1\) give
\[
2H(t)\ge
\left|
\int_{\RR}u_2(y)\delta_t(y)\,\omega(\dd y)
\right|^2 .
\]
By Lemma~\ref{lem:second-mode-cauchy-translate-transform}, the projection is
\[
B(t)=\EE\!\left[\frac{4(X_c/t)^2}{(4+(X_c/t)^2)^2}\right]\ge0.
\]
Hence \(t^4H(t)\ge\frac12\{t^2B(t)\}^2\).  For fixed \(M\),
\[
t^2B(t)\ge
\frac{4}{(4+(M/t)^2)^2}
\EE[X_c^2\mathbf1_{\{|X_c|\le M\}}],
\]
and the right side tends to
\(\frac14\EE[X_c^2\mathbf1_{\{|X_c|\le M\}}]\).  Letting
\(M\to\infty\) proves divergence when the variance is infinite.
\end{proof}

\subsection{Multidimensional quotient jet and covariance layer}

For \(d\ge1\), let
\[
P_t^{(d)}(x)=c_d\,\frac{t}{(t^2+|x|^2)^{(d+1)/2}},
\qquad
\Omega_d(\dd y)=P_1^{(d)}(y)\dd y.
\]
For \(z\in\RR^d\), write
\[
F_y(z)=\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^{(d+1)/2}.
\]
Let \(U_\alpha^{(d)}\) be the Taylor coefficients of \(F_y(z)\) at \(z=0\).

\begin{lemma}[Local multidimensional quotient--Taylor remainder]%
\label{lem:local-multidim-quotient-taylor-remainder}
Let \(d\ge1\), and let
\[
F_y(z)=\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^{(d+1)/2}.
\]
For a multiindex \(\alpha\), put
\[
U_\alpha^{(d)}(y)=\frac1{\alpha!}\partial_z^\alpha F_y(0).
\]
For each integer \(L\ge2\) there is a constant \(C_{d,L}<\infty\) such that
\begin{equation}\label{eq:local-multidim-coefficient-bound}
\sup_{y\in\RR^d}|U_\alpha^{(d)}(y)|\le C_{d,L}
\qquad (|\alpha|\le L),
\end{equation}
and
\begin{equation}\label{eq:local-multidim-taylor-remainder}
\sup_{y\in\RR^d}\left|
F_y(z)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)z^\alpha
\right|
\le C_{d,L}|z|^{L+1}
\qquad (|z|\le1).
\end{equation}
\end{lemma}

\begin{proof}
Put \(a=(d+1)/2\).  For each multiindex \(\beta\), repeated
differentiation in \(z\) gives
\begin{equation}\label{eq:local-multidim-derivative-structure}
\partial_z^\beta F_y(w)
=(1+|y|^2)^a
\frac{P_\beta(y-w)}{(1+|y-w|^2)^{a+|\beta|}},
\end{equation}
where \(P_\beta\) is a polynomial of degree at most \(|\beta|\), depending
only on \(d\) and \(\beta\).  Indeed, \(z\)-differentiation is
\(-\xi\)-differentiation for \(\xi=y-z\), and each differentiation of
\((1+|\xi|^2)^{-b}\) raises the denominator exponent by one and multiplies
by a polynomial whose degree increases by at most one.

If \(|w|\le1\) and \(|y|>2\), then \(|y-w|\ge |y|/2\), and
\[
|\partial_z^\beta F_y(w)|
\le C_{d,\beta}
(1+|y|^2)^a
\frac{(1+|y|)^{|\beta|}}{(1+|y|^2)^{a+|\beta|}}
\le C_{d,\beta}.
\]
If \(|y|\le2\), the same derivative is bounded uniformly for \(|w|\le1\)
because the denominator in \eqref{eq:local-multidim-derivative-structure}
is at least \(1\).  Thus
\[
\sup_{\substack{y\in\RR^d,\ |w|\le1\\ |\beta|\le L+1}}
|\partial_z^\beta F_y(w)|<\infty .
\]
Taking \(w=0\) and \(|\beta|\le L\) proves
\eqref{eq:local-multidim-coefficient-bound}.  The multivariable Taylor
formula at \(z=0\), with integral remainder and the displayed derivative
bound for \(|\beta|=L+1\), gives
\eqref{eq:local-multidim-taylor-remainder}.
\end{proof}

\medskip
\noindent\textbf{Multidimensional radial Poisson jet hypothesis
\((\mathrm{MPJ}_{d,L})\).}
For \(d\ge1\) and an integer \(L\), we say that \((\nu,d,L)\) satisfies
\((\mathrm{MPJ}_{d,L})\) when \(L\ge\max\{2,d+1\}\), the mean
\(\bar\gamma\) exists, the centred tensor moments
\[
\mu_\alpha=\int_{\RR^d}(\gamma-\bar\gamma)^\alpha\,\dd\nu(\gamma)
\qquad(2\le|\alpha|\le L)
\]
exist, and the scaled quotient has the jet
\begin{equation}\label{eq:mpj-dl-definition}
\begin{aligned}
(\mathrm{MPJ}_{d,L}):\qquad
\delta_t^{(d)}(y)
&=
\sum_{2\le|\alpha|\le L}
\mu_\alpha U_\alpha^{(d)}(y)t^{-|\alpha|}
+\rho_{L,t}^{(d)}(y),\\
\|\rho_{L,t}^{(d)}\|_\infty+
\|\rho_{L,t}^{(d)}\|_{L^1(\Omega_d)}
&=o(t^{-L}),\qquad
\|\delta_t^{(d)}\|_\infty=O(t^{-2}).
\end{aligned}
\end{equation}
where
\[
\delta_t^{(d)}(y):=
\frac{(P_t^{(d)}*\nu)(\bar\gamma+ty)}
{P_t^{(d)}(ty)}-1 .
\]
This displayed quotient expansion is the hypothesis used by the entropy
transfer below.  Lemma~\ref{lem:multidim-quotient-remainder} is proved first;
then Theorem~\ref{thm:multidimensional-density-quotient} proves that finite
centred \(L\)th absolute moment implies \((\mathrm{MPJ}_{d,L})\).

\begin{lemma}[Global multidimensional quotient remainder]\label{lem:multidim-quotient-remainder}
Let \(d\ge1\), \(a=(d+1)/2\), and
\[
F_y(z)=\left(\frac{1+|y|^2}{1+|y-z|^2}\right)^a .
\]
For a multiindex \(\alpha\), put
\[
U_\alpha^{(d)}(y)=\frac1{\alpha!}\partial_z^\alpha F_y(0).
\]
If \(L\ge d+1\), then there is a constant \(C_{d,L}<\infty\) such that
\begin{equation}\label{eq:multidim-coefficient-bound}
\sup_{y\in\RR^d}|U_\alpha^{(d)}(y)|\le C_{d,L}
\qquad (|\alpha|\le L),
\end{equation}
\begin{equation}\label{eq:multidim-small-translation-remainder}
\sup_{y\in\RR^d}\left|
F_y(z)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)z^\alpha
\right|
\le C_{d,L}|z|^{L+1}
\qquad (|z|\le1),
\end{equation}
and
\begin{equation}\label{eq:multidim-large-translation-remainder}
\sup_{y\in\RR^d}\left|
F_y(z)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)z^\alpha
\right|
\le C_{d,L}|z|^{L}
\qquad (|z|>1).
\end{equation}
Equivalently, for all \(y,z\),
\begin{equation}\label{eq:multidim-global-translation-remainder}
\left|
F_y(z)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)z^\alpha
\right|
\le C_{d,L}\left(
|z|^{L+1}{\bf1}_{\{|z|\le1\}}
{}
+{}|z|^L{\bf1}_{\{|z|>1\}}
\right).
\end{equation}
Consequently, if \(X\) is an \(\RR^d\)-valued random vector with
\(\EE|X|^L<\infty\), \(\Omega_d\) is the Cauchy probability measure above,
and
\[
r_t(y):=\EE\left[
F_y(X/t)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)(X/t)^\alpha
\right],
\]
then
\begin{equation}\label{eq:multidim-integrated-remainder-bound}
\|r_t\|_{L^\infty(\RR^d)}+\|r_t\|_{L^1(\Omega_d)}
\le C_{d,L}t^{-L}\EE\left[
|X|^L\frac{|X|}{t}{\bf1}_{\{|X|\le t\}}
{}
+{}|X|^L{\bf1}_{\{|X|>t\}}
\right]=o(t^{-L}).
\end{equation}
\end{lemma}

\begin{proof}
The coefficient bound \eqref{eq:multidim-coefficient-bound} and the
small-translation remainder
\eqref{eq:multidim-small-translation-remainder} are exactly
Lemma~\ref{lem:local-multidim-quotient-taylor-remainder}.

It remains to handle large translations.  Fix \(|z|>1\).  First suppose
\(|y-z|\le |z|/2\).  Then \(|y|\le3|z|/2\), and hence
\[
F_y(z)\le (1+|y|^2)^a\le C_d|z|^{d+1}.
\]
On the complementary region \(|y-z|>|z|/2\), the elementary estimate
\[
1+|y|^2
\le 2(1+|y-z|^2+|z|^2)
\le 2(1+|z|^2)(1+|y-z|^2)
\]
gives
\[
F_y(z)
\le 2^a(1+|z|^2)^a
\le C_d|z|^{d+1}
\qquad(|z|>1).
\]
Thus, uniformly in \(y\),
\[
\sup_y F_y(z)\le C_d|z|^{d+1}\le C_{d,L}|z|^L
\qquad(|z|>1),
\]
where the last inequality is precisely where \(L\ge d+1\) is used.  The
coefficient bound from the local lemma gives, again uniformly in \(y\),
\[
\sum_{|\alpha|\le L}|U_\alpha^{(d)}(y)|\,|z|^{|\alpha|}
\le C_{d,L}\sum_{k=0}^{L}|z|^k
\le C_{d,L}'|z|^L
\qquad(|z|>1).
\]
Consequently
\[
\sup_y\left(
|F_y(z)|
+\sum_{|\alpha|\le L}|U_\alpha^{(d)}(y)|\,|z|^{|\alpha|}
\right)
\le C_{d,L}|z|^L
\qquad(|z|>1),
\]
which proves \eqref{eq:multidim-large-translation-remainder}.  Combining
this estimate with the small-translation remainder proves the global form
\eqref{eq:multidim-global-translation-remainder}.

Set \(z=X/t\) in \eqref{eq:multidim-global-translation-remainder}.  Taking
expectations gives the pointwise bound, uniform in \(y\),
\[
|r_t(y)|
\le
C_{d,L}\left[
t^{-(L+1)}
\EE\!\left(|X|^{L+1}{\bf1}_{\{|X|\le t\}}\right)
+t^{-L}
\EE\!\left(|X|^L{\bf1}_{\{|X|>t\}}\right)
\right].
\]
Equivalently,
\[
t^{-(L+1)}
\EE\!\left(|X|^{L+1}{\bf1}_{\{|X|\le t\}}\right)
=
t^{-L}
\EE\!\left[
|X|^L\frac{|X|}{t}{\bf1}_{\{|X|\le t\}}
\right]
=o(t^{-L}),
\]
because \((|X|/t){\bf1}_{\{|X|\le t\}}\le1\) and converges pointwise to
zero, while \(|X|^L\) is integrable.  Also
\[
t^{-L}\EE\!\left(|X|^L{\bf1}_{\{|X|>t\}}\right)=o(t^{-L})
\]
by the absolute continuity of the integral for the integrable function
\(|X|^L\).  These two estimates are independent of \(y\), and therefore give
the \(L^\infty\) part of \eqref{eq:multidim-integrated-remainder-bound}.
The \(L^1(\Omega_d)\) part follows from the same uniform bound because
\(\Omega_d\) is a probability measure.
\end{proof}

\begin{theorem}[Finite-\(L\) multidimensional density quotient expansion]%
\label{thm:multidimensional-density-quotient}
Let \(L\ge\max\{2,d+1\}\), and let \(\nu\) be a probability measure on
\(\RR^d\) with mean \(\bar\gamma\) and finite centred \(L\)th absolute
moment.  Put \(X_c=\gamma-\bar\gamma\) and
\[
\mu_\alpha=\int_{\RR^d}X_c^\alpha\,\dd\nu(\gamma)
\qquad(|\alpha|\le L).
\]
Then, for
\[
\delta_t^{(d)}(y):=
\frac{(P_t^{(d)}*\nu)(\bar\gamma+ty)}
{P_t^{(d)}(ty)}-1,
\]
\begin{equation}\label{eq:multidim-density-quotient-expansion-main}
\delta_t^{(d)}(y)=
\sum_{2\le|\alpha|\le L}\mu_\alpha U_\alpha^{(d)}(y)t^{-|\alpha|}
+\rho_{L,t}^{(d)}(y),
\qquad
\|\rho_{L,t}^{(d)}\|_\infty+
\|\rho_{L,t}^{(d)}\|_{L^1(\Omega_d)}=o(t^{-L}).
\end{equation}
Moreover \(\|\delta_t^{(d)}\|_\infty=O(t^{-2})\).
In particular, \((\nu,d,L)\) satisfies \((\mathrm{MPJ}_{d,L})\).
\end{theorem}

\begin{proof}
For \(x=\bar\gamma+ty\),
\[
\frac{P_t^{(d)}(x-\gamma)}{P_t^{(d)}(x-\bar\gamma)}
=
\left(\frac{t^2+|x-\bar\gamma|^2}{t^2+|x-\gamma|^2}\right)^{(d+1)/2}
=F_y(X_c/t).
\]
Thus
\[
\delta_t^{(d)}(y)=\int_{\RR^d}\{F_y(X_c/t)-1\}\,\dd\nu(\gamma).
\]
Subtract the Taylor polynomial through order \(L\).  By
Lemma~\ref{lem:multidim-quotient-remainder}, the remainder
\[
\rho_{L,t}^{(d)}(y):=
\int_{\RR^d}\left\{
F_y(X_c/t)-\sum_{|\alpha|\le L}U_\alpha^{(d)}(y)(X_c/t)^\alpha
\right\}\dd\nu(\gamma)
\]
satisfies
\[
\|\rho_{L,t}^{(d)}\|_\infty+
\|\rho_{L,t}^{(d)}\|_{L^1(\Omega_d)}
\le C_{d,L}t^{-L}\EE\left[
|X_c|^L\frac{|X_c|}{t}{\bf1}_{\{|X_c|\le t\}}
{}
+{}|X_c|^L{\bf1}_{\{|X_c|>t\}}
\right]
=o(t^{-L}).
\]
This is the explicit \(L^\infty\cap L^1(\Omega_d)\) tail split.  The
\(|X_c|\le t\) region is controlled by the uniform Taylor remainder
\(O(|X_c/t|^{L+1})\); the tail region \(|X_c|>t\) is controlled by the
large-translation bound \(O(|X_c/t|^L)\), which is where \(L\ge d+1\) enters.

The constant term equals \(1\) and is removed by the definition of
\(\delta_t^{(d)}\).  The first-order Taylor layer is
\[
\sum_{|\alpha|=1}\mu_\alpha U_\alpha^{(d)}(y)t^{-1}=0
\]
because \(\nu\) is centred.  The remaining Taylor terms give
\eqref{eq:multidim-density-quotient-expansion-main}.  Finally,
\eqref{eq:multidim-coefficient-bound} and the displayed expansion give
\(\|\delta_t^{(d)}\|_\infty=O(t^{-2})+o(t^{-L})=O(t^{-2})\).
\end{proof}

\begin{theorem}[Finite-\(L\) multidimensional entropy jet]%
\label{thm:multidimensional-poisson-entropy-jets}
Assume that \((\nu,d,L)\) satisfies \((\mathrm{MPJ}_{d,L})\).  Then
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=\sum_{m=4}^{L+2}\mathcal A_m^{(d)}(\nu)t^{-m}
+o(t^{-(L+2)}),
\]
where
\[
\mathcal A_m^{(d)}(\nu)=
\sum_{r=2}^{\lfloor m/2\rfloor}
\frac{(-1)^r}{r(r-1)}
\sum_{\substack{|\alpha_1|+\cdots+|\alpha_r|=m\\
2\le|\alpha_j|\le L}}
\mu_{\alpha_1}\cdots\mu_{\alpha_r}
\int_{\RR^d}\prod_{j=1}^r
U_{\alpha_j}^{(d)}(y)\,\Omega_d(\dd y).
\]
\end{theorem}

\paragraph{Remark.}
The coefficient formula includes the multidimensional parity cancellation:
\(U_\alpha^{(d)}(-y)=(-1)^{|\alpha|}U_\alpha^{(d)}(y)\), while
\(\Omega_d\) is even.  Hence every summand of \(\mathcal A_m^{(d)}\) with
odd total order \(m\) integrates to zero, so all retained odd layers vanish.
For \(d=1\), \(\Sigma_0=0\), \(\operatorname{tr}\Sigma=\mu_2\), and the
quadratic layer below gives \(c_1^{\rm iso}=1/8\); therefore
\(\mathcal A_4^{(1)}=A_4=\mu_2^2/8\), matching the one-dimensional
Cayley--Chebyshev coefficient.

\begin{proof}
Write
\[
D_j^{(d)}(y)=\sum_{|\alpha|=j}\mu_\alpha U_\alpha^{(d)}(y),\qquad
d_t(y)=\sum_{j=2}^{L}D_j^{(d)}(y)t^{-j}.
\]
\((\mathrm{MPJ}_{d,L})\) gives
\[
\delta_t^{(d)}=d_t+\rho_{L,t}^{(d)},\qquad
\|d_t\|_\infty=O(t^{-2}),\qquad
\|\rho_{L,t}^{(d)}\|_\infty+
\|\rho_{L,t}^{(d)}\|_{L^1(\Omega_d)}=o(t^{-L}),
\]
and also \(\|\delta_t^{(d)}\|_\infty=O(t^{-2})\).  Since
\[
P_t^{(d)}(ty)t^d\,\dd y=P_1^{(d)}(y)\dd y=\Omega_d(\dd y)
\]
and both densities have total mass one,
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=\int_{\RR^d}\Phi(\delta_t^{(d)}(y))\,\Omega_d(\dd y),
\]
where \(\Phi(s)=(1+s)\log(1+s)-s\).

Let \(R=\lfloor(L+2)/2\rfloor+1\).  For large \(t\), \(\|\delta_t^{(d)}\|_\infty\)
is small and
\[
\Phi(\delta_t^{(d)})
=\sum_{r=2}^{R}\frac{(-1)^r}{r(r-1)}(\delta_t^{(d)})^r
+O(|\delta_t^{(d)}|^{R+1}).
\]
The Taylor tail integrates to \(O(t^{-2(R+1)})=o(t^{-(L+2)})\).  For
\(2\le r\le R\),
\[
\|(\delta_t^{(d)})^r-d_t^r\|_{L^1(\Omega_d)}
\le C_r\sum_{\ell=1}^{r}
\|d_t\|_\infty^{r-\ell}
\|\rho_{L,t}^{(d)}\|_\infty^{\ell-1}
\|\rho_{L,t}^{(d)}\|_{L^1(\Omega_d)}
=o(t^{-(L+2)}),
\]
with worst layer \(\ell=1,r=2\), namely
\(O(t^{-2})o(t^{-L})\).  Therefore only the pure powers of the finite
polynomial \(d_t\) contribute through order \(L+2\).  Collecting the
coefficient of \(t^{-m}\) in
\[
\sum_{r=2}^{R}\frac{(-1)^r}{r(r-1)}d_t(y)^r
\]
gives precisely the displayed formula for \(\mathcal A_m^{(d)}(\nu)\), and
all omitted layers are \(o(t^{-(L+2)})\).
\end{proof}

\begin{proposition}[Formal covariance layer under finite covariance]%
\label{prop:multidim-formal-covariance-layer}
\label{thm:multidim-leading-positive-defect}
Let \(\nu\) be a probability measure on \(\RR^d\) with finite centred second
moment.  Put
\[
\Sigma=\EE[X_cX_c^{\mathsf T}],\qquad
\Sigma_0=\Sigma-\frac{\operatorname{tr}\Sigma}{d}I_d.
\]
The degree-two centred density mode is
\begin{equation}\label{eq:multidim-leading-theorem-density-mode}
b_\Sigma(y)=
\frac{d+1}{2}\left(
-\frac{\operatorname{tr}\Sigma}{1+|y|^2}
+\frac{(d+3)y^{\mathsf T}\Sigma y}{(1+|y|^2)^2}
\right).
\end{equation}
Its formal quadratic KL layer is
\begin{equation}\label{eq:multidim-leading-positive-defect}
A_{4,\mathrm{quad}}^{(d)}(\nu)
:=\frac12\int_{\RR^d}b_\Sigma(y)^2\,\Omega_d(\dd y)
=c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2,
\end{equation}
with
\begin{equation}\label{eq:multidim-leading-positive-constants}
c_d^{\rm iso}=
\frac{3(d+1)(7d+9)}
{4d(d+3)(d+5)(d+7)},
\qquad
c_d^{\rm tr}=
\frac{3(d+1)(d+3)}
{4(d+5)(d+7)} .
\end{equation}
This is a covariance-layer identity only.  Under finite covariance alone no
KL asymptotic is asserted.
Two immediate checks are
\[
d=1:\qquad \Sigma_0=0,\qquad c_1^{\rm iso}=\frac18,\qquad
A_{4,\mathrm{quad}}^{(1)}(\nu)=\frac{(\operatorname{tr}\Sigma)^2}{8},
\]
and, for an isotropic covariance \(\Sigma=\tau I_d\),
\[
\Sigma_0=0,\qquad
A_{4,\mathrm{quad}}^{(d)}(\nu)=c_d^{\rm iso}d^2\tau^2 .
\]
\end{proposition}

\begin{proof}
Let \(a=(d+1)/2\) and \(A=1+|y|^2\).  The degree-two homogeneous part of
\[
\left(1+\frac{-2y\cdot z+|z|^2}{A}\right)^{-a}
\]
is
\[
a\left(-\frac{|z|^2}{A}
+\frac{(d+3)(y\cdot z)^2}{A^2}\right).
\]
Averaging with \(z=X_c/t\) gives \(b_\Sigma\).  Decompose
\(\Sigma=\tau I_d+\Sigma_0\), \(\tau=d^{-1}\operatorname{tr}\Sigma\).  Then
\[
b_\Sigma(y)=
a\tau\frac{3|y|^2-d}{(1+|y|^2)^2}
+a(d+3)\frac{y^{\mathsf T}\Sigma_0y}{(1+|y|^2)^2}.
\]
The mixed integral vanishes by rotational symmetry.  The remaining constants
come from the radial beta-prime moments
\[
\EE\frac{R^k}{(1+R)^4}
=
\frac{B\bigl(d/2+k,\,1/2+4-k\bigr)}
{B\bigl(d/2,\,1/2\bigr)}
\qquad(k=0,1,2)
\]
for \(R=|Y|^2\), \(Y\sim\Omega_d\).  Equivalently,
\[
\EE\frac{R^k}{(1+R)^4}
=
\frac{\Gamma(d/2+k)\Gamma(9/2-k)}
{\Gamma(d/2)\Gamma(1/2)}
\frac{\Gamma((d+1)/2)}{\Gamma((d+9)/2)} .
\]
Thus, with \(D_d=(d+1)(d+3)(d+5)(d+7)\),
\[
\EE\frac{1}{(1+R)^4}=\frac{105}{D_d},\qquad
\EE\frac{R}{(1+R)^4}=\frac{15d}{D_d},\qquad
\EE\frac{R^2}{(1+R)^4}=\frac{3d(d+2)}{D_d}.
\]
For \(d=1\), the first value is \(35/128\), matching the one-dimensional
Cauchy normalization.  The angular factor is
\[
\EE_\Theta[(\Theta^{\mathsf T}\Sigma_0\Theta)^2]
=\frac{2}{d(d+2)}\|\Sigma_0\|_{\rm HS}^2 .
\]
Consequently
\[
\begin{aligned}
\EE\frac{(3R-d)^2}{(1+R)^4}
&=
9\EE\frac{R^2}{(1+R)^4}
-6d\EE\frac{R}{(1+R)^4}
+d^2\EE\frac1{(1+R)^4} \\
&=
\frac{6d(7d+9)}
{(d+1)(d+3)(d+5)(d+7)} .
\end{aligned}
\]
The isotropic contribution to \(\frac12\int b_\Sigma^2\,\dd\Omega_d\) is
\[
\begin{aligned}
\frac12 a^2\frac{(\operatorname{tr}\Sigma)^2}{d^2}
\EE\frac{(3R-d)^2}{(1+R)^4}
&=
\frac{3(d+1)(7d+9)}
{4d(d+3)(d+5)(d+7)}
(\operatorname{tr}\Sigma)^2 .
\end{aligned}
\]
For the traceless part,
\[
\begin{aligned}
\int_{\RR^d}
\frac{(y^{\mathsf T}\Sigma_0y)^2}{(1+|y|^2)^4}\,\Omega_d(\dd y)
&=
\EE\frac{R^2}{(1+R)^4}\,
\EE_\Theta[(\Theta^{\mathsf T}\Sigma_0\Theta)^2] \\
&=
\frac{6}{(d+1)(d+3)(d+5)(d+7)}
\|\Sigma_0\|_{\rm HS}^2 .
\end{aligned}
\]
Multiplication by \(\frac12a^2(d+3)^2\) gives
\[
\frac{3(d+1)(d+3)}{4(d+5)(d+7)}
\|\Sigma_0\|_{\rm HS}^2 .
\]
This proves \eqref{eq:multidim-leading-positive-constants}.  The constants are
positive, so the layer vanishes exactly when \(\Sigma=0\).
\end{proof}

\begin{corollary}[Leading KL coefficient under \((\mathrm{MPJ}_{d,L})\)]%
\label{cor:multidim-leading-kl-coefficient}
Let \(d\ge1\) and let \(L\) be an integer.  If \((\nu,d,L)\) satisfies
\((\mathrm{MPJ}_{d,L})\), then
\[
D_{\rm KL}\bigl(P_t^{(d)}*\nu\,\|\,P_t^{(d)}(\cdot-\bar\gamma)\bigr)
=A_{4,\mathrm{quad}}^{(d)}(\nu)t^{-4}+o(t^{-4}).
\]
\end{corollary}

\begin{proof}
The entropy jet identifies the first nonzero retained layer as
\(\frac12\int b_\Sigma^2\,\dd\Omega_d\), and
Proposition~\ref{prop:multidim-formal-covariance-layer} computes that
integral.  The hypothesis \((\mathrm{MPJ}_{d,L})\) is exactly the quotient
hypothesis needed by Theorem~\ref{thm:multidimensional-poisson-entropy-jets};
Theorem~\ref{thm:multidimensional-density-quotient} is the preceding
finite-moment result which proves that hypothesis.  The covariance-layer
calculation itself remains the separate finite-second-moment identity of
Proposition~\ref{prop:multidim-formal-covariance-layer}.
\end{proof}
