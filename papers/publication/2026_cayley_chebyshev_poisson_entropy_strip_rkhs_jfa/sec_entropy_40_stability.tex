\subsection{Coefficient-level moment-defect corollaries}
\label{subsec:fourth-moment-stability}

This subsection has two layers.  The first layer records the sixth-order
coefficient defect.  Its fourth-moment algebra begins with the moment
polynomial \(A_6^{\mathrm{mom}}\) from
\eqref{eq:n4-notation-a6}; after the sixth-coefficient identification in
Theorem~\ref{thm:poisson-kl-eighth}, this same polynomial is the genuine
\(t^{-6}\) entropy coefficient \(A_6(\nu)\), and
\(\Delta_6(\nu)\) is its two-point defect.
The second layer is the primary finite-time application,
Theorem~\ref{thm:finite-time-poisson-entropy-stability}: after fixing a
variance-and-seventh-moment shell, it combines a measured KL value with
the shell constants explicitly displayed in that theorem,
\(C_{\sigma,M}^{(8)},B_{\sigma,M}^{(8)},T_{\sigma,M}^{(8)}\).  The measured
KL value supplies only the statistic \(\mathfrak D_t^{(6)}(\nu)\).  The subsidiary
fixed-law oracle form, Theorem~\ref{thm:oracle-finite-time-poisson-entropy-stability},
instead takes supplied eighth-order certificate bounds \(C,B,T\).  In both
finite-time forms, the coefficient and remainder bounds are fixed before the
measured KL value is inserted.

\paragraph{Fourth-moment algebra.}

\begin{lemma}[Fourth-moment algebraic defect]%
\label{lem:fourth-moment-algebraic-defect}
Let $\nu$ have mean~$\bar\gamma$, variance $\sigma^2>0$, and finite
centred fourth moment.  Define
\[
A_6(\nu):=
\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64},
\qquad
\Delta_6(\nu):=
-\frac{7}{64}\sigma^6-A_6(\nu).
\]
These are the \(A_6^{\mathrm{mom}}\) and
\(\Delta_6^{\mathrm{mom}}\) quantities from
\eqref{eq:n4-notation-a6}--\eqref{eq:n4-notation-delta6}, used here only as
fourth-moment polynomials with local unsuperscripted names.  On $(\RR,\nu)$ set
\begin{align*}
X&:=\frac{\gamma-\bar\gamma}{\sigma},
&
\beta_3&:=\EE[X^3],
&
\beta_4&:=\EE[X^4],
\\
Q_2&:=X^2-1-\beta_3X,
&
\kappa&:=\|Q_2\|_{L^2(\nu)}^2.
\end{align*}
Then the pure fourth-moment identity is
\begin{equation}\label{eq:defect-sixth-fourth-moment}
\Delta_6(\nu)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa,
\qquad
\kappa=\beta_4-1-\beta_3^2\ge0.
\end{equation}
Moreover, \(\Delta_6(\nu)=0\) if and only if the centred law of
$\gamma-\bar\gamma$ is $\rho_\sigma=\frac12(\delta_{-\sigma}
+\delta_\sigma)$.
\end{lemma}

\begin{proof}
The identity for $\kappa$ follows by expanding $Q_2^2$ and using
$\EE[X]=0$, $\EE[X^2]=1$, and $\EE[X^3]=\beta_3$:
\[
\kappa
=\EE[(X^2-1-\beta_3X)^2]
=\beta_4-1-\beta_3^2.
\]
Since $\mu_3=\sigma^3\beta_3$ and $\mu_4=\sigma^4\beta_4$,
\[
-\frac{7}{64}\sigma^6-A_6(\nu)
=\frac{\sigma^6}{64}\bigl(-7-1-6\beta_3^2+8\beta_4\bigr)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa.
\]
This proves \eqref{eq:defect-sixth-fourth-moment} and non-negativity.  If
\(\Delta_6(\nu)=0\), then $\beta_3=0$ and $\kappa=0$, so
$X^2=1$ almost surely.  The conditions $\EE[X]=0$ and $\EE[X^2]=1$ then
force $X=\pm1$ with equal probabilities, i.e. the centred law is
$\rho_\sigma$.  The converse is immediate by substitution.
\end{proof}

\begin{lemma}[Fourth-moment algebraic coupling]%
\label{lem:fourth-moment-algebraic-coupling}
Let $\nu$ have mean~$\bar\gamma$, variance $\sigma^2>0$, and finite
centred fourth moment.  Let $\nu_c$ be the centred law of
$X_c=\gamma-\bar\gamma$ and set
\[
\rho_\sigma:=\frac12(\delta_{-\sigma}+\delta_\sigma).
\]
For the fourth-moment polynomial $\Delta_6(\nu)$ defined above,
\[
\Delta_6(\nu)\ge0
\]
and
\begin{equation}\label{eq:fourth-moment-algebraic-coupling}
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\end{equation}
Moreover, $\Delta_6(\nu)=0$ if and only if
$\nu_c=\rho_\sigma$.
\end{lemma}

\begin{proof}
All random variables below live on $(\RR,\nu)$.  Set
\[
X:=\frac{\gamma-\bar\gamma}{\sigma},
\qquad
\beta_3:=\EE[X^3],
\qquad
Q_2:=X^2-1-\beta_3X,
\qquad
\kappa:=\|Q_2\|_{L^2(\nu)}^2 .
\]
By \eqref{eq:defect-sixth-fourth-moment},
\begin{equation}\label{eq:algebraic-coupling-delta-six}
\Delta_6(\nu)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa .
\end{equation}
Define
\[
Y:=\mathbf{1}_{\{X\ge 0\}}-\mathbf{1}_{\{X<0\}}\in\{-1,1\}.
\]
For every real $x$, with $\operatorname{sgn}(0)=1$,
\[
|x-\operatorname{sgn}(x)|\le |x^2-1|.
\]
Therefore
\[
\EE[(X-Y)^2]\le \EE[(X^2-1)^2].
\]
The decomposition $X^2-1=\beta_3X+Q_2$ is orthogonal in $L^2(\nu)$ because
\[
\EE[XQ_2]=\EE[X^3]-\EE[X]-\beta_3\EE[X^2]=0.
\]
Thus
\[
\EE[(X^2-1)^2]
=\beta_3^2+\kappa
\le 2\beta_3^2+8\kappa
=\frac{64}{\sigma^6}\Delta_6(\nu),
\]
where the last equality is \eqref{eq:algebraic-coupling-delta-six}.  Since
$\EE[X]=0$,
\[
|\EE[Y]|=|\EE[Y-X]|
\le \EE[|Y-X|]
\le \EE[(Y-X)^2]^{1/2}
\le \frac{8}{\sigma^3}\Delta_6(\nu)^{1/2}.
\]
Let $Z$ be a symmetric Rademacher random variable.  If
$p=\mathbb P(Y=1)$, then $\EE[Y]=2p-1$ and the optimal transport between
$\mathcal L(Y)$ and the symmetric law on $\{-1,1\}$ moves
$|p-1/2|$ mass across distance~$2$.  Hence
\[
W_2(\mathcal L(Y),\mathcal L(Z))^2
=4|p-1/2|=2|\EE[Y]|
\le \frac{16}{\sigma^3}\Delta_6(\nu)^{1/2}.
\]
Also
\[
W_2(\mathcal L(X),\mathcal L(Y))
\le \EE[(X-Y)^2]^{1/2}
\le \frac{8}{\sigma^3}\Delta_6(\nu)^{1/2}.
\]
The laws satisfy $\nu_c=\mathcal L(\sigma X)$ and
$\rho_\sigma=\mathcal L(\sigma Z)$.  Taking square roots in the preceding
$W_2$ bound, then using scaling and the triangle inequality, gives
\eqref{eq:fourth-moment-algebraic-coupling}.
The zero statement follows from
Lemma~\ref{lem:fourth-moment-algebraic-defect}.
\end{proof}

\begin{remark}[Algebraic layer]
Lemma~\ref{lem:fourth-moment-algebraic-coupling} belongs to the
fourth-moment algebra layer.  The entropy coefficient interpretation enters
only through Lemma~\ref{lem:seventh-moment-bridge}, and the finite-time
estimates enter later through certified eighth-order data.
\end{remark}

\paragraph{Fourth-moment coefficient bridge.}

\begin{lemma}[Fourth-moment coefficient identification bridge]%
\label{lem:seventh-moment-bridge}
Assume that $\nu$ has mean~$\bar\gamma$, variance $\sigma^2>0$, and finite
centred fourth absolute moment.  Theorem~\ref{thm:poisson-kl-even-orders},
applied with \(N=3\), identifies the notation-block polynomial \(A_6(\nu)\)
as the universal \(t^{-6}\) entropy coefficient and gives
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\frac{\sigma^4}{8t^4}+\frac{A_6(\nu)}{t^6}
+o(t^{-6}).
\]
Equivalently,
\begin{equation}\label{eq:c6-entropy-bridge-explicit}
A_6(\nu)
=\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64}.
\end{equation}
Consequently \(\Delta_6(\nu)\) is the corresponding sixth-order
coefficient defect.  The seventh moment enters later through the
\(N=4,\eta=1\) quantitative \(O(t^{-9})\) remainder used in the finite-time
estimate.
\end{lemma}

\begin{proof}
The finite fourth absolute moment is precisely the \(N=3\) hypothesis in
Theorem~\ref{thm:poisson-kl-even-orders}; it is the algebraic threshold for
evaluating the displayed universal moment-polynomial coefficient \(A_6\).
That theorem gives
\[
D_{\mathrm{KL}}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=\frac{\sigma^4}{8t^4}
+\frac{\sigma^6+6\mu_3^2-8\sigma^2\mu_4}{64\,t^6}
+o(t^{-6}),
\]
using the evaluated universal coefficients \(A_4(\nu)\) and
\(A_6(\nu)\).  The explicit value of \(A_6(\nu)\) is the sixth-order arithmetic already recorded
in Lemma~\ref{lem:n4-entropy-coefficient-substitution}; it involves only
\(\mu_2,\mu_3,\mu_4\) and hence is available under the fourth-moment
hypothesis.
The coefficient in an expansion of the form
\[
F(t)=a_4t^{-4}+a_6t^{-6}+o(t^{-6})
\]
is unique: if the same \(F\) is also
\(\tilde a_4t^{-4}+\tilde a_6t^{-6}+o(t^{-6})\), then multiplying the
difference first by \(t^4\) and then, after \(a_4=\tilde a_4\), by \(t^6\)
gives \(a_6=\tilde a_6\).  Hence the genuine entropy coefficient is exactly
the displayed polynomial \(A_6(\nu)\), proving
\eqref{eq:c6-entropy-bridge-explicit}.  Substituting this identity into
\(-7\sigma^6/64-A_6(\nu)\) gives the displayed polynomial
\(\Delta_6(\nu)\), now read as the coefficient defect.
\end{proof}

\begin{corollary}[Coefficient-defect coupling corollary]%
\label{cor:coefficient-defect-coupling}
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$,
variance $\sigma^2>0$, and finite centred fourth absolute moment.
By Lemma~\ref{lem:seventh-moment-bridge}, \(A_6(\nu)\) is the genuine
\(t^{-6}\) entropy coefficient under this fourth-moment hypothesis, and
\(\Delta_6(\nu)\) is the corresponding entropy-coefficient defect.
On $(\RR,\nu)$ set
\[
X=\frac{\gamma-\bar\gamma}{\sigma},
\qquad
\beta_3=\EE X^3,
\qquad
Q_2=X^2-1-\beta_3X,
\qquad
\kappa=\|Q_2\|_{L^2(\nu)}^2 .
\]
Under this fourth-moment hypothesis,
\begin{equation}\label{eq:w2-theorem-moment-defect-form}
\Delta_6(\nu)
=\frac{\sigma^6}{32}\beta_3^2+\frac{\sigma^6}{8}\kappa .
\end{equation}
Let $\nu_c$ be the centred law of $X_c=\gamma-\bar\gamma$ and set
\[
\rho_\sigma:=\frac12(\delta_{-\sigma}+\delta_\sigma).
\]
Then the coupling argument gives
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\]
In particular \(\Delta_6(\nu)\ge0\).  This is a coefficient-defect estimate for
closeness to the symmetric two-point law.  The numerical constants $4$ and
$8$ are universal.  At this coefficient level the only non-universal
parameters displayed in the \(W_2\) modulus are the variance scale $\sigma$
and the coefficient defect \(\Delta_6(\nu)\) itself; no finite-time remainder
constant, measured KL value, or eighth-order oracle input is involved.  The
finite-time theorems below first certify those eighth-order inputs and only
then insert the observed statistic \(\mathfrak D_t^{(6)}(\nu)\).
In particular, for fixed $\sigma$,
\[
W_2(\nu_c,\rho_\sigma)=O\!\bigl(\Delta_6(\nu)^{1/4}\bigr)
\qquad\text{as }\Delta_6(\nu)\downarrow 0.
\]
Moreover \(\Delta_6(\nu)=0\) if and only if
\(\nu_c=\rho_\sigma\).
\end{corollary}

\begin{proof}
The displayed fourth absolute moment condition is exactly the hypothesis
of Lemma~\ref{lem:seventh-moment-bridge}.  Hence \(A_6(\nu)\) is the
sixth-order entropy coefficient and \(\Delta_6(\nu)\) is its
coefficient defect.
The identity \eqref{eq:w2-theorem-moment-defect-form} is
\eqref{eq:defect-sixth-fourth-moment} after applying this bridge.
Substituting
\eqref{eq:fourth-moment-algebraic-coupling} gives the stated $W_2$ estimate,
and the non-negativity and zero characterization are those of
Lemma~\ref{lem:fourth-moment-algebraic-defect}.
The \(O((\Delta_6)^{1/4})\) statement for fixed \(\sigma\)
follows immediately from the displayed estimate.
\end{proof}

\begin{center}
\fbox{\begin{minipage}{0.91\textwidth}
\textbf{Certificate hypothesis for the finite-time theorem.}
Before any measured KL value is used, fix a scale \(\sigma>0\) and a
seventh-moment radius \(M\ge\sigma^7\), set
\(\widehat M=M/\sigma^7\), and restrict to the nonempty shell
\[
\mathcal S_{\sigma,M}:=
\left\{\nu:\ \mu_2(\nu)=\sigma^2,\quad
\EE_\nu|\gamma-\bar\gamma|^7\le M\right\}.
\]
Equivalently, for \(X=(\gamma-\bar\gamma)/\sigma\),
\[
\EE X=0,\qquad \EE X^2=1,\qquad \EE|X|^7\le\widehat M .
\]
The certificate consists of the dimensionless shell functions
\(C(\widehat M)\), \(B(\widehat M)\), and \(T(\widehat M)\) recorded in
Appendix~\ref{app:finite-time-defect-estimates}, or equivalently their scale
restores \(C_{\sigma,M}^{(8)}\), \(B_{\sigma,M}^{(8)}\), and
\(T_{\sigma,M}^{(8)}\).  Only after these constants have been fixed may the
observed KL value be inserted into
\(\mathfrak D_t^{(6)}(\nu)\).  Thus the theorem is a
certificate-first stability estimate, not a one-time KL stability estimate.
\end{minipage}}
\end{center}

\begin{theorem}[Conditional certified-shell finite-time sixth-defect estimate]%
\label{thm:finite-time-poisson-entropy-stability}
Fix \(\sigma>0\).  Assume \(M\ge\sigma^7\); otherwise
\(\mathcal S_{\sigma,M}\) is empty.  Put
\[
\widehat M:=M/\sigma^7\ge1 .
\]
The theorem is conditional on the nonempty variance-and-seventh-moment shell
\[
\mathcal S_{\sigma,M}:=
\left\{\nu:\ \mu_2(\nu)=\sigma^2,\quad
\EE_\nu|\gamma-\bar\gamma|^7\le M\right\}.
\]
For \(\nu\in\mathcal S_{\sigma,M}\), let
\[
X=(\gamma-\bar\gamma)/\sigma .
\]
Then \(\EE X^2=1\), \(\EE|X|^7\le \widehat M\), and all shell constants
are functions only of \(\widehat M\), with the dimensional powers of
\(\sigma\) restored in \eqref{eq:main-controlled-shell-c8}.  Thus the theorem
is first a variance-one statement for \(X=(\gamma-\bar\gamma)/\sigma\) and
the dimensionless shell radius \(\widehat M=M/\sigma^7\); the conclusion below
is then rescaled by the powers of \(\sigma\) displayed in
\eqref{eq:main-controlled-shell-c8}.
The condition \(M\ge\sigma^7\) is necessary for nonemptiness, since
\(\EE|\gamma-\bar\gamma|^7\ge(\EE|\gamma-\bar\gamma|^2)^{7/2}
=\sigma^7\), and it is attained by the two-point law
\(\frac12(\delta_{\bar\gamma-\sigma}+\delta_{\bar\gamma+\sigma})\).
Equivalently, in the normalized variable \(X\), the shell is
\[
\EE X=0,\qquad \EE X^2=1,\qquad \EE|X|^7\le\widehat M .
\]
The certificate hypothesis above is part of the theorem.  Let
\(C(u),B(u),T(u)\) be the explicit finite normalized shell functions recorded
in Appendix~\ref{app:finite-time-defect-estimates}; they depend only on the
dimensionless radius \(u\ge1\) and are fixed before any KL value is inserted.
The dimensional constants used below are exactly the scale restores
\begin{equation}\label{eq:main-controlled-shell-c8}
C_{\sigma,M}^{(8)}:=\sigma^8 C(M/\sigma^7),\qquad
B_{\sigma,M}^{(8)}:=\sigma^9 B(M/\sigma^7),\qquad
T_{\sigma,M}^{(8)}:=\sigma T(M/\sigma^7).
\end{equation}
Thus the certificate is the scale \(\sigma\) and the dimensionless shell
radius \(\widehat M\), equivalently \((\sigma,M)\), together with the three
normalized functions \(C,B,T\).  This certificate is fixed before any observed
KL value is inserted; the theorem does not certify closeness from the measured
value alone.  Appendix~\ref{app:finite-time-defect-estimates} gives the exact
constant formulas obtained by applying
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} to the variance-one law
of \(X\).
Let \(\nu\in\mathcal S_{\sigma,M}\).  Put
\[
h_t:=P_t*\nu,\qquad g_t(x):=P_t(x-\bar\gamma),
\]
let \(\nu_c\) be the centred law of \(\gamma-\bar\gamma\), and let
\(\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma)\).  Let
\(A_6(\nu)\) be the sixth-order entropy coefficient identified by
Theorem~\ref{thm:poisson-kl-even-orders} with \(N=3\), and set
\[
\Delta_6(\nu)
:=-\frac{7}{64}\sigma^6-A_6(\nu).
\]
Also set
\[
\tau:=t/\sigma,\qquad
\widehat A_6(\nu):=\sigma^{-6}A_6(\nu),\qquad
\widehat\Delta_6(\nu):=\sigma^{-6}\Delta_6(\nu).
\]
Define the finite-time sixth-order entropy statistic
\begin{equation}\label{eq:main-finite-time-sixth-deficit}
\mathfrak D_t^{(6)}(\nu)
:=
t^6\left(
\frac{\sigma^4}{8t^4}
-\frac{7\sigma^6}{64t^6}
-D_{\mathrm{KL}}(h_t\|g_t)
\right).
\end{equation}
Equivalently, the dimensionless statistic is
\[
\widehat{\mathfrak D}_{\tau}^{(6)}(\nu)
:=\sigma^{-6}\mathfrak D_{\sigma\tau}^{(6)}(\nu)
=
\tau^6\left(
\frac{1}{8\tau^4}
-\frac{7}{64\tau^6}
-D_{\mathrm{KL}}(h_{\sigma\tau}\|g_{\sigma\tau})
\right).
\]
For every \(t\ge T_{\sigma,M}^{(8)}\), the certified shell, independently of
the observed KL value, supplies numbers
\(A_8(\nu)\) and \(E_\nu(t)\) with
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+E_\nu(t),
\qquad
|A_8(\nu)|\le C_{\sigma,M}^{(8)},\quad
|E_\nu(t)|\le B_{\sigma,M}^{(8)}t^{-9}.
\]
Equivalently, writing
\(\widehat A_8(\nu):=\sigma^{-8}A_8(\nu)\) and
\(\widehat E_\nu(\tau):=E_\nu(\sigma\tau)\), the normalized bounds are
\[
|\widehat A_8(\nu)|\le C(\widehat M),\qquad
|\widehat E_\nu(\tau)|\le
B(\widehat M)\tau^{-9},
\qquad \tau\ge T(\widehat M) .
\]
The statistic is governed first by the exact certified-shell identities
\begin{equation}\label{eq:main-certified-shell-sixth-identity}
\mathfrak D_t^{(6)}(\nu)
=\Delta_6(\nu)-A_8(\nu)t^{-2}-t^6E_\nu(t),
\qquad
\widehat{\mathfrak D}_{\tau}^{(6)}(\nu)
=\widehat\Delta_6(\nu)-\widehat A_8(\nu)\tau^{-2}
-\tau^6\widehat E_\nu(\tau),
\end{equation}
and only then by the bound
\begin{equation}\label{eq:main-finite-time-defect-estimate}
0\le\Delta_6(\nu)
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+C_{\sigma,M}^{(8)}t^{-2}
+B_{\sigma,M}^{(8)}t^{-3}.
\end{equation}
Equivalently,
\[
0\le\widehat\Delta_6(\nu)
\le
\bigl|\widehat{\mathfrak D}_{\tau}^{(6)}(\nu)\bigr|
+C(\widehat M)\tau^{-2}
+B(\widehat M)\tau^{-3}.
\]
Consequently, for every \(t\ge T_{\sigma,M}^{(8)}\),
\begin{equation}\label{eq:main-finite-time-w2-stability}
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\mathcal A_t^{1/4}
+8\,\sigma^{-2}\mathcal A_t^{1/2},
\end{equation}
where
\[
\mathcal A_t
:=
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+C_{\sigma,M}^{(8)}t^{-2}
+B_{\sigma,M}^{(8)}t^{-3}.
\]
In normalized variables this is the same estimate as
\[
\frac1\sigma W_2(\nu_c,\rho_\sigma)
\le
4\,\widehat{\mathcal A}_{\tau}^{1/4}
+8\,\widehat{\mathcal A}_{\tau}^{1/2},
\qquad
\widehat{\mathcal A}_{\tau}:=
\bigl|\widehat{\mathfrak D}_{\tau}^{(6)}(\nu)\bigr|
+C(\widehat M)\tau^{-2}
+B(\widehat M)\tau^{-3}.
\]

In particular, if
\[
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
\le \varepsilon,
\qquad
C_{\sigma,M}^{(8)}t^{-2}\le\varepsilon,
\qquad
B_{\sigma,M}^{(8)}t^{-3}\le\varepsilon,
\]
then
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}(3\varepsilon)^{1/4}
+8\,\sigma^{-2}(3\varepsilon)^{1/2}.
\]
\end{theorem}

\begin{remark}[Certified shell data]
The observable inputs for \(\mathfrak D_t^{(6)}(\nu)\) are
\(D_{\mathrm{KL}}(h_t\|g_t)\), \(t\), and \(\sigma^2\).  They define only the
finite-time statistic.  The shell constants
\(C_{\sigma,M}^{(8)}\), \(B_{\sigma,M}^{(8)}\), and
\(T_{\sigma,M}^{(8)}\) are the finite functions of the
variance-and-seventh-moment shell defined in
Appendix~\ref{app:finite-time-defect-estimates}
before the KL value at time \(t\) is inserted into
\(\mathfrak D_t^{(6)}(\nu)\).  The theorem is therefore not a
one-observation stability theorem: the measured KL value is used only after
the controlled shell \((\sigma,M)\) has certified the possible sizes of
\(A_8(\nu)\) and \(E_\nu(t)\).
\end{remark}

\begin{proof}
Let \(\widehat\nu\) be the law of
\[
X=\frac{\gamma-\bar\gamma}{\sigma}.
\]
Then \(\widehat\nu\) has mean zero, variance one, and
\(\EE_{\widehat\nu}|X|^7\le\widehat M\).  Under the change of variables
\(x=\bar\gamma+\sigma y\), the Cauchy kernels scale as
\[
P_{\sigma\tau}(x-\gamma)\,\dd x
=P_\tau(y-X)\,\dd y,\qquad
P_{\sigma\tau}(x-\bar\gamma)\,\dd x=P_\tau(y)\,\dd y.
\]
Hence
\[
D_{\mathrm{KL}}(P_{\sigma\tau}*\nu\|P_{\sigma\tau}(\cdot-\bar\gamma))
=D_{\mathrm{KL}}(P_\tau*\widehat\nu\|P_\tau).
\]
Applying Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} to
\(\widehat\nu\), and using
\(|\EE_{\widehat\nu}X^n|\le\widehat M^{n/7}\) for \(3\le n\le6\), gives
for every \(\tau\ge T(\widehat M)\)
\[
D_{\mathrm{KL}}(P_\tau*\widehat\nu\|P_\tau)
=\frac{1}{8\tau^4}
+\frac{\widehat A_6(\nu)}{\tau^6}
+\frac{\widehat A_8(\nu)}{\tau^8}
+\widehat E_\nu(\tau),
\qquad
|\widehat E_\nu(\tau)|
\le B(\widehat M)\tau^{-9}.
\]
Here \(B(\widehat M)\) and \(T(\widehat M)\) are the normalized shell
functions displayed in Appendix~\ref{app:finite-time-defect-estimates}.  The
explicit eighth coefficient for \(\widehat\nu\) is
\[
\widehat A_8(\nu)
=\frac{1}{256}\left(
3-12\widehat\mu_4
+9\widehat\mu_3^2+12\widehat\mu_6
-30\widehat\mu_3\widehat\mu_5+20\widehat\mu_4^2
\right),
\]
where \(\widehat\mu_j=\EE_{\widehat\nu}X^j=\sigma^{-j}\mu_j\).  Lyapunov's
inequality gives \(|\widehat\mu_j|\le\widehat M^{j/7}\) for
\(3\le j\le6\), so
\[
|\widehat A_8(\nu)|\le C(\widehat M).
\]
Restoring \(t=\sigma\tau\) gives
\[
A_6(\nu)=\sigma^6\widehat A_6(\nu),\qquad
A_8(\nu)=\sigma^8\widehat A_8(\nu),\qquad
E_\nu(t)=\widehat E_\nu(t/\sigma),
\]
and therefore the expansion and bounds stated in dimensional variables:
\[
|A_8(\nu)|\le C_{\sigma,M}^{(8)},\qquad
|E_\nu(t)|\le B_{\sigma,M}^{(8)}t^{-9},\qquad
t\ge T_{\sigma,M}^{(8)}.
\]
Substitution into \eqref{eq:main-finite-time-sixth-deficit} gives the
identity \eqref{eq:main-certified-shell-sixth-identity}.  Combining this
identity with the certified bounds for \(A_8(\nu)\) and \(E_\nu(t)\) yields
\[
0\le \Delta_6(\nu)
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+C_{\sigma,M}^{(8)}t^{-2}
+B_{\sigma,M}^{(8)}t^{-3},
\]
where the non-negativity is Corollary~\ref{cor:coefficient-defect-coupling}.
This proves \eqref{eq:main-finite-time-defect-estimate}.  The same corollary
also gives
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\]
Inserting the preceding defect bound into the coefficient-defect coupling
proves
\eqref{eq:main-finite-time-w2-stability}, and the
\(\varepsilon\)-form is immediate.
\end{proof}

The algebra behind the certificate finite-time form is the identity
\begin{equation}\label{eq:oracle-sixth-deficit-identity-before-theorem}
\mathfrak D_t^{(6)}(\nu)
=\Delta_6(\nu)-A_8(\nu)t^{-2}-t^6E_\nu(t).
\end{equation}

\begin{center}
\fbox{\begin{minipage}{0.91\textwidth}
\textbf{Certified eighth-order data.}
The constants \(C\), \(B\), and \(T\) are fixed before the value of
\(D_{\mathrm{KL}}(h_t\|g_t)\) entering
\(\mathfrak D_t^{(6)}(\nu)\) is observed.  For all \(t\ge T\), the certified
data assert
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+E(t),
\qquad
|A_8(\nu)|\le C,\qquad |E(t)|\le Bt^{-9}.
\]
This is the certificate used below; the single KL value supplies only
\(\mathfrak D_t^{(6)}(\nu)\), not the bounds \(C,B,T\).
\end{minipage}}
\end{center}

\begin{theorem}[Certified finite-time bound from eighth-order expansion data]%
\label{thm:oracle-finite-time-poisson-entropy-stability}
The quantities \(C\ge0\), \(B\ge0\), and \(T>0\) are oracle or shell
certificate inputs supplied before the measured KL value is used.
Let $\nu$ be a probability measure on~$\RR$ with mean~$\bar\gamma$, variance
\(\sigma^2>0\), and finite centred fourth absolute moment.  Put
\[
h_t:=P_t*\nu,\qquad g_t(x):=P_t(x-\bar\gamma),
\]
let \(\nu_c\) be the centred law of \(\gamma-\bar\gamma\), and let
\(\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma)\).  Let
\(A_6(\nu)\) be the sixth-order entropy coefficient identified by
Theorem~\ref{thm:poisson-kl-even-orders} with \(N=3\), and set
\[
\Delta_6(\nu)
:=-\frac{7}{64}\sigma^6-A_6(\nu).
\]
Assume that there are a number \(A_8(\nu)\in\RR\) and an error function
\(E(t)\) such that the certified data satisfy
\begin{equation}\label{eq:main-oracle-eighth-expansion}
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+E(t),
\qquad
|A_8(\nu)|\le C,\qquad
|E(t)|\le Bt^{-9}
\end{equation}
for every \(t\ge T\).  Define \(\mathfrak D_t^{(6)}(\nu)\) by
\eqref{eq:main-finite-time-sixth-deficit}.  Then, for every \(t\ge T\),
\begin{equation}\label{eq:main-oracle-finite-time-defect-estimate}
0\le \Delta_6(\nu)
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|+Ct^{-2}+Bt^{-3}.
\end{equation}
Consequently, for every \(t\ge T\),
\begin{equation}\label{eq:main-oracle-finite-time-w2-stability}
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\mathcal A_{t,C,B}^{1/4}
+8\,\sigma^{-2}\mathcal A_{t,C,B}^{1/2},
\end{equation}
where
\[
\mathcal A_{t,C,B}
:=
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|+Ct^{-2}+Bt^{-3}.
\]

In particular, if
\[
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
\le \varepsilon,
\qquad
Ct^{-2}\le\varepsilon,
\qquad
Bt^{-3}\le\varepsilon,
\]
then
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}(3\varepsilon)^{1/4}
+8\,\sigma^{-2}(3\varepsilon)^{1/2}.
\]

\end{theorem}

\begin{remark}[Certificate scope]
The theorem is a certificate theorem, not a stability statement from one KL
number alone.  In applications justified by
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}, a fixed law may supply
finite constants \(C\ge |A_8(\nu)|\), \(B\), and \(T\).  On the controlled
shell of Theorem~\ref{thm:finite-time-poisson-entropy-stability}, the
certificate is the special choice
\[
C=C_{\sigma,M}^{(8)},\qquad
B=B_{\sigma,M}^{(8)},\qquad
T=T_{\sigma,M}^{(8)} .
\]
All three constants are fixed before the measured KL value supplies
\(\mathfrak D_t^{(6)}(\nu)\).
\end{remark}

\begin{proof}
Substitution of \eqref{eq:main-oracle-eighth-expansion} into
\eqref{eq:main-finite-time-sixth-deficit} gives, for \(t\ge T\),
\[
\begin{aligned}
\mathfrak D_t^{(6)}(\nu)
&=-\frac{7}{64}\sigma^6-A_6(\nu)
-\frac{A_8(\nu)}{t^2}-t^6E(t)\\
&=\Delta_6(\nu)-\frac{A_8(\nu)}{t^2}-t^6E(t).
\end{aligned}
\]
Hence
\[
0\le \Delta_6(\nu)
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+|A_8(\nu)|t^{-2}+Bt^{-3}
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|+Ct^{-2}+Bt^{-3},
\]
where the non-negativity is Corollary~\ref{cor:coefficient-defect-coupling}.
This proves \eqref{eq:main-oracle-finite-time-defect-estimate}.  The same
corollary also gives
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\]
Inserting the preceding bound for \(\Delta_6(\nu)\) proves
\eqref{eq:main-oracle-finite-time-w2-stability}, and the
\(\varepsilon\)-form is immediate.
\end{proof}

\begin{corollary}[Controlled-shell specialization of the certificate bound]\label{cor:controlled-shell-finite-time-oracle}
Under the hypotheses and notation of
Theorem~\ref{thm:finite-time-poisson-entropy-stability}, for every
\(t\ge T_{\sigma,M}^{(8)}\),
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\mathcal A_{t,\sigma,M}^{1/4}
+8\,\sigma^{-2}\mathcal A_{t,\sigma,M}^{1/2},
\]
where
\[
\mathcal A_{t,\sigma,M}:=
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+C_{\sigma,M}^{(8)}t^{-2}+B_{\sigma,M}^{(8)}t^{-3}.
\]
Here the shell constants \(C_{\sigma,M}^{(8)}\),
\(B_{\sigma,M}^{(8)}\), and \(T_{\sigma,M}^{(8)}\) are the explicit functions
of \((\sigma,M)\) displayed in
Theorem~\ref{thm:finite-time-poisson-entropy-stability}; they are fixed
before the KL value entering \(\mathfrak D_t^{(6)}(\nu)\) is used.  The
right-hand side is exactly the certificate theorem with
\[
C=C_{\sigma,M}^{(8)},\qquad
B=B_{\sigma,M}^{(8)},\qquad
T=T_{\sigma,M}^{(8)},
\]
and therefore uses the measured KL statistic together with controlled-shell
certificate constants, not an observed value of \(A_8(\nu)\).
\end{corollary}

\begin{proof}
This is exactly \eqref{eq:main-finite-time-w2-stability} with the displayed
definition of \(\mathcal A_t\).
\end{proof}

Appendix~\ref{app:finite-time-defect-estimates} records the constant-regime
bookkeeping and checks that the explicit shell formulas used here dominate
the fixed-law constants uniformly on the controlled moment shell.
