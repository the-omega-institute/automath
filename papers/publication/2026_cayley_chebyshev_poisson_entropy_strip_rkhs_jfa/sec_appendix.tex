% sec_appendix.tex -- Weighted trigonometric integrals
\section{Endpoint-normalized Haar-pullback uniqueness}%
\label{app:haar-pullback-uniqueness}

The following elementary statement fixes the chart normalization behind
\eqref{eq:haar-pullback}.  The entropy expansion uses only the pullback
identity itself; the uniqueness assertion is included here to remove any
ambiguity about the endpoint convention.

\begin{proposition}[Endpoint-normalized Haar-pullback uniqueness]%
\label{prop:haar-pullback-uniqueness}
Let $\varphi:\overline\RR\to\TT$ be an orientation-preserving homeomorphism
such that $\varphi|_{\RR}$ is of class $C^{1}$,
$\varphi(0)=1$, and $\varphi(\infty)=-1$.  Then the following are
equivalent:
\begin{enumerate}[label=(\roman*)]
\item $\varphi^{*}m_{\TT}=\omega$ on~$\RR$;
\item if $\varphi(x)=e^{\ii\alpha(x)}$ with
      $\alpha:\RR\to(-\pi,\pi)$ increasing, then
      $\alpha'(x)=2/(1+x^{2})$ for every $x\in\RR$;
\item $\varphi=\iota$.
\end{enumerate}
If the orientation assumption is removed, the same conclusion holds up to
complex conjugation: $\varphi=\iota$ or $\varphi=\overline{\iota}$.
\end{proposition}

\begin{proof}
For any orientation-preserving $\varphi$ satisfying the endpoint
normalization, there is a unique increasing $C^{1}$ lift
$\alpha:\RR\to(-\pi,\pi)$ with $\varphi(x)=e^{\ii\alpha(x)}$ and
$\alpha(0)=0$.  Pullback of normalized arc length is
\[
\varphi^{*}m_{\TT}=\frac{1}{2\pi}\alpha'(x)\dd x .
\]
Hence $\varphi^{*}m_{\TT}=\omega$ is equivalent to
\(\alpha'(x)=2/(1+x^{2})\).  Integrating from \(0\) to \(x\) gives
\(\alpha(x)=2\arctan x\), hence \(\varphi=\iota\).  Conversely, for
\(\iota\), Proposition~\ref{prop:phase-derivative} gives
\[
\iota^{*}m_{\TT}
=\frac{1}{2\pi}\vartheta'(x)\dd x
=\frac{1}{\pi(1+x^{2})}\dd x
=\omega(\dd x).
\]
If orientation is reversed, the lift is decreasing and the same argument
applied to \(\overline{\varphi}\) gives the stated conjugate alternative.
\end{proof}

\section{Related work and theorem map}%
\label{app:related-work-scope}

\noindent\textbf{Theorem-level comparison.}
The following table is the comparison used in the main text.  Its object is not
to list all surrounding literature, but to keep the output of each present
theorem separate from the closest theorem class that might otherwise be
mistaken for it.  The point of comparison is the theorem output: fixed-law
Poisson KL coefficients and the moving-quotient KL tail are different from
heat-kernel profile terms, normalized-sum entropy limits, weighted Cauchy
functional inequalities, parametric Cauchy divergence formulae, and general
Hardy--Stein disintegration identities.

\begingroup
\scriptsize
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{p{0.14\textwidth}p{0.20\textwidth}p{0.24\textwidth}p{0.24\textwidth}}
\hline
Object & Prior theorem & Hypothesis/output & Difference\\
\hline
\endfirsthead
\hline
Object & Prior theorem & Hypothesis/output & Difference\\
\hline
\endhead
Theorems~\ref{thm:multidimensional-density-quotient}--%
\ref{thm:multidim-leading-positive-defect}: multidimensional fixed-law Poisson
quotient and leading KL coefficient
& Anker--Papageorgiou--Zhang on large-time heat profiles on symmetric spaces
\cite{AnkerPapageorgiouZhang2023HeatSymmetricSpaces}, and
Crismale--De~Luca--Kubin--Ninno--Ponsiglione on fractional-flow asymptotics
\cite{CrismaleDeLucaKubinNinnoPonsiglione2023}.
& Prior output: evolved heat or fractional-heat profile, limit flow, or distance
from a fundamental solution, under \(L^1\), finite-moment, or variational-flow
hypotheses.  Present hypothesis/output: for a fixed law with finite centred
\(L\)th absolute moment, \(L\ge\max\{2,d+1\}\),
\((P_t^d*\nu)(\bar\gamma+ty)/P_t^d(ty)-1
=\sum_{2\le n\le L}D_n(y)t^{-n}+\rho_L(t,y)\), then
\(D_{\rm KL}(P_t^d*\nu\,\|\,P_t^d(\cdot-\bar\gamma))
=\sum_{4\le m\le L+2}\mathcal A_m^{(d)}(\nu)t^{-m}+o(t^{-(L+2)})\),
with leading positive decomposition
\(\mathcal A_4^{(d)}
=c_d^{\rm iso}(\operatorname{tr}\Sigma)^2
+c_d^{\rm tr}\|\Sigma_0\|_{\rm HS}^2\).
& The present theorem transfers a fixed-input Poisson quotient through the
nonlinear KL integrand and extracts covariance-level entropy coefficients.
The prior profile theorems compute density/profile asymptotics, not
fixed-law Poisson KL coefficient jets.
\\
\hline
Theorem~\ref{thm:poisson-kl-even-orders}: universal fixed-law Cauchy KL
coefficient polynomial
& Bobkov--G{\"o}tze on R\'enyi-divergence CLT normal approximation
\cite{BobkovGotzeRenyi2025} and on the Esscher-transform CLT
\cite{BobkovGotzeEsscher2025}; stable-law entropy comparators include
Bobkov--Chistyakov--G{\"o}tze~\cite{BobkovChistyakovGotze2013,
BobkovChistyakovGotzeStableFisher2014}.
& Prior output: CLT or stable-law normal approximation for a changing law, such
as \(n^{-1/2}(X_1+\cdots+X_n)\), measured by entropy, Fisher information,
R\'enyi divergence, or an Esscher criterion.  Present hypothesis/output: the
law \(\nu\) is fixed, the comparison profile is only dilated, the finite
centred \((2N-2)\)th absolute moment is assumed, and
\(H(t)=D_{\rm KL}(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma))
=\int \Phi(\delta_t)\,\dd\omega\) has the universal moment-polynomial jet
\(H(t)=\sum_{m=2}^{N}A_{2m}(\nu)t^{-2m}+o(t^{-2N})\).  Each
\(A_m\) is the finite sum over \(2\le r\le\lfloor m/2\rfloor\) and
\(n_1+\cdots+n_r=m\) of the Taylor coefficient, the centred-moment product,
and the Cauchy mode integral displayed in
\eqref{eq:coefficient-identification-transfer-expansion}.
& The prior theorems give convergence or normal/stable approximation criteria
for changing sequences of laws.  The present theorem computes fixed-law
Cauchy/Poisson KL coefficients for one input distribution.
\\
\hline
Theorems~\ref{thm:poisson-kl-eighth} and
\ref{thm:poisson-kl-eighth-seventh-remainder}: sixth/eighth fixed-law KL
coefficient evaluation
& Parametric Cauchy information formulae
\cite{ChyzakNielsen2019CauchyKL,NielsenOkamura2023Cauchy,
BouhlelRousseau2022MultivariateCauchyKL,Verdu2023Cauchy}, together with
finite-moment profile coefficient calculations for heat/fractional-heat
kernels.
& Prior output: a closed-form divergence inside a finite-dimensional Cauchy
family, or linear profile coefficients multiplying kernel derivatives.
Present hypothesis/output: \(P_t*\nu\) is not usually Cauchy; under finite
centred fourth, sixth, and then seventh absolute moments,
\(H(t)=\sigma^4/(8t^4)+A_6(\nu)t^{-6}+A_8(\nu)t^{-8}
+O(t^{-9})\), with
\(A_6=(\sigma^6+6\mu_3^2-8\sigma^2\mu_4)/64\) and
\(A_8=(3\sigma^8-12\sigma^4\mu_4+9\sigma^2\mu_3^2
+12\sigma^2\mu_6-30\mu_3\mu_5+20\mu_4^2)/256\).
& These coefficients are zero-mode Laurent evaluations of products created by
\(\Phi\).  They are neither profile coefficients
\(M_\alpha\partial^\alpha p_t\) nor closed-form divergences between two
Cauchy-family parameters.
\\
\hline
Corollary~\ref{cor:coefficient-defect-coupling},
Corollary~\ref{thm:poisson-defect-amplification},
Appendix Lemma~\ref{lem:controlled-shell-conditional-certificate}, and
Remark~\ref{rem:fixed-law-finite-time-certificate}: coefficient-level
defect/certificate bounds
& Functional-inequality stability and transport-functional inequality
theorems: Chen--Lu--Tang on sharp log-Sobolev/Moser--Onofri
stability~\cite{ChenLuTang2023LogSobolevStability},
Brigati--Dolbeault--Simonov on Gaussian log-Sobolev stability
\cite{BrigatiDolbeaultSimonov2024LogSobolev}, Carlen on log-HLS stability
\cite{Carlen2025LogHLS}, and L{\'o}pez-Rivera--Shenfeld on the Poisson
transport map~\cite{LopezRiveraShenfeld2025PoissonTransport}.
& Prior output: deficits in functional inequalities, distances from extremizer
manifolds, or transport maps from a Poisson point process to ultra-log-concave
measures.  Present hypothesis/output: after the \(A_6\) entropy coefficient is
identified, the coefficient defect \(\Delta_6(\nu)\) and the optional
finite-time statistic
\(D_t^{(6),{\rm obs}}
=t^6(\sigma^4/(8t^4)-7\sigma^6/(64t^6)-K_t^{\rm obs})\)
whose error radius satisfies
\(|K_t^{\rm obs}-H(t)|\le\eta_t\) obey
\(0\le\Delta_6(\nu)
\le |D_t^{(6),{\rm obs}}|+t^6\eta_t+Ct^{-2}+Bt^{-3}\),
and the resulting two-point \(W_2\) bound after inserting the two-point
defect algebra.
& The present certificate is coefficient bookkeeping from a fixed-law KL jet.
It is not a stability theorem for log-Sobolev,
Moser--Onofri, log-HLS, weighted Poincar\'e, weighted log-Sobolev, or
ultra-log-concave transport inequalities.
\\
\hline
Theorem~\ref{thm:poisson-kl-doob-tail} and
Proposition~\ref{prop:dissipation-tail-coefficient-expansion}: moving Poisson
Doob quotient KL tail
& Nonlocal Hardy--Stein and Sobolev--Bregman identity theorems, especially
the polarized Hardy--Stein identity of
Bogdan--Gutowski--Pietruska-Pa{\l}uba
\cite{BogdanGutowskiPietruskaPaluba2025PolarizedHardyStein}.
& Prior output: general polarized energy disintegration for functions or pairs
of functions associated with a Markov semigroup or jump form, often for
\(L^p\)-type Bregman quantities.  Present hypothesis/output: for the moving
Poisson Doob quotient
\(u_t(x)=(P_t*\nu)(x)/P_t(x-\bar\gamma)\)
for the one-dimensional Cauchy generator, under finite centred second moment,
the quotient-domain cutoff passage proves \(H'(t)=-I_\nu(t)\) and hence
\(D_{\rm KL}(P_t*\nu\,\|\,P_t(\cdot-\bar\gamma))
=\int_t^\infty I_\nu(s)\,\dd s\),
where \(I_\nu\) is the explicit two-point KL Bregman dissipation, followed by
the coefficient-tail expansion of the same \(A_{2m}(\nu)\).
& The prior theorem supplies the analytic Hardy--Stein/Bregman mechanism.  The
new output is the Cauchy-generator moving-quotient KL tail and its fixed-law
Poisson coefficient tail, not another general Hardy--Stein theorem.
\\
\hline
\end{longtable}
\endgroup

Fixed-law Poisson entropy jets expand one scalar matched KL functional as the
Poisson scale grows.  Normalized-sum entropy theorems change the law with the
number of summands; flow asymptotics describe large-time profiles or evolution
limits; transport and functional-inequality stability compare near extremizers
or transport structures; and parametric Cauchy divergence formulae give closed
forms within Cauchy families.  In the present problem \(P_t*\nu\) is generally
not Cauchy, and \(\nu\) is not rescaled as a summand law.

The distinction from heat and fractional-heat profile expansions is more
specific.  The papers of Duoandikoetxea--Zuazua, Escobedo--Zuazua,
Karch--Schonbek, and Ishige--Kawakami--Michihisa
\cite{DuoandikoetxeaZuazua1992,EscobedoZuazua1991,KarchSchonbek2001,
IshigeKawakami2012,IshigeKawakamiMichihisa2017,
IshigeKawakamiMichihisa2022RefinedFractional} give finite-moment asymptotic
profiles for heat, viscous, fractional, or stable-kernel evolutions.  In that
lineage the coefficients multiply derivatives of the relevant kernel or
profile and describe the evolved density or solution.  The density-level
analogue in this paper is the quotient estimate
\begin{equation}\label{eq:appendix-profile-expansion-comparison}
u(t,x)=
\sum_{|\alpha|\le K}M_\alpha(\varphi)\,\partial^\alpha p_t(x)
+R_K(t,x),
\qquad
\frac{P_t*\nu}{P_t(\cdot-\bar\gamma)}-1
=\sum_{j=2}^{L}D_j(y)t^{-j}+\rho_L(t,y).
\end{equation}
The article isolates this quotient layer before the entropy theorem.  The main coefficients
\(A_{2m}(\nu)\), however, are obtained only after inserting that quotient into
the nonlinear Bregman integrand
\begin{equation}\label{eq:appendix-kl-transfer-comparison}
D_{\rm KL}(P_t*\nu\|P_t(\cdot-\bar\gamma))
=
\int
\Phi\!\left(\sum_{j=2}^{L}D_j(y)t^{-j}+\rho_L(t,y)\right)\dd\Omega(y),
\qquad
\Phi(s)=(1+s)\log(1+s)-s,
\end{equation}
and retaining all products of modes of total order \(2m\).  Thus the final
coefficients are nonlinear KL coefficients, not density-profile coefficients.
This is why the coefficient-evaluation threshold in
Theorem~\ref{thm:poisson-kl-even-orders} is stated for the displayed universal
moment-polynomial coefficient of the scalar entropy functional.

The distinction from formal Taylor extraction is also theorem-level.  Expanding
the quotient \(R_\varepsilon\) or its multivariable analogue gives candidate
modes only.  The entropy theorems prove that those modes may be averaged under
finite moments, that the quotient remainder is small in the weighted spaces
needed for KL, that centering removes the odd obstruction, and that the
Bregman Taylor expansion of \((1+u)\log(1+u)-u\) has an integrable tail with
the stated \(o(t^{-2N})\) or finite-time remainder.  The explicit Laurent
calculus in dimension one is therefore the coefficient-evaluation layer of an
analytic finite-moment Poisson theorem, not the theorem by itself.

Entropic CLT and stable-law entropy asymptotics include
\cite{Barron1986,CarlenSoffer1991,BallBartheNaor2003,ArtsteinBallBartheNaor2004,JohnsonBarron2004,MadimanBarron2007,BobkovChistyakovGotze2013,BobkovChistyakovGotzeFisher2014,BobkovChistyakovGotzeEdgeworth2013,BobkovChistyakovGotzeBerryEsseen2014,BobkovGotzeRenyi2025,BobkovGotzeEsscher2025}.
Heat, fractional-heat, and Poisson-kernel large-time asymptotics include
\cite{DuoandikoetxeaZuazua1992,EscobedoZuazua1991,KarchSchonbek2001,IshigeKawakami2012,IshigeKawakamiMichihisa2017,IshigeKawakamiMichihisa2022RefinedFractional,AnkerPapageorgiouZhang2023HeatSymmetricSpaces,Papageorgiou2024FractionalLargeTime,CrismaleDeLucaKubinNinnoPonsiglione2023,CaffarelliSilvestre2007,StingaTorrea2010,Kwasnicki2017}.
Entropy/transport stability, heavy-tail functional inequalities, and
entropy-dissipation references are deliberately split by role.  The
functional-inequality stability and transport references
\cite{BobkovGotzeTransport1999,OttoVillani2000,BolleyVillani2005,
ChenLuTang2024HLSStability,BrigatiDolbeaultSimonov2024LogSobolev,
Carlen2025LogHLS,GozlanHerryPeccati2021PointMeasureTransport,
LopezRiveraShenfeld2025PoissonTransport,
ErbarHuesmannJalowyMuller2025StationaryPointTransport}
are background only: they concern transport inequalities, extremizer
stability, or stability of log-Sobolev/Moser--Onofri/log-HLS type
inequalities, not coefficient-level fixed-law KL asymptotics.  Here
Chen--Lu--Tang's HLS stability paper is used as a non-JFA background
reference; their JFA log-Sobolev/Moser--Onofri paper is listed in the
introduction's theorem-comparison table.

The heavy-tail Cauchy functional-inequality references
\cite{BobkovLedoux2009WeightedCauchyPoincare,
CattiauxGozlanGuillinRoberto2010HeavyTailFunctionalIneq,
CattiauxGuillinWu2011WeightedLogSobolev,
Huguet2024GeneralisedCauchyPoincare,
Huguet2026GeneralisedCauchyLogSobolev}
are also background only.  They explain why Cauchy and more general
heavy-tailed measures naturally require weighted Poincar\'e, weighted
logarithmic Sobolev, or weak functional-inequality formulations.  Weighted
Poincar\'e and weighted log-Sobolev inequalities for generalized Cauchy
measures were developed by Bobkov--Ledoux and Cattiaux--Guillin--Wu, with
recent refinements by Huguet.  Those results control
functional-inequality deficits or concentration for heavy-tailed reference
measures.  They do not compute fixed-law Poisson KL coefficient jets.  No
theorem in this paper proves, uses, or improves such a weighted functional
inequality.
The stable-law entropy-dissipation references
\cite{BobkovChistyakovGotze2013,
BobkovChistyakovGotzeStableFisher2014,
JohnsonStableDeBruijn2013,Toscani2015,Toscani2016,
Toscani2019StablePoincare}
are background-adjacent: they are adjacent in using entropy or Fisher-type
dissipation around stable laws, but their scaling problem is normalized-sum or
stable-density convergence, whereas the present law \(\nu\) is fixed and only
the Poisson comparison scale grows.

The genuinely adjacent sources for Section~\ref{sec:doob-phi-entropy} are
the nonlocal Hardy--Stein and Sobolev--Bregman identity papers
\cite{Stein1970SingularIntegrals,Bregman1967,
BanuelosBogdanLuks2016HardyStein,
BanuelosKim2019HardySteinNonsymmetric,
BogdanGrzywnyPietruskaPalubaRutkowski2023NonlinearNonlocalDouglas,
Gutowski2023HardySteinPureJump,
GutowskiKwasnicki2025BeurlingDenySobolevBregman,
BogdanGutowskiPietruskaPaluba2025PolarizedHardyStein}, because the KL tail
identity specializes their Bregman two-point dissipation mechanism to the
Cauchy generator with the moving Poisson Doob quotient.  The modern
Hardy--Stein cluster used here is cited through publisher DOI data and, where
available, arXiv identifiers in the bibliography: the
Bañuelos--Bogdan--Luks semigroup identity, the Bañuelos--Kim nonsymmetric
L\'evy-process identity, the nonlinear nonlocal Douglas identity, Gutowski's
pure-jump Dirichlet-form identity, the Beurling--Deny Sobolev--Bregman formula,
and the polarized Hardy--Stein identity.  The Markov-chain and
jump-gradient-flow entropy papers
\cite{Maas2011EntropyMarkov,ErbarMaas2012RicciMarkov,
Mielke2013EntropyMarkov,Erbar2014JumpEntropy}
are broader entropy-dissipation background rather than inputs to the proof.
In contrast, the principal shift-invariant-space and RKHS sampling references
are deliberately not part of this theorem map.  The entropy theorems use only
the stated moment hypotheses and the Laurent coefficient table is certified by
the finite constant-term arithmetic displayed in the text.
Cauchy information formulae include the survey of Verd{\'u}~\cite{Verdu2023Cauchy},
the Cauchy KL formulae of Chyzak--Nielsen~\cite{ChyzakNielsen2019CauchyKL},
and the Cauchy \(f\)-divergence formulae of
Nielsen--Okamura~\cite{NielsenOkamura2023Cauchy}.

The finite-time certificate uses the coefficient layers as inputs.  It is a
bookkeeping consequence of the sixth-defect identity after either controlled
shell bounds or fixed-law bounds have supplied estimates for the eighth
coefficient and the order-nine remainder before the KL statistic is evaluated.

\section{Weighted trigonometric integrals}\label{app:entropy-integrals}

We collect the weighted identities used in the Laurent representatives and
constant-term table of
Lemmas~\ref{lem:n4-normalized-laurent-representatives} and
\ref{lem:eighth-order-coefficient-arithmetic}.  With
\(y=\tan\theta\), each identity is a zero-Fourier-mode extraction in an
elementary trigonometric product.

\begin{lemma}[Weighted Cayley mode integrals]\label{lem:entropy-integrals}
Let $u_2,\dots,u_6$ be the mode functions defined by
\eqref{eq:cayley-first-modes} and \eqref{eq:cayley-next-modes}.  Then
\[
\int_{\RR}\frac{u_2(y)^2}{\pi(1+y^2)}\dd y=\frac14,
\qquad
\int_{\RR}\frac{u_3(y)^2}{\pi(1+y^2)}\dd y=\frac{3}{16},
\]
\[
\int_{\RR}\frac{u_2(y)u_4(y)}{\pi(1+y^2)}\dd y=-\frac18,
\qquad
\int_{\RR}\frac{u_2(y)^3}{\pi(1+y^2)}\dd y=-\frac{3}{32},
\]
\[
\int_{\RR}\frac{u_2(y)u_6(y)}{\pi(1+y^2)}\dd y=\frac{3}{64},
\qquad
\int_{\RR}\frac{u_3(y)u_5(y)}{\pi(1+y^2)}\dd y=-\frac{15}{128},
\]
\[
\int_{\RR}\frac{u_4(y)^2}{\pi(1+y^2)}\dd y=\frac{5}{32},
\qquad
\int_{\RR}\frac{u_2(y)^2u_4(y)}{\pi(1+y^2)}\dd y=\frac{3}{32},
\]
\[
\int_{\RR}\frac{u_2(y)u_3(y)^2}{\pi(1+y^2)}\dd y=-\frac{9}{128},
\qquad
\int_{\RR}\frac{u_2(y)^4}{\pi(1+y^2)}\dd y=\frac{9}{64}.
\]
\end{lemma}

\begin{proof}
Set $y=\tan\theta$ with $\theta\in(-\pi/2,\pi/2)$, so that
$\dd y/(1+y^2)=\dd\theta$, and write
\[
c_k(\theta):=\cos(2k\theta),
\qquad
s_k(\theta):=\sin(2k\theta).
\]
By Theorem~\ref{thm:cayley-chebyshev-mode},
\[
u_2(\tan\theta)=-\frac12(c_1+c_2),
\qquad
u_3(\tan\theta)=-\frac14(s_1+2s_2+s_3),
\]
\[
u_4(\tan\theta)=\frac18(c_1+3c_2+3c_3+c_4),
\qquad
u_5(\tan\theta)=\frac1{16}(s_1+4s_2+6s_3+4s_4+s_5),
\]
\[
u_6(\tan\theta)=-\frac1{32}(c_1+5c_2+10c_3+10c_4+5c_5+c_6).
\]
On $(-\pi/2,\pi/2)$ one has
\[
\int_{-\pi/2}^{\pi/2} c_k(\theta)^2\,\dd\theta
=\int_{-\pi/2}^{\pi/2} s_k(\theta)^2\,\dd\theta
=\frac{\pi}{2}
\qquad(k\ge 1),
\]
and mixed products with different parity or different frequency
integrate to zero.  Thus each identity follows by expanding the
corresponding trigonometric polynomial and retaining only the constant
term.  Equivalently, if $Q_n(e^{2\ii\theta})=u_n(\tan\theta)$, then each
integral is the zero Laurent coefficient $[z^0]$ of the indicated product;
the coefficient-level constant terms are listed in
Lemma~\ref{lem:eighth-order-coefficient-arithmetic}.
For instance,
\[
\int_{\RR}\frac{u_2(y)^2}{\pi(1+y^2)}\dd y
=\frac14\cdot\frac1\pi\int_{-\pi/2}^{\pi/2}(c_1+c_2)^2\,\dd\theta
=\frac14,
\]
and
\[
\int_{\RR}\frac{u_2(y)u_4(y)}{\pi(1+y^2)}\dd y
=-\frac1{16}\cdot\frac1\pi\int_{-\pi/2}^{\pi/2}
(c_1+c_2)(c_1+3c_2+3c_3+c_4)\,\dd\theta
=-\frac18.
\]
The remaining identities are accounted for by the same zero-Fourier-coefficient
computation.
\end{proof}

The coefficient-level use of these identities is consolidated in
Lemmas~\ref{lem:eighth-order-coefficient-arithmetic} and
\ref{lem:n4-entropy-coefficient-substitution}; the computations above give
the elementary trigonometric verification of the weighted mode integrals.  The
finite Laurent certificate in
Lemma~\ref{lem:eighth-order-coefficient-arithmetic} is intentionally
self-contained: recurrence
\eqref{eq:eighth-order-constant-term-recurrence} reconstructs each zero-sum
convolution row from the displayed all-sign coefficient vectors.  In
particular, the sign-sensitive rows \(I(3,5)=-15/128\) and
\(I(2,3,3)=-9/128\) are part of the displayed finite data and require no
external certificate.

\section{KL Doob quotient bounds}\label{app:doob-bregman-domain}

The general Sobolev--Bregman/Hardy--Stein identity is cited in
Section~\ref{sec:doob-phi-entropy}; it is not reproved here.  This appendix
records only the quotient-domain estimates for the Poisson comparison
quotient \(u_t=h_t/g_t\) that are used to apply that identity in the KL/Cauchy
case.

\begin{lemma}[KL quotient range and Bregman majorant]%
\label{lem:appendix-kl-quotient-domain-bounds}
Let \(\nu\) be a probability measure on~\(\RR\) with mean \(\bar\gamma\) and
finite centred second moment
\[
\mu_2:=\int_{\RR}(\gamma-\bar\gamma)^2\dd\nu(\gamma).
\]
Fix \(0<a<b<\infty\), and put
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),\qquad u_t=h_t/g_t .
\]
Then there are constants \(0<m_{a,b}\le M_{a,b}<\infty\) and
\(L_{a,b}<\infty\) such that, for all \(t\in[a,b]\) and \(x\in\RR\),
\[
0<m_{a,b}\le u_t(x)\le M_{a,b}<\infty,
\qquad
|\partial_xu_t(x)|\le L_{a,b}.
\]
For
\[
\Lambda(\alpha,\beta):=
\alpha\log\frac{\alpha}{\beta}-\alpha+\beta
\qquad(\alpha,\beta>0),
\]
there is \(C_{a,b,\nu}<\infty\) such that, for all \(t\in[a,b]\) and
\(x\ne y\),
\[
\frac{g_t(x)\Lambda(u_t(x),u_t(y))}{(x-y)^2}
\le
C_{a,b,\nu}g_t(x)\min\{1,|x-y|^{-2}\}.
\]
\end{lemma}

\begin{proof}
Write \(X_c=\gamma-\bar\gamma\), \(x=\bar\gamma+tz\), and
\[
R_\varepsilon(z):=\frac{1+z^2}{1+(z-\varepsilon)^2}.
\]
Then
\[
u_t(x)=
\int_{\RR}R_{X_c/t}\!\left(\frac{x-\bar\gamma}{t}\right)\dd\nu(\gamma).
\]
The elementary inequalities
\[
R_\varepsilon(z)\le2(1+\varepsilon^2),
\qquad
R_\varepsilon(z)\ge\frac{1}{2(1+\varepsilon^2)}
\]
give
\[
\frac{1}{2(1+\mu_2/a^2)}
\le u_t(x)\le
2+\frac{2\mu_2}{a^2}.
\]
Thus one may take
\[
m_{a,b}:=\frac{1}{2(1+\mu_2/a^2)},
\qquad
M_{a,b}:=2+\frac{2\mu_2}{a^2}.
\]
Differentiating \(R_\varepsilon\) gives
\[
\partial_zR_\varepsilon(z)
=
\frac{2\varepsilon(1-w^2)-2\varepsilon^2w}{(1+w^2)^2},
\qquad w=z-\varepsilon,
\]
and hence \(|\partial_zR_\varepsilon(z)|\le3(1+\varepsilon^2)\).  Since
\(\partial_x=t^{-1}\partial_z\),
\[
|\partial_xu_t(x)|
\le
\frac{3}{a}\left(1+\frac{\mu_2}{a^2}\right)
=:L_{a,b}.
\]

On the compact interval \([m_{a,b},M_{a,b}]\), the second derivative of
\(s\log s\) is bounded by \(m_{a,b}^{-1}\).  Therefore
\[
0\le\Lambda(\alpha,\beta)
\le \frac{1}{2m_{a,b}}|\alpha-\beta|^2
\qquad
(m_{a,b}\le\alpha,\beta\le M_{a,b}).
\]
Combining this with
\[
|u_t(x)-u_t(y)|\le L_{a,b}|x-y|,
\qquad
|u_t(x)-u_t(y)|\le M_{a,b}-m_{a,b},
\]
yields
\[
\Lambda(u_t(x),u_t(y))
\le C'_{a,b,\nu}\min\{|x-y|^2,1\}.
\]
Dividing by \((x-y)^2\) and multiplying by \(g_t(x)\) gives the stated
majorant.
\end{proof}


\input{sec_appendix_entropy_constants}

\section{Certificate constants for finite-time sixth-defect bookkeeping}
\label{app:finite-time-defect-estimates}

This appendix uses one ordered certificate interface for the finite-time
sixth-defect bookkeeping.  It is separate from the theorem-level entropy
asymptotics: the coefficient and remainder data are inputs, and an observed
KL value is inserted only after those inputs have been fixed.  On a controlled
shell
\[
\mu_2=\sigma^2,\qquad \EE|X_c|^7\le M,\qquad M\ge\sigma^7,
\]
write \(\widehat M=M/\sigma^7\ge1\) and first fix
\[
C_{\sigma,M}^{(8)}=\sigma^8C(\widehat M),\qquad
B_{\sigma,M}^{(8)}=\sigma^9B(\widehat M),\qquad
T_{\sigma,M}^{(8)}=\sigma T(\widehat M).
\]
These constants, defined in
Definition~\ref{def:n4-finite-time-constant-dictionary}, supply bounds for
\(A_8\) and the order-nine remainder before an observed KL value is used.  Only
after this certificate has been fixed does one form the exact finite-time
statistic
\begin{equation}\label{eq:finite-time-sixth-deficit}
\mathfrak D_t^{(6)}(\nu)
:=
t^6\left(
\frac{\sigma^4}{8t^4}
-\frac{7\sigma^6}{64t^6}
-D_{\mathrm{KL}}(h_t\|g_t)
\right).
\end{equation}
Here \(h_t=P_t*\nu\) and \(g_t(x)=P_t(x-\bar\gamma)\).  The exact identity used
after the statistic is formed is
\[
\mathfrak D_t^{(6)}(\nu)=\Delta_6(\nu)-A_8(\nu)t^{-2}-t^6E(t).
\]
If the inserted value is instead an observation
\(K_t^{\mathrm{obs}}\) with independently certified error
\[
\bigl|K_t^{\mathrm{obs}}-D_{\mathrm{KL}}(h_t\|g_t)\bigr|\le\eta_t,
\]
then the operational statistic is
\[
D_t^{(6),\mathrm{obs}}
:=
t^6\left(
\frac{\sigma^4}{8t^4}
-\frac{7\sigma^6}{64t^6}
-K_t^{\mathrm{obs}}
\right),
\]
and
\[
\bigl|\mathfrak D_t^{(6)}(\nu)-D_t^{(6),\mathrm{obs}}\bigr|
\le t^6\eta_t .
\]
Lemma~\ref{lem:controlled-shell-conditional-certificate} below is exactly the
deterministic propagation of these preassigned bounds, with the observation
error added as \(t^6\eta_t\).  The exact-KL specialization is recovered by
taking \(\eta_t=0\).

\begin{lemma}[Deterministic error propagation lemma]%
\label{lem:controlled-shell-conditional-certificate}
\label{thm:finite-time-poisson-entropy-stability}
\label{cor:controlled-shell-finite-time-oracle}
Let \(\sigma>0\), \(M\ge\sigma^7\), \(t>0\), and \(\eta_t\ge0\).  Let
\(\nu\) be a probability law on~\(\RR\) with mean \(\bar\gamma\) and centred
variable \(X_c=\gamma-\bar\gamma\) satisfying
\[
\mu_2=\EE X_c^2=\sigma^2,\qquad \EE|X_c|^7\le M .
\]
Put
\[
h_t=P_t*\nu,\qquad g_t(x)=P_t(x-\bar\gamma),
\]
let \(A_6(\nu)\) be the sixth-order entropy coefficient, and set
\[
\Delta_6(\nu):=-\frac{7}{64}\sigma^6-A_6(\nu),\qquad
\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma).
\]
Assume that \(K_t^{\mathrm{obs}}\) is an observed, computed, or estimated KL
value with an independently supplied deterministic accuracy certificate
\begin{equation}\label{eq:observed-kl-error-certificate}
\bigl|K_t^{\mathrm{obs}}-D_{\mathrm{KL}}(h_t\|g_t)\bigr|\le \eta_t .
\end{equation}
For a probabilistic estimator, the lemma applies on any event on which
\eqref{eq:observed-kl-error-certificate} holds.  The exact-KL specialization
is the case \(\eta_t=0\) and
\(K_t^{\mathrm{obs}}=D_{\mathrm{KL}}(h_t\|g_t)\).

First fix the shell constants, before inserting \(K_t^{\mathrm{obs}}\):
with \(\widehat M:=M/\sigma^7\), define
\[
C=C_{\sigma,M}^{(8)}:=\sigma^8C(\widehat M),\qquad
B=B_{\sigma,M}^{(8)}:=\sigma^9B(\widehat M),\qquad
T=T_{\sigma,M}^{(8)}:=\sigma T(\widehat M),
\]
where the functions \(C,B,T\) on the right are those of
Definition~\ref{def:n4-finite-time-constant-dictionary}.  If \(t\ge T\), then
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}, after normalization and
rescaling, supplies \(A_8(\nu)\) and \(E(t)\) such that
\begin{equation}\label{eq:main-controlled-shell-eighth-expansion}
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+E(t),
\qquad
|A_8(\nu)|\le C,\qquad
|E(t)|\le Bt^{-9}.
\end{equation}

Insert the observed KL value only through
\begin{equation}\label{eq:main-finite-time-sixth-deficit}
D_t^{(6),\mathrm{obs}}
:=
t^6\left(
\frac{\sigma^4}{8t^4}
-\frac{7\sigma^6}{64t^6}
-K_t^{\mathrm{obs}}
\right),
\end{equation}
and define the corresponding exact statistic
\begin{equation}\label{eq:main-exact-finite-time-sixth-deficit}
\mathfrak D_t^{(6)}(\nu)
:=
t^6\left(
\frac{\sigma^4}{8t^4}
-\frac{7\sigma^6}{64t^6}
-D_{\mathrm{KL}}(h_t\|g_t)
\right).
\end{equation}
Then
\begin{equation}\label{eq:main-certified-shell-sixth-identity}
\mathfrak D_t^{(6)}(\nu)
=\Delta_6(\nu)-A_8(\nu)t^{-2}-t^6E(t),
\end{equation}
and therefore
\begin{equation}\label{eq:main-finite-time-defect-estimate}
0\le\Delta_6(\nu)
\le
\bigl|D_t^{(6),\mathrm{obs}}\bigr|
+t^6\eta_t
+Ct^{-2}
+Bt^{-3}.
\end{equation}
Consequently, for \(\nu_c=\mathrm{Law}(X_c)\),
\begin{equation}\label{eq:main-finite-time-w2-stability}
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\mathcal A_t^{1/4}
+8\,\sigma^{-2}\mathcal A_t^{1/2},
\end{equation}
where
\[
\mathcal A_t
:=
\bigl|D_t^{(6),\mathrm{obs}}\bigr|
+t^6\eta_t
+Ct^{-2}
+Bt^{-3}.
\]
In particular, if each of the four summands in \(\mathcal A_t\) is bounded
by \(\varepsilon\), then
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}(4\varepsilon)^{1/4}
+8\,\sigma^{-2}(4\varepsilon)^{1/2}.
\]
\end{lemma}

\begin{proof}
The condition \(M\ge\sigma^7\) is necessary for the shell to be nonempty by
Lyapunov's inequality.  For \(X=(\gamma-\bar\gamma)/\sigma\), the law of
\(X\) has mean zero, variance one, and
\(\EE|X|^7\le\widehat M=M/\sigma^7\).  Applying
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder} to this normalized law
and restoring scale gives
\[
C_{\sigma,M}^{(8)}=\sigma^8C(\widehat M),\qquad
B_{\sigma,M}^{(8)}=\sigma^9B(\widehat M),\qquad
T_{\sigma,M}^{(8)}=\sigma T(\widehat M),
\]
as recorded in \eqref{eq:dictionary-controlled-shell-c8}, and gives
\eqref{eq:main-controlled-shell-eighth-expansion}.  Substitution into
\eqref{eq:main-exact-finite-time-sixth-deficit} gives, for \(t\ge T\),
\[
\begin{aligned}
\mathfrak D_t^{(6)}(\nu)
&=-\frac{7}{64}\sigma^6-A_6(\nu)
-\frac{A_8(\nu)}{t^2}-t^6E(t)\\
&=\Delta_6(\nu)-\frac{A_8(\nu)}{t^2}-t^6E(t).
\end{aligned}
\]
This proves \eqref{eq:main-certified-shell-sixth-identity}.  Since
Corollary~\ref{cor:coefficient-defect-coupling} gives
\(\Delta_6(\nu)\ge0\),
\[
0\le \Delta_6(\nu)
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|
+|A_8(\nu)|t^{-2}+Bt^{-3}
\le
\bigl|\mathfrak D_t^{(6)}(\nu)\bigr|+Ct^{-2}+Bt^{-3}.
\]
The observation certificate gives
\[
\bigl|\mathfrak D_t^{(6)}(\nu)-D_t^{(6),\mathrm{obs}}\bigr|
=t^6\bigl|K_t^{\mathrm{obs}}-D_{\mathrm{KL}}(h_t\|g_t)\bigr|
\le t^6\eta_t,
\]
which proves \eqref{eq:main-finite-time-defect-estimate}.  Applying
Corollary~\ref{cor:coefficient-defect-coupling} again gives
\[
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\Delta_6(\nu)^{1/4}
+8\,\sigma^{-2}\Delta_6(\nu)^{1/2}.
\]
Inserting the bound for \(\Delta_6(\nu)\) yields
\eqref{eq:main-finite-time-w2-stability}; the \(\varepsilon\)-form follows
from \(\mathcal A_t\le4\varepsilon\).
\end{proof}

\begin{remark}[Fixed-law conditional certificate bookkeeping]%
\label{rem:fixed-law-finite-time-certificate}
The same deterministic propagation applies to one fixed law outside a
controlled seventh-moment shell only after independent certificate data have
been supplied.  If \(C,B,T\), \(A_8(\nu)\), and \(E(t)\) are fixed before
observing the KL value and satisfy
\[
D_{\mathrm{KL}}(h_t\|g_t)
=\frac{\sigma^4}{8t^4}
+\frac{A_6(\nu)}{t^6}
+\frac{A_8(\nu)}{t^8}
+E(t),
\qquad
|A_8(\nu)|\le C,\qquad |E(t)|\le Bt^{-9}
\]
for \(t\ge T\), and if
\(|K_t^{\mathrm{obs}}-D_{\mathrm{KL}}(h_t\|g_t)|\le\eta_t\), then the proof
above, with
\((C_{\sigma,M}^{(8)},B_{\sigma,M}^{(8)},T_{\sigma,M}^{(8)})\) replaced by
\((C,B,T)\), gives
\[
0\le\Delta_6(\nu)
\le
\bigl|D_t^{(6),\mathrm{obs}}\bigr|+t^6\eta_t+Ct^{-2}+Bt^{-3}
\]
and the same \(W_2\) estimate with
\(\mathcal A_{t,C,B}:=
|D_t^{(6),\mathrm{obs}}|+t^6\eta_t+Ct^{-2}+Bt^{-3}\).  The observed
finite-time KL value does not certify \(C\), \(B\), \(T\), \(A_8\), the
remainder bound, or its own error radius.
\end{remark}

The exact fixed-law constants \(B_{\nu,8},T_{\nu,8}\), normalized shell
functions \(C(u),B(u),T(u)\), and dimensional shell constants
\(C_{\sigma,M}^{(8)},B_{\sigma,M}^{(8)},T_{\sigma,M}^{(8)}\) are the entries
of Definition~\ref{def:n4-finite-time-constant-dictionary}, placed directly
before Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}.  In particular,
for \(\widehat M=M/\sigma^7\), the dimensional constants are exactly the
variance-one normalized constants with the powers of \(\sigma\) restored as
in \eqref{eq:dictionary-controlled-shell-c8}.

Table~\ref{tab:n4-constant-regimes} is the single regime map used by the
main text and by this appendix.  It separates the universal coefficient-level
\(W_2\) constants, the fixed-law quantitative remainder constants, the
external fixed-law certificate inputs, and the normalized and dimensional
controlled-shell constants.  In the fixed-law conditional row, if the
certificate data are supplied by
Theorem~\ref{thm:poisson-kl-eighth-seventh-remainder}, then \(B=B_{\nu,8}\),
\(T=T_{\nu,8}\), and \(C\) is any independently fixed finite bound with
\(C\ge |A_8(\nu)|\).  In the controlled-shell row, the dimensional constants
are exactly the scale restores of the normalized functions depending only on
\(\widehat M=M/\sigma^7\), together with the universal \(N=4\) envelopes from
Lemma~\ref{lem:closed-cayley-mode-constant-envelopes}.

\begin{corollary}[Constant dependence for moment-defect \(W_2\) estimates]%
\label{cor:defect-stability-constant-regimes}
The coefficient-level constants in
Corollary~\ref{cor:coefficient-defect-coupling} are universal apart from the
explicit variance powers forced by scaling against
\(\rho_\sigma=\frac12(\delta_{-\sigma}+\delta_\sigma)\).  The finite-time
constants are separate inputs.  The controlled-shell certificate lemma uses
the constants defined for
Lemma~\ref{lem:controlled-shell-conditional-certificate};
the fixed-law variant in
Remark~\ref{rem:fixed-law-finite-time-certificate} requires externally
supplied certificate bounds \(C,B,T\).  The measured value supplies only
\(D_t^{(6),\mathrm{obs}}\), and a separate accuracy certificate
supplies the additional term \(t^6\eta_t\).
\end{corollary}

\begin{proof}
This restates the hypotheses and conclusions of
Corollary~\ref{cor:coefficient-defect-coupling},
Lemma~\ref{lem:controlled-shell-conditional-certificate}, and
Remark~\ref{rem:fixed-law-finite-time-certificate}.
\end{proof}

\begin{corollary}[Appendix check of the controlled-shell certificate constants]%
\label{cor:uniform-finite-time-defect-stability}
Fix \(\sigma>0\) and \(M\ge\sigma^7\), and use the explicit constants
\(B_{\sigma,M}^{(8)}\), \(T_{\sigma,M}^{(8)}\), and
\(C_{\sigma,M}^{(8)}\) defined for
Lemma~\ref{lem:controlled-shell-conditional-certificate}.  These constants are
the only shell certificate inputs needed in the lemma.  Consequently, for
every law in the controlled shell \(\mu_2=\sigma^2\),
\(\EE|X_c|^7\le M\), and every \(t\ge T_{\sigma,M}^{(8)}\) after an observed
KL value \(K_t^{\mathrm{obs}}\) and an error radius \(\eta_t\) satisfying
\eqref{eq:observed-kl-error-certificate} have been inserted into
\(D_t^{(6),\mathrm{obs}}\),
\begin{equation}\label{eq:uniform-finite-time-defect-shell}
W_2(\nu_c,\rho_\sigma)
\le
4\,\sigma^{-1/2}\mathcal A_t^{1/4}
+8\,\sigma^{-2}\mathcal A_t^{1/2},
\end{equation}
where
\[
\mathcal A_t:=
\bigl|D_t^{(6),\mathrm{obs}}\bigr|+t^6\eta_t
+C_{\sigma,M}^{(8)}t^{-2}+B_{\sigma,M}^{(8)}t^{-3}.
\]
Outside such a shell, supplied fixed-law certificate bounds \(C,B,T\) in
Remark~\ref{rem:fixed-law-finite-time-certificate} are the asserted level of
generality.
\end{corollary}

\begin{proof}
This is exactly \eqref{eq:main-finite-time-w2-stability}.  The shell
control is proved inside
Lemma~\ref{lem:controlled-shell-conditional-certificate} by applying the
seventh-moment remainder theorem to the variance-one law
\((\gamma-\bar\gamma)/\sigma\) and restoring the factors of \(\sigma\).
\end{proof}
