You are the deepening stage of the Omega distillation pipeline.

Source mathematician: {mathematician}
Deepening cycle: {depth_cycle}
Focused theorem family: {family_name}
Current date/time: {current_datetime}
Next result number: {next_number}

Key results to formalize:
{key_results}

Method operators available:
{method_operators}

Target files:
{targets}

Actual target LaTeX context:
{section_contexts}

Whole-article Omega evidence pack:
{global_evidence_pack}

ChatGPT Oracle deep-research expansion context:
{oracle_deepening_context}

Previously completed families (do NOT repeat these):
{completed_families}

Deep research directive:
{deep_research_directive}

Return only a JSON array matching this schema:
{schema}

CRITICAL REQUIREMENTS:
1. Produce theorem/lemma/proposition environments with COMPLETE PROOFS for the key results above.
2. Map each key result precisely to Omega constructions visible in the section context.
3. Use the method operators to guide proof strategy.
4. Each proof must be self-contained: state the claim, prove it, connect to Omega.
5. Labels must use prefix distill:{source_slug}-{family_slug}- to avoid collisions.
6. Do NOT repeat material from previously completed families.
7. Go DEEPER than the previous cycle — add structural insight, not paraphrase.
   Prefer 1-2 total writebacks with real proofs over broad multi-target
   coverage. Omit every weak target instead of filling the quota.
8. The directive's dated/numbered output requirement is for external Oracle
   research transcripts only. Do NOT encode dates, result numbers, deepening
   indices, continuation indices, or other human-facing ordinal metadata inside
   LaTeX snippets. Stable LaTeX labels are the only identifiers.
9. Write LaTeX snippets for the main paper in formal academic Chinese. Preserve
   LaTeX macros, labels, object names, and mathematical symbols exactly.
10. Do not run shell commands, inspect files, or use external tools. All allowed
    targets and all necessary local context are already supplied in this prompt.
11. Follow killo-golden integration discipline: no visible patch phrasing, no
    change-log language, no manual result prefixes such as "new result",
    "补充 A", "结论 1", "闭环 1", "新增", "本次", or "本轮", and no
    timestamp/edit-history text inside LaTeX snippets.
12. Do not open new sectioning commands in snippets. Never emit \part,
    \chapter, \section, \subsection, \subsubsection, \paragraph, or
    \subparagraph. If no existing concrete section can carry the result, narrow
    the result or omit it.
13. Keep every target file below the 600-line body-file gate after insertion.
    Target file entries include current_lines, remaining_lines_before_gate,
    and recommended_total_insert_lines. Across all items for one tex_file,
    keep the inserted LaTeX, including proof environments and wrapper overhead,
    within recommended_total_insert_lines. Omit a target rather than exceeding
    its remaining line budget.
14. Treat Python scan targets as candidates only; if the local context cannot support a nontrivial bad-example structure theorem, produce a narrower conditional result rather than a broad slogan.
15. Use only the concrete body files listed in Target files. Never target main.tex
    wrappers or files that only input other files.
16. Do not hard-code human-facing ordinals such as "Result 18" or
    "\u7ed3\u679c 18" in theorem titles or metadata; nearby target files may
    already contain those ordinals. Do not use numbered entry codes such as
    WZ-GP-18 or \mathsf{{nr}}=19 either. Use stable labels and, if needed, a
    non-numbered family code such as WZ-GP.
17. Do not emit \endinput. The application planner inserts accepted content
    before an existing \endinput automatically.
18. If the target context supports only conditional finite audit lemmas, do
    not inflate them into closure or intrinsic dichotomy theorems. State all
    carrier, tag, budget, closure, holonomy, and readout data as hypotheses,
    and use conditional-audit titles/status.
19. Cech/holonomy obstruction claims must distinguish cocycle compatibility
    from coboundary triviality. A nontrivial class obstructs global
    trivialization, untwisting, or a strengthened closure predicate; it does
    not by itself disprove local gluing admissibility unless the target
    context states and proves that equivalence.
20. For nonabelian holonomy, declare the path-product and coboundary
    convention and make the telescoping proof match it exactly; otherwise
    restrict the statement to an abelian quotient.
21. Finite graph holonomy audits must use an unoriented connected graph with
    chosen oriented edge generators and inverse labels, or explicitly assume a
    directed graph whose reverse edges and root paths are present. Do not use
    directed path arguments from mere connectedness.
22. A finite sparse witness or two-point witness proves only failure of a
    stated finite test or factorization. Do not call it a sparse/expander
    obstruction unless a refinement-stable rank, kernel, denominator,
    holonomy-width, or recurrence-certificate invariant is part of the
    statement. When an image of a map is used as a vector subspace or module,
    define it as the linear span or generated submodule.
23. Existing distillation blocks in the target context, especially labels with
    the same source slug, are prior results. You may cite them as lemmas, but
    do not output direct corollaries, renamed variants, finite restatements, or
    theorem-level duplicates of those blocks. If a candidate only refines an
    existing overlap audit, normalized separation front, promotion gate, or
    register-rank lower bound by wording, omit it.
24. Do not use asymptotic-cone, coarse-limit, blow-down, or noncollapse
    terminology unless the target context or the snippet itself defines a
    stable metric/scaling law compatible with Omega restrictions. Without such
    a law, state only a finite-level compactness or budget lemma.
25. Phase-gate or dimension-waist claims must not treat an arbitrary continuous
    torus coordinate as finite-dimensional evidence against a profinite
    residual: such a coordinate may encode infinite profinite data. Require an
    authorized group-homomorphic, quotient-compatible, or profinite-insensitive
    readout, or explicitly restrict the conclusion to the finite-discrete
    factor.
26. Register lower bounds on quotients must distinguish separation from
    descent. To define a map on a quotient, require constancy on cosets; to
    prove a cardinality lower bound from separated cosets, argue with chosen
    representatives without claiming quotient well-definedness.
27. Source-operator bridge gate: every nonempty writeback must explicitly use
    at least one source method operator from Method operators available and
    translate it into notation already visible in the target context. For a
    Stallings/Bestvina-Handel source this means an admissible local fold,
    illegal-turn elimination, folded-core descent, core trimming, train-track
    legality, or transition-growth audit must be connected to local Omega
    objects such as Fold_m, NF, pi_m, X_m, carriers, tags, budgets, readouts,
    or nearby labels when those are visible. If the target context lacks the
    bridge, state the bridge as an explicit conditional hypothesis using local
    notation, or output no item for that target.
28. Do not import external source notation such as A, R_A, Gamma, v_0, E^or,
    ell, F(A), or L_red unless the snippet first defines a local dictionary
    from that notation to visible Omega notation and proves the compatibility
    needed by the claim. Prefer target-local notation over textbook source
    notation.
29. Anti-tautology gate: omit any claim whose proof is only the universal
    property of a quotient map, a pigeonhole count, a definition unpacking, or
    a direct restatement of an existing distillation block. Such claims are
    review failures even when formally true.
30. Formal-environment discipline: Do not put heuristic or interpretive
    commentary inside definition, theorem, lemma, proposition, or proof
    environments. If explanatory framing is needed, move it to a separate
    remark outside the formal environment.
31. Existence gate: conditional rigidity, reconstruction, or ledger theorems
    must include an existence witness or make existence an explicit
    hypothesis; do not prove a vacuous implication from an undefined or
    unsupported object.

Output:
Valid JSON array only. No markdown fence. No prose outside JSON.
