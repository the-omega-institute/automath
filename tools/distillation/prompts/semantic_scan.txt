You are Stage S+ of the Omega distillation pipeline.

Source:
{mathematician}

Raw research JSON:
{raw_research}

Deterministic Python scan candidates:
{deterministic_scores}

Whole-article Omega evidence pack:
{global_evidence_pack}

Candidate LaTeX contexts:
{candidate_contexts}

Deep research directive:
{deep_research_directive}

Return only one JSON object matching this schema:
{schema}

Task:
Rerank and enrich the deterministic scan using mathematical judgment.  The
Python scan is only a recall pass over trigger phrases; it is not allowed to be
the final arbiter of where a publishable bad-example structure theorem can be
closed.

Requirements:
- Do not run shell commands, inspect files, or use external tools. Use only the
  supplied evidence pack and candidate contexts.
- Select only sections whose local definitions, labels, or constructions can
  support the bad-example-structure strategy in the directive.
- You may select a section outside the top candidate contexts only if the
  whole-article evidence pack gives concrete labels or snippets proving that it
  is more relevant than a vocabulary-only candidate.
- Prefer sections that expose unfinished interfaces: fold-aware inverse
  systems, pseudo-invariant elimination, sticky near-maximal fibres, escort
  freezing, Cech/holonomy obstruction, boundary/Walsh/Stokes/Euler certificates,
  prime-register budgets, external ledgers, or local-to-global gluing failure.
- For every semantic match, name the concrete mechanism and the strongest
  theorem-chain role it can play.
- Use semantic_score as a number between 0 and 1.
- Reject sections that only share vocabulary but do not support a new theorem
  chain.
- Do not produce writebacks here.  This stage only chooses and explains
  targets for later deepening.

Output:
Valid JSON only. No markdown fence. No prose outside JSON.
