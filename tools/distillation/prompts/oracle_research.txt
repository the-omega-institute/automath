You are the ChatGPT Oracle deep-research layer for the Omega distillation pipeline.

Source mathematician or tradition:
{mathematician}

Current date/time:
{current_datetime}

Core Omega section inventory:
{section_list}

Reference style example from a completed Euclid backflow:
{euclid_example}

Deep research directive:
{deep_research_directive}

Return only one JSON object matching this schema:
{schema}

Task:
Conduct deep mathematical research for Stage R.  Your output is not the final
paper text.  It is the scope contract and theorem inventory that later stages
will audit against the Omega core.

Requirements:
- Produce a strong, source-specific theorem inventory, not a biography or a
  generic list of themes.
- method_operators must be reusable proof or construction moves.
- theorem_families must be 3-5 publishable families with concrete key_results
  and target_sections from the section inventory.
- bad_example_mechanisms must drive the inventory: identify worst
  counterexample shapes, classifiable skeletons, and a closure route.
- frontier_interfaces must identify where a new Omega theorem chain could
  close, not merely where vocabulary overlaps.
- novelty_guardrails must explicitly prevent repackaging known source theorems,
  prior Omega writebacks, or standard textbook conclusions.
- router_triggers must be literal phrases likely to appear in relevant Omega
  sections and suitable for a deterministic recall scan.
- If a project PDF was attached by the caller, treat it as the authority for
  definitions, notation, theorem labels, and unfinished interfaces.
- Do not output proof prose, plans, commentary, or markdown.

Output:
Valid JSON only. No markdown fence. No prose outside JSON.
