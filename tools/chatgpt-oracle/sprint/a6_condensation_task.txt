Read artifacts/oracle_sprint_A6_stdobj.md in full first. Do not ask for design approval, do not
run any brainstorming or approval workflow.

An external expert audited which objects OF THE FIELD this paper's machinery can reach. He
confirms your account of M1-M8 is accurate in all material respects and that the Dushistova
boundary is correctly represented, including the termwise identity b_{2d+1}(s) = sigma_{s/2}(d+1).
He identifies exactly one genuinely field-standard structural theorem within reach, at 64 percent.

TARGET - a total-variation condensation theorem for denominator-weighted Brocot fractions of
fixed order. Take the exact statement from his report; it is fully specified there. In outline:
fix s > 2; let Q_n be the Brocot fractions of order n in canonical regular continued-fraction form
(a_r >= 2, a_1 + ... + a_r = n); weight them by P_{n,s}{x} = den(x)^{-s} / Z_n(s); let M_n be the
largest partial quotient, and on {M_n > n/2} let J_n be the unique index attaining it, with U_n
and V_n the words to its left and right. With rho_s = zeta(s-1)/zeta(s) and U, V independent
random words with laws proportional to K(u)^{-s} and K(v)^{-s} on the appropriate word sets, prove

  P_{n,s}{M_n > n/2} -> 1;
  d_TV( law(U_n, V_n), law(U, V) ) -> 0;
  hence n - M_n converges in total variation to |U|_1 + |V|_1 and M_n/n -> 1 in probability;
  the explicit law of D = |U|_1 + |V|_1;
  the joint limit (J_n - 1, r - J_n) -> (l(U), l(V)) in total variation;
  and q_n / (M_n K(U_n) K(V_n)) -> 1 in probability.

No terminology introduced by the manuscript occurs in that statement. That is the point of it.

ALMOST ALL OF THE PROOF IS ALREADY IN THE PAPER. He states that Theorem 6.3 and Lemmas 6.5-6.6
already prove the pointwise context limit, the summable domination, and the negligibility of every
configuration without a unique digit exceeding n/2 - and that the manuscript stops just before
normalizing those estimates into a probability law. Verify each of those three for yourself before
relying on it, and report any where he is wrong. The remaining work is the L^1 / total-variation
upgrade of the already proved context convergence, plus the joint and denominator-ratio limits.

INDEX TRANSLATION - DO NOT SKIP THIS. The field object is indexed by the total digit sum
n = a_1 + ... + a_r, whereas our letter cost uses n = d + 1 and c = 2d + 1. The target theorem must
be stated in the n-variable used for Brocot fractions, NOT in the renewal cost. Get this right
throughout; a mismatch here would silently make the theorem about a different family.

NOVELTY AND ARCHITECTURE ARE THE RISK, NOT THE MATHEMATICS. He says the technical probability is
much higher than 64 percent and that the discount is almost entirely novelty positioning and
architecture. So:
 - Search the literature properly before claiming anything: Dushistova's own paper and the
   Moshchevitin-Zhigljavsky line on partition and entropy sums over the Brocot/Farey hierarchy,
   and Kessebohmer-Stratmann on continued-fraction and Stern-Brocot multifractal structure.
   Condensation phenomena for heavy-tailed conditioned sums are also a well-developed subject -
   check whether this statement is an immediate corollary of a known conditioned-limit theorem for
   heavy-tailed sums, because a referee will ask exactly that. If it is, say so plainly.
 - Architecture: he judges this can be made the coherent central result of the paper. If you agree
   after reading, restructure so it is presented as such. If you do not agree, say why rather than
   forcing it.

DO NOT ATTEMPT the objects he rules out. He is explicit that the machinery does NOT reach: local
dimensions or local-dimension level sets of the golden Bernoulli convolution; finite-type
neighbour graphs, net intervals or transition matrices; normalization transducers; transfer
operators for R, the finite frequencies or the Erdos measure; or Stern-Brocot multifractal spectra
and Diophantine growth level sets. He gives structural reasons for each. In particular, equality
of exponential-moment pressure does NOT determine pointwise local dimension - do not let any
sentence in the paper suggest otherwise.

PRESERVE: Sidorov-Vershik, Lau-Ngai, Feng-Olivier and Feng outside our novelty ledger; the
recovery propositions as recovery propositions; the L^q-spectrum and its negative-q first-order
transition as imported/recovered, not claimed; the Knauf Perron-Frobenius asymptotic and
Stern-Brocot pressure as published inputs; the denominator-layer theorem's anti-cheating property
(its proof does not assume the regular variation later used by the renewal transfer); the
Dushistova correction exactly as it stands, with the mechanism (the restriction u > 1 lost when
defining Sigma_2, empty left context included, convolution doubled, endpoint counted twice with
excess exactly R_s, giving 2 R_s^2) and the displayed finite identity.

RULES:
 - Every claim proved at referee standard; numerics are sanity checks, never a substitute.
 - Keep every .tex under 800 lines; split if needed.
 - Alternating clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined
   references, citations and multiply-defined labels in both documents.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Regenerate
   artifacts/SHA256SUMS (LF) if artifacts change; confirm zero failures from artifacts/.
 - No revision marks anywhere.

REPORT: the literature findings first, including whether this follows from a known heavy-tailed
conditioned-limit theorem; which of his three "already proved" claims you verified and any he got
wrong; the theorem as typeset in the correct n-variable; what restructuring you did and why; page
and theorem counts before and after; compile and test results.
