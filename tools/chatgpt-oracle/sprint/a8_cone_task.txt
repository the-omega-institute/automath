Read artifacts/oracle_sprint_A8_reassess.md in full first. Do not ask for design approval, do not
run any brainstorming or approval workflow. Proceed directly to editing.

THE EQUIVALENCE THEOREM PASSED. The expert states that Theorem 4.1 is in substance the
experiment-equivalence theorem he had in mind, that both deficiency constructions are correct,
that the kernels are adequately measurable, and - the question we most wanted answered - that the
boundary-cycle conditioning problem is GENUINELY REMOVED RATHER THAN DISPLACED. He puts about 0.95
confidence on the core equivalence Delta(E_N, F_N) -> 0. Do not weaken, restate or renumber
Theorem 4.1 or Theorems A-G.

He puts 0.90 rather than 0.95 on the theorem block including every decision-theoretic and
efficiency consequence, for exactly one reason, and fixing it is the mathematical part of this
task.

 1. QUALIFY THE CONE SENTENCE. The manuscript says that all conclusions continue to hold after
    restriction to a cone or any fixed local subset. That is too broad if it is meant to include
    the unmodified canonical-gradient efficiency bound. What is true: the Le Cam equivalence and
    the bounded-loss risk transport unquestionably survive parameter restriction, and the
    information matrix remains the information matrix of the ambient DQM family. What is NOT
    generally true: at a boundary or in a tangent cone the relevant efficiency or minimax bound
    can be a CONSTRAINED-EXPERIMENT bound rather than the ordinary unconstrained
    canonical-gradient variance. Rewrite the sentence to say exactly that, taking his wording, and
    check every other place where restriction to a cone or local subset is asserted.

 2. RETARGET THE PAPER TO STOCHASTIC PROCESSES AND THEIR APPLICATIONS. His venue answer is
    explicit and we should act on it rather than reach: the result condition for a Bernoulli
    submission is now met, but the earlier condition that the paper be sharply rebuilt around the
    central statistical results is NOT. Bernoulli is therefore defensible on mathematical content
    but remains an intentionally high-risk submission rather than the honest modal target. In the
    present 70-page form - retaining renewal observation, stationary censoring, D-MAP realization,
    phase-type structure and singular inference - SPA is the most honest first target. EJS becomes
    at least as natural as SPA only if the manuscript is recast around experiment equivalence,
    LAN, efficient testing and minimax recovery with most of the representation and diagnostic
    material removed from the main article; we are not doing that recast now.
    Update submission_metadata.md to record: SPA as the honest first target for the manuscript as
    assembled; Bernoulli as defensible on mathematical content but high-risk on architecture, with
    his exact observation that the mathematical case improved while the editorial case did not;
    EJS as the natural target only under a recast that has not been performed. Rewrite the cover
    letter for SPA, leading with the renewal-experiment equivalence and the singular inference
    application, and do not claim the architecture condition has been satisfied.

PRESERVE: the two deficiency bounds and the coupling argument exactly as they stand, since he has
now confirmed them; the rank-one reset formulation T_1 = c beta with failure tied to
pre-click-state dependence of the normalized marked rows; the domain qualification restricting the
stationary product likelihood to records with at least one renewal; the standard endpoint
terminology, A the forward recurrence or residual life and R the backward recurrence or age; the
o_P(1) claim for the increments of the endpoint log-factors only; the statement that the
observation object is a stationary binary lattice-renewal indicator, excluding generic sampled
point processes, Markov-renewal and MAP models; the Bickel-Kwon, Ramirez-Cobo-Lillo-Wiper,
Meszaros-Telek and He-Zhang attributions and the "candidate original result pending complete
reduction" qualification; G(iii) as the residualized score test with likelihood-ratio coincidence
only in the limiting Gaussian half-space experiment; the eventually exact two-point multiset
distance with its threshold; the Batenkov-Goldman-Yomdin and Kulaitis-Munk-Werner comparison with
their observation operators and losses distinguished; and the recorded ceiling that the
equivalence does not broaden the singular geometry.

ALSO SPLIT sec_records_part10.tex. It is at 790 lines, ten under the limit, so any further edit
breaks the rule. Split it into coherent parts.

RULES:
 - Every claim proved at referee standard; numerics are sanity checks, never a substitute.
 - Keep every .tex under 800 lines.
 - Alternating clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined
   references, citations and multiply-defined labels in both documents.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Regenerate
   artifacts/SHA256SUMS (LF) if artifacts change.
 - No revision marks anywhere. Do not leave any planning or log file in the paper directory.
 - If you must resume a codex session, resume by explicit session ID. Never `resume --last`.

ALSO: verify every DOI in the bibliography against Crossref, OpenAlex or Semantic Scholar, and
every entry without a DOI by title and author. A fabricated entry with a plausible title and a
non-resolving DOI was just found in a sibling paper after surviving several referee rounds. Do not
silently delete anything you cannot confirm and do not invent a replacement - list it as
unverified in artifacts/literature_check.md with what you tried. Report the table.

REPORT: the qualified cone sentence as typeset and every location changed; the SPA retargeting and
the cover letter as rewritten; how sec_records_part10.tex was split with resulting line counts; the
full bibliography verification table including anything unconfirmed; page and theorem counts before
and after; compile and test results.
