Read artifacts/oracle_sprint_A4_tier.md in full first - it is a tier-raising assessment from
the external expert who refereed this paper. Do not ask for design approval, do not run any
brainstorming or approval workflow. Proceed directly to work.

HIS HONEST FRAMING, WHICH YOU MUST NOT OVERSELL. He states there is no candidate with both
success probability above 50 percent and tier impact at least 8/10, and therefore no clear
full-tier jump is presently reachable. The structural ceiling is a quantifier limitation:
the present mechanism extracts one specially synchronized orbit from an arbitrary infinite
MCFL; it does not control the whole language, every sufficiently long word, or simultaneous
representations in two numeration systems. Candidate 1 below is the best available move and
may raise the paper within or across a nearby boundary, but it does NOT remove that ceiling.
Nothing you write may claim otherwise.

DO BOTH OF THE FOLLOWING, in this order.

TASK 1 - Weak-Perron radical classification (his rank 1, 68% x 7.5).
Extend the current strict-Perron endpoint to weak Perron numbers, meaning every conjugate
beta' satisfies |beta'| <= beta rather than strict inequality. The target is the equivalence
he states: for a standard greedy linear numeration basis U with U_0 = 1, strictly increasing
integer place values with bounded quotients, R_U the LSD-first reversal of its greedy
representation language, minimal tail recurrence polynomial mu_U the minimal polynomial of a
weak Perron beta > 1, and S_r the set of prime divisors of mu_U(0), the following are
equivalent:
  (i)   there exist k < infinity and an infinite k-MCFL L contained in R_U with
        sup_{w in L} omega_{S_r^c}(val_U(w)) < infinity;
  (ii)  R_U contains a geometric synchronized scheme whose values are c b^t with c >= 1,
        b >= 2;
  (iii) some positive power of beta is an integer, beta^m = B in Z_{>=2};
  (iv)  mu_U(X) divides X^m - B for some m >= 1, B >= 2;
  (v)   U_{n+m} = B U_n for all n >= n_0, for some m >= 1, B >= 2, n_0.
Take the exact statement and numbering of the conditions from HIS report, not from this
paraphrase.

He judges the proof path clear, using the current inverse theorem and the greedy interval
argument almost verbatim, and locates the risk as technical rather than conceptual: proving
the weak-Perron residue asymptotics with all coefficients and tail thresholds handled
correctly, and ensuring the proposed class of greedy systems is standard and NONEMPTY BEYOND
TOY EXAMPLES. Treat that last point as mandatory - exhibit genuine members with beta
nonintegral, or the classification is decorative.

TASK 2 - Eliminate the strict length-ordering hypothesis (U3) (his rank 2, 90% x 4.5).
He says the necessary substitutions are visible in the existing proofs and that the
quotient-chain selection argument appears to repair every later use of monotonicity. Remove
(U3), verify no downstream proof silently depends on it, and state the broadened theorem.
The danger he names is overlooking one later use of monotonicity - so enumerate every place
(U3) or length monotonicity is used and account for each.

DO NOT PURSUE candidate 3, general decidability of geometric-scheme existence. He puts it at
12 percent, and the missing step is the hard global issue rather than an omitted lemma: the
present proof offers candidate verification but no way to bound the candidate search.

DO NOT claim the Le Gonidec degeneracy / Cobham-extension problem. He explains why it is out
of reach: a Cobham-type theorem compares the global structure of one set in two
multiplicatively independent representations, whereas our weak pumping input yields one
synchronized ray inside one infinite MCFL, and two languages representing the same set could
supply two unrelated rays with no common values. You may cite it as the nearest named open
problem and say plainly that this paper does not settle it.

HONEST OUTCOMES: if Task 1 does not close, say so in the FIRST LINE of your report and give
the exact step that fails. A sharp obstruction is a real result. Do not assume the
weak-Perron residue asymptotics you are supposed to prove, and do not quietly narrow the
class of greedy systems until the theorem becomes true but empty.

RULES:
 - Every claim proved at referee standard; numerics are sanity checks, never a substitute.
 - Do not weaken, restate or renumber existing theorems; preserve the priority wording for
   Seki-Matsumura-Fujii-Kasami and the other credited prior work.
 - Keep every .tex under 800 lines; split if needed.
 - Alternating clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined
   references, citations and multiply-defined labels in both documents.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Regenerate
   artifacts/SHA256SUMS (LF) if artifacts change and confirm zero failures from artifacts/.
 - No revision marks anywhere.

REPORT: for Task 1, the exact equivalence proved or the exact obstruction, plus the concrete
nonintegral members exhibited; for Task 2, the enumerated list of every use of (U3) and how
each was discharged; page and theorem counts before and after; compile and test results.
