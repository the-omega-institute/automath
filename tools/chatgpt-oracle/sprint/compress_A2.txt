This manuscript is 90 pages. Its target journal is the Journal of Functional Analysis, which will not consider a submission of that length. Bring the compiled main.pdf down to roughly 45-55 pages.

HARD CONSTRAINTS — violating any of these makes the work useless:
- Do NOT delete, weaken, or restate any theorem, proposition, lemma, corollary, or definition. Nothing that is currently proved may become unproved or less general.
- Compression is RELOCATION and PROSE TIGHTENING only. Move supporting material out of the main text into the supplement files that already exist and are not inputted by main.tex (supplement_optional_coefficient_appendices.tex, supplement_optional_missing_top_moment_tails.tex, sec_supplement_doob_phi_closure.tex), or into a new supplement file. Where material moves, leave a precise pointer in the main text saying what is proved there and where to find it.
- Every cross-reference must still resolve. The final compile must report zero undefined references and zero undefined citations.
- The verification scripts must still pass unchanged: artifacts/verify_oracle_A2.py, verify_moment_equivalence.py, verify_boundary_regular_variation.py, and the pytest suite.

WHAT BELONGS IN THE MAIN TEXT for this venue: the core theorem chain and its proofs — the all-order Poisson entropy coefficient theorem with its remainder theory, the sharp moment-equivalence statements, the defect ladder, the Poisson-strip RKHS theorem package, and the results verified in this sprint (including thm:raw-tail-poisson-energy-decomposition).

WHAT SHOULD MOVE OUT: long coefficient arithmetic, routine or mechanical verifications, numerically-certified tables, secondary normalization variants, and any layer that only supports a main theorem rather than being one.

Judge what is core yourself by reading the manuscript; do not guess from filenames alone.

Then compile: latexmk -pdfxe -interaction=nonstopmode -halt-on-error main.tex
Iterate until exit 0. Re-run the verification scripts and the pytest suite.

Report ONLY: the page count before and after, what you moved and where, confirmation that no theorem was deleted or weakened, the undefined-reference and undefined-citation counts from the final log, and the verifier/pytest results. Do NOT git commit.
