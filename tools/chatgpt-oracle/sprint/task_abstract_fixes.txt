TASK: fix three defects found by a referee-style read. Edit files directly. You are
pre-authorised; do not ask, do not brainstorm. These are corrections, not rewrites: change as
little as possible.

PAPER: papers/publication/2026_zeckendorf_stable_arithmetic_fibonacci_congruence_online/

DEFECT 1, a dropped hypothesis. The abstract says:

    every exact most-significant-digit-first multiplier at effective resolution n has delay at
    least n-1

The theorem, in source/07_emergent_arithmetic_06_def_stable_mul.tex, concludes delta_n >= n-1
only "for every n>=3", and additionally assumes the most-significant-digit-first delay
convention of thm:zeckendorf-add-fst, padded synchronous inputs with L >= n+2, c,d in X_n
extended by zeros, and that the machine computes the stable product for every pair in
X_n x X_n. The abstract states the conclusion without the n>=3 and without the model. As
written the sentence claims more than is proved. Add n>=3 and enough of the model that the
sentence is true; you do not need to restate every hypothesis, but the reader must not think
it holds for all n and for any multiplier.

DEFECT 2, a mathematical error. The abstract says:

    field phases when F_{m+2} is prime and Chinese-remainder decompositions when it is composite

The body's own trichotomy is in source/07_emergent_arithmetic_03_zeckendorf_stable_value.tex
around lines 349-350: prime modulus gives a field; a PRIME POWER is local and does NOT split by
CRT; only a modulus with at least two distinct prime factors splits. A composite that is a
prime power therefore has no nontrivial CRT decomposition, so the abstract is false as written.
Change "when it is composite" to the condition the body actually proves, namely at least two
distinct prime divisors, and make sure the prime-power case is not implied to split.

DEFECT 3, an unsupported chronology claim, in cover_letter_integers.txt. The letter says:

    ITA-2026-0032 was submitted before the present work and does not cite it

The repository establishes that ITA-2026-0032 is under review at RAIRO ITA and that it does not
cite this manuscript. It does NOT establish the submission ordering. Drop the ordering claim and
keep only what is supported: that the present manuscript cites ITA-2026-0032, that
ITA-2026-0032 does not cite this one, and that the RAIRO ITA editors will be informed. Do not
replace it with a different unverifiable assertion.

CONSTRAINTS.

  - Do not touch any theorem, lemma or proof.
  - Do not restructure the abstract; these are three local corrections.
  - No revision marks, no changelog language.
  - Run latexmk and confirm the paper still builds.

WHEN DONE: paste the corrected abstract sentences and the corrected cover-letter passage.
