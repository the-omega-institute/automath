Deep exploration round. Last round you were handed a pre-assigned upgrade target and correctly judged it not worth doing. That target was someone else's guess, not a verdict that this paper is exhausted. This round YOU choose what to work on.

YOUR OWN CONCLUSION LAST ROUND: You said the more valuable nearby target is the active finite-window boundary and critical crossover, currently out of reach because it needs a sharp joint local theorem for prime-exponent vectors. Consider building that local theorem itself — with Weinstein's rational multivariate generating function (2 - prod_{p in S}(1-z_p)/(1-p z_p))^{-1} in hand, a saddle-point or multivariate singularity analysis may be within reach.

STEP 1 — GENERATE CANDIDATES (do this properly, do not shortcut to one idea).
Re-read what this manuscript actually proves and, more importantly, what MACHINERY it has built — the constructions, estimates, certificates and structural lemmas that exist here and nowhere else. Then produce at least FIVE candidate theorems that this machinery could plausibly reach. Look in these directions:
 - the natural generalisation the machinery already almost supports;
 - the converse or sharpness of something currently proved one-way;
 - a classification or dichotomy where the paper currently gives examples;
 - an effective/quantitative version of a qualitative statement (or vice versa);
 - a question a specialist in the adjacent field would ask on reading the abstract, which this paper is unusually well-placed to answer;
 - anything the paper currently states as an open problem or an explicit gap.

STEP 2 — SCORE THEM. For each candidate give: reach (can the existing machinery get there, and what is missing), novelty (search the literature — arXiv API, Crossref, Semantic Scholar, zbMATH — and name the nearest prior work), and value (would a specialist consider it a real contribution, or bookkeeping). Be honest; a candidate that fails is useful information.

STEP 3 — PICK ONE AND PROVE IT. Choose the best by value x reach, and do the mathematics. Give complete proofs with explicit hypotheses. If mid-proof you find it fails, say exactly where it fails — a precisely located failure is a real result and is worth reporting.

STEP 4 — INTEGRATE. Only what you actually proved. Extend artifacts/ verification scripts to cover it and run them. Compile with latexmk -pdfxe -interaction=nonstopmode -halt-on-error main.tex until exit 0. Never state as proved anything you have not proved.

There is almost certainly something worth doing here. "Nothing to do" is an acceptable conclusion ONLY if you can name the five candidates you considered and say why each fails.

Do NOT git commit. Do NOT run pytest at the paper root (memory); run individual test files.

Report, at most 45 lines:
 (a) the five-plus candidates with their reach/novelty/value scores;
 (b) which you chose and why;
 (c) what you proved — or precisely where it failed;
 (d) what you integrated;
 (e) verifier and compile results.
