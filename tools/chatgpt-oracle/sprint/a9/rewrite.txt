This manuscript was rejected by Annals of Pure and Applied Logic without its mathematics ever being evaluated. The editor's words: it "does not meet the standard requirements for a mathematical paper in terms of style", it "uses terminology in a way that is not standard and is not explained", and therefore "an evaluation of its content is not possible in its current state".

A terminology audit has already been done for you. Read it first, in full:
  /d/omega/automath/tools/chatgpt-oracle/sprint/a9/TERMINOLOGY_AUDIT.md
It inventories 68 coined term families, 29 of them in direct collision with established usage, 27 opaque non-colliding coinages, 12 aligned. It contains renaming proposals, a glossary table, and a 145-word abstract draft in standard language. Its central finding is that the core results can be stated almost entirely with standard terms (teams, presheaves), so most of the vocabulary is idiosyncratic naming of standard constructions.

YOUR TASK is to make this paper readable by a specialist, without changing any mathematics.

1. APPLY THE RENAMINGS. Work through the audit's proposals. Rename every term that collides with an established meaning — in particular `realization` (model theory: realizing a type), `slice` (category theory: slice category), `bouquet` (topology: wedge sum). Where the audit says a coinage is merely an idiosyncratic name for a standard construction, replace it with the standard name outright. Where the audit judges a term load-bearing because the object genuinely is new, keep it but define it at first use, prominently.

2. ADD THE GLOSSARY immediately after the introduction: every retained coined term mapped to its standard counterpart or a one-line definition in standard language.

3. REWRITE THE ABSTRACT AND INTRODUCTION in standard mathematical language. Lead with the crisp result — on the good-cover site of a wedge of beta 2-spheres, a nonzero finite abelian G occurs as a pure two-branch resolution kernel exactly when d(G) <= 2*beta and G is not a cyclic p-group — stated so that a category theorist or logician understands it in one reading. State plainly, early, what is imported (Giraud H^2, the universal coefficient theorem, stackification) and what is ours.

4. STATE THE RELATION TO ABRAMSKY-MANSFIELD-BARBOSA HONESTLY AND EARLY. Their obstruction lives in a relative Cech H^1; ours is a derived H^2 for a sheaf band. We proved these do not correspond: only the support-level correspondence is functorial, and the d(G) <= 2*beta characterisation does not transfer to bare empirical models. Say this in the introduction rather than leaving a reader to discover it. A referee who sees the difference acknowledged up front will read on; one who suspects it is being glossed will not.

Do NOT change any theorem statement, hypothesis or proof. This is a presentation pass. If applying a renaming would change meaning, keep the old name and say why in the glossary.

Compile with latexmk -pdfxe -interaction=nonstopmode -halt-on-error main.tex until exit 0, zero undefined references and citations. Verify the theorem-environment count across all .tex sources is unchanged from before your edits.

Report at most 25 lines: how many terms renamed, which load-bearing terms you kept and why, confirmation that the theorem-environment count is unchanged, and the compile result. Do NOT git commit.
