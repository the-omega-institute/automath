The rename of this manuscript is still half done, across two interrupted passes. The build is now healthy (exit 0, 96pp, zero undefined references, 159 theorem environments) so you are starting from a clean base. Your only job is to FINISH THE RENAME.

Residual counts: `slice` 114, `realization` 69, `bouquet` 13.

Read the agreed renamings:
  /d/omega/automath/tools/chatgpt-oracle/sprint/a9/TERMINOLOGY_AUDIT.md

Work file by file, smallest first, and compile after each file rather than at the end — the previous two passes both died mid-edit and left the build broken. Committing progress incrementally is the point.

`realization` collides with model theory (realizing a type). `slice` collides with slice category. `bouquet` collides with wedge sum. Replace each per the audit, except where the audit explicitly says a term is load-bearing — in that case keep it and ensure it is defined at first use.

Non-negotiable at every step: theorem-environment count stays 159, no theorem statement, hypothesis or proof changes, and latexmk -pdfxe -interaction=nonstopmode -halt-on-error main.tex exits 0 with zero undefined references and citations.

Report at most 12 lines: residual counts of the three terms, why each survivor is legitimate, environment count, compile result. Do NOT git commit.
