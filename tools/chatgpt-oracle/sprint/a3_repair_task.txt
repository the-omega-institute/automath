Read artifacts/oracle_sprint_A3_referee.md (or
../../../tools/chatgpt-oracle/sprint/result_A3_referee.md) in full first — it is the
referee report for DCDS-A and it is correct. Execute its required repairs. Do not add
new theorems.

Verdict: Major Revision. It becomes Reject-and-Resubmit if we will not restructure the
priority narrative, the theorem hierarchy and the core proofs. Two blocks of work:

(1) PRIORITY — one definite substantive omission. The collision-graph / directed-cycle /
    invertibility decision used in Theorem 6.2 and Corollary 6.3 belongs to the classical
    tradition of one-dimensional local codes, tessellation automata, cellular automata,
    and fiber-product / pair-graph constructions. It CANNOT be presented as a new
    framework of ours. Cite that tradition accurately and restate Theorem 6.2 /
    Corollary 6.3 as an application of the classical decision to our setting, keeping as
    ours only the part that genuinely is.

(2) PROOFS — several parameter-uniform estimates and exhaustion steps are compressed to
    the point where the referee will not sign off. Each of these must be written out in
    full, and the referee states the consequence if we do not:
      - Lemma 5.3. If not supplied, Theorem 5.5 and everything downstream — the exact
        thresholds, the Fischer cover, the Markov order — lose their unified proof and
        the verdict becomes reject. Include the natural-overlap-graph right-resolving
        decomposition: two edges from the same source have rank difference h*Q_{m-1}
        nonzero mod Q_m; the negative-sign chamber is direct, the positive-sign chamber
        needs the already-proved ratio bound. Also supply the standard two-line step from
        "a label word of length m-1 has two paths ending at different Fischer states" to
        "that word is not intrinsically synchronizing": use follower-separatedness to pick
        a successor belonging to only one terminal state, then use right-resolvingness to
        exclude splicing the other path.
      - Lemma 6.7 and Lemma 6.8. Without these, the key two-window exclusion in
        Theorem 6.9 is not adequately proved and the cubic unbounded theorem is not
        acceptable. The present text argues by verbal induction; that is not enough.
      - The cubic root count: the manuscript proves one positive root and one negative
        root in (-1,0), then simply asserts the remaining two roots are a non-real
        conjugate pair. Prove it.
    State explicitly that small apertures r = 2, 3 have no gap between them and the
    uniform argument (the referee confirms there is none — make the text show it).

(3) RESTRUCTURE FOR DCDS-A. The referee says that after (1) and (2) the genuine increment
    DOES support a DCDS-A paper, and that we should NOT downgrade the venue. The increment
    is: exact thresholds for ALL quadratic Pisot bases, with exact finite-block onset and
    Markov order; the special compression and the aperture dichotomy for the simple-Parry
    arithmetic difference graph; and the exact unbounded future decoding depth on the
    Bassino cubic family. Rewrite the introduction and the theorem hierarchy so that these
    three are the headline results, in dynamical-systems language, since DCDS has published
    finite-type classification of intermediate beta-shifts, follower/predecessor/extender
    sets for classical beta-shifts, and canonical graph presentations of sofic shifts.

Rules, all mandatory:
 - Compile after EVERY file you finish, not batched at the end.
 - Keep every .tex under 800 lines; split if needed.
 - Final state: latexmk -pdfxe exit 0, zero undefined references, zero undefined citations,
   zero multiply-defined labels, on a CLEAN rebuild with .aux removed.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts.
 - No revision marks: never write "revised", "corrected", "in this version", or refer to
   earlier drafts.
 - Report at the end: which proofs you completed and which you had to weaken, the priority
   restatement, page count before/after, and compile/test results.
