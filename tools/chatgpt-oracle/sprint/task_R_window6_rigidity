Following your sporadic-classification answer, I verified everything you asserted and found
one more piece of structure. I now want the remaining theorem proved, or told it is hard.

WHAT I CONFIRMED INDEPENDENTLY. I reran exact colour refinement of the binary Zeckendorf-prefix
fold. Your table reproduces exactly: nontrivial coarsest equitable refinements at m = 6, 8, 9
with 48, 192 and 384 cells, and discrete refinement at m = 7, 10, 11, 12. I extended it and the
refinement is also discrete at m = 13, 14, 15 and 16.

Your two new involutions are correct. Writing the map as "swap two positions and complement
both", m = 6 swaps positions 1 and 5, m = 8 swaps 1 and 4, m = 9 swaps 2 and 5. In each case
the two binary weights sum to a Fibonacci number: 32 + 2 = 34 = F_9 for m = 6, and
128 + 16 = 144 = F_12 for both m = 8 and m = 9. All three are involutions, all three preserve
the fold, and in all three the orbit partition coincides exactly with the computed coarsest
equitable refinement.

ONE PIECE OF STRUCTURE YOU DID NOT STATE. The cell counts are not three unrelated numbers. In
each of the three cases the refinement consists of exactly 2^(m-1) singletons and 2^(m-2)
pairs, so the number of cells is 3 * 2^(m-2). That gives 48, 192, 384 for m = 6, 8, 9.
Equivalently, the involution has exactly 2^(m-1) fixed points, which is half the cube. Please
confirm or correct that, and if it is right, say why exactly half the vertices are fixed - I
would expect the fixed set to be where the two swapped coordinates are complementary, which is
half the cube, but I want the argument stated rather than assumed.

WHAT I WANT NOW.

1. Prove eventual rigidity, or say it is hard. The statement: there is M such that for every
   m >= M the coarsest equitable refinement of the fold partition is the discrete partition.
   You said this looks likely true but is not a two-week theorem. Given that it now holds
   computationally through m = 16, and given that the sparse-binary-Fibonacci obstruction is
   classified, is there a proof? If the honest answer is that no proof is in reach, say so and
   say what the obstruction is.

2. Is M = 10 provable, or only M = some ineffective constant? An explicit M would let the
   paper state a complete classification rather than an asymptotic one.

3. Does the sparse-binary classification of Fibonacci numbers actually close the argument, or
   does it only rule out this one mechanism? Specifically: could a coarsest equitable
   refinement be nontrivial without arising from an affine involution at all? If so, the
   computation through m = 16 is evidence but the classification is not proved.

4. State the final theorem you would put in the paper, in full, with hypotheses, assuming the
   answers above go the best plausible way. One theorem, written out.
