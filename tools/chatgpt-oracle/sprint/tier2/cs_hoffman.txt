TASK: add the missing Hoffman citation in
papers/publication/2026_cubical_stokes_inverse_boundary_readout_jdsgt. Small, targeted.
PRE-AUTHORIZED to edit that directory. Do not commit.

Hoffman's circulation criterion is named five times in the text and has no bibliography entry. Add
one, and cite it at the first mention (main.tex around line 71) and at the theorem where the
extension test is applied.

I have already verified the reference against Crossref, so use exactly this and do not substitute a
different one:
  A. J. Hoffman, "Some recent applications of the theory of linear inequalities to extremal
  combinatorial analysis", Proceedings of Symposia in Applied Mathematics, volume 10, 1960,
  pages 113-127, DOI 10.1090/psapm/010/0114759.
Crossref returns that title, that author and that container for that DOI; I checked.

The bibliography file is references_local.bib. While you are there, also make sure max-flow/min-cut
and LP duality have citations if they are asserted as standard facts rather than proved - if they
do not, add standard references, but VERIFY each one against Crossref plus one other index and
compare the returned title and authors before adding. Do not add an entry you could not verify;
this project has been burned by DOIs that resolve correctly to unrelated papers. Report the raw
count of entries checked and the raw count that passed.

Change nothing else: no theorem, no proof, no abstract wording.

VERIFY: latexmk -C, delete .aux, rebuild from zero with no command-line macro definitions; report
exit code, page count, undefined control sequences, unresolved references, unresolved citations,
all of which must be 0; reconcile citations both directions and report both counts.
Do not run git commit.
