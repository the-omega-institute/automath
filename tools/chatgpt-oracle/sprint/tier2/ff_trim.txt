TASK: bring papers/publication/2026_fibonacci_folding_zeckendorf_normalization_gauge_anomaly_spectral_fingerprints
from 34 pages to 23-26. PRE-AUTHORIZED to edit that directory. Do not commit.

READ FIRST: artifacts/oracle_sprint_FF2_r1.md. Read-only evidence, as is every artifacts/oracle_*.md.

THE DIAGNOSIS, which is the reason for the cut rather than a page target for its own sake. The
referee says the paper has one result worth a specialist's attention and that it is buried among
standard consequences: for every m >= 3 the sliding code is a topological conjugacy onto its image
and its causal inverse has exact memory 2, independently of m, while whole-block reconstruction has
the sharp threshold 2m-1. It also says most of what follows the four-state graph consists of
consequences of having identified that graph, not separate advances - and that the manuscript
already says so itself.

A previous round reframed that material as consequences. This round removes it.

KEEP, and do not weaken:
 - Theorems 5.3 and 5.5, the exact causal decoder and the conjugacy;
 - Theorem 5.2, the sharp block separation, as the arithmetic backbone - the contrast between the
   m-scale whole-block threshold and the uniform three-label recovery is what makes the result
   interesting;
 - the four-state carry transducer and the two language inclusions, since the decoder rests on them;
 - the strictly sofic discrepancy factor with its Fischer cover and entropy log phi;
 - Section 3's compatible-restriction material including the sharp commutation proposition, the
   count ceil((2^{m+2}+1)/3) and the all-depths corollary;
 - Lemma 4.4, the bounded-delay pair criterion, with its window-3 example, at its current one-page
   size and out of the abstract where it already is.

CUT, or reduce to a remark of a few lines each: the covariance formulas, the power spectrum, the
joint rotation polygon with its plastic-constant frontier subshifts, and the weighted-fibre
thermodynamics with the Legendre transform. These are the standard finite-state calculations the
referee names. Where one of them is genuinely striking - the exact asymptotic variance 118/243, the
pentagon - state the result in a sentence with a pointer, rather than deriving it.

PRESERVE, DO NOT DESTROY: everything cut goes verbatim into
sections/_cut_consequences.tex, not input by main.tex, with a one-line header per block naming what
it was.

The abstract and introduction must be re-ordered to match: the decoder first, the block threshold as
its companion, the graph as the tool, and the statistics named but not advertised as coequal results.

DO NOT reach the page target by any means other than removing content. No linespread, no margin or
font changes, no \clearpage removal games - I check for all of it. If the honest cut lands at 27 or
28 pages, report that; do not squeeze.

VERIFY: latexmk -C, delete .aux, rebuild from zero with NO command-line macro definitions; report
exit code, page count before and after, undefined control sequences, unresolved references,
unresolved citations, all 0. Every shipped .tex under 800 lines. Note that pdftotext DROPS ff, fi and
fl ligatures in this document even with -enc UTF-8, so search both spellings and print a control
count before concluding anything. Also scan for stripped leading backslashes on control words - a
sibling paper here had five that compiled silently and printed the letters into the PDF - and report
the raw count.
Report what you kept, what you cut, and anything in the kept material you believe is weak.
Do not create planning files. Do not run git commit.
