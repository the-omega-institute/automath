TASK: add one sharp proposition to Section 3 of
papers/publication/2026_fibonacci_folding_zeckendorf_normalization_gauge_anomaly_spectral_fingerprints,
AT NO NET COST IN LENGTH. PRE-AUTHORIZED to edit that directory. Do not commit.

READ FIRST the referee report artifacts/oracle_sprint_FF2_r1.md. It is read-only evidence, as is
every artifacts/oracle_*.md file.

THE TENSION, stated up front so you handle it deliberately. That referee says the paper's problem is
that its one real theorem is buried among standard consequences, and that the manuscript is 34 pages
against a target of 23-26. So this addition must REPLACE looser existing discussion in Section 3,
not sit on top of it. If after drafting you judge that it makes Section 3 longer rather than
sharper, say so and leave the section alone - that is an acceptable outcome and I would rather have
it than a padded section.

WHAT TO ADD. Section 3 currently proves that naive truncation and the compatible restriction can
disagree at every visible coordinate, and that no uniformly bounded tail correction repairs the
failure. What it does not say is exactly WHEN naive truncation does commute. That gap is fillable
sharply, and I have verified each statement below numerically.

Proposition (sharp commutation set). For omega in Omega_{m+1},
    proj_{m+1 -> m}( Fold_{m+1}(omega) )  =  Fold_m( naive_{m+1 -> m}(omega) )
holds if and only if
    N(omega) < F_{m+3},
the no-wrap condition. Verified with zero mismatches for all m up to 16, that is over every one of
the 131,072 words at m = 16.

Count and density. The number of omega in Omega_{m+1} satisfying the no-wrap condition is exactly
    ceil( (2^{m+2} + 1) / 3 ),
so naive truncation commutes on a set of density tending to 2/3 and fails on density 1/3. Exact for
m = 1..19 with zero mismatches; the nearby closed form (2^{m+2}+2)/3 matches only 9 of those 19, so
the formula is pinned rather than fitted.

Corollary (all depths). Every adjacent diagram commutes at every prefix of omega if and only if
omega has no adjacent ones, that is omega lies in the golden-mean language, where Fold is already
the identity. Verified with zero mismatches over 262,140 words. State the consequence plainly: the
only trajectories admitting a fully projective tower of folded prefixes are those that require no
folding at all.

That corollary is the point of the addition. The paper currently shows the obstruction is severe;
this says exactly how large the good set is and identifies the fully compatible trajectories as the
degenerate ones.

PROVE THEM, do not cite my numerics. The no-wrap direction should follow from the paper's own
observation that a length-m window value is less than 2 F_{m+2}, so reduction subtracts either
nothing or exactly F_{m+2}; the count follows from a two-term recursion on the layer; the corollary
follows by applying the no-wrap condition at every prefix. If any of the three does not in fact
follow, say which and stop rather than writing something that reads like a proof.

Add a reproduction script under artifacts/ that recomputes the characterisation, the count and the
corollary, and that includes the failing control formula so the check is shown to discriminate.

VERIFY: latexmk -C, delete .aux, rebuild from zero with NO command-line macro definitions; report
exit code, page count, undefined control sequences, unresolved references, unresolved citations, all
0, and report the page count BEFORE and AFTER so I can see the net cost. No linespread or other line
stretching. Note that pdftotext drops ff/fi/fl ligatures in this document even with -enc UTF-8, so
search both spellings and print a control count before concluding anything.
Do not create planning files. Do not run git commit.
