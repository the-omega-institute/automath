TASK: make the two phase constants in
papers/publication/2026_scan_projection_address_semantics_sigma_nonexpansion_etds independently
reproducible. PRE-AUTHORIZED to edit that directory. Do not commit.

You rewrote this paper around periodic survivors and its central claim is a genuine phase dependence:
the period-two example yields two DISTINCT critical Poisson means,
   c_{2,0} = 953/2809,     c_{2,1} = 267/(338 sqrt 5),
approximately 0.3393 and 0.3533. Everything rests on those being different. I cannot check them
without reconstructing the example from the prose, and I am not going to accept them on assertion.

WHAT TO PRODUCE. A script under artifacts/ that starts from the transition data of the period-two
example as it is defined in sec_open_system_resonance.tex - not from the answers - and computes:
 - rho_s for s = 1, 2;
 - A_{s,j}(1) for both phases at both s, so the printed values 53/89, 52/89, 953/7921 and
   2136/(7921 sqrt 5) are outputs rather than inputs;
 - c_{2,0} and c_{2,1} through the relation the paper labels eq:phase-renyi-constant;
 - and the pair Renyi rate that follows.
Work in exact arithmetic where possible, sympy or fractions, so the rationals come out as rationals
and the sqrt 5 factors are symbolic rather than floating point.

INCLUDE A CONTROL. Recompute the same quantities under a deliberately phase-BLIND normalisation -
the one the paper says fails - and show that it produces a single constant rather than two. A check
that cannot distinguish the phase-resolved answer from the phase-free one proves nothing, and the
whole claim is that these differ.

IF THE SCRIPT DOES NOT REPRODUCE THE PRINTED VALUES, say so plainly and do not adjust the script to
match the paper. Fix whichever is wrong and report which it was. I would much rather learn now that a
constant is off than after submission.

Also state, in your report, exactly which hypotheses the period-two example needs and confirm the
paper states them where the example is introduced.

VERIFY if you edit any .tex: latexmk -C, delete .aux, rebuild from zero with no command-line macro
definitions; report exit code, page count, undefined control sequences, unresolved references,
unresolved citations, all 0. Do not create planning files. Do not run git commit.
