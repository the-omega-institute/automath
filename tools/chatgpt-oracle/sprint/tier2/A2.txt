GOAL: raise this manuscript to TIER-2 (JFA / ETDS / Trans. AMS / Math. Comp. level). An independent assessment just placed it at TIER-3, with the note that the sharp stable exponent is potentially Tier-2 material but the manuscript is an overextended collection of several partly disconnected parts. This is the target, not a stretch goal.

That assessment named exactly what would move it up a tier:

Prove, for every isotropic alpha-stable kernel and every centred finite-covariance law, the law-by-law decomposition H_{alpha,d}(s) = Q_{alpha,d}(Sigma) s^{-4} + integral Phi( integral_{|x|>s} p_1(y-x/s)/p_1(y) nu(dx) ) Omega_{alpha,d}(dy) + o(s^{-4}). NOTE: the assessment says your best theorem may already be Tier-2 and that STRUCTURE is what caps this paper. Weigh restructuring around the stable-exponent spine against proving this decomposition, and do whichever buys more.

WEAKEST LINK it also named (fix this too, it is what a referee will attack):
The manuscript's breadth: several partly disconnected parts diluting the main line.

HOW TO WORK:
1. Prior art first. Search arXiv API, Crossref, Semantic Scholar, zbMATH before proving anything, and name the nearest prior work. We have shipped results whose core mechanism was already published; do not repeat that.
2. Attempt the tier-up theorem above. If it fails, say precisely at which step and what input is missing — a located failure is a real result.
3. Fix the weakest link: close it rigorously, or weaken the statement to exactly what is proved and record the gap as an explicit open interface.
4. If the tier-up theorem is out of reach, do NOT pad with easier theorems. Instead consider whether the paper's structure is what caps it: an overextended manuscript with partly disconnected parts reads as Tier-3 even when its best theorem is Tier-2. Restructuring around a single spine, with the rest moved to a compiled supplement, can be worth more than another lemma. You are authorised to do that.
5. Integrate only what you proved. Extend artifacts/ verification scripts and run them. Compile with latexmk -pdfxe -interaction=nonstopmode -halt-on-error main.tex until exit 0.

Never state as proved anything you have not proved. Do NOT git commit. Do NOT run pytest at the paper root (memory); run individual test files.

Report at most 40 lines: (a) prior art found; (b) tier-up attempt — proved / failed at which step / restructured instead; (c) weakest link disposition; (d) what you integrated; (e) verifier and compile results.
