TASK: turn papers/publication/2026_folded_histograms_sampling_certificates_parry_mismatch_etds into
the 6-8 page note its referee says it actually is. PRE-AUTHORIZED to edit that directory. Do not commit.

READ FIRST, IN FULL: artifacts/oracle_sprint_FH_r2.md. Read-only evidence; do not edit it or any
artifacts/oracle_*.md file.

THE VERDICT, so you do not soften it. The 48-page manuscript was rejected for significance, not
error: its all-resolution content reduces to a two-letter interval-overlap test followed by an
elementary residue argument in the high-density case. The referee's instruction is to publish it as
what it is - a short note stating one clean observation - and it says plainly why: a six-page note
asks the journal to value one clean observation, while a 48-page manuscript asks the referee to
believe that observation supports a research program.

WHAT TO BUILD. A 6-8 page note built on Theorem 4.1, targeted at The Fibonacci Quarterly, where the
referee estimates roughly 45 percent. Write Proposition 2.6 down explicitly and early - the referee
notes a reader may conclude the whole result is an attractive exercise once it is written down, so
it is better for the note to state it plainly than to look as though it were hiding it.

WHAT MUST GO, and the referee is specific that none of it supports either principal theorem:
 - Theorem 3.2's discrepancy bound, which counts at most 2m interval atoms and multiplies by star
   discrepancy; it holds for any deterministic map on a finite interval partition and has no role in
   the collision classification;
 - Section 5.5's support-budget inequality, which the manuscript itself concedes gives no primitive
   SFT optimizer classification and no finite-block rigidity analogue;
 - Section 6 entirely - it says of itself that it adds no new estimates and only illustrates the
   preceding chain.

THE SECOND RESULT. Theorem 5.14 is the one other independently publishable item, and the referee
would not manufacture a second paper out of Sections 3 or 5.5. Do NOT try to force it into the note.
Move it, verbatim and intact, into sections/_second_paper_thm_5_14.tex, not input by main.tex, with a
header saying what it is and that it is a candidate for a separate note. I will decide separately
whether to develop it.

PRESERVE, DO NOT DESTROY: everything else cut goes verbatim into
sections/_cut_folded_histograms.tex, not input by main.tex, with a one-line header per block.

Do not pad to reach 8 pages and do not use linespread or any line-stretching; the page count must
come from content, and I check for it. Do not claim a broader dynamical theorem - the referee is
explicit that there is none, and the previous version's problem was exactly that framing.

VERIFY, and expect me to redo it: latexmk -C, delete .aux, rebuild from zero with NO command-line
macro definitions; report exit code, page count, undefined control sequences, unresolved references,
unresolved citations, all of which must be 0. Reconcile the bibliography both directions and delete
entries orphaned by the cut. Every shipped .tex under 800 lines. Before any pdftotext grep, check
whether this document drops ff/fi/fl ligatures - some in this repository do, even with -enc UTF-8 -
and search both spellings, printing a control count before concluding anything.
Report what you kept, what you cut, page count before and after, and anything still weak.
Do not create planning files. Do not run git commit.
