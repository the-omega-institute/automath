TASK: give papers/publication/2026_single_primitive_universality_hierarchy a real bibliography and
a real related-work position. You are PRE-AUTHORIZED to edit files in that directory without
asking. Do not stop for approval. Do not commit; I commit after verifying independently.

CONTEXT. This manuscript was just rebuilt from 90 pages to 9 around the results a referee said were
real: fixed-q carry automata for the Zeckendorf fold, the losslessness of the bounded signed-
Fibonacci carry automaton, the primitive transfer matrix T_q, the exact recurrence
S_2(m) = 2 S_2(m-1) + 2 S_2(m-2) - 2 S_2(m-3) with initial values 6, 14, 36, the maximal fibre
heights M_{2s-1} = F_{s+1} and M_{2s} = 2 F_s, and the nonrationality of the bivariate moment
series. Target venue: Discrete Mathematics & Theoretical Computer Science.

The cut removed the L0/L1/L2 hierarchy framing, and it took ALL of the related-work positioning
with it. references.bib is down to two entries. A DMTCS editor will not send that out.

I have independently recomputed both exact claims (m up to 18, with a mutation test); the
mathematics is not in question here. Scholarship and positioning are.

=== WHAT TO DO ===

1. Build the bibliography the paper actually needs. The relevant literatures are: Fibonacci and
   Pisot numeration and normalization by automata (Frougny and coauthors are the core), Zeckendorf
   representations and their combinatorics, transfer-matrix and Perron methods for counting in
   regular languages, and the combinatorics of words / automatic sequences background. Also look
   for prior work on FIBRE SIZES or COLLISION COUNTS of a numeration fold specifically -- if
   someone has already computed a moment like S_2, I need to know before submission, not after.

2. VERIFY EVERY ENTRY. This project has been burned twice: once by a fabricated citation that
   survived several referee rounds, and once by a library-wide audit that found 59 defective DOIs
   across 16 directories, of which 24 RESOLVED CORRECTLY BUT POINTED AT AN UNRELATED PAPER. So:
   - query Crossref (and OpenAlex or Semantic Scholar as a second source) for every DOI you add;
   - confirm the returned title and authors match the entry you wrote, not merely that the DOI
     resolves;
   - report the raw count of entries checked and the raw count that passed, before you conclude
     anything. A path bug and a clean bibliography look identical in a summary.
   Do not add an entry you could not verify. Fewer real references beat more plausible ones.

3. Write the positioning. The introduction currently states the theorems but does not say what was
   known before them. Add a short related-work passage that says, concretely: normalization by
   finite automata in Fibonacci numeration is classical and whose result that is; what is NOT
   classical is the fixed-degree moment transfer and the losslessness argument; and why the
   losslessness step is the substantive one (a truncation that merely contains the likely
   large-weight paths can still delete an accepting path and change every moment). Do not write
   "to the best of our knowledge" as a substitute for having looked.

4. Do not touch the mathematics, the theorem statements, or the proofs. Do not restore any cut
   material. Do not edit sections/_cut_hierarchy_eml_richardson.tex or any artifacts/oracle_*.md
   file -- those are read-only evidence and I diff them against HEAD.

5. Length: the paper may grow to 11-13 pages from the added prose and references. Do not pad it
   further.

6. Verify and report:
   - latexmk -C, delete .aux, rebuild from zero with NO extra command-line macro definitions;
   - report exit code, page count, and the counts of undefined control sequences, unresolved
     references and unresolved citations; all four counts must be 0;
   - reconcile both directions: every \cite key resolves, every entry in references.bib is cited;
   - report the Crossref/OpenAlex verification table from step 2 in full.

7. Tell me at the end anything you found that WEAKENS the paper -- in particular if step 1 turns up
   prior work that already contains one of these results. I would rather hear it from you.

Do not create planning files or notes inside the paper directory. Do not run git commit.
