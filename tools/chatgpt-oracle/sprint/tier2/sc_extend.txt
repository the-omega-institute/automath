TASK: decide papers/publication/2026_scan_projection_address_semantics_sigma_nonexpansion_etds by
attempting the one extension that would make it publishable, or by establishing that it does not
close. PRE-AUTHORIZED to edit that directory. Do not commit.

READ FIRST, IN FULL: artifacts/oracle_sprint_SC_r1.md. Read-only evidence, as is every
artifacts/oracle_*.md file.

THE SITUATION, stated so you do not soften it. The referee rejected this paper and recommended
withdrawal. Theorem 6.1 writes the Bayes error as a matrix identity that follows from having defined
b_r to be the terminal-state conditional Bayes ambiguity - correct bookkeeping, not a theorem about
open dynamics. Theorem 7.2 is the best result present, but its proof is precise power-sum
asymptotics, a strict l^p-type spectral inequality and the standard dependency-graph Chen-Stein
bound; generalized birthday Poisson laws and that approximation are classical, so it is an elegant
verification of a standard scheme rather than a new limit-theorem mechanism.

The referee is explicit that mere cutting is not enough, and that without either a substantial
strengthening or the extension below the project should stop at withdrawal rather than proceed
through cosmetic revision. So do NOT produce a shortened version of the present content.

=== PART 1, THE ATTEMPT ===

Make the Hoelder / open-transfer-operator extension into an actual theorem. The manuscript currently
lists the spectral-gap, forward and backward spectrum, and regularity steps as future work. The
referee names three directions that would take the result beyond the primitive finite-state case,
any one of which suffices: Gibbs or Hoelder survivor laws for cylinder holes; periodic survivor
components; or a joint or multitype collision process. Pick the one you judge most likely to close
and say why you picked it.

If it closes, build the paper the referee describes: the extension together with the collision
theorem, 18 to 22 pages, aimed at a dynamics venue.

If it does NOT close, that is a real and acceptable outcome and I want it stated precisely: which
step fails, at which hypothesis, and whether the obstruction is technical or structural. Do not
produce something that reads like a proof of a step you could not complete. A clean negative report
means the paper is withdrawn, which is the referee's default recommendation, and I would rather have
that than a fourth revision of material that was never enough.

=== PART 2, REGARDLESS OF THE OUTCOME ===

The referee's quantifier audit found the abstract advertises more than the body proves. It omits
q >= 2, omits that R is a nonempty proper subset of Z/qZ, and most importantly omits the condition
that for every i in S and every r in Z/qZ the attainable first-hit times occur both in R - r and in
its complement. Theorem 7.2 further requires the hypotheses inherited from Corollary 7.1 and hence
from Theorem 6.1, that the cardinality of S_infinity is at least 2, independent survivor prefixes
with the common law, and a fixed integer k >= 2. Fix every one of these so the advertised statements
carry their hypotheses. This is the recurring defect across this whole project and it must be
corrected even if the paper is ultimately withdrawn, so that the archived version is honest.

=== VERIFY, and expect me to redo it ===
latexmk -C, delete .aux, rebuild from zero with NO command-line macro definitions; report exit code,
page count before and after, undefined control sequences, unresolved references, unresolved
citations, all 0. No linespread or other line stretching. Every shipped .tex under 800 lines.
Before any pdftotext grep, check whether this document drops ff/fi/fl ligatures and search both
spellings with a control count printed first.
Also scan the source for stripped leading backslashes on control words - a sibling paper in this
repository had five, which compile silently and print the letters into the PDF. Report the raw count.
Report at the end which route you took, whether it closed, and anything you believe is still weak.
Do not create planning files. Do not run git commit.
