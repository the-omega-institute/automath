Do not ask for design approval, do not run any brainstorming or approval workflow, do not propose a
plan and wait. Proceed directly. This is a small, tightly bounded edit.

SCOPE: the ABSTRACT of papers/publication/2026_finite_parts_dynamical_zeta_shifts_finite_type_etds
(the \begin{abstract} block in main.tex), and the corresponding abstract in the submission bundle
mirror if one exists. Nothing else. Do not touch any section file, any theorem, the title, the
introduction, or artifacts/oracle_*.md. Other agents are working on other papers.

THE PROBLEM. The abstract now names the unverified hypothesis (KN85) four times:

 1. "Assume the named statement (KN85) quoted in the paper." - before the headline claim;
 2. "The finite-sampling and algebraic-collision upper bounds rest on (KN85), a named statement
    attributed to a published 1985 paper whose bibliographic record is verified but whose printed
    theorem the authors were unable to consult; the exact statement required is quoted in the
    paper.";
 3. "All results not explicitly carrying (KN85) are unconditional. In particular, the sharp
    squarefree Mahler bound, the collision-jet inequality, the odd-prime multi-collision theorem,
    and the sampling lower bound require no such input.";
 4. "Conditional on (KN85), the squarefree estimate gives a linear prime-primary collision-jet
    theorem."

Nothing there is buried or hedged, and the honesty is the point - but four namings in one abstract
reads as anxious, and this project has just stripped precisely that register out of eight sibling
papers on referee instruction. The conditionality should be stated with complete clarity, once, and
then the abstract should get on with saying what the paper proves.

WHAT TO DO.

Keep, because each earns its place:
 - ONE statement of the dependence, carrying all three facts a reader needs: that the
   finite-sampling and algebraic-collision upper bounds rest on a named statement (KN85); that its
   bibliographic record is verified while its printed theorem could not be consulted; and that the
   exact statement required is quoted in the paper.
 - The scoping sentence naming what does NOT depend on it - the sharp squarefree Mahler bound, the
   collision-jet inequality, the odd-prime multi-collision theorem and the sampling lower bound.
   This is not defensive; it tells the reader which results stand alone, which is information.

Remove or fold in:
 - the bare "Assume the named statement (KN85) quoted in the paper." before the headline claim. The
   headline result must still read as conditional - achieve that by wording the claim itself, for
   instance "under (KN85)" attached to the assertion, rather than a standalone imperative sentence.
 - "All results not explicitly carrying (KN85) are unconditional." - this is a reading instruction
   about the paper's own notation and belongs in the introduction, not the abstract. Check that the
   introduction already says it; if it does not, move it there rather than deleting it.
 - the fourth mention, "Conditional on (KN85), the squarefree estimate gives...", if the sentence
   can carry its conditionality by construction instead. If removing the qualifier would make that
   sentence read as unconditional, KEEP it - accuracy outranks economy, and in that case say so in
   your report.

Target: (KN85) named twice in the abstract, or three times if the fourth sentence genuinely needs
its qualifier. Not four. Do not reduce it to one at the cost of leaving any claim looking
unconditional when it is not.

ABSOLUTE CONSTRAINT: no claim in the abstract may end up reading as unconditional if it is not. If
you cannot reduce the count without that happening, stop, leave the abstract as it is, and report
that the current wording is the honest minimum. That is an acceptable outcome and I would rather
have it than a tidier abstract that misleads.

RULES:
 - Clean rebuild: latexmk -C, remove auxiliaries, alternating passes until latexmk -pdfxe exits 0
   with zero undefined references, zero undefined citations and zero multiply-defined labels for
   main and supplement.
 - Page count should not change materially; report it before and after.
 - Extraction check by pdftotext for filenames, printed command names and "??". Note that pdftotext
   is evidence, not an authority: if a glyph or ligature prevents it from confirming something that
   is correct, say so and supply page-image evidence. Never edit the manuscript to make a check
   pass.
 - If a submission bundle mirrors the abstract, update it identically and rebuild it there too.
 - No revision marks. No scratch files. Do not commit.
 - Kill only your own process tree if you must stop something; never an image-wide taskkill.

REPORT: the abstract before and after, verbatim; the final count of (KN85) mentions and where each
sits; confirmation that no claim reads as unconditional; whether the introduction already carries
the "results not explicitly carrying (KN85) are unconditional" reading instruction, and what you did
about it; page counts and compile results.
