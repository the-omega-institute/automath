Read artifacts/oracle_sprint_A2_reassess.md in full first. Do not ask for design approval, do not
run any brainstorming or approval workflow. Proceed directly to editing.

THE THEOREM PASSED. The expert states that Theorem 4.2 is proved to the standard he had in mind,
that he finds no hidden cancellation at infinity, no missing moment margin, and no weakening of
the six conclusions, and that the annular argument genuinely closes the two Green pairings
separately before combining them. He confirms the d+alpha moment is sufficient exactly at the
endpoint claimed. Do not weaken, restate or renumber anything in Theorem 4.2 or its proof.

VENUE IS SETTLED: the Journal of Functional Analysis, and he says it is now a serious submission
rather than an ambitious lottery ticket. He explicitly would NOT move the 86-page composite
manuscript to a higher generalist venue. Do not retarget it and do not inflate the cover letter.

He will sign off after the following. Make all of them.

 1. INSERT THE OMITTED ONE-LINE JUSTIFICATION AT EQUATION (4.24). Differentiation first gives

        H'(t) = - integral log u_t A f_t + integral (u_t - 1) A g_t,

    and the manuscript writes the second term as integral u_t A g_t. That is correct because
    integral A g_t = 0, which follows from integral A_{eps,R} g_t = 0 together with the L^1
    convergence in Lemma 4.3 - but the line must be stated. He calls it an omitted one-line
    justification, not a gap. While there, also state explicitly that A f_t = -d/dt f_t and
    A g_t = -d/dt g_t hold in L^1, by convolving the stable-kernel identity with the initial
    probability measures.

 2. CORRECT THE "USED EXACTLY ONCE" WORDING. It is accurate that the d+alpha ORDER of the
    assumption enters only in the upper quotient bound. It is not literally true that the
    hypothesis is used nowhere else: part (iv) uses the resulting finite first moments to make
    W_1(mu,nu) finite. That endpoint argument uses only the first-moment consequence, not the
    d+alpha exponent. Remark 4.4 essentially says this already; make the distinction explicit
    everywhere the phrase "used exactly once" or equivalent appears.

 3. ADD TWO PRECEDENTS. These are priority repairs and he is emphatic that without them the
    phrase "moving denominator" itself would sound like the novelty.
      (a) Hilder, Peletier, Sharma and Tse, who write, for two positive solutions of the same
          forward Kolmogorov equation on a finite or countable state space, the identity
          d/dt H(mu_t | rho_t) = - sum_{x,y} rho_t(x) L(x,y) Lambda(mu_t(x)/rho_t(x),
          mu_t(y)/rho_t(y)), and who trace the entropy contraction back to Voigt's 1981
          stochastic-operator result. This is the discrete analogue of exactly our one-sided
          Bregman integrand. Cite both.
      (b) Hirata, Nemoto and Yoshida, "An Integral Representation of the Relative Entropy" (2012,
          Entropy), which treats the Gaussian moving-reference de Bruijn identity and derives an
          integral representation. NOTE CAREFULLY: the manuscript currently cites a DIFFERENT
          Hirata-Nemoto-Yoshida article as reference [46]; the 2012 Entropy paper is absent.
          Check which is which and add the missing one rather than editing the existing entry.
    Neither erases our result. After adding them, the defensible phrasing of the contribution is
    his: "the stable-continuum measure-data domain and endpoint theorem for the classical
    two-solution entropy-production identity". Use that framing, not "moving-denominator theorem".

 4. QUALIFY THE JOHNSON LANGUAGE. Johnson's Theorem 5.1 gives a derivative formula along the same
    stable interpolation in terms of an inner product between an MMSE-type score and a
    Fisher-score difference, and his Open Problem 4 asks for an integral representation of the
    relative entropy specifically "using (17)". Our integrand is the nonlocal logarithmic Bregman
    jump form and we do not identify it with Johnson's score inner product. Therefore:
      - Write the claim as: "We provide an exact nonlocal Bregman integral representation along
        Johnson's symmetric-stable interpolation, under the stated finite-moment and
        finite-initial-entropy domain."
      - Do NOT write that the paper solves Open Problem 4 in the specific form "using (17)".
      - Where the finite-variance Cauchy case is said to "resolve" the problem, change it to
        "resolves it in an equivalent nonlocal form".
      - He notes that a short corollary equating the two derivative expressions wherever
        Johnson's formula applies would remove the distinction entirely. If you can prove such a
        corollary cleanly, add it and then the stronger claim becomes available. If you cannot,
        do NOT add it and do NOT make the stronger claim. Report which you did.

 5. FIX THE ARCHITECTURE. He calls this the most credible route to a "major revision"
    recommendation. The file does not literally lead with Theorem 4.2: the abstract opens with
    the first-unmatched-moment stable asymptotic theorem and the two-flow result is only the
    second paragraph; in the introduction's contribution survey the simultaneous-flow theorem
    appears only after the extensive Cauchy, stable-moment and proxy discussion; and the TITLE
    does not signal stable-flow dissipation at all. A referee could read Theorem 4.2 as a
    six-page field-facing insertion into a paper whose real architecture is the older
    Cayley/stable-asymptotic programme. Restructure so the stable-flow dissipation theorem leads
    the title, the abstract and the contribution survey. The RKHS and lattice demotion is already
    correct - keep it.

 6. SPLIT sec_introduction.tex. It is at 799 lines, one line under the limit, so any further
    edit breaks the rule. Split it into coherent parts as part of the restructuring in item 5.

PRESERVE: the settled priority wording with Chen-Niles-Weed and the Ishige-Kawakami-Michihisa
expansions outside our novelty ledger; the disclaimer that the two-point logarithmic algebra is
not new, with the Hardy-Stein, polarized Hardy-Stein, Sobolev-Bregman and Klimsiak-Rozkosz
citations; the explicit disclaimer of any theorem for all isotropic unimodal Levy semigroups or
all subordinate Brownian motions; the open status of finite first moment, finite initial relative
entropy and intrinsic Dirichlet-form domains; and the rigidity result as ASYMPTOTIC COEFFICIENT
RIGIDITY INSIDE A PRESCRIBED JET-FAITHFUL ANSATZ, which must not drift back toward uniqueness.

RULES:
 - Every claim proved at referee standard; numerics are sanity checks, never a substitute.
 - Keep every .tex under 800 lines; split if needed.
 - Alternating clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined
   references, citations and multiply-defined labels in both documents.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Regenerate
   artifacts/SHA256SUMS (LF) if artifacts change; confirm zero failures from artifacts/.
 - No revision marks anywhere. Do not leave any planning or log file in the paper directory.
 - If you must resume a codex session, resume by explicit session ID. Never `resume --last`: it
   is global across concurrently running agents and can attach to a different paper's session.

REPORT: the inserted justification as typeset; the corrected moment-use wording; the two
precedents as cited, including which Hirata-Nemoto-Yoshida entry was already present and which
you added; the Johnson wording as changed and whether you added the comparison corollary; the new
title, abstract opening and contribution order; how sec_introduction.tex was split and the
resulting line counts; page and theorem counts before and after; compile and test results.
