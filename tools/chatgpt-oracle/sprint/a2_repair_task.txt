Read artifacts/oracle_sprint_A2_referee.md (or
../../../tools/chatgpt-oracle/sprint/result_A2_referee.md) in full first — it is the
referee report and it is correct. Execute its required repairs. Do not ask for design
approval, do not run any brainstorming or approval workflow, and do not add new
theorems beyond the one extension explicitly named in (2) below. Proceed directly to
editing.

Verdict: reject in the present form; he does not object to resubmission elsewhere after
the priority is redefined, the mechanistic results are supplied, and the paper is
rewritten. His findings:

(1) PRIORITY. We omitted Chen-Niles-Weed on the exact KL asymptotics of an arbitrary
    first unmatched moment under Gaussian smoothing — a prior result that corresponds
    structurally and directly to our theorem. He calls this a rejection-level literature
    positioning defect, because as written the reader believes the mechanism "an
    arbitrary first unmatched moment controls the leading KL term" originates here. It
    does not. Verify our current text actually credits this correctly and states the
    boundary; if it does not, fix it.
    After deducting it, what remains credibly ours is: pushing the Gaussian mechanism to
    heavy-tailed isotropic stable kernels; determining the critical absolute-moment
    exponent; proving endpoint sufficiency; and uniform sharpness over the full moment
    class. Note the honest contrast to state: Chen-Niles-Weed assume more (sub-Gaussian
    tails); we exclude alpha = 2 but for 0 < alpha < 2 we determine a far weaker and
    optimal moment condition. State this comparison explicitly rather than leaving it
    implicit.

(2) THE r = 1 GAP — this is the one place to gain, so do it properly. The exclusion of
    r = 1 currently has no mathematical justification. The referee's judgement is that
    the existing proof appears to cover r = 1 already and we simply never explained the
    exclusion. He says only two things remove the objection: bring r = 1 inside the same
    theorem, or state precisely which step fails irreparably. TRY THE FIRST. Work through
    the proof at r = 1 and, if it goes through, extend the theorem statement to include
    it and adjust every downstream statement and the abstract. Only if it genuinely
    fails, identify the exact step and say so in the text. Do not leave "we do not treat
    r = 1" standing.

(3) MECHANISM HIDDEN IN "STANDARD ESTIMATES". The denominator positivity mechanism —
    important to the theorem — is currently buried. Write it out: the denominator
    quotient does not need L-infinity convergence to 1, only a uniform positive lower
    bound, mass normalisation, and L^1 convergence, and these follow in the proof from
    the bounded gradient of log p_1, Jensen, and L^1 continuity of translation. Make that
    explicit in the text instead of citing "standard estimates".

(4) SHARPNESS SECTION IS OVER-COMPRESSED. The construction is correct, but the key
    probability separation, the distance control over all opposite-sign atoms, and the
    Bernoulli KL lower bound are too compressed to verify. State the Bernoulli lower
    bound formally as its own lemma, and split out the moving-ball estimate and the
    Bernoulli data handling as separate lemmas. Keep the counterexample explicit: for
    d + alpha > 2r we construct an example with a finite nonzero r-th difference tensor
    for which a moment hypothesis below the critical exponent cannot control normalised
    KL — this is a construction, not merely a limitation of one proof method.

(5) VENUE. His assessment: this is a strong specialist-journal result; submitting to JFA
    with Theorem 4.19 as the core is a legitimate attempt but rejection is significantly
    more likely than acceptance, and the current priority statement cannot securely
    support a JFA acceptance. After the repairs above, recommend a specific target
    journal in the manuscript's submission_metadata.md and write a matching cover letter.
    Choose the venue by fit and justify the choice in submission_metadata.md.

Rules, all mandatory:
 - Compile after EVERY file you finish, never batched at the end.
 - Keep every .tex under 800 lines; split if needed.
 - Final state: latexmk -pdfxe exit 0, zero undefined references, zero undefined
   citations, zero multiply-defined labels, on a CLEAN rebuild with .aux removed.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts.
 - No revision marks: never write "revised", "corrected", "in this version", or refer to
   earlier drafts.
 - Report at the end: whether r = 1 was brought inside the theorem or provably fails and
   where; the new lemma structure in the sharpness section; page and theorem counts
   before and after; the chosen venue and why; and compile/test results.
