Write a question putting THIS paper's newly proved main theorem to an external expert as a REFEREE, not as an idea generator.

Read main.tex, sec_verified_A2_results.tex (in particular the effective rational Mahler coboundary certificate and the r8 integrations), and artifacts/verify_a5_results.py.

The paper's position: an independent assessment rates it upper TIER-3, with ETDS plausible but not secured. We want to know what a referee at that level would object to, before we submit rather than after.

The question you write must:
 - state the all-order theorem precisely and completely, with all hypotheses, in standard language;
 - state exactly what is proved at each order and what is NOT — in particular that the finite p-group certificate chain, the arbitrary-finite-group multi-operator extension, and the literature-exhaustiveness assertion were all REFUSED as unproved, because key elimination, common-zero or special-value lifting steps remain open;
 - list what is imported and cited versus what is ours;
 - give the verification evidence (37 tests pass).

Then ask him, as a referee for a journal of that level, to answer four things:
 1. Is the theorem correct? Name any step he cannot follow or believes is wrong.
 2. Is it new? Name the nearest prior result and say precisely what the increment is.
 3. Is the increment large enough for that class of journal, and if not, what would have to be added?
 4. What would he demand in revision?

Tell him we would rather hear a hard rejection now with reasons than a soft yes. Ask him to state, for any objection, exactly what would resolve it.

Write next_A5_referee.txt in this paper's directory, in Chinese, formal academic register, at most 45 lines, ending with the standard closing line used in this project's rounds.

Report only the one-sentence summary. Do NOT git commit.
