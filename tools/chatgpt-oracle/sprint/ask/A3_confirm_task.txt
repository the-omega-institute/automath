Write a confirmation question to the external expert and write it ONLY to
tools/chatgpt-oracle/sprint/ask/A3_confirm.txt (relative to D:\omega\automath). No preamble, no
commentary, no other files. Self-contained: he sees only your text plus main.pdf.

CONTEXT, and it matters. He judged that adding only an exact depth formula for fixed theta would
strengthen the paper but not raise its tier, because it would remain one more exact model of our
own self-defined cyclic rank-fold, dismissible in one line as an exact analysis of an
ingeniously constructed family rather than a structural theorem about Pisot numeration,
beta-normalization or beta-dynamics. He said that if reachability required about 50 percent then
no single result was both reachable and sufficient. He then named Theorem A as marginally
reachable at about 0.35, called it the ONLY tier-raising target, and COMMITTED IN ADVANCE to
accepting it as sufficient to raise the tier if proved. It is now proved, and this question asks
him to make good on that.

State the results precisely, reading them from sections/05d_pisot_linear_depth.tex and the
abstract:
 - (A1): for a strictly increasing Pisot numeration system U with nonstandard initial values
   allowed, u_0 = 1, canonical greedy digit set D_U = {0,...,d_U} and canonical normal words,
   assuming U preserves zeros equivalently Condition F on the leading Pisot root, the constant
   C_U < infinity is effectively computable from the recurrence, the canonical digit set and the
   bounded-zero normalization automaton, and for every m >= 2, Phi_{U,m} injective implies
   l_cau(U,m) <= C_U m; equivalently, if no first-coordinate-nonzero state of the difference
   graph reaches a directed cycle then every directed path from that initial set has length at
   most C_U m - 1;
 - (A2) and (A3) for theta the real root of x^3 - 2x^2 + x - 1, giving
   l_cau(theta,m) = 2 floor(m/2) - 1 for m >= 4 and the terminal collision classification, hence
   l_cau(theta,m)/m -> 1;
 - that the abstract states the first main result as UNIFORM OVER STANDARD PISOT NUMERATION
   SYSTEMS, not over our own family.

Say what replaced what: the previous general bound l_cau <= (2 d_U + 1)^{m-1} - 1 was exponential
and came from counting nonzero difference-graph vertices without using Pisot contraction or the
fine structure of the normalization automaton. C_U instead comes from effective root isolation
giving positive algebraic bounds. The fixed-theta results now serve as the matching lower bound
rather than as an isolated example.

Say the results are analytic; the numerical batteries are sanity checks only, and were not used
as evidence. Give the verification: three verifiers with zero failures, zero counterexamples and
zero unexpected collisions, 32 tests, and fixed-theta regression covering complete
collision-graph counts through m = 10 and recursive terminal-word congruences through m = 14.

Then ask him:
 1. Is (A1) correct? Name any step he cannot follow, in particular the effectivity of C_U and
    the reachability argument for first-coordinate-nonzero states.
 2. Is the quantification genuinely over standard Pisot numeration systems, or has the class
    been narrowed in some way that makes it our own family in disguise? This is the failure mode
    we most want checked - a theorem that reads as general but whose hypotheses no standard
    system outside our construction satisfies would be worthless.
 3. Are (A2) and (A3) a genuine matching lower bound, so that the linear bound is sharp in
    growth order?
 4. Is it new? Six of our papers were found to have priority omissions and two of those were on
    work produced in this same push, so treat this as critical: does a linear causal-depth bound
    of this kind for Pisot numeration already exist?
 5. He committed in advance to accepting this as sufficient to raise the tier. Does he still, now
    that he has seen it - and what is the honest venue judgement?

Tell him that if the theorem falls short of what he meant, or if the class is narrower than it
looks, a blunt statement of that is far more useful than honouring the earlier commitment out of
consistency.
