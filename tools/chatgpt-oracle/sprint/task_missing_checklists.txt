TASK: create submission_checklist.md for the two papers that lack one. Pre-authorised; do not
ask, do not brainstorm.

PAPERS:
  papers/publication/2026_projection_ontological_mathematics_core_tams/
  papers/publication/2026_zeckendorf_stable_arithmetic_fibonacci_congruence_online/

Four of the six sprint papers have a submission_checklist.md; these two do not. zeck_arith
additionally records no page count in submission_metadata.md.

USE AN EXISTING ONE AS THE TEMPLATE, for example the brocot or window6 checklist, so the six
files stay comparable in shape.

EVERY LINE MUST BE MEASURED, NOT ESTIMATED. This is the whole point and it is not negotiable.
A checklist in this repository was found to contain, simultaneously: a FAIL stating the author
block was empty when both authors were declared, a page count of "approximately 35 pages"
obtained by scaling source line counts against a real build of 18, and two MSC codes that appear
nowhere in the manuscript. So:

  - Page count: run latexmk from a cleared state and read pdfinfo. State the engine, because the
    count is engine-dependent in at least one paper here. If both engines are plausible, give
    both numbers and name each engine.
  - MSC codes and keywords: copy them from main.tex, character for character. Do not retype from
    memory and do not "tidy" them.
  - Authors: confirm against the rendered PDF front page, not only the source.
  - Bibliography: report cited-key and entry counts you actually computed, and list any unused or
    missing keys by name.
  - Abstract word count: report the tool and the number it printed, and state the journal's limit
    or that the journal states none. Do not invent a limit.

For zeck_arith also add the measured page count and its engine to submission_metadata.md.

A VERIFICATION SCRIPT NOW EXISTS: tools/chatgpt-oracle/sprint/check_submission_records.py. Run
it when you are done. It compares page counts, MSC codes, keywords, authors and target journal
between each record and the source, and it prints MISMATCH with both values. Your checklists must
produce zero MISMATCH. Note that NOFIND means the script could not locate something, which is a
different thing; do not edit a paper to silence a NOFIND.

CONSTRAINTS.
  - Do not modify main.tex or any section file in either paper, except to add nothing at all --
    these two papers' sources are not to be touched.
  - No revision marks, no changelog language.

WHEN DONE: paste both checklists and the output of check_submission_records.py.
