Read artifacts/oracle_sprint_A2_arborder.md in full first - it is the referee report on the
new arbitrary-order theorem. Do not ask for design approval, do not run any brainstorming or
approval workflow. Proceed directly to editing.

THE VERDICT IS GOOD, AND THE WORK IS TO CLOSE IT OUT. He found no counterexample and no
false step; positivity, normalization, first-variation cancellation, the Hessian limit and
the cubic remainder all follow from the stated estimates, including at r = 1 and when
Delta_r = 0. His disposition is major revision on EXPOSITION AND PROOF CLOSURE, with no
rejection on mathematical correctness. He confirms this IS the arbitrary-order law-by-law
nonnegative tail-defect decomposition he named as capable of changing the tier assessment,
and that the retained common jet does not evade that threshold but is precisely what makes
the signed-tail problem tractable. His objection is that the two-background argument is
compressed into a few sentences that conceal most of the estimates doing the work, and that
proxy-entropy finiteness is dispatched too quickly. These are missing written
justifications, not false claims.

MANDATORY, all of them:

 1. Insert the two-background KL perturbation lemma as a STANDALONE, FULLY ABSTRACT lemma,
    with all four derivatives and estimates written out, and then apply it after verifying
    its hypotheses. This is his prescribed clean repair; do not inline it back into the
    theorem proof.
 2. Write the proxy-finiteness proof explicitly. Do not discharge it with "as in
    Theorem 4.25" or any similar pointer.
 3. State and prove the two background estimates explicitly:
        || A_s^lambda - 1 ||_1 = o(1),
        || A_s^nu - A_s^eta ||_1 = o(s^{-r}).
 4. Narrow the novelty language so it distinguishes three separate things:
      - Gaussian first-unmatched-moment KL asymptotics (prior work);
      - stable and fractional density expansions (prior work);
      - the new nonnegative law-by-law proxy decomposition (ours).
    He searched and found prior arbitrary-order Gaussian first-unmatched-moment KL
    asymptotics and prior all-order stable-kernel density expansions, but did NOT find a
    prior nonnegative law-by-law proxy-KL decomposition with this exact vanishing criterion,
    for Gaussian or stable smoothing. He says the priority claim is credible IF STATED
    NARROWLY. State it narrowly.
 5. Remove any claim of canonicity or uniqueness. "An explicit tail-defect energy" is
    supported; "the canonical defect" is not.

STRONGLY ADVISABLE, do both:

 6. State the non-vacuousness corollary. The existing sharpness construction for the uniform
    moment theorem already yields, in the regime d + alpha > 2r, pairs of laws with finite
    r-th moments for which s^{2r} D_KL is unbounded along a subsequence. Through the new
    theorem this gives
        limsup_{s -> infinity} s^{2r} E_{r,s} = +infinity
    for suitable laws. He explicitly wants this stated, because it answers the objection
    that the proxy might have been designed so that its vanishing is tautologically
    equivalent to the desired asymptotic. That objection is serious - do not skip this.
 7. Add a complementary positive example beyond compact support, or a simple moment
    condition implying s^{2r} E_{r,s} -> 0, to clarify the law-by-law interface.

Do NOT attempt, in this pass, the abstract kernel theorem covering stable, Gaussian, Poisson
and other polynomial-tail kernels. He lists it as what would improve the judgement beyond
"JFA-defensible", but it is a separate research target and must not be rushed into this
closure pass.

VENUE. His judgement after closure: "JFA is a defensible target, not an overreach. I would
no longer reject the manuscript for insufficient theorem-level depth." Retarget
submission_metadata.md and the cover letter to the Journal of Functional Analysis, recording
Annales de l'Institut Henri Poincare, Probabilites et Statistiques as the thematically
natural probability alternative. Note in the metadata that acceptance remains uncertain on
presentation, breadth and priority - not on the level of the central increment.

RULES:
 - Every new claim proved at referee standard. No step may rest on numerics; numerical
   checks are sanity checks only and never substitute for a proof.
 - Do not weaken, restate or renumber any existing theorem.
 - Keep every .tex under 800 lines; split if needed.
 - Clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined references, zero
   undefined citations, zero multiply-defined labels.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Any
   archived output must stay deterministic - no wall-clock timings, and two consecutive runs
   byte-identical. Regenerate artifacts/SHA256SUMS (LF endings) if artifacts change.
 - No revision marks anywhere.

REPORT: the abstract lemma statement, the two background estimates as proved, the
non-vacuousness corollary, the positive example or moment condition, the narrowed novelty
wording, page and theorem counts before and after, and compile and test results.
