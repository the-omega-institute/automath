TASK: add a reproducibility statement to four manuscripts. Edit files directly. You are
pre-authorised; do not stop to request approval and do not open a brainstorming discussion.

WHY. The project charter requires that every table, figure and numerical result be
script-generated and one-click reproducible, and lists a reproducibility statement giving code
and data paths as a required component of every paper. Four sprint manuscripts have none. The
apparatus mostly exists already and the manuscripts simply do not point at it, so a referee
learns nothing of it.

THE FOUR PAPERS, under papers/publication/, with their artifact script counts:

    2026_brocot_condensation_critical_fibonacci_renewal_tams          19 scripts
    2026_projection_ontological_mathematics_core_tams                 10 scripts
    2026_scan_projection_address_semantics_sigma_nonexpansion_etds     3 scripts
    2026_cubical_stokes_inverse_boundary_readout_jdsgt                 3 scripts

Do NOT touch zeckendorf_stable_arithmetic; it already has one.

THE ABSOLUTE REQUIREMENT. Run every script before you name it. A named script that does not
run is worse than no statement at all, and this is not negotiable. Run each one, and name only
the scripts that exit 0. If a script fails, leave it out and say in your report which ones you
excluded and why. Do not fix failing scripts as part of this task and do not describe a script
you did not execute.

Note that some scripts must be run from a particular directory. Try the paper root and the
artifacts directory before concluding that a script is broken.

WHAT EACH STATEMENT MUST CONTAIN.

  - The path to each script you verified, and one clause saying what it checks.
  - For brocot specifically: artifacts/REPRODUCE.md and artifacts/SHA256SUMS already exist.
    Reference them; they do not need to be rewritten.
  - An honest scope sentence in the register the papers already use elsewhere, for example
    that the finite checks corroborate but do not replace the proofs. Do not overstate what a
    script establishes.

A MODEL is in this repository. The window6 manuscript has a section "Final remarks and
reproducibility" that names six scripts, says what each does, and states plainly that the
checks "corroborate, but do not replace" the criterion and the cited theorems. Follow that
shape and that register.

CONSTRAINTS.

  - Do not alter any theorem statement, proof, or abstract.
  - No revision marks, no changelog, no "this version corrects" language.
  - Every paper must still compile. Run latexmk on each and fix anything you break.

WHEN DONE: report, per paper, which scripts you ran, which passed, which you excluded and why,
and the latexmk exit status.
