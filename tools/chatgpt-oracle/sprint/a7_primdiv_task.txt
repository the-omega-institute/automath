Read artifacts/oracle_sprint_A7_stdobj.md in full first. Do not ask for design approval, do not
run any brainstorming or approval workflow.

An external expert audited which objects OF THE FIELD this paper's machinery can reach. He found
no theorem-level correctness failure in the 33-page main paper, and he found a target that
completely changes this paper's outlook. The previously recorded ceiling - probability under 3
percent for the almost-all equivalent with coefficient log2/4 - stands and is NOT reopened. This
is a different theorem.

TARGET - large primitive divisor / Fibonacci-Wieferich alternative. Take the exact statement from
his report. In outline: with the manuscript's discriminant-excluding primitive-divisor convention
(p | F_n and p does not divide 5 F_1 ... F_{n-1}, small exceptional indices 1,2,5,6,12), for every
real eps > 0 there is N_eps >= 13 such that for every n >= N_eps at least one of the following
holds - (i) F_n has a primitive prime divisor p >= n^{2-eps}; or (ii) F_n has a primitive prime
divisor q with q^2 | F_n, in which case z(q) = n and q is a Wall-Sun-Sun (Fibonacci-Wieferich)
prime. Consequently, if no Wall-Sun-Sun primes exist, then liminf log P_prim(F_n)/log n >= 2, i.e.
P_prim(F_n) >= n^{2-o(1)}.

No manuscript-defined term occurs in that statement. That is the point.

THE PROOF IS SHORT AND HE SUPPLIES THE CHAIN. Verify each step against the manuscript:
 - Step 1: Lemma 6.3 defines Psi_n(1) = prod_{d|n} F_d^{mu(n/d)}, and (6.15) gives
   log Psi_n(1) = phi(n) log(golden ratio) + O(1).
 - Step 2: for n >= 13 the exact-rank primes counted by a(n) are exactly the primitive prime
   divisors of F_n, and Theorem 6.4 gives a(n) <= (2/log phi + o(1)) phi(n)/log n. The factor 1/2
   is essential - it comes from primitive primes lying in the two progressions +-1 mod n, giving
   factorial spacing rather than merely p >= n-1.
 - Step 3 IS THE ONE MISSING INGREDIENT, and it is classical: the standard Lucas cyclotomic-factor
   theorem Psi_n(1) = c_n U_n^prim with c_n = 1 or c_n a prime divisor of n, hence 1 <= c_n <= n
   and log U_n^prim = phi(n) log phi + O(log n). Granville records the general Lucas-sequence
   statement. The manuscript currently retains only the one-way divisibility
   prod_{z(p)=n} p | Psi_n(1), which bounds the NUMBER of primes but not the size of their
   collective radical.
 - Step 4: if alternative (ii) fails then every primitive prime occurs to exponent one, so
   log U_n^prim <= a(n) log P_prim(F_n); combining with Steps 2 and 3 gives
   log P_prim(F_n) >= (2 - o(1)) log n. Proposition 4.1 identifies q^2 | F_{z(q)} with exceptional
   rank lifting, which for q > 5 is the standard Wall-Sun-Sun condition.

Nothing new has to be proved about exact-rank dispersion, Chebotarev, sieves, or growing ranks.
The work is: state the standard primitive-part theorem in our convention, reconcile the
exceptional factor 5, combine with Theorem 6.4, and state the Wall-Sun-Sun equivalence.

DO THE LITERATURE AUDIT FIRST - HE IS EMPHATIC THAT THIS, NOT THE PROOF, IS THE RISK. His
probability of a correct proof is above 95 percent; the discount to 74 percent is almost entirely
the risk that this alternative, or its conditional n^{2-o(1)} corollary, is already recorded or
would be judged an immediate folklore corollary. So before writing anything, audit Stewart's
primitive-factor papers, Granville's cyclotomic primitive-part discussion, Hong's references, and
surveys of Wall's conjecture, for any statement of the form "no Fibonacci-Wieferich prime implies
P_prim(F_n) >= n^{2-o(1)}". IF SUCH A STATEMENT ALREADY EXISTS, STOP AND REPORT IT - the theorem
then fails the novelty test and we do not want it. This paper already carries a Wagner priority
history; a ninth omission is not acceptable.

Run his short algebraic check too: verify, under exactly our primitive-divisor convention, that
Psi_n(1) / U_n^prim <= n.

Compare honestly with Hong's current large-primitive-divisor result, which gives a primitive
divisor outside n+-1, ..., kappa n +- 1 for each fixed kappa, hence at least (kappa+1)n - 1. Our
exponent 2 - o(1) is far stronger but conditional on the absence of the lifting anomaly. State
plainly that the theorem does NOT resolve Wall's question; its content is that failure of a
near-quadratic primitive divisor cannot occur for an ordinary rank-lifting layer but forces a
Fibonacci-Wieferich anomaly at that exact rank.

HE ALSO DEMOLISHED SEVERAL OF OUR OBJECT NAMES AND YOU MUST FIX THE FRAMING ACCORDINGLY:
 - F_d is the standard Fibonacci cyclotomic (fibotomic) integer Psi_d(1). It must NOT be
   presented as a new object - only the notation is ours.
 - "Atomic" prime powers is misleading: in the divisor lattice an atom is a prime, not p^e with
   e > 1. Rename these rank-jump prime powers.
 - "Birth layer" is local terminology; B_n = alpha^{-1}(n) is the ordinary level set of the rank
   map and taking minimal elements is a generic poset operation.
 - The n-witness cover in the prime case is exactly Wagner's irredundant lcm cover; the ladder
   case is a Fibonacci-decorated predecessor version of standard join irredundance. The
   "unique factorization" part is largely unique factorization in Z; the nontrivial content is the
   admissibility test.
 - The hypergraph/split-graph construction is an encoding of the weighted cover sum, not a new
   result about general transversals or split graphs.
 - Section 7 is a direct corollary of classical primitive-divisor existence, as the paper already
   acknowledges - keep it that way.
Apply these as framing corrections. Do not delete the mathematics; state accurately what is
standard and what is ours.

PRESERVE: the Wagner 1978 and Hearne-Wagner priority wording; the conditional labelling in
Section 6, which he confirms is correct and not a silent claim of the failed asymptotic; the
unconditional two-sided bound on log #M_n; and the existing statement that the requested almost-all
equivalent is NOT proved.

RULES:
 - Every claim proved at referee standard; numerics are sanity checks, never a substitute.
 - Keep every .tex under 800 lines; split if needed.
 - Alternating clean rebuild with .aux removed: latexmk -pdfxe exit 0, zero undefined
   references, citations and multiply-defined labels in both documents.
 - Re-run every artifacts/verify*.py and artifacts/test_*.py; report pass counts. Regenerate
   artifacts/SHA256SUMS (LF) if artifacts change; confirm zero failures from artifacts/.
 - No revision marks anywhere.

REPORT: the literature audit result FIRST and whether the theorem survives it; the Psi_n(1)/U_n^prim
<= n check; the theorem as typeset with the standard primitive-part input cited precisely; the
Hong comparison; the framing corrections as applied; page and theorem counts before and after;
compile and test results.
