$xelatex = 'xelatex -cnf-line=max_in_open=50 %O %S';
$bibtex_use = 2;    # run bibtex when needed, delete .bbl on clean
$force_mode = 1;    # continue multi-pass even if first pass returns non-zero (undefined refs)
