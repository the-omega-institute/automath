\subsection{边界层盲区与联合预算：半径预算、dyadic 深度与端点热核的三端闭包}\label{subsec:typed-address-biaxial-completion-boundary-joint-budget}
上一节把可读性归结为“类型化地址被接纳、记录轴规格合法、正交阈值越界、证书纤维非空”的并行闭包。但在 unitary-slice 的边界层里，仍存在一种更细的失败：对象并非没有被类型化，而是被端点压缩推入固定协议下的结构性盲区。第 \ref{subsec:cdim-three-end-certificate-format} 小节与第 \ref{subsec:cdim-blindspot-budget-theorem} 小节已经把这种失败整理为三条彼此正交、不可互偿的预算轴：半径采样预算、dyadic 地址深度预算与端点热核预算。它们分别控制盘内阈值显影、相位寻址碰撞与端点承载强度，并共同进入统一离线验证器与失败见证模板。本节的任务，是把这三条预算轴压缩为完备相位对象在边界层的统一接口。

\subsubsection{盲区并非缺证，而是压缩后的不可见域}\label{subsubsec:typed-address-biaxial-completion-boundary-blindspot}
边界层盲区的本质，并不是证书对象缺失，而是固定图表下的可见通道把离切片事件压进了一个无法由当前协议显影的薄壳。

\begin{definition}[边界层预算向量]\label{def:typed-address-biaxial-completion-boundary-budget-vector}
对完备相位对象在边界层的读出，定义三端预算向量
\[
\mathbf b^{\partial}:=
\bigl(
\Delta_{\varrho},
b,
N
\bigr),
\]
其中
\[
\Delta_{\varrho}:=1-\varrho_{\max}
\]
是有限半径采样所能达到的边界层厚度预算；\(b\in\NN\) 是 dyadic 地址深度；\(N\in\NN\) 是 Toeplitz--端点热核探针的截断深度。这三项分别约束阈值显影、地址分离与端点原子提取，因而在协议上不可彼此替代。
\end{definition}

\begin{proposition}[盲区的结构性来源]\label{prop:typed-address-biaxial-completion-boundary-blindspot}
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_blindspot}
设存在离切片零点
\[
\rho=\beta+\mathrm{i}\gamma,
\qquad
\beta<\frac12,
\qquad
\delta:=\frac12-\beta>0,
\]
其圆盘像记为 \(w_{\rho}\in\DD\)，并记阈值半径
\[
r_{\rho}:=|w_{\rho}|\in(0,1).
\]
若有限缺陷证书只采样到
\[
\varrho_{\max}\le r_{\rho},
\]
则该证书后端不能强制生成排除该零点的正缺陷见证；换言之，该零点处于当前半径制度的结构性盲区。并且在高高度量纲下存在常数 \(c_1,c_2>0\) 使
\[
c_1\frac{\delta}{\gamma^2}\le 1-r_{\rho}\le c_2\frac{\delta}{\gamma^2}.
\]
因此
\[
\frac{\delta}{\gamma^2}\lesssim \Delta_{\varrho}
\]
可视为“目标事件仍埋在边界层薄壳中”的可审计判据。增加采样点数而不提升 \(\varrho_{\max}\)，不能消除这一盲区。
\end{proposition}
\begin{proof}
由推论 \ref{cor:app-offcritical-horocycle-busemann} 与定理 \ref{thm:xi-offcritical-quadratic-radial-compression}，离切片偏移在固定图表下满足二次径向压缩律，故 \(1-r_{\rho}\asymp \delta/\gamma^2\)。若 \(\varrho_{\max}\le r_{\rho}\)，则所有半径证书都停留在目标缺陷之外，不能在类型层产生排除该点的正缺陷见证。由此盲区具有结构性，而非采样点数不足所致。
\end{proof}

\begin{proposition}[破盲只有两类协议改写]\label{prop:typed-address-biaxial-completion-boundary-unblind-paths}
一旦盲区来自端点二次压缩，则解除盲区只有两类可审计机制：其一是外置前缀并做共动局部化，把高度红移压回与偏移同阶的一阶可见量；其二是进入可逆层析通道，把二维参数重新编码为可反演的一维谱指纹。也就是说，盲区不是再多算一点就会自然消失，而必须通过协议层改写来打破。
\end{proposition}
\begin{proof}
命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 表明，在固定图表与固定半径制度内，仅增加采样点数不能改变盲区的几何来源；而本节后续的 dyadic 深度预算与端点热核预算分别只负责寻址分离与端点承载，也都不直接改变可见量的红移尺度。因此，要想消除盲区，只能改写图表本身，或改写读出对象，使其进入下一节所述的可逆层析接口。
\end{proof}
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_unblind\_paths}

\subsubsection{第一轴：半径预算负责阈值显影}\label{subsubsec:typed-address-biaxial-completion-boundary-radius}
半径预算的职责，不是完成对象的全部识别，而只是把被压入端点薄层的事件显影到可见域内。

\begin{definition}[缺陷端证书]\label{def:typed-address-biaxial-completion-boundary-defect-certificate}
定义缺陷端证书对象为有限集合
\[
C_{\mathrm{def}}
:=
\{(\varrho_j,\varepsilon_j)\}_{j=1}^{M}
\subset (0,1)\times\QQ_{\ge 0},
\]
其语义是：对每个采样半径 \(\varrho_j\)，都可离线核验
\[
D_{\zeta}(\varrho_j)\le \varepsilon_j.
\]
由排斥半径
\[
r_{\ast}(\varrho):=\varrho\,e^{-D_{\zeta}(\varrho)}
\]
可编译得到禁入律；若 \(D_{\zeta}(\varrho)\le \varepsilon\)，则
\[
r_{\ast}(\varrho)\ge \varrho e^{-\varepsilon}.
\]
因此，缺陷端的功能不是直接给出零点，而是把有限半径上的 Jensen 缺陷上界编译成显式的禁入半径下界。
\end{definition}

\begin{corollary}[半径预算的任务边界]\label{cor:typed-address-biaxial-completion-boundary-radius-role}
半径预算只负责判定是否越过目标阈值半径。若
\[
\varrho_{\max}\le r_{\rho},
\]
则无论缺陷端证书写得多精细，都仍不能在类型层排除目标零点。于是半径预算的制度任务是跨越阈值半径，而不是承担地址分离或端点承载的职责。
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_radius\_role}
\end{corollary}
\begin{proof}
这只是定义 \ref{def:typed-address-biaxial-completion-boundary-defect-certificate} 与命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 的直接组合：缺陷端只能输出禁入半径链，而若 \(\varrho_{\max}\) 从未越过 \(r_{\rho}\)，则该链不可能触及目标缺陷。
\end{proof}

\subsubsection{第二轴：dyadic 深度预算负责寻址分离}\label{subsubsec:typed-address-biaxial-completion-boundary-dyadic}
当半径预算已经足以显影事件之后，仍需地址深度去防止端点压缩所强制产生的高重数碰撞。

\begin{definition}[dyadic 地址]\label{def:typed-address-biaxial-completion-boundary-dyadic-address}
令
\[
\upsilon(t)=\pi^{-1}\arctan(t)\in\left(-\frac12,\frac12\right),
\qquad
u_e(t)=\upsilon(t)+\frac12\in(0,1).
\]
对深度 \(b\in\NN\) 定义 dyadic 地址
\[
A_b(t):=\lfloor 2^b u_e(t)\rfloor.
\]
\end{definition}

\begin{proposition}[地址碰撞强制律]\label{prop:typed-address-biaxial-completion-boundary-address-collision}
存在常数 \(c>0\)，对足够大的高度窗 \((T,2T]\) 与任意 \(b\ge 1\)，总可找到某个地址值 \(a\) 使
\[
\#\{\gamma\in(T,2T]:A_b(\gamma)=a\}
\ge
c\,\frac{T^2\log T}{2^b}.
\]
因此若希望在高度窗内达到地址级单射读出，必须满足
\[
2^b\gtrsim T^2\log T,
\qquad\text{等价地}\qquad
b\ge 2\log_2 T+\log_2\log T-O(1).
\]
这说明 dyadic 深度不足时，碰撞并非偶然，而是由端点压缩同一 \(T^{-2}\) 量纲强制出来的。
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_address\_collision}
\end{proposition}
\begin{proof}
这正是定理 \ref{thm:xi-phase-prefix-collision-lower-bound} 在定义 \ref{def:typed-address-biaxial-completion-boundary-dyadic-address} 的地址记号下的直接重述。端点压缩把高度窗映到 \(T^{-2}\) 级窄弧，而 dyadic 盒的有限深度只会把该窄弧组织为高占用柱事件，从而得到所示强制碰撞下界。
\end{proof}

\begin{corollary}[深度预算不能替代半径预算]\label{cor:typed-address-biaxial-completion-boundary-depth-not-radius}
虽然
\[
\Delta_{\varrho}\asymp \frac{\delta}{\gamma^2}
\qquad\text{与}\qquad
2^{-b}\asymp (T^2\log T)^{-1}
\]
都源于同一端点二次压缩量纲，但二者分别作用于阈值显影与寻址分离。因此提升 \(b\) 不能替代提升 \(\varrho_{\max}\)，反之亦然。
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_depth\_not\_radius}
\end{corollary}
\begin{proof}
命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 与命题 \ref{prop:typed-address-biaxial-completion-boundary-address-collision} 分别给出这两条预算轴的必要量纲。前者控制是否触及目标缺陷，后者控制是否在已触及的前提下实现地址级分离，故其作用对象不同，不能相互替代。
\end{proof}

\subsubsection{第三轴：端点热核预算负责端点承载强度}\label{subsubsec:typed-address-biaxial-completion-boundary-endpoint}
边界层的第三条预算轴不负责显影或寻址，而负责对端点 \(\xi=-1\) 处的承载强度作单调审计。

\begin{definition}[端点热核量]\label{def:typed-address-biaxial-completion-boundary-endpoint-heat}
在 Toeplitz--PSD（Carath\'eodory）语义中，对 Toeplitz 主子矩阵
\[
\mathbf{T}_N=(c_{j-k})_{0\le j,k\le N}
\]
与二项式向量 \(b^{(N)}\) 定义
\[
a_N:=\frac{\bigl(b^{(N)}\bigr)^{\mathsf T}\mathbf{T}_N\,b^{(N)}}{4^N}.
\]
它满足严格恒等式
\[
a_N=\int_{\TT}\left|\frac{1-\xi}{2}\right|^{2N}\,d\mu(\xi)\ge 0.
\]
\end{definition}

\begin{proposition}[单调极限与指数误差]\label{prop:typed-address-biaxial-completion-boundary-endpoint-heat}
端点热核量单调下降并收敛到端点原子质量：
\[
a_N\downarrow \mu(\{-1\})\qquad(N\to\infty).
\]
若 \(K\subset \TT\setminus\{-1\}\) 为闭集，且
\[
q(K):=\sup_{\xi\in K}\left|\frac{1-\xi}{2}\right|\in(0,1),
\]
则有误差界
\[
0\le a_N-\mu(\{-1\})\le q(K)^{2N}\mu(K)+\mu\bigl(\TT\setminus(\{-1\}\cup K)\bigr).
\]
在支撑受控情形下进一步简化为
\[
0\le a_N-\mu(\{-1\})\le q(K)^{2N}\mu(K).
\]
因此，给定目标误差 \(\eta>0\)，若支撑受控，则只需
\[
N\ge N_{\min}(K,\eta):=
\frac{\log(\mu(K)/\eta)}{2\log q(K)^{-1}}
\]
便可保证
\[
a_N-\mu(\{-1\})\le \eta.
\]
这把 \(N\) 明确解释为端点承载的分辨率深度。
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_endpoint\_heat}
\end{proposition}
\begin{proof}
单调极限由定理 \ref{thm:xi-endpoint-heat-kernel-probe} 给出；指数型误差界由推论 \ref{cor:xi-endpoint-heat-kernel-exp-error} 给出。将误差上界反解为 \(N\) 的下界，即得 \(N_{\min}(K,\eta)\) 的显式表达。
\end{proof}

\begin{corollary}[端点预算与前两轴正交]\label{cor:typed-address-biaxial-completion-boundary-endpoint-orthogonal}
端点热核预算不通过提升 \(\varrho_{\max}\) 去触发盘内 Jensen 贡献，也不通过增加 \(b\) 去消除地址碰撞；它只对 \(\mu(\{-1\})\) 作单调抽取。因此它对应一条独立记录轴，不能由半径预算或深度预算替代。
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_endpoint\_orthogonal}
\end{corollary}
\begin{proof}
定义 \ref{def:typed-address-biaxial-completion-boundary-endpoint-heat} 中的 \(a_N\) 只依赖 Toeplitz 数据与端点核，既不改变可见半径阈值，也不改变 dyadic 地址盒的占用结构。其作用对象与前两轴完全不同，故预算正交。
\end{proof}

\subsubsection{三端证书对象与统一离线验证器}\label{subsubsec:typed-address-biaxial-completion-boundary-verifier}
三条预算轴一旦并置，就需要一个把地址端、缺陷端与线性代数端同时读入的统一验证接口。

\begin{definition}[三端证书对象]\label{def:typed-address-biaxial-completion-boundary-triple-certificate}
把边界层读出的输入统一写为
\[
C^{\partial}=(C_{\mathrm{addr}},C_{\mathrm{def}},C_{\mathrm{lin}}),
\]
其中
\[
C_{\mathrm{addr}}=(L,\theta,b)\in \operatorname{Addr}_{\mathrm{US}},
\qquad
\operatorname{Addr}_{\mathrm{US}}:=\{(L,\theta,b):L>1,\ \theta\in\RR,\ b\in\NN\},
\]
并由三值偏函数
\[
\operatorname{Read}_{\mathrm{US}}:\operatorname{Addr}_{\mathrm{US}}\rightharpoonup
\{\mathrm{NonZero},\mathrm{RootInterval},\mathrm{NULL}\}
\]
给出确定性语义；
\[
C_{\mathrm{def}}=\{(\varrho_j,\varepsilon_j)\}_{j=1}^{M}
\]
是缺陷端禁入律证书；
\[
C_{\mathrm{lin}}=
\bigl(
\mathbf{T}_N,\ \mathbf{T}_N\succeq 0,\ a_N,\ \text{必要的谱裕度或一致维数上界信息}
\bigr)
\]
是线性代数端证书。
\end{definition}

\begin{definition}[统一离线验证器]\label{def:typed-address-biaxial-completion-boundary-verifier}
定义边界层验证器
\[
\operatorname{Ver}^{\partial}:
\bigl(C_{\mathrm{addr}},C_{\mathrm{def}},C_{\mathrm{lin}}\bigr)
\longmapsto
\{\mathrm{Accept},\mathrm{Reject},\mathrm{NULL}\},
\]
其组合原则是：先核验地址端的类型合法性与读出语义，再把缺陷端编译为禁入半径链，最后用 Toeplitz--PSD 与端点热核量检查端点承载强度；若三端局部证书可在同一协议口径下闭合，则接受，否则输出失败见证。这里的拒绝并不是“数值上看起来不稳”，而是一个可离线复核的对象层事件。
\end{definition}

\subsubsection{联合预算定理与失败见证}\label{subsubsec:typed-address-biaxial-completion-boundary-joint-theorem}
三端预算在协议层闭合时，才真正构成“非盲读出”的充足条件。

\begin{theorem}[三端预算的最小联合充分条件]\label{thm:typed-address-biaxial-completion-boundary-joint-sufficiency}
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_joint\_sufficiency}
设目标是在高度窗 \((T,2T]\) 内，对某个离切片偏移量 \(\delta\) 级别的边界层事件做非盲读出，并把端点原子误差压到 \(\eta\)。则在支撑受控或谱隙存在的前提下，一组可操作的联合充分条件可写为
\[
\Delta_{\varrho}\lesssim \frac{\delta}{\gamma^2},
\qquad
2^b\gtrsim T^2\log T,
\qquad
N\ge N_{\min}(K,\eta).
\]
更具体地说，
\[
\varrho_{\max}>r_{\rho}
\]
负责越过结构性半径盲区；
\[
b\ge 2\log_2 T+\log_2\log T-O(1)
\]
负责消除高度窗中的地址碰撞强制；
\[
q(K)^{2N}\mu(K)\le \eta
\]
负责把端点热核误差压到可审计阈值内。
当三端预算同时闭合，且 \(C_{\mathrm{lin}}\) 中的 Toeplitz--PSD 核验通过时，统一离线验证器 \(\operatorname{Ver}^{\partial}\) 可把“边界层可读”编译为一个非 \(\mathrm{NULL}\) 的有限证书对象；若任一条件失败，则必须输出失败见证，而不能以别轴预算代偿。
\end{theorem}
\begin{proof}
第一条条件由命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 给出；第二条由命题 \ref{prop:typed-address-biaxial-completion-boundary-address-collision} 给出；第三条由命题 \ref{prop:typed-address-biaxial-completion-boundary-endpoint-heat} 给出。三者共同输入定义 \ref{def:typed-address-biaxial-completion-boundary-verifier} 的三端验证流程，并由上一节的正交预算律保证不可跨轴替代。
\end{proof}
\leanverified{paper\_typed\_address\_biaxial\_completion\_boundary\_joint\_sufficiency}

\begin{definition}[失败见证的边界层模板]\label{def:typed-address-biaxial-completion-boundary-witness-template}
沿用定义 \ref{def:typed-address-biaxial-completion-failure-witness} 的失败见证包
\[
W=
\bigl(
\operatorname{tag},
\operatorname{subtag},
\operatorname{loc},
\operatorname{lb},
\operatorname{aux}
\bigr),
\]
并把其中的 \(\mathrm{Hard}\) 细分为边界层副标签 \(\mathrm{RadBlind}\) 与 \(\mathrm{Addr}\)。对边界层三端预算，可取以下规范模板：
\begin{enumerate}
  \item 若半径端失败，则取
  \[
  \operatorname{tag}=\mathrm{Coll},
  \qquad
  \operatorname{subtag}=\mathrm{RadBlind},
  \]
  并把
  \[
  \operatorname{loc}=\varrho_{\max},
  \qquad
  \operatorname{lb}:\ \Delta_{\varrho}\lesssim \delta/\gamma^2
  \]
  连同 \(r_{\rho}\) 的区间下界放入 \(\operatorname{aux}\)；
  \item 若深度端失败，则取
  \[
  \operatorname{tag}=\mathrm{Coll},
  \qquad
  \operatorname{subtag}=\mathrm{Addr},
  \]
  并把
  \[
  \operatorname{loc}=(L,\theta,b),
  \qquad
  \operatorname{lb}: b\ge 2\log_2 T+\log_2\log T-O(1)
  \]
  以及占用下界
  \[
  c\,T^2\log T/2^b
  \]
  装入 \(\operatorname{aux}\)；
  \item 若线性代数端仍不足以压低端点误差或缺乏一致维数上界，则取
  \[
  \operatorname{tag}\in\{\mathrm{Prot},\mathrm{Dim}\},
  \]
  并把
  \[
  \operatorname{loc}=N,
  \qquad
  \operatorname{lb}: N\ge N_{\min}(K,\eta)\ \text{或需要一致维数上界 }D
  \]
  连同 \(q(K)\)、\(a_N\)、谱裕度、负惯性或区间算术外向舍入证书打入 \(\operatorname{aux}\)。
\end{enumerate}
这些量都必须由有限有理或整数数据给出，使第三方验证器能在实现无关意义下复核拒绝的必要性。
\end{definition}

\begin{proposition}[边界半径阈值的区间认证正规形]\label{prop:distill-euclid-elements-boundary-radius-interval-normal-form}
\textbf{Dependency status: conditional audit.}
沿用命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 的记号。设半径端审计并不直接给出真实阈值 \(r_\rho\)，而只给出有理区间证书
\[
\mathsf R_\rho=(\underline r_\rho,\overline r_\rho)\in\QQ^2,
\qquad
0<\underline r_\rho\le r_\rho\le \overline r_\rho<1.
\]
则半径端只能作如下三值判定：
\begin{enumerate}
  \item 若 \(\varrho_{\max}\le \underline r_\rho\)，则必有 \(\varrho_{\max}\le r_\rho\)，半径端可输出 \(\mathrm{RadBlind}\) 失败见证；
  \item 若 \(\varrho_{\max}>\overline r_\rho\)，则必有 \(\varrho_{\max}>r_\rho\)，半径阈值已被穿过，但这只关闭半径盲区，不替代 dyadic 深度端或端点热核端；
  \item 若 \(\underline r_\rho<\varrho_{\max}\le\overline r_\rho\)，则半径端处于 \(\mathrm{RadUndec}\) 区间不决状态。此时任何只根据下界 \(\underline r_\rho\) 就宣称阈值已穿过的验证器，或任何只根据上界 \(\overline r_\rho\) 就宣称仍在盲区的验证器，都不是对区间证书 \(\mathsf R_\rho\) 声明的健全后端。
\end{enumerate}
因此，定义 \ref{def:typed-address-biaxial-completion-boundary-verifier} 的统一离线验证器若使用区间半径证书，必须把 \(\mathrm{RadUndec}\) 作为需要加细区间或返回 \(\mathrm{NULL}\) 的正规失败，而不能把它升级为 \(\mathrm{Accept}\)。
\end{proposition}

\begin{proposition}[三端区间证书的轴向充分接受律]\label{distill:euclid_elements-finite_construction_certificate_families-boundary-axiswise-sufficient-acceptance}
沿用定义 \ref{def:typed-address-biaxial-completion-boundary-triple-certificate} 与定义 \ref{def:typed-address-biaxial-completion-boundary-verifier}。设边界层目标半径只以有理区间证书
\[
\mathsf R_\rho=(\underline r_\rho,\overline r_\rho),
\qquad
0<\underline r_\rho\le r_\rho\le\overline r_\rho<1
\]
给出。再设地址端给出一个有限分离谓词
\[
\mathsf{Sep}_T(b)
\]
其语义是：在高度窗 \((T,2T]\) 与当前 dyadic 地址语法内，深度 \(b\) 已通过离线 collision 表或已登记分离下界的核验。端点端给出有限数据
\[
(K,q_K,M_K,N,\eta),
\qquad
0<q_K<1,\quad M_K\ge0,\quad \eta>0,
\]
并给出 Toeplitz--PSD 证书 \(\mathbf T_N\succeq0\)。若三条轴向条件同时成立：
\[
\varrho_{\max}>\overline r_\rho,
\qquad
\mathsf{Sep}_T(b),
\qquad
q_K^{2N}M_K\le\eta,\quad \mathbf T_N\succeq0,
\]
则统一离线验证器可输出一个非 \(\mathsf{NULL}\) 的边界层接受证书。

此外，该接受规则关于三端预算单调：若
\[
\varrho'_{\max}\ge\varrho_{\max},\qquad b'\ge b,\qquad N'\ge N
\]
并且 \(\mathsf{Sep}_T(b)\Rightarrow\mathsf{Sep}_T(b')\) 是该地址端登记的单调规则，则同一半径区间与同一端点支撑控制下，\((\varrho'_{\max},b',N')\) 仍满足接受条件。若半径端处于
\[
\underline r_\rho<\varrho_{\max}\le\overline r_\rho
\]
的区间不决状态，或者某一轴缺少对应证书，则本命题不推出拒绝的必要性；它只说明上述有限三端账本是充分接受证书。
\end{proposition}

\begin{proposition}[三端边界预算的轴向 overflow 正规形]\label{distill:euclid_elements-lifted_congruence_and_residual_rigidity_families-boundary-axiswise-overflow-normal-form}
\textbf{Dependency status: conditional audit.}
沿用定义 \ref{def:typed-address-biaxial-completion-boundary-budget-vector}、定义 \ref{def:typed-address-biaxial-completion-boundary-triple-certificate} 与定义 \ref{def:typed-address-biaxial-completion-boundary-verifier}。固定一份区间半径证书
\[
\mathsf R_\rho=(\underline r_\rho,\overline r_\rho),\qquad
0<\underline r_\rho\le r_\rho\le\overline r_\rho<1,
\]
一个地址端单调分离谓词 \(\mathsf{Sep}_T(b)\)，以及端点支撑控制数据
\[
(K,q_K,M_K,N,\eta),\qquad 0<q_K<1,\quad M_K\ge0,\quad \eta>0.
\]
再设 Toeplitz--PSD 可接受深度集合
\[
\mathsf{PSD}\subseteq\NN
\]
已经作为线性代数端证书登记。定义充分接受域
\[
\mathfrak A
:=
\left\{(\varrho_{\max},b,N):
\varrho_{\max}>\overline r_\rho,\ \mathsf{Sep}_T(b),\ q_K^{2N}M_K\le\eta,\ N\in\mathsf{PSD}
\right\}.
\]
若 \((\varrho_{\max},b,N)\notin\mathfrak A\)，则至少出现以下轴向 overflow 见证之一：
\begin{enumerate}
  \item \(\mathrm{RadOverflow}\)：\(\varrho_{\max}\le\overline r_\rho\)；
  \item \(\mathrm{AddrOverflow}\)：\(\mathsf{Sep}_T(b)\) 未通过；
  \item \(\mathrm{HeatOverflow}\)：\(q_K^{2N}M_K>\eta\) 或 \(N\notin\mathsf{PSD}\)。
\end{enumerate}
并且每一类 overflow 都不可由其余两轴补偿：若某一失败不等式在该轴上保持不变，则任意增大另外两项预算都不能使三元组进入 \(\mathfrak A\)。
\end{proposition}

\begin{proposition}[三端边界证书的重叠账本门控正规形]\label{distill:euclid_elements-diagrammatic_compatibility_audit_families-boundary-overlap-gated-triple-verifier}
令 \(\mathcal N\) 为一组边界图表的有限 nerve，令 \(A\) 与 \(\bar A\) 为有限阿贝尔群，并固定群同态
\[
\kappa:A\longrightarrow \bar A.
\]
对每个顶点 \(i\in\mathcal N_0\)，假设已经给出一份三端边界局部证书，包含区间半径证书
\[
\mathsf R_i=(\underline r_i,\overline r_i),\qquad
0<\underline r_i\le r_i\le\overline r_i<1,
\]
半径读数 \(\varrho_i\)、地址分离谓词 \(\mathsf{Sep}_{T_i}(b_i)\)、端点支撑控制数据
\[
(q_i,M_i^{\rm end},N_i,\eta_i),\qquad 0<q_i<1,\quad M_i^{\rm end}\ge0,\quad \eta_i>0,
\]
以及 Toeplitz--PSD 可接受深度集合 \(\mathsf{PSD}_i\subseteq\NN\)。称第 \(i\) 个局部证书轴向接受，若
\[
\varrho_i>\overline r_i,\qquad
\mathsf{Sep}_{T_i}(b_i),\qquad
q_i^{2N_i}M_i^{\rm end}\le\eta_i,\qquad
N_i\in\mathsf{PSD}_i,\qquad
\mathbf T_{N_i}\succeq0.
\]
再设每条有向重叠边 \(ij\in\mathcal N_1^{\rm or}\) 携带一个类型化重叠残差
\[
h_{ij}\in A,\qquad h_{ji}=-h_{ij},
\]
并记
\[
\bar h_{ij}:=\kappa(h_{ij})\in\bar A.
\]
对有向二单形 \((i,j,k)\) 定义
\[
(\delta\bar h)_{ijk}:=\bar h_{jk}-\bar h_{ik}+\bar h_{ij}.
\]
定义重叠门控验证器 \(\operatorname{Ver}^{\partial}_{\rm ov}\) 输出 \(\mathsf{Accept}\) 当且仅当：所有顶点局部证书均轴向接受；所有二单形满足 \(\delta\bar h=0\)；并且由 \(\bar h\) 定义的类满足
\[
[\bar h]=0\in \check H^1(\mathcal N;\bar A).
\]
则成立：
\begin{enumerate}
  \item 若 \(\operatorname{Ver}^{\partial}_{\rm ov}=\mathsf{Accept}\)，则存在顶点势 \(a_i\in\bar A\) 使
  \[
  \bar h_{ij}=a_j-a_i\qquad(ij\in\mathcal N_1^{\rm or}).
  \]
  因而经由同一顶点势重置后，所有 reduced overlap residual 同时消失；
  \item 若所有顶点局部证书均轴向接受但某个二单形上 \((\delta\bar h)_{ijk}\ne0\)，则失败是三重重叠 incidence/cocycle failure，此时不得书写 \([\bar h]\) 作为上同调类；
  \item 若所有顶点局部证书均轴向接受且 \(\delta\bar h=0\)，但 \([\bar h]\ne0\)，则全部局部图表与三重重叠检查均已通过，而全局图解闭合仍因 reduced holonomy obstruction 失败；
  \item 若 \(\check H^1(\mathcal N;\bar A)=0\)，则任意通过前两道门的 reduced overlap ledger 自动通过 holonomy 门。一般情形下，图解证明闭合的等价条件正是显式给出上述顶点势，亦即显式杀死 monodromy。
\end{enumerate}
\end{proposition}
\begin{proof}
轴向接受条件逐项包含半径端、地址端与端点热核端的充分接受不等式，因此每个顶点处的局部读出先作为有限证书对象成立。这一步只使用已经登记的区间半径、分离谓词、指数尾界与 Toeplitz--PSD 数据，没有使用图形直观。

接着，边 \(ij\) 上的可见重叠关系被替换为元素 \(h_{ij}\in A\)，再经同态 \(\kappa\) 送入 reduced 系数群 \(\bar A\)。由于 \(\kappa\) 是群同态，\(\bar h\) 是 \(\bar A\)-值一上链；但只有当 \(\delta\bar h=0\) 时，它才是 \v Cech 一 cocycle。因此在 \(\delta\bar h\ne0\) 的情形中，上同调类根本未定义，失败只能记录为三重重叠 incidence/cocycle failure。

若 \(\delta\bar h=0\)，则 \(\bar h\in Z^1(\mathcal N;\bar A)\)。按 \(\check H^1=Z^1/B^1\) 的定义，\([\bar h]=0\) 当且仅当存在 \(a\in C^0(\mathcal N;\bar A)\) 使 \(\bar h=\delta a\)，即
\[
\bar h_{ij}=a_j-a_i
\]
对每条有向边成立。把 ledger 替换为同调等价的 \(\bar h-\delta a\) 后，所有 reduced 边残差同时为零，故所有重叠粘接在 reduced 通道中闭合。反过来，若存在这样的顶点势，则 \(\bar h=\delta a\)，从而 \(\delta\bar h=\delta^2a=0\) 且 \([\bar h]=0\)。最后，若 \(\check H^1(\mathcal N;\bar A)=0\)，每个 cocycle 都是 coboundary，故 holonomy 门自动通过。该证明正是 diagram-to-ledger conversion：每个可见重叠先被类型化为边账本，再分别审计 cocycle 与 monodromy。证毕。
\end{proof}

\begin{proof}
充分接受域 \(\mathfrak A\) 是四个条件的交。若三元组不在其中，则至少有一个条件失败。前三类标签正是把失败条件按半径端、地址端与端点线性代数端分组后的穷尽列表。因此任一非接受三元组必携带至少一个列出的 overflow 见证。

下面证明不可补偿性。若 \(\mathrm{RadOverflow}\) 成立，即 \(\varrho_{\max}\le\overline r_\rho\)，则 \(\mathfrak A\) 的第一条条件已经失败；无论如何改变 \(b\) 与 \(N\)，只要 \(\varrho_{\max}\) 保持不变，仍有 \(\varrho_{\max}\le\overline r_\rho\)，故不能进入 \(\mathfrak A\)。这正是命题
\ref{prop:distill-euclid-elements-boundary-radius-interval-normal-form}
中区间半径端的健全性要求。

若 \(\mathrm{AddrOverflow}\) 成立，则当前 dyadic 地址语法下 \(\mathsf{Sep}_T(b)\) 未被离线核验。\(\mathfrak A\) 的定义要求同一个 \(b\) 通过该谓词；因此在保持 \(b\) 不变时，提高半径或端点热核深度都不能补出地址分离账本。

若 \(\mathrm{HeatOverflow}\) 成立，则要么指数尾界 \(q_K^{2N}M_K\le\eta\) 失败，要么 Toeplitz--PSD 深度证书缺失。二者均属于端点承载轴的条件。保持 \(N\) 与其 PSD 登记状态不变时，半径穿越与 dyadic 分离不会改变端点热核误差估计，也不会生成新的 Toeplitz--PSD 证书，故仍不能进入 \(\mathfrak A\)。

于是接受域的补集在当前有限三端账本中被这三个轴向 overflow 面覆盖；该覆盖是协议层面的结构分类，而非数值近似误差。证毕。
\end{proof}

\begin{proof}
半径端由区间包含给出。若 \(\varrho_{\max}>\overline r_\rho\ge r_\rho\)，则有限半径采样已经越过真实阈值半径，故命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 中的半径盲区不再由该目标点触发。注意这里只使用区间上界，因此没有把 \(\underline r_\rho<\varrho_{\max}\le\overline r_\rho\) 的不决情形误判为通过。

地址端不是由本命题重新证明渐近必要下界，而是由已登记的有限谓词 \(\mathsf{Sep}_T(b)\) 承担。该谓词的作用正是把 dyadic 地址图上的可见 collision 检查转化为 incidence ledger：一旦 \(\mathsf{Sep}_T(b)\) 被核验，验证器便可在当前高度窗中把地址端视为已分离。

端点端由命题 \ref{prop:typed-address-biaxial-completion-boundary-endpoint-heat} 的误差估计给出。在支撑受控记录 \((K,q_K,M_K)\) 下，\(q_K^{2N}M_K\le\eta\) 保证端点热核尾误差不超过 \(\eta\)，而 \(\mathbf T_N\succeq0\) 给出 Toeplitz--PSD 的线性代数合法性。于是三端分别给出半径穿越、地址分离与端点承载控制；定义 \ref{def:typed-address-biaxial-completion-boundary-verifier} 的组合原则正是把三端局部证书在同一协议口径下闭合后输出接受。

单调性逐项成立：增大 \(\varrho_{\max}\) 保持 \(\varrho_{\max}>\overline r_\rho\)；地址端单调性是题设登记规则；而因 \(0<q_K<1\)，若 \(N'\ge N\)，则 \(q_K^{2N'}M_K\le q_K^{2N}M_K\le\eta\)。因此三端接受条件保持。最后一句只是说明本命题给出充分规则而非所有健全验证器的必要条件。证毕。
\end{proof}

\begin{proof}
前两条只是区间包含关系的直接推论。若 \(\varrho_{\max}\le\underline r_\rho\le r_\rho\)，则所有半径采样仍停在阈值半径内侧，故命题 \ref{prop:typed-address-biaxial-completion-boundary-blindspot} 的结构性盲区结论适用。若 \(\varrho_{\max}>\overline r_\rho\ge r_\rho\)，则半径采样已经越过真实阈值。

对第三条，假设 \(\underline r_\rho<\varrho_{\max}\le\overline r_\rho\)。取
\[
r^-:=\frac{\underline r_\rho+\varrho_{\max}}2,
\qquad
r^+:=\varrho_{\max}.
\]
则 \(r^-\in[\underline r_\rho,\overline r_\rho]\) 且 \(\varrho_{\max}>r^-\)，而 \(r^+\in[\underline r_\rho,\overline r_\rho]\) 且 \(\varrho_{\max}\le r^+\)。同一份区间证书因此同时允许一个已穿过阈值的真实半径和一个未穿过阈值的真实半径。任何单侧结论都不由该证书逻辑蕴含。证毕。
\end{proof}

\subsubsection{小结：破盲是三端闭包，而不是单轴加码}\label{subsubsec:typed-address-biaxial-completion-boundary-summary}
本节可以压缩为如下闭包原则：
\[
\text{non-blind readable}
\Longleftarrow
(\text{radius threshold crossed})
\wedge
(\text{dyadic separation crossed})
\wedge
(\text{endpoint mass audited}).
\]

其中 \(\Delta_{\varrho}\) 控制阈值显影，\(b\) 控制地址碰撞，\(N\) 控制端点承载。三者都来自同一个边界压缩制度，却分别落在不同的正交轴上；因此它们共享量纲来源，却不共享可替代性。半径预算不过阈，零点仍埋在边界层盲区；深度预算不过阈，高度窗内必然发生地址碰撞；端点热核预算不过阈，端点原子质量仍不能被稳定抽取。只有三端证书在统一离线验证器里同时闭合，上一节的可读性判据才真正穿过了边界层。
