\subsection{第一个具体实例：window-\texorpdfstring{$6$}{6} 的最小可审计粗糙时空样例}\label{subsec:typed-address-biaxial-completion-window6-auditable-rough-spacetime-example}
上一节给出了首选最小实例簇 \(\mathcal I_{\min}\) 及其诱导的粗糙时空四元组
\[
\mathfrak G=(\preceq,\tau,d_{\mathrm{res}},\Omega).
\]
本节不再停留在模板层，而是把这套结构第一次压到一个最小、有限、可逐项复算的样例上。
选择 window-\(6\) 的原因并不在于它已经“最像物理”，而在于它是本文中第一个同时具备如下四类对象的窗口：其一，可见层已经非平凡，\(|X_6|=21\) 且纤维直方图严格分裂为 \(2{:}8,3{:}4,4{:}9\)；其二，boundary 扇区已经给出真正的二层覆盖与 \((\ZZ/2\ZZ)^3\) 中心寄存器；其三，精确反演预算与 anomaly 预算已经严格分叉为 \(2\) 与 \(21\) 两个不同层级；其四，高阶碰撞、mixed collision、phase--ledger 分裂与动态 prime-register 的接口都已在前文对象化。
因此，window-\(6\) 不是最终物理模型，而是第一个既小到能完全审计、又大到足以同时承载粗糙可见量、表观随机、boundary holonomy 与账本外置分裂的锚点。

\subsubsection{可见层：\texorpdfstring{$X_6$}{X6}、\texorpdfstring{$V_6$}{V6} 坐标与显式纤维}\label{subsubsec:typed-address-biaxial-completion-window6-visible-layer}
固定
\[
\Omega_6=\{0,1\}^6,
\qquad
X_6=\{w\in\{0,1\}^6:\ w_iw_{i+1}=0\ (1\le i<6)\}.
\]
由定理 \ref{thm:conclusion-window6-visible-crt-arithmetic-phase-space}，可见稳定层在 Zeckendorf 坐标
\[
V_6(w):=\sum_{k=1}^{6}w_kF_{k+1}
\]
下规范识别为
\[
V_6:X_6\overset{\sim}{\longrightarrow}\{0,1,\dots,20\}.
\]
另一方面，推论 \ref{cor:conclusion-window6-anomaly-root-torus-mod2-splitting} 给出 canonical split
\[
X_6=X_6^{\mathrm{cyc}}\sqcup X_6^{\mathrm{bdry}},
\qquad
|X_6^{\mathrm{cyc}}|=18,
\qquad
X_6^{\mathrm{bdry}}=\{\texttt{100001},\texttt{100101},\texttt{101001}\}.
\]

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的显式纤维模型]\label{prop:typed-address-biaxial-completion-window6-explicit-fibers}
对 \(\Fold_6^{\mathrm{bin}}:\{0,\dots,63\}\to X_6\)，记
\[
d(w):=d_6^{\mathrm{bin}}(w)=\bigl|\Fold_6^{\mathrm{bin}}{}^{-1}(w)\bigr|.
\]
则其纤维满足仿射 Boolean 标准形
\[
\Fold_6^{\mathrm{bin}}{}^{-1}(w)=V_6(w)+\beta(I_{d(w)}),
\qquad
\beta(a,b):=21a+34b,
\]
其中
\[
\beta(I_2)=\{0,34\},
\qquad
\beta(I_3)=\{0,21,34\},
\qquad
\beta(I_4)=\{0,21,34,55\},
\]
并且
\[
d(w)\in\{2,3,4\},
\qquad
\#\{d=2\}=8,\quad
\#\{d=3\}=4,\quad
\#\{d=4\}=9.
\]
特别地，boundary 扇区满足
\[
\Fold_6^{\mathrm{bin}}{}^{-1}(w)=\{V_6(w),V_6(w)+34\}
\qquad (w\in X_6^{\mathrm{bdry}}).
\]
\end{proposition}

\begin{proof}
仿射 Boolean 标准形正是定理 \ref{thm:xi-time-part9xab-window6-hidden-fiber-affine-boolean-normal-form} 的内容。
三档纤维大小与直方图由推论 \ref{cor:fold-bin-m6-fiber-hist} 给出；而 boundary 二层覆盖则由表 \ref{tab:fold6_boundary_sheet_pairs} 及推论 \ref{cor:terminal-delta-parity-rule} 直接固定。证毕。
\end{proof}
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_explicit\_fibers}

命题 \ref{prop:typed-address-biaxial-completion-window6-explicit-fibers} 的意义在于：本节的可见层不是“一个抽象有限集”，而是一个带显式前缀坐标、带显式 boundary 扇区、带显式 hidden offset 模型的有限对象。

\subsubsection{局部图表与最小局部截面}\label{subsubsec:typed-address-biaxial-completion-window6-local-charts}
在这个具体窗口上，最自然的局部覆盖直接由上一段的 canonical split 给出：
\[
\mathcal U_6:=\{U_{\mathrm{cyc}}\}\cup \{U_w:\ w\in X_6^{\mathrm{bdry}}\},
\]
其中 \(U_{\mathrm{cyc}}\) 承载全部 cyclic 稳定类型，而每个 \(U_w\) 只围住单个 boundary 词 \(w\) 的 dyadic 二层结构。

\begin{definition}[window-\texorpdfstring{$6$}{6} 的最小局部截面]\label{def:typed-address-biaxial-completion-window6-local-sections}
定义 \(U_{\mathrm{cyc}}\) 上的 canonical section
\[
s_{\mathrm{cyc}}(w):=V_6(w)
\qquad (w\in X_6^{\mathrm{cyc}}),
\]
并对每个 \(w\in X_6^{\mathrm{bdry}}\) 定义两条 boundary 局部截面
\[
s_w^{(0)}:=V_6(w),
\qquad
s_w^{(1)}:=V_6(w)+34.
\]
\end{definition}

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的最小局部覆盖是 boundary 上的二层图表系统]\label{prop:typed-address-biaxial-completion-window6-two-sheet-charts}
定义 \ref{def:typed-address-biaxial-completion-window6-local-sections} 给出的 \(\mathcal U_6\) 构成首个非平凡局部覆盖样例，满足：
\begin{enumerate}
  \item cyclic 扇区只需一条 canonical section；
  \item 每个 boundary 图表都携带一条内生的 \(\ZZ_2\) 二层覆盖；
  \item 因而最小非平凡 holonomy 不是来自大范围复杂闭路，而已出现在三条 boundary 图表的选层切换上。
\end{enumerate}
\end{proposition}

\begin{proof}
第一条来自 \(V_6\) 在 \(X_6\) 上的规范可见坐标。
第二条由表 \ref{tab:fold6_boundary_sheet_pairs} 给出的显式预像对
\[
\{14,48\},\qquad \{19,53\},\qquad \{17,51\}
\]
直接得到。
第三条则是前两条的直接合并：局部可读在 cyclic 扇区上是单值的，而在 boundary 扇区上立即分裂为两层，因此局部—全局障碍在最小尺度上已经可见。证毕。
\end{proof}
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_two\_sheet\_charts}

\subsubsection{统计层：固定窗 collision 锚点}\label{subsubsec:typed-address-biaxial-completion-window6-collision-anchor}
既然 window-\(6\) 的纤维直方图已经完全显式，固定窗碰撞量就可写成闭式。定义
\[
S_q^{\mathrm{bin}}(6):=\sum_{w\in X_6}\bigl(d_6^{\mathrm{bin}}(w)\bigr)^q.
\]

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的固定窗碰撞闭式]\label{prop:typed-address-biaxial-completion-window6-collision-closed-form}
对任意整数 \(q\ge 1\)，有
\[
S_q^{\mathrm{bin}}(6)=8\cdot 2^q+4\cdot 3^q+9\cdot 4^q.
\]
特别地，
\[
S_2^{\mathrm{bin}}(6)=212,
\qquad
S_3^{\mathrm{bin}}(6)=748,
\qquad
S_4^{\mathrm{bin}}(6)=2756.
\]
相应归一化碰撞量满足
\[
\mathrm{Col}^{\mathrm{bin}}_q(6)=\frac{S_q^{\mathrm{bin}}(6)}{64^q},
\]
从而
\[
\mathrm{Col}^{\mathrm{bin}}_2(6)=\frac{53}{1024}.
\]
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_collision\_closed\_form}
\end{proposition}

\begin{proof}
由推论 \ref{cor:fold-bin-m6-fiber-hist} 的直方图
\[
2:8,\qquad 3:4,\qquad 4:9
\]
直接代入即得。证毕。
\end{proof}

命题 \ref{prop:typed-address-biaxial-completion-window6-collision-closed-form} 的作用不是单纯给出三个数值，而是给出第一个完全闭式的统计锚点：一旦选定某个比较基线 \(u_6\) 或另一机制分布 \(w_6^{(1)}\)，便可在同一窗口上直接评估 \(\Delta_q^{(6)}\) 与定义 \ref{def:frt-normalized-mixed-collision-similarity} 的
\[
\Xi_q^{(0,1)}(6).
\]
更重要的是，\S\ref{subsubsec:frt-apparent-randomness-freezing-rough-statistics} 已证明 mixed collision 在 finite-state online 口径下仍有限态可编译，因此这里并不是临时拼凑一套统计，而是在调用一条已经对象化的谱接口。

\subsubsection{两条不同的隐藏通道}\label{subsubsec:typed-address-biaxial-completion-window6-two-hidden-channels}
在这个具体实例里，“隐藏层”并不是一个未命名的单一比特，而至少已经分裂成两条互不等价的通道。
一条是由 \(21=F_8\) 是否可激活所控制的 tail-repair 通道；另一条是由 \(34=F_9\) 二层覆盖所控制的 boundary parity 通道。

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的两类 hidden channel]\label{prop:typed-address-biaxial-completion-window6-two-hidden-channels}
对任意 \(w\in X_6\)，记
\[
\Delta(w):=\{n-V_6(w):\ n\in \Fold_6^{\mathrm{bin}}{}^{-1}(w)\}.
\]
则：
\begin{enumerate}
  \item \(21\in \Delta(w)\) 当且仅当 \(d(w)\ge 3\)；
  \item \(34\in \Delta(w)\) 对一切 \(w\in X_6\) 都成立，而在 boundary 扇区上它进一步退化为唯一非平凡选层差值；
  \item 因而 \(F_8\)-repair 与 \(F_9\)-sheet parity 不能相互替代：前者区分三层/四层纤维，后者区分 boundary 二层覆盖。
\end{enumerate}
\end{proposition}

\begin{proof}
由定理 \ref{thm:xi-time-part9xab-window6-hidden-fiber-affine-boolean-normal-form}，
\[
\Delta(w)\in\bigl\{\{0,34\},\ \{0,21,34\},\ \{0,21,34,55\}\bigr\}.
\]
故第一条与第二条直接成立。
boundary 情形再由推论 \ref{cor:terminal-delta-parity-rule} 精化为
\[
\Delta(w)=\{0,34\}
\qquad (w\in X_6^{\mathrm{bdry}}).
\]
于是 \(21\) 与 \(34\) 分别记录了不同的 hidden channel。证毕。
\end{proof}
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_two\_hidden\_channels}

命题 \ref{prop:typed-address-biaxial-completion-window6-two-hidden-channels} 给出一个在后文非常重要的最小教训：若要谈“对 hidden sector 的改写”，就不能只问“有没有一个隐藏 bit”，而必须区分改写的是 repair 通道、sheet parity 通道，还是更深的 ledger 通道。

\subsubsection{账本层：二比特精确反演与二十一维 anomaly 签名}\label{subsubsec:typed-address-biaxial-completion-window6-ledger-budget}
window-\(6\) 的账本层已经出现严格的二级预算分裂。

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的账本预算分裂]\label{prop:typed-address-biaxial-completion-window6-ledger-budget}
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_ledger\_budget}
在 window-\(6\) 上，操作可逆、交换可见与 anomaly 完备三条预算轴满足
\[
B_6^\star=2,
\qquad
\mu_{\mathrm{comm}}(A_6)=21,
\qquad
\mu_{\mathrm{faith}}(A_6)=64.
\]
同时，规范体积与 collision-entropy 锚点满足
\[
g_6=\frac{39}{64}\log 2+\frac{13}{64}\log 3\approx 0.645542,
\qquad
\kappa_6=\frac{11}{8}\log 2+\frac{3}{16}\log 3\approx 1.159067.
\]
\end{proposition}

\begin{proof}
前一组等式正是推论 \ref{cor:conclusion-window6-operational-commutative-faithful-triple-separation} 的内容。
后一组常数则由定理 \ref{thm:conclusion-window6-rational-envelope-reconstructs-block-spectrum} 给出。证毕。
\end{proof}

基于命题 \ref{prop:typed-address-biaxial-completion-window6-ledger-budget}，本节把账本预算轴 \(L\) 细分为
\[
L\in\{0,1,2,21,+\infty\},
\]
其语义分别是：局部单值无须补账、单条 boundary 图表只需一位选层 bit、window-\(6\) 的精确反演需要两比特、保持全部 anomaly 坐标需要二十一维二元账本，而 \(+\infty\) 则表示当前协议下根本无忠实外置。
由此可见，“可逆性”与“异常完备性”并不是同一层级的资源，不能相互抵代。

\subsubsection{boundary holonomy 作为最小曲率候选}\label{subsubsec:typed-address-biaxial-completion-window6-boundary-holonomy}
在这个最小实例中，最自然的 holonomy 不是来自大覆盖，而是来自三条 boundary 词上的二层换位。

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的 boundary holonomy 残差]\label{prop:typed-address-biaxial-completion-window6-boundary-holonomy}
\leanverified{paper\_typed\_address\_biaxial\_completion\_window6\_boundary\_holonomy}
记
\[
C_{\partial}
:=
\langle \tau_{100001},\tau_{100101},\tau_{101001}\rangle
\cong
(\ZZ/2\ZZ)^3
\subset Z(G_6^{\mathrm{bin}}).
\]
再记强局域 geometric uplift 所保留的几何可实现子群为
\[
P_6^{\mathrm{geo}}
:=
\langle (1,1,1)\rangle
\cong
\ZZ/2\ZZ.
\]
则 boundary holonomy 的不可平坦化残差由商群
\[
C_{\partial}/P_6^{\mathrm{geo}}
\cong
(\ZZ/2\ZZ)^2
\]
度量。
特别地，window-\(6\) 至少携带两位不能被局域几何 uplift 吸收的 irreducible boundary residual。
\end{proposition}

\begin{proof}
定理 \ref{thm:conclusion-window6-boundary-parity-directsummand-rational-blindness} 给出
\[
C_{\partial}\cong (\ZZ/2\ZZ)^3.
\]
定理 \ref{thm:conclusion-window6-boundary-parity-three-layer-blind-filtration} 进一步给出严格滤过
\[
0\subset P_6^{\mathrm{geo}}\subset C_{\partial},
\qquad
C_{\partial}/P_6^{\mathrm{geo}}\cong (\ZZ/2\ZZ)^2.
\]
因此除去局域几何可实现的对角 bit 之后，仍剩两位不可平坦化残差。证毕。
\end{proof}

命题 \ref{prop:typed-address-biaxial-completion-window6-boundary-holonomy} 给出了本节最稳妥的“曲率候选”语义：在这里，曲率首先不是某个连续张量，而是两位不能被局域几何重参数化消去的 boundary holonomy 自由度。

\subsubsection{动态 prime-register：把历史接入同一个样例}\label{subsubsec:typed-address-biaxial-completion-window6-dynamic-ledger}
到目前为止，window-\(6\) 仍是一个固定窗对象。
要把它提升成粗糙时空样例，还需把历史接入同一容器。

\begin{definition}[window-\texorpdfstring{$6$}{6} 的有限前缀事件字母表]\label{def:typed-address-biaxial-completion-window6-event-alphabet}
取有限事件字母表
\[
E_6=
\{
\mathtt{stay},
\mathtt{refine},
\mathtt{flip}_{100001},
\mathtt{flip}_{100101},
\mathtt{flip}_{101001},
\mathtt{decode}
\}.
\]
对任意事件流前缀 \(e_1\cdots e_T\in E_6^\ast\)，定义其动态 prime-register 为
\[
r_T\in \NN^{(\mathbb P)},
\qquad
N_T:=\prod_{t=1}^{T}p_t^{\mathrm{code}(e_t)}.
\]
\end{definition}

\begin{remark}[window-\texorpdfstring{$6$}{6} 中的历史外置]\label{rem:typed-address-biaxial-completion-window6-dynamic-ledger}
定义 \ref{def:typed-address-biaxial-completion-window6-event-alphabet} 只使用有限前缀，因此已足够把当前窗口的局部历史对象化。
若进一步要求乘法层与事件层的严格同态账本，则推论 \ref{cor:killo-no-finite-additive-register-linearization} 表明有限加法寄存器不再可行；而 \S\ref{subsubsec:emergent-arithmetic-dynamic-prime-register-godel-dynamics} 及定理 \ref{thm:killo-godel-semidir-affine-2adic}、推论 \ref{cor:killo-infinite-stream-2adic-holographic-point} 又说明该前缀账本可被统一嵌入 \(2^L\)-进仿射地址几何。
因此，window-\(6\) 一旦接上动态 prime-register，便不再只是固定窗口，而成为一个带前缀历史、带位移规则、带外置成本的局部时空块。
\end{remark}

\subsubsection{资源准距离的具体写法}\label{subsubsec:typed-address-biaxial-completion-window6-resource-distance}
现在可以把定义 \ref{def:typed-address-biaxial-completion-resource-vector} 与定义 \ref{def:typed-address-biaxial-completion-resource-quasidistance} 压到 window-\(6\) 的具体版本上。
令局部状态记为
\[
p=(i,w,\sigma,\ell),
\qquad
q=(j,w',\sigma',\ell'),
\]
其中 \(i,j\) 为观察者纤维标签，\(w,w'\in X_6\) 为可见稳定类型，\(\sigma,\sigma'\) 为 boundary parity 记录，\(\ell,\ell'\) 为动态 prime-register 的有限前缀账本。

\begin{definition}[window-\texorpdfstring{$6$}{6} 的预算向量]\label{def:typed-address-biaxial-completion-window6-resource-vector}
对任意可比较证书 \(c\)，定义
\[
\mathbf B_6(p,q;c):=\bigl(S_6(p,q;c),T_6(p,q;c),\Pi_6(p,q;c),L_6(p,q;c)\bigr),
\]
其中：
\[
S_6(p,q;c):=
\begin{cases}
0,& w=w',\\[4pt]
\log d_6^{\mathrm{bin}}(w)+\log d_6^{\mathrm{bin}}(w'),& w\neq w',
\end{cases}
\]
\[
T_6(p,q;c):=\text{将 }p,q\text{ 带入同一可比较图表所需的最小同步深度},
\]
\[
\Pi_6(p,q;c):=
\sup_{2\le r\le r_\ast}
\bigl|
\log \Xi_r^{(p,q)}(6)
\bigr|,
\]
其中 \(\Xi_r^{(p,q)}(6)\) 表示由 \(p,q\) 所诱导的两条可见分布在 window-\(6\) 上的归一化 mixed collision 相似度；
并定义账本预算
\[
L_6(p,q;c):=
\begin{cases}
0,& \text{同图表且单值可读},\\
1,& \text{只需单个 boundary 选层 bit},\\
2,& \text{要求 window-6 级精确反演},\\
21,& \text{要求保留全部 anomaly 签名},\\
+\infty,& \text{当前协议下无忠实账本}.
\end{cases}
\]
\end{definition}

\begin{definition}[window-\texorpdfstring{$6$}{6} 的资源准距离]\label{def:typed-address-biaxial-completion-window6-resource-distance}
对任意单调次可加泛函
\[
\Lambda:\RR_{\ge 0}^4\to \RR_{\ge 0},
\]
定义
\[
d_{\mathrm{res},6}(p,q):=\inf_c \Lambda\bigl(\mathbf B_6(p,q;c)\bigr).
\]
\end{definition}

定义 \ref{def:typed-address-biaxial-completion-window6-resource-distance} 的关键不在于某一组具体常数，而在于它第一次把本文此前分散的对象压成了一个真正可算的距离候选。
尤其重要的是：\(L_6=2\) 与 \(L_6=21\) 对应不同任务，而命题 \ref{prop:typed-address-biaxial-completion-window6-boundary-holonomy} 中
\[
C_{\partial}/P_6^{\mathrm{geo}}\cong(\ZZ/2\ZZ)^2
\]
又说明即使空间、时间与谱预算足够，某些 boundary residual 仍然不能被局域几何吸收。
因此 \(d_{\mathrm{res},6}\) 在原则上只能是准度规，而不是经典度规。

\subsubsection{最小粗糙可见量}\label{subsubsec:typed-address-biaxial-completion-window6-rough-visible-observable}
为了把这个局部时空块真正接回 \S\ref{subsubsec:groupoid-zeckendorf-multiscale-rough-readout} 的粗糙可见量，本节定义一个最小的 window-\(6\) 粗糙读出。

\begin{definition}[window-\texorpdfstring{$6$}{6} 的最小粗糙读出]\label{def:typed-address-biaxial-completion-window6-rough-readout}
对任意 \(\omega=(\omega_1,\dots,\omega_6)\in\Omega_6\)，记
\[
\mathrm{int}_6(\omega):=\sum_{k=1}^{6}\omega_k2^{6-k},
\]
并定义
\[
w(\omega):=\Fold_6^{\mathrm{bin}}\!\bigl(\mathrm{int}_6(\omega)\bigr),
\]
令 \(\sigma(\omega)\in (\ZZ/2\ZZ)^3\) 记录其 boundary parity（若 \(\omega\) 不落在 boundary 扇区，则取零元），并令 \(\ell_j(\omega)\) 表示动态 prime-register 的第 \(j\) 个前缀读数。
取 \(0<a<1\) 与一族相位映射 \(\phi_j\)，定义
\[
Y_\omega(t)
:=
\sum_{j\ge 0}
a^j
\cos\!\Bigl(
2^j t+\phi_j\bigl(w(\omega),\sigma(\omega),\ell_j(\omega)\bigr)
\Bigr).
\]
\end{definition}

定义 \ref{def:typed-address-biaxial-completion-window6-rough-readout} 中三类相位各司其职：\(w(\omega)\) 决定可见稳定类型，\(\sigma(\omega)\) 回写 boundary holonomy 的最小离散残差，\(\ell_j(\omega)\) 则把历史账本逐尺度回写到可见层。
因此，这不是一个任意拼接的粗糙级数，而是第一次把
\[
\text{Fold 稳定层}
\quad+\quad
\text{boundary holonomy}
\quad+\quad
\text{dynamic prime-register}
\]
写入同一条可见读出。
在这个样例上，最自然的首批审计量包括
\[
\operatorname{osc}(Y_\omega;2^{-n}),
\qquad
\text{\(p\)-variation},
\qquad
\Delta_q^{(6)},
\qquad
\Xi_q^{(0,1)}(6),
\qquad
\Omega_{\partial,6}(\ell).
\]

\subsubsection{样例主命题}\label{subsubsec:typed-address-biaxial-completion-window6-example-main}
将上面的对象合并，得到这一节的主命题。

\begin{proposition}[window-\texorpdfstring{$6$}{6} 的最小可审计粗糙时空样例]\label{prop:typed-address-biaxial-completion-window6-auditable-example}
令
\[
\mathcal I_6
:=
\bigl(
\mathcal P_6,
\mathcal X_6,
\mathcal U_6,
\mathcal R_6,
\mathcal E_6,
\mathcal C_6
\bigr)
\]
分别由 window-\(6\) 上的 LOST 限制、Fold 可见层、局部图表系统、fixed-window collision 谱层、boundary/prime-ledger 外置层与预算向量层组成。
则 \(\mathcal I_6\) 构成定义 \ref{def:typed-address-biaxial-completion-admissible-instantiation} 意义下的一组允许实例化，并自然诱导粗糙时空四元组
\[
\mathfrak G_6=
(\preceq_6,\tau_6,d_{\mathrm{res},6},\Omega_6).
\]
此外它满足：
\begin{enumerate}
  \item \textbf{可见层非平凡：}
  \[
  |X_6|=21,
  \qquad
  2:8,\ 3:4,\ 4:9;
  \]
  \item \textbf{局部—全局障碍非平凡：}
  \[
  C_{\partial}\cong (\ZZ/2\ZZ)^3,
  \qquad
  C_{\partial}/P_6^{\mathrm{geo}}\cong (\ZZ/2\ZZ)^2;
  \]
  \item \textbf{账本预算分裂非平凡：}
  \[
  B_6^\star=2,
  \qquad
  \dim_{\FF_2}(G_6^{\mathrm{bin}})^{\mathrm{ab}}=21;
  \]
  \item \textbf{统计谱可审计：}
  fixed-window collision 闭式可得，mixed collision 仍保留 finite-state 可计算性；
  \item \textbf{历史外置非平凡：}
  若要求乘法层与事件层的严格同态账本，则 prime-register 方向被结构强迫；其有限前缀又可嵌入 \(2^L\)-进仿射地址动力学。
\end{enumerate}
因此，\(\mathfrak G_6\) 是本文中第一个同时包含粗糙可见性、表观随机统计、boundary holonomy 与动态账本的最小可审计实例。
\end{proposition}

\begin{proof}
\(\mathcal P_6\) 由定义 \ref{def:logic-expansion-observer-spacetime-state-model} 的 LOST 数据在固定窗口上的限制给出；
\(\mathcal X_6\) 由命题 \ref{prop:typed-address-biaxial-completion-window6-explicit-fibers} 的可见层与 hidden fiber 公式给出；
\(\mathcal U_6\) 由命题 \ref{prop:typed-address-biaxial-completion-window6-two-sheet-charts} 的局部覆盖给出；
\(\mathcal R_6\) 由命题 \ref{prop:typed-address-biaxial-completion-window6-collision-closed-form} 及 \S\ref{subsubsec:frt-apparent-randomness-freezing-rough-statistics} 的 collision/mixed-collision 接口给出；
\(\mathcal E_6\) 由命题 \ref{prop:typed-address-biaxial-completion-window6-two-hidden-channels}、命题 \ref{prop:typed-address-biaxial-completion-window6-boundary-holonomy} 与备注 \ref{rem:typed-address-biaxial-completion-window6-dynamic-ledger} 给出；
\(\mathcal C_6\) 则由定义 \ref{def:typed-address-biaxial-completion-window6-resource-vector} 与定义 \ref{def:typed-address-biaxial-completion-window6-resource-distance} 给出。
因此 \(\mathcal I_6\) 满足定义 \ref{def:typed-address-biaxial-completion-admissible-instantiation} 的六条条件，并由命题 \ref{prop:typed-address-biaxial-completion-instantiation-extraction} 自动诱导粗糙时空四元组 \(\mathfrak G_6\)。
各条非平凡性断言则分别由推论 \ref{cor:fold-bin-m6-fiber-hist}、定理 \ref{thm:conclusion-window6-boundary-parity-three-layer-blind-filtration}、推论 \ref{cor:conclusion-window6-operational-commutative-faithful-triple-separation}、定理 \ref{thm:xi-window6-oracle-anomaly-coordinate-bifurcation} 及 \S\ref{subsubsec:emergent-arithmetic-dynamic-prime-register-godel-dynamics} 给出。证毕。
\end{proof}

\subsubsection{小结：window-\texorpdfstring{$6$}{6} 作为最小研究起点}\label{subsubsec:typed-address-biaxial-completion-window6-example-summary}
至此，window-\(6\) 已不再只是一个“有趣的有限窗口”，而成为本文第一组真正可逐项审计的粗糙时空样例。
在这个样例中：
\begin{enumerate}
  \item \(\mathcal X_{\mathrm{Fold}}+\mathcal E_{\mathrm{ledger}}\) 给出粗糙可见量的最小对象层；
  \item \(\mathcal X_{\mathrm{Fold}}+\mathcal R_{\mathrm{spec}}\) 给出表观随机与冻结相的最小统计层；
  \item \(\mathcal P_{\mathrm{LOST}}+\mathcal U_{\mathrm{loc}}+\mathcal C_{\mathrm{budget}}\) 则把这些对象压成同一个粗糙时空四元组。
\end{enumerate}
因此，后续最自然的任务不再是继续扩展抽象模板，而是把这一样例直接送入下一节的数值审计协议：逐项固定 \(Y_\omega\) 的采样方案、window-\(6\) 上的 \(\Delta_q^{(6)}\) 与 \(\Xi_q^{(0,1)}(6)\)、boundary 闭路 residual 的枚举，以及 \(L_6\in\{1,2,21\}\) 三档预算对应的实验任务。
