\paragraph{Poisson--Cauchy 粗粒化二阶正规形下的剖面极限与散度常数闭包}
以下结论统一沿用命题 \ref{con:xi-poisson-coarsegraining-second-order-normal-form} 的记号。设
\[
\tilde h_t(x):=(\mathsf P_t*\tilde\nu)(x),\qquad
\tilde g_t(x):=\mathsf P_t(x-\bar\gamma),\qquad
\mathsf P_t(x):=\frac{1}{\pi}\frac{t}{x^2+t^2},
\]
其中 \(\tilde\nu\) 为 \(\RR\) 上概率测度，\(\bar\gamma=\int \gamma\,\dd\tilde\nu(\gamma)\)，\(\sigma^2=\int(\gamma-\bar\gamma)^2\,\dd\tilde\nu(\gamma)\in(0,\infty)\)。
并记
\[
r_t(x):=\frac{1}{\pi}\frac{t\bigl(3(x-\bar\gamma)^2-t^2\bigr)}{\bigl((x-\bar\gamma)^2+t^2\bigr)^3}
=\frac12\,\partial_x^2\mathsf P_t(x-\bar\gamma),
\]
使得
\[
\tilde h_t=\tilde g_t+\sigma^2 r_t+\varepsilon_t,
\]
且在三阶中心绝对矩有限时有 \(\|\varepsilon_t\|_\infty=O(t^{-4})\)、\(\|\varepsilon_t\|_{L^1}=O(t^{-3})\)。

\begin{conclusion}[\texorpdfstring{$L^p$}{Lp} 族锐渐近与普适常数谱]\label{con:xi-poisson-lp-spectrum-sharp-family}
若
\[
\int_{\RR}|\gamma-\bar\gamma|^3\,\dd\tilde\nu(\gamma)<\infty,
\]
则对任意 \(p\in(1,\infty)\) 有
\[
\lim_{t\to\infty}
\frac{t^{3-\frac1p}}{\sigma^2}\,
\|\tilde h_t-\tilde g_t\|_{L^p(\RR)}
=
\frac1\pi\left(\int_{\RR}\frac{|3y^2-1|^p}{(1+y^2)^{3p}}\,\dd y\right)^{\!1/p},
\]
并且 \(p=\infty\) 端点满足
\[
\lim_{t\to\infty}
\frac{t^3}{\sigma^2}\,
\|\tilde h_t-\tilde g_t\|_{L^\infty(\RR)}
=\frac1\pi.
\]
特别地，
\[
\lim_{t\to\infty}
\frac{t^{5/2}}{\sigma^2}\,
\|\tilde h_t-\tilde g_t\|_{L^2(\RR)}
=\frac{\sqrt3}{4\sqrt\pi}.
\]
\end{conclusion}
\begin{proof}[证明]
由
\(
\tilde h_t-\tilde g_t=\sigma^2 r_t+\varepsilon_t
\)
与余项估计可得 \(\|\varepsilon_t\|_{L^p}=o(\|r_t\|_{L^p})\)（\(p\in(1,\infty]\)）。
令 \(y=(x-\bar\gamma)/t\)，则
\[
r_t(\bar\gamma+ty)=\frac{1}{\pi t^3}\frac{3y^2-1}{(1+y^2)^3},
\]
从而
\[
\|r_t\|_{L^p(\RR)}
=t^{-3+1/p}\frac1\pi\left(\int_{\RR}\frac{|3y^2-1|^p}{(1+y^2)^{3p}}\,\dd y\right)^{1/p}\quad(p<\infty),
\]
\[
\|r_t\|_{L^\infty(\RR)}
=\frac{1}{\pi t^3}\sup_{y\in\RR}\frac{|3y^2-1|}{(1+y^2)^3}
=\frac{1}{\pi t^3}.
\]
结合扰动项即可得到极限式。再由
\(
\int_{\RR}\frac{(3y^2-1)^2}{(1+y^2)^6}\dd y=\frac{3\pi}{16}
\)
推出 \(p=2\) 常数。证毕。
\end{proof}

\begin{conclusion}[自相似剖面极限与强收敛]\label{con:xi-poisson-selfsimilar-profile-strong-limit}
在结论 \ref{con:xi-poisson-lp-spectrum-sharp-family} 的条件下，定义
\[
\Delta_t(y):=\frac{t^3}{\sigma^2}
\Bigl(\tilde h_t(\bar\gamma+ty)-\tilde g_t(\bar\gamma+ty)\Bigr),\qquad y\in\RR.
\]
则
\[
\Delta_t(y)\xrightarrow[t\to\infty]{}\Delta_\infty(y)
:=\frac1\pi\frac{3y^2-1}{(1+y^2)^3}
\]
在 \(\RR\) 上一致成立；并且对任意 \(p\in[1,\infty)\) 有
\[
\Delta_t\to\Delta_\infty\quad\text{于 }L^p(\RR,\dd y).
\]
\end{conclusion}
\begin{proof}[证明]
由二阶正规形，
\[
\Delta_t(y)=t^3r_t(\bar\gamma+ty)+\frac{t^3}{\sigma^2}\varepsilon_t(\bar\gamma+ty)
=\Delta_\infty(y)+\frac{t^3}{\sigma^2}\varepsilon_t(\bar\gamma+ty).
\]
而 \(\|\varepsilon_t\|_\infty=O(t^{-4})\) 给出
\[
\sup_{y\in\RR}\left|\frac{t^3}{\sigma^2}\varepsilon_t(\bar\gamma+ty)\right|=O(t^{-1})\to0,
\]
故一致收敛成立。
又 \(\Delta_\infty\in L^p(\RR)\)（任意有限 \(p\)），并且
\(
|\Delta_t-\Delta_\infty|\le Ct^{-1}
\)
一致有界，支配收敛推出 \(L^p\) 收敛。证毕。
\end{proof}

\begin{conclusion}[\texorpdfstring{$D_{\mathrm{KL}}$}{KL} 的两项锐展开与显式六阶系数]\label{con:xi-poisson-kl-two-term-sharp-explicit}
若进一步满足
\[
\int_{\RR}|\gamma-\bar\gamma|^5\,\dd\tilde\nu(\gamma)<\infty,
\]
并记中心矩
\(
\mu_k:=\int(\gamma-\bar\gamma)^k\,\dd\tilde\nu(\gamma)\ (k\ge3),
\)
则
\[
D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_t)
=\frac{\sigma^4}{8t^4}
+\frac{1}{t^6}\left(
\frac{\sigma^6}{64}-\frac{\sigma^2\mu_4}{8}+\frac{3\mu_3^2}{32}
\right)
+o(t^{-6}).
\]
\end{conclusion}
\begin{proof}[证明]
该式即定理 \ref{thm:xi-cauchy-poisson-kl-sharp-asymptotics}（等价写法见
\ref{thm:xi-poisson-kl-sixth-order-refined-binomials}）
的同参数化表达。证毕。
\end{proof}

\input{subsubsec__xi-poisson-cauchy-profile-divergence-closure-laws-part2}

\begin{conclusion}[Rényi 相对熵主系数的全参数普适性]\label{con:xi-poisson-renyi-alpha-over-8}
在结论 \ref{con:xi-poisson-lp-spectrum-sharp-family} 的设定下，若四阶中心矩有限，则对任意
\[
\alpha\in(0,\infty)\setminus\{1\},
\]
记
\[
D_\alpha(\tilde h_t\mid \tilde g_t)
:=\frac{1}{\alpha-1}\log\int_{\RR}\tilde h_t(x)^\alpha \tilde g_t(x)^{1-\alpha}\,\dd x,
\]
有
\[
\lim_{t\to\infty}\frac{t^4}{\sigma^4}\,D_\alpha(\tilde h_t\mid \tilde g_t)=\frac{\alpha}{8}.
\]
\end{conclusion}
\begin{proof}[证明]
令 \(\delta_t:=\tilde h_t/\tilde g_t-1\)。由二阶正规形有 \(\delta_t=O(t^{-2})\) 且
\(\int \tilde g_t\delta_t\,\dd x=0\)。
于是
\[
\int \tilde h_t^\alpha \tilde g_t^{1-\alpha}\,\dd x
=\int \tilde g_t(1+\delta_t)^\alpha\,\dd x
=1+\frac{\alpha(\alpha-1)}{2}\int \tilde g_t\delta_t^2\,\dd x+o(t^{-4}).
\]
再用
\(
\int \tilde g_t\delta_t^2\,\dd x=\sigma^4/(4t^4)+o(t^{-4})
\)
与 \(\log(1+u)=u+o(u)\)，得到
\[
D_\alpha(\tilde h_t\mid \tilde g_t)
=\frac{\alpha}{2}\cdot\frac{\sigma^4}{4t^4}+o(t^{-4})
=\frac{\alpha\sigma^4}{8t^4}+o(t^{-4}),
\]
从而结论成立。证毕。
\end{proof}

\begin{conclusion}[KL 与 \texorpdfstring{$L^1$}{L1} 平方等价的渐近常数]\label{con:xi-poisson-kl-l1-square-equivalence-2pi2-27}
在结论 \ref{con:xi-poisson-lp-spectrum-sharp-family} 的条件下，
\[
\lim_{t\to\infty}
\frac{D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_t)}
{\|\tilde h_t-\tilde g_t\|_{L^1(\RR)}^2}
=\frac{2\pi^2}{27}.
\]
等价地，若
\(
\mathrm{TV}(\tilde h_t,\tilde g_t):=\frac12\|\tilde h_t-\tilde g_t\|_{L^1},
\)
则
\[
D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_t)
\sim \frac{8\pi^2}{27}\,\mathrm{TV}(\tilde h_t,\tilde g_t)^2
\qquad(t\to\infty).
\]
\end{conclusion}
\begin{proof}[证明]
由定理 \ref{thm:xi-cauchy-poisson-tv-sharp-constant} 与
\ref{thm:xi-cauchy-poisson-kl-sharp-asymptotics}，
\[
\|\tilde h_t-\tilde g_t\|_{L^1}
=\frac{3\sqrt3}{4\pi}\frac{\sigma^2}{t^2}+O(t^{-3}),
\qquad
D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_t)=\frac{\sigma^4}{8t^4}+o(t^{-4}),
\]
直接比值化简即得常数 \(\frac{2\pi^2}{27}\)；
TV 版本乘以系数 \(4\) 即得。证毕。
\end{proof}

\begin{conclusion}[反对称剖面分离三阶中心矩]\label{con:xi-poisson-antisymmetric-profile-mu3}
在结论 \ref{con:xi-poisson-lp-spectrum-sharp-family} 的条件下，若四阶中心绝对矩有限，且 \(\mu_3\neq 0\)，记
\[
\mu_3:=\int_{\RR}(\gamma-\bar\gamma)^3\,\dd\tilde\nu(\gamma).
\]
则对任意 \(y\in\RR\) 有一致极限
\[
\lim_{t\to\infty}
\frac{t^4}{\mu_3}
\Bigl(\tilde h_t(\bar\gamma+ty)-\tilde h_t(\bar\gamma-ty)\Bigr)
=\frac{8}{\pi}\frac{y(y^2-1)}{(1+y^2)^4}.
\]
\end{conclusion}
\begin{proof}[证明]
三阶展开给出
\[
\tilde h_t=\tilde g_t+\sigma^2 r_t+\mu_3 s_t+\eta_t,\qquad
s_t:=-\frac16\partial_x^3\mathsf P_t(\cdot-\bar\gamma),
\]
且 \(\|\eta_t\|_\infty=O(t^{-5})\)。
由于 \(\tilde g_t,r_t\) 关于 \(\bar\gamma\) 为偶函数，差分后仅剩奇项：
\[
\tilde h_t(\bar\gamma+ty)-\tilde h_t(\bar\gamma-ty)
=2\mu_3 s_t(\bar\gamma+ty)+O(t^{-5}).
\]
直接微分得到
\[
s_t(\bar\gamma+ty)=\frac{4}{\pi t^4}\frac{y(y^2-1)}{(1+y^2)^4},
\]
故
\[
\frac{t^4}{\mu_3}\Bigl(\tilde h_t(\bar\gamma+ty)-\tilde h_t(\bar\gamma-ty)\Bigr)
=\frac{8}{\pi}\frac{y(y^2-1)}{(1+y^2)^4}+O(t^{-1}),
\]
且余项一致，结论成立。证毕。
\end{proof}

\begin{conclusion}[对称残差分离四阶中心矩]\label{con:xi-poisson-symmetric-residual-mu4}
在结论 \ref{con:xi-poisson-kl-two-term-sharp-explicit} 的条件下，对任意 \(y\in\RR\) 有一致极限
\[
\lim_{t\to\infty}\frac{t^5}{\mu_4}
\Bigl[
\tilde h_t(\bar\gamma+ty)+\tilde h_t(\bar\gamma-ty)
-2\tilde g_t(\bar\gamma+ty)-2\sigma^2 r_t(\bar\gamma+ty)
\Bigr]
=\frac{2}{\pi}\frac{1-10y^2+5y^4}{(1+y^2)^5}.
\]
\end{conclusion}
\begin{proof}[证明]
四阶展开写作
\[
\tilde h_t=\tilde g_t+\sigma^2 r_t+\mu_3 s_t+\mu_4 q_t+\rho_t,\qquad
q_t:=\frac{1}{24}\partial_x^4\mathsf P_t(\cdot-\bar\gamma),
\]
且 \(\|\rho_t\|_\infty=O(t^{-6})\)。
对称化后奇项 \(s_t\) 抵消，于是
\[
\tilde h_t(\bar\gamma+ty)+\tilde h_t(\bar\gamma-ty)-2\tilde g_t(\bar\gamma+ty)-2\sigma^2 r_t(\bar\gamma+ty)
=2\mu_4 q_t(\bar\gamma+ty)+O(t^{-6}).
\]
又
\[
q_t(\bar\gamma+ty)=\frac{1}{\pi t^5}\frac{1-10y^2+5y^4}{(1+y^2)^5},
\]
故乘以 \(t^5/\mu_4\) 即得结论。证毕。
\end{proof}

\begin{conclusion}[同中心 Cauchy 族的信息投影与最优尺度修正]\label{con:xi-poisson-cauchy-information-projection-optimal-scale}
在结论 \ref{con:xi-poisson-lp-spectrum-sharp-family} 的设定下，考虑同中心 Cauchy 族
\[
\tilde g_s(x):=\mathsf P_s(x-\bar\gamma)\qquad(s>0).
\]
取任意尺度 \(s_t\) 满足 \(s_t/t\to1\)，并记
\[
\beta:=\lim_{t\to\infty}t(s_t-t)
\]
（可在任意收敛子列上定义）。令
\[
G(y):=\frac{1}{\pi(1+y^2)},\qquad
u(y):=\frac{3y^2-1}{(1+y^2)^2},\qquad
\phi(y):=\frac{y^2-1}{1+y^2}.
\]
则
\[
D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_{s_t})
=\frac{\sigma^4}{2t^4}
\int_{\RR}G(y)\left(u(y)-\frac{\beta}{\sigma^2}\phi(y)\right)^2\dd y
+o(t^{-4}).
\]
特别地，
\[
\liminf_{t\to\infty}\frac{t^4}{\sigma^4}D_{\mathrm{KL}}(\tilde h_t\mid \tilde g_{s_t})\ge \frac{1}{16},
\]
且当且仅当
\[
s_t=t+\frac{\sigma^2}{2t}+o(t^{-1})
\]
时取等号；此时
\[
D_{\mathrm{KL}}\!\left(\tilde h_t\mid \tilde g_{\,t+\sigma^2/(2t)}\right)
=\frac{\sigma^4}{16t^4}+o(t^{-4}).
\]
\end{conclusion}
\begin{proof}[证明]
该结论与定理 \ref{thm:xi-poisson-optimal-cauchy-rescaling-kl-constant} 同构。
令 \(y=(x-\bar\gamma)/t\)，并写 \(s_t=t+\beta/t+o(t^{-1})\)，则
\[
\frac{\tilde g_{s_t}(\bar\gamma+ty)}{\tilde g_t(\bar\gamma+ty)}
=1+\frac{\beta}{t^2}\phi(y)+O(t^{-4}),
\]
而
\[
\frac{\tilde h_t(\bar\gamma+ty)}{\tilde g_t(\bar\gamma+ty)}
=1+\frac{\sigma^2}{t^2}u(y)+O(t^{-3}).
\]
故
\[
\frac{\tilde h_t}{\tilde g_{s_t}}-1
=\frac{1}{t^2}\left(\sigma^2u-\beta\phi\right)+O(t^{-3}),
\]
代入 KL 二阶展开即可得到主项二次型。
最小化参数 \(c:=\beta/\sigma^2\)：
\[
\Psi(c):=\int G(u-c\phi)^2
=\int Gu^2-2c\int Gu\phi+c^2\int G\phi^2,
\]
并用
\[
\int Gu^2=\frac14,\qquad
\int Gu\phi=\frac14,\qquad
\int G\phi^2=\frac12
\]
得 \(\Psi(c)=\frac14-\frac{c}{2}+\frac{c^2}{2}\)，唯一极小点 \(c=\frac12\)，最小值 \(\Psi_{\min}=\frac18\)。
因此最小 KL 主项为 \(\frac12\cdot\frac18=\frac1{16}\)，对应
\(
s_t=t+\sigma^2/(2t)+o(t^{-1})
\)。
证毕。
\end{proof}

\begin{conclusion}[Poisson--Cauchy 位置混合的二阶常数律与显式余项]\label{con:xi-poisson-cauchy-second-order-constants-with-mu3-remainder}
在命题 \ref{con:xi-poisson-coarsegraining-second-order-normal-form} 的单通道记号下，记 \(\Var:=\int_{\RR}(\gamma-\bar\gamma)^2\,\dd\tilde\nu(\gamma)\)、\(\mu_3:=\int_{\RR}|\gamma-\bar\gamma|^3\,\dd\tilde\nu(\gamma)<\infty\)。
\[
\|h_t-g_t\|_{L^1(\RR)}
=\frac{3\sqrt3}{4\pi}\frac{\Var}{t^2}
+O\!\left(\frac{\mu_3}{t^3}\right),
\]
\[
D_{\mathrm{KL}}(h_t\mid g_t)
=\frac{1}{8}\frac{\Var^2}{t^4}
+O\!\left(\frac{\mu_3\Var}{t^5}+\frac{\mu_3^2}{t^6}\right).
\]
\end{conclusion}
\begin{proof}[证明]
由命题 \ref{con:xi-poisson-coarsegraining-second-order-normal-form}，
\[
h_t-g_t=\Var\,r_t+\varepsilon_t,\qquad
\|\varepsilon_t\|_{L^1}=O(\mu_3 t^{-3}),\qquad
\left\|\frac{\varepsilon_t}{g_t}\right\|_{L^\infty}=O(\mu_3 t^{-3}),
\]
其中
\(
r_t(x)=\frac{1}{\pi}\frac{t(3(x-\bar\gamma)^2-t^2)}{((x-\bar\gamma)^2+t^2)^3}
\)。
第一式由定理 \ref{thm:xi-cauchy-poisson-tv-sharp-constant} 与上述余项界直接得到。

对第二式，令 \(u_t:=(h_t-g_t)/g_t\)。由 \(\int g_tu_t\,\dd x=0\) 与
\(
(1+u)\log(1+u)=u+\frac12u^2+O(|u|^3)
\)，有
\[
D_{\mathrm{KL}}(h_t\mid g_t)
=\frac12\int_{\RR}g_tu_t^2\,\dd x
+O\!\left(\|u_t\|_{L^\infty}\int_{\RR}g_tu_t^2\,\dd x\right).
\]
主项分解为
\[
\frac12\Var^2\int_{\RR}\frac{r_t^2}{g_t}\,\dd x
+\Var\,O\!\left(\left\|\frac{r_t}{g_t}\right\|_{L^\infty}\|\varepsilon_t\|_{L^1}\right)
+O\!\left(\left\|\frac{\varepsilon_t}{g_t}\right\|_{L^\infty}\|\varepsilon_t\|_{L^1}\right).
\]
其中 \(\int r_t^2/g_t=\frac{1}{4t^4}\)（见定理 \ref{thm:xi-cauchy-poisson-tv-sharp-constant} 及相关常数计算），
故主导项为 \(\frac18\Var^2t^{-4}\)，其余项阶即为所述。证毕。
\end{proof}

\begin{theorem}[Poisson--Cauchy 位置混合的全阶正规形与统一余项界]\label{thm:xi-poisson-cauchy-all-order-normal-form-uniform-remainder}
\leanverified{paper\_xi\_poisson\_cauchy\_all\_order\_normal\_form\_uniform\_remainder}
在结论 \ref{con:xi-poisson-cauchy-second-order-constants-with-mu3-remainder} 的记号下，设 \(N\ge2\) 且
\[
\mu_{N+1}:=\int_{\RR}|\gamma-\bar\gamma|^{N+1}\,\dd\tilde\nu(\gamma)<\infty.
\]
令
\[
f(y):=\frac{1}{1+y^2},\qquad y:=\frac{x-\bar\gamma}{t},\qquad
m_k:=\int_{\RR}(\gamma-\bar\gamma)^k\,\dd\tilde\nu(\gamma).
\]
则存在只依赖于 \(N\) 的常数 \(C_N>0\)，使得
\[
h_t(x)
=\frac{1}{\pi t}\sum_{k=0}^{N}\frac{(-1)^k m_k}{k!\,t^k}\,
f^{(k)}\!\left(\frac{x-\bar\gamma}{t}\right)
+R_{N+1}(t;x),
\]
且对全体 \(x\in\RR\) 有一致估计
\[
|R_{N+1}(t;x)|
\le \frac{C_N}{\pi}\,
\frac{\mu_{N+1}}{t^{N+2}}\,
\frac{1}{1+\bigl(\frac{x-\bar\gamma}{t}\bigr)^2}.
\]
\end{theorem}
\begin{proof}
写成缩放形式
\[
\frac{1}{\pi}\frac{t}{(x-\gamma)^2+t^2}
=\frac{1}{\pi t}\,
f\!\left(y-\frac{\gamma-\bar\gamma}{t}\right),\qquad
y:=\frac{x-\bar\gamma}{t}.
\]
对 \(u=(\gamma-\bar\gamma)/t\) 在 \(u=0\) 做 \(N\) 阶 Taylor 展开，
\[
f(y-u)=\sum_{k=0}^{N}\frac{(-u)^k}{k!}f^{(k)}(y)
+\frac{(-u)^{N+1}}{N!}\int_0^1(1-s)^N f^{(N+1)}(y-su)\,\dd s.
\]
对 \(\tilde\nu\) 积分即得主和式，其中系数恰为中心矩 \(m_k\)。
余项由上式最后一项给出。由于
\(
f^{(N+1)}(z)=P_{N+1}(z)/(1+z^2)^{N+2}
\)
（\(P_{N+1}\) 为多项式），存在 \(C_N\) 使
\(
|f^{(N+1)}(z)|\le C_N(1+z^2)^{-1}
\)。
再结合 \(\mu_{N+1}<\infty\) 与缩放因子 \(t^{-(N+2)}\) 即得结论。证毕。
\end{proof}
\begin{corollary}[Fisher--Krein 耗散尾常数的显式封口]\label{cor:xi-poisson-cauchy-fisher-krein-tail-half-var2}
\leanverified{paper\_xi\_poisson\_cauchy\_fisher\_krein\_tail\_half\_var2}
在结论 \ref{con:xi-poisson-cauchy-second-order-constants-with-mu3-remainder} 的设定下，令
\[
I(t):=-\frac{\dd}{\dd t}D_{\mathrm{KL}}(h_t\mid g_t).
\]
若 \(t\mapsto D_{\mathrm{KL}}(h_t\mid g_t)\) 可微并满足命题
\ref{prop:xi-poisson-kl-dissipation-fractional-fisher} 的耗散恒等式，则
\[
I(t)=\frac12\,\frac{\Var^2}{t^5}
+O\!\left(\frac{\mu_3\Var}{t^6}+\frac{\mu_3^2}{t^7}\right),
\]
从而
\[
\lim_{t\to\infty}t^5 I(t)=\frac12\,\Var^2.
\]
\end{corollary}
\begin{proof}
由结论 \ref{con:xi-poisson-cauchy-second-order-constants-with-mu3-remainder}
中 \(D_{\mathrm{KL}}\) 的展开逐项求导即得。证毕。
\end{proof}
\begin{theorem}[双参数 Poisson--Cauchy 正规形的无迹协方差结构]\label{thm:xi-poisson-cauchy-two-parameter-tracefree-covariance-structure}
\leanverified{paper\_xi\_poisson\_cauchy\_two\_parameter\_tracefree\_covariance\_structure}
令 \(\mu\in\mathcal P(\RR\times(0,\infty))\)，\((\Gamma,\Delta)\sim\mu\)，并记
\[
\bar\gamma:=\mathbb E[\Gamma],\qquad
\bar\delta:=\mathbb E[\Delta],\qquad
\Sigma:=
\begin{pmatrix}
\Var(\Gamma) & \Cov(\Gamma,\Delta)\\
\Cov(\Gamma,\Delta) & \Var(\Delta)
\end{pmatrix}.
\]
若
\(
\mathbb E\!\bigl[(|\Gamma-\bar\gamma|+|\Delta-\bar\delta|)^3\bigr]<\infty
\)，定义
\[
H_t(x):=\mathbb E\!\left[\frac{1}{\pi}\frac{t+\Delta}{(x-\Gamma)^2+(t+\Delta)^2}\right],\qquad
G_t(x):=\frac{1}{\pi}\frac{t+\bar\delta}{(x-\bar\gamma)^2+(t+\bar\delta)^2}.
\]
则当 \(t\to\infty\) 时，在 \(x\in\RR\) 上一致成立
\[
H_t(x)=G_t(x)
+\frac12\bigl(\Var(\Gamma)-\Var(\Delta)\bigr)\,\partial_x^2 G_t(x)
-\Cov(\Gamma,\Delta)\,\partial_x\partial_t G_t(x)
+O(t^{-4}),
\]
其中余项由三阶绝对中心矩控制。
\end{theorem}
\begin{proof}
对
\(
F(x,t;\gamma,\delta):=\frac{1}{\pi}\frac{t+\delta}{(x-\gamma)^2+(t+\delta)^2}
\)
在 \((\bar\gamma,\bar\delta)\) 处做二变量 Taylor 展开并取期望。
一次项因中心化消失。二次项收缩为
\[
\frac12\Var(\Gamma)\,\partial_\gamma^2F
+\Cov(\Gamma,\Delta)\,\partial_\gamma\partial_\delta F
+\frac12\Var(\Delta)\,\partial_\delta^2F.
\]
用 \(\partial_\gamma=-\partial_x\)、\(\partial_\delta=\partial_t\) 改写，再利用 Poisson 核调和恒等式
\(
\partial_t^2G_t+\partial_x^2G_t=0
\)
消去迹项，即得到仅含无迹组合的二阶主项。三阶余项由三阶矩与核导数尺度估计给出 \(O(t^{-4})\) 一致界。证毕。
\end{proof}
\begin{corollary}[协方差平衡触发的超二阶信息塌缩]\label{cor:xi-poisson-cauchy-covariance-balance-supersecond-collapse}
\leanverified{paper\_xi\_poisson\_cauchy\_covariance\_balance\_supersecond\_collapse}
在定理 \ref{thm:xi-poisson-cauchy-two-parameter-tracefree-covariance-structure} 的条件下，若
\[
\Var(\Gamma)=\Var(\Delta),\qquad
\Cov(\Gamma,\Delta)=0,
\]
则
\[
\|H_t-G_t\|_{L^1(\RR)}=O(t^{-3}),\qquad
D_{\mathrm{KL}}(H_t\mid G_t)=O(t^{-6}).
\]
隐含常数由三阶绝对中心矩控制。
\end{corollary}
\begin{proof}
在协方差平衡条件下，定理
\ref{thm:xi-poisson-cauchy-two-parameter-tracefree-covariance-structure}
的二阶主项完全抵消，故 \(H_t-G_t=O(t^{-4})\) 一致于 \(x\)，并给出 \(L^1\) 量级 \(O(t^{-3})\)。
再令 \(u_t:=(H_t-G_t)/G_t\)，则 \(\|u_t\|_{L^\infty}=O(t^{-3})\)。
由
\(
(1+u)\log(1+u)=u+\frac12u^2+O(|u|^3)
\)
在 \(G_t\)-加权积分下展开，得到
\(
D_{\mathrm{KL}}(H_t\mid G_t)=O(\int G_tu_t^2)=O(t^{-6})
\)。
证毕。
\end{proof}
\begin{conclusion}[黄金分辨率证书链的可计算加速：无迹协方差是唯一二阶障碍]\label{con:xi-golden-resolution-certificate-tracefree-covariance-bottleneck}
设缺陷测度取有限原子口径
\[
\nu=\sum_j m_j\delta_j\,\delta_{(\gamma_j,\delta_j)},
\]
并令共动 Poisson 场 \(G_t\) 如
\eqref{eq:xi-offcritical-defect-measure-nu}--\eqref{eq:xi-offcritical-comoving-poisson-field}
定义。记
\[
H_t(x):=\frac{G_t(x)}{\int_{\RR}G_t},\qquad
M_0:=\sum_j m_j\delta_j,\qquad
\mathbb P\!\left[(\Gamma,\Delta)=(\gamma_j,\delta_j)\right]=\frac{m_j\delta_j}{M_0}.
\]
则在 \(t\to\infty\) 的证书构造中，二阶可区分度主项（例如 \(L^1\) 与 \(D_{\mathrm{KL}}\)）
仅由
\[
\Var(\Gamma)-\Var(\Delta),\qquad
\Cov(\Gamma,\Delta)
\]
决定。因而任何“粗粒化可分辨度 \(\to0\)”的链式证书，其二阶瓶颈等价于同时压制上述两项。
若进一步实现
\[
\Var(\Gamma)=\Var(\Delta),\qquad \Cov(\Gamma,\Delta)=0,
\]
则自动触发推论
\ref{cor:xi-poisson-cauchy-covariance-balance-supersecond-collapse}
的超二阶塌缩，把相对熵衰减从 \(t^{-4}\) 制度性提升到 \(t^{-6}\)。
\end{conclusion}
\begin{proof}
把有限原子权重归一化后，\((\Gamma,\Delta)\) 的二阶统计量即由 \(\nu\) 显式给出。
将定理
\ref{thm:xi-poisson-cauchy-two-parameter-tracefree-covariance-structure}
与推论
\ref{cor:xi-poisson-cauchy-covariance-balance-supersecond-collapse}
应用于该随机向量，即得证书链的二阶瓶颈与加速结论。证毕。
\end{proof}
\begin{conjecture}[\texorpdfstring{\(\zeta\)}{zeta} 共动缺陷测度的协方差平衡机制]\label{conj:xi-zeta-comoving-defect-measure-covariance-balance}
在文内 \(\zeta\)-动力学框架中，缺陷测度
\(
\nu\subset\RR\times(0,\infty)
\)
同时编码高度 \(\gamma\) 与离线深度 \(\delta\)，并经共动 Poisson 场在尺度 \(t\) 上读出。
猜想存在自然规一化，使得在粗粒化极限（至少在有限缺陷分块口径下逐块）成立
\[
\Var(\Gamma)=\Var(\Delta),\qquad
\Cov(\Gamma,\Delta)=0.
\]
若该条件成立，则由结论
\ref{con:xi-golden-resolution-certificate-tracefree-covariance-bottleneck}
可把粗粒化信息塌缩从二阶提升到三阶，并在 Jensen 缺陷与相对熵可对齐的桥接框架中系统性加速黄金分辨率证书链。
\end{conjecture}
\endinput
