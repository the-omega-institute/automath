\begin{proposition}[四阶核族的偶谱五次闭式与偶谱单纯性]\label{prop:pom-a4t-even-zeta-quintic}
\leanverified{paper\_pom\_a4t\_even\_zeta\_quintic}
沿注 \ref{rem:pom-collision-rh-break-a4-threshold}，令 \(A_4(t)\) 为四阶碰撞核族，并定义
\[
D_t(z):=\det(I-zA_4(t)).
\]
则存在严格恒等式
\[
D_t(z)\,D_t(-z)=\det(I-z^2A_4(t)^2)=:E_t(u)\qquad(u=z^2),
\]
其中 \(E_t(u)\in\ZZ[t,u]\) 为关于 \(u\) 的五次整系数多项式，并且具有完全显式形式与判别式分解（式 \eqref{eq:collision_kernel_A4_even_zeta_quintic}）。
特别地，对所有 \(t\ge 0\) 且 \(t\neq 1\)，有 \(\mathrm{Disc}_u(E_t)\neq 0\)，从而 \(E_t(u)\) 的五个根在偶谱层面均为单根；因此在包含 \(t_\star\) 与整数点 \(t=7\) 的参数区间上，偶谱退化不可能作为核版 Newman 贴线破缺的触发机制。
\end{proposition}
\begin{proof}
恒等式来自行列式的乘法性：
\(
\det(I-zA)\det(I+zA)=\det\bigl((I-zA)(I+zA)\bigr)=\det(I-z^2A^2)
\)。
式 \eqref{eq:collision_kernel_A4_even_zeta_quintic} 给出 \(\mathrm{Disc}_u(E_t)=-256(t-1)^2(t+1)^2Q(t)\)，其中 \(Q(t)\) 的系数全为正，故当 \(t\ge 0\) 时 \(Q(t)>0\)；于是 \(\mathrm{Disc}_u(E_t)=0\) 在该半轴上仅可能发生于 \(t=1\)。
因此对 \(t\ge 0\) 且 \(t\neq 1\)，\(E_t\) 无重根，偶谱点始终单纯。证毕。
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_even_zeta_quintic}

\begin{proposition}[完成多项式的扭曲互反与 \texorpdfstring{$\Gamma$}{Gamma}-因子分裂]\label{prop:pom-a4t-even-spectrum-completion-gamma-factorization}
\leanverified{paper\_pom\_a4t\_even\_spectrum\_completion\_gamma\_factorization}
沿命题 \ref{prop:pom-a4t-even-zeta-quintic}，令
\[
E_t(u):=\det(I-uA_4(t)^2)\in\ZZ[t,u],
\qquad
\Xi_t(u):=u^5E_t(1/u)+4E_t(u).
\]
则存在严格恒等式
\[
\boxed{\;
\Xi_t(u)
=
u\,(tu+3u^2-2)\,(tu+4t-5u^2+4u+6)\;}
\]
从而 \(\Xi_t(u)\) 在 \(\ZZ[t,u]\) 中完全分裂为一个线性因子与两条二次 \(\Gamma\)-因子。
\end{proposition}
\begin{proof}
由式 \eqref{eq:collision_kernel_A4_even_zeta_quintic} 中的闭式，
\(E_t(u)=1-(2t+4)u+(t^2-4)u^2+(4t+8)u^3+4u^4-4u^5\)。
将其代入 \(\Xi_t(u)=u^5E_t(1/u)+4E_t(u)\) 并展开因式分解即可得到所示恒等式。证毕。
\end{proof}

\begin{proposition}[\texorpdfstring{$\Gamma$}{Gamma}-因子反演：由两条二次因子唯一重建偶谱五次多项式]\label{prop:pom-a4t-even-spectrum-gamma-inversion}
\leanverified{paper\_pom\_a4t\_even\_spectrum\_gamma\_inversion}
在命题 \ref{prop:pom-a4t-even-spectrum-completion-gamma-factorization} 的记号下，令
\[
Q_t(u):=(tu+3u^2-2)(tu+4t-5u^2+4u+6).
\]
则偶谱多项式满足精确重建公式
\[
\boxed{\;
E_t(u)=\frac{4u\,Q_t(u)-u^4\,Q_t(1/u)}{15}\ \in\ \ZZ[t,u]\; } .
\]
从而分子在 \(\ZZ[t,u]\) 中被 \(15\) 整除为结构性事实。
\end{proposition}
\begin{proof}
命题 \ref{prop:pom-a4t-even-spectrum-completion-gamma-factorization} 给出
\[
u^5E_t(1/u)+4E_t(u)=uQ_t(u).
\]
将该恒等式在 \(u\mapsto 1/u\) 下重写并乘以 \(u^5\)，得到
\(
E_t(u)+4u^5E_t(1/u)=u^4Q_t(1/u)
\)。
将两式视为关于未知量 \((E_t(u),u^5E_t(1/u))\) 的线性方程组消元，得到
\(
15E_t(u)=4uQ_t(u)-u^4Q_t(1/u)
\)。
由于左端为整系数多项式，右端必在 \(\ZZ[t,u]\) 中被 \(15\) 整除，且商即为 \(E_t(u)\)。证毕。
\end{proof}

\begin{proposition}[Newman 八次证书的曲线交点刻画：Perron 曲线与偶谱嵌入曲线的消元结果式]\label{prop:pom-a4t-newman-resultant-curve-intersection}
沿注 \ref{rem:pom-collision-rh-break-a4-threshold}，令四阶核族特征多项式为
\(
p_t(x)=\det(xI-A_4(t))=x^5-2x^4-tx^3-2x+2
\)，并将其 Perron 分支写成曲线
\[
P_t(r):=r^5-2r^4-tr^3-2r+2=0
\qquad(r>0).
\]
令 \(E_t(u)=\det(I-uA_4(t)^2)\) 如命题 \ref{prop:pom-a4t-even-zeta-quintic}，并定义偶谱嵌入曲线
\(
r^5E_t(1/r)=0
\)。
则二者关于 \(r\) 的结式消元给出严格恒等式（式 \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection}）：
其非平凡因子正是边权阈值的 8 次整系数证书 \(P_4(t)\)（式 \eqref{eq:collision_kernel_A4_edge_weight_threshold}）。
因此四阶 Newman 临界 \(t_\star\) 可等价刻画为：在排除退化点 \(t=-1\) 后，使得 Perron 曲线与偶谱嵌入曲线在 \((t,r)\)-平面上相交的唯一正参数点。
\end{proposition}
\begin{proof}
由式 \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection} 的结果式恒等式可知，
\(\mathrm{Res}_r(P_t(r),r^5E_t(1/r))\) 的零点集合（除去退化因子 \(t=-1\)）与 \(P_4(t)\) 的零点集合一致。
另一方面，式 \eqref{eq:collision_kernel_A4_edge_weight_threshold} 给出 \(P_4\) 的唯一正实根 \(t_\star\)，从而交点参数在正半轴上唯一。证毕。
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_newman_resultant_curve_intersection}

\begin{proposition}[Newman 八次阈值的更短消元刻画：两条五次曲线的交点证书]\label{prop:pom-a4t-newman-short-elimination}
\leanverified{paper\_pom\_a4t\_newman\_short\_elimination}
沿命题 \ref{prop:pom-a4t-even-spectrum-gamma-inversion}，定义
\[
G_t(r):=4r^4Q_t(1/r)-rQ_t(r).
\]
则恒有严格恒等式
\[
\boxed{\;G_t(r)=15\,r^5E_t(1/r)\;}
\]
从而关于 \(r\) 的结式满足
\[
\boxed{\;
\mathrm{Res}_r\!\bigl(P_t(r),\,G_t(r)\bigr)
=-2^3\cdot 3^5\cdot 5^5\cdot (t+1)^2\,P_4(t)\;}
\]
其中 \(P_4(t)\) 为式 \eqref{eq:collision_kernel_A4_edge_weight_threshold} 中的八次整系数阈值证书。
\end{proposition}
\begin{proof}
由命题 \ref{prop:pom-a4t-even-spectrum-gamma-inversion} 取 \(u=1/r\) 得
\[
E_t(1/r)=\frac{4r^3Q_t(1/r)-Q_t(r)}{15r^4},
\]
两边乘以 \(15r^5\) 即得 \(G_t(r)=15r^5E_t(1/r)\)。
\par
结式的比例因子由结果式的齐次性给出：若 \(f\) 为次数 \(m\) 多项式，则 \(\mathrm{Res}(f,cg)=c^{m}\mathrm{Res}(f,g)\)。
这里 \(P_t(r)\) 为关于 \(r\) 的五次多项式，故
\[
\mathrm{Res}_r(P_t,\,G_t)=15^5\,\mathrm{Res}_r(P_t,\,r^5E_t(1/r)).
\]
再代入式 \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection} 的恒等式并用 \(15^5=3^5\cdot 5^5\) 化简即得结论。证毕。
\end{proof}

\begin{proposition}[阈值的互反锁定律：\texorpdfstring{$Q_t$}{Qt} 在临界点处的刚性比例]\label{prop:pom-a4t-newman-reciprocal-locking}
\leanverified{paper\_pom\_a4t\_newman\_reciprocal\_locking}
令 \(t_\star>0\) 为 \(P_4(t)\) 的唯一正实根（式 \eqref{eq:collision_kernel_A4_edge_weight_threshold}）。
则存在 \(r_\star>1\) 使得同时满足 \(P_{t_\star}(r_\star)=0\) 与 \(G_{t_\star}(r_\star)=0\)；
后一条件等价于互反锁定关系
\[
\boxed{\;
Q_{t_\star}(r_\star)=4r_\star^{\,3}Q_{t_\star}(1/r_\star)\;}
\]
从而 Newman 临界点由一条“正向评估/反向评估”之间的严格三次幂比例平衡刻画。
\end{proposition}
\begin{proof}
令 \(r_\star=\rho(A_4(t_\star))>1\) 为 Perron 分支，故 \(P_{t_\star}(r_\star)=0\)。
由命题 \ref{prop:pom-a4t-newman-resultant-curve-intersection}，在该临界参数处 Perron 曲线与偶谱嵌入曲线相交，
从而 \(E_{t_\star}(1/r_\star)=0\)。
再由命题 \ref{prop:pom-a4t-newman-short-elimination} 的恒等式 \(G_t(r)=15r^5E_t(1/r)\)，得到 \(G_{t_\star}(r_\star)=0\)。
最后将 \(G_{t_\star}(r_\star)=0\) 展开即为所示互反锁定关系。证毕。
\end{proof}
\begin{proposition}[Newman 临界的极点二次共振刻画：\texorpdfstring{$D_t(v^2)=D_t(-v)=0$}{Dt(v^2)=Dt(-v)=0}]\label{prop:pom-a4t-newman-pole-quadratic-resonance}
\leanverified{paper\_pom\_a4t\_newman\_pole\_quadratic\_resonance}
沿注 \ref{rem:pom-collision-rh-break-a4-threshold}，令
\(
D_t(z):=\det(I-zA_4(t))
\)
为四阶核族的谱行列式（其显式闭式见式 \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance}）。
则四阶 Newman 临界参数 \(t_\star\) 等价于存在唯一 \(v_\star\in(0,1)\) 使得
\[
D_{t_\star}(v_\star^2)=0,\qquad D_{t_\star}(-v_\star)=0.
\]
等价地，若记 \(z_+(t)\in(0,1)\) 为 \(D_t\) 的最小正实根并记 \(z_-(t)\in(-1,0)\) 为其负实根分支，则
\[
z_+(t_\star)=z_-(t_\star)^2.
\]
消去 \(t\) 后，\(v_\star\) 满足不可约八次约束，且 \(t_\star\) 可由 \(v_\star\) 显式给出（式 \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance}）。
\end{proposition}
\begin{proof}
由定义，
\(
D_t(z)=\det(I-zA_4(t))=z^5\det(z^{-1}I-A_4(t))=z^5p_t(1/z)
\)；
故 \(D_t(z)=0\) 当且仅当 \(1/z\in\mathrm{spec}(A_4(t))\)。
另一方面，注 \ref{rem:pom-collision-rh-break-a4-threshold} 给出 Newman 达界的负实根通道刻画：\(t=t_\star\) 当且仅当存在 \(y<0\) 使 \(p_{t_\star}(y)=p_{t_\star}(y^2)=0\)。
令 \(v:=-1/y\in(0,1)\)，则 \(p_{t_\star}(y)=0\iff D_{t_\star}(-v)=0\)，并且 \(p_{t_\star}(y^2)=0\iff D_{t_\star}(v^2)=0\)，从而得到所示等价刻画。
\par
八次约束与 \(t_\star\) 的两条显式闭式由直接消元与代入展开给出；其可复现输出见式 \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance}（脚本 \texttt{scripts/exp\_collision\_kernel\_A4\_pole\_quadratic\_resonance\_and\_cyclotomic\_adjacency\_injection.py}）。证毕。
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_newman_pole_quadratic_resonance}

\begin{proposition}[核版 Newman 偏移的极点几何判据：\texorpdfstring{$\delta(t)=\log(z_+(t)/z_-(t)^2)$}{delta(t)=log(z+/z-^2)}]\label{prop:pom-a4t-newman-offset-pole-geometry}
沿注 \ref{rem:pom-collision-rh-break-a4-threshold}，令 \(t\ge 0\) 并记
\(
D_t(z):=\det(I-zA_4(t))
\)。
设 \(z_+(t)\in(0,1)\) 为 \(D_t\) 的最小正实根，并设 \(z_-(t)\in(-1,0)\) 为 \(D_t\) 的唯一负实根。
令
\[
r(t):=\rho(A_4(t))=\frac{1}{z_+(t)},\qquad
\mu_-(t):=\frac{1}{z_-(t)}<0,\qquad
\Lambda(t):=|\mu_-(t)|=-\frac{1}{z_-(t)}.
\]
则 Newman 型偏移 \(\delta(t):=\log(\Lambda(t)^2/r(t))\) 满足恒等式
\[
\delta(t)=\log\!\Bigl(\frac{z_+(t)}{z_-(t)^2}\Bigr),
\]
从而
\[
\delta(t)\begin{cases}
<0 &\Longleftrightarrow z_+(t)<z_-(t)^2,\\
=0 &\Longleftrightarrow z_+(t)=z_-(t)^2,\\
>0 &\Longleftrightarrow z_+(t)>z_-(t)^2.
\end{cases}
\]
并且 \(\delta(t)=0\) 当且仅当 \(t=t_\star\)。
\end{proposition}
\begin{proof}
由 \(D_t(z)=0\iff 1/z\in\mathrm{spec}(A_4(t))\) 可知 \(z_+(t)=1/r(t)\)。
另一方面，注 \ref{rem:pom-a4t-discriminant-root-type} 给出：对每个 \(t\ge 0\)，\(p_t\) 恰有三条实根与一对共轭非实根；而 \(A_4(t)\) 为非负矩阵，故其谱半径 \(r(t)\) 为实正特征值。
因此剩余两条实根一正一负，其中负实根对应的极点恰为 \(z_-(t)\in(-1,0)\)，并且唯一性成立。
由定义
\(
\delta(t)=\log(\Lambda(t)^2/r(t))
\)
以及
\(
\Lambda(t)=-1/z_-(t),\ r(t)=1/z_+(t)
\)
立得 \(\delta(t)=\log(z_+(t)/z_-(t)^2)\) 及其符号等价。
\par
最后，\(\delta(t)=0\iff z_+(t)=z_-(t)^2\)，而该二次共振条件与 \(t=t_\star\) 的等价性由命题 \ref{prop:pom-a4t-newman-pole-quadratic-resonance} 给出。证毕。
\end{proof}

\begin{remark}[极点比值的单阈值几何：\texorpdfstring{$z_+(t)/z_-(t)^2$}{z+(t)/z-(t)^2} 的审计单调窗]\label{rem:pom-a4t-pole-ratio-monotone-window}
在命题 \ref{prop:pom-a4t-newman-offset-pole-geometry} 的记号下，令
\(
g(t):=z_+(t)/z_-(t)^2
\)。
脚本 \texttt{scripts/exp\_collision\_kernel\_A4\_pole\_quadratic\_resonance\_and\_cyclotomic\_adjacency\_injection.py}
在包含 Newman 临界与 \((E_6,E_7,E_8)\) 指数交点的区间 \([t_{E_6},t_{E_8}]\) 上对 \(g(t)\) 作网格审计，
得到 \(g\) 在该窗内严格递增；结合 \(g(t_\star)=1\) 的唯一性，可把四阶相变等价为“正极点是否位于负极点平方的左侧/右侧”的几何判据。
\end{remark}
\paragraph{偶谱曲线的二次可解性、Lucas 消元与黄金比例共振塔}
命题 \ref{prop:pom-a4t-even-zeta-quintic} 与式 \eqref{eq:collision_kernel_A4_even_zeta_quintic} 给出 \(E_t(u)=\det(I-uA_4(t)^2)\) 的整系数闭式。
将该五次偶谱方程视为 \((t,u)\)-平面中的代数曲线，可进一步推出一条“关于 \(t\) 的二次可解性 \(\Rightarrow\) Lucas 坐标的四次消元 \(\Rightarrow\) 整参数二次分裂 \(\Rightarrow\) \(\varphi\)-幂谱离散共振”的结论链。

\begin{conclusion}[四阶偶谱曲线在 \(t\) 上的二次可解性与平方判别式]\label{con:pom-a4-even-spectrum-t-quadratic}
沿注 \ref{rem:pom-collision-rh-break-a4-threshold}，令 \(A_4(t)\) 为四阶五态核族，并令
\[
E_t(u):=\det(I-uA_4(t)^2)\in\ZZ[t,u]
\]
为偶谱多项式（式 \eqref{eq:collision_kernel_A4_even_zeta_quintic}）。
则对任意固定 \(u\neq 0\)，方程 \(E_t(u)=0\) 等价为关于 \(t\) 的二次方程
\[
\boxed{\;
u^2 t^2+(4u^3-2u)t+\bigl(1-4u-4u^2+8u^3+4u^4-4u^5\bigr)=0\; } .
\]
其判别式在 \(\ZZ[u]\) 中完全平方化：
\[
\boxed{\;
\Delta(u):=\mathrm{Disc}_t\!\bigl(E_t(u)\bigr)
=16u^3(u-1)^2(u+1)^2\; } .
\]
因此除退化点 \(u\in\{0,\pm1\}\) 外，偶谱曲线 \(E_t(u)=0\) 在 \(t\)-坐标上给出两条互异代数分支；其分支仅由 \(\sqrt u\) 与 \(u^2-1\) 控制。
\end{conclusion}

\begin{conclusion}[全局有理参数化：\texorpdfstring{$u=v^2$}{u=v^2} 归一化与支交换对合]\label{con:pom-a4-even-spectrum-rational-param}
在结论 \ref{con:pom-a4-even-spectrum-t-quadratic} 的记号下，置 \(u=v^2\)（\(v\neq 0\)）。
则偶谱曲线 \(E_t(u)=0\) 的归一化可由参数 \(v\) 给出，两支解为显式有理函数
\[
\boxed{\;
t_{\pm}(v)= -2v^2+\frac{1}{v^2}\ \pm\ 2\Bigl(v^3-\frac{1}{v}\Bigr)\; } .
\]
并且满足严格对称性 \(t_{-}(v)=t_{+}(-v)\)，故支交换由中心对合 \(v\mapsto -v\) 给出；分支退化仅出现在 \(v\in\{0,\pm1,\infty\}\) 的极限处（等价于 \(u\in\{0,\pm1\}\)）。
因此偶谱曲线的归一化为亏格 \(0\) 的有理曲线。
\end{conclusion}

\begin{conclusion}[Lucas 坐标消元的四次约束与判别式恒平方]\label{con:pom-a4-even-spectrum-lucas-elimination}
沿结论 \ref{con:pom-a4-even-spectrum-t-quadratic}，令 \(u\neq 0\) 并记
\[
L:=u+u^{-1}.
\]
则任意满足 \(E_t(u)=0\) 的点 \((t,u)\) 的投影 \((t,L)\) 必满足四次整系数方程
\[
\boxed{\;
R(t,L)=t^4+2Lt^3+(18+16L-3L^2-4L^3)t^2+(192+18L-80L^2-4L^3+8L^4)t
+(337-48L-164L^2+28L^3+20L^4-4L^5)=0\; } .
\]
并且 \(R(\cdot,L)\) 关于变量 \(t\) 的判别式在 \(\ZZ[L]\) 中为完全平方：
\[
\boxed{\;
\mathrm{Disc}_t(R)=256\,(L-2)^6(L+2)^6(4L-17)^2(4L-1)^2\; } .
\]
因此 \((t,L)\mapsto L\) 的四次覆盖在判别式意义下受交替型约束；其退化仅可能发生于
\(
L\in\{2,-2,17/4,1/4\}
\)。
\end{conclusion}

\begin{conclusion}[整参数子族 \texorpdfstring{$L=a^2-2$}{L=a^2-2} 的完全二次分裂]\label{con:pom-a4-even-spectrum-lucas-quadratic-splitting}
在结论 \ref{con:pom-a4-even-spectrum-lucas-elimination} 的多项式 \(R(t,L)\) 下，取整数参数 \(a\in\ZZ\) 并置 \(L=a^2-2\)。
则在 \(\ZZ[a,t]\) 中有严格分解
\[
\boxed{\;
R(t,a^2-2)=P_a^{(+)}(t)\,P_a^{(-)}(t)\; } ,
\]
其中两条二次因子为
\[
P_a^{(+)}(t)=t^2+\bigl(2a^3+a^2-8a-2\bigr)t+\bigl(-2a^5-6a^4+6a^3+24a^2+8a+1\bigr),
\]
\[
P_a^{(-)}(t)=t^2+\bigl(-2a^3+a^2+8a-2\bigr)t+\bigl(2a^5-6a^4-6a^3+24a^2-8a+1\bigr).
\]
因此存在一条以 \(a\in\ZZ\) 为层级指标的“二次可计算”共振塔：每个层级至多给出四个实参数 \(t\)，使得偶谱约束 \(E_t(u)=0\) 在 Lucas 位置 \(L=u+u^{-1}=a^2-2\) 发生精确命中。
\end{conclusion}

\begin{conclusion}[Lucas 层的黄金比例共振梯：\texorpdfstring{$u=\varphi^{\pm 4n}$}{u=phi^{pm4n}} 的实偶谱命中]\label{con:pom-a4-even-spectrum-golden-resonance-ladder}
令 \(\varphi=(1+\sqrt5)/2\)，并令 Lucas 数列 \((L_n)_{n\ge 0}\) 由 \(L_0=2,L_1=1\) 与 \(L_{n+1}=L_n+L_{n-1}\) 定义。
取 \(a=L_{2n}\in\ZZ\)（\(n\ge 1\)），则有恒等式
\[
L:=a^2-2=L_{2n}^2-2=L_{4n}.
\]
对任一满足 \(P_{L_{2n}}^{(\pm)}(t)=0\) 的实参数 \(t\)，结论 \ref{con:pom-a4-even-spectrum-lucas-quadratic-splitting} 给出 \(R(t,L_{4n})=0\)，从而存在 \(u\in\RR_{>0}\) 使 \(u+u^{-1}=L_{4n}\) 且 \(E_t(u)=0\)。
由于 \(L_{4n}=\varphi^{4n}+\varphi^{-4n}\)，故上述正实根必为
\[
u=\varphi^{4n}\qquad\text{或}\qquad u=\varphi^{-4n}.
\]
等价地，矩阵 \(A_4(t)\) 的偶谱命中强制出现实特征值
\[
\lambda=\pm u^{-1/2}
\in\{\pm\varphi^{2n},\ \pm\varphi^{-2n}\}.
\]
因此四阶核族在一条完全离散且可无限延拓的二次域参数序列上，精确吸附到 \(\varphi\)-幂谱通道。
\end{conclusion}

\begin{conclusion}[共振塔的尺度律：\texorpdfstring{$t_n\sim 2\varphi^{6n}$}{t_n~2phi^{6n}} 与 \texorpdfstring{$\varphi^6$}{phi^6} 放大比率]\label{con:pom-a4-even-spectrum-resonance-scale-law}
在结论 \ref{con:pom-a4-even-spectrum-golden-resonance-ladder} 的设定下，令 \(t_n\) 为二次方程 \(P_{L_{2n}}^{(-)}(t)=0\) 的最大正实根（等价地，为 \(R(t,L_{4n})=0\) 的最大正实根）。
则当 \(n\to\infty\) 时成立渐近展开
\[
\boxed{\;
t_n=2\varphi^{6n}-2\varphi^{4n}+O(\varphi^{2n})\; } ,
\]
并且层间放大比率极限存在且满足
\[
\boxed{\;
\lim_{n\to\infty}\frac{t_{n+1}}{t_n}=\varphi^6\; } .
\]
因此上述 Lucas/黄金比例共振并非零散点集，而呈现严格指数层级；其尺度常数由 \(\varphi^6\) 锁定。
\end{conclusion}

\begin{conclusion}[共振塔与 Newman 临界的算术分离：二次可计算族不包含八次临界点]\label{con:pom-a4-even-spectrum-resonance-vs-newman-separation}
由结论 \ref{con:pom-a4-even-spectrum-lucas-quadratic-splitting}，Lucas 共振塔所生成的全部共振参数 \(t\) 都满足某个二次方程 \(P_a^{(\pm)}(t)=0\)，其代数次数至多为 \(2\)，并且
\[
t\in\QQ(\sqrt{a^2-4})
\qquad(a\in\ZZ).
\]
相反，四阶 Newman 临界参数 \(t_\star\) 由式 \eqref{eq:collision_kernel_A4_edge_weight_threshold} 的八次整系数证书 \(P_4(t_\star)=0\) 确定，且 \(P_4\) 在 \(\QQ[t]\) 上不可约，从而 \(t_\star\) 的最小多项式次数为 \(8\)。
因此
\[
\boxed{\;
t_\star\notin \bigcup_{a\in\ZZ}\ \QQ(\sqrt{a^2-4})\;}
\]
即 Newman 临界机制在代数次数层面与上述全部 Lucas/黄金比例共振塔严格互不相交；两类现象属于不同的算术分辨率层级。
\end{conclusion}
\paragraph{Newman 八次临界场的稀疏生成元与局部算术指纹}
在同一八次分辨率层级上，Newman 临界场不仅可由 \(t\)-坐标的八次证书 \(P_4(t)=0\)（式 \eqref{eq:collision_kernel_A4_edge_weight_threshold}）刻画；
沿注 \ref{rem:pom-collision-rh-break-a4-threshold} 的负实根通道，还可选择一个判别式显著更小的稀疏整系数生成元，从而把局部模 \(p\) 指纹与分歧肖像压缩到更短的可审计证书上。

\begin{conclusion}[Newman 八次场的稀疏生成元与一次五次消元约束]\label{con:pom-a4-newman-sparse-generator-elimination}
令两多项式 \(\mathcal Z(z)\in\ZZ[z]\)、\(\mathcal C(t,z)\in\ZZ[t,z]\) 如式 \eqref{eq:collision_kernel_A4_newman_sparse_generator}。
则存在严格消元恒等式
\[
\boxed{\;
\mathrm{Res}_z\!\bigl(\mathcal Z(z),\mathcal C(t,z)\bigr)=-8\,P_4(t)\;}
\]
其中 \(P_4(t)\) 为四阶 Newman 阈值的八次整系数证书（式 \eqref{eq:collision_kernel_A4_edge_weight_threshold}）。
因此对任一根 \(z\) 满足 \(\mathcal Z(z)=0\)，由于 \(\mathcal Z\) 无零根，方程 \(\mathcal C(t,z)=0\) 在 \(t\) 上给出唯一解
\[
t=t(z)=\frac{z^5+2z^4-2z-2}{z^3}\in\QQ(z),
\]
并且该 \(t(z)\) 必满足 \(P_4(t(z))=0\)。
由于 \(P_4\) 在 \(\QQ[t]\) 上不可约（结论 \ref{con:pom-a4-even-spectrum-resonance-vs-newman-separation}），故 \([\QQ(t(z)):\QQ]=8\)；
另一方面 \(\QQ(t(z))\subseteq \QQ(z)\) 且 \(\deg\mathcal Z=8\) 强制 \([\QQ(z):\QQ]\le 8\)，从而 \([\QQ(z):\QQ]=8\) 且
\[
\boxed{\;\QQ(t(z))=\QQ(z)\;}
\]
即 Newman 临界八次场可等价理解为：一条五次一次约束 \(\mathcal C(t,z)=0\) 与一条六项稀疏八次约束 \(\mathcal Z(z)=0\) 所生成的同一数域。
可复现脚本：\texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator.py}。
\end{conclusion}

\input{sections/generated/eq_collision_kernel_A4_newman_sparse_generator}

\begin{conclusion}[判别式平方差与幂整指数：\(t\)-生成元的额外坏素数来自指数贡献]\label{con:pom-a4-newman-discriminant-index-artifact}
记 \(\mathrm{Disc}(\cdot)\) 为多项式判别式。
则式 \eqref{eq:collision_kernel_A4_newman_sparse_generator} 给出 \(\mathrm{Disc}_z(\mathcal Z)\) 与 \(\mathrm{Disc}_t(P_4)\) 的完全因子分解，并满足严格平方比关系
\[
\boxed{\;
\frac{\mathrm{Disc}_t(P_4)}{\mathrm{Disc}_z(\mathcal Z)}
=\Bigl(2^{5}\cdot 5\cdot 109^{2}\cdot 4139873\Bigr)^2\;}
\]
从而素数 \(5,109,4139873\) 不出现在稀疏生成元 \(\mathcal Z\) 的判别式支撑中，而仅以完全平方指数出现在 \(t\)-生成元的判别式中。
在算术意义上，这等价于：它们不承载该八次场的二次分辨率信息，而仅记录 \(\ZZ[t]\) 相对同一数域整数环的幂整指数贡献（参见命题 \ref{prop:pom-e8-intersection-field-discriminant-index} 的判别式—指数恒等式口径）。
\end{conclusion}

\begin{conclusion}[共同二次脊柱：\texorpdfstring{$\QQ(\sqrt{-1151})$}{Q(sqrt(-1151))} 支配两种生成元的 Frobenius 奇偶]\label{con:pom-a4-newman-common-quadratic-spine}
由式 \eqref{eq:collision_kernel_A4_newman_sparse_generator} 的判别式分解，\(\mathrm{Disc}_z(\mathcal Z)\) 与 \(\mathrm{Disc}_t(P_4)\) 的平方自由部分一致，均为 \(-1151\)；
\par
令 \(L_\star\) 为 \(P_4(t)\) 的分裂域（等价地，亦为 \(\mathcal Z(z)\) 的分裂域）。
则 \(L_\star\) 含有唯一判别式二次子域
\[
K_2=\QQ(\sqrt{-1151})
\]
（与命题 \ref{prop:pom-a4t-newman-k2-class-number} 的 \(t_\star\)-口径一致）。
对任意素数 \(p\nmid 2\cdot 7\cdot 23\cdot 1151\)，在群 \(\mathrm{Gal}(L_\star/\QQ)\subseteq S_8\) 的自然置换表示下，Frobenius 置换的奇偶性由同一 Kronecker 符号严格给出：
\[
\mathrm{sgn}(\mathrm{Frob}_p)=\left(\frac{-1151}{p}\right).
\]
因此四阶 Newman 临界的全部局部奇偶诊断可在任一生成元表示下无损转移，而 \(\QQ(\sqrt{-1151})\) 构成该临界算术的不可消去二次脊柱。
\end{conclusion}

\begin{conclusion}[稀疏生成元的伽罗瓦群为 \texorpdfstring{$S_8$}{S8}：由三条分裂型证书排除原始小群]\label{con:pom-a4-newman-sparse-galois-s8}
设 \(G=\mathrm{Gal}(\mathcal Z/\QQ)\)。
由脚本 \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator.py} 的模 \(p\) 分解证书：
\begin{itemize}
  \item 在模 \(13\) 下，\(\mathcal Z\bmod 13\) 不可约（Frobenius 含 \(8\)-循环）；
  \item 在模 \(3\) 下，\(\mathcal Z\bmod 3\) 的因子次数型为 \((7,1)\)（Frobenius 含 \(7\)-循环，因而原始）；
  \item 在模 \(5\) 下，\(\mathcal Z\bmod 5\) 的因子次数型为 \((6,2)\)（Frobenius 含 \((6)(2)\) 循环型）。
\end{itemize}
又由于 \(\mathrm{Disc}_z(\mathcal Z)\) 非平方，故 \(G\nsubseteq A_8\)。
结合度 \(8\) 原始子群的分类：含 \(7\)-循环的原始候选仅为 \(S_8,A_8,\mathrm{AGL}(1,8),\mathrm{PGL}_2(7)\)；
其中后两者不含 \((6,2)\) 循环型，故被排除。
因此
\[
\boxed{\;G=S_8\;}
\]
并且由结论 \ref{con:pom-a4-newman-sparse-generator-elimination} 的域同构，\(P_4(t)\) 的分裂域与 \(\mathcal Z\) 相同，故 \(P_4\) 亦具有同一满对称伽罗瓦群 \(S_8\)。
\end{conclusion}

\begin{conclusion}[判别式支撑素数的局部分歧肖像：两处三重重根与一处二重重根]\label{con:pom-a4-newman-ramification-portrait}
在判别式支撑素数处，稀疏生成元 \(\mathcal Z\) 的约化分解呈现稳定的重根结构：
\[
\mathcal Z(z)\equiv (z-2)^3\cdot Q_7(z)\pmod 7\quad(\deg Q_7=5),
\]
\[
\mathcal Z(z)\equiv (z-6)^3\cdot(z-7)\cdot Q_{23}^{(1)}(z)\cdot Q_{23}^{(2)}(z)\pmod{23}\quad(\deg Q_{23}^{(i)}=2),
\]
\[
\mathcal Z(z)\equiv (z+499)^2\cdot\prod_{i=1}^{3}Q_{1151}^{(i)}(z)\pmod{1151}\quad(\deg Q_{1151}^{(i)}=2).
\]
并且在模 \(2\) 下有完全塌缩 \(\mathcal Z(z)\equiv z^8\pmod 2\)。
因此该八次场在 \(7\) 与 \(23\) 处出现三重重根指纹，在 \(1151\) 处出现二重重根指纹，而在 \(2\) 处呈现全阶野分歧的重根指纹；
这给出一个可直接核验的局部分歧肖像：两处三重核、一处二重核以及一处全阶野核。
\end{conclusion}

\begin{conclusion}[\texorpdfstring{$E_8$}{E8} 指数影子：去除二次脊柱后，唯一奇素数支撑为 \(\{7,23\}\)]\label{con:pom-a4-newman-e8-exponent-shadow}
由式 \eqref{eq:collision_kernel_A4_newman_sparse_generator}，\(\mathrm{Disc}_z(\mathcal Z)\) 的奇素数支撑除去二次脊柱 \(1151\) 后仅剩
\[
\{7,23\}.
\]
而 \(E_8\) 的 Coxeter 指数集合为 \(\{1,7,11,13,17,19,23,29\}\)，其中恰含素数指数 \(7\) 与 \(23\)。
因此在四阶 Newman 临界的稀疏生成元坐标中，出现一个可严格陈述的指数投影：除去二次脊柱后，该八次场的全部奇素数分歧被压缩为 \(E_8\) 的两枚素数指数。
\end{conclusion}

\begin{conclusion}[局部指纹的无损转移：用稀疏生成元替代阈值多项式进行模 \texorpdfstring{$p$}{p} 解码]\label{con:pom-a4-newman-local-fingerprint-transfer}
由结论 \ref{con:pom-a4-newman-sparse-generator-elimination} 的域同构与结论 \ref{con:pom-a4-newman-discriminant-index-artifact} 的指数平方比，
对任意素数
\[
p\nmid 2\cdot 5\cdot 7\cdot 23\cdot 109\cdot 1151\cdot 4139873,
\]
两种生成元给出同一有限 \(\FF_p\)-代数（同构为分解同态的直积），从而
\[
\boxed{\;
\mathcal Z(z)\bmod p\ \text{与}\ P_4(t)\bmod p\ \text{具有相同的因子次数多重集}\;}
\]
亦即二者诱导出同一 Frobenius 循环型。
因此四阶 Newman 临界场的模 \(p\) 局部指纹（分裂型与由二次脊柱决定的奇偶层）可在计算上转移到六项稀疏多项式 \(\mathcal Z\) 上完成，从而将高系数八次阈值多项式的模分解问题压缩为稀疏八次多项式的快速分解问题。
\end{conclusion}
\paragraph{稀疏生成元场的整环与局部化：幂整基、分歧指数与剩余代数厚度}
在稀疏生成元坐标下，Newman 八次临界场不仅在判别式与模分裂型层面呈现可压缩结构，
其幂整基、四个分歧素数处的局部化及剩余代数亦可被写成完全显式且可复核的算术接口。

\begin{conclusion}[Newman 稀疏生成元场的幂整基与 2-Eisenstein 结构]\label{con:pom-a4-newman-integral-basis-2eisenstein}
令 \(K=\QQ(\theta)\)，其中 \(\theta\) 为稀疏八次多项式 \(\mathcal Z(z)\in\ZZ[z]\)（式 \eqref{eq:collision_kernel_A4_newman_sparse_generator}）的任一根。
则 \(\mathcal Z\) 在素数 \(2\) 上为 Eisenstein，从而在 \(\QQ\) 上不可约且 \(2\) 在 \(K/\QQ\) 中全分歧。
并且 \(K\) 为单生成（monogenic）八次场：
\[
\mathcal O_K=\ZZ[\theta],
\qquad
\{1,\theta,\theta^2,\dots,\theta^7\}\ \text{为}\ \mathcal O_K\ \text{的一组整基}.
\]
因此其域判别式满足
\[
\mathrm{Disc}(K)=\mathrm{Disc}_z(\mathcal Z)=-2^{10}\cdot 7^{2}\cdot 23^{2}\cdot 1151,
\]
并且 \(\mathcal Z\) 的判别式等于该幂整基的判别式（无额外指数伪影）。
此外，\(\mathcal Z\) 具有恰两条实根，故 \(K\) 的签名为 \((r_1,r_2)=(2,3)\)，从而单位秩为 \(r_1+r_2-1=4\)。
可复现脚本：\texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator\_arithmetic.py}。
\end{conclusion}

\input{sections/generated/eq_collision_kernel_A4_newman_sparse_generator_arithmetic}

\begin{conclusion}[四个分歧素数的完全素理想分解：\(2\) 全野分歧，\(\{7,23,1151\}\) 纯驯分歧]\label{con:pom-a4-newman-prime-ideal-decomposition-ramified}
在结论 \ref{con:pom-a4-newman-integral-basis-2eisenstein} 的数域 \(K\) 中，
\(2,7,23,1151\) 为全部分歧素数；其素理想分解如式 \eqref{eq:collision_kernel_A4_newman_sparse_generator_arithmetic} 所列。
特别地：
\begin{itemize}
  \item \(2\) 处仅有一条素理想 \(\mathfrak p_2\) 且 \(e(\mathfrak p_2\mid 2)=8,f(\mathfrak p_2\mid 2)=1\)，故 \((2)=\mathfrak p_2^{8}\)；
  \item \(7\) 处分解为两支：\(\mathfrak p_7\) 满足 \(e=3,f=1\)，而 \(\mathfrak q_7\) 满足 \(e=1,f=5\)，故 \((7)=\mathfrak p_7^{3}\mathfrak q_7\)；
  \item \(23\) 处分解为四支：\(\mathfrak p_{23}\) 满足 \(e=3,f=1\)，\(\mathfrak a_{23}\) 满足 \(e=1,f=1\)，\(\mathfrak b_{23},\mathfrak c_{23}\) 满足 \(e=1,f=2\)，故 \((23)=\mathfrak p_{23}^{3}\mathfrak a_{23}\mathfrak b_{23}\mathfrak c_{23}\)；
  \item \(1151\) 处分解为四支：\(\mathfrak p_{1151}\) 满足 \(e=2,f=1\)，其余三支满足 \(e=1,f=2\)，故 \((1151)=\mathfrak p_{1151}^{2}\mathfrak b_{1151}\mathfrak c_{1151}\mathfrak d_{1151}\)。
\end{itemize}
\end{conclusion}

\begin{conclusion}[判别式指数的驯分歧最小公式与局部惯性群阶]\label{con:pom-a4-newman-tame-discriminant-inertia}
对每个奇分歧素数 \(p\in\{7,23,1151\}\)，由结论 \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} 的分解可知 \(p\nmid e(\mathfrak p\mid p)\)，故 \(K/\QQ\) 在一切奇分歧处均为纯驯分歧。
因此域判别式的 \(p\)-进指数满足严格恒等式
\[
\boxed{\;
v_p(\mathrm{Disc}(K))=\sum_{\mathfrak p\mid p} f(\mathfrak p\mid p)\,\bigl(e(\mathfrak p\mid p)-1\bigr)\;}
\]
从而
\[
v_7(\mathrm{Disc}(K))=2,\qquad v_{23}(\mathrm{Disc}(K))=2,\qquad v_{1151}(\mathrm{Disc}(K))=1.
\]
并且在驯分歧情形下惯性子群必为循环群：在 \(p=7,23\) 处惯性群阶为 \(3\)，在 \(p=1151\) 处惯性群阶为 \(2\)。
此外，由结论 \ref{con:pom-a4-newman-integral-basis-2eisenstein} 的 \(\mathcal O_K=\ZZ[\theta]\)，判别式指数中不存在任何来自生成元指数的额外平方项。
\end{conclusion}

\begin{conclusion}[“两素数证书”强制 \texorpdfstring{$S_8$}{S8}：由 \(p=2\) 的 Eisenstein、\(p=7\) 的 \((3)(5)\) 与非平方判别式]\label{con:pom-a4-newman-two-prime-s8}
令 \(L\) 为 \(\mathcal Z\) 的分裂域，并记 \(G=\mathrm{Gal}(L/\QQ)\le S_8\) 作用于 \(\mathcal Z\) 的八个根。
由结论 \ref{con:pom-a4-newman-integral-basis-2eisenstein}，\(\mathcal Z\) 在素数 \(2\) 上为 Eisenstein，从而在 \(\QQ\) 上不可约；因此 \(G\) 在八根上的置换表示为传递群。
\par
另一方面，结论 \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} 表明 \(7\) 在 \(K/\QQ\) 中出现一支 \(e=3\) 的驯分歧并同时出现一支剩余次数 \(5\) 的分解支；
因此在分裂域 \(L\) 的某个分解群中，惯性群含有阶 \(3\) 的元素，而剩余域 Frobenius（在商群 \(D/I\) 中）含有阶可被 \(5\) 整除的元素。
由于 \(7\equiv 1\pmod 3\)，驯分歧情形下 Frobenius 对惯性群的共轭作用为 \(x\mapsto x^{7}\)，从而该作用在阶 \(3\) 的惯性上平凡；
于是分解群含有阶 \(15\) 的元素。
在 \(S_8\) 的自然置换表示下，阶 \(15\) 的循环型只能为 \((3)(5)\)，从而 \(G\) 含有一条 \(5\)-循环。
\par
群论上，含 \(5\)-循环的传递子群必为原始；由 Jordan 定理，原始子群一旦包含素数长循环（此处为 \(5\)）便含 \(A_8\)。
最后，由结论 \ref{con:pom-a4-newman-integral-basis-2eisenstein}，\(\mathrm{Disc}_z(\mathcal Z)\) 非平方，故 \(G\nsubseteq A_8\)。
综上 \(G=S_8\)。
\end{conclusion}

\begin{conclusion}[四个分歧点的剩余代数分解：\(\mathcal O_K/p\mathcal O_K\) 的幂零厚度]\label{con:pom-a4-newman-residue-algebra-decomposition}
由结论 \ref{con:pom-a4-newman-integral-basis-2eisenstein} 的 \(\mathcal O_K=\ZZ[\theta]\) 与结论 \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} 的局部分解，
四个分歧素数处的剩余代数可写成显式直积同构：
\[
\mathcal O_K/2\mathcal O_K \cong \FF_2[\varepsilon]/(\varepsilon^8),
\]
\[
\mathcal O_K/7\mathcal O_K \cong \FF_7[\varepsilon]/(\varepsilon^3)\times \FF_{7^5},
\]
\[
\mathcal O_K/23\mathcal O_K \cong \FF_{23}\times \FF_{23}[\varepsilon]/(\varepsilon^3)\times \FF_{23^2}\times \FF_{23^2},
\]
\[
\mathcal O_K/1151\mathcal O_K \cong \FF_{1151}[\varepsilon]/(\varepsilon^2)\times (\FF_{1151^2})^{3}.
\]
因此该八次场在四个分歧点处的幂零厚度参数为 \((8;3;3;2)\)，并对应剩余域次数谱
\[
(1;\,5;\,1,2,2;\,2,2,2).
\]
\end{conclusion}

\begin{conclusion}[四个规范化局部坐标：\(\theta\) 与 \(\theta-a\) 给出统一化的赋值标度]\label{con:pom-a4-newman-normalized-local-parameters}
在结论 \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} 的分歧素理想处，存在天然的“坐标—赋值”锁定。
记 \(v_{\mathfrak p}\) 为与 \(\mathfrak p\mid p\) 对应的离散赋值，按 \(v_{\mathfrak p}(p)=1\) 归一化。
则 \(\theta\) 分别在四个分歧点给出局部参数：
\[
v_{\mathfrak p_2}(\theta)=\frac{1}{8},\qquad
v_{\mathfrak p_7}(\theta-2)=\frac{1}{3},\qquad
v_{\mathfrak p_{23}}(\theta-6)=\frac{1}{3},\qquad
v_{\mathfrak p_{1151}}(\theta+499)=\frac{1}{2}.
\]
该归一化使得任何与临界相关的代数量之 \(p\)-进敏感度可被直接投射到分歧指数 \((8,3,3,2)\) 所锁定的赋值常数上。
可复现脚本：\texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator\_arithmetic.py}。
\end{conclusion}
