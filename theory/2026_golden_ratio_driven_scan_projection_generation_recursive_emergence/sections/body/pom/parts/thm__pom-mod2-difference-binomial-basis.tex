\begin{theorem}[模 \(p\) 的幂零差分与二项式基规范形]\label{thm:pom-modp-difference-binomial-basis}
令 \(p\) 为素数，令 \((a_m)_{m\ge 0}\) 为取值于 \(\FF_p\) 的序列。
记前移算子 \(E\) 为 \((Ea)_m:=a_{m+1}\)，并记差分算子 \(\Delta:=E-1\)。
若存在整数 \(b\ge 1\) 与阈值 \(m_0\) 使得对所有 \(m\ge m_0\) 有
\[
\Delta^b a_m=0,
\]
则存在唯一系数 \(c_0,\dots,c_{b-1}\in\FF_p\) 使得对所有充分大的 \(m\) 都有
\[
a_m=\sum_{j=0}^{b-1} c_j\binom{m}{j}\quad(\mathrm{mod}\ p).
\]
此外，令 \(k:=\lceil\log_p b\rceil\)。
则该最终序列为纯周期序列，且周期整除 \(p^k\)，更精确地，
\[
a_{m+p^k}=a_m\quad(\mathrm{mod}\ p)
\qquad
(\text{对所有充分大的 }m).
\]
\end{theorem}
\begin{proof}
组合恒等式给出 \(\Delta\binom{m}{j}=\binom{m}{j-1}\)（并约定 \(\binom{m}{-1}=0\)），在 \(\FF_p\) 上仍成立。
因此当且仅当 \(j<b\) 时有 \(\Delta^b\binom{m}{j}=0\)，从而任意右端线性组合必满足 \(\Delta^b a_m=0\)。
\par
反之，设对所有 \(m\ge m_0\) 有 \(\Delta^b a_m=0\)。
则 \(\Delta^{b-1}a_m\) 在 \(m\ge m_0\) 上最终常值，记该常值为 \(c_{b-1}\in\FF_p\)。
令
\(
a^{(1)}_m:=a_m-c_{b-1}\binom{m}{b-1}
\)，
则对充分大的 \(m\) 有 \(\Delta^{b-1}a^{(1)}_m=0\)。
递归下降，依次构造 \(c_{b-2},\dots,c_0\) 即得所示表示；唯一性由 \(\{\binom{m}{j}\}_{j=0}^{b-1}\) 在线性递推解空间中的线性无关性给出。
\par
取 \(k\) 使 \(p^k\ge b\)。
在 \(\FF_p[E]\) 中有
\[
(E-1)^{p^k}=E^{p^k}-1,
\]
因为
\((E-1)^{p^k}=\sum_{j=0}^{p^k}\binom{p^k}{j}E^{p^k-j}(-1)^j\)，
且对 \(0<j<p^k\) 有 \(p\mid \binom{p^k}{j}\)。
由 \(\Delta^b a_m=0\) 且 \(p^k\ge b\) 得 \(\Delta^{p^k}a_m=0\)（对充分大 \(m\)），故 \((E^{p^k}-1)a_m=0\)，即 \(a_{m+p^k}=a_m\)（对充分大 \(m\)）。证毕。
\end{proof}
\leanverified{paper\_pom\_modp\_difference\_binomial\_basis}

\begin{theorem}[模 \(2\) 塌缩特征多项式强制的高阶差分多项式性]\label{thm:pom-mod2-difference-binomial-basis}
\leanverified{paper\_pom\_mod2\_difference\_binomial\_basis}
令 \((a_m)_{m\ge 0}\) 为取值于 \(\FF_2\) 的序列，记前移算子 \(E\) 为 \((Ea)_m:=a_{m+1}\)，并记差分算子
\[
\Delta:=E+1
\]
（在特征 \(2\) 下与前向差分一致）。
若存在整数 \(b\ge 1\) 与阈值 \(m_0\) 使得对所有 \(m\ge m_0\) 有
\[
\Delta^b a_m=0,
\]
则存在常数 \(c_0,\dots,c_{b-1}\in\FF_2\) 使得对所有充分大的 \(m\) 都有
\[
a_m=\sum_{j=0}^{b-1} c_j\binom{m}{j}\quad(\mathrm{mod}\ 2).
\]
因此 \((a_m)\) 在 \(\FF_2\) 上最终等于一个次数 \(<b\) 的“二项式基多项式序列”。
\end{theorem}

\begin{proof}
组合恒等式给出 \(\Delta\binom{m}{j}=\binom{m}{j-1}\)（并约定 \(\binom{m}{-1}=0\)），在 \(\FF_2\) 上仍成立。
因此当且仅当 \(j<b\) 时有 \(\Delta^b\binom{m}{j}=0\)，从而任意右端线性组合必满足 \(\Delta^b a_m=0\)。
\par
反之，设对所有 \(m\ge m_0\) 有 \(\Delta^b a_m=0\)。
则 \(\Delta^{b-1}a_m\) 在 \(m\ge m_0\) 上最终常值，记该常值为 \(c_{b-1}\in\FF_2\)。
令
\(
a^{(1)}_m:=a_m-c_{b-1}\binom{m}{b-1}
\)，
则对充分大的 \(m\) 有 \(\Delta^{b-1}a^{(1)}_m=0\)。
递归下降，依次构造 \(c_{b-2},\dots,c_0\) 使得
\(
a_m-\sum_{j=0}^{b-1}c_j\binom{m}{j}
\)
的高阶差分最终为零，从而该差序列最终恒为零，得到所示表示。证毕。
\end{proof}

\begin{corollary}[碰撞矩序列的模 \(2\) 阶跃：\(q=7\) 处差分阶数从 \(2\) 跃迁到 \(4\)]\label{cor:pom-mod2-staircase-q7-difference-order-jump}
沿用注 \ref{rem:pom-charpoly-mod2-staircase-q2-10} 的记号。
在 \(q\le 10\) 的已核验范围内，最小递推特征多项式满足
\[
P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\pmod 2,
\]
且 \(b_q=2\) 在 \(q=4,5,6\) 上成立，而从 \(q=7\) 起跃迁为 \(b_q=4\) 并在 \(q=7,8,9,10\) 上稳定保持。
因此对充分大的 \(m\) 有
\[
\Delta^{b_q}S_q(m+a_q)\equiv 0\pmod 2,
\]
并由定理 \ref{thm:pom-mod2-difference-binomial-basis} 得：\(S_q(m)\bmod 2\) 在 \(q=4,5,6\) 时最终为次数 \(<2\) 的二项式基多项式序列，而在 \(q=7,8,9,10\) 时最终为次数 \(<4\) 的二项式基多项式序列。
\end{corollary}
\leanverified{paper\_pom\_mod2\_staircase\_q7\_difference\_order\_jump}

\begin{corollary}[模 \(2\) 影子差分推出最终周期与周期上界]\label{cor:pom-mod2-shadow-eventual-period-bound}
沿用推论 \ref{cor:pom-mod2-staircase-q7-difference-order-jump} 的记号，并令 \(s_m:=S_q(m)\bmod 2\in\FF_2\)。
取 \(t:=\lceil\log_2 b_q\rceil\)，则对充分大的 \(m\) 恒有
\[
s_{m+a_q+2^t}=s_{m+a_q}.
\]
因此序列 \((s_m)\) 在去掉有限前段并平移 \(m\mapsto m+a_q\) 后为纯周期序列，其最终周期整除 \(2^t\)。
\end{corollary}
\leanverified{paper\_pom\_mod2\_shadow\_eventual\_period\_bound}

\begin{proof}
由推论 \ref{cor:pom-mod2-staircase-q7-difference-order-jump}，对充分大的 \(m\) 有 \(\Delta^{b_q}s_{m+a_q}=0\)。
取 \(t=\lceil\log_2 b_q\rceil\) 则 \(2^t\ge b_q\)，从而 \(\Delta^{2^t}s_{m+a_q}=0\) 亦成立。
在 \(\FF_2[E]\) 中有恒等式 \((E+1)^{2^t}=E^{2^t}+1\)，故
\[
(E^{2^t}+1)s_{m+a_q}=0,
\]
即 \(s_{m+a_q+2^t}=s_{m+a_q}\)（对充分大的 \(m\)）。
最后一句由“存在周期 \(2^t\)”推出“最终周期整除 \(2^t\)”立得。证毕。
\end{proof}

\begin{conjecture}[模 \(2\) 塌缩指数的幂次塔：二值跳跃层级无界]\label{conj:pom-mod2-collapse-exponent-tower}
设 \(b_q\) 为模 \(2\) 塌缩分解
\(
P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\ (\mathrm{mod}\ 2)
\)
中 \((\lambda+1)\) 的指数（按最小整系数递推特征多项式 \(P_q\) 的模 \(2\) 约化定义）。
猜想：
\begin{enumerate}
\normalfont
  \item \(b_q\) 仅取 \(2\) 的幂并随 \(q\) 非减；
  \item 存在无穷增序列 \(q_1<q_2<\cdots\) 使得 \(b_{q_{n+1}}=2\,b_{q_n}\)；
  \item 对每个 \(q\)，\(b_q\) 给出 \(S_q(m)\bmod 2\) 的最小幂零差分阶数：对充分大的 \(m\)，有 \(\Delta^{b_q}S_q(m)=0\) 且 \(\Delta^{b_q/2}S_q(m)\not\equiv 0\)。
\end{enumerate}
该猜想把 \(q=7\) 处的指数翻倍提升为一般规律，并预言碰撞矩的二值影子携带无界的“幂零深度”。
\end{conjecture}

\begin{conjecture}[模 \(2\) 影子周期的无前段与满周期机制]\label{conj:pom-mod2-shadow-no-preperiod-full-period}
沿用注 \ref{rem:pom-charpoly-mod2-staircase-q2-10} 的记号，并令 \(s_m:=S_q(m)\bmod 2\in\FF_2\)。
猜想对所有 \(q\ge 4\)：
\begin{enumerate}
\normalfont
  \item \(a_q=0\)，即 \(s_m\) 自 \(m=0\) 起即满足 \((E+1)^{b_q}s=0\)；
  \item \(s_m\) 的最小周期恰为 \(2^{\lceil\log_2 b_q\rceil}\)（而非其真因子）；
  \item \((\lambda+1)^{b_q}\) 即为 \(s_m\) 在 \(\FF_2\) 上的最小湮灭多项式（不存在更低次数的非零湮灭多项式）。
\end{enumerate}
该猜想把已核验的塌缩分解提升为一个可离线检验的算术动力学断言：当 \(b_q\) 发生翻倍跃迁时，奇偶影子的最小周期亦同步倍增且不出现短周期塌缩。
\end{conjecture}

\begin{conjecture}[\(q^\star\) 阈值与 \texorpdfstring{$b_q$}{bq} 下一次跃迁的锁相]\label{conj:pom-mod2-bq-next-jump-locks-to-qstar}
沿用注 \ref{rem:pom-quantile-budget-computable} 的 Chernoff 最优矩阶 \(q^\star(m,\varepsilon)\) 与断点相图（表 \ref{tab:fold_collision_qstar_breakpoints_eps1e6_q2_20}）。
猜想存在一个与奇偶性相关的有限阈值区间 \(\mathcal{I}\subset\ZZ_{\ge 2}\)（在已审计的 DP 截断范围内可取 \(\mathcal{I}\subseteq\{18,19,20\}\)），使得当最优矩阶 \(q^\star\) 穿越 \(\mathcal{I}\) 时，
模 \(2\) 塌缩指数发生下一次翻倍跃迁
\[
b_q:\ 4\ \longrightarrow\ 8.
\]
等价地：当某个 \(q_0\) 出现 \(b_{q_0}=8\) 时，
\(S_{q_0}(m)\bmod 2\) 在去掉有限前缀并平移 \(m\mapsto m+a_{q_0}\) 后应表现为次数 \(<8\) 的二项式基多项式序列（定理 \ref{thm:pom-mod2-difference-binomial-basis}），并且满足可离线核验的差分证书
\[
\Delta^{8}S_{q_0}(m+a_{q_0})\equiv 0\pmod 2,
\qquad
\Delta^{4}S_{q_0}(m+a_{q_0})\not\equiv 0\pmod 2
\]
（对充分大的 \(m\)）。
\end{conjecture}

\endinput
