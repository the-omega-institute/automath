\subsection{大统一方向：几何化、跑动与可证伪锚点}\label{subsec:group-unification-gut-forward}

在 \S\ref{subsec:group-unification-spectral-alignment} 已将统一判据落实为谱隙耦合 \(g_{\varrho}(u)\) 的对齐与自对偶 completion 下统一点钉在 \(u=1\)（注 \ref{rem:selfdual-u1-six-branch-lock}），并给出数值锚点 \(W_{\mathrm{GUT}}\approx 5.32\)（命题 \ref{prop:real-input-40-unified-generator-alignment}）。本小节在上述结构之上给出若干条可直接落地的进一步结论与可检验预言，并在末尾补充一组接口级断言（以可检验形式陈述）；各条均回扣既有符号与接口，但不复述前文已写明的编号结论。

\paragraph{统一点 \(u=1\) 的几何化为 \(\Omega\)--反演的唯一固定点}
在正实轴尺度参数 \(\rho>0\) 上取一个参考尺度 \(\rho_h>0\)，并定义反演
\(
I:\ \rho\mapsto\tilde\rho=\rho_h^2/\rho
\)。
该反演交换双端 \(\rho\to 0\) 与 \(\rho\to\infty\)，其唯一固定点为 \(\rho=\rho_h\)。
取无量纲尺度 \(u:=\rho/\rho_h\)，则 \(I\) 等价于 \(\Omega\)--反演 \(u\mapsto 1/u\)，其唯一固定点为 \(u=1\)。

\begin{proposition}[统一点与反演固定点的同一]\label{prop:u1-throat-identity}
自对偶 completion 下将统一点钉在 \(u=1\) 的谱学事实（注 \ref{rem:selfdual-u1-six-branch-lock}）可提升为纯几何固定点陈述：统一点 \(u=1\) 等同于 \(\Omega\)--反演 \(u\mapsto 1/u\) 的唯一固定点。
因此，统一点的固定性并非来自参数微调，而来自对偶约束在固定点处的强制执行；在该意义下，统一可被理解为协议在固定点处不得不执行的自对偶翻转（扫描/读出角色互换的几何化）。
\leanverified{paper\_gut\_u1\_throat\_identity\_seeds}
\leanverified{paper\_gut\_u1\_throat\_identity\_package}
\leanverified{paper\_u1\_throat\_identity}
\end{proposition}

\paragraph{自对偶群提升为 Möbius/模群型对偶结构}
在同一反演对偶结构下可引入 Cayley 型 Möbius 坐标
\(
J(\rho)=(\rho-\rho_h)/(\rho+\rho_h)
\)，
满足反演下 \(J(\tilde\rho)=-J(\rho)\)，从而 \(J(\rho)^2\) 在反演下保持不变。
在分辨率折叠的 \(\zeta\) 正规化中，以 \(z=r/\varphi\) 将主极点规范到 \(r=1\)，并将“允许禁转移”解释为单位盘内部的 pole-barrier 语言。

\begin{proposition}[三种自对偶/边界固定点的 Möbius 统一]\label{prop:mobius-duality-unified}
体系中已有的三种自对偶/边界固定点可纳入同一 Möbius 对偶结构：
\begin{itemize}
  \item \(\Omega\)--反演：\(u\mapsto 1/u\)（唯一固定点 \(u=1\)）；
  \item Cayley：\(J\mapsto -J\)（反演下的符号翻转；\(J^2\) 为不变量）；
  \item \(\zeta\) 的 pole-barrier：主极点钉到 \(r=1\) 的单位盘边界（可稳定延拓的边界）。
\end{itemize}
三者可在协议参数空间上由 Möbius 变换生成的对偶群组织；在最简生成元下与典型模群生成方式（反演与平移）同构。统一因而不仅是谱隙对齐，而是协议参数落在自对偶群的固定点/基本域边界上，为将 Ramanujan/模形式/素数轴与反演对偶几何、谱隙跑动置于同一对偶图提供硬接口。
\leanverified{paper\_gut\_mobius\_duality\_unified}
\end{proposition}

\paragraph{谱隙跑动的 \(\beta\) 函数闭式（PF 期望）}
沿用定义 \ref{def:spec-coupling} 的谱隙耦合 \(g_{\varrho}(u)=\log(\lambda_1(u)/\eta_{\varrho}(u))\)。设 \(M(u)\) 为正矩阵族（或适用 Perron--Frobenius 理论的离散化），\(\rho(M(u))\) 为 Perron 根，左、右 PF 本征向量满足 \(M r=\rho r\)，\(\ell^\top M=\rho \ell^\top\)，\(\ell^\top r=1\)。则有
\[
\frac{\dd}{\dd u}\log\rho(M(u))
=\frac{\ell(u)^\top M'(u)\,r(u)}{\rho(M(u))}.
\]
定义协议 \(\beta\) 函数（以 \(\log u\) 为尺度）：
\[
\beta_\varrho(u)
:=\frac{\dd}{\dd\log u}\,g_\varrho(u)
=\frac{\dd}{\dd\log u}\log\lambda_1(u)
-\frac{u\,\ell(u)^\top M'(u)\,r(u)}{\rho(M(u))}.
\]

\begin{proposition}[\(\beta\) 的 PF 期望形式]\label{prop:beta-pf-expectation}
\leanverified{paper\_gut\_beta\_pf\_expectation}
在本体系内，重整化群跑动无需微扰展开：\(\beta\) 函数等于“基准谱”与“通道 PF 压力”的两项可审计期望差，通道项为 \(M'(u)\) 在 PF 平衡态下的期望。这将“统一=谱隙对齐”的静态判据升级为统一点附近跑动由协议动力学锁定的动力学判据，且完全有限维、可复现。
\end{proposition}

\paragraph{二阶 Jet 对齐作为稳定性选择器}
仅凭 \(g_{\chi,\varrho}(u)\) 在某点的值对齐不足以保证统一稳定：轻微改变观测门或核可能导致统一点漂移或“表面对齐、跑动分叉”。

\begin{proposition}[Jet 对齐的强统一判据]\label{prop:jet-alignment-unification}
\leanverified{paper\_jet\_alignment\_unification}
真正可筛掉“伪统一”的判据为在统一点处的 Jet 对齐：
\[
g_{\chi,\varrho}(1)\ \text{对齐},\quad
g_{\chi,\varrho}'(1)\ \text{对齐},\quad
g_{\chi,\varrho}''(1)\ \text{对齐}.
\]
等价地，在统一点处所有激活通道的尺度势在各自 PF 平衡态下的一阶矩与二阶矩同时匹配。二阶导数可写为含协方差/二次响应项的表达式（压力函数的二阶响应）。因此可将“统一”从标量条件升级为可被数据拒绝的强条件：若某通道的曲率 \(g''(1)\) 不匹配，则判定其不属于同一统一类；同时匹配一阶与二阶响应可显著提高群选择器的锋利度。
\end{proposition}

\paragraph{分辨率折叠与跑动的最小步长 \(\Delta_{\min}\)}
分辨率折叠给出平均纤维规模 \(d_m=2^m/F_{m+2}\)；由 Fibonacci 渐近 \(F_{m+2}\sim \varphi^{m+2}/\sqrt5\) 得规范化极限 \(d_m(\varphi/2)^m\to\sqrt5/\varphi^2\)，故
\(
\log d_m=m\log(2/\varphi)+O(1).
\)
规范差大偏差接口（命题 \ref{prop:fold-gauge-anomaly-ldp}）给出端点稀有事件指数 \(I(0)=\log(2/\varphi)\)。

\begin{proposition}[\(\beta\) 差值的离散步长]\label{prop:beta-quantization-step}
\leanverified{paper\_gut\_beta\_quantization\_step}
体系内存在由分辨率折叠与规范差端点代价共同锁定的不可约步长
\[
\Delta_{\min}:=\log(2/\varphi),
\]
它同时控制“折叠隐藏度的指数增长”与“规范差端点的大偏差代价”。因此所有通道的 \(\beta\) 函数在统一点附近的差值应落在整数倍网格上：
\[
\beta_i(1)-\beta_j(1)\in \Delta_{\min}\,\ZZ,
\]
其中整数来源于通道在折叠尾部分布上占用的 Fibonacci 层数/权重。该形式在传统 RG 文献中非标准，但在本体系内自然：跑动由分辨率折叠导致的有效自由度变化给出，自由度变化的指数底数为 \(2/\varphi\)。
\end{proposition}

\paragraph{\(W_{\mathrm{GUT}}\) 的闭式候选与可证伪性}
体系内必然出现的常数包括 \(\varphi\)（折叠/稳定语法/黄金分支）、\(\log(2/\varphi)=I(0)\)（规范差端点代价）以及几何常数 \(\pi\)。

\begin{conjecture}[\(W_{\mathrm{GUT}}\) 的闭式候选]\label{conj:Wgut-closed-form}
统一常数 \(W_{\mathrm{GUT}}\)（命题 \ref{prop:real-input-40-unified-generator-alignment}）可能由体系内已出现常数的小整数系数线性组合给出：
\[
W_{\mathrm{GUT}}\stackrel{?}{=}\pi+2\varphi-5\log(2/\varphi).
\]
右端数值约为 \(5.3179838535\ldots\)，与文中 \(5.32\) 在 \(10^{-3}\) 量级内一致；通过提高定义 \(W(v)\) 的脚本精度即可对该式作直接的数值证伪；若通过，则可在更高小数位上继续进行一致性检验。若成立，则得到非数秘的、可审计的“统一常数闭式”，系数可由通道数/层数/素数轴权重解释。
\end{conjecture}

\paragraph{可检验预言}
以下三条均可由现有脚本与谱数据直接检验。

\begin{enumerate}
  \item \textbf{反演对偶预言（对称--谱）} 在统一类中应有近似反演对称
  \(
  g_{\chi,\varrho}(u)\approx g_{\chi,\varrho}(1/u)
  \)
  （因 \(\Omega\)--反演为 \(u\mapsto 1/u\) 且交换双端极限）。检验方式：取对称点对 \((u,1/u)\)，检查谱隙耦合的镜像误差是否在证书界内。

  \item \textbf{\(\beta\) 差离散网格预言} 在统一点附近，任意两通道的 \(\beta(1)\) 之差应落在 \(\log(2/\varphi)\) 的整数倍网格上。该条件比“耦合值接近”更苛刻，可排除仅在某点偶然对齐的模型。

  \item \textbf{Jet 对齐预言（强稳定性）} 若候选统一方案真实，则在不同观测门/核的微扰下，统一点 \(u=1\) 的二阶响应 \(g''(1)\) 应保持同类（误差随证书缩小）；否则说明统一由参数微调造成的偶然碰撞所致。
\end{enumerate}

\paragraph{接口级断言：统一骨架的可检验表述}\label{par:gut-seven-additional-results}
下列断言均以本文已建立的 \(G_\infty^{\mathrm{GUT}}=\widehat{\ZZ}\times G_{\mathrm{chan}}\)、提升算子 \(T_{\mathrm{GUT}}\) 与双对偶分解
\(
T_{\mathrm{GUT}}\simeq\bigoplus_{\chi}\bigoplus_{\varrho}M_{\chi,\varrho}
\)
为共同底座（注 \ref{rem:pom-grand-unified-lift}）。各条仅给最终断言与核验方式，不展开推导。

另一方面，在不引入任何先验连续规范群叙事的前提下，window-\(6\) 锚点本身已经强制携带一组可封闭表述、且可由有限证书逐项核验的统一骨架。
为突出其在“黄金分支时间—6D 微态—Fold6—Zeckendorf 边界塔”口径下的终端收束，先给出一组仅含结论与审计指针的命题组：

\input{10_group_unification_terminal_results_window6}

其后，若进一步将分辨率层—边界层—同步核视为同一可审计对象族，则可将 window-\(6\) 的统一骨架压缩为下述总结定理（同样仅陈述结论与离线核验入口）：

\begin{theorem}[window-\(6\) 锚点下“分辨率层—边界层—同步核”的可审计结论组]\label{thm:window6-unified-skeleton}
\leanverified{paper\_window6\_unified\_skeleton}
在以 window-\(6\) 的折叠接口为最小锚点、并将分辨率层—边界层—同步核视为同一可审计对象族的口径下，可抽取如下五组结论。各结论均以有限对象（字典、行列式、端点常数、块多重度）封闭表述，并可由确定性离线脚本逐项复核：
\begin{enumerate}
  \item \textbf{四步 uplift 同构与 \(m=10\) 边界层的 \texorpdfstring{$(6+6+6+3)$}{(6+6+6+3)} 根系分块：}
  四步平移 uplift 在任意 \(n\ge 2\) 上给出规范同构 \(X_n\simeq X_{n+4}^{\mathrm{bdry}}\)，并在 \(n=6\) 时确定边界层 \(X_{10}^{\mathrm{bdry}}\) 的 \(21\) 词接口（定理 \ref{thm:boundary-shift4-uplift-isomorphism}）。
  在 \(B_3/C_3\) 根字典口径下，该层可被规范标号为 \(18\) 个根方向与 \(3\) 个 Cartan 方向，并刚性分裂为
  \(
  6\ (\mathrm{short})+6\ (A_2(+))+6\ (A_2(-))+3\ (\mathrm{Cartan})
  \)
  （定理 \ref{thm:boundary-m10-b3c3-rigid-four-block-split}；表 \ref{tab:boundary_uplift_m10_dictionary}）。
  与之相容地，任意 Weyl-不变可观测量的 cyclic 平均严格坍缩为短/长两轨道平均（推论 \ref{cor:fold6-weyl-two-orbit-compression}）。

  \item \textbf{arity-charge 的代数热力学：两变量行列式闭式、度 \(7\) 代数压力与零温端点：}
  在真实输入 \(40\) 状态同步核的 essential core 上，arity-charge \(\chi\) 在同调意义下可规范为逐边 \(0/1\) 指示，从而闭轨上 \(\chi(\gamma)\ge 0\) 且 primitive 多项式不含 Laurent 负幂（定理 \ref{thm:real-input-40-arity-charge-coboundary}；推论 \ref{cor:real-input-40-arity-charge-no-laurent}）。
  其加权矩阵 \(M(q)\) 的两变量行列式具有完全闭式分解（从而压力曲线为次数 \(7\) 的代数分支）：
  \input{sections/generated/eq_real_input_40_arity_charge_det_closed}
  并且零荷子系统的 \(\zeta\) 非平凡（命题 \ref{prop:real-input-40-arity-charge-zero-zeta}），从而存在周期轨道 \(\gamma\) 使 \(\chi(\gamma)=0\)，并满足
  \[
  \min_{\gamma}\frac{\chi(\gamma)}{|\gamma|}=0.
  \]
  零温端点具有严格主项
  \(
  P_\chi(\theta)=\tfrac12\,\theta+\tfrac12\log 3+o(1)
  \)
  （命题 \ref{prop:real-input-40-arity-charge-endpoints}），并由 \(\chi\) 切片的有限部二阶导数定义无量纲耦合比
  \(W_\chi\)（推论 \ref{cor:chi-slice-logm-derivatives-and-W}；表 \ref{tab:real-input-40-logM-chi-local-derivatives}），作为常数层耦合强度的数值锚点。

  \item \textbf{碰撞矩谱的 \texorpdfstring{$\sqrt{\varphi}$}{sqrt(phi)} 端点与 Rényi 端点维数（显式误差界）：}
  令 \(S_q(m)=\sum_{x\in X_m}d_m(x)^q\) 为碰撞矩计数、\(r_q=\lim_{m\to\infty}S_q(m)^{1/m}\) 为其指数增长率，并记 \(h_q:=q\log 2-\log r_q\)。
  则对任意整数 \(q\ge 2\) 有两侧界
  \[
  \frac{2^q}{\varphi^{q-1}}\le r_q\le 2\,\varphi^{(q-1)/2},
  \qquad
  (q-1)\log\!\left(\frac{2}{\sqrt{\varphi}}\right)\le h_q\le (q-1)\log\varphi,
  \]
  并在极端阶满足端点极限与显式误差界
  \[
  \lim_{q\to\infty} r_q^{1/q}=\sqrt{\varphi},\qquad
  0\le \log\!\left(\frac{2}{\sqrt{\varphi}}\right)-\frac{h_q}{q}\le \frac{\log\varphi}{q},
  \]
  从而 Rényi 维数谱端点满足
  \(
  \lim_{q\to\infty}D_q=\log\!\left(\frac{2}{\sqrt{\varphi}}\right)/\log\varphi\approx 0.94042009
  \)
  （命题 \ref{prop:pom-rq-universal-bounds} 与命题 \ref{prop:pom-rq-qinfty-endpoint}）。

  \item \textbf{自对偶统一点的四块谱简并签名（可证伪预测）：}
  在 \(55=12\oplus 9\oplus 24\oplus 10\) 的四块维数骨架下，自对偶统一点 \(u=1\) 处的线性化读出应呈现稳定的四块不变谱结构，块多重度鲁棒为 \(\{12,9,24,10\}\)，并满足通道响应张量的 \(U(3)\)-各向同性恢复（命题 \ref{prop:gut-u1-four-block-spectrum-signature}）。

  \item \textbf{\texorpdfstring{$SU(5)$}{SU(5)} 的 \(21+3\) 计数闭合（结构化实现）：}
  利用四步 uplift 同构与边界层计数，可将
  \(
  24=|X_{10}^{\mathrm{bdry}}|+|X_6^{\mathrm{bdry}}|
  =|X_6|+|X_2|=21+3
  \)
  组织为稳定层的计数闭合；其中 \(|X_6|=21\) 的层已携带 \(B_3/C_3\) 根字典与 uplift 分块，从而该闭合在结构层面由字典证书实现（推论 \ref{cor:su5-21-plus-3-closure}）。
\end{enumerate}
\end{theorem}

\paragraph{6D 分辨率折叠—同步核—可审计算术接口：凸几何骨架、剪切降维与算术刚性签名}
依 \cite{Internal2026POMGenerativeAddressThreeRegisterBridgeRH} 所固定的统一口径，在 window-\(6\) 锚点所给出的“分辨率层—边界层—同步核”骨架之上，可将可观测动力学、扭曲参数与高阶碰撞矩等信息压缩为三类互补证书：其一为 rotation polytope 的有理凸多面体与 facet 不等式（凸几何骨架）；其二为 \(\chi\)-扭曲的线性剪切冗余与零温相图（normal fan）的线性拉回（严格降一维）；其三为最小整系数递推与 Hankel 证书在特征 \(2\) 与 \(p\)-进层面的刚性签名（算术审计与重构稳定性）。因此，下述结论组可独立成文，并可由有限对象逐项核验。

\begin{theorem}[离散—凸几何—算术三重接口（可审计收束）]\label{thm:tri-interface-discrete-convex-arithmetic}
\leanverified{paper\_tri\_interface\_discrete\_convex\_arithmetic}
在本文既定的扫描—投影读出与 \(\Fold\) 核口径下，下述六条结论均可在不引入额外解释层的前提下，以有限对象给出闭式陈述，并可由离线脚本逐项复核：
\begin{enumerate}
  \item \textbf{真实输入 \(40\) 状态核的 rotation polytope：6 顶点闭式与 7 条尖锐 facet 不等式。}
  在真实输入同步核的 essential \(20\) 状态核心上，三观测量 \((e,\nu,\xi)\)（等价地 \((\chi,\nu,\xi)\)）的 rotation set \(\mathrm{Rot}(\Phi)\subset\RR^3\) 为一个 \(6\) 顶点有理多面体，并且等价地由 \(7\) 条尖锐 facet 不等式刻画；该断言对任意 \(\sigma\)-不变概率测度与任意周期轨道极限同时成立（定理 \ref{thm:real-input-40-rotation-polytope-6v}，并见注 \ref{rem:real-input-40-rotation-polytope-exact-repro}）。

  \item \textbf{二维投影的完全显式化：\(\mathrm{Rot}(\chi,\nu)\) 为四边形且不等式尖锐。}
  令 \(\pi(\bar\chi,\bar\nu,\bar\xi)=(\bar\chi,\bar\nu)\)。则
  \[
  \mathrm{Rot}(\chi,\nu)
  =\mathrm{conv}\Bigl\{(0,0),\ \bigl(\tfrac12,0\bigr),\ \bigl(\tfrac12,\tfrac12\bigr),\ (0,1)\Bigr\},
  \]
  等价地对任意不变测度有尖锐不等式
  \[
  0\le \bar\chi\le\tfrac12,
  \qquad
  0\le \bar\nu\le 1-\bar\chi,
  \]
  且四个顶点均由短周期轨道实现（推论 \ref{cor:real-input-40-rotation-set-chi-nu-quad}）。

  \item \textbf{\(\chi\)-扭曲的线性剪切冗余：相图 normal fan 的线性拉回与严格降一维。}
  由 \(\chi=e-\xi\) 定义线性剪切
  \[
  L(x_e,x_\nu,x_\xi):=(x_e-x_\xi,\ x_\nu,\ x_\xi).
  \]
  则
  \(
  \mathrm{Rot}(\Phi^{(\chi)})=L(\mathrm{Rot}(\Phi^{(e)}))
  \)，并且零温相图（normal fan）在对偶参数空间中沿直线族 \(\theta_2-\theta_e=\mathrm{const}\) 作线性拉回（命题 \ref{prop:rotation-polytope-shear}）。
  在算术扭曲（单位根 Dirichlet 角色）口径下，权重恒等式
  \[
  u_\chi^{\chi(e)}u_-^{\nu(e)}u_2^{\xi(e)}
  =u_\chi^{e(e)}u_-^{\nu(e)}\left(\frac{u_2}{u_\chi}\right)^{\xi(e)}
  \]
  表明沿 \(\chi\) 的扭曲不引入新的自由度，从而三轴枚举存在一条可消去的线性冗余方向（推论 \ref{cor:chi-twist-dimension-reduction}）。

  \item \textbf{最小整系数递推的端点刚性：奇滞后中性、首系数恒等、偶阶尾差分块。}
  在已核验范围内，折叠碰撞矩 \(S_q(m)\) 的最小整系数递推呈现三类刚性签名。
  写递推为 \(S_q(m)=\sum_{i=1}^{d_q}c_iS_q(m-i)\)，并约定特征多项式 \(P_q(\lambda)=\lambda^{d_q}-\sum_{i=1}^{d_q}c_i\lambda^{d_q-i}\)。
  则有：其一，阶数 \(d_q\) 为奇数且满足奇滞后中性
  \(
  \sum_{i\ \mathrm{odd}}c_i=0
  \)
  （等价于 \(P_q(1)+P_q(-1)=0\)，命题 \ref{prop:pom-moment-odd-lag-neutrality}）；
  其二，首系数恒为 \(c_1=2\)；
  其三，在已核验的偶阶例中末端系数固定为 \((8,-8)\)，使最深滞后仅以差分信道 \(8(S_q(m-d_q+1)-S_q(m-d_q))\) 进入（注 \ref{rem:pom-recurrence-endpoint-rigidity}）。

  \item \textbf{特征 \(2\) 的谱坍缩：\(\bmod 2\) 仅余 \(\{0,-1\}\) 两点谱（注意 \(-1\equiv 1\in\mathbb{F}_2\)）并诱导差分湮灭律。}
  在共振窗口 \(q=9,\dots,17\) 中，最小递推特征多项式模 \(2\) 约化满足
  \[
  P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\pmod 2,\qquad a_q+b_q=d_q,
  \]
  等价地，令前移算子 \(E\) 作用为 \((ES)(m):=S(m+1)\)，并取差分算子 \(\mathsf{D}:=E+1\)，则有
  \[
  E^{a_q}\mathsf{D}^{b_q}S_q\equiv 0\pmod 2,
  \]
  从而得到有限阶差分湮灭与 Lucas 型周期上界（注 \ref{rem:pom-charpoly-mod2-shadow}；表 \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17}）。

  \item \textbf{Hankel 证书的 \(p\)-进算术刚性：分母由 \(\det(H_0)\) 统一控制并诱导 \(\mathsf{Stiff}_p\) 指数。}
  若整数序列 \((a_n)_{n\ge 0}\subset\ZZ\) 具有有限 Hankel 秩 \(d\)，记
  \(H_0=(a_{i+j})_{0\le i,j\le d-1}\)、\(H_1=(a_{i+j+1})_{0\le i,j\le d-1}\)。
  则存在最小线性实现 \(a_n=\alpha^{\mathsf T}A^n\beta\)，且
  \[
  A=H_0^{-1}H_1=\frac{\mathrm{adj}(H_0)\,H_1}{\det(H_0)}
  \]
  可由 Hankel 块闭式构造；因此所有分母均整除 \(\det(H_0)\)（命题 \ref{prop:pom-hankel-realization-protocol}）。
  对任意素数 \(p\) 定义
  \(
  \mathsf{Stiff}_p:=v_p(\det(H_0))
  \)，它给出在 \(\ZZ_p\) 中无歧义恢复最小实现所需的最小 \(p\)-进精度门槛（注 \ref{rem:pom-hankel-denominator-stiffness}）。
\end{enumerate}
上述六条结论在统一结构上形成互补闭包：rotation polytope 提供可观测相图的精确凸几何骨架，线性剪切冗余给出扭曲轴的严格降维，而递推端点刚性、特征 \(2\) 的谱坍缩与 \(p\)-进 Hankel 证书则提供可审计的算术刚性签名；由此在“6D 读出—\(\Fold\) 核—素数影子/碰撞矩”之间建立一条可复现、可证伪、可直接引用的离散—凸几何—算术三重接口。
\end{theorem}

\paragraph{window-\(6\) 锚点的离散结论簇：由 \texorpdfstring{$\mathrm{Fold}_6/\mathrm{Fold}^{\mathrm{bin}}_6$}{Fold6/Foldbin6} 向一般 \texorpdfstring{$m$}{m} 的可审计推广}
为便于在不引入解释层叙事的前提下复用 window-\(6\) 的离散锚点，以下将正文与附录中已建立的若干结果收束为一组可独立引用的结论簇：它们均是对有限离散对象的断言，并且其证明与数值审计入口已在相应位置给出。
\input{10_group_unification_window6_discrete_results}

\paragraph{循环特化刚性与统一扇区：共同慢模的因子结构证书}
在谱隙统一判据（注 \ref{rem:spec-gap-minimax-unification}）中，“对齐”允许是数值上的近似现象；而在自对偶 completion 与 cyclotomic 迹坐标 \(s_m=2\cos(\pi/m)\) 的口径下（注 \ref{rem:completion-invariant-ring}），可以提出更强的、纯代数的统一锚点：\emph{在无限多个 cyclotomic 层上出现精确共同慢模}。
下述结果表明该现象一旦发生，就不可能被解释为偶然一致性，而必然等价于完成化行列式族在 \(\QQ[s,w]\) 中出现非平凡公共因子（统一扇区）。

\begin{definition}[cyclotomic 共同慢模层]\label{def:cyclotomic-common-slowmode-layer}
设 \(\mathcal{R}_{\mathrm{obs}}\) 为一组可观测通道族（与注 \ref{rem:spec-gap-minimax-unification} 一致的索引集合）。
对每个 \(\varrho\in\mathcal{R}_{\mathrm{obs}}\)，设其完成化行列式在变量 \(w,s\) 下可写为
\[
\widehat{\Delta}_{\varrho}(w,s)\in\ZZ[s][w],
\]
其中 \(s=r+r^{-1}\) 为 completion 的迹坐标、\(w=zr\) 为完成化频率变量（注 \ref{rem:completion-invariant-ring}）。
对每个整数 \(m\ge 3\)，令 \(s_m:=2\cos(\pi/m)\) 并记 \(K_m:=\QQ(s_m)\)。
称 \(m\) 为一个 \textbf{cyclotomic 共同慢模层}，若存在 \(w_m\in\CC\) 使得对所有 \(\varrho\in\mathcal{R}_{\mathrm{obs}}\) 都有
\[
\widehat{\Delta}_{\varrho}(w_m,s_m)=0.
\]
记满足该性质的 \(m\) 的集合为 \(\mathcal{U}(\mathcal{R}_{\mathrm{obs}})\)。
\end{definition}

\begin{theorem}[循环特化刚性：无限多层共同慢模 \texorpdfstring{$\Rightarrow$}{=>} 统一扇区存在]\label{thm:cyclotomic-specialization-rigidity}
若 \(\mathcal{U}(\mathcal{R}_{\mathrm{obs}})\) 为无限集，则存在非常数多项式
\[
H(s,w)\in \QQ[s,w]\setminus\QQ^\times
\]
使得对一切 \(\varrho\in\mathcal{R}_{\mathrm{obs}}\) 都有 \(H\mid \widehat{\Delta}_{\varrho}\)（在 \(\QQ[s,w]\) 中整除）。
换言之，
\[
\gcd_{\QQ[s,w]}\bigl(\widehat{\Delta}_{\varrho}:\varrho\in\mathcal{R}_{\mathrm{obs}}\bigr)\ \neq\ 1,
\]
并将该最大公因子（取首一代表）称为 \textbf{统一扇区}。
\end{theorem}
\leanverified{paper\_gut\_cyclotomic\_specialization\_rigidity}
\leanverified{paper\_cyclotomic\_specialization\_rigidity}
\begin{proof}
先取任意两通道 \(\varrho_1,\varrho_2\in\mathcal{R}_{\mathrm{obs}}\)，令
\[
E(s):=\operatorname{Res}_{w}\bigl(\widehat{\Delta}_{\varrho_1}(w,s),\widehat{\Delta}_{\varrho_2}(w,s)\bigr)\in\ZZ[s],
\]
其中 \(\operatorname{Res}_{w}\) 表示关于变量 \(w\) 的消元 resultant。
若 \(\widehat{\Delta}_{\varrho_1},\widehat{\Delta}_{\varrho_2}\) 在 \(\QQ[s,w]\) 中互素，则 \(E(s)\not\equiv 0\)；
并且对任意复数 \(s_0\)，有 \(E(s_0)=0\) 当且仅当在 \(s=s_0\) 处二者存在共同 \(w\)-根。
由假设 \(\mathcal{U}(\mathcal{R}_{\mathrm{obs}})\) 无限，存在无限多个互异的 \(m\) 使得在 \(s=s_m\) 处共同根存在，故 \(E(s_m)=0\) 对无限多点成立；从而 \(E\equiv 0\)，推出二者在 \(\QQ[s,w]\) 中不互素，存在非平凡公因子。
对 \(\mathcal{R}_{\mathrm{obs}}\) 中其余通道迭代取 gcd 即得共同因子 \(H\)。证毕。
\end{proof}

\begin{corollary}[有限可证伪性：无统一扇区 \texorpdfstring{$\Rightarrow$}{=>} 共同慢模层必有限]\label{cor:cyclotomic-specialization-falsifiability}
\leanverified{paper\_cyclotomic\_specialization\_falsifiability}
若
\(
\gcd_{\QQ[s,w]}(\widehat{\Delta}_{\varrho}:\varrho\in\mathcal{R}_{\mathrm{obs}})=1
\)，
则 \(\mathcal{U}(\mathcal{R}_{\mathrm{obs}})\) 为有限集。
更定量地，对任意固定 \(\varrho_0\in\mathcal{R}_{\mathrm{obs}}\)，有
\[
\mathcal{U}(\mathcal{R}_{\mathrm{obs}})
\subseteq
\bigcap_{\varrho\in\mathcal{R}_{\mathrm{obs}}\setminus\{\varrho_0\}}
\bigl\{\,m\ge 3:\ \operatorname{Res}_{w}(\widehat{\Delta}_{\varrho_0},\widehat{\Delta}_{\varrho})(s_m)=0\,\bigr\},
\]
从而其基数可由上述 resultants 的次数给出显式上界。
\end{corollary}
\leanverified{paper\_gut\_cyclotomic\_specialization\_falsifiability}

\begin{theorem}[specialization 的 gcd 稳定性：除有限例外外共同因子不增长]\label{thm:cyclotomic-gcd-stability}
\leanverified{paper\_gut\_cyclotomic\_gcd\_stability}
令
\[
H(s,w):=\gcd_{\QQ[s,w]}\bigl(\widehat{\Delta}_{\varrho}(w,s):\varrho\in\mathcal{R}_{\mathrm{obs}}\bigr)
\]
取为首一代表，并写 \(\widehat{\Delta}_{\varrho}=H\cdot Q_{\varrho}\)，其中
\(
\gcd_{\QQ[s,w]}(Q_{\varrho}:\varrho\in\mathcal{R}_{\mathrm{obs}})=1
\)。
则存在有限集合 \(\mathcal{E}\subset\{m\ge 3\}\)，使得对一切 \(m\notin\mathcal{E}\)，在域 \(K_m=\QQ(s_m)\) 上有：
\[
\gcd_{K_m[w]}\bigl(\widehat{\Delta}_{\varrho}(w,s_m):\varrho\in\mathcal{R}_{\mathrm{obs}}\bigr)
\ \text{与}\ H(s_m,w)\ \text{在}\ K_m[w]\ \text{中相差一个单位因子}.
\]
因此除有限例外外，第 \(m\) 层的所有共同慢模均由 \(H(s_m,w)=0\) 完全刻画。
\end{theorem}

\begin{proposition}[统一点 projected Jet congruence 的 lifted channel 审计]\label{distill:euclid_elements-lifted_congruence_and_residual_rigidity_families-gut-projected-jet-lift-audit}
\textbf{Dependency status: conditional audit.}
沿用本节的自对偶统一点 \(u=1\) 与通道分解
\[
T_{\mathrm{GUT}}\simeq \bigoplus_{\chi}\bigoplus_{\varrho}M_{\chi,\varrho}.
\]
令 \(A\) 为一组有限激活通道指标 \((\chi,\varrho)\)，并取有限审计族
\[
E\subseteq \bigoplus_{\alpha\in A}M_\alpha.
\]
设
\[
J:E\to Y
\]
为统一点处已登记的 projected Jet readout，记录当前协议允许比较的
\(g(1),g'(1),g''(1)\) 型有限数据；再设
\[
R:E\to Z
\]
为 lifted channel residual readout。假设二者共同构成当前有限审计族上的完整通道坐标，即对任意 \(x,x'\in E\)，
\[
J(x)=J(x')\quad\text{且}\quad R(x)=R(x')
\qquad\Longleftrightarrow\qquad
x=x'
\]
于 \(\bigoplus_{\alpha\in A}M_\alpha\) 中成立。对 \(y\in J(E)\)，记
\[
\mathcal R_E(y):=R\bigl(E\cap J^{-1}(y)\bigr),\qquad
\mu_J(E):=\max_{y\in J(E)}|\mathcal R_E(y)|.
\]
则：
\begin{enumerate}
  \item projected Jet congruence
  \(J(x)=J(x')\) 在 \(E\) 上推出 lifted channel equality \(x=x'\)，当且仅当
  \[
  \mu_J(E)=1;
  \]
  \item 任一有限 strictification register \(\rho:E\to Q\) 若满足
  \[
  J(x)=J(x'),\quad \rho(x)=\rho(x')
  \quad\Longrightarrow\quad x=x',
  \]
  则必有
  \[
  |Q|\ge \mu_J(E),
  \]
  且该下界可达；
  \item 因此，任何只验证 projected Jet 对齐而未登记 \(R\)-residual 的统一固定点断言，都只能证明 projected unification，不能证明 lifted unification。其最坏反例必落在某个 \(J\)-纤维的 residual 多点集 \(\mathcal R_E(y)\) 中。
\end{enumerate}
\end{proposition}
\begin{proof}
由完整通道坐标假设，两个审计态 \(x,x'\in E\) 相等当且仅当它们的 projected Jet readout 与 lifted residual readout 同时相等。因此，在已知 \(J(x)=J(x')\) 的前提下，是否能推出 \(x=x'\) 完全等价于是否自动有 \(R(x)=R(x')\)。这对每个 \(J\)-纤维同时成立，当且仅当每个集合
\(\mathcal R_E(y)\) 都是单点，即 \(\mu_J(E)=1\)。第一条得证。

对第二条，固定 \(y\in J(E)\)。若 \(z,z'\in\mathcal R_E(y)\) 且 \(z\ne z'\)，取代表
\(x,x'\in E\cap J^{-1}(y)\) 使 \(R(x)=z\)、\(R(x')=z'\)。完整通道坐标假设给出 \(x\ne x'\)。若 \(\rho(x)=\rho(x')\)，则 strictification 条件会推出 \(x=x'\)，矛盾。因此 \(\rho\) 在每个 \(J\)-纤维的 residual 值上必须取不同值，故
\(|Q|\ge|\mathcal R_E(y)|\)。对 \(y\) 取最大值得到
\(|Q|\ge\mu_J(E)\)。

为证明可达性，取 \(|Q|=\mu_J(E)\)。对每个 \(y\in J(E)\) 选择单射
\[
\iota_y:\mathcal R_E(y)\hookrightarrow Q,
\]
并令
\[
\rho(x):=\iota_{J(x)}(R(x)).
\]
若 \(J(x)=J(x')\) 且 \(\rho(x)=\rho(x')\)，则在同一 \(y=J(x)\) 下由 \(\iota_y\) 单射得
\(R(x)=R(x')\)，再由完整通道坐标假设得 \(x=x'\)。故下界可达。

第三条只是前两条在统一点语义下的读法：projected Jet 对齐只是在 \(Y\) 中相等；若某个 \(J\)-纤维中残余集合有多点，则其中任意两个不同 residual 代表给出 projected congruent 但不 lifted equal 的坏例子。故自对偶固定点的全通道固定性必须经过 lifted channel audit，而不能由 projected Jet congruence 单独转移。证毕。
\end{proof}

\begin{proof}
若对无限多个 \(m\) 有
\(
\gcd_{K_m[w]}(Q_{\varrho}(w,s_m):\varrho\in\mathcal{R}_{\mathrm{obs}})
\)
非平凡，则存在无限多层 \(m\) 使得 \(\{Q_{\varrho}(w,s_m)\}\) 共享某个共同 \(w\)-根。
由定理 \ref{thm:cyclotomic-specialization-rigidity}（对族 \(\{Q_{\varrho}\}\) 适用）推出
\(
\gcd_{\QQ[s,w]}(Q_{\varrho}:\varrho\in\mathcal{R}_{\mathrm{obs}})\neq 1
\)，与构造矛盾。
因此额外 gcd 只能在有限多个 \(m\) 上出现；对其余 \(m\)，共同因子即为 \(H(s_m,w)\)（在 \(K_m[w]\) 中仅差一个单位）。证毕。
\end{proof}

\begin{itemize}
  \item \textbf{三通道耦合的单对象化：同一算术对象的三个 Satake 分量。}
  在“prime-skeleton + Euler/Hecke 闭包 + 可审计系数恢复链”的协议口径下，三条投影通道（注 \ref{rem:pom-grand-unified-lift} 的 \((\varphi,\pi,e)\)）在素数处的局部指纹不应被视为三条独立自由曲线；更强的统一形态应为：存在一个全局 Hecke 本征对象（等价地，一个全局 \(L\)-数据/自守表示候选），使得三通道的局部数据是同一局部因子的三个可计算分量（最自然候选为 \(GL(2)\) 本征对象的 \(\mathrm{Sym}^2\) 提升所诱导的三重局部参数）。
  \par\noindent\emph{核验方式：}（i）对每个素数 \(p\)，三通道局部指纹必须满足同一族 Hecke/Euler 递推约束（不允许通道间相对漂移自由度）；（ii）全局一致性可用有限确定性模板完成：依 \S\ref{subsec:group-unification-audit-pointers} 中 Hecke 刚性与 Sturm 界的有限素数骨架判定模板，比较有限素数集上的局部数据即可对“单对象化”假设作有限判定。

  \item \textbf{通道群的最小闭合：\(U(3)\rtimes \ZZ_2^{\mathrm{sheet}}\)。}
  在连续包络口径下，三通道势—响应辛结构强迫最小连续对称为 \(\mathrm{Sp}(6,\RR)\)，其最大紧子群为 \(U(3)\)（注 \ref{rem:sp6-minimal-continuous-envelope}）。同时，window-6 的 dyadic uplift 在 boundary 扇区内诱导二层覆盖与 sheet parity \(\pi_{\mathrm{sheet}}\in\ZZ_2\)（见 \S\ref{subsec:bdry-tower-zeck-gut} 的 boundary 扇区双层覆盖构造）。因此可将通道群的最小闭合形态组织为半直积
  \[
  \boxed{\;
  G_{\mathrm{chan}}\ \cong\ U(3)\rtimes \ZZ_2^{\mathrm{sheet}},
  \qquad \ZZ_2^{\mathrm{sheet}}=\langle \pi_{\mathrm{sheet}}\rangle.
  \;}
  \]
  \par\noindent\emph{核验方式：}在统一类的可观测通道族上，应出现一个与 \(U(3)\) 混合自由度相对独立的二元分支；并且所有被允许的稳定更新/提升算子应与 \(\pi_{\mathrm{sheet}}\) 的分级相容（否则在审计证书上表现为跨分支的异常残差）。

  \item \textbf{统一耦合常数的无参标定：\(\alpha_{\mathrm{U}}^{-1}=W_{\mathrm{GUT}}^2\)。}
  将真实输入 \(40\) 状态核上由方向耦合比 \(\mathcal{W}(v)\) 读出的主导统一常数 \(W_{\mathrm{GUT}}\approx 5.32\)（命题 \ref{prop:real-input-40-unified-generator-alignment}）作为统一层的无量纲锚点，则最简无参归一化给出统一耦合预测
  \[
  \boxed{\ \alpha_{\mathrm{U}}^{-1}:=W_{\mathrm{GUT}}^2\ \approx\ 28.30,\qquad
  g_{\mathrm{U}}:=\sqrt{\frac{4\pi}{\alpha_{\mathrm{U}}^{-1}}}=\frac{\sqrt{4\pi}}{W_{\mathrm{GUT}}}\ \approx\ 0.666\ }.
  \]
  \par\noindent\emph{核验方式：}该预测不引入新数据，只依赖 \(W_{\mathrm{GUT}}\) 的复现精度；一旦 \(W_{\mathrm{GUT}}\) 的小数位被脚本证书锁定，上式即给出可直接核验的数值锚点。

  \item \textbf{统一阈值的离散层级：按 \(\varphi^6\) 的六分支步进。}
  自对偶 completion 在统一点 \(u=1\) 处出现六分支锁定（注 \ref{rem:selfdual-u1-six-branch-lock}），提示统一层级的最小尺度步进与分支数 \(6\) 绑定。以 \(\varphi\)-对数刻度 \(r:=\log_{\varphi}(\mu/\mu_0)\) 表述，则给出离散阈值预测
  \[
  \boxed{\ \Delta r=6\qquad\Longleftrightarrow\qquad \frac{\mu_{m+1}}{\mu_m}=\varphi^6=9+4\sqrt5\approx 17.94427191\ }.
  \]
  \par\noindent\emph{核验方式：}若把统一类的候选“阈值点/层级跃迁”落实为谱隙对齐或 adelic 平坦点的稳定极小族，则其在尺度轴上的出现位置应呈现 \(r\equiv 0\pmod 6\) 的离散层级结构（对核/门的可审计微扰保持鲁棒，否则可被直接证伪）。

  \item \textbf{超选择约束：跨稳定指纹扇区的跃迁算符一阶缺失。}
  本文已给出一族在 \(p\)-进塔上稳定的审计不变量：\(\theta\)-导数的稳定首位残差 \(\alpha_{p,r}\in\mathbb{F}_p\)（推论 \ref{cor:witt-theta-stable-residue}）以及 \(p=2\) 的稳定奇偶指纹 \(\varepsilon_r\in\{0,1\}\)（推论 \ref{cor:witt-theta-binary-fingerprint}）；同时异常上同调类 \([\mathrm{Anom}(w)]\) 对水平复合严格加法（推论 \ref{cor:pom-anom-class-additive}）。因此在“完成闭层/稳定更新”口径下，允许算子代数应被这些稳定标签分解为直和的超选择扇区；任何必须改变 \(([\mathrm{Anom}],\{\alpha_{p,r}\},\{\varepsilon_r\})\) 的跃迁在一阶近似下不属于可实现算子。
  \par\noindent\emph{核验方式：}给定任意候选“破缺算子/跃迁规则”，若其作用导致上述任一稳定指纹发生可审计改变，则可在有限层审计中直接否决其“闭层可实现性”。

  \item \textbf{adelic anomaly 的强口径：乘积公式式的精确抵消与离散标签的整化。}
  注 \ref{rem:pom-adelic-anom-functional} 已将局部异常强度 \(A_p(\theta)\) 汇总为整体泛函 \(\mathcal{A}_{\le P}(\theta)\) 并以稳定极小作为统一点判据。更强的口径是：异常不止“最小化”，而应在 adelic 粘合意义下形成全局平凡类（局部—整体精确抵消，类似乘积公式的闭合约束）。
  \par\noindent\emph{核验方式：}随截断 \(P\to\infty\) 增大，局部异常签名的上同调类代表应在可审计规范下稳定，并且其全局类必须可被消去（否则统一点只能是截断依赖的伪极小）。该强口径同时带来一个后果：允许的离散标签/超选择标签（例如 \(\pi_{\mathrm{sheet}}\) 及其与通道纤维的耦合方式）将被“可粘合性”条件一并整化，从而减少建模自由度。

  \item \textbf{有限资源律：可见统一表示为截断显现，且 \(n_{\max}\sim N/\log N\)。}
  在任何以 Peter--Weyl/傅里叶分解读取 \((\chi,\varrho)\) 通道族的实现中，有限样本 \(N\) 的最坏频率误差由星差异度 \(D_N^*\) 确定性控制（\S\ref{sec:statistical-stability}；并见第 \ref{sec:experiments} 节的确定性 TV/KL 证书链）。因此存在不可回避的资源上界：可稳定恢复/判等的最大模式（表示）索引满足
  \[
  \boxed{\ n_{\max}\ \asymp\ \frac{1}{D_N^*}\ }.
  \]
  对 Kronecker/旋转扫描基准，当 \(D_N^*=O((\log N)/N)\) 时得到渐近资源律
  \[
  \boxed{\ n_{\max}\ =\Theta\!\left(\frac{N}{\log N}\right)\ }.
  \]
  \par\noindent\emph{核验方式：}随着 \(N\) 增大，超过 \(n_{\max}\) 的高频模式应系统性地落入证书误差带（呈现“阶梯式显现”而非连续显现）；该阈值可由 \(D_N^*\) 的可计算上界直接预估，并用于统一审计预算分配。
\end{itemize}
