\subsection{ZG 的 Tauberian 边界、全息满移位共轭与 Lee--Yang 五分支 Haar 极限}\label{subsec:conclusion-zg-hologram-leyang-residue-fullshift-haar}
沿用定理 \ref{thm:xi-zg-abel-residue-log-density}、推论 \ref{cor:xi-zg-absolute-convergence-critical-line}、定理 \ref{thm:xi-time-part62deb-hologram-affine-fullshift-conjugacy}、推论 \ref{cor:xi-time-part62deb-hologram-fullshift-entropy-primitive-zeta}、定理 \ref{thm:xi-time-part62dc-hologram-bernoulli-exact-dimensional}、推论 \ref{cor:xi-terminal-zm-leyang-ramification-pullback-2n}、推论 \ref{cor:xi-terminal-zm-leyang-branchpoint-tate-torsor-decomposition} 与定理 \ref{thm:xi-time-part9gc-resolution-tower-haar-limit} 的记号。前述链条已经分别给出：\(\mathrm{ZG}\) 的自然密度与 Dirichlet 级数的 Abel 边界律、全息事件像与单侧满移位的严格仿射共轭、Bernoulli 事件源在 faithful 地址像上的精确维度公式，以及 Lee--Yang 五有限分歧族在 dyadic 逆极限中的 Tate-torsor 分解与 Haar 极限律。由此还可进一步抽出如下九条后果：\(\mathrm{ZG}\) 的简单极点常数与自然密度的一致性、其调和 Ces\`aro 主项与 \(\zeta\)-归一化 residue 的三重同常数律、全息事件系统的满移位动力学闭式、primitive 周期轨道的 Möbius 公式、同一地址系统上的轨道谱与度量维数解耦、faithful 单流地址测度与 Lee--Yang Haar sheet 之间的维数隔离，以及 Lee--Yang 五分支有限层均匀测度向分量平均 Haar 测度的边界收敛。

\begin{theorem}[ZG 的 Dirichlet 级数在 \texorpdfstring{$s=1^+$}{s=1+} 的简单极点律，其主部系数等于自然密度]\label{thm:conclusion-zg-dirichlet-simple-pole-density}
\leanverified{paper\_conclusion\_zg\_dirichlet\_simple\_pole\_density}
定义
\[
A_{\mathrm{ZG}}(x):=\#\{n\le x:\ n\in \mathrm{ZG}\},
\qquad
D_{\mathrm{ZG}}(s):=\sum_{n\in \mathrm{ZG}}n^{-s}
\qquad(\Re(s)>1),
\]
以及
\[
\delta_{\mathrm{ZG}}:=\lim_{x\to\infty}\frac{A_{\mathrm{ZG}}(x)}{x}.
\]
则 \(\delta_{\mathrm{ZG}}\in(0,1)\)，并且成立
\[
\boxed{\;
\lim_{\sigma\downarrow 1}(\sigma-1)D_{\mathrm{ZG}}(\sigma)=\delta_{\mathrm{ZG}}.\;}
\]
特别地，
\[
\boxed{\;
D_{\mathrm{ZG}}(\sigma)\sim \frac{\delta_{\mathrm{ZG}}}{\sigma-1}
\qquad(\sigma\downarrow 1),\;}
\]
且其收敛横坐标满足
\[
\boxed{\;
\sigma_c(D_{\mathrm{ZG}})=1.\;}
\]
换言之，\(D_{\mathrm{ZG}}\) 在实轴边界 \(s=1^+\) 的主导奇性是一条简单极点律，其主部系数恰为自然密度 \(\delta_{\mathrm{ZG}}\)。
\end{theorem}
\begin{proof}
第一条极限正是定理 \ref{thm:xi-zg-abel-residue-log-density} 的第一条。由推论 \ref{cor:xi-zg-absolute-convergence-critical-line}，进一步有
\[
D_{\mathrm{ZG}}(\sigma)\sim \frac{\delta_{\mathrm{ZG}}}{\sigma-1}
\qquad(\sigma\downarrow 1),
\]
从而得到所述简单极点律。最后，定理 \ref{thm:xi-zg-abel-residue-log-density} 的第三条已给出
\[
\sigma_c(D_{\mathrm{ZG}})=1.
\]
证毕。
\end{proof}

\begin{corollary}[ZG 上的对数加权 Mertens--Ces\`aro 律]\label{cor:conclusion-zg-weighted-mertens-cesaro}
\leanverified{paper\_conclusion\_zg\_weighted\_mertens\_cesaro}
沿用定理 \ref{thm:conclusion-zg-dirichlet-simple-pole-density} 的记号。对任意固定整数 \(k\ge 0\)，成立
\[
\boxed{\;
\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}\frac{(\log n)^k}{n}
=
\frac{\delta_{\mathrm{ZG}}}{k+1}(\log X)^{k+1}
+o\!\bigl((\log X)^{k+1}\bigr)
\qquad(X\to\infty).\;}
\]
特别地，
\[
\boxed{\;
\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}\frac1n
=
\delta_{\mathrm{ZG}}\log X+o(\log X).\;}
\]
\end{corollary}
\begin{proof}
由定理 \ref{thm:xi-zg-abel-residue-log-density} 的证明，记
\[
A_{\mathrm{ZG}}(x)=\delta_{\mathrm{ZG}}x+r(x),
\qquad
r(x)=o(x).
\]
对
\[
f_k(x):=\frac{(\log x)^k}{x}
\]
作 Abel 分部求和，得到
\[
\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}f_k(n)
=
A_{\mathrm{ZG}}(X)f_k(X)-\int_1^X A_{\mathrm{ZG}}(t)f_k'(t)\,\dd t.
\]
代入 \(A_{\mathrm{ZG}}(t)=\delta_{\mathrm{ZG}}t+r(t)\) 后，
\[
\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}f_k(n)
=
\delta_{\mathrm{ZG}}
\left(
Xf_k(X)-\int_1^X tf_k'(t)\,\dd t
\right)
+
r(X)f_k(X)-\int_1^X r(t)f_k'(t)\,\dd t.
\]
由恒等式
\[
\frac{\dd}{\dd t}\bigl(tf_k(t)\bigr)=f_k(t)+tf_k'(t)
\]
可得
\[
Xf_k(X)-\int_1^X tf_k'(t)\,\dd t
=
\int_1^X f_k(t)\,\dd t+f_k(1),
\]
故
\[
\delta_{\mathrm{ZG}}
\left(
Xf_k(X)-\int_1^X tf_k'(t)\,\dd t
\right)
=
\delta_{\mathrm{ZG}}\int_1^X \frac{(\log t)^k}{t}\,\dd t+O(1)
=
\frac{\delta_{\mathrm{ZG}}}{k+1}(\log X)^{k+1}+O(1).
\]

另一方面，
\[
r(X)f_k(X)=o\!\bigl((\log X)^k\bigr)
=
o\!\bigl((\log X)^{k+1}\bigr).
\]
又因
\[
f_k'(t)=\frac{k(\log t)^{k-1}-(\log t)^k}{t^2},
\]
故有估计
\[
|f_k'(t)|\ll_k \frac{(\log t)^k}{t^2}.
\]
任取 \(\varepsilon>0\)，取 \(T_0\) 足够大，使 \(|r(t)|\le \varepsilon t\) 对 \(t\ge T_0\) 成立，则
\[
\left|\int_{T_0}^X r(t)f_k'(t)\,\dd t\right|
\ll_k
\varepsilon\int_{T_0}^X \frac{(\log t)^k}{t}\,\dd t
\ll_k
\varepsilon(\log X)^{k+1},
\]
而有限区间 \([1,T_0]\) 上的积分只是常数。令 \(\varepsilon\downarrow 0\)，得到
\[
\int_1^X r(t)f_k'(t)\,\dd t
=
o\!\bigl((\log X)^{k+1}\bigr).
\]
综上，
\[
\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}\frac{(\log n)^k}{n}
=
\frac{\delta_{\mathrm{ZG}}}{k+1}(\log X)^{k+1}
+o\!\bigl((\log X)^{k+1}\bigr).
\]
当 \(k=0\) 时便恢复定理 \ref{thm:xi-zg-abel-residue-log-density} 的调和主项。证毕。
\end{proof}

\begin{theorem}[ZG 的调和 Ces\`aro 主项与 Tauberian 三重同常数]\label{thm:conclusion-zg-harmonic-tauberian-triple-constant}
沿用定理 \ref{thm:conclusion-zg-dirichlet-simple-pole-density} 的记号，并记
\[
H_{\mathrm{ZG}}(X):=\sum_{\substack{n\le X\\ n\in \mathrm{ZG}}}\frac1n.
\]
则同一常数 \(\delta_{\mathrm{ZG}}\) 同时满足
\[
A_{\mathrm{ZG}}(X)=\delta_{\mathrm{ZG}}X+o(X),
\]
\[
H_{\mathrm{ZG}}(X)=\delta_{\mathrm{ZG}}\log X+o(\log X),
\]
\[
\lim_{\sigma\downarrow 1}\frac{D_{\mathrm{ZG}}(\sigma)}{\zeta(\sigma)}=\delta_{\mathrm{ZG}}.
\]
特别地，\(\delta_{\mathrm{ZG}}\) 不仅是 \(\mathrm{ZG}\) 的自然密度，也是其调和平均主项与 Dirichlet 边界 residue 的统一常数；并且由 prime-register 的矩阵 Euler 实现，这同一个常数亦正是相应 hard-core transfer 动力学的临界常数。
\leanverified{paper\_conclusion\_zg\_harmonic\_tauberian\_triple\_constant}
\end{theorem}

\begin{proof}
第一式正是定理 \ref{thm:xi-zg-abel-residue-log-density} 的第一条。第二式是推论 \ref{cor:conclusion-zg-weighted-mertens-cesaro} 在 \(k=0\) 时的特例。第三式则由推论 \ref{cor:xi-time-part62a-zg-zeta-normalized-density-limit} 直接给出；该推论同时指出，这个极限常数也等于 prime-register 矩阵 Euler 动力学所生成的临界常数。证毕。
\end{proof}

\begin{theorem}[全息事件像与单侧满移位的严格共轭]\label{thm:conclusion-hologram-fullshift-topological-conjugacy}
\leanverified{paper\_conclusion\_hologram\_fullshift\_topological\_conjugacy}
设 \(E\) 为有限事件集，取单射
\[
\mathrm{code}:E\hookrightarrow \{0,\dots,M\}\subset \ZZ,
\qquad
B:=2^L>M.
\]
沿用定理 \ref{thm:xi-time-part62deb-hologram-affine-fullshift-conjugacy} 的地址映射
\[
X:E^{\NN}\longrightarrow T_2(E_{\min}),
\qquad
X((e_t)_{t\ge 1})
:=
\sum_{t\ge 1}\mathrm{code}(e_t)B^{t-1}v,
\]
并记其像为
\[
\mathcal X:=X(E^{\NN})\subset T_2(E_{\min}),
\qquad
\sigma(e_1,e_2,\dots):=(e_2,e_3,\dots).
\]
则 \(X\) 是从 \(E^{\NN}\) 到 \(\mathcal X\) 的拓扑同胚，从而存在唯一连续映射
\[
\Sigma_{\mathcal X}:\mathcal X\to \mathcal X
\]
使
\[
\boxed{\;
X\circ \sigma=\Sigma_{\mathcal X}\circ X.\;}
\]
特别地，
\[
\boxed{\;
h_{\mathrm{top}}(\Sigma_{\mathcal X})=\log|E|,\qquad
\#\operatorname{Fix}(\Sigma_{\mathcal X}^n)=|E|^n\quad(n\ge 1),\;}
\]
并且 Artin--Mazur 动力学 \(\zeta\) 函数满足
\[
\boxed{\;
\zeta_{\Sigma_{\mathcal X}}(z)=\frac{1}{1-|E|z}.\;}
\]
\end{theorem}
\begin{proof}
定理 \ref{thm:xi-time-part62deb-hologram-affine-fullshift-conjugacy} 已证明：\(\mathcal X\) 上存在连续映射 \(\mathcal T\) 使
\[
\mathcal T\circ X=X\circ \sigma,
\]
且 \(X:E^{\NN}\to \mathcal X\) 为拓扑同胚。于是唯一可取
\[
\Sigma_{\mathcal X}:=\mathcal T=X\circ \sigma\circ X^{-1},
\]
从而得到严格共轭关系。

再由推论 \ref{cor:xi-time-part62deb-hologram-fullshift-entropy-primitive-zeta}，
\[
h_{\mathrm{top}}(\mathcal T)=\log|E|,
\qquad
\#\operatorname{Fix}(\mathcal T^n)=|E|^n,
\qquad
\zeta_{\mathcal T}(z)=\frac{1}{1-|E|z}.
\]
将 \(\mathcal T\) 改记为 \(\Sigma_{\mathcal X}\) 即得结论。证毕。
\end{proof}

\begin{corollary}[全息事件系统的 primitive 周期轨道公式]\label{cor:conclusion-hologram-fullshift-primitive-orbit-prime-law}
沿用定理 \ref{thm:conclusion-hologram-fullshift-topological-conjugacy} 的记号。记 \(\pi_{\mathcal X}(n)\) 为 \(\Sigma_{\mathcal X}\) 的 primitive \(n\)-周期轨道条数。则
\[
\boxed{\;
\pi_{\mathcal X}(n)
=
\frac1n\sum_{d\mid n}\mu(d)\,|E|^{n/d}.\;}
\]
特别地，
\[
\boxed{\;
\pi_{\mathcal X}(n)
=
\frac{|E|^n}{n}
+O\!\left(\frac{|E|^{n/2}}{n}\right),\;}
\]
从而
\[
\boxed{\;
\lim_{n\to\infty}\frac1n\log\bigl(n\,\pi_{\mathcal X}(n)\bigr)=\log|E|.\;}
\]
\end{corollary}
\begin{proof}
由定理 \ref{thm:conclusion-hologram-fullshift-topological-conjugacy}，
\[
\#\operatorname{Fix}(\Sigma_{\mathcal X}^n)=|E|^n.
\]
另一方面，固定点数与 primitive 周期轨道数满足标准关系
\[
\#\operatorname{Fix}(\Sigma_{\mathcal X}^n)
=
\sum_{d\mid n} d\,\pi_{\mathcal X}(d).
\]
对该卷积作 Möbius 反演，即得
\[
\pi_{\mathcal X}(n)=\frac1n\sum_{d\mid n}\mu(d)\,|E|^{n/d}.
\]
主项对应 \(d=1\)。若 \(d>1\)，则 \(n/d\le n/2\)，故其余项总和满足
\[
\frac1n\sum_{\substack{d\mid n\\ d>1}}|E|^{n/d}
=
O\!\left(\frac{|E|^{n/2}}{n}\right).
\]
于是
\[
\pi_{\mathcal X}(n)
=
\frac{|E|^n}{n}
+O\!\left(\frac{|E|^{n/2}}{n}\right).
\]
最后，
\[
n\,\pi_{\mathcal X}(n)=|E|^n\bigl(1+O(|E|^{-n/2})\bigr),
\]
取对数并除以 \(n\) 即得
\[
\lim_{n\to\infty}\frac1n\log\bigl(n\,\pi_{\mathcal X}(n)\bigr)=\log|E|.
\]
证毕。
\end{proof}

\begin{theorem}[全息地址系统中的轨道谱--度量维数完全解耦]\label{thm:conclusion-hologram-orbit-spectrum-metric-dimension-decoupling}
沿用定理 \ref{thm:conclusion-hologram-fullshift-topological-conjugacy} 的记号。对任意 Bernoulli 概率向量
\[
\mathbf p=(p_a)_{a\in E}
\]
记相应地址推前测度为
\[
\mu_{\mathbf p}:=X_*\Bigl(\bigotimes_{n\ge 1}\sum_{a\in E}p_a\delta_a\Bigr).
\]
则在固定 \((E,B,v)\) 后有如下严格分裂：
\begin{enumerate}
  \item 全部周期轨道数据只由字母表基数 \(|E|\) 决定，即
        \[
        \#\operatorname{Fix}(\Sigma_{\mathcal X}^n)=|E|^n,
        \qquad
        \pi_{\mathcal X}(n)=\frac1n\sum_{d\mid n}\mu(d)\,|E|^{n/d},
        \qquad
        \zeta_{\Sigma_{\mathcal X}}(z)=\frac{1}{1-|E|z};
        \]
  \item 与之正交地，全部度量维数数据只由源熵 \(H(\mathbf p)\) 决定，即
        \[
        \dim_{\mathrm H}(\mu_{\mathbf p})=\frac{H(\mathbf p)}{\log B}.
        \]
\end{enumerate}
因此，只要 \(|E|\ge 2\)，同一 faithful 地址动力系统上便存在一族连续参数的 Bernoulli 不变测度：它们共享完全相同的 Artin--Mazur \(\zeta\) 函数、全部周期点数与 primitive 轨道计数，但其 Hausdorff 维数沿一条非平凡连续曲线变化；取其中熵值互异的子族，则对应测度两两互相奇异。
\end{theorem}

\begin{proof}
定理 \ref{thm:conclusion-hologram-fullshift-topological-conjugacy} 已给出
\[
\#\operatorname{Fix}(\Sigma_{\mathcal X}^n)=|E|^n,
\qquad
\zeta_{\Sigma_{\mathcal X}}(z)=\frac{1}{1-|E|z},
\]
而推论 \ref{cor:conclusion-hologram-fullshift-primitive-orbit-prime-law} 已给出 primitive 周期轨道的 Möbius 反演公式。故轨道谱侧完全只依赖 \(|E|\)。

另一方面，定理 \ref{thm:xi-time-part62dc-hologram-bernoulli-exact-dimensional} 直接给出
\[
\dim_{\mathrm H}(\mu_{\mathbf p})=\frac{H(\mathbf p)}{\log B}.
\]
故度量维数侧完全只依赖 \(H(\mathbf p)\)。

若 \(|E|\ge 2\)，取一条使 \(H(\mathbf p_t)\) 严格变化的 Bernoulli 参数曲线 \(t\mapsto \mathbf p_t\)。则 \(\dim_{\mathrm H}(\mu_{\mathbf p_t})\) 随 \(t\) 严格变化，而轨道谱数据保持不变。若 \(s\neq t\) 且
\[
\dim_{\mathrm H}(\mu_{\mathbf p_s})<\dim_{\mathrm H}(\mu_{\mathbf p_t}),
\]
则由 \(\mu_{\mathbf p_s}\) 的 exact-dimensional 性，可取 Borel 集 \(A_s\subseteq K_{E,v}\) 使
\[
\mu_{\mathbf p_s}(A_s)=1,
\qquad
\dim_{\mathrm H}(A_s)=\dim_{\mathrm H}(\mu_{\mathbf p_s}).
\]
但 exact-dimensional 测度 \(\mu_{\mathbf p_t}\) 的几乎处处局部维数严格大于 \(\dim_{\mathrm H}(A_s)\)，故
\[
\mu_{\mathbf p_t}(A_s)=0.
\]
于是 \(\mu_{\mathbf p_s}\perp \mu_{\mathbf p_t}\)。证毕。
\end{proof}

\begin{theorem}[faithful 单流地址测度与 Lee--Yang Haar 边界的维数隔离]\label{thm:conclusion-hologram-leyang-haar-dimension-separation}
设
\[
K_{E,v}:=X(E^{\NN})\subset T_2(E_{\min}),
\]
其中 \(B=2^L\) 且 \(B>\max_{a\in E}\mathrm{code}(a)\)。对任意 Bernoulli 概率向量 \(\mathbf p\) 于 \(E\) 上，记其地址推前为 \(\mu_{\mathbf p}\)。则：
\begin{enumerate}
  \item \(\mu_{\mathbf p}\) 为 exact-dimensional，且
        \[
        \dim_{\mathrm H}(\mu_{\mathbf p})
        =
        \frac{H(\mathbf p)}{\log B}
        \le
        \frac{\log|E|}{\log B}
        \le 1;
        \]
  \item 在定理 \ref{thm:conclusion-leyang-five-branch-haar-boundary-limit} 的每个 Lee--Yang sheet 上，Haar 分量满足 dyadic cylinder 质量律
        \[
        m_{\mathrm{Haar}}\bigl(U(a,b;k)\bigr)=4^{-k},
        \]
        因而其局部维数恰为 \(2\)。
\end{enumerate}
从而在任意固定 sheet 的识别下，
\[
\mu_{\mathbf p}\perp m_{\mathrm{Haar}},
\]
并因此与
\[
\frac15\sum_{i=0}^{4}\delta_i\otimes m_{\mathrm{Haar}}
\]
的任一 sheet 分量都互相奇异。换言之，faithful 单流地址系统在对象层可以完全编码事件流，但在测度层却始终与 Lee--Yang 的二维 Haar 边界保持严格分离。
\end{theorem}

\begin{proof}
第一条由定理 \ref{thm:xi-time-part62dc-hologram-bernoulli-exact-dimensional} 直接给出。又因 \(\mathrm{code}:E\hookrightarrow\{0,\dots,M\}\) 为单射且 \(M<B\)，故
\[
|E|\le M+1\le B,
\]
从而
\[
\dim_{\mathrm H}(\mu_{\mathbf p})
\le
\frac{\log|E|}{\log B}
\le 1.
\]

另一方面，定理 \ref{thm:conclusion-leyang-five-branch-haar-boundary-limit} 已表明每个 Lee--Yang sheet 上的 Haar 分量对深度 \(k\) 的 dyadic cylinder 恰赋质量 \(4^{-k}\)，故其局部维数为
\[
\lim_{k\to\infty}\frac{\log 4^{-k}}{\log 2^{-k}}=2.
\]

最后，由地址级数定义可见
\[
K_{E,v}\subseteq \ZZ_2 v,
\]
因而 \(K_{E,v}\subseteq K_{B,M}(v)\)。定理 \ref{thm:xi-time-part9gc-haar-cantor-splitting} 已证明：任何支撑在 rank-one 地址 Cantor 子系统上的概率测度都与 full-rank Haar 测度互相奇异。对每个固定 sheet 识别 \( \mathbf P_i+T\cong T\cong \ZZ_2^2\) 后，遂得
\[
\mu_{\mathbf p}\perp m_{\mathrm{Haar}}.
\]
这正实现了上面的 \(1\)-维 faithful 地址测度与 \(2\)-维 Lee--Yang Haar 边界之间的结构分离。证毕。
\end{proof}

\begin{corollary}[faithful 单流地址系统不能实现 Lee--Yang sheet 的 Haar 边界平衡]\label{cor:conclusion-hologram-leyang-haar-balance-impossible}
沿用定理 \ref{thm:conclusion-hologram-leyang-haar-dimension-separation} 的记号。则地址像本身满足
\[
\dim_{\mathrm H}(K_{E,v})=\frac{\log|E|}{\log B}\le 1.
\]
因此，不存在任何支撑于同一 \(K_{E,v}\) 的概率测度能够等于某个 Lee--Yang sheet 上的 Haar 测度。特别地，固定一个 faithful 单流全息地址系统后，任意支撑在 \(K_{E,v}\) 上的概率测度列都不可能弱收敛到
\[
m_{\mathrm{Haar}}
\]
或
\[
\frac15\sum_{i=0}^{4}\delta_i\otimes m_{\mathrm{Haar}}
\]
中的任一 sheet 分量。
\end{corollary}

\begin{proof}
由定理 \ref{thm:xi-time-part62dc-hologram-bernoulli-exact-dimensional} 的均匀分布特例，
\[
\dim_{\mathrm H}(K_{E,v})=\frac{\log|E|}{\log B}\le 1.
\]
因此 \(K_{E,v}\) 是 \(T_2(E_{\min})\) 中的一个低维紧集。由定理 \ref{thm:conclusion-hologram-leyang-haar-dimension-separation}，任一支撑在 \(K_{E,v}\) 上的概率测度都与任一 Lee--Yang sheet 的 Haar 分量互相奇异，故当然不可能等于该 Haar 测度。

若 \(\mu_j\Rightarrow \mu\) 且 \(\operatorname{supp}(\mu_j)\subseteq K_{E,v}\) 对所有 \(j\) 成立，则因 \(K_{E,v}\) 为闭集，仍有
\[
\operatorname{supp}(\mu)\subseteq K_{E,v}.
\]
于是 \(\mu\) 不可能成为任何 sheet Haar 分量。故所述弱收敛同样不可能发生。证毕。
\end{proof}

\begin{theorem}[Lee--Yang 五有限分歧族的 Haar 边界极限]\label{thm:conclusion-leyang-five-branch-haar-boundary-limit}
设
\[
T:=T_2(E_{\min})\cong \varprojlim_n E_{\min}[2^n]\cong \ZZ_2^2.
\]
对 \(i\in\{0,\dots,4\}\) 与 \(n\ge 0\)，沿用推论 \ref{cor:xi-terminal-zm-leyang-branchpoint-tate-torsor-decomposition} 的记号
\[
\Pi_{i,n}:=[2^n]^{-1}(P_i),
\qquad
\Pi_{i,\infty}:=\varprojlim_n(\Pi_{i,n},[2]).
\]
固定每个分支分量中的一条相容提升
\[
\mathbf P_i\in \Pi_{i,\infty},
\]
并通过
\[
\Pi_{i,\infty}\cong \mathbf P_i+T
\]
把第 \(i\) 个分支 sheet 识别为 \(T\) 的一个仿射平移。再固定一个坐标识别 \(T\cong \ZZ_2^2\)，并以标准代表嵌入
\[
\iota_n:(\ZZ/2^n\ZZ)^2\hookrightarrow \ZZ_2^2.
\]
定义离散并空间
\[
\mathfrak R:=\{0,\dots,4\}\times T,
\]
以及支撑在深度 \(n\) 五分支有限层上的均匀原子测度
\[
\nu_n:=
\frac{1}{5\cdot 4^n}
\sum_{i=0}^{4}\ \sum_{u\in(\ZZ/2^n\ZZ)^2}
\delta_{(i,\ \mathbf P_i+\iota_n(u))}.
\]
则在弱收敛意义下，
\[
\boxed{\;
\nu_n\Longrightarrow
\frac15\sum_{i=0}^{4}\delta_i\otimes m_{\mathrm{Haar}}
\qquad(n\to\infty),\;}
\]
其中 \(m_{\mathrm{Haar}}\) 为 \(T\cong \ZZ_2^2\) 上的 Haar 概率测度。等价地，对任意 \(i\in\{0,\dots,4\}\) 与任意 dyadic 圆柱
\[
U(a,b;k):=
\{(x,y)\in \ZZ_2^2:\ x\equiv a,\ y\equiv b\pmod{2^k}\},
\]
都有
\[
\boxed{\;
\lim_{n\to\infty}\nu_n\bigl(\{i\}\times U(a,b;k)\bigr)=\frac{1}{5\cdot 4^k}.\;}
\]
\end{theorem}
\begin{proof}
由推论 \ref{cor:xi-terminal-zm-leyang-ramification-pullback-2n}，每个有限分歧族 \(\Pi_{i,n}\) 都是 \(E_{\min}[2^n]\) 的平移余类，因而
\[
\#\Pi_{i,n}=4^n,
\qquad
\sum_{i=0}^{4}\#\Pi_{i,n}=5\cdot 4^n.
\]
这正对应于 \(\nu_n\) 中每个原子权重为 \((5\cdot 4^n)^{-1}\)。

现在固定 \(i\) 与一个深度 \(k\) 的 dyadic 圆柱 \(U(a,b;k)\)。当 \(n\ge k\) 时，在标准代表集
\[
\iota_n\bigl((\ZZ/2^n\ZZ)^2\bigr)\subset \ZZ_2^2
\]
中，落入圆柱 \(U(a,b;k)\) 的点恰有
\[
4^{n-k}
\]
个，因为前 \(k\) 位二进制坐标被固定，而后 \(n-k\) 层每层仍各有 \(4\) 种选择。于是
\[
\nu_n\bigl(\{i\}\times U(a,b;k)\bigr)
=
\frac{4^{n-k}}{5\cdot 4^n}
=
\frac{1}{5\cdot 4^k}.
\]
该值对一切 \(n\ge k\) 都恒定，因此极限存在并等于 \(1/(5\cdot4^k)\)。

另一方面，由定理 \ref{thm:xi-time-part9gc-resolution-tower-haar-limit}，
\[
m_{\mathrm{Haar}}(U(a,b;k))=4^{-k}.
\]
故
\[
\left(\frac15\sum_{i=0}^{4}\delta_i\otimes m_{\mathrm{Haar}}\right)
\bigl(\{i\}\times U(a,b;k)\bigr)
=
\frac15\cdot 4^{-k}
=
\frac{1}{5\cdot 4^k}.
\]
而矩形集
\[
\{i\}\times U(a,b;k)
\]
生成 \(\mathfrak R\) 的 Borel \(\sigma\)-代数，故上述 cylinder 质量公式唯一确定弱极限。于是
\[
\nu_n\Longrightarrow \frac15\sum_{i=0}^{4}\delta_i\otimes m_{\mathrm{Haar}}.
\]
证毕。
\end{proof}

\input{thm__conclusion-natural-extension-rightshift-address-filtration}

\noindent
由此，\(\mathrm{ZG}\) 的算术统计已经在 Tauberian 意义下完全闭合：自然密度、调和 Ces\`aro 主项、\(\zeta\)-归一化 Dirichlet residue 以及 prime-register matrix Euler 的临界常数，都被同一个 \(\delta_{\mathrm{ZG}}\) 精确锁定。与此同时，全息事件像并不只是静态地址编码，而是与单侧满移位严格共轭的 \(2^L\)-进动力系统；其周期点数、primitive 轨道与 Artin--Mazur \(\zeta\) 函数只依赖字母表基数，而 Bernoulli 推前测度的 Hausdorff 维数则只依赖源熵，二者在同一地址系统上发生严格解耦。另一方面，Lee--Yang 五有限分歧族的 dyadic 拉回塔在每个分支 sheet 上都以 \(4^{-k}\) 的精确 cylinder 频率趋向 full-rank Haar 几何；而 faithful 单流地址测度始终困在维数至多为 \(1\) 的 rank-one Cantor 子系统内，因而与任一 sheet 的二维 Haar 边界绝对奇异。于是，Tauberian 常数、符号轨道谱与 \(2\)-进边界测度三条原本分散的链，在结论层被压成一组既显式又彼此正交的终端律。

\endinput
