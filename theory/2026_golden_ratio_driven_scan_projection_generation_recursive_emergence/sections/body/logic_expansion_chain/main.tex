\documentclass[../../../main.tex]{subfiles}

\begin{document}
\input{sec__logic-expansion-chain}
\end{document}

% --- Backflow from publication papers (auto-generated) ---
\begin{remark}[Results proven in dedicated publications]
  The following results from this section have been developed into
  standalone publications with full proofs and independent verification:
  \begin{itemize}
    \item \textbf{Complexity upper bounds} (Theorem \texttt{thm:axis-complexity-upper-bounds}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Branch-sensitive visible quotient} (Theorem \texttt{thm:branch-sensitive-visible-quotient}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Branch-uniform visible quotient} (Theorem \texttt{thm:branch-uniform-visible-quotient}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Exact comparison of branch-sensitive and branch-uniform visibility} (Theorem \texttt{thm:branch-comparison-exact-sequence}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Criterion for the absence of branch gain} (Corollary \texttt{cor:no-branch-gain-criterion}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Dual character description of the branch-resolution kernel} (Corollary \texttt{cor:branch-resolution-dual}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Exact branch-resolution budget} (Theorem \texttt{thm:branch-resolution-budget}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Factorization of uniform hidden information} (Corollary \texttt{cor:uniform-hidden-budget-factorization}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Multi-branch strictification budget} (Theorem \texttt{thm:multi-branch-strictification-budget}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Single-branch reduction} (Corollary \texttt{cor:single-branch-reduction}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Compatibility translation for contextuality data} (Proposition \texttt{prop:ab-compatibility-translation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Unique-branch comparison with the no-global-section picture} (Theorem \texttt{thm:unique-branch-contextuality-comparison}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Component gerbes of visible branches} (Theorem \texttt{thm:component-gerbe-decomposition}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Global branch decomposition under branch constancy} (Corollary \texttt{cor:branch-decomposition}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Constructing the Cech obstruction of a branch gerbe} (Theorem \texttt{thm:cech-bridge-compatible-realizations}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Compatible local sections versus visible branches} (Proposition \texttt{prop:compsecs-vs-visible-branches}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Global sections versus neutral component gerbes} (Proposition \texttt{prop:global-sections-vs-neutral-components}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Branched gerbe semantics of gluing-level absence} (Theorem \texttt{thm:gerbe-null-semantics}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Initial visible quotient of a gluing obstruction} (Theorem \texttt{thm:initial-visible-quotient}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Definability barrier for character channels} (Theorem \texttt{thm:character-definability-barrier}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{The visible quotient is exactly the character-visible quotient} (Corollary \texttt{cor:visible-quotient-character-separation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Minimal information cost of strictification} (Theorem \texttt{thm:minimal-information-cost-of-strictification}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Conservative recovery requires hidden-state budget} (Corollary \texttt{cor:conservative-recovery-budget}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Finite nerve comparison isomorphism} (Proposition \texttt{prop:finite-nerve-comparison}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Strict versus intrinsic hidden subgroups} (Proposition \texttt{prop:strict-vs-intrinsic-hidden-subgroups}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character-theoretic detection of the intrinsic visible quotient} (Theorem \texttt{thm:intrinsic-visible-quotient}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character detection of the intrinsic visible quotient} (Corollary \texttt{cor:intrinsic-character-detection}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Initiality among pure-extension quotients} (Theorem \texttt{thm:intrinsic-pure-ext-initiality}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Class-trivializing initiality when $H_1$ vanishes} (Corollary \texttt{cor:h1-vanishing-class-trivializing}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Comparison with strict visible quotients} (Corollary \texttt{cor:strict-to-intrinsic-visible}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Strict visibility versus homological visibility} (Proposition \texttt{prop:chain-vs-homological-visibility}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Criterion for coincidence of strict and intrinsic visibility} (Corollary \texttt{cor:strict-intrinsic-coincidence-criterion}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character-blind pure-extension obstructions} (Theorem \texttt{thm:character-blind-obstructions}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Automatic blindness when $H_2$ vanishes} (Corollary \texttt{cor:h2-vanishing-blindness}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No character-blind obstructions when $H_1$ is free} (Corollary \texttt{cor:h1-free-no-blindness}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Intrinsic hidden-state lower bound} (Corollary \texttt{cor:intrinsic-hidden-state-lower-bound}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Prime decomposition of intrinsic visibility and budget} (Theorem \texttt{thm:intrinsic-prime-decomposition}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Intrinsic budget versus strict budget} (Corollary \texttt{cor:strict-intrinsic-budget-comparison}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Maximal collapse criterion} (Corollary \texttt{cor:maximal-collapse}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Persistence of forcing} (Proposition \texttt{prop:forcing-persistence}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Monotone growth of state theories} (Corollary \texttt{cor:state-theory-monotonicity}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Singleton conservativity} (Proposition \texttt{prop:singleton-conservativity}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Lifted soundness of pointwise reasoning} (Proposition \texttt{prop:lifted-pointwise-soundness}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Updates preserve forcing} (Proposition \texttt{prop:updates-preserve-forcing}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Delayed decidability does not create truth} (Proposition \texttt{prop:delayed-no-new-truth}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Delayed classification is non-retrocausal} (Proposition \texttt{prop:nonretrocausal-delayed-classification}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Indispensability implies minimality} (Proposition \texttt{prop:indispensability-implies-minimality}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Explicit lifting principle} (Corollary \texttt{cor:explicit-lifting}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement monotonicity of branch visibility} (Theorem \texttt{thm:branch-visibility-monotonicity}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement decreases hidden branch budget} (Corollary \texttt{cor:branch-budget-monotonicity}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Value-preserving rewrites do not create facts} (Proposition \texttt{prop:value-preserving-no-creation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Normalization does not create facts} (Corollary \texttt{cor:normalization-no-fact-creation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Global sections yield compatible local sections} (Proposition \texttt{prop:global-implies-compatible-local}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheafification characterizes compatible local sectionability} (Theorem \texttt{thm:sheafification-characterization}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheaf condition at a reference} (Corollary \texttt{cor:sheaf-local-to-global}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheafification removes gluing failure} (Corollary \texttt{cor:sheafification-removes-glue}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Canonical split realization prestack} (Proposition \texttt{prop:split-realization-prestack}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sufficient criterion for gluing-sensitive realization} (Theorem \texttt{thm:gluing-sensitive-realization}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Components of the stackification} (Proposition \texttt{prop:stackification-components}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No new global objects at $a$} (Proposition \texttt{prop:no-new-global-objects}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Visible value classes as global connected components} (Theorem \texttt{thm:visible-value-components}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Address before value} (Proposition \texttt{prop:address-before-value}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Non-null readout criterion} (Corollary \texttt{cor:nonnull-readout-criterion}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement stability of defined readouts} (Proposition \texttt{prop:typed-readout-persistence}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{The three modes lie inside null} (Proposition \texttt{prop:null-modes-imply-null}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Mutual exclusivity of the null modes} (Proposition \texttt{prop:null-modes-exclusive}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Null trichotomy} (Proposition \texttt{prop:null-trichotomy}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Classification after refinement} (Theorem \texttt{thm:null-classification-under-refinement}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{A finite model for the three absence modes} (Proposition \texttt{prop:finite-three-mode-model}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Pointwise irreducibility of gluing predicates} (Theorem \texttt{thm:pointwise-irreducibility}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Composition of conservative extensions} (Proposition \texttt{prop:conservative-composition}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Finite-chain invariance} (Corollary \texttt{cor:whole-chain-invariance}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Concrete instantiation of the finite chain} (Proposition \texttt{prop:chain-instantiation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Lower-layer projection} (Proposition \texttt{prop:lower-layer-projection}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Semantic fidelity} (Proposition \texttt{prop:semantic-fidelity}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Transport of typed readouts} (Proposition \texttt{prop:readout-transport}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Concrete normalization does not create facts} (Corollary \texttt{cor:concrete-normalization-no-fact-creation}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Regional forcing persists under local refinement} (Proposition \texttt{prop:regional-forcing-persistence}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Persistence of shared forcing} (Proposition \texttt{prop:shared-forcing-persistence}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No privileged observer} (Proposition \texttt{prop:no-privileged-observer}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Observer-neutral transport of readout status} (Corollary \texttt{cor:observer-neutral-readout}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Conservativity of the observer-spacetime layer} (Proposition \texttt{prop:ost-conservative}) --- Annals of Pure and Applied Logic paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Complexity upper bounds} (Theorem \texttt{thm:axis-complexity-upper-bounds}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Lower-layer projection} (Proposition \texttt{prop:lower-layer-projection}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Semantic fidelity} (Proposition \texttt{prop:semantic-fidelity}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Transport of typed readouts} (Proposition \texttt{prop:readout-transport}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Concrete normalization does not create facts} (Corollary \texttt{cor:concrete-normalization-no-fact-creation}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Component gerbes of visible branches} (Theorem \texttt{thm:component-gerbe-decomposition}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Global branch decomposition under branch constancy} (Corollary \texttt{cor:branch-decomposition}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Constructing the Cech obstruction of a branch gerbe} (Theorem \texttt{thm:cech-bridge-compatible-realizations}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Branched gerbe semantics of gluing-level absence} (Theorem \texttt{thm:gerbe-null-semantics}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character-theoretic detection of the intrinsic visible quotient} (Theorem \texttt{thm:intrinsic-visible-quotient}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character detection of the intrinsic visible quotient} (Corollary \texttt{cor:intrinsic-character-detection}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Initiality among pure-extension quotients} (Theorem \texttt{thm:intrinsic-pure-ext-initiality}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Class-trivializing initiality when $H_1$ vanishes} (Corollary \texttt{cor:h1-vanishing-class-trivializing}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Character-blind pure-extension obstructions} (Theorem \texttt{thm:character-blind-obstructions}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Automatic blindness when $H_2$ vanishes} (Corollary \texttt{cor:h2-vanishing-blindness}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No character-blind obstructions when $H_1$ is free} (Corollary \texttt{cor:h1-free-no-blindness}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Unique-branch comparison with the no-global-section picture} (Theorem \texttt{thm:unique-branch-contextuality-comparison}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Flat team-style reading of lower-layer forcing} (Proposition \texttt{prop:flat-team-reading}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Persistence of forcing} (Proposition \texttt{prop:forcing-persistence}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Monotone growth of state theories} (Corollary \texttt{cor:state-theory-monotonicity}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Singleton conservativity} (Proposition \texttt{prop:singleton-conservativity}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Lifted soundness of pointwise reasoning} (Proposition \texttt{prop:lifted-pointwise-soundness}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Updates preserve forcing} (Proposition \texttt{prop:updates-preserve-forcing}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Delayed decidability does not create truth} (Proposition \texttt{prop:delayed-no-new-truth}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Delayed classification is non-retrocausal} (Proposition \texttt{prop:nonretrocausal-delayed-classification}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Indispensability implies minimality} (Proposition \texttt{prop:indispensability-implies-minimality}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Explicit lifting principle} (Corollary \texttt{cor:explicit-lifting}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement monotonicity of branch visibility} (Theorem \texttt{thm:branch-visibility-monotonicity}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement decreases hidden branch budget} (Corollary \texttt{cor:branch-budget-monotonicity}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Value-preserving rewrites do not create facts} (Proposition \texttt{prop:value-preserving-no-creation}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Normalization does not create facts} (Corollary \texttt{cor:normalization-no-fact-creation}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Global sections yield compatible local sections} (Proposition \texttt{prop:global-implies-compatible-local}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheafification characterizes compatible local sectionability} (Theorem \texttt{thm:sheafification-characterization}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheaf condition at a reference} (Corollary \texttt{cor:sheaf-local-to-global}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Sheafification removes gluing failure} (Corollary \texttt{cor:sheafification-removes-glue}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Canonical split realization prestack} (Proposition \texttt{prop:split-realization-prestack}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Components of the stackification} (Proposition \texttt{prop:stackification-components}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No new global objects at $a$} (Proposition \texttt{prop:no-new-global-objects}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Visible value classes as global connected components} (Theorem \texttt{thm:visible-value-components}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Address before value} (Proposition \texttt{prop:address-before-value}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Non-null readout criterion} (Corollary \texttt{cor:nonnull-readout-criterion}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Refinement stability of defined readouts} (Proposition \texttt{prop:typed-readout-persistence}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{The three modes lie inside null} (Proposition \texttt{prop:null-modes-imply-null}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Mutual exclusivity of the null modes} (Proposition \texttt{prop:null-modes-exclusive}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Null trichotomy} (Proposition \texttt{prop:null-trichotomy}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Classification after refinement} (Theorem \texttt{thm:null-classification-under-refinement}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{A finite model for the three absence modes} (Proposition \texttt{prop:finite-three-mode-model}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Pointwise irreducibility of gluing predicates} (Theorem \texttt{thm:pointwise-irreducibility}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Regional forcing persists under local refinement} (Proposition \texttt{prop:regional-forcing-persistence}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Persistence of shared forcing} (Proposition \texttt{prop:shared-forcing-persistence}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{No privileged observer} (Proposition \texttt{prop:no-privileged-observer}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Observer-neutral transport of readout status} (Corollary \texttt{cor:observer-neutral-readout}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Conservativity of the observer-spacetime layer} (Proposition \texttt{prop:ost-conservative}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Composition of conservative extensions} (Proposition \texttt{prop:conservative-composition}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Finite-chain invariance} (Corollary \texttt{cor:whole-chain-invariance}) --- unknown paper \texttt{conservative_extension_chain}, status: ACCEPT.
    \item \textbf{Complexity upper bounds} (Theorem \texttt{thm:axis-complexity-upper-bounds}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Lower-layer projection} (Proposition \texttt{prop:lower-layer-projection}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Semantic fidelity} (Proposition \texttt{prop:semantic-fidelity}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Transport of typed readouts} (Proposition \texttt{prop:readout-transport}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Concrete normalization does not create facts} (Corollary \texttt{cor:concrete-normalization-no-fact-creation}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Component gerbes of visible branches} (Theorem \texttt{thm:component-gerbe-decomposition}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Global branch decomposition under branch constancy} (Corollary \texttt{cor:branch-decomposition}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Constructing the Cech obstruction of a branch gerbe} (Theorem \texttt{thm:cech-bridge-compatible-realizations}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Branched gerbe semantics of gluing-level absence} (Theorem \texttt{thm:gerbe-null-semantics}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Character-theoretic detection of the intrinsic visible quotient} (Theorem \texttt{thm:intrinsic-visible-quotient}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Character detection of the intrinsic visible quotient} (Corollary \texttt{cor:intrinsic-character-detection}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Initiality among pure-extension quotients} (Theorem \texttt{thm:intrinsic-pure-ext-initiality}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Class-trivializing initiality when $H_1$ vanishes} (Corollary \texttt{cor:h1-vanishing-class-trivializing}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Character-blind pure-extension obstructions} (Theorem \texttt{thm:character-blind-obstructions}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Automatic blindness when $H_2$ vanishes} (Corollary \texttt{cor:h2-vanishing-blindness}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{No character-blind obstructions when $H_1$ is free} (Corollary \texttt{cor:h1-free-no-blindness}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Unique-branch comparison with the no-global-section picture} (Theorem \texttt{thm:unique-branch-contextuality-comparison}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Flat team-style reading of lower-layer forcing} (Proposition \texttt{prop:flat-team-reading}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Persistence of forcing} (Proposition \texttt{prop:forcing-persistence}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Monotone growth of state theories} (Corollary \texttt{cor:state-theory-monotonicity}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Singleton conservativity} (Proposition \texttt{prop:singleton-conservativity}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Lifted soundness of pointwise reasoning} (Proposition \texttt{prop:lifted-pointwise-soundness}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Updates preserve forcing} (Proposition \texttt{prop:updates-preserve-forcing}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Delayed decidability does not create truth} (Proposition \texttt{prop:delayed-no-new-truth}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Delayed classification is non-retrocausal} (Proposition \texttt{prop:nonretrocausal-delayed-classification}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Indispensability implies minimality} (Proposition \texttt{prop:indispensability-implies-minimality}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Explicit lifting principle} (Corollary \texttt{cor:explicit-lifting}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Refinement monotonicity of branch visibility} (Theorem \texttt{thm:branch-visibility-monotonicity}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Refinement decreases hidden branch budget} (Corollary \texttt{cor:branch-budget-monotonicity}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Value-preserving rewrites do not create facts} (Proposition \texttt{prop:value-preserving-no-creation}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Normalization does not create facts} (Corollary \texttt{cor:normalization-no-fact-creation}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Global sections yield compatible local sections} (Proposition \texttt{prop:global-implies-compatible-local}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Sheafification characterizes compatible local sectionability} (Theorem \texttt{thm:sheafification-characterization}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Sheaf condition at a reference} (Corollary \texttt{cor:sheaf-local-to-global}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Sheafification removes gluing failure} (Corollary \texttt{cor:sheafification-removes-glue}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Canonical split realization prestack} (Proposition \texttt{prop:split-realization-prestack}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Components of the stackification} (Proposition \texttt{prop:stackification-components}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{No new global objects at $a$} (Proposition \texttt{prop:no-new-global-objects}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Visible value classes as global connected components} (Theorem \texttt{thm:visible-value-components}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Address before value} (Proposition \texttt{prop:address-before-value}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Non-null readout criterion} (Corollary \texttt{cor:nonnull-readout-criterion}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Refinement stability of defined readouts} (Proposition \texttt{prop:typed-readout-persistence}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{The three modes lie inside null} (Proposition \texttt{prop:null-modes-imply-null}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Mutual exclusivity of the null modes} (Proposition \texttt{prop:null-modes-exclusive}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Null trichotomy} (Proposition \texttt{prop:null-trichotomy}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Classification after refinement} (Theorem \texttt{thm:null-classification-under-refinement}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{A finite model for the three absence modes} (Proposition \texttt{prop:finite-three-mode-model}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Pointwise irreducibility of gluing predicates} (Theorem \texttt{thm:pointwise-irreducibility}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Regional forcing persists under local refinement} (Proposition \texttt{prop:regional-forcing-persistence}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Persistence of shared forcing} (Proposition \texttt{prop:shared-forcing-persistence}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{No privileged observer} (Proposition \texttt{prop:no-privileged-observer}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Observer-neutral transport of readout status} (Corollary \texttt{cor:observer-neutral-readout}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Conservativity of the observer-spacetime layer} (Proposition \texttt{prop:ost-conservative}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Composition of conservative extensions} (Proposition \texttt{prop:conservative-composition}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
    \item \textbf{Finite-chain invariance} (Corollary \texttt{cor:whole-chain-invariance}) --- APAL paper \texttt{gluing_failure_visible}, status: ACCEPT.
  \end{itemize}
\end{remark}
% --- End backflow ---
