\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\caption{Executable demo of the value-layer rewriting system (RZ,RNF) for \texttt{PROJ\_NORM} normalization. We show the input word, its normal form, and the number of normalization gates before/after rewriting.}
\label{tab:pom_rewriting_engine_demo}
\begin{tabular}{l l r r}
\toprule
input word & normal form & \#norm (in) & \#norm (nf)\\
\midrule
\texttt{PROJ\_NORM; PROJ\_NORM} & \texttt{PROJ\_NORM} & 2 & 1\\
\texttt{EVOLVE(add); PROJ\_NORM; CLEAN} & \texttt{EVOLVE(add); PROJ\_NORM; CLEAN} & 1 & 1\\
\texttt{PROJ\_NORM; EVOLVE(add); PROJ\_NORM; CLEAN} & \texttt{EVOLVE(add); PROJ\_NORM; CLEAN} & 2 & 1\\
\texttt{EVOLVE(mul); PROJ\_NORM; EVOLVE(add); PROJ\_NORM; CLEAN} & \texttt{EVOLVE(mul); EVOLVE(add); PROJ\_NORM; CLEAN} & 2 & 1\\
\texttt{PROJ\_NORM; EVOLVE(mul); EVOLVE(add); PROJ\_NORM; PROJ\_NORM; CLEAN} & \texttt{EVOLVE(mul); EVOLVE(add); PROJ\_NORM; CLEAN} & 3 & 1\\
\texttt{EVOLVE(trace); PROJ\_NORM; EVOLVE(add); PROJ\_NORM; EVOLVE(mul); PROJ\_NORM} & \texttt{EVOLVE(trace); EVOLVE(add); EVOLVE(mul); PROJ\_NORM} & 3 & 1\\
\bottomrule
\end{tabular}
\end{table}
